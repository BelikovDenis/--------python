 Урок №16. SCSS.

Учебные материалы

На прошлом занятии мы познакомились с CSS-препроцессорами и узнали, чем они полезны при разработке стилей.
Сегодня углубимся в возможности SCSS — синтаксиса, расширяющего стандартный CSS.
Мы научимся структурировать код с помощью вложенности, использовать переменные и примеси для сокращения числа повторов,
а также создавать функции для автоматизации расчётов. Всё это позволит писать стили быстрее,
делать их логичнее и удобнее для поддержки.

Сегодня мы:

научимся задавать и использовать переменные в SCSS;
освоим создание и подключение примесей;
разберёмся с вложенной структурой селекторов;
увидим, как применять функции в SCSS для упрощения расчётов и обработки данных.
Глоссарий к шестнадцатому занятию
SASS (Syntactically Awesome Stylesheets)

SASS — один из самых популярных CSS-препроцессоров, который добавляет переменные, вложенные правила,
примеси и другие возможности, недоступные в стандартном CSS.

LESS (Leaner Style Sheets)

LESS — препроцессор для CSS, который позволяет использовать переменные,
миксины и функции для сокращения повторяющегося кода и упрощения стилей.

SCSS (Sassy CSS)

SCSS — синтаксис SASS, максимально приближенный к стандартному CSS (поэтому более привычный для разработчиков).

Mixin (Примесь)

Mixin — блок повторяемого кода в препроцессорах, его можно вызывать в различных частях файла для сокращения кода.

Пример:

@mixin flex-center {
  display: flex;
  justify-content: center;
  align-items: center;
}
Nesting (Вложенность)

Nesting — возможность структурировать CSS-код так, чтобы он отражал иерархию HTML-элементов.

Пример:

ul {
  li {
    a {
      color: black;
    }
  }
}
Что такое SCSS?
SCSS (Sassy CSS) — это синтаксически улучшенная версия Sass (Syntactically Awesome Stylesheets), препроцессора для CSS.

SCSS позволяет разработчикам писать более структурированные и гибкие стили с использованием возможностей,
недоступных в обычном CSS.

SCSS расширяет возможности CSS, добавляя такие функции, как:

переменные,
вложенные стили,
примеси,
функции.
SCSS — не самостоятельный язык, а расширение CSS. Это означает, что все CSS-коды являются допустимыми SCSS-кодами.

Для использования SCSS необходимо:

Написать код в файле с расширением .scss.
Скомпилировать SCSS в стандартный CSS с помощью специального инструмента (например, через команду sass style.scss style.css).
Подключить скомпилированный файл CSS к HTML-документу.
SCSS-файл компилируется в CSS с помощью компилятора Sass. Это можно сделать через командную строку,
а можно настроить автоматическую компиляцию с использованием таких инструментов, как Gulp или Webpack.

Переменные

Переменные в SCSS позволяют хранить значения, которые часто используются в разных частях стилей CSS.
Мы можем присваивать переменным цвета, шрифты, размеры и другие параметры.

Это помогает избежать дублирования в коде и облегчает внесение изменений в будущем. Например, 
если нужно поменять основной цвет на сайте, достаточно обновить переменную, 
а не искать каждое использование цвета по всему коду.

Пример использования переменных в SCSS:

$primary-color: #3498db; // Определяем переменную для цвета

body {
  background-color: $primary-color; // Используем переменную
}

button {
  color: $primary-color; // И снова используем переменную
}
Переменная в SCSS начинается с символа $, за которым идет её название. После этого указывается значение,
которое будет храниться в переменной.

$primary-color: #3498db; // Пример переменной для цвета
$font-size: 16px; // Пример переменной для размера шрифта
После того как переменная задана, её можно использовать в любом месте стилей, просто сославшись на её имя (начинается с $).

body {
  background-color: $primary-color;
  font-size: $font-size;
}
Рекомендуется использовать понятные и описательные имена для переменных, чтобы было ясно, какое значение они хранят.
 Обычно используется стиль camelCase или kebab-case.

Пример хорошего именования:

$main-color: #ff5733;
$button-padding: 10px;
Пример плохого именования (слишком общие имена):

$color1: #ff5733;
$padding1: 10px;
Функции
В CSS нам доступно множество функций. Например url, rgb или функции, которые используются в фильтрах, 
такие как blur или grayscale. Однако возможности для создания новых функций в стандартном CSS ограничены.

SCSS поддерживает создание собственных функций, которые могут быть полезны для выполнения вычислений,
 преобразования значений или обработки данных.

Например, можно создавать функции для вычисления значений отступов или преобразования цветов.

Пример использования функции:

@function calculate-spacing($value) {
  @return $value * 1.5; // Умножаем значение на 1.5
}

.container {
  margin-top: calculate-spacing(10px); // Применяем функцию для вычисления отступа
}
Функция объявляется с помощью ключевого слова @function, после которого указывается её имя.

В круглых скобках перечисляются параметры, которые она принимает.

Для возврата значения используется ключевое слово @return.

Синтаксис функций:

@function multiply($number, $multiplier) {
  @return $number * $multiplier;
}
В этом образце мы создали функцию multiply, которая принимает на вход параметры $number и $multiplier.

Она возвращает произведение двух параметров.

Кроме того, SCSS предоставляет множество встроенных функций, например для работы с цветами (lighten, darken),
 строками (quote, unquote) и математическими операциями (round, ceil, floor).

Обратите внимание на синтаксис использования функций в SCSS. Например, при использовании lighten и darken,
 которые делают цвета светлее или темнее, следует указывать 2 значения:

Цвет, который вы хотите изменить.
Уровень изменения цвета в процентах.
Рассмотрим пример:

$primary-color: #282828;
$secondary-color: lighten($primary-color, 10%);
Здесь мы записали в переменную primary-color оттенок серого #282828. Это первичный цвет (например,
 основной цвет веб-страницы). Также мы записали в переменную secondary-color оттенок первичного цвета, он на 10 % светлее.

Особенности использования функций в SCSS мы смотрим в документации Sass.
 Ссылку на неё вы найдете в блоке с дополнительными материалами.

Примеси

Примеси (или миксины) в SCSS позволяют создавать блоки, которые могут быть повторно использованы в различных частях кода.

Примеси упрощают работу с повторяющимися стилями. Их удобно применять для стилизации элементов,
 которые должны иметь схожие свойства (например, кнопки или карточки).

Пример использования примесей:

@mixin border-radius($radius) {
  border-radius: $radius;
}

.box {
  @include border-radius(10px); // Включаем примесь для добавления скругления
}
Примеси объявляются с помощью ключевого слова @mixin. Для их подключения используется директива @include.

Примеси можно использовать многократно, а внутри них можно применять условные операторы @if и @else:

@mixin responsive-padding($device) {
  @if $device == 'mobile' {
    padding: 5px;
  } @else if $device == 'tablet' {
    padding: 10px;
  } @else {
    padding: 15px;
  }
}

.container {
  @include responsive-padding('tablet');
}
Вложенность
Одна из основных особенностей SCSS — это возможность использовать вложенные правила.

Вложенность делает код логичным и удобочитаемым, так как структура стилей отражает структуру HTML-элементов.

Вложенные правила помогают избежать дублирования и дают возможность легко управлять
 стилизацией различных элементов на странице.

Пример использования вложенности:

nav {
  background-color: #333;

  ul {
    list-style: none;

    li {
      display: inline-block;

      a {
        color: white;
        text-decoration: none;

        &:hover {
          text-decoration: underline;
        }
      }
    }
  }
}
Структура SCSS повторяет иерархию HTML-элементов. Вложенные селекторы определяют стили для дочерних элементов.

Работая с SCSS, легко столкнуться с ошибками, особенно на начальном этапе. Некоторые из них могут 
привести к некорректной компиляции, другие — усложнить поддержку и читаемость кода. Ниже разберём
 самые распространённые ошибки и покажем, как их избежать, что поможет вам писать более чистый и понятный код.


Шпаргалка к уроку
Типичные ошибки
Тип ошибки 1: Глубокая вложенность селекторов

Глубокая вложенность усложняет чтение и поддержку кода. Это может привести к созданию избыточно
 длинных селекторов в результирующем CSS.

Пример ошибки:

.menu {
  ul {
    li {
      a {
        span {
          color: red;
        }
      }
    }
  }
}
Решение:

Ограничьте вложенность 2–3 уровнями.

.menu {
  ul {
    li a {
      color: red;
    }
  }
}
Тип ошибки 2: Ошибки в использовании переменных

Опечатки в именах переменных или попытка использовать переменную до её объявления.

Пример ошибки:

body {
  background-color: $main-bg;
}

$main-bg: #f5f5f5;
Решение:

Объявляйте переменные перед их использованием.

$main-bg: #f5f5f5;

body {
  background-color: $main-bg;
}
Практикум
Задача 1: Создание карточки галереи товаров продуктового магазина с применением SCSS

Ситуация: вы разрабатываете веб-страницу для продуктового магазина. На странице нужно отобразить 
карточки товаров, каждая из которых будет содержать название, изображение, описание и цену. 
Для упрощения работы с оформлением используйте SCSS.

Задача: создайте стиль карточки товара с помощью SCSS и подключите скомпилированный файл CSS к HTML-странице.

Требования:

Требования к карточке товара:

Карточка должна содержать:

изображение товара,
название товара,
краткое описание,
цену с кнопкой «Купить».
Карточка должна быть адаптивной и красиво выглядеть на разных устройствах.

При наведении на карточку фон должен меняться.

Требования к SCSS:

Используйте переменные для цветов и размеров.
Примените примеси для создания стилей кнопок.
Используйте вложенность для создания иерархии стилей.
Шаги реализации:

Создайте файловую структуру:
project/
├── index.html
├── style.scss
└── style.css
Реализуйте стили карточки в style.scss:
// Переменные
$primary-color: #ff6f61;
$secondary-color: #ffffff;
$border-color: #ddd;
$text-color: #333;

// Примесь для кнопки
@mixin button-style {
  background-color: $primary-color;
  color: $secondary-color;
  border: none;
  padding: 10px 20px;
  border-radius: 5px;
  cursor: pointer;
  text-transform: uppercase;
  font-weight: bold;
  transition: background-color 0.3s ease;

  &:hover {
    background-color: darken($primary-color, 10%);
  }
}

// Стили для карточки
.card {
  border: 1px solid $border-color;
  border-radius: 10px;
  padding: 15px;
  text-align: center;
  background-color: $secondary-color;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: transform 0.3s ease, box-shadow 0.3s ease;

  &:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
  }

  img {
    max-width: 100%;
    border-radius: 10px 10px 0 0;
  }

  .title {
    font-size: 1.5rem;
    color: $text-color;
    margin: 10px 0;
  }

  .description {
    font-size: 1rem;
    color: darken($text-color, 20%);
    margin: 10px 0;
  }

  .price {
    font-size: 1.2rem;
    color: $primary-color;
    margin: 15px 0;
    font-weight: bold;
  }

  .button {
    @include button-style;
  }
}
Скомпилируйте SCSS в CSS:
sass style.scss style.css
Создайте разметку в index.html:
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Галерея товаров</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="gallery">
    <div class="card">
      <img decoding="async" src="https://via.placeholder.com/300x200" alt="Товар">
      <h2 class="title">Апельсины</h2>
      <p class="description">Свежие и сочные апельсины прямо из Испании.</p>
      <p class="price">199 ₽</p>
      <button class="button">Купить</button>
    </div>
  </div>
</body>
</html>
Задача 2: Создание карточек для магазина электротехники с применением SCSS

Ситуация: вы разрабатываете веб-страницу для интернет-магазина электротехники.
 На странице должны быть карточки товаров, содержащие изображение, название, краткое описание,
   цену и кнопку «Добавить в корзину». Для упрощения работы с оформлением используется SCSS.

Задача: создайте карточки товаров электротехники с помощью SCSS и подключите скомпилированный файл CSS к HTML-странице.

Требования:

Требования к карточке товара:

Карточка должна содержать:

изображение товара,
название товара,
краткое описание,
цену,
кнопку «Добавить в корзину».
Карточка должна выглядеть профессионально, иметь чёткие границы и адаптивный дизайн.

При наведении на карточку и кнопку должны срабатывать визуальные эффекты.

Требования к SCSS:

Использовать переменные для основных цветов и шрифтов.
Применить примеси для создания стилей кнопок и теней.
Использовать вложенность для структуризации стилей.
Шаги реализации:

Создайте файловую структуру:
project/
├── index.html
├── style.scss
└── style.css
Реализуйте стили карточки в style.scss:
// Переменные
$primary-color: #007bff;
$secondary-color: #ffffff;
$price-color: #28a745;
$border-color: #ccc;
$text-color: #444;

// Примесь для кнопки
@mixin button-style {
  background-color: $primary-color;
  color: $secondary-color;
  border: none;
  padding: 10px 15px;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
  transition: background-color 0.3s;

  &:hover {
    background-color: darken($primary-color, 10%);
  }
}

// Примесь для теней
@mixin box-shadow {
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: box-shadow 0.3s;

  &:hover {
    box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
  }
}

// Стили карточки
.card {
  border: 1px solid $border-color;
  border-radius: 10px;
  padding: 20px;
  text-align: center;
  background-color: $secondary-color;
  @include box-shadow;

  img {
    max-width: 100%;
    border-radius: 10px 10px 0 0;
  }

  .title {
    font-size: 1.4rem;
    font-weight: bold;
    color: $text-color;
    margin: 10px 0;
  }

  .description {
    font-size: 1rem;
    color: darken($text-color, 20%);
    margin-bottom: 15px;
  }

  .price {
    font-size: 1.2rem;
    font-weight: bold;
    color: $price-color;
    margin-bottom: 20px;
  }

  .button {
    @include button-style;
  }
}
Скомпилируйте SCSS в CSS:
sass style.scss style.css
Создайте разметку в index.html:
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Магазин электротехники</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="gallery">
    <div class="card">
      <img decoding="async" src="https://via.placeholder.com/300x200" alt="Ноутбук">
      <h2 class="title">Ноутбук ASUS</h2>
      <p class="description">Мощный ноутбук для работы и игр.</p>
      <p class="price">59,999 ₽</p>
      <button class="button">Добавить в корзину</button>
    </div>
    <div class="card">
      <img decoding="async" src="https://via.placeholder.com/300x200" alt="Смартфон">
      <h2 class="title">Смартфон Samsung</h2>
      <p class="description">Новейшая модель с лучшей камерой.</p>
      <p class="price">34,999 ₽</p>
      <button class="button">Добавить в корзину</button>
    </div>
  </div>
</body>
</html>
Итоги:
Сегодня мы разобрали основные возможности SCSS:

освоили работу с переменными в SCSS;
научились создавать функции;
узнали, как создавать примеси;
изучили процесс создания вложенных конструкций для структурирования кода.
На следующем занятии мы познакомимся с препроцессором Pug.