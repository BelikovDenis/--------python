Урок №14. Введение в сборщики (bundlers).
Учебные материалы
Современные проекты в веб-разработке состоят из множества файлов: JavaScript-скриптов, CSS-таблиц,
изображений и других ресурсов. Чтобы сайт загружался быстрее и оставался удобным в работе,
эти файлы нужно грамотно организовать и оптимизировать.

Для этого используются сборщики (bundlers) — специальные инструменты, которые объединяют,
сжимают и подготавливают ресурсы проекта к работе в интернете.

Сегодня мы:

разберемся, для чего нужны сборщики в веб-разработке;
научимся подключать сборщики в проект;
познакомимся с популярными сборщиками: Gulp, Webpack и Vite.
Глоссарий к четырнадцатому занятию
Bundler (Сборщик)

Bundler — инструмент, который объединяет и оптимизирует различные документы (например, файлы JavaScript,
                                                                              CSS, изображения) в один или несколько файлов, чтобы улучшить производительность веб-приложений.

Modules (Модули)

Modules — части кода, которые могут быть использованы в других частях программы. Например,
в JavaScript модули помогают разделять код на логические блоки. Это улучшает его структуру и упрощает повторное использование.

Loader (Загрузчик)

Loader — механизм, позволяющий сборщикам (например, Webpack) обрабатывать файлы определённого типа
(например, изображения, шрифты, CSS, TypeScript), преобразуя их в формат, который можно использовать в проекте.

Plugin (Плагин)

Plugin — расширение для сборщика, которое добавляет дополнительные функции, такие как оптимизация кода,
сжатие файлов или генерация HTML-файлов.

Code Splitting (Разделение кода)

Code Splitting — техника, при которой код разбивается на несколько частей, которые загружаются только 

по мере необходимости. Это помогает уменьшить начальную нагрузку на страницу и ускорить загрузку.

Configuration (Конфигурация)

Конфигурация — настройки сборщика, которые определяют, как и какие файлы должны быть обработаны и собраны. 
Конфигурация может включать правила для обработки различных типов файлов, плагины, а также параметры оптимизации.


Когда проект растёт, вместе с ним увеличивается и количество файлов: скрипты, стили, изображения, шрифты. 
Если загружать каждый файл отдельно, сайт становится медленным и неудобным. Чтобы решить эту проблему, в веб-разработке используют сборщики (bundlers).

Сборщик — это инструмент, который объединяет разные типы файлов (JavaScript, CSS, изображения и другие)
в один или несколько оптимизированных файлов, готовых к публикации. Это позволяет:

уменьшить количество HTTP-запросов к серверу,
минимизировать и сжать файлы для ускорения загрузки,
работать с современными технологиями вроде модульного JavaScript (ES6 Modules),
разделять код на части и загружать их только по мере необходимости (code splitting).
Сборщики становятся особенно важны в реальных проектах, где скорость загрузки и удобство
разработки напрямую влияют на успех сайта.

Давайте рассмотрим три популярных сборщика и узнаем, чем они отличаются друг от друга.

Gulp

Gulp — это инструмент для автоматизации задач в веб-разработке. Gulp позволяет упростить сборку
проекта и другие повторяющиеся задачи, требующие автоматизации.

Gulp помогает упростить такие задачи, как:

сжатие CSS и JS,
оптимизация изображений,
компиляция препроцессоров (например, SCSS, с которым мы познакомимся на следующем занятии),
обновление браузера при изменении файлов.
Gulp работает через Node.js, поэтому для работы с ним необходимо установить Node.js: https://nodejs.org/.

Далее необходимо установить Gulp CLI. Для этого откройте терминал и выполните команду:

npm install --global gulp-cli
Чтобы инициализировать проект, необходимо создать папку для проекта, перейти в созданную директорию и выполнить команду:

npm init -y
Это действие создает файл package.json.

Теперь Gulp необходимо установить в наш проект:

npm install gulp --save-dev
Далее создайте файл gulpfile.js в корне проекта. Это файл конфигурации, в который вы сможете
добавлять задачи для автоматизации.

В начале файла gulpfile.js подключите Gulp:

const gulp = require('gulp');
Добавим простую задачу, которая выводит на экран сообщение:

gulp.task('hello', function(done) {
    console.log('Привет, Gulp!');
    done();
});
Теперь запустите задачу командой:

gulp hello
При успешном запуске в консоль выведется следующее сообщение:

[21:50:46] Using gulpfile ~/courses/gulpfile.js
[21:50:46] Starting 'hello'...
Привет, Gulp!
[21:50:46] Finished 'hello' after 2.84 ms
Рассмотрим продвинутый пример работы с Gulp — автообновление браузера. Эта возможность позволяет
отслеживать изменения в файлах HTML и CSS в режиме реального времени без необходимости обновлять
страницу браузера каждый раз, когда вы вносите изменения в файлы проекта в процессе отладки.

Установите Browsersync:

npm install browser-sync --save-dev
Добавьте задачу:

const browserSync = require('browser-sync').create();

gulp.task('serve', function() {
    browserSync.init({
        server: {
            baseDir: './'
        }
    });

    gulp.watch('src/scss/**/*.scss', gulp.series('styles')).on('change', browserSync.reload);
    gulp.watch('./*.html').on('change', browserSync.reload);
});
Запустите сервер:

gulp serve
Webpack

Webpack — это инструмент для сборки модулей JavaScript.

Webpack позволяет упростить такие задачи, как:

сборка JavaScript, CSS, изображений и других файлов,
преобразование и оптимизация кода: сжатие файлов, удаление неиспользуемого кода,
загрузка файлов через разделение кода (code splitting).
Поскольку Webpack предназначен для работы с модулями JavaScript, этот сборщик может быть сложным
в использовании для новичков.

Как и Gulp, Webpack работает через Node.js.

Для начала работы со сборщиком установите Webpack и Webpack CLI. Для этого выполните команды в терминале:

npm install --save-dev webpack webpack-cli
Инициализируйте проект, если это еще не сделано, командой:

npm init -y
Создайте файл webpack.config.js в корне проекта. Это файл конфигурации Webpack.

Пример базовой конфигурации:

const path = require('path');

module.exports = {
    entry: './src/index.js', // точка входа
    output: {
        filename: 'bundle.js', // имя итогового файла
        path: path.resolve(__dirname, 'dist') // папка для сборки
    },
    mode: 'development', // режим разработки
};
Создайте папку src и файл index.js внутри нее, чтобы Webpack знал, где искать входную точку.

Для сборки проекта выполните команду:

npx webpack
В результате Webpack создаст папку dist с файлом bundle.js, содержащим собранный код.

Vite

Vite — инструмент для разработки и сборки фронтенд-проектов. Основная особенность
Vite заключается в использовании ES-модулей для модулирования кода на JavaScript.

Vite предоставляет следующие возможности:

создание серверной среды для разработки,
поддержка JavaScript, CSS, HTML, и других ресурсов,
сборка проекта с оптимизацией,
интеграция с современными фреймворками (React, Vue, Svelte и другими).
Для работы с Vite также необходимо установить Node.js.

Новый проект инициализируется с помощью команды:

npm create vite@latest
Следуйте инструкциям, чтобы выбрать:

Имя проекта.
Шаблон (например, Vanilla, React, Vue).
Перейдите в папку проекта и установите зависимости:

cd <имя_проекта>
npm install
Для запуска сервера разработки используйте команду:

npm run dev
По умолчанию проект будет доступен в браузере по адресу http://localhost:5173/.

Основной файл конфигурации Vite — vite.config.js или vite.config.ts. Он создается автоматически
при инициализации некоторых шаблонов или может быть добавлен вручную.

Пример базового vite.config.js:

import { defineConfig } from 'vite';

export default defineConfig({
    server: {
        port: 3000, // изменить порт
    },
    build: {
        outDir: 'dist', // папка для сборки
    },
});
Рассмотрим процесс добавления стилей. Создайте файл стилей, например, src/style.css,
и подключите его в src/main.js или src/main.ts:

import './style.css';
Vite автоматически обработает CSS.

Рассмотрим еще один пример работы с Vite. Для обработки изображений можно использовать плагин vite-plugin-imagemin.

Установите его:

npm install vite-plugin-imagemin --save-dev
Добавьте в vite.config.js:

import { defineConfig } from 'vite';
import viteImagemin from 'vite-plugin-imagemin';

export default defineConfig({
    plugins: [
        viteImagemin({
            gifsicle: { optimizationLevel: 7 },
            optipng: { optimizationLevel: 7 },
            mozjpeg: { quality: 20 },
            svgo: { plugins: [{ name: 'removeViewBox' }] },
        }),
    ],
});
Для сборки проекта выполните команду:

npm run build
Vite создаст оптимизированную версию проекта в папке dist.

Даже при работе с готовыми инструментами могут возникнуть ошибки. 
Разберём самые распространённые и способы их избежать.

Типичные ошибки
Тип ошибки 1: Node.js не установлен

Установщик сборщика (npm install, npx create-vite, и т.д.) выдаёт ошибку.

Пример ошибки:

command not found: node
Решение:

Установите Node.js:

Скачайте последнюю версию с официального сайта Node.js.
Убедитесь, что во время установки выбран флажок для установки npm (Node Package Manager).
Проверьте установку:

После установки выполните команды:
node -v
npm -v
Это должно вывести версии Node.js и npm.

Тип ошибки 2: Попытка инициализации проекта вне папки

Ошибки могут возникать, когда вы случайно запустили команду npm init или npm install в
корневой директории системы или в другой неподходящей папке.

В этом случае в текущей папке появятся файлы package.json, node_modules и другие файлы сборщика,
которые не должны там находиться.

Решение:

Удалите созданные файлы из неправильной директории:

rm -rf package.json package-lock.json node_modules
Затем создайте новую папку для проекта:

mkdir my-project
cd my-project
Запустите команды повторно:

npm init -y
Практика
Задача 1: Сжатие css с помощью Gulp

Ситуация: вы работаете над небольшим веб-проектом. У вас есть несколько CSS-файлов,
которые вы используете для стилизации веб-страниц. Эти файлы имеют большие размеры из-за избыточных пробелов,
комментариев и других ненужных данных, что замедляет загрузку страницы.

Вам нужно автоматизировать процесс сжатия этих файлов, чтобы улучшить производительность веб-страницы.
Вы решаете использовать Gulp, чтобы создать задачу, которая будет сжимать все
CSS-файлы и сохранять их в оптимизированном виде в отдельной папке.

Задача: написать простой Gulp скрипт, который автоматизирует сжатие CSS-файлов,
чтобы ускорить загрузку веб-страницы.

Требования:

У вас есть исходные CSS-файлы в папке src/css/:

style.css
layout.css
responsive.css
После сжатия этих файлов, вы хотите сохранить оптимизированные версии в папке dist/css/ для 
дальнейшего использования на веб-странице.

Вам нужно использовать Gulp для автоматизации этого процесса. Необходимо:

Прочитать все CSS-файлы из папки src/css/.
Сжать их, удалив все лишние пробелы, комментарии и другие ненужные символы.
Сохранить сжатые файлы в папке dist/css/.
Вы решаете использовать плагин gulp-clean-css, чтобы сжать файлы CSS.

Шаги реализации:

Установите Gulp и необходимые зависимости:
npm init -y
npm install --save-dev gulp gulp-clean-css
Создайте файл gulpfile.js в корне вашего проекта.

Вставьте следующий код в gulpfile.js:

const gulp = require('gulp');
const cleanCSS = require('gulp-clean-css');

// Задача для сжатия CSS файлов
gulp.task('minify-css', function () {
  return gulp.src('src/css/*.css') // Укажите путь к вашим CSS-файлам
    .pipe(cleanCSS({ compatibility: 'ie8' })) // Сжимаем CSS
    .pipe(gulp.dest('dist/css')); // Сохраняем сжатые файлы в папке dist/css
});

// Задача по умолчанию, которая запускает задачу minify-css
gulp.task('default', gulp.series('minify-css'));
gulp.src('src/css/*.css'): указывает на все CSS-файлы в папке src/css/.
cleanCSS(): плагин для сжатия CSS.
gulp.dest('dist/css'): cохраняет сжатые файлы в папке dist/css.
Для запуска задачи в терминале выполните команду:
gulp
После этого Gulp автоматически сожмет все CSS-файлы из папки src/css и сохранит их в папке dist/css.

Задача 2: Минификация и объединение JavaScript с помощью Gulp

Ситуация: вы работаете над веб-проектом, и у вас есть несколько JavaScript файлов, 
которые содержат код для различных функциональностей на вашем сайте. Эти файлы могут быть 
раздроблены и занимать много места, что негативно сказывается на времени загрузки страницы.

Вам нужно автоматизировать процесс минификации и объединения этих файлов в один для улучшения производительности. 
Для этого вы решаете использовать Gulp, чтобы создать задачу, которая будет минифицировать
и объединять все JavaScript файлы и сохранять их в одном файле.

Задача: написать простой Gulp скрипт, который будет минифицировать и объединять все JavaScript файлы
в один, чтобы ускорить загрузку веб-страницы.

Требования:

У вас есть исходные JavaScript файлы в папке src/js/:

app.js
main.js
utils.js
После минификации и объединения этих файлов, вы хотите сохранить результирующий файл в папке dist/js/
для дальнейшего использования на веб-странице.

Вам нужно использовать Gulp для автоматизации этого процесса. Необходимо:

Прочитать все JavaScript файлы из папки src/js/.
Объединить их в один файл.
Минифицировать объединенный файл.
Сохранить результат в папке dist/js/.
Вы решаете использовать плагины gulp-concat для объединения файлов и gulp-uglify для минификации.

Шаги реализации:

Установите Gulp и необходимые зависимости:
npm init -y
npm install --save-dev gulp gulp-concat gulp-uglify
Создайте файл gulpfile.js в корне вашего проекта (если еще не создан).

Вставьте следующий код в gulpfile.js:

const gulp = require('gulp');
const concat = require('gulp-concat');
const uglify = require('gulp-uglify');

// Задача для минификации и объединения JavaScript файлов
gulp.task('minify-js', function () {
  return gulp.src('src/js/*.js') // Укажите путь к вашим JS файлам
    .pipe(concat('app.min.js')) // Объединяем все файлы в один
    .pipe(uglify()) // Минифицируем объединенный файл
    .pipe(gulp.dest('dist/js')); // Сохраняем результат в папке dist/js
});

// Задача по умолчанию, которая запускает задачу minify-js
gulp.task('default', gulp.series('minify-js'));
gulp.src('src/js/*.js'): указывает на все JavaScript файлы в папке src/js/.
concat('app.min.js'): объединяет все JavaScript файлы в один с именем app.min.js.
uglify(): минифицирует объединенный файл.
gulp.dest('dist/js'): сохраняет минифицированный файл в папке dist/js.
Для запуска задачи в терминале выполните команду:
gulp
После этого Gulp автоматически объединит и минифицирует все JavaScript файлы из папки src/js и 
сохранит результат в папке dist/js как app.min.js.

Итоги:
На этом занятии мы познакомились с сборщиками (bundlers) для сборки проектов в веб-разработке:

научились устанавливать сборщики на компьютер.
освоили команды для запуска сборщика.
научились запускать сборку проекта на своем устройстве.
Дальше мы познакомимся с препроцессорами.