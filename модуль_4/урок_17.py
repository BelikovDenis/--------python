Урок №17. Pug.

Учебные материалы

На прошлом занятии мы поработали с препроцессором SCSS и узнали, как упростить написание CSS-кода.
Сегодня продолжим изучение инструментов, которые делают разработку веб-страниц более гибкой.
Поговорим про Pug — HTML-препроцессор с лаконичным синтаксисом, который позволяет создавать
структурированные шаблоны без лишнего кода.

Pug особенно полезен, когда необходимо:

упростить разметку страниц за счёт использования отступов вместо тегов;
избавиться от повторов с помощью примесей и циклов;
повысить читаемость кода и ускорить его написание.
Сегодня мы:

научимся писать HTML-разметку с помощью Pug;
освоим использование функций и примесей в Pug;
рассмотрим, как генерировать списки с помощью цикла;
попрактикуемся в создании шаблонов для различных типов веб-страниц.
Глоссарий к семнадцатому занятию
Pug
Pug — HTML-препроцессор, который позволяет писать лаконичный и структурированный код с 
использованием синтаксиса на основе отступов.

Indentation (Отступы)
Отступы — ключевая особенность синтаксиса Pug, которая заменяет открывающие и закрывающие теги,
определяя структуру HTML-документа.

Loops (Циклы)
Циклы — механизм для автоматической генерации повторяющихся элементов, таких как списки, таблицы или карточки.

Interpolation (Интерполяция)
Интерполяция — способ вставки переменных и выражений внутрь текста, позволяющий динамически изменять
содержимое HTML-элементов.




Преимущества Pug

Pug — это инструмент, который упрощает написание HTML-кода. Для начала работы необходимо установить
его на свой компьютер. Браузеры не поддерживают Pug напрямую. Код, написанный на Pug, компилируется
в стандартный HTML, который воспринимают все браузеры.

Pug избавляет нас от необходимости вручную писать открывающие и закрывающие теги, атрибуты и вложенные конструкции.

Такие возможности Pug, как примеси, функции, циклы и переменные, значительно упрощают создание сложных и повторяющихся шаблонов.

Pug легко интегрируется в проекты на Node.js и может быть использован с другими инструментами разработки, 
такими как Webpack или Gulp.

Установка Pug

Установка Pug производится через npm:

npm install pug-cli -g
Проверка установки:

pug --version
Чтобы создать проект, инициализируем проект через npm init, а затем устанавливаем локальный пакет Pug:

npm install pug --save-dev
Синтаксис Pug

Pug использует лаконичный синтаксис, основанный на отступах. Они используются для определения вложенности
элементов и заменяют открывающие и закрывающие теги HTML. Вложенные элементы записываются с помощью
табуляции или пробелов. Это делает код более читаемым и структурированным.

Пример синтаксиса Pug:

doctype html
html
  head
    title Урок Pug
  body
    h1 Добро пожаловать!
    p Это ваш первый шаблон Pug.
После компиляции в HTML код выглядит так:

<!DOCTYPE html>
<html>
  <head>
    <title>Урок Pug</title>
  </head>
  <body>
    <h1>Добро пожаловать!</h1>
    <p>Это ваш первый шаблон Pug.</p>
  </body>
</html>
Классы и идентификаторы

В Pug классы и идентификаторы элементов задаются прямо в имени тега, что позволяет избежать создания
дополнительных атрибутов.

Классы задаются через точку, а идентификаторы — через решетку.

Например:

p.text-sample#text Это текст.
Это эквивалентно следующему HTML:

<p class="text-sample" id="text">Это текст.</p>
Атрибуты

Атрибуты в Pug записываются в круглых скобках после имени тега.

Пример:

a(href="https://example.com" target="_blank") Посетить сайт
Результат в HTML:

<a href="https://example.com" target="_blank">Посетить сайт</a>
Атрибуты можно записывать в несколько строк для удобства и улучшения читаемости кода:

input(
  type="text"
  placeholder="Введите текст"
  required
)
Переменные и интерполяция

Pug позволяет использовать переменные для динамического наполнения шаблонов. Переменные объявляются
с помощью конструкции - перед блоком разметки. Для их вставки в текст используется интерполяция.

Интерполяция в контексте Pug (и других шаблонных языков) — это способ вставки значений переменных 
или результатов выражений прямо в текстовую часть HTML-кода. Она позволяет динамически изменять 
содержимое шаблона на основе данных или логики.

Как работает интерполяция?

В Pug интерполяция осуществляется с использованием синтаксиса #{}.
Внутри фигурных скобок можно указывать переменные, функции или выражения на JavaScript.
Пример использования переменных:

- var name = 'Иван'
p Привет, #{name}!
Результат в HTML:

<p>Привет, Иван!</p>
Можно использовать выражения внутри интерполяции:

- var count = 5
p У вас #{count > 1 ? 'новых сообщений' : 'новое сообщение'}.
Этот код на Pug демонстрирует использование переменной и интерполяции для динамического отображения
текста в зависимости от значения переменной count.

Переменная count задаётся с помощью var и содержит значение 5.

Далее создаётся HTML-тег <p> с текстом, который включает динамическое выражение в фигурных скобках #{}.

Результат в HTML:

<p>У вас 5 новых сообщений.</p>
Компиляция

Рассмотрим процесс создания разметки с помощью Pug. Создаём файл index.pug и задаём базовую структуру
HTML в синтаксисе Pug:

doctype html
html
  head
    title Моя страница
  body
    h1 Добро пожаловать на сайт
    p Это пример разметки с использованием Pug.
Перейдём к компиляции Pug в HTML. Для этого в интерфейсе командной строки введём следующее:

pug index.pug --pretty
Функции в Pug

Функции в Pug позволяют выполнять вычисления или обрабатывать данные прямо в шаблоне. 
Обычно они используются для подготовки данных перед их отображением.

Функции объявляются в JavaScript-коде (например, в Node.js), а затем вызываются внутри Pug.

Рассмотрим пример создания функции в Pug.

Сначала создаём JavaScript-код:

function capitalize(word) {
  return word.charAt(0).toUpperCase() + word.slice(1);
}
Это пример скрипта для капитализации слова: первая буква слова заменяется на большую.

Добавляем скрипт в Pug-шаблон:

- var name = 'иван'
p Привет, #{capitalize(name)}!
Мы передали переменную name в созданную функцию.

Получаем следующий результат в HTML после компиляции:

<p>Привет, Иван!</p>
Примеси в Pug

Примеси — это аналог функций, которые генерируют HTML-код. Они удобны для повторного использования
шаблонов с динамическими параметрами.

Примесь объявляется с помощью ключевого слова mixin, после которого указывается её имя.

Создадим примесь для создания ссылки по заданному тексту и URL:

mixin button(text, url)
a.button(href=url) #{text}
Примесь вызывается по имени:

+button('Перейти', 'https://example.com')
+button('Подробнее', '/details')
Получаем следующий результат в HTML:

<a class="button" href="https://example.com">Перейти</a>
<a class="button" href="/details">Подробнее</a>
Кроме того, в примеси можно добавлять условия с помощью if и else:

mixin statusBadge(status)
if status === 'active'
  span.badge.active Активен
else
  span.badge.inactive Неактивен
Вызовем примесь с двумя разными условиями:

+statusBadge('active')
+statusBadge('inactive')
Получаем следующий результат в HTML для двух вариантов:

<span class="badge active">Активен</span>
<span class="badge inactive">Неактивен</span>
Генерация списков в Pug

Генерация списков позволяет создавать повторяющиеся элементы, такие как списки или таблицы, на основе массива данных.

Цикл each используется для перебора массива или объекта:

- var fruits = ['Яблоко', 'Банан', 'Апельсин']
ul
  each fruit in fruits
    li= fruit
Мы задали массив элементов в переменной fruits и с помощью цикла each записали каждый элемент в список.

Получаем следующий результат в HTML:

<ul>
  <li>Яблоко</li>
  <li>Банан</li>
  <li>Апельсин</li>
</ul>
Также Pug поддерживает генерацию списков с индексами для создания индексированных (например, нумерованных) списков:

each fruit, index in fruits
li #{index + 1}. #{fruit}
Результат в HTML:

<li>1. Яблоко</li>
<li>2. Банан</li>
<li>3. Апельсин</li>
Рассмотрим еще один вариант генерации списков для заданных объектов:

- var users = { 1: 'Иван', 2: 'Мария', 3: 'Пётр' }
ul
  each name, id in users
    li Пользователь #{id}: #{name}
Результат в HTML:

<ul>
  <li>Пользователь 1: Иван</li>
  <li>Пользователь 2: Мария</li>
  <li>Пользователь 3: Пётр</li>
</ul>
Осваивая синтаксис Pug, легко допустить неточности, особенно в отступах, примесях и работе с атрибутами.
Ниже разберём распространённые ошибки, которые мешают корректной компиляции и усложняют отладку шаблонов.

Типичные ошибки
Тип ошибки 1: Ошибки в отступах

Pug использует отступы с пробелами или табуляцией для определения вложенности. Несогласованное 
использование отступов приводит к ошибкам компиляции.

Пример ошибки:

html
head
  title Мой сайт
 body
  h1 Добро пожаловать!
В этом примере строка body имеет некорректный отступ.

Результат: ошибка компиляции (Inconsistent indentation detected).

Решение:

Используйте только пробелы или только табуляцию для отступов во всём проекте. Настройте редактор
кода для автоматической вставки пробелов или табуляции.

Исправим ошибку:

html
head
  title Мой сайт
body
  h1 Добро пожаловать!
Тип ошибки 2: Пропущенные отступы

В этом примере li Элемент 1 находится на одном уровне с ul, однако этот элемент должен быть вложенным:

Пример ошибки:

ul
li Элемент 1
  li Элемент 2
Решение:

В этом примере мы исправили ошибку в отступах и восстановили вложенность:

ul
li Элемент 1
li Элемент 2
Тип ошибки 3: Отсутствие кавычек в атрибутах

Значения атрибутов должны быть заключены в кавычки.

Пример ошибки:

a(href=https://example.com target=_blank) Ссылка
Решение:

В этом примере мы корректно использовали кавычки:

a(href="https://example.com" target="_blank") Ссылка
Тип ошибки 4: Ошибки в синтаксисе примесей

При использовании примесей необходимо корректно использовать синтаксис.

В этом примере пропущены скобки при вызове примесей.

Пример ошибки:

mixin button(text, url)
  a.button(href=url) #{text}

+button 'Подробнее', '/details'
Решение:

В этом примере мы исправили синтаксис и использовали скобки при вызове примесей:

mixin button(text, url)
  a.button(href=url) #{text}

+button('Подробнее', '/details')
Практикум
Задача 1: Создание веб-страницы с галереей товаров с использованием Pug и SCSS

Ситуация: вы работаете над созданием веб-сайта для продуктового магазина. Основная задача – разработать
привлекательную и удобную галерею товаров, которая будет адаптироваться под разные устройства и обеспечивать
положительный пользовательский опыт. Для упрощения разработки и повышения гибкости проекта вы используете
HTML-препроцессор Pug и CSS-препроцессор SCSS.

Каждая карточка товара в галерее должна быть информативной и удобной для восприятия: содержать
изображение продукта, его название, краткое описание, цену, а также кнопку для совершения покупки.

Задача: разработайте веб-страницу для продуктового магазина, которая включает:

заголовок страницы с названием магазина;
подпись с описанием;
галерею товаров, каждая карточка которой содержит изображение, название, описание и цену с кнопкой «Купить».
Требования:

Структура веб-страницы:

заголовок с названием магазина;
подпись с кратким описанием магазина;
галерея товаров, где каждый товар представлен карточкой.
Требования к карточке товара:

Обязательные элементы:
изображение товара,
название товара,
краткое описание,
цена,
кнопка «Купить».
Дизайн:
Карточка должна быть аккуратной и эстетичной.
Должны быть использованы стандартные размеры изображений и отступы для равномерного размещения элементов.
При наведении на карточку фон должен слегка изменяться, а тень – становиться более выраженной.
Адаптивность:
Карточки должны корректно отображаться на экранах разного размера: от мобильных устройств до настольных компьютеров.
Требования к SCSS:

Должны быть использованы переменные для определения основных цветов и размеров.
Примеси должны быть применены для создания кнопок с учётом состояния наведения.
Вложенность должна помочь в создании понятных иерархических стилей.
Инструменты и процессы:

Для разметки страницы используйте Pug.
Для стилей используйте SCSS с последующей компиляцией в CSS.
Подготовьте изображения для карточек товаров (можно использовать заглушки).
Функциональность:

После выполнения всех этапов страница должна быть полностью работоспособной, с отображением заголовка,
подписи и галереи товаров.
Код должен быть структурированным и легко поддерживаемым.
Шаги реализации:

Подготовка файловой структуры.
Создайте следующую файловую структуру:
project/
├── index.pug
├── style.scss
├── style.css
├── images/
│   └── placeholder.jpg
Создание разметки с помощью Pug.
Файл: index.pug
doctype html
html(lang="ru")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Галерея товаров
    link(rel="stylesheet" href="style.css")
  body
    header
      h1 Магазин "Вкусные товары"
      p Самые свежие и качественные продукты для вас!

    section.gallery
      each product in products
        .card
          img(src=product.image alt=product.name)
          h2.title= product.name
          p.description= product.description
          p.price #{product.price} ₽
          button.button Купить

// Данные для галереи
- var products = [
  {name: "Апельсины", description: "Свежие и сочные апельсины прямо из Испании.", price: 199, image: "images/placeholder.jpg"},
  {name: "Яблоки", description: "Красные и зеленые яблоки для вашего здоровья.", price: 149, image: "images/placeholder.jpg"},
  {name: "Бананы", description: "Спелые и сладкие бананы для всей семьи.", price: 89, image: "images/placeholder.jpg"}
]
Создание стилей с помощью SCSS.
Файл: style.scss
// Переменные
$primary-color: #ff6f61;
$secondary-color: #ffffff;
$border-color: #ddd;
$text-color: #333;

// Примесь для кнопок
@mixin button-style {
  background-color: $primary-color;
  color: $secondary-color;
  border: none;
  padding: 10px 20px;
  border-radius: 5px;
  cursor: pointer;
  text-transform: uppercase;
  font-weight: bold;
  transition: background-color 0.3s ease;

  &:hover {
    background-color: darken($primary-color, 10%);
  }
}

// Стили страницы
body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
  background-color: #f9f9f9;
  color: $text-color;
  line-height: 1.6;
}

header {
  text-align: center;
  padding: 20px;
  background-color: $primary-color;
  color: $secondary-color;

  h1 {
    margin: 0;
    font-size: 2.5rem;
  }

  p {
    font-size: 1.2rem;
    margin-top: 10px;
  }
}

.gallery {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  justify-content: center;
  padding: 20px;

  .card {
    border: 1px solid $border-color;
    border-radius: 10px;
    padding: 15px;
    text-align: center;
    background-color: $secondary-color;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    max-width: 300px;

    &:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
    }

    img {
      max-width: 100%;
      border-radius: 10px 10px 0 0;
    }

    .title {
      font-size: 1.5rem;
      color: $text-color;
      margin: 10px 0;
    }

    .description {
      font-size: 1rem;
      color: darken($text-color, 20%);
      margin: 10px 0;
    }

    .price {
      font-size: 1.2rem;
      color: $primary-color;
      margin: 15px 0;
      font-weight: bold;
    }

    .button {
      @include button-style;
    }
  }
}
Компиляция SCSS в CSS.
Запустите компиляцию SCSS:
sass style.scss style.css
Открытие страницы в браузере.
Скомпилированный файл index.pug преобразуйте в HTML с помощью Pug-компилятора:

pug index.pug --pretty
Откройте созданный файл index.html в браузере.

Задача 2: Создание веб-страницы для магазина электротехники с использованием Pug и SCSS

Ситуация: вы создаёте веб-страницу для магазина электротехники. На странице нужно отобразить галерею товаров,
где каждый из них представлен карточкой с изображением, названием, кратким описанием, ценой и кнопкой для покупки.

Задача: создать адаптивную галерею товаров электротехники, используя препроцессоры Pug для разметки и SCSS для стилей.

Требования:

Структура страницы:
Название магазина (заголовок): «ТехноМаркет».
Подзаголовок с текстом: «Современные устройства для вашего комфорта».
Галерея товаров, содержащая карточки с техникой.
Карточки товаров:
Обязательные элементы:
изображение товара,
название товара,
краткое описание (например, основные характеристики устройства).
цена,
кнопка «Добавить в корзину».
Дизайн:
Используйте минималистичный стиль: чёткие линии, спокойные цвета.
При наведении: легкое увеличение изображения товара и изменение фона карточки.
Адаптивность:
Карточки должны корректно отображаться в сетке на экранах разного размера.
Требования к SCSS:
Определите переменные для ключевых цветов (например, background,primary, $hover-color).
Используйте примеси для кнопок с эффектами наведения.
Стилизуйте элементы карточек с использованием вложенности.
Инструменты:
Для HTML-разметки используйте Pug.
Для стилей используйте SCSS с последующей компиляцией в CSS.
Шаги реализации:

Создайте файловую структуру:
project/
├── index.html
├── gallery.pug
├── style.scss
└── style.css
Пример кода Pug:
doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title ТехноМаркет
    link(rel="stylesheet", href="style.css")
  body
    header
      h1 ТехноМаркет
      p Современные устройства для вашего комфорта
    main
      .gallery
        each product in products
          .card
            img(src=product.image, alt=product.name)
            h2= product.name
            p.description= product.description
            p.price #{product.price} ₽
            button.button Добавить в корзину
Пример данных для Pug:
const products = [
  {
    name: "Смартфон XYZ",
    image: "https://via.placeholder.com/300x200",
    description: "Мощный процессор, качественная камера",
    price: "49,999"
  },
  {
    name: "Ноутбук ABC",
    image: "https://via.placeholder.com/300x200",
    description: "13 дюймов, 16 ГБ ОЗУ, SSD 512 ГБ",
    price: "89,999"
  },
  {
    name: "Наушники DEF",
    image: "https://via.placeholder.com/300x200",
    description: "Шумоподавление, до 40 часов работы",
    price: "19,999"
  }
];
Пример SCSS (style.scss):
// Переменные
$background: #f9f9f9;
$primary: #0a74da;
$hover-color: #0849a3;
$text-color: #333;

// Примесь для кнопки
@mixin button-style {
  background-color: $primary;
  color: white;
  padding: 10px 15px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
  text-transform: uppercase;
  transition: background-color 0.3s ease;

  &:hover {
    background-color: $hover-color;
  }
}

// Стили для галереи
.gallery {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  justify-content: center;
  padding: 20px;

  .card {
    background: $background;
    border-radius: 10px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    padding: 15px;
    width: 300px;
    text-align: center;
    transition: transform 0.3s, box-shadow 0.3s;

    &:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
    }

    img {
      max-width: 100%;
      border-radius: 10px;
    }

    .description {
      margin: 10px 0;
      color: $text-color;
    }

    .price {
      color: $primary;
      font-weight: bold;
      margin: 10px 0;
    }

    .button {
      @include button-style;
    }
  }
}
Скомпилируйте SCSS в CSS:
sass style.scss style.css
Откройте созданный файл index.html в браузере.

Итоги:
Сегодня мы познакомились с препроцессором Pug:

изучили основные принципы работы с Pug;
разобрали, как использовать функции и примеси в Pug;
освоили генерацию списков в Pug с помощью циклов.
Впереди — финальный проект.