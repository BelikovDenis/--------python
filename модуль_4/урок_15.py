 Урок №15. Введение в препроцессоры.
Учебные материалы
Стандартные стили CSS становятся неудобными в сложных проектах.
Раньше в CSS нельзя было задавать переменные для повторяющихся значений,
создавать вложенные структуры без длинных селекторов, а также использовать собственные
функции для упрощения кода.

Например, для определения стилей вложенных элементов в обычном CSS приходилось писать:

nav ul li a {
  text-decoration: none;
}
Такая запись плохо отражает вложенность структуры HTML-документа и затрудняет поддержку кода.

Чтобы решить эти проблемы, разработчики используют препроцессоры, расширяющие возможности CSS:
они позволяют работать с переменными, создавать вложенные правила, использовать функции и примеси.

Сегодня мы:

познакомимся с основными возможностями препроцессоров;
научимся подключать препроцессоры в проект.
Глоссарий к пятнадцатому занятию
Preprocessor (Препроцессор)
Препроцессор — инструмент, расширяющий возможности CSS (или HTML) и позволяющий использовать переменные,
функции, вложенность и другие инструменты для упрощения работы с кодом.

Compilation (Компиляция)
Компиляция — процесс преобразования кода препроцессора (например, SCSS) в обычный CSS, который «понимают» браузеры.

Custom Properties (Пользовательские свойства)
Custom Properties — переменные CSS, записываемые с использованием синтаксиса --variable-name, введённые в стандарт CSS.

Variables (Переменные)
Переменные — специальные конструкции в препроцессорах, позволяющие задавать значения и повторно их использовать (цвета, размеры шрифтов).

Например, $primary-color: #3498db; присваивает значение цвета в переменной, которую потом можно будет указывать вместо значения Hex.



Что такое препроцессор?

Препроцессор — это инструмент, который расширяет возможности стандартных языков веб-разработки (например,
                                                     CSS или HTML), добавляя новые функции, упрощающие работу.

После обработки препроцессором код преобразуется в стандартный (например, из SCSS в CSS). Это называется компиляцией.

Вот некоторые популярные препроцессоры:

CSS-препроцессоры: SASS, LESS, Stylus.
HTML-препроцессоры: Pug (ранее Jade), HAML.
Препроцессоры упрощают работу с кодом, помогают повысить читаемость, задать структуру проекта. Например,
многие препроцессоры поддерживают использование переменных, функций, примесей и вложенности.

Переменные позволяют заменять значения, которые часто встречаются в вашем коде (например, цвета или параметры шрифта),
на специальные конструкции, которые гораздо проще воспроизвести.

Функции и примеси снижают количество строк дублирующегося кода путем замены записей на их краткие варианты.

Преимущества использования препроцессоров

Использование препроцессоров помогает повысить читаемость кода. Например, такие свойства, как вложенность стилей и кода,
облегчают понимание структуры документа. Вложенность позволяет представлять иерархию HTML-документа при определении стилей:

ul {
  li {
    a {
      color: black;
    }
  }
}
В этом примере мы создали вложенную структуру, которая после компиляции в стандартный CSS преобразуется следующим образом:

ul li a {
  color: black;
}
Переменные позволяют управлять цветами, шрифтами и другими свойствами CSS.

$primary-color: #3498db;

body {
  color: $primary-color;
}
Здесь мы записали #3498db в переменную $primary-color. Название переменной запомнить и воспроизвести в коде проще,
чем запись Hex. Переменные удобно использовать в больших проектах,
где для разных элементов HTML-кода применяются одинаковые цвета, шрифты и другие методы стилизации.

Также для упрощения кода используются функции и примеси:

@mixin center {
  display: flex;
  justify-content: center;
  align-items: center;
}

.box {
  @include center;
}
В этом примере мы создали примесь center с помощью ключевого слова @mixin. Внутри неё мы прописали свойства CSS,
которые могут применяться к различным элементам HTML-страницы. Название (center) используется для вызова
примеси в других частях кода. Чтобы применить её к элементу, мы используем ключевое слово @include.

Подключение препроцессора к проекту

Рассмотрим процесс подключения препроцессора к проекту на примере SASS (Syntactically Awesome Style Sheets) — 
препроцессора на основе CSS, предназначенного для упрощения работы с файлами каскадных таблиц стилей.

Начнем с установки. Для этого нам вновь понадобится Node.js. Как и сборщики, SASS устанавливается через npm:

npm install -g sass
Теперь мы можем определить структуру проекта. Внутри него необходимо создать папку scss для файлов
препроцессора, а в ней — файл styles.scss.

Чтобы протестировать базовые возможности препроцессоров, пропишем такие стили:

$primary-color: #2ecc71;

body {
    background-color: $primary-color;
}
Здесь мы создали переменную $primary-color, в которую записали оттенок #2ecc71. 
Также мы записали значение этого цвета в background-color, как стандартных таблицах стилей, 
однако вместо непосредственного значения использовали переменную.

Следующий шаг — компиляция SCSS в CSS. Запустим команду в терминале:

sass scss/styles.scss css/styles.css
Она преобразовывает код, созданный с использованием препроцессоров, в стандартные таблицы стилей CSS.

Автоматическое отслеживание изменений

Препроцессоры могут автоматически отслеживать изменения в файлах и компилировать их без повторного запуска.
Для этого используется флаг --watch:

sass --watch scss:css
При запуске этой команды препроцессор начинает автоматически отслеживать все изменения в вашем проекте,
сохраняя результат в папку css.

Теперь мы можем подключить созданный файл styles.css к HTML-документу (обратите внимание, 
что расширение файла таблицы стилей должно быть именно *.css) и запустить веб-страницу привычным способом.

Препроцессор LESS

Рассмотрим еще один пример. Установим препроцессор LESS через через npm:

npm install -g less
Компиляция файла производится с помощью следующей команды:

lessc styles.less styles.css
Обратите внимание, что после компиляции файл CSS подключается в HTML привычным способом:

<link rel="stylesheet" href="css/styles.css">
При работе с препроцессорами могут возникать ошибки. Разберём самые частые из них.

Типичные ошибки
Тип ошибки 1: Не произведена компиляция препроцессорного файла

Если вы используете препроцессоры, например SASS или LESS, то браузер не воспринимает файлы с расширением .scss или .less.
Их необходимо компилировать в стандартный .css.

Пример ошибки:

Файл styles.scss создан, но не был скомпилирован. В этом случае в HTML будет подключён несуществующий
файл styles.css. Таблицы стилей не будут использованы в проекте.

Решение:

Убедитесь, что файл .scss скомпилирован в .css.

Например, с помощью команды SASS, если вы используете препроцессор SASS:

sass styles.scss styles.css
Или с помощью команды LESS, если вы используете данный препроцессор:

lessc styles.less styles.css
Тип ошибки 2: Указан файл с расширением .scss вместо .css в HTML

В HTML нельзя подключать файл styles.scss: браузер не cможет его интерпретировать.

Пример ошибки:

<link rel="stylesheet" href="styles.scss">
Решение:

Убедитесь, что подключаете скомпилированный CSS-файл с расширением *.css.

<link rel="stylesheet" href="styles.css">
Практикум
Задача 1: Разработка сайта-визитки с помощью SCSS

Ситуация: вы разрабатываете сайт-визитку, который включает заголовок, описание визитки и цветной фон.
Для упрощения работы с цветами фона вы используете SCSS с переменными. Вам нужно: настроить SCSS-файл,
скомпилировать его в CSS, подключить результат в HTML.

Задача: создать простую HTML-страницу с заготовкой для сайта-визитки; создать файл SCSS, подключить его к HTML.

Требования:

Требования к результату:

Фоны страницы и заголовка меняются при изменении значений переменных в SCSS.
Визитка содержит минимум: заголовок и описание.
Требования к SCSS:

Создать файл style.scss.
Скомпилировать его в style.css.
Подключить style.css в index.html.
Требования к инструментам для работы:

Использовать SASS (например, команду sass style.scss style.css).
Использовать редактор кода для работы с файлами (например, VS Code).
Шаги реализации:

Создайте файл SCSS с переменными для цветов фона.
Создайте следующую файловую структуру:
project/
├── index.html
├── style.scss
└── style.css
Реализуйте простой стиль визитки через SCSS.

В файле style.scss создайте переменные для цвета фона:
// Переменные
$background-color: #f5f5f5;
$text-color: #333;

// Стили
body {
  background-color: $background-color;
  color: $text-color;
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
}

header {
  background-color: lighten($background-color, 20%);
  padding: 20px;
  text-align: center;
}

h1 {
  margin: 0;
  color: $text-color;
}
Подключите итоговый CSS-файл в index.html.

Скомпилируйте SCSS в CSS, используя терминал:
sass style.scss style.css
Создайте файл index.html.

В файле index.html создайте базовую разметку и подключите скомпилированный style.css:
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Визитка</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header>
    <h1>Имя Фамилия</h1>
  </header>
  <main>
    <p>Добро пожаловать на мой сайт-визитку! Здесь вы можете узнать обо мне больше.</p>
  </main>
</body>
</html>
Протестируйте результат.

Откройте index.html в браузере.
Проверьте, что цвета фона и текста соответствуют значениям, указанным в переменных.
Попробуйте изменить переменные в style.scss, перекомпилировать файл и обновить страницу, чтобы убедиться, что стили обновляются.
Задача 2: Разработка страницы галереи товаров магазина с помощью SCSS

Ситуация: вы разрабатываете сайт-галерею для интернет-магазина, который включает заголовок,
описание и секцию с товарами. Для удобства управления стилями требуется использовать SCSS с переменными. 
Вам нужно: настроить SCSS-файл, скомпилировать его в CSS, подключить результат в HTML.

Задача: создать простую HTML-страницу с заготовкой для сайта-галереи товаров; создать файл SCSS, подключить его к HTML.

Требования:

Требования к результату:

Фон страницы сайта и фон секций товаров меняются при изменении значений переменных в SCSS.
Галерея включает минимум: заголовок, описание, карточки товаров.
Требования к SCSS:

Создать файл gallery.scss.
Скомпилировать его в gallery.css.
Подключить gallery.css в index.html.
Требования к инструментам для работы:

Использовать SASS (например, команду sass gallery.scss gallery.css).
Использовать редактор кода для работы с файлами (например, VS Code).
Шаги реализации:

Создайте файл SCSS с переменными для цветов.

Создайте следующую файловую структуру:
project/
├── index.html
├── gallery.scss
└── gallery.css
Реализуйте стиль галереи товаров через SCSS

В файле gallery.scss создайте переменные для цветов фона:
// Переменные
$background-color: #e8f5e9; // Светло-зелёный фон страницы
$product-card-bg: #ffffff; // Белый фон карточек
$text-color: #333; // Цвет текста

// Стили
body {
  background-color: $background-color;
  color: $text-color;
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
}

header {
  background-color: darken($background-color, 10%);
  padding: 20px;
  text-align: center;
}

h1 {
  margin: 0;
  color: $text-color;
}

.main-content {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
  padding: 20px;
}

.product-card {
  background-color: $product-card-bg;
  border: 1px solid lighten($text-color, 40%);
  border-radius: 10px;
  padding: 15px;
  width: 200px;
  text-align: center;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

.product-card h2 {
  font-size: 18px;
  margin: 10px 0;
}

.product-card p {
  font-size: 14px;
  margin: 5px 0;
}
Подключите итоговый CSS-файл в `index.html.

Скомпилируйте SCSS в CSS, используя терминал:
sass gallery.scss gallery.css
Создайте файл index.html.

В файле index.html создайте разметку для галереи товаров:
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Галерея товаров</title>
  <link rel="stylesheet" href="gallery.css">
</head>
<body>
  <header>
    <h1>Магазин GreenStore</h1>
  </header>
  <main class="main-content">
    <div class="product-card">
      <h2>Товар 1</h2>
      <p>Описание товара 1</p>
    </div>
    <div class="product-card">
      <h2>Товар 2</h2>
      <p>Описание товара 2</p>
    </div>
    <div class="product-card">
      <h2>Товар 3</h2>
      <p>Описание товара 3</p>
    </div>
  </main>
</body>
</html>
Протестируйте результат.

Откройте index.html в браузере.
Убедитесь, что заголовок, фон страницы и стили карточек соответствуют переменным, указанным в gallery.scss.
Попробуйте изменить значения переменных в gallery.scss, перекомпилируйте файл и обновите страницу.
Итоги:
На этом занятии мы познакомились с препроцессорами и разобрались, как подключать препроцессоры SCSS к HTML-документу:

изучили основные возможности препроцессоров;
узнали, как устанавливать препроцессоры и подключать их в проект;
научились использовать переменные в SCSS.
На следующем занятии мы продолжим знакомство с препроцессорами и освоим работу с SCSS.
