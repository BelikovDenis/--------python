На прошлом занятии мы изучили основы работы с процессами и межпроцессным взаимодействием (IPC),
используя такие инструменты, как Queue и Pipe. 
Однако в некоторых случаях нам нужно разделять данные между процессами более эффективно, 
особенно когда речь идёт о больших объёмах данных. 
Для этого в Python 3.8 был добавлен модуль multiprocessing.shared_memory, 
который позволяет процессам совместно использовать участки памяти.


На этом занятии мы:

разберём, что такое разделяемая память и зачем она нужна;
научимся создавать и использовать разделяемую память с помощью multiprocessing.shared_memory;
рассмотрим примеры синхронизации процессов при работе с разделяемой памятью.



Представим офис с несколькими сотрудниками (процессами), которые работают над одним проектом. 
У каждого сотрудника есть свой компьютер (собственная память),
но им нужно обмениваться важной информацией быстро и без лишних задержек.
Если они будут передавать друг другу бумажки (использовать Queue или Pipe), 
это создаст дополнительную нагрузку и задержки.

Чтобы решить эту проблему, в офисе устанавливают большую доску объявлений (разделяемая память).
Теперь каждый сотрудник может не тратить время на передачу сообщений —
он просто записывает нужные данные на доску, а другие могут сразу их прочитать и использовать.

Однако, если все сотрудники начнут писать на доске одновременно, 
могут возникнуть хаос и путаница. Поэтому вводят правила синхронизации:



Кто-то пишет, а остальные ждут.
Все следуют установленным правилам, чтобы информация не потерялась.


В мире программирования такую роль выполняет разделяемая память — это специальный участок оперативной памяти,
доступный нескольким процессам одновременно. 
В отличие от стандартных методов передачи данных (например, через очереди или каналы),
разделяемая память позволяет процессам напрямую обмениваться данными без их копирования.
Сегодня мы разберёмся, как правильно с ней работать и для чего нам вообще это может понадобиться.


Почему разделяемая память важна?
Использование разделяемой памяти через multiprocessing.shared_memory в 
Python находит применение в различных реальных кейсах, 
особенно когда речь идёт о высокопроизводительных вычислениях или обработке больших объёмов данных.

Разделяемая память очень часто используется в разработке. Вот несколько примеров:

Машинное обучение и обработка данных

При обработке больших наборов данных, например, 
в задачах машинного обучения (обучение моделей, обработка изображений, работа с большими таблицами)
использование разделяемой памяти позволяет процессам быстро обмениваться
данными без необходимости создавать многочисленные копии.



Обработка больших потоков данных

При обработке больших данных (например, в анализе журналов, потоков или в больших базах данных)
часто нужно распределить задачи на несколько процессов, чтобы ускорить обработку,
сохраняя при этом возможность совместного использования больших объёмов данных.

Игровые движки и симуляции

В сложных симуляциях и играх, где используются несколько параллельных процессов для симуляции физики, 
движения объектов или других параметров, разделяемая память позволяет оптимизировать производительность.

Рассмотрим основные преимущества разделяемой памяти:

1. Эффективность. Передача данных между процессами без копирования значительно снижает накладные расходы.

2. Скорость. Доступ к данным в разделяемой памяти значительно быстрее, 
чем в других механизмах межпроцессного взаимодействия (IPC).

3. Универсальность. Она подходит для работы с любыми типами данных, 
включая массивы и сложные структуры.

Этапы работы разделяемой памяти

Создание: главный процесс создаёт участок памяти и записывает туда данные.
Доступ: другие процессы получают доступ к этой памяти по имени, переданному им главным процессом.
Обновление: процессы могут читать или изменять данные в этой памяти.
Очистка: после завершения работы участок памяти должен быть закрыт и удалён.
Пример использования разделяемой памяти

Рассмотрим пример, в котором главный процесс создаёт массив и 
передаёт его в разделяемую память, а другой процесс изменяет данные в массиве:

from multiprocessing import Process, shared_memory
import array

def worker(shm_name, array_type, array_size):
    # Подключаемся к разделяемой памяти
    existing_shm = shared_memory.SharedMemory(name=shm_name)
    # Создаём массив на основе этой памяти
    shared_array = array.array(array_type, existing_shm.buf)
    # Изменяем данные в массиве
    shared_array[0] += 10
    print(f"Обновленный массив в процессе: {shared_array}")
    # Закрываем доступ к разделяемой памяти
    existing_shm.close()

if __name__ == "__main__":
    # Создаём массив данных
    data = array.array('i', [5, 10, 15])  # 'i' — тип данных для целых чисел
    # Создаём разделяемую память
    shm = shared_memory.SharedMemory(create=True, size=data.buffer_info()[1] * data.itemsize)
    # Копируем данные в разделяемую память
    shared_array = array.array('i', shm.buf[:data.buffer_info()[1] * data.itemsize])
    shared_array[:] = data[:]
    print(f"Исходный массив: {shared_array}")

    # Создаём и запускаем процесс
    process = Process(target=worker, args=(shm.name, 'i', len(data)))
    process.start()
    process.join()

    # Проверяем обновления
    print(f"Массив после изменений: {shared_array}")

    # Закрываем и удаляем разделяемую память
    shm.close()
    shm.unlink()




Что происходит в этом коде?

1. Создание массива и разделяемой памяти (главный процесс)

В блоке if __name__ == "__main__" создаётся обычный массив data с числами [5, 10, 15].
Затем выделяется участок разделяемой памяти с помощью SharedMemory:

shm = shared_memory.SharedMemory(create=True, size=data.buffer_info()[1] * data.itemsize), где:

create=True указывает, что мы создаём новый участок памяти;
size=data.buffer_info()[1] * data.itemsize задаёт размер участка памяти,
равный размеру массива data в байтах (buffer_info()[1] — количество элементов, itemsize — размер одного элемента).
После этого массив shared_array привязывается к разделяемой памяти:

shared_array = array.array('i', shm.buf)
shared_array[:] = data[:]
shared_array[:] = data[:] копирует данные из массива data в разделяемую память.

На этом этапе данные в разделяемой памяти совпадают с исходным массивом data: [5, 10, 15].

2. Запуск нового процесса (главный процесс)

Главный процесс создаёт новый worker, который принимает три аргумента:

имя разделяемой памяти (shm.name)
форму массива (data.shape)
тип данных массива (data.dtype)
Код запуска процесса:

process = Process(target=worker, args=(shm.name,))
process.start() process.join()
process.start() запускает выполнение функции worker в отдельном процессе;
process.join() заставляет главный процесс дождаться завершения worker.

3. Подключение к разделяемой памяти (новый процесс)

В функции worker процесс подключается к уже созданной разделяемой памяти по имени shm.name:

existing_shm = shared_memory.SharedMemory(name=shm_name)

После подключения создаётся массив, связанный с этой памятью:

shared_array = array.array('i', existing_shm.buf)

buffer=existing_shm.buf позволяет массиву array напрямую использовать данные из разделяемой памяти.

4. Изменение данных (новый процесс)

Новый процесс изменяет первый элемент массива, увеличивая его на 10:

array[0] += 10

Теперь данные в разделяемой памяти обновлены: [15, 10, 15].

После изменения данных процесс выводит обновлённый массив.

5. Закрытие доступа к разделяемой памяти (новый процесс)

После завершения работы процесс закрывает доступ к разделяемой памяти:

existing_shm.close()

6. Проверка обновлений (главный процесс)

Когда новый процесс завершает работу, главный процесс проверяет содержимое массива shared_array,
связанного с разделяемой памятью:

print(f"Массив после изменений: {shared_array.tolist()}")

Поскольку shared_array использует ту же память, что и array в новом процессе, изменения видны: [15, 10, 15].

7. Закрытие и удаление памяти (главный процесс)

После проверки данных главный процесс завершает работу с разделяемой памятью:

shm.close()
shm.unlink()
close() освобождает ресурсы, связанные с этим объектом в процессе;
unlink() удаляет разделяемую память из системы.
При работе с разделяемой памятью важно синхронизировать доступ к данным,
чтобы избежать состояний гонки. Для этого можно использовать блокировки Lock, 
о которых мы уже говорили на предыдущих занятиях.



Доработка предыдущей программы
Теперь давайте доработаем программу из прошлого занятия, 
используя знания о shared_memory. Представим, что главный процесс имеет массив чисел, 
который нужно модифицировать параллельно. 
Каждый процесс будет вычислять квадрат числа из массива и записывать результат обратно в этот же массив. 
Это позволит нескольким процессам совместно работать с одним ресурсом.



from multiprocessing import Process, Queue, shared_memory
import array
import time

# Функция для вычисления квадрата числа и записи результата в разделяемую память
def compute_square(shm_name, index, queue):
    # Подключаемся к разделяемой памяти
    shm = shared_memory.SharedMemory(name=shm_name)

    # Создаём массив на основе разделяемой памяти
    shared_array = array.array('i', shm.buf)  # 'i' означает int

    # Вычисляем квадрат числа по индексу
    num = shared_array[index]
    result = num ** 2
    shared_array[index] = result

    # Отправляем результат в очередь для дополнительной проверки
    queue.put((index, result))

    # Закрываем доступ к разделяемой памяти
    shm.close()

    # Имитация задержки
    time.sleep(1)

if __name__ == "__main__":
    # Исходные данные: список целых чисел
    data = array.array('i', [1, 2, 3])  # Массив целых чисел

    # Создаём разделяемую память
    shm = shared_memory.SharedMemory(create=True, size=data.buffer_info()[1] * data.itemsize)

    # Копируем данные в разделяемую память
    shared_array = array.array('i', shm.buf)
    shared_array[:] = data[:]

    # Создаём очередь для обмена результатами
    queue = Queue()

    # Создаём и запускаем процессы
    processes = []
    for i in range(len(data)):
        process = Process(target=compute_square, args=(shm.name, i, queue))
        processes.append(process)
        process.start()

    # Ожидаем завершения всех процессов
    for process in processes:
        process.join()

    # Чтение результатов из очереди
    print("\nРезультаты, полученные из очереди:")
    while not queue.empty():
        index, result = queue.get()
        print(f"Индекс {index}, квадрат = {result}")

    # Чтение итогового массива из разделяемой памяти
    print("\nИтоговый массив в разделяемой памяти:")
    shared_array = array.array('i', shm.buf)  # Обновляем ссылку на массив
    print(shared_array.tolist())  # Преобразуем в список для вывода

    # Освобождаем ресурсы разделяемой памяти
    shm.close()
    shm.unlink()

    print("Все процессы завершены.")



Какие произошли изменения и зачем они нужны?

Добавление разделяемой памяти
Вместо того, чтобы каждый процесс передавал свои результаты через очередь, 
мы используем разделяемую память. 
Она позволяет всем процессам работать с одним и тем же массивом, прямо изменяя его элементы.

Комбинация очереди и shared_memory
Очередь здесь используется для мониторинга выполнения процессов и контроля изменений, 
однако основная работа происходит через прямое взаимодействие с разделяемой памятью.

Такой подход полезен в задачах, где требуется параллельная обработка большого массива данных.
Допустим, можно представить, что числа в массиве — это результаты измерений, 
которые необходимо преобразовать, 
а разделяемая память позволяет избежать создания и передачи копий массивов между процессами.

Value и Array в multiprocessing
Также в модуле multiprocessing имеются инструменты Value и Array для работы с объектами,
они позволяют процессам читать и записывать общие данные в разделяемую память.

Multiprocessing.Value
Объект Value используется для хранения одного значения в разделяемой памяти. 
Тип значения задаётся при создании объекта. Value поддерживает встроенные примитивные типы данных, такие как:


‘i’: целое число (integer)
‘d’: число с плавающей точкой (double)
‘c’: одиночный символ (char)

Создание Value:

from multiprocessing import Value

shared_value = Value('i', 0)  # Целое число, начальное значение 0
Работа с Value:

print(shared_value.value)  # Чтение значения
shared_value.value = 42    # Запись нового значения
Пример использования:

import multiprocessing

def increment(shared_value):
    for _ in range(10):
        shared_value.value += 1

shared_value = multiprocessing.Value('i', 0)
process = multiprocessing.Process(target=increment, args=(shared_value,))
process.start()
process.join()

print(f"Итоговое значение: {shared_value.value}")



Multiprocessing.Array
Объект Array позволяет работать с массивом фиксированного размера, 
разделённым между процессами. Все элементы массива должны быть одного типа. 
Тип данных задаётся при создании. Мы можем читать и изменять значение, используя атрибут .value:.

Создание Array:

from multiprocessing import Array

shared_array = Array('i', 5)  # Массив целых чисел длиной 5
Первый аргумент — тип элементов массива (аналогично Value).
Второй аргумент — итерируемый объект (список) или размер массива.
Пример использования:

from multiprocessing import Process, Array
def square_elements(shared_array):
    for i in range(len(shared_array)):
        shared_array[i] = shared_array[i] ** 2
if __name__ == "__main__":
    shared_array = Array('i', [1, 2, 3, 4])
    process = Process(target=square_elements, args=(shared_array,))
    process.start()
    process.join()
    print("Итоговый массив:", list(shared_array))
Когда использовать Value и Array?

Value подходит для хранения одного параметра, например, счётчика.
Array удобен для работы с наборами данных, например, вектором, матрицей или списком.
Оба инструмента обеспечивают безопасность доступа к данным через автоматическое использование блокировок.

В отличие от традиционных методов обмена данными (например, через Queue или Pipe), 
разделяемая память позволяет нескольким процессам одновременно обращаться к общим данным, 
эффективно их обрабатывая, что особенно важно при работе с большими объёмами информации.

Чтобы закрепить изученное, рассмотрим ещё одно образное сравнение. 
Представим кухню семьи и каждый готовит свое блюдо, но вместе они используют один и тот же набор ингредиентов.

Каждый член семьи (процесс) может одновременно с другими брать продукты с одной общей полки 
(разделяемая память), но, чтобы не было путаницы (конфликтов),
у каждого есть своё время и способ использования содержимого этой полки (синхронизация).

Это помогает ускорить процесс приготовления, так как они не тратят время на отдельные заготовки, 
а используют, что уже подготовлено заранее.






Типичные ошибки
Тип ошибки 1: Утечка разделяемой памяти

Ошибка возникает в случае, если разделяемая память была не закрыта и не удалена.

Пример ошибки:

shm = shared_memory.SharedMemory(create=True, size=100)
# ...
# Забыли вызвать shm.close() и shm.unlink()
Решение:

вызываем close() и unlink().

shm.close()
shm.unlink()





Тип ошибки 2: Отсутствие синхронизации

Ошибка возникает в состоянии гонки при одновременном доступе к памяти.

Пример ошибки:

def worker():
    array[0] += 1  # Без блокировки
Решение:

используем Lock для синхронизации.

with lock:
    array[0] += 1






Тип ошибки 3: Некорректный размер памяти

Ошибка возникает при недостаточном объёме памяти.

Пример ошибки:

shm = SharedMemory(create=True, size=10)  # Нужно 20 байт
Решение:

рассчитываем размер через nbytes.

size = data.nbytes
Практикум








Задание 1. Синхронизированный счётчик

Ситуация: мы разрабатываем многопроцессорное приложение,
в котором каждый из процессоров обращается к общему счётчику.

Задача — реализовать приложение, использующее счётчик с применением shared_memory и Lock. 
Каждый процесс должен увеличить счётчик 5 раз.

Шаги реализации:

Создадим разделяемую память (shared_memory) для хранения значения счётчика.
Используем объект Lock для синхронизации доступа к счётчику.
Реализуем функцию worker, которая будет увеличивать значение счётчика в цикле.
Запустим несколько процессов, каждый из которых выполняет функцию worker.
Реализация:

from multiprocessing import Process, shared_memory, Lock
import array

def worker(shm_name, lock):
    shm = shared_memory.SharedMemory(name=shm_name)
    counter = array.array('q', shm.buf)  # 'q' означает целые числа (int64)

    for _ in range(5):
        with lock:
            counter[0] += 1  # Увеличиваем счётчик

    shm.close()

if __name__ == "__main__":
    shm = shared_memory.SharedMemory(create=True, size=array.array('q', [0]).itemsize)
    # Создаём разделяемую память для int64
    counter = array.array('q', shm.buf)
    counter[0] = 0  # Инициализируем счётчик значением 0

    lock = Lock()
    processes = [Process(target=worker, args=(shm.name, lock)) for _ in range(3)]

    for p in processes:
        p.start()
    for p in processes:
        p.join()

    final_value = array.array('q', shm.buf)[0]
    print("Итоговое значение:", final_value)

    shm.close()
    shm.unlink()








Задание 2. Параллельное обновление матрицы

Ситуация: мы продолжаем разрабатывать многопроцессорное приложение, 
которое анализирует данные, представленные в виде матрицы.
Каждый из процессов умножает отдельную строку матрицы на 2.

Задача — разработать приложение, создающее матрицу n*m. 
Каждый процесс приложения должен обрабатывать строки заданным путём. 
Применить синхронизацию так, чтобы строки обрабатывались независимо.

Шаги реализации:

Создадим разделяемую память для хранения матрицы.
Разделим матрицу на строки, каждая из которых будет обрабатываться отдельным процессом.
Используем объект Lock для защиты операций записи в разделяемую память.
Реализуем функцию process_row, которая умножает элементы строки на 2.
Запустим процессы для обработки всех строк.
Реализация:

from multiprocessing import process, shared_memory, lock
import array

def process_row(shm_name, row, cols, lock):
    shm = shared_memory.sharedmemory(name=shm_name)
    matrix = array.array('i', shm.buf)  # 'i' означает целые числа (int)
    start = row * cols  # начало строки
    end = start + cols  # конец строки

    with lock:
        for i in range(start, end):
            matrix[i] *= 2  # умножаем каждый элемент строки на 2

    shm.close()

if __name__ == "__main__":
    rows, cols = 3, 3
    data = [[i * cols + j for j in range(cols)] for i in range(rows)]  # создаём исходную матрицу
    flat_data = [item for sublist in data for item in sublist]  # преобразуем в одномерный список

    # создаём массив для хранения данных в разделяемой памяти
    arr = array.array('i', flat_data)
    shm = shared_memory.sharedmemory(create=true, size=arr.buffer_info()[1] * arr.itemsize)
    shm.buf[:] = arr.tobytes()  # копируем данные в разделяемую память

    lock = lock()

    processes = [
        process(target=process_row, args=(shm.name, i, cols, lock))
        for i in range(rows)
    ]

    for p in processes:
        p.start()
    for p in processes:
        p.join()

    # читаем результат из разделяемой памяти
    result_matrix = array.array('i', shm.buf)
    result_matrix = [result_matrix[i * cols:(i + 1) * cols] for i in range(rows)]

    print("итоговая матрица:")
    for row in result_matrix:
        print(row)

    shm.close()
    shm.unlink()




Итоги:
На этом занятии мы:

научились создавать и использовать разделяемую память для межпроцессного взаимодействия;
изучили важность синхронизации с помощью Lock для предотвращения состояний гонки;
рассмотрели типичные ошибки при работе с shared_memory и способы их решения;
реализовали практические задачи, демонстрирующие параллельное обновление данных.
Использование разделяемой памяти и методов синхронизации крайне важно при разработке
высокопроизводительных приложений, особенно в таких областях, как машинное обучение,
обработка больших баз данных и симуляция. 
Эти средства помогают управлять ресурсами и улучшать производительность при работе с
параллельными процессами, сокращая время обработки данных. В дальнейшем, 
когда мы будем разрабатывать комплексные многопоточные приложения, 
работающие с файлами или сетью, эти знания нам очень пригодятся