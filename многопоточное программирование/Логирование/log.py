Что произойдёт, если ваша программа получит некорректные данные? Она сломается?
Выдаст ошибку? Или, может быть, просто продолжит работать неправильно?

Сегодня мы разберём два важных момента работы с Python,
которые помогают делать программы надёжными и удобными.
Они защищают код от неожиданных ситуаций и делают его более понятным как для вас,
так и для других разработчиков..

Логирование (logging «регистрация, запись») — это процесс записи событий, которые происходят в программе.

Для этого существует стандартная библиотека Python — logging.
Она позволяет вести журнал действий, что полезно для отладки,
анализа работы и мониторинга программ в реальном времени.

Мы рассмотрим, как настроить логирование,
выбрать уровень важности сообщений и добавить дату, используя библиотеку datetime,
чтобы иметь возможность точно отслеживать, когда происходят события в приложении.
Мы выясним, как данные инструменты используются для улучшения работы программ.

На занятии мы:

научимся настраивать логирование в программе и устанавливать его различные уровни;
поймём, как записывать лог-сообщения в файл;
освоим создание объектов даты и времени;
научимся выполнять операции с объектами даты и времени и форматировать их для желаемого вывода.





На этапе разработки приложения логирование обязательно. Рассмотрим следующий пример.

Представим, что наша организация запускает ракету в космос.
В этом процессе задействовано множество подразделений, работа каждого обособлена,
но у них есть общая задача — подготовить ракету к запуску.
На каждом этапе они предоставляют отчёт о степени подготовки головному центру.
Аналогичная ситуация и в разработке.
Большое приложение чаще всего состоит из нескольких маленьких:
одно отвечает за хранение данных, другое обеспечивает связь с пользователем и т.д.,
а на этапе разработки программисту нужно контролировать «поведение» каждого из них.
Для этого и нужно логирование.




Логирование в Python (библиотека logging)
Основы библиотеки logging
Как уже было сказано, ни один крупный проект в разработке не обходится без логирования.
Библиотека logging предоставляет механизм для записи логов (журналирования) в Python.
Логи являются важной частью разработки, так как они помогают отслеживать поведение программы,
диагностировать ошибки и анализировать её работу.
В Python для этой цели используется стандартная библиотека logging.

Возвращаясь к занятию про тестирование, возникает вопрос: для чего нужно logging?
Ведь мы же рассматривали вариант, при котором в местах, где могут произойти ошибки,
расставляли print, и это работало?

На самом деле, так и есть.
Однако тогда нам самостоятельно придётся контролировать дату вывода сообщения,
его сохранение в файл, корректное отображение.
Чтобы каждый раз об этом не думать, программисты и используют библиотеку logging.

Этот рисунок визуализирует работу системы логирования в Python.
Представим, что мы управляем почтовым отделением, где сообщения — это письма,
которые нужно доставить, а разные этапы логирования — это этапы обработки этих писем.






Компоненты логирования
1. Logger (логгер) — центр управления

Logger — это как главный офис почтовой службы.
Он получает все письма (логи), решает, что с ними делать, и отправляет их дальше.
У Logger есть несколько функций:

SetLevel устанавливает минимальный уровень важности писем.
Например, принимает только срочные письма (ERROR) или любые, даже рекламные листовки (DEBUG).

AddHandler подключает курьеров, которые будут доставлять письма.
Курьеры могут быть разными: кто-то работает с бумажной почтой, а кто-то — с электронной.

AddFilter (фильтры) — это как проверка писем на содержание.
Например, принимает только те письма, которые относятся к финансам, а остальные — игнорирует.




2. Handlers (обработчики) — служба доставки

Handlers — это своего рода курьеры, которые доставляют письма по назначению. Каждый Handler имеет свои настройки:

1) StreamHandler — этот курьер работает с потоками данных, например,
выводит сообщения в консоль, как если бы он зачитывал письма вслух.

SetLevel настраивает, какие письма он берёт с собой (например, только срочные).
SetFormatter устанавливает формат писем. Это как выбирать,
писать письмо от руки или печатать его на компьютере.
AddFilter применяет фильтр, чтобы брать только подходящие письма.
2) FileHandler — этот курьер доставляет письма в файл, то есть сохраняет логи на диск.

Настройки аналогичны StreamHandler, но конечная доставка происходит не в консоль, а в файл.

3. Formatter (форматирование) — стиль писем

Formatter — это как шаблон для письма. Он решает, как будет выглядеть письмо:
какой шрифт, порядок текста и подпись.

Например:

Для писем об ошибках можно использовать красный текст и указание точного времени.
Для информационных писем — простой текст с минимальными деталями.



4. Filter (фильтр) — проверка писем

Filter — это как отдел цензуры, который проверяет письма перед отправкой. 
Он может решить, что письма с определённым содержанием (например, связанные с рекламой) не отправляются.

Образный пример

Допустим, у нас есть Logger, настроенный следующим образом:

Он принимает все письма уровня «срочные» (ERROR) и выше.

Он работает с двумя курьерами:

StreamHandler — зачитывает письма вслух в офисе.
FileHandler — сохраняет копии писем в архиве.
У каждого курьера есть свой шаблон писем:

StreamHandler выводит только текст сообщения.
FileHandler добавляет дату и время.
У Logger есть фильтр, который блокирует все письма, не связанные с темой «финансы».

В итоге, если Logger получает письмо об ошибке в финансовой системе, оно:

будет зачитано вслух через StreamHandler;
будет сохранено в архив через FileHandler.
Если же письмо касается другой темы, оно не пройдёт фильтр и будет проигнорировано.

Таким образом, система логирования позволяет не только собирать информацию, 
но и распределять её так, как нужно, упрощая диагностику проблем.

Уровни логирования
Логирование в Python поддерживает несколько уровней важности сообщений. 
Каждый уровень определяет тип информации, которую необходимо записать в лог:

DEBUG — наименее важный уровень, предназначен для диагностических сообщений.
INFO — уровень для информативных сообщений, 
обычно используется для указания нормального хода работы программы.
WARNING — уровень для предупреждений о возможных проблемах.
ERROR — уровень для сообщений об ошибках, которые не являются критическими.
CRITICAL — самый высокий уровень, используется для критических ошибок, 
которые могут привести к сбою программы.
Пример использования разных уровней логирования:

import logging

# Настройка базового конфигуратора
logging.basicConfig(level=logging.DEBUG)

logging.debug('Это сообщение отладочного уровня')
logging.info('Это информационное сообщение')
logging.warning('Это предупреждение')
logging.error('Это сообщение об ошибке')
logging.critical('Это критическая ошибка')
2025-01-08 22:01:21,646 - DEBUG - Это сообщение отладочного уровня
2025-01-08 22:01:21,646 - INFO - Это информационное сообщение
2025-01-08 22:01:21,647 - WARNING - Это предупреждение
2025-01-08 22:01:21,648 - ERROR - Это сообщение об ошибке
2025-01-08 22:01:21,649 - CRITICAL - Это критическая ошибка



Форматирование сообщений
Каждое сообщение в логе может быть отформатировано с использованием шаблонов,
которые включают различную информацию, такую как время, уровень логирования и само сообщение.
Формат определяется через объект Formatter.

Пример с использованием формата для лог-сообщений:

import logging

# Настройка логгера
logging.basicConfig(
    level=logging.DEBUG,
    format='%(asctime)s - %(levelname)s - %(message)s'
)

logging.debug('Это отладочное сообщение')
logging.info('Это информационное сообщение')
logging.warning('Это предупреждающее сообщение')
logging.error('Это ошибка')
2025-01-08 21:59:39,825 - DEBUG - Это отладочное сообщение
2025-01-08 21:59:39,825 - INFO - Это информационное сообщение
2025-01-08 21:59:39,825 - WARNING - Это предупреждающее сообщение
2025-01-08 21:59:39,825 - ERROR - Это ошибка
Формат можно настроить с использованием различных атрибутов:

%(asctime)s — время записи сообщения.
%(levelname)s — уровень важности сообщения (например, DEBUG, INFO).
%(message)s — текст сообщения.
%(filename)s — имя файла, из которого было вызвано лог-сообщение.
%(lineno)d — номер строки, где было вызвано лог-сообщение.



Обработчики для Logger в Python
Обработчики (Handlers) в библиотеке logging — это компоненты, 
которые отвечают за обработку лог-сообщений и определяют,
куда и как эти сообщения будут записаны. Логгер сам генерирует сообщения, 
а обработчики направляют их, например, в файл, консоль или по сети.

В Python есть несколько типов обработчиков, 
каждый из которых может быть использован в зависимости от требований к логированию.



Основные типы обработчиков

1. StreamHandler
Этот обработчик используется для вывода лог-сообщений на стандартный поток (например, в консоль).
Он по умолчанию выводит сообщения в консоль,
но можно настроить его и для вывода в любой поток, например, в файл.

Пример использования StreamHandler:

import logging

# Создание логгера
logger = logging.getLogger('example_logger')
logger.setLevel(logging.DEBUG)

# Создание обработчика для вывода в консоль
console_handler = logging.StreamHandler()
console_handler.setLevel(logging.INFO)

# Форматирование сообщений
formatter = logging.Formatter('%(asctime)s - %(levelname)s - %(message)s')
console_handler.setFormatter(formatter)

# Добавление обработчика к логгеру
logger.addHandler(console_handler)

# Логирование
logger.debug('Это сообщение DEBUG')
logger.info('Это сообщение INFO')
logger.warning('Это сообщение WARNING')
2025-01-08 22:03:37,210 - DEBUG - Это сообщение DEBUG
2025-01-08 22:03:37,210 - INFO - Это сообщение INFO
2025-01-08 22:03:37,210 - INFO - Это сообщение INFO
2025-01-08 22:03:37,213 - WARNING - Это сообщение WARNING
2025-01-08 22:03:37,213 - WARNING - Это сообщение WARNING
В этом примере все сообщения уровня INFO и выше будут выводиться в консоль.






2. FileHandler

Этот обработчик записывает лог-сообщения в файл.
Он полезен, когда нужно сохранить журнал работы программы для последующего анализа.

Пример использования FileHandler:

import logging

# Создание логгера
logger = logging.getLogger('example_logger')
logger.setLevel(logging.DEBUG)

# Создание обработчика для записи в файл
file_handler = logging.FileHandler('app.log')
file_handler.setLevel(logging.ERROR)

# Форматирование сообщений
formatter = logging.Formatter('%(asctime)s - %(levelname)s - %(message)s')
file_handler.setFormatter(formatter)

# Добавление обработчика к логгеру
logger.addHandler(file_handler)

# Логирование
logger.debug('Это сообщение DEBUG')
logger.error('Это сообщение ERROR')
2025-01-08 22:05:48,475 - DEBUG - Это сообщение DEBUG
2025-01-08 22:05:48,475 - ERROR - Это сообщение ERROR
2025-01-08 22:05:48,475 - ERROR - Это сообщение ERROR

После завершения работы программы или при изменении конфигурации 
логгера можно удалить обработчики с помощью метода RemoveHandler.




Работа с исключениями
Логирование также может быть полезным для записи информации о возникших исключениях.
Библиотека logging предоставляет метод exception,
который автоматически добавляет информацию об исключении.

Пример логирования исключения:

import logging

# Настройка логгера
logging.basicConfig(level=logging.ERROR)

try:
    result = 10 / 0
except ZeroDivisionError:
    logging.exception('Произошла ошибка при делении на ноль')
Метод exception записывает стек вызовов исключения и помогает анализировать,
где именно произошла ошибка.

В логировании очень важно отслеживать значения даты и времени.
Для этого мы сейчас ознакомимся с библиотекой, 
которая предоставляет такие возможности в Python.
Мы кратко ознакомимся с его возможностями, которые пригодятся нам при логировании.







Работа с датой и временем в Python (модуль datetime)
Модуль datetime в Python предоставляет инструменты для работы с датами и временем.
Этот модуль позволяет создавать, манипулировать и форматировать объекты даты и времени,
а также вычислять различные временные промежутки. 
Модуль включает несколько классов, которые позволяют работать с датами, 
временем и их комбинациями.



datetime.datetime

Класс datetime модуля datetime объединяет дату и время в одном объекте.
Он включает как дату (год, месяц, день), так и время 
(часы, минуты, секунды, микросекунды).
Чаще всего именно этот класс используется при логировании,
так как он является самым информативным.

Пример создания объекта datetime:

from datetime import datetime

# Текущая дата и время
now = datetime.now()
print(now)  # Например, 2025-01-08 14:30:45.123456

# Создание объекта datetime с конкретными значениями
custom_datetime = datetime(2023, 5, 15, 14, 30, 45)
print(custom_datetime)  # 2023-05-15 14:30:45
2025-01-08 22:21:12.742365
2023-05-15 14:30:45




datetime.timedelta
Класс timedelta представляет разницу между двумя объектами времени
(например, дни, секунды или микросекунды).
Он используется для выполнения арифметических операций с датами и временем.

Пример использования timedelta:

from datetime import datetime, timedelta

# Разница в 5 дней
delta = timedelta(days=5)
print(delta)  # 5 days, 0:00:00

# Арифметика с датами
today = datetime.today()
future_date = today + timedelta(days=10)
print(future_date)  # Текущая дата + 10 дней
5 days, 0:00:00
2025-02-14 12:42:37.340813
Основные операции с датами и временем
1. Получение текущей даты и времени
Чтобы получить текущие дату и время, используем метод datetime.now(), например:

from datetime import datetime

now = datetime.now()
print(now)  # Например, 2025-01-08 14:30:45.123456
2025-01-08 22:24:26.209428



2. Форматирование даты и времени
Для вывода даты и времени в определённом формате используем метод strftime().

Он позволяет форматировать объект datetime в строку по заданному шаблону.

Пример форматирования:

from datetime import datetime

now = datetime.now()
formatted_date = now.strftime('%Y-%m-%d %H:%M:%S')
print(formatted_date)  # 2025-01-08 14:30:45
Вот некоторые спецификаторы, которые можно использовать в шаблоне:

%Y — год (четырёхзначный);
%m — месяц (двухзначный);
%d — день (двухзначный);
%H — час (24-часовой формат);
%M — минуты;
%S — секунды.



3. Вычисление разницы между датами
Для вычисления разницы между двумя объектами datetime или date
используется оператор вычитания, который возвращает объект timedelta.

Пример:

from datetime import datetime

date1 = datetime(2025, 1, 1)
date2 = datetime(2025, 1, 8)

delta = date2 - date1
print(delta)  # 7 days, 0:00:00
print(delta.days)  # 7
print(type(delta))
7 days, 0:00:00
7
<class 'datetime.timedelta'>
В данном примере вычисляется разница между двумя датами, 
результатом является объект timedelta, 
который может быть использован для извлечения количества дней, секунд и т.д.





Типичные ошибки
Тип ошибки 1: Неуказание уровня логирования

Возникает, когда при настройке Logger не был указан уровень логирования, 
это приводит к игнорированию сообщений с более низким уровнем.

Пример ошибки:

import logging

logger = logging.getLogger('example_logger')


logger.debug('Это сообщение DEBUG')
logger.error('Это сообщение ERROR')

# вывод:
# ERROR:example_logger:Это сообщение ERROR
Видим, что сообщение DEBUG не было выведено.

Решение:

убедимся, что уровень логирования установлен, причём корректно,
чтобы отфильтровать сообщения по нужному уровню.

Пример:

import logging

logger = logging.getLogger('example_logger')
logger.setLevel(logging.DEBUG)

logger.debug('Это сообщение DEBUG')
logger.error('Это сообщение ERROR')

# вывод:
# DEBUG:example_logger:Это сообщение DEBUG
# ERROR:example_logger:Это сообщение ERROR




Тип ошибки 2: Использование неправильных форматов логирования

Возникает, когда при настройке Logger не был указан формат сообщений. 
Некорректное форматирование может привести к тому, 
что в логах будет недостаточно информации, например, 
нет времени события или уровня логирования.

Пример ошибки:

import logging

logger = logging.getLogger('example_logger')
logger.setLevel(logging.DEBUG)

console_handler = logging.StreamHandler()
console_handler.setLevel(logging.INFO)

# Форматирование сообщений
# formatter = logging.Formatter('%(asctime)s - %(levelname)s - %(message)s')
# console_handler.setFormatter(formatter)

logger.addHandler(console_handler)

logger.info('Это сообщение INFO')
logger.warning('Это сообщение WARNING')

# вывод:
# Это сообщение INFO
# Это сообщение WARNING
Решение:

настроим форматирование с использованием всех нужных полей: %(asctime)s, %(levelname)s, %(message)s.

Пример:

import logging

logger = logging.getLogger('example_logger')
logger.setLevel(logging.DEBUG)

console_handler = logging.StreamHandler()
console_handler.setLevel(logging.INFO)

formatter = logging.Formatter('%(asctime)s - %(levelname)s - %(message)s')
console_handler.setFormatter(formatter)

logger.addHandler(console_handler)

logger.info('Это сообщение INFO')
logger.warning('Это сообщение WARNING')

# вывод:
# 2025-01-17 15:56:22,428 - INFO - Это сообщение INFO
# 2025-01-17 15:56:22,428 - WARNING - Это сообщение WARNING









Практикум
Задание 1. Логирование для магазина

Ситуация: в интернет-магазине система обработки заказов генерирует ошибки
в процессе покупки товара (например, при неверно введённых данных пользователем). 
Эти ошибки должны быть логированы,
чтобы администраторы могли отслеживать их и быстро устранять. 
Нужно настроить систему логирования для записей ошибок,
а также обеспечить сохранение логов в файл, чтобы можно было анализировать ошибки в будущем.
Логи должны содержать информацию о времени, уровне ошибки и тексте.

Задача — настроить систему логирования, которая будет записывать все ошибки в файл order_errors.log.

Логи должны включать следующую информацию:

время возникновения ошибки;
уровень ошибки (например, ERROR);
текст ошибки.
Логирование должно работать только для сообщений с уровнем ERROR или выше.

Шаги реализации:

Импортируем модуль logging.
Создадим логгер с именем order_logger и установим уровень логирования ERROR.
Добавим обработчик FileHandler, чтобы все ошибки записывались в файл order_errors.log.
Настроим форматирование логов, чтобы в каждом сообщении были дата, уровень ошибки и текст.
Добавим логирование ошибки с уровнем ERROR в случае возникновения исключения.
Реализация:

import logging

# Создание логгера
logger = logging.getLogger('order_logger')
logger.setLevel(logging.ERROR)

# Создание обработчика для записи в файл
file_handler = logging.FileHandler('order_errors.log')
file_handler.setLevel(logging.ERROR)

# Настройка формата логов
formatter = logging.Formatter('%(asctime)s - %(levelname)s - %(message)s')
file_handler.setFormatter(formatter)

# Добавление обработчика к логгеру
logger.addHandler(file_handler)

# Пример логирования ошибки
try:
    # Симуляция ошибки при обработке заказа
    raise ValueError("Некорректные данные заказа")
except ValueError as e:
    logger.error(f"Ошибка обработки заказа: {e}")






Задание 2. Дата доставки

Ситуация: вы разрабатываете бэкенд для сайта интернет магазина. 
На текущем этапе необходимо разработать логику рассчитывания даты доставки заказа. 
Известно, что любой заказ придет не позже, чем через пять рабочих дней после оформления.

Задача — написать программу, которая вычисляет дату доставки заказа без учета выходных дней.

Шаги реализации:

Импортируем модули datetime и timedelta.
Создадим объект datetime для текущего времени.
Рассчитаем дату, прибавив 5 рабочих дней, исключив выходные.
Выведем полученную дату доставки.
Реализация:

from datetime import datetime, timedelta,

# Функция для вычисления даты с учётом рабочих дней
def add_weekdays(start_date, num_days):
    current_date = start_date
    while num_days > 0:
        current_date += timedelta(days=1)
        if current_date.weekday() < 5:  # Понедельник - пятница
            num_days -= 1
    return current_date

# Получаем текущее время
date_now = datetime.now()

# Рассчитываем дату доставки (5 рабочих дней)
delivery_date = add_weekdays(date_now, 5)

# Выводим результат
print(f"Дата доставки: {delivery_date.strftime('%Y-%m-%d %H:%M:%S')}")
Дата доставки: 2025-01-16 01:34:13
Итоги:
Сегодня на занятии мы:

научились настраивать логирование в программе;
освоили создание объектов даты и времени;
научились выполнять операции с датой и форматировать её для вывода в нужном виде.
Эти навыки очень важны для каждого программиста — при разработке любой программы важно 
отслеживать статус её выполнения и делать это правильно, в чём нам и помогают logging и datetime.