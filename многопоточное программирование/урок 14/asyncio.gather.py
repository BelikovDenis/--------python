На прошлом занятии мы познакомились с основами асинхронного программирования,
корутинами и тасками, а сегодня мы продолжим углубляться в тему и узнаем,
как эффективно управлять сразу несколькими асинхронными задачами с помощью asyncio.gather — инструмент, 
который позволяет запускать несколько задач одновременно и собирать их результаты в одном месте.

На этом занятии мы:

научимся использовать asyncio.gather для одновременного выполнения нескольких задач;
создадим примеры, демонстрирующие преимущества параллельного выполнения задач;
рассмотрим типичные ошибки и лучшие практики при использовании asyncio.gather.



Главное преимущество асинхронного программирования — это
возможность выполнять несколько задач одновременно. 
Сегодня мы разберёмся, как правильно управлять множеством задач и их результатами.



Что такое asyncio.gather?
Asyncio.gather — это функция,
которая позволяет запускать несколько асинхронных задач одновременно и собирать их результаты.
Она принимает на вход одну или несколько корутин (или тасок)
и возвращает результаты всех задач в виде списка (или другого объекта, если это требуется).

Основные преимущества asyncio.gather

Удобный способ запускать и отслеживать выполнение нескольких задач одновременно.

Задачи запускаются параллельно (в пределах одного потока), что позволяет экономить время.

Подходит для широкого спектра задач — от обработки данных до асинхронных запросов.

Пример использования asyncio.gather:

import asyncio

async def task1():
    await asyncio.sleep(2)
    return "Результат задачи 1"

async def task2():
    await asyncio.sleep(1)
    return "Результат задачи 2"

async def main():
    results = await asyncio.gather(task1(), task2())
    print(results)

asyncio.run(main()) # ['Результат задачи 1', 'Результат задачи 2']

Объяснение:

Task1 и task2 запускаются одновременно.

Asyncio.gather ждёт завершения этих задач.

Результаты этих задач собираются в список и выводятся на экран.

Для понимания давайте напишем тот же код,
но без использования этой инструкции, то есть так, как мы делали на предыдущем занятии:

import asyncio

async def task1():
    await asyncio.sleep(2)
    return "Результат задачи 1"

async def task2():
    await asyncio.sleep(1)
    return "Результат задачи 2"

async def main():
    result1 = await task1()
    result2 = await task2()
    print([result1, result2])

asyncio.run(main())



Различие между этими двумя примерами в том,
что в первом решении задачи выполняются якобы параллельно,
тогда как во втором они выполняются последовательно друг за другом,
что увеличивает время их выполнения,
так как мы сначала ждём завершения task1, а потом запускаем task2.



Выводы:

Без asyncio.gather задачи выполняются последовательно, каждая запускается только после завершения предыдущей.
С asyncio.gather задачи выполняются параллельно (конкурентно), что значительно экономит время.
Зачем и когда использовать asyncio.gather?
Asyncio.gather особенно полезна в ситуациях, 
когда нам нужно выполнить несколько асинхронных задач одновременно и дождаться их завершения.
Вот несколько примеров, когда рекомендуется её использовать:


1. Параллельная обработка данных. 
Если у нас есть несколько независимых задач, 
которые можно выполнять одновременно, asyncio.gather позволяет запустить их все сразу и дождаться завершения.

2. Асинхронные запросы к API. 
Когда нам нужно сделать несколько запросов к разным API,
asyncio.gather позволяет выполнить их одновременно, что значительно ускоряет процесс.

3. Обработка файлов. Если нам нужно обработать несколько файлов асинхронно,
asyncio.gather поможет запустить все задачи одновременно и дождаться их завершения.

4. Сбор данных. Когда нам нужно собрать данные из нескольких источников, 
asyncio.gather позволяет сделать это одновременно, что экономит время.

Представим, что нам нужно отправить несколько запросов к разным API.
Вместо того, чтобы ждать выполнения каждого запроса по очереди, мы можем отправить их одновременно:

import asyncio
import random

async def fetch_data(api_id):
    print(f"Запрос к API {api_id} начат.")
    await asyncio.sleep(random.randint(1, 3))  # Симуляция задержки ответа
    print(f"Запрос к API {api_id} завершён.")
    return f"Данные API {api_id}"

async def main():
    tasks = [fetch_data(i) for i in range(1, 4)]
    results = await asyncio.gather(*tasks)
    print("Все запросы выполнены:", results)

asyncio.run(main())

Пошаговый разбор

Функция fetch_data:

Эта корутина представляет запрос к API,
asyncio.sleep симулирует задержку, которая может возникнуть при ожидании ответа от сервера.

В реальной ситуации здесь может быть код для выполнения HTTP-запроса.

Создание задач:

В main мы создаём список задач, передавая в fetch_data разные api_id,
это позволяет нам работать с несколькими запросами одновременно.

Список задач: [fetch_data(1), fetch_data(2), ..., fetch_data(5)].

asyncio.gather:

Здесь все задачи запускаются одновременно,
gather возвращает результаты их выполнения в виде списка, сохраняя порядок задач.
В примере все запросы (fetch_data) стартуют одновременно.
asyncio.sleep (или другая блокирующая операция) освобождает управление другим задачам,
так что они продолжают выполняться. 
Общее время выполнения соответствует времени выполнения самой длинной задачи, 
так как все запросы осуществляются параллельно.

Давайте ещё раз рассмотрим на примере, что будет, если не использовать asyncio.gather:

async def main():
    # Последовательный запуск запросов
    result1 = await fetch_data(1)
    result2 = await fetch_data(2)
    result3 = await fetch_data(3)
    result4 = await fetch_data(4)
    result5 = await fetch_data(5)
    print("Все запросы выполнены.")
    print("Результаты:", [result1, result2, result3, result4, result5])

asyncio.run(main())



Объяснение:

Каждый запрос запускается только после завершения предыдущего.
Если каждый запрос выполняется 1-3 секунды,
то общее время выполнения будет равно суммарному количеству времени 
выполнения всех запросов (например, 10-15 секунд для пяти запросов).

Преимущества использования asyncio.gather

Экономия времени

Вместо выполнения запросов последовательно мы осуществляем их одновременно,
значительно сокращая общее время выполнения.
В приведённом примере общее время выполнения будет равно самой длительной задержке среди запросов (1–3 секунды).
Удобство управления

Все результаты собираются в одном месте. Мы можем обрабатывать их по мере необходимости, не усложняя код.
Теперь представим, как работает asyncio.gather, используя метафору с рестораном.

asyncio.gather — заказ нескольких блюд одновременно

Когда клиенты заказывают несколько блюд сразу (например, салат, суп и десерт),
официант (таска) передаёт их на кухню все разом.
Шеф-повар (Event Loop) распределяет заказы между поварами (корутинами), чтобы блюда готовились одновременно.

Что происходит с заказом в обычной ситуации?

Если официант относит заказы по одному (без asyncio.gather), то получается так:

Сначала он ждёт, пока приготовят салат.
Потом он относит следующий заказ — суп.
И только после этого заказывает десерт.
Это затягивает время, так как официант ждёт завершения каждого этапа, прежде чем перейти к следующему.

Что происходит с заказом с asyncio.gather?

asyncio.gather позволяет отдать все заказы сразу:

Официант приносит заказы салат, суп и десерт на кухню одновременно.
Шеф-повар распределяет блюда между поварами.
Один повар готовит салат.
Второй варит суп.
Третий делает десерт.
Все повара работают параллельно, а официант ждёт, пока все блюда будут готовы.
Когда всё завершено, официант возвращается с целым подносом и отдаёт блюда клиенту.

Мы освоили асинхронность на новом уровне и теперь знаем,
как запускать несколько задач одновременно с помощью asyncio.gather.
Это позволит нам существенно экономить время, параллельно выполняя независимые задачи.
Такой навык даёт возможность значительно оптимизировать работу в будущем,
когда мы будем взаимодействовать со сторонними ресурсами, такими как базы данных или API.




Типичные ошибки
Тип ошибки 1: Некорректная распаковка задач в asyncio.gather

Ошибка возникает, когда задачи передаются в gather в виде списка без распаковки.

Пример ошибки:

import asyncio

async def task():
    await asyncio.sleep(1)

async def main():
    tasks = [task(), task(), task()]
    await asyncio.gather(tasks)  # Ошибка: передача списка без распаковки

asyncio.run(main())


Решение:

используем оператор * для распаковки списка задач.

await asyncio.gather(*tasks)




Тип ошибки 2: Игнорирование исключений в задачах

Возникает, когда одна из задач вызывает исключение.
В этом случае gather завершается с ошибкой, если не использовать параметр return_exceptions=True.

Пример ошибки:

import asyncio

async def faulty_task():
    raise ValueError("Ошибка в задаче")

async def main():
    await asyncio.gather(faulty_task(), asyncio.sleep(1))

asyncio.run(main())  # ValueError: Ошибка в задаче
Решение:

обрабатываем исключения через return_exceptions=True.

results = await asyncio.gather(faulty_task(), asyncio.sleep(1), return_exceptions=True)





Тип ошибки 3: Передача не awaitable объектов

Ошибка возникает при передаче в gather объектов, которые не являются корутинами или задачами.

Пример ошибки:

import asyncio

def sync_function():
    return 42

async def main():
    await asyncio.gather(sync_function())  # Ошибка: функция не является корутиной

asyncio.run(main())
Решение:

обернём синхронную функцию в корутину.

async def wrapper():
    return sync_function()

await asyncio.gather(wrapper())





Тип ошибки 4: Отсутствие await перед asyncio.gather

Возникает при отсутствии await перед вызовом gather.

Пример ошибки:

import asyncio

async def task():
    await asyncio.sleep(1)

async def main():
    asyncio.gather(task(), task())  # Нет await, задачи не выполнятся

asyncio.run(main())
Решение:

всегда используем await с gather.

await asyncio.gather(task(), task())













Практикум
Задание 1. Мониторинг теплицы с использованием asyncio.gather

Ситуация: мы продолжаем разработку «умного дома» из предыдущего занятия. 
Напоминаю, что в данный момент мы работаем над системой, контролирующей микроклимат в теплице.
Для контроля условий выращивания растений установлены два датчика: 
температуры и влажности. В этот раз наша система должна гарантировать, 
что время прихода данных с датчиков (температуры и влажности) не превышает 3 секунд.

Задача:

Реализовать имитацию сбора данных с двух датчиков за заданное время
(случайные значения от 1 до 3 секунд, использовать библиотеку random).
Параллельно получать данные с обоих датчиков,
чтобы не терять время в ожидании завершения работы одного из них.
Реализация:

import asyncio
import random

async def read_temperature_sensor():
    """Имитация считывания данных с датчика температуры."""
    print("Считывание данных с датчика температуры...")
    await asyncio.sleep(random.uniform(1, 3))  # Имитация задержки
    temp = random.uniform(18, 30)  # Генерация случайной температуры
    print(f"Температура в теплице: {temp:.2f}°C")
    return temp

async def read_humidity_sensor():
    """Имитация считывания данных с датчика влажности."""
    print("Считывание данных с датчика влажности...")
    await asyncio.sleep(random.uniform(2, 4))  # Имитация задержки
    humidity = random.uniform(40, 70)  # Генерация случайной влажности
    print(f"Влажность в теплице: {humidity:.2f}%")
    return humidity

async def monitor_climate():
    """Мониторинг данных с датчиков температуры и влажности."""
    # Запускаем оба считывания с использованием asyncio.gather
    temperature_task = read_temperature_sensor()
    humidity_task = read_humidity_sensor()

    # Ожидаем завершения обеих задач
    results = await asyncio.gather(temperature_task, humidity_task)

    # Собираем результаты
    temperature, humidity = results
    print(f"Средняя температура: {temperature:.2f}°C")
    print(f"Средняя влажность: {humidity:.2f}%")

# Запуск мониторинга
asyncio.run(monitor_climate())







Задание 2. Сканирование портов

Ситуация: в нашей компании случилась утечка данных,
и отдел кибербезопасности предполагает,
что проблема в открытом порте на одном из серверов.
Нам поставили задачу написать утилиту, проверяющую наличие утечек данных.
Для ускорения работы нужно выполнять проверку нескольких портов одновременно.

Задача:

Написать программу, которая будет имитировать проверку списка портов.
Время проверки каждого порта составляет от 1 до 3 секунд (использовать модуль random).
После завершения проверки каждого порта вывести сообщение об успехе или ошибке (например, порт закрыт).
Все проверки должны выполняться параллельно.

Реализация:

import asyncio
import random

# Список портов для проверки
ports = [22, 80, 443, 8080, 3306]

async def check_port(port):
    """Симуляция проверки порта."""
    delay = random.uniform(1, 3)  # Время проверки порта
    print(f"Проверка порта {port} начата (займёт {delay:.2f} сек.).")
    await asyncio.sleep(delay)
    is_open = random.choice([True, False])  # Симуляция открытого/закрытого порта
    if is_open:
        print(f"Порт {port} открыт.")
    else:
        print(f"Порт {port} закрыт.")

async def main():
    print("Начинаем сканирование портов...\n")

    # Создаём таски для проверки портов
    tasks = [check_port(port) for port in ports]

    # Ожидаем завершения всех задач
    await asyncio.gather(*tasks)

    print("\nСканирование завершено.")

# Запуск
asyncio.run(main())





Итоги:
Сегодня мы:

научились использовать asyncio.gather для одновременного выполнения нескольких задач;
создали примеры, демонстрирующие преимущества параллельного выполнения задач;
рассмотрели типичные ошибки и лучшие практики при использовании asyncio.gather.
Теперь вы знаете, как управлять несколькими асинхронными задачами и
использовать asyncio.gather для их параллельного выполнения. Ура!