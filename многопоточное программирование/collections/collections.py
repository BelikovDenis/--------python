Очень часто разработчик сталкивается с задачами,
которые могут быть решены за счёт расширения возможностей стандартных коллекций.
Чтобы каждый раз не прописывать их логику, сегодня мы разберёмся с модулем collections.

Модуль collections в Python — это инструмент, который упрощает работу с данными.
Он предоставляет специализированные структуры данных,
которые расширяют возможности стандартных списков, словарей и кортежей.

Сегодня мы сосредоточимся на трёх из них: deque, Counter и defaultdict.

Эти коллекции предлагают более эффективные и гибкие решения для задач,
которые часто встречаются в программировании: подсчёт элементов, группировка данных,
управление очередями и многое другое.

Использование продвинутых коллекций делает код не только быстрее, но и легче для понимания.

На этом занятии мы освоим инструменты модуля collections, в частности:

изучим, как использовать deque для эффективной работы с очередями и двухсторонними структурами данных;
разберёмся с Counter, чтобы быстро и удобно подсчитывать элементы в коллекциях;
поймём, как работает defaultdict и зачем нужны значения по умолчанию;
узнаем, как выбирать подходящую структуру данных для конкретной задачи.
В итоге мы сможем не только уверенно использовать инструменты из модуля collections,
но и понимать, когда они подходят лучше всего для решения задач.


Глоссарий к седьмому занятию
Deque (Двухсторонняя очередь)

Deque — структура данных, поддерживающая быстрое добавление и удаление элементов с обеих сторон.

Counter (Счётчик)

Counter — коллекция для подсчёта количества элементов в наборе данных.

Defaultdict (Словарь со значениями по умолчанию)

Defaultdict — словарь, который автоматически создаёт значение для нового ключа при его доступе.

Mutable / Immutable (Изменяемый / неизменяемый)

Mutable / Immutable — описывают, можно ли изменять объект после его создания.
Например, deque — изменяемый, а tuple — неизменяемый.

FIFO (Первым пришёл, первым ушёл)

FIFO (First In First Out) — способ организации данных, при котором данные,
которые «пришли» первыми, уходят первыми.

LIFO (Последним пришёл, первым ушёл)

LIFO (Last In First Out) — способ организации данных, при котором данные,
которые «пришли» последними, уходят первыми.



Нередко встречаются задачи, в которых нестандартные контейнеры проявляют себя намного эффективнее,
в сравнении с уже известными нам. Сегодня мы рассмотрим некоторые из таких контейнеров.

Deque
Deque (double-ended queue «двухсторонняя очередь») — это структура данных из модуля collections,
которая позволяет эффективно добавлять и удалять элементы с обеих сторон.

В отличие от стандартных списков Python,
операции добавления и удаления элементов на концах deque выполняются значительно быстрее
благодаря внутренней реализации.

Для создания объекта deque нужно импортировать его из модуля collections:

from collections import deque

Создание пустой очереди:

d = deque()
Создание очереди с начальными элементами:

d = deque([1, 2, 3])

Когда использовать deque?

Если нужно часто добавлять или удалять элементы в начале или в конце коллекции.
Для реализации задач с очередями (FIFO) или стеками (LIFO).
Когда нужно ограничить длину коллекции, чтобы сохранить только последние элементы.
Основные методы deque

Добавление элементов:

append(value) добавляет элемент в конец очереди;

appendleft(value) добавляет элемент в начало очереди.

Пример:

d = deque()
d.append(10)
d.appendleft(5)
d
>>> deque([5, 10])


2. Удаление элементов:

pop() удаляет элемент с конца;

popleft() удаляет элемент с начала.

Пример:

d = deque([1, 2, 3])
d.pop()
>>> 3

d
>>> deque([1, 2])

d.popleft()
>>> 1

d
>>> deque([2])



3. Индексация по очереди:

к элементам можно обращаться по индексу.

d = deque([1, 2, 3, 4])
d[0]
>>> 1

d[-1]
>>> 4

d[-2]
>>> 3




4. Ограничение длины очереди:

deque позволяет задавать максимальную длину, и, если количество элементов
превышает заданное значение, старые элементы удаляются.

d = deque(maxlen=3)
d.append(1)
d.append(2)
d.append(3)
d
>>> deque([1, 2, 3], maxlen=3)

d.append(4)  # [2, 3, 4]
d
>>> deque([2, 3, 4], maxlen=3)

Как мы видим, после добавления четвёртого элемента в очередь максимальной длины 3 первый элемент из неё удалился.

Перестановка элементов:
rotate(n) сдвигает элементы на n позиций вправо (если n>0) или влево (если n<0).

d = deque([1, 2, 3, 4])
d.rotate(1)
d
>>> deque([4, 1, 2, 3])

d.rotate(-2)
d
>>> deque([2, 3, 4, 1])

Преимущества deque

Быстродействие — операции добавления и удаления элементов на концах работают за O(1),
в отличие от списков, где они могут занимать O(n).
Гибкость — deque легко использовать для реализации стеков,
очередей и даже циклических буферов благодаря возможности ограничения длины.

Здесь должен возникнуть вопрос: что за O(1) и O(n)?

Представим, что мы идём в магазин за хлебом. Если магазин рядом, мы потратим 5 минут.
А если он далеко, то 30 минут.
Теперь представим, что нам нужно ходить за хлебом каждый день на протяжении года.
Важно понимать, как растут затраты времени в зависимости от расстояния до магазина.

Нотация O большое — это способ описать, как «быстро» или «медленно» увеличивается время (или количество шагов),
если задачи становятся больше. Например:

O(1) — магазин прямо у двери, всегда тратим одинаковое время, хоть раз в год, хоть сто;
O(n) — мы проходим 1 километр за 10 минут, а 2 километра — за 20 минут,
время растёт прямо пропорционально расстоянию;
O(n²) — представим, что на каждом метре дороги нам нужно выкопать яму.
Чем больше метров, тем больше ям, и количество работы растёт очень быстро.
Это как волшебный инструмент, который помогает программистам понять,
насколько их алгоритмы будут «медленными», если работы станет больше.

Counter
Counter — это специальный словарь из модуля collections,
предназначенный для подсчёта количества объектов в коллекции.

Он автоматически подсчитывает, сколько раз каждый элемент встречается в данных,
и представляет результаты в виде словаря, где ключ — это элемент, а значение — количество его появлений.

Примеры использования:

Из списка
Counter([1, 2, 2, 3, 3, 3])
>>> Counter({3: 3, 2: 2, 1: 1})

Из строки
Counter("abracadabra")
>>> Counter({'a': 5, 'b': 2, 'r': 2, 'c': 1, 'd': 1})

Из словаря
Counter({'a': 2, 'b': 1})
>>> Counter({'a': 2, 'b': 1})

Когда использовать Counter?

Если нужно быстро подсчитать частоту элементов в коллекции.
Для обработки текстов (подсчёт слов или символов).
В задачах анализа данных, где важно понимать распределение элементов.
Основные методы и операции:

Подсчёт элементов:

Counter автоматически подсчитывает количество каждого элемента.

data = ['apple', 'banana', 'apple', 'orange', 'banana', 'banana']
counter = Counter(data)
counter
>>> Counter({'banana': 3, 'apple': 2, 'orange': 1})
2. Доступ к значениям:

доступ к подсчёту для конкретного элемента можно получить по ключу.

counter['banana']
>>> 3
counter['grape']
>>> 0
Обратим внимание! В случае, если элемент отсутствует, возвращается 0 (как во втором случае).

3. Метод most_common():

возвращает список самых частых элементов в порядке убывания их частоты.

counter.most_common(2)
>>> [('banana', 3), ('apple', 2)]


4. Метод update():

позволяет обновить счётчик, добавляя новые элементы или увеличивая количество уже существующих.

counter.update(['apple', 'banana', 'grape'])
counter
>>> Counter({'banana': 4, 'apple': 3, 'orange': 1, 'grape': 1})


5. Арифметические операции:

объекты Counter поддерживают сложение, вычитание, пересечение и объединение.

Сложение:
c1 = Counter(a=2, b=1)
c2 = Counter(a=1, b=2)
c1 + c2
>>> Counter({'a': 3, 'b': 3})
Вычитание:
c1 - c2
>>> Counter({'a': 1})
Пересечение:
c1 & c2
>>> Counter({'a': 1, 'b': 1})
Объединение:
c1 | c2
>>> Counter({'a': 2, 'b': 2})
Примеры использования Counter:

Подсчёт символов в строке
text = "hello world"
Counter(text)
>>> Counter({'l': 3, 'o': 2, 'h': 1, 'e': 1, ' ': 1, 'w': 1, 'r': 1, 'd': 1})


2. Поиск самых частых элементов

words = ["python", "java", "python", "c++", "java", "python"]
counter = Counter(words)
counter.most_common(1)
>>> [('python', 3)]


3. Подсчёт частоты чисел в списке

numbers = [1, 2, 2, 3, 3, 3, 4, 4, 4, 4]
Counter(numbers)
>>> Counter({4: 4, 3: 3, 2: 2, 1: 1})

Преимущества Counter

Удобный синтаксис для подсчёта.
Множество встроенных методов для обработки данных.
Возможность использования арифметических операций для объединения или сравнения счётчиков.




Defaultdict
Defaultdict — это подкласс стандартного словаря Python,
который позволяет задавать значения по умолчанию для новых ключей.
В случае использования defaultdict сложные вложенные структуры создаются значительно быстрее.

При обращении к несуществующему ключу defaultdict автоматически создаёт значение для него,
используя функцию, переданную при создании объекта.

Пример создания defaultdict:

from collections import defaultdict

dd = defaultdict(list)

dd = defaultdict(int)

Особенности defaultdict

Автоматическое создание значений:
при обращении к несуществующему ключу он автоматически добавляется в словарь с заданным значением по умолчанию.
Функция по умолчанию:
функция, переданная при создании, используется для генерации значений (например, list, int, str).
Когда использовать defaultdict?

Когда нужно работать со словарями, инициализируя значения по умолчанию для новых ключей.
Для подсчёта элементов (аналогично Counter).
Для группировки данных.
В задачах, где часто приходится проверять существование ключей.
Примеры использования defaultdict:

С list:

полезно для группировки данных
dd = defaultdict(list)
dd['a'].append(1)
dd['b'].append(2)
dd['a'].append(3)
dd
>>> defaultdict(<class 'list'>, {'a': [1, 3], 'b': [2]})



2. С int:

часто используется для подсчёта элементов
dd = defaultdict(int)
data = ['apple', 'banana', 'apple', 'orange', 'banana', 'banana']
for fruit in data:
  dd[fruit] += 1
dd
>>> defaultdict(<class 'int'>, {'apple': 2, 'banana': 3, 'orange': 1})



3. С пользовательской функцией:

можем использовать свою функцию для задания значений по умолчанию
def default_value():
  return "значение по умолчанию"
dd = defaultdict(default_value)
dd['missing_key']
>>> "значение по умолчанию"




4. Группировка данных:

использование defaultdict для группировки элементов по ключу
data = [('a', 1), ('b', 2), ('a', 3), ('b', 4)]
grouped = defaultdict(list)
for key, value in data:
  grouped[key].append(value)
grouped
>>> defaultdict(<class 'list'>, {'a': [1, 3], 'b': [2, 4]})

Преимущества defaultdict

Удобство — устраняет необходимость проверять наличие ключа в словаре перед его использованием.
Гибкость — позволяет задавать любую функцию для генерации значений по умолчанию.
Уменьшение кода — упрощает и делает код более читаемым.





Типичные ошибки
Тип ошибки 1: Ограничение длины deque

Старые элементы неожиданно удаляются при добавлении новых.

Пример ошибки:

from collections import deque
d = deque([1, 2, 3], maxlen=3)
d
>>> deque([1, 2, 3])
d.append(5)
d
>>> deque([2, 3, 5])

Решение:

убедимся, что длина задаётся правильно.

d = deque(maxlen=3)
d.append(1)
d.append(2)
d.append(3)
d.append(4)
d
>>> [2, 3, 4]






Тип ошибки 2: Неправильное использование Counter с итерируемыми объектами

Ошибка возникает, когда мы пытаемся создать Counter для данных,
которые не являются итерируемыми объектами, например, словаря или числовых значений.

Пример ошибки:

from collections import Counter

# Ошибка, передаем словарь в Counter
counter = Counter({"a": 2, "b": 3})
print(counter)  # Вывод: Counter({'a': 2, 'b': 3})

# Но это не то, что нам нужно. Лучше передавать список или строку
counter = Counter(["a", "a", "b", "b", "b"])
print(counter)  # Вывод: Counter({'b': 3, 'a': 2})


Решение:

убедимся, что мы передаём итерируемый объект (например, строку или список) в Counter.
Если нужно подсчитать частоту для более сложных данных, передаём соответствующую коллекцию.





Тип ошибки 3: Неправильная функция для значения по умолчанию

Ошибка возникает, если мы используем функцию с аргументами для создания значения по умолчанию.

Пример ошибки:

from collections import defaultdict
dd = defaultdict(list())
---------------------------------------------------------------------------
TypeError                                 Traceback (most recent call last)
<ipython-input-7-8aacf77a7878> in <cell line: 3>()
      1 from collections import defaultdict
      2
----> 3 dd = defaultdict(list())

TypeError: first argument must be callable or None

Решение:

используем функции без аргументов.








Практикум
Задание 1. Очередь обработки задач с ограничением

Ситуация: мы создаём систему обработки задач,
где одновременно можно хранить только последние 5 задач для обработки.
Если добавляется новая, самая старая автоматически удаляется.
Нам нужно реализовать эту систему с использованием deque.

Задача — создать класс TaskQueue, который:

Инициализируется с максимальной длиной очереди.
Имеет метод add_task(task), который добавляет новую задачу в очередь.
Имеет метод get_tasks(), который возвращает список текущих задач.
Используем deque с параметром maxlen для реализации.

Шаги реализации:

Создадим класс TaskQueue и инициализируем его объектом deque с заданной максимальной длиной.
Реализуем метод add_task(task) для добавления задач.
Реализуем метод get_tasks() для получения всех текущих задач в очереди.
Реализация:


from collections import deque

class TaskQueue:
    def __init__(self, max_length):
        self.queue = deque(maxlen=max_length)

    def add_task(self, task):
        self.queue.append(task)

    def get_tasks(self):
        return list(self.queue)







Задание 2. Подсчёт продуктов, группировка по категориям

Ситуация: мы работаем над приложением для продуктового магазина.
У нас есть список покупок, где каждый элемент представляет собой кортеж (категория, продукт).
Нам нужно сгруппировать продукты по категориям, чтобы упростить подсчёт.

Задача — реализовать функцию group_products_by_category(items),
которая принимает список кортежей (категория, продукт) и возвращает словарь,
где ключи — категории, а значения — списки продуктов.
Использовать defaultdict для упрощения группировки.

Шаги реализации:

Создаём defaultdict, где значения — это списки.
Пройдёмся по каждому элементу списка покупок.
Добавим продукт в соответствующую категорию.
Вернём итоговый словарь.
Реализация:

from collections import defaultdict

def group_products_by_category(items: list) -> defaultdict:
    # Создаём defaultdict с типом list для автоматической инициализации
    grouped = defaultdict(list)

    # Проходим по каждому элементу списка
    for category, product in items:
        grouped[category].append(product)  # Добавляем продукт в соответствующую категорию

    return grouped

items = [
    ("Фрукты", "Яблоко"),
    ("Овощи", "Морковь"),
    ("Фрукты", "Банан"),
    ("Молочные продукты", "Молоко"),
    ("Овощи", "Картофель")
]

result = group_products_by_category(items)







Итоги:
Мы выяснили, что в некоторых задачах использование обычных коллекций может оказаться недостаточным.
В подобных случаях нам на помощь и приходят их расширенные варианты,
с которыми мы сегодня познакомились. Сегодня мы:

изучили коллекцию deque и научились работать с двухсторонними очередями,
которые позволяют эффективно добавлять и удалять элементы как с начала, так и с конца коллекции;
рассмотрели, как использовать Counter для подсчёта частоты появления элементов в коллекциях,
таких как строки, списки и другие;
разобрались, как defaultdict упрощает работу с отсутствующими ключами в словарях.