В Python конструкция with и менеджеры контекста обеспечивают
удобный и безопасный способ работы с ресурсами, такими как файлы, базы данных или соединения.

Менеджеры контекста автоматически управляют началом и завершением работы с ресурсами,
что позволяет избежать ошибок, связанных с их неосвобождением, например, с забытым закрытием файла.

На занятии мы:

изучим концепцию менеджеров контекста в Python;
ознакомимся с использованием конструкции with;
научимся создавать собственные менеджеры контекста;
разберём типичные ошибки при работе с менеджерами контекста;
применим полученные знания на практике.



В процессе решения задач нам нередко будут встречаться объекты,
которые требуют дополнительных действий при начале работы с ними и её завершении.
Например, при работе с файлом нам сначала надо его открыть, а после того,
как мы считаем из него весь текст, надо не забыть его закрыть.
Именно в таких ситуациях и пригодятся менеджеры контекста.

 
Менеджеры контекста
Менеджеры контекста — это объекты, которые позволяют организовать работу с ресурсами
(файлы, соединения с базами данных, блокировки и т.д.) так,
чтобы они автоматически освобождались по завершении работы,
даже если в процессе возникли ошибки.
Другими словами, это помощники в работе с какими-либо объектами.

Для этого используются два основных метода:

__enter__() вызывается при входе в блок with.
Обычно этот метод выполняет необходимые действия для подготовки ресурса
(например, открывает файл или создаёт соединение).

__exit__() вызывается при выходе из блока with и предназначен для очистки ресурса
(например, закрытия файла или завершения работы с базой данных).
Этот метод также может обрабатывать исключения, если они возникли в теле блока with.



Основы работы с конструкцией with
Конструкция with применяется,
когда нужно гарантированно выполнить некую очистку ресурсов после завершения работы с ними.

Например, это особенно полезно при работе с файлами,
когда их нужно закрыть после завершения работы, или при работе с многозадачностью,
где важно освободить блокировки.


Пример использования менеджера контекста с файлом:

with open('example.txt', 'r') as file:
    content = file.read()
    print(content)

Здесь open('example.txt', 'r') является менеджером контекста, который открывает файл для чтения.
Когда выполнение выходит из блока with, метод __exit__() автоматически закроет файл, освобождая ресурс.

Пример с собственным менеджером контекста

Создадим свой менеджер контекста для работы с текстовым файлом.
Он будет записывать данные в файл и автоматически его закрывать:

class FileWriter:
    def __init__(self, filename):
        self.filename = filename
        self.file = None

    def __enter__(self):
        self.file = open(self.filename, 'w')  # Открытие файла на запись
        return self  # Возвращаем объект менеджера для записи данных

    def write(self, text):
        self.file.write(text)  # Запись данных в файл

    def __exit__(self, exc_type, exc_value, traceback):
        if self.file:
            self.file.close()  # Закрытие файла после работы с ним

# Использование:
with FileWriter('output.txt') as writer:
    writer.write("Hello, world!")

В методе __enter__ открывается файл для записи.
В методе write выполняется запись данных в файл.
В методе __exit__ файл закрывается автоматически вне зависимости от того,
возникли ли ошибки в блоке with.

Обработка исключений в менеджере контекста
Менеджеры контекста могут обрабатывать исключения, возникающие в теле блока with.

Для этого метод __exit__ принимает три аргумента:

exc_type — тип исключения (если оно возникло);
exc_value — само исключение;
traceback — объект трассировки, который можно использовать для диагностики ошибки.
Если метод __exit__ возвращает True, то исключение не будет выброшено,
иначе оно было бы передано дальше в код.

Например, если мы не хотим, чтобы ошибка прерывала выполнение программы,
можем вернуть True из метода __exit__:

class FileWriter:
    def __init__(self, filename):
        self.filename = filename
        self.file = None

    def __enter__(self):
        self.file = open(self.filename, 'w')
        return self

    def write(self, text):
        self.file.write(text)

    def __exit__(self, exc_type, exc_value, traceback):
        if exc_type:
            print(f"Ошибка: {exc_value}")
        self.file.close()
        # Если возвращаем True, исключение не будет поднято
        return True

with FileWriter('output.txt') as writer:
    writer.write("Hello, world!")
    raise Exception("Ошибка при записи!")


Даже если в блоке with возникнет ошибка, метод __exit__ не пропустит её дальше,
потому что возвращаемое значение True указывает на то, что мы обработали исключение.

Создание сложных менеджеров контекста
Теперь рассмотрим создание более сложного менеджера контекста,
который будет работать с несколькими ресурсами, например, с базой данных и файлом одновременно.

Для этого мы можем реализовать несколько методов __enter__() и __exit__() для разных ресурсов:

class DBConnection:
    def __enter__(self):
        print("Открываем соединение с базой данных")
        return self

    def __exit__(self, exc_type, exc_value, traceback):
        print("Закрываем соединение с базой данных")
        if exc_type:
            print(f"Произошла ошибка: {exc_value}")
        return True

class FileWriter:
    def __enter__(self):
        self.file = open("log.txt", "w")
        return self

    def __exit__(self, exc_type, exc_value, traceback):
        self.file.close()
        if exc_type:
            print(f"Ошибка в файле: {exc_value}")
        return True

# Использование двух менеджеров контекста
with DBConnection() as db, FileWriter() as file:
    file.file.write("Запись в лог файл")
    print("Работаем с базой данных")

Здесь мы используем два менеджера контекста одновременно,
управляя соединением с базой данных и записью в файл в одном блоке with.
Это пример сложного использования менеджеров контекста.





Типичные ошибки
Тип ошибки 1: Не обрабатываются исключения в __exit__()

Возникает, когда разработчик забывает обработать исключения.

Пример ошибки:

class MyContextManager:
    def __enter__(self):
        print("Входим в контекст")
        return self

    def __exit__(self, exc_type, exc_value, traceback):
        print("Выходим из контекста")
        # Неправильное поведение: исключение игнорируется
        if exc_type:
            print(f"Произошло исключение: {exc_value}")
        # Возвращаем True, чтобы "поглотить" исключение
        return True

with MyContextManager():
    print("Внутри контекстного менеджера")
    raise ValueError("Это ошибка!")


Решение:

метод __exit__() должен обрабатывать исключения или хотя бы логировать их,
иначе они могут быть неожиданно перехвачены и вызвать сбои в программе.

Корректная реализация:

class MyContextManager:
    def __enter__(self):
        print("Входим в контекст")
        return self

    def __exit__(self, exc_type, exc_value, traceback):
        print("Выходим из контекста")
        if exc_type:
            # Логируем ошибку
            print(f"Произошло исключение: {exc_value}")
            # Не возвращаем True, чтобы исключение всплыло дальше
            return False

with MyContextManager():
    print("Внутри контекстного менеджера")
    raise ValueError("Это ошибка!")






Тип ошибки 2: Неправильная работа с ресурсами

Возникает, когда разработчик забывает закрыть файл или соединение с базой данных в случае ошибок.

Пример ошибки:

class OpenToRead:
    def __init__(self, filename):
        self.filename = filename
        self.file = None

    def __enter__(self):
        self.file = open(self.file, 'r')

    def __exit__(self, exc_type, exc_value, traceback):
        pass  # завершаем метод, не закрыв файл


Решение:

корректно реализовываем логику освобождения ресурсов.

Корректная реализация:

class OpenToRead:
    def __init__(self, filename):
        self.filename = filename
        self.file = None

    def __enter__(self):
        self.file = open(self.file, 'r')

    def __exit__(self, exc_type, exc_value, traceback):
        self.file.close()






Практикум
Задание 1. Работа с файлами

Ситуация: мы работаем с текстовыми файлами и часто открываем и закрываем их.
Чтобы избежать ошибок, связанных с забытым закрытием файла, используем менеджеры контекста.

Задача — написать код, который создаёт файл, записывает в него строку,
а затем считывает содержимое и выводит на экран. Использовать конструкцию with для работы с файлом.

Шаги реализации:

Создадим файл с использованием менеджера контекста.
Запишем текст в файл.
Считаем текст из файла и выведем его.







Реализация:

with open('example.txt', 'w') as f:
    f.write("Привет, мир!")

with open('example.txt', 'r') as f:
    content = f.read()
    print(content)








Задание 2. Базы данных

Ситуация: мы работаем с подключениями к базе данных и должны гарантировать,
что соединение будет закрыто после работы независимо от того, возникли ли ошибки.

Задача — реализовать менеджер контекста для управления соединением с базой данных (имитация).

Шаги реализации:

Создадим класс с методами __enter__ и __exit__.
Метод __enter__ должен возвращать соединение.
Метод __exit__ должен закрывать соединение.
Реализация:

class DBConnection:
    def __enter__(self):
        print("Открываем соединение с базой данных")
        return self  # Возвращаем "соединение"

    def __exit__(self, exc_type, exc_value, traceback):
        print("Закрываем соединение с базой данных")
        if exc_type:
            print(f"Произошла ошибка: {exc_value}")
        return True  # Если возвращаем True, исключение не будет поднято

# Использование
with DBConnection():
    print("Работаем с базой данных")









Итоги:
Подводя итог, можно сказать, что менеджеры контекста предоставляют нам
безопасный и стабильный доступ к объектам и ресурсам.
В работе они часто используются при взаимодействии с базами данных или API сервисов.
Именно поэтому нам необходимо знать, как работают менеджеры контекста и как их применять.

На сегодняшнем занятии мы:

изучили работу менеджеров контекста в Python;
научились использовать конструкцию with;
создали собственный менеджер контекста.
Эти знания помогут нам корректно работать со сторонними ресурсами,
что является очень важным в работе разработчика.
Любой программист каждый день работает с файлами,
базами данных и так далее — в этих случаях необходимо правильно распоряжаться ресурсами системы,
что и является основной ролью менеджеров контекста.
