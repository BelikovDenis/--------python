"""Сегодня мы погрузимся в работу со сторонними пакетами и виртуальными средами в Python. 
Эти инструменты расширяют возможности нашего кода и помогают поддерживать порядок в проектах.

Возможно, вы уже сталкивались с ситуациями,
где нужно установить дополнительную библиотеку для выполнения задачи.
Именно здесь «на сцену» выходит pip — менеджер пакетов Python,
который делает процесс установки библиотек максимально простым.

Сегодня мы научимся находить и устанавливать сторонние пакеты,
управлять их версиями и понимать, как они влияют на наши проекты.


Проведём такую аналогию: представим, что мы живём в большом доме,
где, помимо нас, живут ещё пять человек.
У каждого есть свои посуда, постельное бельё и продукты.
Чтобы не перепутать между собой личные вещи, мы храним их в личной комнате,
а не на кухне, доступ к которой есть у всех.

Так и в Python: virtualenv позволяет изолировать окружение каждого проекта,
создавая для него собственную «песочницу»:
все библиотеки устанавливаются именно в неё и не конфликтуют с остальными, установленными в глобальное окружение.

Это особенно полезно, когда мы работаем над несколькими проектами, требующими разных версий библиотек.

Эти знания пригодятся не только для разработки масштабных программ,
но и для поддержания порядка в нашем рабочем процессе.
К концу занятия мы сможем уверенно использовать пакеты Python
и настраивать среду для проектов так, чтобы работа с ними была быстрой, удобной и предсказуемой.

На этом занятии мы:

изучим, что такое менеджер пакетов pip и для чего он используется;

научимся устанавливать, обновлять и удалять сторонние пакеты;

освоим команды для управления зависимостями;

узнаем, что такое виртуальная среда и почему она важна для изоляции проектов;

настроим рабочий процесс для проектов с зависимостями;

поговорим о важности изоляции проектов.




Глоссарий к пятому занятию

Package (Пакет) — это сборник кода (модулей), упакованный для распространения. Например, библиотека requests — это пакет.

Dependency (Зависимость) — это другой пакет, от которого зависит работа нашего проекта или установленного пакета.

Environment (Среда) — контекст, в котором работает Python, включая установленные библиотеки и настройки.

Virtual Environment (Виртуальная среда) — изолированная копия Python, где каждая библиотека устанавливается отдельно от глобальной системы.

Install (Установить) — загрузить и добавить пакет в нашу среду.

Requirement (Требование, зависимость) — описание пакетов, которые необходимы нашему проекту. Обычно хранится в файле requirements.txt.

Freeze (Заморозить) — команда для фиксации текущих версий всех установленных пакетов.

Activate/Deactivate (Активировать/Деактивировать) — включить/выключить виртуальную среду для работы.


В реальном мире разработчик в Python в основном работает с какой-либо сторонней библиотекой,
а то и с несколькими. В то же время у отдельного разработчика в один момент может быть несколько проектов,
и все они используют разные модули.
Для того чтобы ему не запутаться во всей этой массе модулей,
были придуманы различные инструменты, с которыми мы сейчас и познакомимся.

Pip — менеджер пакетов Python
Нередко разработчик сталкивается с ситуацией,
когда в проекте требуется дополнительный функционал,
на написание которого может уйти уйма времени.
Однако у Python огромное количество пользователей,
и зачастую необходимый функционал уже написан до нас — с перечнем сторонних библиотек
можно ознакомиться на сайте PyPI.
Но не стоит забывать и о том, что в Python есть свои встроенные модули,
которые не нужно устанавливать, например, functools,
о котором шла речь в первом занятии при работе с reduce.

Pip — это инструмент для установки и управления сторонними библиотеками. С его помощью мы можем:
устанавливать новые пакеты;
обновлять уже установленные библиотеки;
удалять ненужные пакеты;
управлять зависимостями в нашем проекте.

Основные команды pip

Установка пакета:
pip install {название_пакета}

Например, чтобы установить библиотеку для HTTP-запросов:
pip install requests

Просмотр установленных пакетов:
pip list

Удаление пакета:
pip uninstall название_пакета

Обновление пакета:
Библиотеки постоянно обновляются. Так, то, что работало сегодня, может уже не работать завтра.
Для этого важно следить за версиями модулей.
А чтобы обновить модуль, используется следующая команда:
pip install --upgrade название_пакета

Обновление самого pip:
pip — такой же модуль, как и любой другой, поэтому его тоже можно обновить:
pip install --upgrade pip

Фиксация текущих зависимостей:
pip freeze > requirements.txt
Этот файл (requirements.txt) содержит список всех используемых библиотек и их версий в текущей среде.

А что значит "в текущей среде"?
Давайте разберёмся, какие они бывают и почему нам может понадобиться больше, чем одна среда.

Виртуальные среды: изоляция проектов
Когда мы работаем над разными проектами,
каждый из них может требовать разные библиотеки или их версии.
Например, один проект использует Django версии 3.0, а другой — версии 4.2.
Без виртуальных сред установка этих пакетов глобально приведёт к конфликтам.

Однако и сами по себе библиотеки также могут использовать другие библиотеки,
это называется зависимостями.
Нередко возникают ситуации, когда две разные библиотеки имеют одну зависимость,
но разных версий. Для разрешения таких конфликтов и используются виртуальные среды.

Виртуальная среда — это изолированная копия Python, где каждая библиотека устанавливается отдельно и не влияет на другие проекты.

Преимущества виртуальных сред:

Изоляция проектов — каждый использует свои зависимости.

Удобство работы в команде — можно легко восстановить окружение с помощью файла requirements.txt.

Избежание конфликта версий библиотек.

Важное примечание: В стандартной библиотеке Python есть модуль venv
Ранее также был популярен сторонний пакет virtualenv.
Их функциональность очень похожа, но venv является стандартным и рекомендуемым инструментом.

Создание и использование виртуальной среды
Модуль venv, входящий в стандартную библиотеку Python, позволяет создавать виртуальные среды.

Основные шаги

Создание окружения:
Рекомендуется использовать вызов модуля напрямую через python -m venv
Это гарантирует, что вы используете правильный интерпретатор.
python -m venv {env_name}

где env_name — название виртуального окружения (например, .venv или venv).

Активация:
source {env_name}/bin/activate

После активации в начале строки командной оболочки вы должны увидеть имя вашей виртуальной среды.
Это значит, что все последующие команды pip и python будут относиться к этой среде.

Установка пакетов в среду:
pip install {package_name}
После активации все установленные пакеты будут принадлежать только этой среде.

Деактивация среды:
Чтобы выйти из виртуального окружения, используем команду:
deactivate

Удаление среды:
Для удаления среды просто удалим папку, содержащую окружение.

macOS/Linux/Windows:
rm -r {env_name}


Когда мы захотим воспользоваться другим проектом, велика вероятность,
что у нас он просто не запустится, а связано это может быть с тем,
что в текущей среде не установлены необходимые библиотеки.
Чтобы разработчик знал, какие библиотеки нужны для работы программы,
зачастую рядом с кодом в папке лежит файл requirements.txt.

Файл requirements.txt
Файл requirements.txt — это список библиотек, которые используются в среде выполнения.
Создать такой файл можно с помощью команды:
pip freeze > requirements.txt

Важное примечание: Команда pip freeze выводит все пакеты,
установленные в текущей виртуальной среде, включая косвенные зависимости.
Для больших проектов это может создать избыточный список.
В профессиональной разработке часто используются более сложные инструменты (такие как pip-tools или Poetry),
которые помогают создавать точные списки зависимостей.
Однако для начала pip freeze вполне подходит.

Для установки пакетов из файла используем:
pip install -r requirements.txt

Метафора виртуального окружения Python
Верхний уровень
В помещении хранится множество инструментов.
Это напоминает хранилище библиотек,
которые часто устанавливаются в виртуальное окружение Python.
Здесь царит хаос без структурированного подхода.

Нижний уровень

Левая комната со стеллажами и компьютером связана с Django.
Она представляет проект веб-разработки, использующий эту библиотеку.

Центральная комната содержит графики и таблицы, указывая на задачи Data Science.
Это другое виртуальное окружение, в котором сосредоточены библиотеки для анализа данных.

Правая комната связана с искусственным интеллектом (ИИ)
там используются свои специализированные инструменты и библиотеки.

Каждая комната на нижнем уровне отделена от других.
Это показывает, что виртуальные окружения позволяют использовать разные версии библиотек
для различных проектов, предотвращая конфликты.

На верхнем уровне в хаосе хранится всё, что можно установить.

Использование виртуальных окружений Python упорядочивает всё это,
разделяя разные проекты (комнаты) и создавая для них индивидуальные условия.

Как это работает в реальности: типовой рабочий процесс
Вы получаете проект (например, из Git-репозитория).

Переходите в его папку и создаете виртуальную среду: python -m venv venv

Активируете ее: source venv/bin/activate (или аналог).

Устанавливаете зависимости: pip install -r requirements.txt

Работаете над проектом. Если добавляется новая библиотека,
вы устанавливаете ее через pip install и обновляете requirements.txt с помощью
pip freeze > requirements.txt.

После завершения работы деактивируете среду командой deactivate.





Типичные ошибки
Тип ошибки 1: Pip недоступен

Мы пытаемся выполнить команду pip, но получаем сообщение об ошибке.

Пример ошибки: pip: command not found

Причина: Возникает в случае,
если Python установлен некорректно или pip не добавлен в переменные окружения.

Решение:

Проверим, установлен ли pip: python -m ensurepip --upgrade

Убедимся, что Python и pip добавлены в PATH (особенно на Windows).

Используем команду с указанием Python: python -m pip install {package_name}









Тип ошибки 2: Установка пакета требует прав администратора

Возникает при попытке установить пакет в глобальную область, не имея достаточных прав.

Пример: При установке пакета появляется ошибка Permission denied.

Решение:

Используем виртуальную среду, чтобы не трогать глобальные зависимости (рекомендуется).

python -m venv {venv_name}
source {venv_name}/bin/activate
pip install {package_name}

Если установка глобально всё же нужна, добавим sudo (не рекомендуется): sudo pip install пакет







Тип ошибки 3: Пакет не найден

Возникает при попытке установки пакета,
который отсутствует в официальном репозитории PyPI,
или указанная версия недоступна.

Пример ошибки: Could not find a version that satisfies the requirement {package_name}

Решение:

Проверим, правильно ли написано название пакета (например, вместо pandas могли написать panda).

Убедимся, что интернет-соединение активно.

Если пакет есть в другом репозитории,
укажем его URL: pip install {package_name} --index-url {repo_url}











Тип ошибки 4: Виртуальная среда не активируется

Команда activate (активации) виртуальной среды не работает.

Пример ошибки: The term 'activate' is not recognized as the name of a cmdlet... (Windows/PowerShell).

Решение:

На Windows включим разрешение на выполнение скриптов в PowerShell:
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

Убедимся, что правильно указан путь к activate (для Windows CMD: .\venv\Scripts\activate, для PowerShell: .\venv\Scripts\Activate.ps1).











Тип ошибки 5: requirements.txt не работает

Происходит в случае, когда requirements.txt содержит пакеты,
несовместимые с нашей текущей системой или средой.

Пример ошибки: Мы пытаемся восстановить зависимости, но получаем ошибку о несовместимости версий.


Решение:

Убедимся, что используем ту же версию Python, в которой был создан проект.

Проверим, нет ли в requirements.txt библиотек для других операционных систем.

Попробуем установить пакеты по одному, чтобы выявить проблемную зависимость: pip install {package_name}.


















Практикум
Задание 1. Установка библиотек и управление зависимостями

Ситуация: Мы работаем над проектом, который анализирует погоду.
Для работы программы нам нужны следующие библиотеки:

requests для работы с HTTP-запросами;

matplotlib для построения графиков;

pandas для анализа данных.


Задачи:

Создать виртуальную среду для проекта и активировать её.

Установить все необходимые библиотеки.

Сохранить список зависимостей в файл requirements.txt.

Проверить, указаны ли в файле все установленные библиотеки и их версии.

Шаги реализации:

Создадим виртуальную среду venv и активируем её.

Установим требуемые библиотеки с помощью pip.

Зафиксируем список зависимостей с помощью pip freeze.

Вручную перепроверим файл с зависимостями.

Реализация:


python -m venv venv
# Активация для Windows (CMD):
.\venv\Scripts\activate
# Активация для macOS/Linux:
# source venv/bin/activate

pip install requests matplotlib pandas
pip freeze > requirements.txt
# Открыть файл и проверить наличие библиотек и их версии
















Задание 2. Восстановление окружения из файла требований

Ситуация: Коллега передал нам проект с файлом requirements.txt, содержащим следующие строки:


numpy==1.21.0
scipy==1.7.0
seaborn==0.11.1


Задачи:

Создать виртуальную среду для этого проекта.

Установить все зависимости из файла requirements.txt.

Убедиться, что указанные версии библиотек установлены.

Добавить ещё одну библиотеку, например, statsmodels, в виртуальную среду.

Обновить файл requirements.txt, чтобы он содержал все актуальные зависимости.

Шаги реализации:

Создадим виртуальную среду venv и активируем её.

Установим библиотеки из файла requirements.txt с помощью pip.

Проверим корректность установки библиотек с помощью pip list.

Добавим библиотеку statsmodels в окружение с помощью pip install.

Зафиксируем обновленный список зависимостей с помощью pip freeze.

Реализация:


python -m venv venv
# Активация для Windows (CMD):
.\venv\Scripts\activate
# Активация для macOS/Linux:
# source venv/bin/activate

pip install -r requirements.txt
pip list
pip install statsmodels
pip freeze > requirements.txt
"""