На предыдущих занятиях мы изучали асинхронное программирование с использованием asyncio, 
которое позволяет эффективно управлять задачами в одном потоке. 
Однако асинхронное программирование не подходит для задач, 
требующих истинного параллелизма, таких как выполнение CPU-bound операций 
(например, сложных вычислений). Для этого нам нужно использовать потоки (threads).



На этом занятии мы:

разберём, что такое потоки и зачем они нужны;
научимся создавать и запускать потоки;
напишем многопоточное приложение для параллельной обработки данных;
рассмотрим типичные ошибки и лучшие практики при работе с потоками.



Потоки в Python — это не какая-то магия, доступная только избранным.
Это инструмент, который при правильном использовании значительно уменьшит длительность
выполнения наших программ и упростит выполнение конкретных задач.


Правда, у потоков в Python есть свои особенности,которые нужно учитывать. 
Они отлично справляются с задачами, связанными с вводом-выводом (I/O-bound tasks),
и могут значительно ускорить выполнение программы, если применять их с умом.

Когда потоки — это хороший выбор?
Давайте разберёмся, в каких случаях стоит использовать потоки, а когда лучше поискать другие решения.

1. Задачи, связанные с вводом-выводом (I/O-bound tasks)

Если программа много времени тратит на ожидание (например, сетевые запросы, 
чтение/запись файлов или взаимодействие с базами данных), 
потоки могут стать нашим лучшим другом.
Они позволяют выполнять несколько таких операций одновременно, не блокируя основной поток программы.



2. Много численных расчётов (CPU-bound tasks)

Если же задача упирается в вычислительные ресурсы 
(например, сложные математические вычисления или обработка больших объёмов данных),
потоки в Python мало помогут. В этом случае лучше обратиться к модулю multiprocessing,
который позволяет задействовать несколько ядер процессора.

Основы создания потоков
Итак, давайте начнём с простого: как вообще создать поток? Вот пример:

import threading
import time

def print_numbers():
    for i in range(10):
        print(f"Number: {i}")
        time.sleep(0.5)

# Создаём поток
thread = threading.Thread(target=print_numbers)
thread.start()

# Основной поток продолжает выполнять свои задачи
print("Поток запущен!")

# Ждём, пока поток завершится
thread.join()
print("Поток завершен!")


Здесь мы создали поток с помощью threading.Thread, 
передав ему функцию print_numbers, которая будет выполняться в новом потоке. 
Затем мы вызвали start(), чтобы запустить поток, и join(), чтобы дождаться его завершения.

Вроде всё просто, правда? Но вот тут-то и начинают встречаться подводные камни: 
потоки в Python имеют доступ к общей памяти, и это тот самый момент,
где легко ошибиться, поймав себе баг или, того хуже, гонку данных.

Гонки данных и как с ними бороться
Race condition (гонка данных) — это ситуация,
когда несколько потоков (или процессов) одновременно пытаются изменить общий ресурс
(например, переменную, файл или объект), и результат выполнения программы становится непредсказуемым.
Это происходит потому, что порядок выполнения операций между потоками
не контролируется и они могут «пересекаться» в своих действиях.

Давайте рассмотрим пример, где несколько потоков пытаются увеличить значение одной и той же переменной:

import threading

# Общая переменная, которую будем изменять
counter = 0

def increment():
    global counter
    for _ in range(100000):
        counter += 1

# Создаём 5 потоков, которые будут увеличивать counter
threads = [threading.Thread(target=increment) for _ in range(5)]

# Запускаем все потоки
for thread in threads:
    thread.start()

# Ждём, пока все потоки завершатся
for thread in threads:
    thread.join()

# Ожидаемое значение counter: 500000 (5 потоков * 100000 итераций)
print(f"Counter value: {counter}")



Что происходит в этом коде?

Общая переменная: у нас есть глобальная переменная counter,
которую все потоки пытаются увеличить.

Потоки: мы создаём 5 потоков, каждый из которых выполняет функцию increment().
Внутри этой функции переменная counter увеличивается на 1 в цикле 100000 раз.

Ожидаемый результат: мы ожидаем, 
что после завершения всех потоков значение counter будет равно 500000 (5 потоков * 100000 итераций).

Почему возникает Race condition?

Проблема в том, что операция counter += 1 не является атомарной. 
Это значит, что она состоит из нескольких шагов:

Чтение текущего значения counter.

Увеличение значения на 1.

Запись нового значения обратно в counter.

Когда несколько потоков работают одновременно, они могут «пересекаться» на этих шагах.

Например:

поток 1 читает значение counter (допустим, 10);

поток 2 тоже читает значение counter (всё ещё 10);

поток 1 увеличивает значение до 11 и записывает его;

поток 2 увеличивает значение до 11 и записывает его.

В результате, хотя оба потока выполнили операцию увеличения, 
значение counter увеличилось только на 1, а не на 2, как должно было быть.

Как это выглядит на практике?

Если запустить код выше, результат может быть, например, таким:

Counter value: 432157
Вместо ожидаемых 500000 мы получаем меньшее значение. 
Это и есть Race condition: из-за того, 
что потоки «пересекаются» при изменении общей переменной, результат становится непредсказуемым.

Как избежать Race condition?

Чтобы избежать Race condition, нужно использовать механизмы синхронизации, такие как Lock.

Lock позволяет «заблокировать» доступ к общему ресурсу,
чтобы только один поток мог работать с ним в конкретный момент времени:

import threading

# Общая переменная, которую будем изменять
counter = 0

# Создаём Lock для синхронизации
lock = threading.Lock()

def increment():
    global counter
    for _ in range(100000):
        with lock:  # Блокируем доступ к counter
            counter += 1

# Создаём 5 потоков, которые будут увеличивать counter
threads = [threading.Thread(target=increment) for _ in range(5)]

# Запускаем все потоки
for thread in threads:
    thread.start()

# Ждём, пока все потоки завершатся
for thread in threads:
    thread.join()

# Теперь counter будет равен 500000
print(f"Counter value: {counter}")



Что изменилось?

1. Lock: мы добавили объект lock = threading.Lock().

2. Блокировка: внутри функции increment() мы используем with lock:,
чтобы заблокировать доступ к counter на время выполнения операции counter += 1.

3. Результат: теперь значение counter будет всегда равно 500000,
потому что потоки больше не «пересекаются» при изменении переменной.

Почему это работает?

Когда поток входит в блок with lock:, он «захватывает» Lock.
Если другой поток попытается войти в этот блок, он будет ждать, пока Lock не освободится.

После завершения блока with lock: поток освобождает Lock,
и следующий поток может его захватить.

Таким образом, только один поток может изменять counter в конкретный момент времени,
что исключает Race condition.

Queue для передачи данных между потоками
Часто задачи не просто запускают отдельные потоки,
но и передают между ними данные. Для этого в Python есть класс Queue,
который является потокобезопасным. Вот пример:

import threading
import queue
import time

def worker(q):
    while not q.empty():
        task = q.get()
        print(f"Processing {task}")
        time.sleep(1)
        q.task_done()

# Создаём очередь и заполняем задачами
q = queue.Queue()
for i in range(5):
    q.put(f"Task {i}")

# Запускаем несколько потоков-воркеров
threads = [threading.Thread(target=worker, args=(q,)) for _ in range(3)]

for thread in threads:
    thread.start()

# Ждём, пока все задачи не будут завершены
q.join()
print("All tasks are processed!")



Здесь мы запускаем три потока, которые берут задачи из очереди и выполняют их.
Обратим внимание на q.join() — он дожидается завершения всех задач в очереди, что упрощает контроль.

Преимущества использования Queue

Потокобезопасность
Очередь автоматически управляет блокировками, 
так что нам не нужно вручную использовать Lock или другие механизмы синхронизации.
Это делает код проще и безопаснее.

Удобство передачи данных
Очередь позволяет легко передавать данные между потоками. 
Например, один поток может добавлять задачи в очередь, а другой — извлекать и выполнять их.

Гибкость
Очередь может использоваться для различных сценариев, таких как:

распределение задач между потоками;

сбор результатов работы потоков;

организация pipeline (цепочки обработки данных);

контроль за выполнением задач: с помощью метода join() можно дождаться завершения всех задач в очереди,
что упрощает управление потоками.
Давайте рассмотрим пример, где один поток генерирует задачи,
а несколько потоков-воркеров выполняют их:

import queue
import time

# Функция, которую будут выполнять воркеры
def worker(q):
    while not q.empty():  # Пока в очереди есть задачи
        task = q.get()    # Извлекаем задачу из очереди
        print(f"Processing {task}")
        time.sleep(1)     # Имитируем выполнение задачи
        q.task_done()     # Сообщаем, что задача выполнена

# Создаём очередь и заполняем её задачами
q = queue.Queue()
for i in range(10):  # Добавляем 10 задач
    q.put(f"Task {i}")

# Создаём и запускаём потоки-воркеры
threads = [threading.Thread(target=worker, args=(q,)) for _ in range(3)]  # 3 воркера

for thread in threads:
    thread.start()

# Ждём, пока все задачи не будут завершены
q.join()  # Блокируем основной поток, пока очередь не опустеет
print("All tasks are processed!")




Что происходит в этом коде?

1. Очередь задач: мы создаём очередь q и заполняем её 10 задачами (строками вида task 0, task 1 и т.д.).

2. Потоки-воркеры: мы создаём 3 потока, каждый из которых выполняет функцию worker(). Внутри этой функции:

поток извлекает задачу из очереди с помощью q.get();

выполняет задачу (в данном случае просто печатает её и ждёт 1 секунду);

сообщает, что задача выполнена, с помощью q.task_done().

3. Ожидание завершения: мы используем q.join(), чтобы основной поток дождался, 
пока все задачи в очереди не будут выполнены. Это гарантирует, что программа не завершится раньше времени.

GIL (Global Interpreter Lock)
GIL (Global Interpreter Lock) — это механизм, встроенный в интерпретатор Python (CPython),
который позволяет только одному потоку исполнять Python-код в конкретный момент времени.
Это означает, что даже если у вас многопоточная программа, работающая на многоядерном процессоре,
только один поток может выполнять Python-код одновременно.

Как бороться с GIL?

Ответ простой — не бороться. Если наша задача — это ввод-вывод,
мы почти не почувствуем влияние GIL. Но, если нужно много считать, 
лучше задуматься об asyncio или multiprocessing.

Давайте теперь решим задачу и применим новые знания.

Нам нужно написать программу, которая обрабатывает текстовые файлы. Программа должна выполнить следующие шаги:

У нас есть список путей к текстовым файлам. Каждый файл содержит строки текста.
Программа должна подсчитать количество строк в каждом файле и общее количество строк во всех файлах.
Для ускорения обработки используем потоки (threading) и очередь (queue).
Напишем решение с обработкой ошибок: если файл недоступен или пустой, программа должна корректно обработать это.
import threading
import queue
import os

# Функция для обработки файла
def process_file(q, result, lock):
    while not q.empty():
        file_path = q.get()
        try:
            # Проверяем доступность файла
            if not os.path.exists(file_path):
                print(f"File not found: {file_path}")
                q.task_done()
                continue

            # Считаем количество строк
            with open(file_path, 'r') as f:
                lines = f.readlines()
                line_count = len(lines)

            # Обновляем общий результат (синхронизируем доступ с помощью Lock)
            with lock:
                result['total_lines'] += line_count
                result['file_results'][file_path] = line_count

            print(f"Processed {file_path}: {line_count} lines")

        except Exception as e:
            print(f"Error processing file {file_path}: {e}")

        finally:
            q.task_done()  # Сообщаем, что задача завершена

# Основная программа
def main():
    # Список файлов для обработки
    file_paths = [
        'file1.txt',
        'file2.txt',
        'file3.txt',  # Добавим пути к своим файлам
    ]

    # Очередь задач
    task_queue = queue.Queue()

    # Заполняем очередь задачами (путями к файлам)
    for path in file_paths:
        task_queue.put(path)

    # Результаты обработки
    result = {
        'total_lines': 0,
        'file_results': {}
    }

    # Lock для синхронизации доступа к результатам
    result_lock = threading.Lock()

    # Создаём и запускаем потоки
    num_threads = 3
    threads = [
        threading.Thread(target=process_file, args=(task_queue, result, result_lock))
        for _ in range(num_threads)
    ]

    for thread in threads:
        thread.start()

    # Ждём завершения всех задач
    task_queue.join()

    # Выводим результаты
    print("\nResults:")
    for file_path, line_count in result['file_results'].items():
        print(f"{file_path}: {line_count} lines")
    print(f"Total lines: {result['total_lines']}")

# Запускаем программу
if __name__ == '__main__':
    main()



Пошаговое объяснение кода

Подготовка данных:

Список file_paths содержит пути к текстовым файлам, которые нужно обработать.
Очередь task_queue используется для передачи путей к файлам потоков.
Функция process_file:

Эта функция извлекает путь к файлу из очереди.
Проверяет, доступен ли файл.
Если файл доступен, считает количество строк в файле.
Обновляет общий результат (result) и количество строк для каждого файла.
Для синхронизации доступа используется Lock.
Создание потоков:

Мы создаём несколько потоков (в данном примере 3) для обработки файлов.
Потоки запускаются и извлекают задачи из очереди.
Синхронизация результатов:

Поскольку несколько потоков одновременно обновляют общий результат (result),
мы используем Lock для предотвращения гонки данных.
Ожидание завершения:

Метод task_queue.join() блокирует основной поток,
пока все задачи в очереди не будут выполнены.
Вывод результатов:

После завершения всех потоков программа выводит количество строк в каждом файле и их общее количество.
Рассмотрим следующую интерпретацию работы с потоками. 
Представим многопользовательский онлайн-игровой процесс, 
где каждый элемент — это часть общей механики игры.

В игре поток (thread) можно представить как персонажа, которым управляет игрок.

Каждый персонаж выполняет свои задачи (например, собирает ресурсы, 
атакует врагов или строит базу) одновременно с другими персонажами.

Все они работают в рамках одной команды (процесса), но действуют независимо.

Задачи, зависящие от ввода-вывода (I/O-bound) — персонажи, которые ждут событий

I/O-bound задачи похожи на персонажей, которые стоят в очереди к торговцу или ждут,
пока врата в подземелье откроются. Время их активности ограничено скоростью внешних процессов 
(например, скоростью реакции торговца или открытием ворот), и в это время они, по сути, 
ничего не делают, просто ожидая.

Задачи, зависящие от процессора (CPU-bound) — персонажи, сражающиеся с боссом

CPU-bound задачи можно сравнить с персонажами, которые участвуют в битве с огромным боссом.
Здесь персонажи полностью поглощены активным боем, и их успех зависит от того,
насколько быстро и эффективно они могут наносить урон (использовать вычислительные ресурсы).

Гонка данных (Race condition) — игроки, собирающие один и тот же сундук

Race condition похожа на ситуацию, когда несколько игроков одновременно пытаются открыть сундук с сокровищами.
Если нет чёткого правила, кто из них первым получает содержимое сундука, 
результаты могут быть хаотичными. Один игрок может успеть взять золото, 
другой — случайно заблокировать сундук, а третий вообще остаться ни с чем.

Блокирующая операция (Blocking Operation) — персонаж, который ждёт загрузки уровня

Blocking Operation можно представить как ситуацию, когда персонаж замер в 
ожидании загрузки нового уровня или открытия следующей области игры.
До завершения этого процесса он не может двигаться, атаковать или выполнять какие-либо действия,
так как его полностью «заморозили».

Как это работает в игре?

Персонажи (потоки) могут действовать параллельно: кто-то сражается с врагами (CPU-bound),
кто-то ждёт открытия сундука (I/O-bound).
Если два персонажа пытаются одновременно взаимодействовать с одним объектом (например, сундуком),
возникает гонка данных (Race condition).
А когда персонаж застревает в ожидании загрузки нового уровня, это блокирующая операция (Blocking Operation), 
которая тормозит его прогресс.
Этот игровой контекст помогает наглядно представить,
как работают потоки и связанные с ними концепции в программировании.





Типичные ошибки
Тип ошибки 1: Гонка данных (Race Condition)

Ошибка возникает, когда несколько потоков одновременно пытаются изменить общий ресурс без синхронизации.

Пример ошибки:

import threading

counter = 0

def increment():
    global counter
    for _ in range(100000):
        counter += 1

threads = [threading.Thread(target=increment) for _ in range(5)]
for thread in threads:
    thread.start()

for thread in threads:
    thread.join()

print(f"Counter value: {counter}")  # Может вывести значение меньше 500000
Решение:

используем механизмы синхронизации, такие как Lock.

import threading

counter = 0
lock = threading.Lock()

def increment():
    global counter
    for _ in range(100000):
        with lock:
            counter += 1

threads = [threading.Thread(target=increment) for _ in range(5)]
for thread in threads:
    thread.start()

for thread in threads:
    thread.join()

print(f"Counter value: {counter}")  # Всегда выведет 500000





Тип ошибки 2: Deadlock (Взаимная блокировка)

Ошибка возникает, когда два или более потока блокируют друг друга, ожидая освобождения ресурсов.

Пример ошибки:

import threading

lock1 = threading.Lock()
lock2 = threading.Lock()

def thread1():
    with lock1:
        with lock2:
            print("Thread 1")

def thread2():
    with lock2:
        with lock1:
            print("Thread 2")

t1 = threading.Thread(target=thread1)
t2 = threading.Thread(target=thread2)

t1.start()
t2.start()

t1.join()
t2.join()
Решение:

упорядочиваем захват блокировок в одинаковом порядке.

def thread1():
    with lock1:
        with lock2:
            print("Thread 1")

def thread2():
    with lock1:
        with lock2:
            print("Thread 2")






Тип ошибки 3: Неправильное использование join()

Ошибка возникает, когда join() вызывается до start(), что приводит к блокировке основного потока.

Пример ошибки:

import threading

def worker():
    print("Working...")

thread = threading.Thread(target=worker)
thread.join()  # Ошибка: join() перед start()
thread.start()
Решение:

всегда вызываем start() перед join().

import threading

def worker():
    print("Working...")

thread = threading.Thread(target=worker)
thread.start()
thread.join()







Практикум
Задание 1. Пул потоков с ограничением на одновременное выполнение

Ситуация: нам поставили задачу оптимизировать работу нашего проекта с сетью. 
Для этого необходимо реализовать систему, которая обрабатывает до трёх задач одновременно.

Задача:

Создать класс ThreadPool, который:
инициализируется с максимальным количеством потоков;
добавляет задачи в очередь и запускает их при наличии свободных потоков;
использует queue.Queue для хранения задач и threading.Thread для их выполнения,
при этом ограничивает количество одновременно выполняемых потоков.
Реализация:

import threading
import queue
import time

class ThreadPool:
    def __init__(self, max_threads):
        self.max_threads = max_threads
        self.task_queue = queue.Queue()
        self.running_threads = 0

    def add_task(self, task_func, *args):
        self.task_queue.put((task_func, args))
        self._try_run_task()

    def _worker(self):
        task_func, args = self.task_queue.get()
        task_func(*args)
        self.task_queue.task_done()

        # после завершения задачи уменьшаем количество работающих потоков
        with threading.Lock():
            self.running_threads -= 1

        # попробуем запустить следующую задачу, если есть свободные потоки
        self._try_run_task()

    def _try_run_task(self):
        # если количество работающих потоков меньше максимального, запускаем новый
        if self.running_threads < self.max_threads:
            with threading.Lock():
                self.running_threads += 1
            threading.Thread(target=self._worker).start()

# пример использования
def process_task(task_id):
    print(f"Начало обработки задачи {task_id}")
    time.sleep(2)
    print(f"Завершение задачи {task_id}")

pool = ThreadPool(3)
for i in range(10):
    pool.add_task(process_task, i)


Задание 2. Синхронизация доступа к общему списку

Ситуация: наш коллега выполнял задачу, подобную прошлой. 
Однако в его реализации несколько потоков добавляют данные в общий список.
Необходимо предотвратить гонку данных.

Задачи:

Создать функцию add_to_shared_list, которая добавляет элемент в общий список с использованием Lock.
Запустить 5 потоков, которые добавляют 1000 элементов каждый.
Реализация:

import threading

shared_list = []
lock = threading.Lock()

def add_to_shared_list(item):
    with lock:
        shared_list.append(item)

def worker():
    for i in range(1000):
        add_to_shared_list(i)

threads = [threading.Thread(target=worker) for _ in range(5)]
for thread in threads:
    thread.start()

for thread in threads:
    thread.join()

print(f"Ожидаемая длина списка: 5000, Фактическая: {len(shared_list)}")




Итоги:
Сегодня мы:

познакомились с основами многопоточности в Python и понятием потоков;
научились создавать и управлять потоками с использованием модуля threading;
разобрали механизмы синхронизации: Lock и Queue;
изучили типичные ошибки, такие как гонки данных и взаимные блокировки;
применили полученные знания на практике, реализовав пул потоков и синхронизацию доступа к общим ресурсам.
Надеемся, что полученные знания и практические навыки помогут вам создавать многопоточные 
приложения для эффективной обработки I/O-bound задач и управлять синхронизацией потоков.