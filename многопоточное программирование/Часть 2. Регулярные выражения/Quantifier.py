На прошлом занятии мы познакомились с основами регулярных выражений. 
Сегодня мы углубимся в тему и изучим метасимволы и квантификаторы, 
которые позволяют создавать более сложные и вариативные шаблоны для поиска и анализа текста.

Представим, что перед нами огромный текст — это может быть лог-файл, 
база данных клиентов или просто веб-страница. 
Наша задача — быстро и точно найти в нём нужную информацию, 
например: все телефонные номера, email-адреса или ссылки. 
Сделать это вручную или с помощью базовых методов обработки текста становится 
либо слишком долгим, либо попросту невозможным.

В этом случае нам и помогут метасимволы и квантификаторы. Как именно, рассмотрим далее.

На занятии мы научимся использовать метасимволы и квантификаторы для создания сложных шаблонов, а именно:

поймём, как использовать метасимволы для создания гибких шаблонов;
применим квантификаторы для поиска повторяющихся паттернов;
создадим регулярные выражения для проверки сложных форматов, таких как телефонные номера.

На прошлом занятии мы изучили основы регулярных выражений, 
которые позволяют искать простые шаблоны в тексте. 
Так теперь мы можем найти конкретное слово или символ, используя прямые совпадения. 
Однако в реальной жизни задачи часто бывают сложнее, и базовых возможностей может не хватить.

Вернёмся к случаю, рассмотренному в начале занятия.

Метасимволы и квантификаторы — инструменты, 
которые превращают регулярные выражения в максимально удобный способ анализа и обработки текста.

Они позволяют:

1. Быстро находить нужные данные: будь то дата, 
записанная в разных форматах, или телефонный номер, где могут быть скобки, пробелы и дефисы.

2. Управлять сложными шаблонами: искать не просто конкретную строку, 
а структуры, соответствующие заданным правилам.

3. Экономить время и силы, автоматизируя рутинные задачи, такие как поиск, 
извлечение или замена данных в больших текстах.

Рассмотрим на примере.

Допустим, у нас есть текст, в котором нужно найти все телефонные номера. 
Номера могут быть записаны в разных форматах:

+7 (900) 123-45-67
89001234567
8-900-123-45-67

С помощью базовых регулярных выражений мы можем попытаться найти номер, к примеру, так:

89001234567

Но это сработает только для одного конкретного формата. 
А что если номер записан с пробелами, скобками или дефисами? 
Базовые шаблоны не смогут справиться с такой вариативностью. 
Здесь на помощь приходят метасимволы и квантификаторы. Давайте познакомимся с ними поближе.

Метасимволы в регулярных выражениях
Метасимволы — это специальные символы, 
которые имеют особое значение в регулярных выражениях. 
Они позволяют создавать более гибкие и эффективные шаблоны.

Рассмотрим основные метасимволы:

. — любой символ, кроме новой строки;
^ — начало строки;
$ — конец строки;
* — ноль или более повторений предыдущего символа;
+ — одно или более повторений предыдущего символа;
? — ноль или одно повторение предыдущего символа;
[] — набор символов, к примеру, [a-z] означает любую строчную букву;
| — логическое ИЛИ, к примеру, cat|dog означает «cat или dog»;
() — группировка, к примеру, (abc)+ означает «abc, повторённое один или более раз».




Почему метасимволы важны?

Без метасимволов мы ограничены в поиске точных совпадений. 
Например, если мы хотим найти все слова, начинающиеся с cat, но не знаем, 
что стоит после, без метасимволов это сделать невозможно. 
С помощью ^cat.* мы можем найти: cat, catalog, category и т.д.

Квантификаторы
Квантификаторы дают возможность указать, сколько раз предыдущий элемент может повторяться.

Рассмотрим основные:

* — ноль или более повторений;
+ — одно или более повторений;
? — ноль или одно повторение;
{n} — ровно n повторений;
{n, m} — от n до m повторений.


Пример задачи, где без квантификаторов не обойтись

Допустим, у нас есть текст и мы хотим найти все последовательности цифр длиной от 3 до 5 символов. 
Без квантификаторов мы бы написали несколько шаблонов
: \d\d\d, \d\d\d\d, \d\d\d\d\d. С квантификаторами это делается одним шаблоном — \d{3,5}.

Жадные и ленивые квантификаторы

По умолчанию квантификаторы являются жадными, 
то есть они пытаются найти максимально возможное совпадение.

Чтобы сделать квантификатор ленивым, нужно добавить ? после него. Например:

жадный .* ищет максимально возможное совпадение;
ленивый .*? ищет минимально возможное совпадение.


Давайте рассмотрим несколько примеров использования метасимволов и квантификаторов.



Пример 1: Поиск текста в кавычках

Давайте представим, что мы работаем над проектом, 
где нужно анализировать текстовые данные. 
Например, у нас есть лог-файл, в котором пользователи оставляют комментарии в кавычках. 
Нам нужно извлечь все эти комментарии для дальнейшего анализа.

Без использования метасимволов и квантификаторов мы можем попытаться найти текст в кавычках, 
но только если знаем его точную длину. Например, если комментарий состоит из 6 символов:

import re

text = 'Пользователь написал: "Привет" и ушёл.'
pattern = r'"......"'
matches = re.findall(pattern, text)
print(matches)  # ['"Привет"']

Что не так?

Мы должны точно знать длину текста в кавычках.

Если комментарий короче или длиннее, шаблон не сработает.

Если в тексте несколько комментариев, мы не сможем их всех извлечь.

Мы придумаем гибкий шаблон с использованием метасимволов и квантификаторов. 
Для этого используем ленивый квантификатор .*?, 
чтобы найти минимальное совпадение между кавычками:

import re

text = 'Пользователь написал: "Привет" и ушёл. А потом добавил: "Пока!".'
pattern = r'"(.*?)"'
matches = re.findall(pattern, text)
print(matches) # ['Привет', 'Пока!']


Тем самым мы извлекли все комментарии в кавычках, независимо от их длины и количества. Теперь мы можем анализировать их дальше.




Пример 2: Поиск телефонного номера

Давайте теперь разберём задачу с номерами телефонов, о которой мы говорили ранее. 
Мы разрабатываем систему для автоматического извлечения контактной информации из текстов. 
Например, у нас есть база данных клиентов, где номера телефонов записаны в разных форматах:

+7 (123) 456-78-90, 81234567890, 8-123-456-78-90

Без использования метасимволов и квантификаторов мы можем искать только 
конкретный номер или строго заданный формат.
Например, если мы знаем, что номер всегда начинается с 8 и состоит из 11 цифр, мы можем написать:

import re

text = "Контактный номер: 81234567890."
pattern = r'8\d{10}'
matches = re.findall(pattern, text)
print(matches)  # ['81234567890']

Что не так?

Мы можем найти только номера, которые начинаются с 8 и состоят из 11 цифр.

Если номер записан в другом формате (например, +7 (123) 456-78-90), шаблон не сработает.

Если в тексте несколько номеров в разных форматах, мы не сможем их всех найти.


Давайте теперь решим эту задачу с использованием универсального шаблона:

import re

text = "Контактные номера: +7 (123) 456-78-90, 81234567890 и 8-123-456-78-90."
pattern = r'\+?\d[\d\s\-()]{10,15}'
matches = re.findall(pattern, text)
print(matches)  # ['+7 (123) 456-78-90', '81234567890', '8-123-456-78-90']

Пояснения по шаблону:

\+? ищет символ +, который может присутствовать, а может и отсутствовать (например, для номеров с кодом страны);

\d ищет одну цифру;

[\d\s\-()]{10,15} ищет от 10 до 15 символов, которые могут быть цифрами, пробелами, дефисами или скобками.

Как мы видим, используя метасимволы и квантификаторы, 
можно теперь искать различные вариации данных, а не строго заданные последовательности.

Кратко сформулируем, когда и какие инструменты регулярных выражений нам использовать (см. таблицу):







Типичные ошибки
Тип ошибки 1: Некорректное использование жадных квантификаторов

Возникает при некорректном использовании жадных квантификаторов, 
в некотором случае они могут захватывать больше текста, чем нужно.

Пример ошибки:

import re

text = '<div>Первый</div><div>Второй</div>'
pattern = r'<div>.*</div>'
matches = re.findall(pattern, text)
print(matches) # ['<div>Первый</div><div>Второй</div>']
Решение:

используем ленивый квантификатор, чтобы захватывать минимально возможное совпадение.

pattern = r'<div>.*?</div>'
matches = re.findall(pattern, text)
print(matches) # ['<div>Первый</div>', '<div>Второй</div>']




Тип ошибки 2: Некорректное использование метасимволов

Возникает, когда метасимволы, такие как ., *, +, 
не экранированы в случае использования их как обычных символов.

Пример ошибки:

text = "Цена: $10.50"
pattern = r'\$\d+.'
matches = re.findall(pattern, text)
print(matches)
Решение:

экранируем точку, чтобы она воспринималась как символ, а не как метасимвол.

pattern = r'\$\d+\.\d+'
matches = re.findall(pattern, text)
print(matches)







Практикум
Задание 1. Поиск телефонных номеров

Ситуация: из колл-центра нам поступил огромной набор данных,
 содержащий тексты из диалогов с клиентами.
   В этих диалогах содержатся номера телефонов.

Задача — написать программу,
 которая позволяет пользователю найти все телефонные номера в тексте в формате +7 (XXX) XXX-XX-XX.

Шаги реализации

Импортируем модуль для работы с регулярными выражениями:

используем модуль re, чтобы работать с регулярными выражениями для поиска телефонных номеров.
import re



2. Определяем текст для анализа:

создаём переменную text, которая содержит текст с телефонными номерами.
text = "Мой номер телефона: +7 (123) 456-78-90. Также можно связаться по номеру +7 (987) 654-32-10."



3. Создаём регулярное выражение для поиска телефонных номеров:

используем шаблон r'\+\d{1,3}\s?\(\d{3}\)\s?\d{3}-\d{2}-\d{2}', 
чтобы найти все телефонные номера в формате +7 (XXX) XXX-XX-XX, где:
\+ — символ плюса;
\d{1,3} — одна, две или три цифры (код страны);
\s? — необязательный пробел;
\(\d{3}\) — три цифры в скобках (код города);
\d{3}-\d{2}-\d{2} — три цифры, затем дефис, снова две цифры, дефис, затем две цифры.
pattern = r'\+\d{1,3}\s?\(\d{3}\)\s?\d{3}-\d{2}-\d{2}'


4. Создаём поиск всех телефонных номеров в тексте:

используем функцию re.findall(), чтобы найти все совпадения с шаблоном в тексте;
результат сохраняем в переменную phone_numbers.
phone_numbers = re.findall(pattern, text)


5. Выводим найденные телефонные номера:

выводим список найденных телефонных номеров с помощью функции print().
print("Найденные телефонные номера:", phone_numbers)
Реализация:

# Импорт модуля для работы с регулярными выражениями
import re

# Определение текста для анализа
text = "Мой номер телефона: +7 (123) 456-78-90. Также можно связаться по номеру +7 (987) 654-32-10."

# Создание регулярного выражения для поиска телефонных номеров
pattern = r'\+\d{1,3}\s?\(\d{3}\)\s?\d{3}-\d{2}-\d{2}'

# Поиск всех телефонных номеров в тексте
phone_numbers = re.findall(pattern, text)

# Вывод найденных телефонных номеров
print("Найденные телефонные номера:", phone_numbers)








Задание 2. Поиск повторяющихся слов

Ситуация: нам передали тексты документаций,
 написанные джуниор-разработчиком.
   Поскольку джуниор-разработчик очень задумчивый, некоторые слова он пишет дважды.

Задача — написать программу, которая находит все повторяющиеся слова в тексте и выводит их в консоль.

Шаги реализации

Импортируем модуль для работы с регулярными выражениями:

используем модуль re, чтобы работать с регулярными выражениями для поиска повторяющихся слов.
import re


2. Определяем текст для анализа:

создаём переменную text, которая содержит текст с повторяющимися словами.
text = "Это тестовый текст, в котором есть повторяющиеся слова слова."


3. Создаём регулярное выражение для поиска повторяющихся слов:

используем шаблон r'\b(\w+)\s+\1\b', чтобы найти все повторяющиеся слова, где:
\b — граница слова;
(\w+) — одна или более букв, цифр или символов подчёркивания (первое слово);
\s+ — один или более пробелов;
\1 — ссылка на первое захваченное слово.
pattern = r'\b(\w+)\s+\1\b'


4. Создаём поиск всех повторяющихся слов в тексте:

используем функцию re.findall(), чтобы найти все совпадения с шаблоном в тексте;
результат сохраняем в переменную duplicates.
duplicates = re.findall(pattern, text)


5. Выводим найденные повторяющиеся слова:

выводим список повторяющихся слов с помощью функции print().
print("Найденные повторяющиеся слова:", duplicates)
Реализация:

# Импорт модуля для работы с регулярными выражениями
import re

# Определение текста для анализа
text = "Это тестовый текст, в котором есть повторяющиеся слова слова."

# Создание регулярного выражения для поиска повторяющихся слов
pattern = r'\b(\w+)\s+\1\b'

# Поиск всех повторяющихся слов в тексте
duplicates = re.findall(pattern, text)

# Вывод найденных повторяющихся слов
print("Найденные повторяющиеся слова:", duplicates)









Итоги:
Регулярные выражения — очень удобный инструмент работы с текстом, 
который применим не только в Python. Любой разработчик обязан уметь их применять правильно. Сегодня на занятии мы:

использовали метасимволы для создания гибких шаблонов;
применили квантификаторы для поиска повторяющихся паттернов;
создали регулярные выражения для проверки сложных форматов, таких как телефонные номера.