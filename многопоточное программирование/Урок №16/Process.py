На прошлом занятии мы изучили работу с потоками (threads), 
которые позволяют выполнять задачи параллельно в рамках одного процесса.
Однако, как мы уже и говорили, потоки в Python ограничены Global Interpreter Lock (GIL),
что делает их неэффективными для задач, требующих истинного параллелизма, 
таких как CPU-bound-операций (например, сложных вычислений).
Для таких задач нам нужно использовать процессы (processes).


На этом занятии мы:

разберём, что такое процессы и зачем они нужны;
научимся создавать и управлять процессами;
напишем многопроцессорное приложение для параллельной обработки данных;
рассмотрим типичные ошибки и лучшие практики при работе с процессами.




Процессы (process) в Python — это эффективный способ выполнения задач параллельно, 
особенно когда задачи связаны с интенсивными вычислениями (CPU-bound).
Процессы позволяют задействовать все доступные ядра процессора, 
обходя ограничения GIL (Global Interpreter Lock), которые свойственны потокам.


Процесс — это, грубо говоря, запущенная программа. 
Ему выделяются отдельные ресурсы: память, процессорное время. 
Рассмотрим, почему они так важны и как с ними работать в Python.


Почему процессы важны?
GIL ограничивает выполнение Python-кода в многопоточной среде,
позволяя только одному потоку выполнять байт-код Python в любой момент времени.
Это создаёт ограничение при работе с задачами, требующими интенсивных вычислений.


В отличие от потоков, процессы:

Имеют независимую память. Каждый процесс запускается с отдельным пространством памяти,
что исключает необходимость синхронизации при доступе к общим данным.
Используют все ядра процессора. Каждый процесс выполняется в отдельном ядре,
что обеспечивает истинную параллельность.
Имеют изоляцию ошибок. Ошибки в одном процессе не влияют на другие.
Основы создания процессов
Вот простой пример, который показывает,
как создать и запустить процесс в Python с помощью модуля multiprocessing:

from multiprocessing import Process

def say_hello():
    print("Привет от процесса!")

if __name__ == "__main__":
    # Создаём процесс
    process = Process(target=say_hello)  # Передаём функцию say_hello в процесс
    process.start()  # Запускаем процесс
    process.join()   # Ожидаем завершения процесса

Пошаговое объяснение

Импортируем модуль:

Импортируем класс Process из модуля multiprocessing для работы с процессами.

Определяем функцию:

Создаём функцию say_hello, выводящую сообщение. Это будет задача, которую выполняет процесс.

Создаём процесс:

Создаём объект Process, указывая функцию say_hello как задачу, которую должен выполнить процесс.

Запускаем процесс:

Вызываем метод start(), чтобы запустить новый процесс. При этом Python создаёт отдельный поток управления, который выполняет функцию say_hello.

Ожидаем завершения:

Метод join() заставляет основной процесс дождаться завершения нового. 
Это важно, чтобы убедиться, что процесс выполнил свою задачу до завершения программы.



Управление процессами
Теперь давайте рассмотрим два полезных метода для контроля над процессами:

is_alive() проверяет, работает ли процесс в данный момент;
terminate() завершает процесс принудительно.
from multiprocessing import Process
import time

def long_task():
    print("Процесс начал работу.")
    time.sleep(5)  # Имитируем длительную задачу
    print("Процесс завершил работу.")

if __name__ == "__main__":
    process = Process(target=long_task)
    process.start()

    # Проверяем, работает ли процесс
    time.sleep(2)
    if process.is_alive():
        print("Процесс все еще работает. Прерываем его.")
        process.terminate()  # Принудительно завершаем процесс

    process.join()  # Дожидаемся завершения процесса
    print("Процесс завершён.")

Что происходит в коде?

Мы создаём процесс, который выполняет задачу long_task, имитирующую долгую работу.

После 2 секунд работы проверяем, «жив» ли процесс с помощью is_alive().

Если процесс всё ещё работает, вызываем terminate(), чтобы принудительно завершить его.

Метод join() используется для того, чтобы основной процесс дождался завершения дочернего.

Межпроцессное взаимодействие (IPC)

Для обмена данными между процессами в Python используются такие инструменты, как Queue и Pipe.

Queue (очередь) — это структура данных, которая позволяет безопасно передавать их между процессами.
Она работает по принципу FIFO (first in, first out «первым пришёл — первым ушёл»),
 то есть первым в очередь встаёт тот элемент, который был добавлен первым.



Очередь — это потокобезопасная структура, 
которая автоматически управляет синхронизацией, 
чтобы избежать конфликтов при доступе к данным.

Пример использования Queue для передачи данных между процессами:

from multiprocessing import Process, Queue
import time

def producer(queue):
    for i in range(5):
        print(f"Производитель отправляет: {i}")
        queue.put(i)  # Помещаем данные в очередь
        time.sleep(1)

def consumer(queue):
    while True:
        item = queue.get()  # Получаем данные из очереди
        if item == "DONE":
            break
        print(f"Потребитель получил: {item}")

if __name__ == "__main__":
    queue = Queue()  # Создаём очередь

    # Запускаем два процесса: производитель и потребитель
    producer_process = Process(target=producer, args=(queue,))
    consumer_process = Process(target=consumer, args=(queue,))

    producer_process.start()
    consumer_process.start()

    # Ожидаем завершения процессов
    producer_process.join()
    # Отправляем сигнал завершения для потребителя
    queue.put("DONE")
    consumer_process.join()

    print("Обмен данными завершен.")



Что происходит в коде?

Мы создаём очередь Queue, через которую производитель будет передавать данные потребителю.
Процесс производителя генерирует данные (в данном случае числа от 0 до 4) 
и помещает их в очередь с помощью queue.put().

Процесс потребителя забирает данные из очереди с помощью queue.get() и обрабатывает их.
После завершения работы производителя в очередь помещается строка DONE,
 чтобы сообщить потребителю о завершении работы.

Результат выполнения:

Производитель отправляет: 0
Потребитель получил: 0
Производитель отправляет: 1
Потребитель получил: 1
Производитель отправляет: 2
Потребитель получил: 2
Производитель отправляет: 3
Потребитель получил: 3
Производитель отправляет: 4
Потребитель получил: 4
Обмен данными завершен.
Pipe



Это низкоуровневая структура для передачи данных между двумя процессами.
 Это двусторонний канал связи, через который можно отправлять и получать данные.
В отличие от Queue, Pipe работает на основе «связи» между двумя процессами, а не с глобальной очередью.

Пример использования Pipe для передачи данных между двумя процессами:



from multiprocessing import Process, Pipe
import time

def sender(conn):
    for i in range(5):
        print(f"Отправитель отправляет: {i}")
        conn.send(i)  # Отправляем данные через канал
        time.sleep(1)

def receiver(conn):
    while True:
        data = conn.recv()  # Получаем данные из канала
        if data == "DONE":
            break
        print(f"Получатель получил: {data}")

if __name__ == "__main__":
    # Создаём канал связи
    parent_conn, child_conn = Pipe()

    # Запускаем два процесса: отправитель и получатель
    sender_process = Process(target=sender, args=(parent_conn,))
    receiver_process = Process(target=receiver, args=(child_conn,))

    sender_process.start()
    receiver_process.start()

    sender_process.join()
    # Отправляем сигнал завершения для получателя
    parent_conn.send("DONE")
    receiver_process.join()

    print("Обмен данными завершён.")

Что происходит в коде?

Pipe() создаёт пару соединений — parent_conn и child_conn, которые используют два процесса для обмена данными.
Процесс отправителя передаёт данные через канал с помощью метода send().
Процесс получателя принимает данные с помощью метода recv().
После завершения работы отправителя передаётся строка DONE, чтобы сообщить получателю о завершении работы.
Результат выполнения:

Отправитель отправляет: 0
Получатель получил: 0
Отправитель отправляет: 1
Получатель получил: 1
Отправитель отправляет: 2
Получатель получил: 2
Отправитель отправляет: 3
Получатель получил: 3
Отправитель отправляет: 4
Получатель получил: 4
Обмен данными завершен.
Давайте рассмотрим задачу, где несколько процессов выполняют свои вычисления,
 обмениваются данными между собой и выводят результаты. 
 Мы будем использовать Queue для передачи данных между процессами.


Создадим 3 процесса. Каждый из них будет вычислять квадрат числа от 1 до 3,
 передавать результаты в очередь, а затем главный процесс будет выводить эти результаты по мере их поступления.

from multiprocessing import Process, Queue
import time

# Функция, которая вычисляет квадрат числа и отправляет результат в очередь
def compute_square(num, queue):
    result = num ** 2
    print(f"Процесс {num}: вычисление квадрата {num} = {result}")
    queue.put(result)  # Отправляем результат в очередь
    time.sleep(1)

if __name__ == "__main__":
    # Создаём очередь для обмена данными
    queue = Queue()

    # Создаём 3 процесса, каждый будет вычислять квадрат числа от 1 до 3
    processes = []
    for i in range(1, 4):
        process = Process(target=compute_square, args=(i, queue))
        processes.append(process)
        process.start()

    # Ожидаем завершения всех процессов
    for process in processes:
        process.join()

    # Главный процесс забирает и выводит результаты из очереди
    print("\nРезультаты вычислений:")
    while not queue.empty():
        result = queue.get()  # Получаем результат из очереди
        print(f"Результат: {result}")

    print("Все процессы завершены.")
Что такое Pool (Пул)?

Multiprocessing.Pool в Python — это ещё один инструмент для управления процессами.
 Он позволяет ограничивать количество одновременно работающих процессов и автоматически управляет их созданием,
   выполнением и завершением. Это особенно важно,
     если нам нужно выполнять задачи параллельно,
       но мы не хотим создавать слишком много процессов, которые могут перегрузить систему.

Возможные проблемы при работе с Pool

Когда мы создаём процессы вручную с помощью multiprocessing.Process, 
мы можем столкнуться с рядом проблем:

Исчерпание ресурсов системы: если мы создадим слишком много процессов,
 это может привести к исчерпанию системных ресурсов (памяти или процессорного времени).

Ручное управление: управление большим количеством процессов вручную может оказаться сложным и громоздким.
Управление синхронизацией вручную: вручную управлять синхронизацией между процессами,
 их запуском и завершением — задача не из простых.

Как работает Pool?

Pool создаёт фиксированное количество процессов (размер пула),
 которые используются для выполнения задач. 
 Затем эти процессы переиспользуются для выполнения новых задач,
   что минимизирует накладные расходы на их создание и завершение.

Основные методы Pool

apply() — выполняет функцию в одном из процессов пула и блокирует основной процесс до завершения задачи.
apply_async() — выполняет функцию асинхронно, возвращая объект AsyncResult,
 который можно использовать позже для получения результата.
map() — применяется ко всем элементам итерируемого объекта и возвращает результаты как список.
map_async() — асинхронная версия map(), которая возвращает объект AsyncResult.
starmap() — аналог map(), но позволяет передавать несколько аргументов в виде кортежа.
Пример использования Pool:

from multiprocessing import Pool

def square(x):
    return x ** 2

if __name__ == "__main__":
    # Создаём пул из 4 процессов
    with Pool(processes=4) as pool:
        # Список чисел для обработки
        numbers = [1, 2, 3, 4, 5, 6, 7, 8]

        # Применяем функцию ко всем элементам с помощью map
        results = pool.map(square, numbers)

    print(f"Квадраты чисел: {results}")

Что происходит в коде?

Создание пула процессов:

Pool(processes=4) создаёт пул из 4 процессов. 
Это означает, что в любой момент времени будет выполняться максимум 4 задачи.

Функция map:

Функция map распределяет список numbers между процессами пула. 
Каждый процесс берёт на себя выполнение функции square для одного или нескольких элементов списка.

Результаты:

Возвращаемое значение map — это список, 
где каждому входному элементу из numbers соответствует результат работы функции square.

Контекстный менеджер:

Используем with Pool(...) для автоматического закрытия пула после завершения работы, 
что в принципе является хорошей практикой.

Пример с несколькими процессами и передачей данных
Давайте создадим задачу, где несколько процессов выполняют свои вычисления,
 обмениваются данными между собой и выводят результаты. 
 Мы будем использовать Queue для передачи данных между процессами.




Задача:

Cоздать 3 процесса. Каждый из них будет вычислять квадрат числа от 1 до 3,
 передавать результаты в очередь, а затем главный процесс будет выводить эти результаты по мере их поступления.

from multiprocessing import Process, Queue
import time

# Функция, которая вычисляет квадрат числа и отправляет результат в очередь
def compute_square(num, queue):
    result = num ** 2
    print(f"Процесс {num}: вычисление квадрата {num} = {result}")
    queue.put(result)  # Отправляем результат в очередь
    time.sleep(1)

if __name__ == "__main__":
    # Создаём очередь для обмена данными
    queue = Queue()

    # Создаём 3 процесса, каждый будет вычислять квадрат числа от 1 до 3
    processes = []
    for i in range(1, 4):
        process = Process(target=compute_square, args=(i, queue))
        processes.append(process)
        process.start()

    # Ожидаем завершения всех процессов
    for process in processes:
        process.join()

    # Главный процесс забирает и выводит результаты из очереди
    print("\nРезультаты вычислений:")
    while not queue.empty():
        result = queue.get()  # Получаем результат из очереди
        print(f"Результат: {result}")

    print("Все процессы завершены.")


Что происходит в коде?

Функция compute_square:

Принимает число и очередь.
Вычисляет квадрат числа и выводит сообщение.
Результат помещается в очередь с помощью queue.put(result).
Основной процесс:

Создаёт очередь Queue, которая будет использоваться для обмена данными.
Создаёт 3 процесса, каждый из которых вычисляет квадрат чисел (1, 2 или 3) и отправляет результат в очередь.
Ожидает завершения всех процессов с помощью метода join().
Получение результатов:

После завершения всех процессов главный процесс начинает извлекать данные из очереди с помощью queue.get().
Эти результаты выводятся на экран в том порядке, в котором они были получены.
Чтобы закрепить пройденное, рассмотрим следующую таблицу, отображающую разницу между тремя этими технологиями:


Рассмотрим следующую интерпретацию работы с Queue. Представим автомойки,
 где каждая мойка — это отдельный процесс, а автомобили — это задачи, которые нужно выполнить.


GIL и потоки: очередь на одной мойке

Если бы автомойка работала по принципу GIL, у вас была бы одна большая мойка,
 но обслуживаться автомобили могли бы только по одному, даже если рядом есть другие мойщики.
   Пока один автомобиль чистится, все остальные стоят в очереди, 
   даже если рядом свободно. Это удобно, когда работы немного, но для длинной очереди — это кошмар.

Процессы: отдельные мойки для каждого автомобиля

Теперь представим, что вместо одной мойки вы открыли несколько отдельных боксов.
 Каждый бокс (процесс) работает автономно: у него свои вода, губки, 
 свои работники (память и ресурсы). Если одна мойка сломалась, 
 другие продолжают работать — ничего не останавливает процесс.


Почему это эффективно?

1. Независимая работа.

Каждый бокс обслуживает свой автомобиль и не делит ресурсы с другими.
 Это значит, что никакая проблема в одном боксе не задерживает работу в других.

2. Использование всех возможностей.

У вас, допустим, 8 автомобилей и 4 бокса. Четыре машины одновременно заезжают на мойки, 
а следующие 4 ждут своей очереди. В итоге все работают эффективно,
 и вы используете все возможности своего автопарка (процессора).

3. Очередь под контролем.

Если одна мойка работает медленно или застряла, 
её можно остановить (terminate) и освободить место для другого автомобиля.

Queue (очереди) — это как цепочка автомобилей

Чтобы упорядочить, какие автомобили куда должны заехать, 
мы используем очередь. Представим, что есть шлагбаум, 
который пропускает автомобили в боксы по одному, отправляя их на свободную мойку.
 Как только в боксе заканчивается работа, подаётся сигнал шлагбауму,
   и тот запускает следующую машину. Всё чётко и безопасно.

Pipe — это как прямой разговор

Иногда автомобилям (задачам) нужно «уточнить», где лучше помыть окна, 
а где — кузов. Тогда вместо общей очереди (Queue) каждый автомобиль «переговаривается» с мойщиком напрямую.
 Это и есть Pipe — более быстрый, но ограниченный канал для связи двух процессов.

Pool — это как планировщик для мойки

Если у вас слишком много автомобилей, вы нанимаете менеджера (Pool), 
который будет решать, сколько одновременно должно работать мойщиков,
 кто и когда заезжает на мойку. Например, вы знаете, что у вас только 4 мойщика,
   но хотите обслужить 20 машин. Менеджер запускает первые 4 машины,
     а когда кто-то из мойщиков закончит работу, запускает следующую.

Пример с процессами:

Мы запускаем несколько моек (процессов).
Машины заезжают в случайные боксы.
Каждый бокс работает автономно, не влияя на других.
Если в одном боксе возникла поломка, это никак не мешает остальным продолжать работу.
Сравнение потоков с GIL и процессов в Python — это как выбор между одной универсальной мойкой,
 которая всем управляет, и несколькими автономными боксами, каждый из которых работает на полную мощность.








Типичные ошибки
Тип ошибки 1: Дочерние процессы не успевают завершить выполнение до основного

Ошибка возникает, когда метод join() не используется.

Пример ошибки:

from multiprocessing import Process

def task():
    print("Дочерний процесс выполняется.")

if __name__ == "__main__":
    process = Process(target=task)
    process.start()
    # Забыли вызвать join()
    print("Основной процесс завершен.")
Решение:
Вызывать join() для того, чтобы основной процесс дождался завершения дочернего:

from multiprocessing import Process

def task():
    print("Дочерний процесс выполняется.")

if __name__ == "__main__":
    process = Process(target=task)
    process.start()
    process.join()  # Ждём завершения дочернего процесса
    print("Основной процесс завершён.")






Тип ошибки 2: Недостаточное управление ресурсами

Ошибка возникает при создании большого количества процессов. 
Это может привести к исчерпанию ресурсов системы (например, памяти или времени процессора).

Пример ошибки:

from multiprocessing import Process

def long_task():
    print("Долгая задача выполняется.")

if __name__ == "__main__":
    processes = []
    for _ in range(10000):  # Попытка создать много процессов
        process = Process(target=long_task)
        processes.append(process)
        process.start()

    for process in processes:
        process.join()


Решение:
используем пул процессов Multiprocessing.Pool для управления количеством одновременно работающих процессов.

from multiprocessing import Pool

def long_task(x):
    print(f"Долгая задача выполняется для {x}")

if __name__ == "__main__":
    with Pool(4) as pool:  # Ограничиваем количество одновременно работающих процессов
        pool.map(long_task, range(10))  # Выполняем задачу для нескольких значений







Тип ошибки 3: Игнорирование ошибок в дочерних процессах

Если в дочернем процессе возникает ошибка, она может остаться незамеченной,
 и основной процесс продолжит работу, не зная о сбое.

Пример ошибки:

from multiprocessing import Process

def task():
    print("Процесс начал работу.")
    1 / 0  # Исключение в дочернем процессе
    print("Процесс завершен.")

if __name__ == "__main__":
    process = Process(target=task)
    process.start()
    process.join()
    print("Основной процесс завершен.")

Решение:
логируем ошибки в дочерних процессах и передаём их 
в основной процесс через очередь или другие механизмы IPC.
 Пример с передачей ошибок через Queue:

from multiprocessing import Process, Queue
import traceback

def task(queue):
    try:
        print("Процесс начал работу.")
        1 / 0  # Исключение в дочернем процессе
    except Exception as e:
        queue.put(str(e))  # Отправляем ошибку в очередь

if __name__ == "__main__":
    queue = Queue()
    process = Process(target=task, args=(queue,))
    process.start()
    process.join()

    # Проверяем на наличие ошибок
    if not queue.empty():
        error_message = queue.get()
        print(f"Ошибка в процессе: {error_message}")
    else:
        print("Процесс завершился успешно.")




Тип ошибки 4: Бесконечные создание процессов

Ошибка возникает, когда процессы создаются в основном теле программы 
(вне блока if __name__ == '__main__':).

Пример ошибки:

from multiprocessing import Process

def task():
    print("Процесс работает.")

process = Process(target=task)
process.start()
process.join()
Решение:
Оборачиваем код создания процессов в блок if __name__ == '__main__':.

from multiprocessing import Process

def task():
    print("Процесс работает.")

if __name__ == '__main__':
    process = Process(target=task)
    process.start()
    process.join()








Практикум
Задание 1. Пул процессов для вычисления факториалов

Ситуация: мы проходим собеседование в компании на IT-позицию.
 Нам дали задачу — оптимально вычислить факториалы нескольких чисел.

Задача:

Реализовать программу, которая вычисляет факториалы нескольких чисел,
 используя пул процессов, а именно:

Создать функцию compute_factorial, которая вычисляет факториал числа.
Использовать Pool.map() для параллельного выполнения вычислений.
Ограничить количество одновременно работающих процессов до 4.
Шаги реализации:

Создаём функцию compute_factorial, которая принимает число n и возвращает
 его факториал с помощью math.factorial(n).

Импортируем необходимые модули Pool из Multiprocessing для создания пула процессов и math — для вычисления факториала.
Определяем список чисел, для которых необходимо вычислить факториалы.

Создаём пул с ограничением на 4 одновременно работающих процесса с помощью Pool(4).
Используем метод pool.map(compute_factorial, numbers) для применения функции compute_factorial 

к каждому элементу списка numbers в параллельном режиме.

Выводим результаты вычислений, используя цикл for для отображения каждого числа вместе с его факториалом.
Реализация:

from multiprocessing import Pool
import math

def compute_factorial(n):
    return math.factorial(n)

if __name__ == "__main__":
    numbers = [5, 7, 10, 12, 15]
    with Pool(4) as pool:
        results = pool.map(compute_factorial, numbers)

    for n, result in zip(numbers, results):
        print(f"Факториал {n}: {result}")




Задача 2: Параллельное возведение чисел в степень

Ситуация: мы работаем с большим массивом данных. Для ускорения вычислений, 
нам необходимо распараллелить его обработку.

Задача: реализовать программу, 
которая в нескольких потоках возводит числа определенного массива в некоторую степень.

Создайте функцию power_numbers
Использовать multiprocessing.Queue для передачи данных между процессами.
Шаги реализации:

Создайте функцию power_numbers, которая принимает список чисел и степень, 
в которую нужно возвести эти числа.
Разделите исходный список чисел на несколько частей (по количеству процессов).
Создайте процессы, каждый из которых будет вызывать функцию power_numbers для своей части данных.
Используйте multiprocessing.Queue для передачи результатов от каждого процесса в главный процесс.
Соберите результаты из очереди и выведите их.
Реализация:

from multiprocessing import Process, Queue

def power_numbers(numbers, exponent, result_queue):
    results = [num ** exponent for num in numbers]
    result_queue.put(results)

if __name__ == "__main__":
    numbers = list(range(1, 21))
    exponent = 2
    num_processes = 4

    result_queue = Queue()

    chunk_size = len(numbers) // num_processes
    processes = []
    for i in range(num_processes):
        start_idx = i * chunk_size
        end_idx = (i + 1) * chunk_size if i < num_processes - 1 else len(numbers)
        chunk = numbers[start_idx:end_idx]

        process = Process(target=power_numbers, args=(chunk, exponent, result_queue))
        processes.append(process)
        process.start()

    for process in processes:
        process.join()

    all_results = []
    while not result_queue.empty():
        all_results.extend(result_queue.get())

    print("Результаты:", all_results)




Итоги:
На этом занятии мы:

Познакомились с основами многопроцессорности в Python и понятием процессов.
Научились создавать процессы и управлять ими с помощью модуля Multiprocessing.
Изучили механизмы межпроцессного взаимодействия: Queue и Pipe.
Разобрали использование пула процессов (Pool) для эффективного распределения задач.
Реализовали параллельные вычисления и синхронизацию процессов.
Всё перечисленное выше поможет нам создавать многопроцессорные приложения для обработки
 CPU-bound-задач и управлять взаимодействием между процессами.