Урок №23. Обработка ошибок try/catch.

Учебные материалы

Сегодня мы поговорим о механизме обработки ошибок try/catch, 
который позволяет избежать «падения» приложения при возникновении ошибок в коде, 
данная конструкция состоит из двух блоков:

Блок try: в него помещают часть кода, в котором есть вероятность возникновения ошибки.

Блок catch: в него помещают код, который будет выполнен только в том случае, если в первом блоке возникнет ошибка.

В любой деятельности человека ошибки неизбежны, программирование не исключение. 
Они могут возникать из-за неправильного ввода данных, сбоев в логике программы или внешних факторов. 
Однако важно уметь правильно обрабатывать ошибки, чтобы программа не завершалась аварийно, 
а пользователь получал понятные сообщения о проблеме.

Сегодня мы:

узнаем, что такое ошибки и зачем их обрабатывать;
разберёмся, как ошибки влияют на выполнение программы;
научимся избегать ошибок и обрабатывать их с помощью try/catch;
поймём, почему try/catch лучше, чем if/else в некоторых случаях.



Глоссарий к двадцать третьему занятию
Error (Ошибка)

Error — это объект, который создаётся при возникновении исключительной ситуации во время выполнения программы. 
Ошибки могут быть синтаксическими, логическими или связанными с внешними данными.

Exception (Исключение)

Exception — это событие, которое прерывает нормальный поток выполнения программы. 
В JavaScript исключения обрабатываются с помощью try/catch.

Try (Попытка)

Try — блок кода, в котором выполняется потенциально опасный код. 
Если внутри try возникает ошибка, управление передаётся в блок catch.

Catch (Перехват)

Catch — блок, который выполняется при возникновении ошибки в try. Принимает объект ошибки, 
который можно использовать для анализа проблемы.

Finally (Финализация)

Finally — блок, который выполняется в любом случае независимо от того, произошла ошибка или нет. 
Часто используется для освобождения ресурсов (например, закрытия файлов).

Throw (Выброс ошибки)

Throw — оператор, который позволяет вручную сгенерировать ошибку. Можно использовать для создания кастомных ошибок.

Error Object (Объект ошибки)

Error Object — стандартный объект в JavaScript, содержащий информацию об ошибке (например, name и message). 
Можно расширять для создания собственных типов ошибок.

Потренироваться в переводах и лучше запомнить определения каждого термина мы советуем в приложении Quizlet. 
Чтобы открыть карточки к занятию, перейдите по ссылке.

Что такое ошибки и зачем их обрабатывать?
Ошибка или Исключительная ситуация — это события, которые возникают во время выполнения программы и
приводят к её ненормальному или неправильному поведению.
Обработка ошибок/исключений — специальный механизм, благодаря которому у программы есть инструкции, 
которые необходимо выполнить при возникновении ошибок.

Примеры ошибок:
деление на ноль;
обращение к несуществующему свойству объекта;
передача строки вместо числа в функцию и пр.
Зачем обрабатывать ошибки?
Чтобы программа не ломалась при возникновении проблемы.
Чтобы пользователь получал понятное сообщение об ошибке.
Чтобы можно было безопасно продолжить выполнение кода.
Чтобы лучше понять, как работают исключительные ситуации в программировании, рассмотрим пример:

Допустим, мы пишем программу для игры, где в конце нужно разделить общее количество баллов между всеми участниками.

const totalScore = 1000;
const players = undefined; // допустим, так получилось что к концу игры в команде никого не осталось
let averageScore = totalScore / players.toFixed(2);;
В этом случае возникнет ошибка — деление на ноль, потому что делить на ноль математически невозможно. 
Программа не знает, как обработать такую ситуацию, поэтому она «вылетает» — завершается с ошибкой, 
а пользователь видит сообщение о сбое/зависание и т.п.

Что можно сделать? Самый простой способ — добавить проверку оператором ветвления if (players > 0) перед делением:

const totalScore = 1000;
const players = undefined;
let averageScore;
if (players > 0) {
    averageScore = totalScore / players;
} else {
    console.log("Ошибка: игроков нет, деление невозможно!");
    averageScore = 0; // или другое значение по умолчанию
}
Или мы можем прибегнуть к новому для нас способу (сегодняшняя тема) — обработать исключения, 
то есть заранее предусмотреть возможность появления ошибки в участке кода, где выполняется деление.

В реальных проектах часто используют механизм обработки исключений try/catch, чтобы программа могла 
корректно реагировать на ошибки. Try/catch — конструкция, которая позволяет «ловить» ошибки и обрабатывать их.

Рассмотрим синтаксис:

try {
  // Код, который может вызвать ошибку
} catch (error) {
  // Обработка ошибки
} finally {
  // Код, который выполнится в любом случае
}
Переделаем наш пример — добавим обработки исключений при помощи этой конструкции:

const totalScore = 1000;
const players = undefined;

let averageScore;
try {
    averageScore = totalScore / players;
} catch (error) {
    console.error("Произошла ошибка:", error.message); // "Произошла ошибка: Infinity" (см. пояснение ниже)
    averageScore = 0; // задаем значение по умолчанию
}
Выводы:

Ошибки (исключения) возникают в непредвиденных ситуациях (деление на ноль, 
    обращение к несуществующему свойству, выход за границы массива и т.д.).

Обработка ошибок нужна, чтобы:

программа не завершалась аварийно;
пользователь видел понятное сообщение;
можно было безопасно продолжить выполнение.
Try/catch — удобный механизм для обработки исключений, но не все ошибки в JavaScript автоматически попадают в catch.

Иногда проще и надёжнее использовать проверки if для предупреждения ошибок.

Механизм работы try/catch
Итак, в примере выше мы столкнулись с блоком try/catch, давайте поговорим о нём подробнее.
Блок try/catch — это механизм обработки ошибок в JavaScript, который позволяет:

пытаться выполнить рискованный код (в блоке try);
перехватывать и обрабатывать возможные ошибки (в блоке catch).
Как это работает?
Сначала выполняется код внутри try.
Если ошибки нет, блок catch пропускается.
Если ошибка есть, выполнение try прерывается, и управление переходит в catch.

Что можно перехватить в catch?
Ошибки, сгенерированные через throw (оператора, который вручную вызывает ошибку, 
чтобы прервать выполнение кода и передать управление ближайшему блоку catch, 
подробнее механику его действия мы разберём позже).
Нативные ошибки JavaScript (TypeError, ReferenceError и т.д.).
Исключения из асинхронного кода (если используется async/await).
Вложенные try/catch
Можно комбинировать несколько блоков для разных уровней обработки ошибок. 
Таким образом мы можем разграничить задачи — разные уровни кода могут обрабатывать ошибки разными способами:

Внутренний try/catch — обрабатывает локальные ошибки (например, парсинг данных).

Внешний try/catch — ловит более глобальные сбои (например, ошибку сети).

Пример:

try {
  try {
    JSON.parse("{ некорректный JSON }");
  } catch (error) {
    throw new Error("Ошибка парсинга JSON: " + error.message);
  }
} catch (err) {
  console.error("Внешний перехват:", err.message);
}
Блок finally
Блок finally — это необязательный элемент конструкции try…catch, который выполняется в любом случае (если он присутствует):

если ошибка возникла — после catch;
если ошибки нет — после try.
И даже при return внутри try или catch.

Вот как выглядит синтаксис всей конструкции:

try
{
  // Код, который может вызвать ошибку
}
catch (err)
{
  // Обработка ошибки
}
finally
{
  // Этот код выполнится в любом случае
}
Когда использовать finally?
Основные сценарии применения:

очистка ресурсов — закрытие файлов, соединений с БД;
сброс состояний — отключение лоадеров, возврат исходных значений;
логирование завершения операций.
Как работает finally?
Пример 1: Без ошибок

try {
  console.log('Пытаемся выполнить код');
} catch (err) {
  console.log('Ошибка:', err);
} finally {
  console.log('Это выполнится всегда');
}
// Вывод:
// Пытаемся выполнить код
// Это выполнится всегда
В блоке try выполняется обычный код, который не вызывает ошибок, — просто выводится сообщение в консоль. 
Поскольку исключений не возникает, блок catch полностью пропускается. Однако блок finally выполняется в 
любом случае независимо от наличия ошибок, что и демонстрирует этот пример. 
Это ключевая особенность finally — он гарантированно выполнится после try,
 если только скрипт не будет завершён принудительно.

Пример 2: С ошибкой

try {
  throw new Error('Что-то пошло не так!');
} catch (err) {
  console.log('Ошибка:', err.message);
} finally {
  console.log('Это выполнится всегда');
}
// Вывод:
// Ошибка: Что-то пошло не так!
// Это выполнится всегда
В этом примере специально создаётся ошибка с помощью оператора throw (его мы подробно разберём позже, 
    сейчас он нужен просто для демонстрации работы перехвата ошибки). Когда выполнение доходит до строки 
    с вызовом ошибки (throw ...;), работа блока try немедленно прерывается и управление передаётся в блок catch, 
    где мы видим обработку ошибки — вывод сообщения об ошибке в консоль. Но, несмотря на возникновение исключения, 
    блок finally всё равно выполняется после catch, подтверждая его надёжность. 
    Это особенно важно для освобождения ресурсов, даже если в процессе работы произошла ошибка.

Пример 3: С return

function test() {
  try
  {
    return 'Успех';
  }
  catch (err)
  {
    return 'Ошибка';
  } finally
  {
    console.log('Finally выполняется ДО return');
  }
}

console.log(test());
// Вывод:
// Finally выполняется ДО return
// Успех
Данный пример демонстрирует важную особенность взаимодействия finally и return. 
Хотя в блоке try стоит инструкция return, которая должна немедленно завершить выполнение функции, 
блок finally имеет приоритет и выполняется перед фактическим возвратом значения. 
Это показывает, что finally выполняется не просто после try/catch, а именно перед выходом из конструкции,
 даже если он происходит через return. Такое поведение гарантирует, что код в блоке finally точно выполнится.

Особенности работы finally
Приоритет над return

Блок finally обладает уникальным поведением в JavaScript — он выполняется перед оператором return, 
даже если return находится в блоке try. Это создаёт интересный эффект «отложенного возврата»:

function example()
{
  try
  {
    return 'Значение';
  }
  finally
  {
    console.log('Finally сработал');
  }
}

console.log(example());
// Вывод:
// Finally сработал
// Значение
Движок JS видит return в блоке try и запоминает возвращаемое значение. 
Однако перед фактическим выходом из функции он обязательно выполняет блок finally.

Только после полного выполнения finally функция действительно возвращает запомнившееся значение. 
Даже если функция пытается вернуть результат, код в блоке finally выполнится.

Возврат значений из finally

Если в finally есть свой return, он перезапишет возвращаемое значение:

function test() {
  try {
    return 'Из try';
  } finally {
    return 'Из finally'; // Перезапишет возврат из try
  }
}

console.log(test());
Важно!
Такой подход считается антипаттерном (распространённым, но не правильным подходом к решению задачи!)
Finally в промисах
Аналог finally для промисов — метод .finally():

fetch('https://api.example.com')
  .then(response => response.json())
  .catch(err => console.error('Ошибка:', err))
  .finally(() => console.log('Запрос завершен'));
5. Практические примеры
Пример 1: Закрытие соединения с БД
javascript
let dbConnection = openDatabase();

try {
  const data = dbConnection.query('SELECT * FROM users');
  processData(data);
} catch (err) {
  console.error('Ошибка запроса:', err);
} finally {
  dbConnection.close(); // Всегда закрываем соединение
  console.log('Соединение закрыто');
}
Пример: Скрытие лоадера

Весьма распространённая проблема, когда лоадер (индикатор загрузки в виде песочных часов, 
    полоски или надписи «загрузка») не исчезает со страницы при возникновении ошибки, 
    но мы это предусмотрим в блоке finally, и независимо от наличия ошибки лоадер не останется на экране:

async function loadData() {
  showLoader();

  try {
    const data = await fetchData();
    renderData(data);
  } catch (err) {
    showError(err);
  } finally {
    hideLoader(); // Всегда скрываем индикатор загрузки
  }
}
Игнорирование ошибок в finally
Если cleanup() выбросит ошибку, она перекроет исходную:

try
{
  // ...
}
finally
{
  cleanup();
}
Если в блоке finally произойдёт ошибка (например, функция cleanup() упадёт), она перезапишет оригинальную ошибку из try.

Это может затруднить отладку, так как:

исходная ошибка будет потеряна;
в консоли увидим только ошибку из finally.
Неожиданное поведение с промисами
Finally выполнится ДО разрешения промиса:

async function example()
{
  try
  {
    return await fetchData();
  }
  finally
  {
    console.log('Завершение');
  }
}
Порядок выполнения:

Блок finally срабатывает до того, как промис fetchData() разрешится (resolved/rejected).

Код выглядит логично, хотя на деле finally выполнится сразу после await, но до получения данных, 
это может привести к race condition (гонке данных; возникает, когда несколько процессов/потоков/запросов 
    конкурируют за общие ресурсы, и результат работы зависит от того, кто успеет первым), если, например, 
    в finally закрывается соединение с БД.

Оператор throw
Throw — это оператор в JavaScript, который позволяет собственноручно создавать и вызывать ошибки в процессе 
выполнения программы. Это полезно, когда мы хотим прервать нормальное выполнение кода и сообщить о проблеме.

throw new Error("Что-то пошло не так!");

При выполнении этого кода программа остановится и в консоли появится сообщение об ошибке.

Зачем использовать throw?
Может показаться странным: зачем нам самим вызывать ошибку, если программа и так может «упасть» сама? 
На самом деле throw — это механизм контролируемого прерывания выполнения кода, 
когда дальнейшая работа программы либо невозможна, либо небезопасна.

Throw помогает нам остановить выполнение программы при некорректных данных. Если функция получает
невалидные аргументы, например, отрицательные в поле возраста, лучше явно выбросить ошибку, чем продолжить работу 
с неправильными данными — тратить ресурсы вычислительные и временные, заведомо двигаясь в неправильном направлении.

Итак, throw нужен для:

контроля ошибок — мы можем явно указать, при каких условиях программа должна считать ситуацию ошибочной;
кастомизации ошибок — можно создавать свои типы ошибок с понятными сообщениями;
прерывания выполнения — если дальнейшая работа функции невозможна, 
throw останавливает её и передаёт управление ближайшему блоку catch.
Throw может работать с любым значением:

строками (throw «Ошибка!»);
числами (throw 404);
объектами (throw { code: 500, message: «Сервер упал» }).
Но лучшая практика — использовать встроенный класс Error или его наследников (SyntaxError, TypeError и т.д.),
 потому что они автоматически собирают стек вызовов, что удобно при отладке, и это стандартный подход в JavaScript.

throw new Error("Неверный формат данных");
Пример с кастомной ошибкой:
javascript
class ValidationError extends Error {
  constructor(message) {
    super(message);
    this.name = "ValidationError";
  }
}

throw new ValidationError("Поле 'email' обязательно");
Обработка throw
Выброшенные ошибки можно перехватить с помощью try/catch, чтобы программа не завершалась аварийно.

try {
  const age = 15;
  if (age < 18) {
    throw new Error("Доступ запрещён: возраст меньше 18");
  }
  console.log("Доступ разрешён");
} catch (err) {
  console.error("Ошибка:", err.message); // "Доступ запрещён: возраст меньше 18"
}
Рассмотрим пример использования throw для закрепления:

function sum(a, b)
{
  if (typeof a !== "number" || typeof b !== "number")
    throw new Error("Оба аргумента должны быть числами!"); // Генерация ошибки
  return a + b;
}

try {
  console.log(sum(5, "10")); // Ошибка!
} catch (error) {
  console.log("Ошибка:", error.message); // Выведет сообщение об ошибке
} finally {
  console.log("Попытка завершена.");
}
Итак, throw — это «аварийный тормоз», который позволяет вручную остановить выполнение 
при некорректных данных или в небезопасных условиях.

Особенности обработки асинхронных ошибок
В асинхронном JavaScript ошибки ведут себя иначе, чем в синхронном коде:

try
{
  setTimeout(() =>
  { throw new Error("Асинхронная ошибка");}, 1000);
}
catch (err)
{
  console.log("Эта ошибка НЕ будет поймана");
}
Как видно из примера выше, блок catch не перехватит ошибку, выброшенную внутри setTimeout. 
Она останется необработанной и приведёт к Uncaught Error: 
Асинхронная ошибка в консоли или Возможное падение приложения (в Node.js).

Почему же так происходит?
Дело в том, что контекст try/catch завершается до выполнения асинхронного кода, try/catch работает 
только для синхронного кода внутри блока try, а setTimeout — асинхронная операция: колбэк выполняется позже, 
когда контекст try/catch уже завершился.

И что в такой ситуации делать?

Приведём решения проблемы обработки асинхронных ошибок.

Способ 1: Для простых запросов → .catch()
fetch("https://api.example.com/data")
  .then(response => response.json())
  .catch(err => {
    console.error("Ошибка запроса:", err);
    return { data: [] }; // Возвращаем fallback-значение
  });
Когда использовать?

Когда работаем с цепочкой промисов.
Когда нужно обработать ошибки на последнем этапе цепочки.
Когда требуется вернуть запасные данные при ошибке.
Плюсы решения:

компактный синтаксис для простых случаев;
позволяет продолжить цепочку then после обработки ошибки;
чёткое разделение успешного пути (then) и обработки ошибок (catch).
Минусы:

не обрабатывает синхронные ошибки в then-обработчиках;
может «проглатывать» ошибки, если не пробросить их явно;
менее нагляден для сложной логики обработки.
Способ 2: Для сложной логики → async/await
async function loadData() {
  try {
    const response = await fetch("https://api.example.com/data");
    const data = await response.json();
    return data;
  } catch (err) {
    console.error("Ошибка загрузки:", err);
    throw err; // Пробрасываем ошибку выше
  }
}
Когда использовать?

В асинхронных функциях.
Когда нужен полный контроль над обработкой ошибок.
Для сложной логики с множественными await-вызовами.
Когда важно не пропустить ни одну ошибку.
Плюсы:

обрабатывает как асинхронные, так и синхронные ошибки;
более понятный линейный код;
позволяет делать несколько попыток (retry);
даёт возможность разного поведения для разных типов ошибок.
Минусы:

требует оборачивания в async-функцию;
немного больше boilerplate (повторяющегося стандартного кода).
Then-обработчик
Then-обработчик — это функция, которая прикрепляется к промису через метод .then() и выполняется:

Когда промис успешно завершается (resolve) — получает результат.
Когда промис завершается с ошибкой (reject) — если нет catch-обработчика.
promise.then(
  onFulfilled, // Выполнится при успехе (resolve)
  onRejected   // (Необязательно) Выполнится при ошибке (reject)
);
Проблема: then-обработчик не ловит синхронные ошибки.
Если внутри onFulfilled (первая функция в .then()) возникнет синхронная ошибка (например, 
    обращение к несуществующему свойству), она не будет перехвачена вторым аргументом onRejected или последующим .catch().

Пример:

const promise = Promise.resolve("успех");

promise.then(
  (result) => {
    console.log(result);
    throw new Error("Синхронная ошибка внутри then!"); // Не поймается!
  },
  (error) => {
    console.error("Этот код не сработает:", error); // Не выполнится
  }
).catch((err) => {
  console.error("И этот catch тоже не поможет:", err); // Не выполнится
});
Что произойдёт?

Промис успешно завершится (resolve).
Then-обработчик начнёт выполняться.
Синхронная ошибка throw new Error() вылетит наружу и приведёт к аварийному завершению программы (ошибка в консоли).

Почему так происходит?
onRejected в .then() ловит только ошибки самого промиса, а не синхронные внутри обработчика.

Синхронные ошибки в then-обработчике не попадают в цепочку промисов.

Для обработки таких ошибкок необходимо использовать .catch() после .then().

promise.then((result) => {
    console.log(result);
    throw new Error("Ошибка в then");
  })
  .catch((err) => {
    console.error("Поймали ошибку:", err); // Сработает!
  });
.catch() перехватывает ошибки исходного промиса и синхронные ошибки из любых then-обработчиков выше.

Обернуть опасный код в try/catch внутри then:

promise.then((result) => {
  try {
    console.log(result);
    throw new Error("Ошибка в then");
  } catch (err) {
    console.error("Локальный перехват:", err); // Сработает
  }
});
Если нам нужно обработать ошибку точечно, не прерывая цепочку промисов.

Рассмотрим ещё один пример для закрепления. Мы получаем данные с сервера и обрабатываем их в then:

fetch("/api/data")
  .then((response) => {
    const data = response.json(); // Опасная операция (может быть ошибка синтаксиса)
    console.log(data);
  })
  .catch((err) => {
    console.error("Ошибка:", err); // Поймает и ошибку сети, и синхронную ошибку в then
  });
Если response.json() получит битый JSON, возникнет синхронная ошибка SyntaxError.
Без .catch() она приведёт к «падению» приложения.

Итог:

then-обработчик — функция, которая выполняется при успехе/ошибке промиса;
then-обработчик не перехватывает синхронные ошибки внутри себя — для этого нужен .catch() или try/catch;
всегда добавляем .catch() в конце цепочки промисов, чтобы избежать неожиданных падений.
Глобальная обработка ошибок
Глобальная обработка ошибок — это механизм перехвата всех необработанных исключений в приложении. Она нужна, чтобы:

предотвращать аварийное завершение программы;
логировать критические ошибки;
показывать пользователям дружелюбные сообщения;
отправлять отчёты об ошибках на сервер.
Способы глобальной обработки
Этот код реализует глобальный перехватчик ошибок в браузерном JavaScript, который ловит два типа 
необработанных проблем: перехват всех необработанных ошибок и перехват необработанных промисов. Вот как это выглядит:

// 1. Перехват всех необработанных ошибок
window.addEventListener('error', (event) => {
  console.error('Глобальная ошибка:', event.error);
  event.preventDefault(); // Предотвращаем вывод в консоль
});
// 2. Перехват необработанных промисов
window.addEventListener('unhandledrejection', (event) => {
  console.error('Необработанный промис:', event.reason);
  event.preventDefault();
});
Перехватывает:

Синхронные ошибки (например, undefined() или throw new Error())
Ошибки загрузки ресурсов (скриптов, изображений)
Другие глобальные ошибки выполнения
НЕ перехватывает:

Ошибки внутри промисов и асинхронного кода
Какие ошибки можно перехватить глобально?
Глобальные обработчики ошибок могут ловить:

1. Синхронные ошибки — обычные исключения, которые возникают в основном потоке выполнения.

// 1. Явный выброс ошибки
throw new Error("Что-то пошло не так");
// 2. Ошибки в операциях
const value = undefined;
console.log(value.property); // TypeError
// 3. Ошибки в блоках try без catch
try {
  riskyOperation();
} finally {
  // Если riskyOperation выбросит ошибку,
  // она не будет обработана и попадёт в глобальный обработчик
}
Эти ошибки автоматически «всплывают» до глобального обработчика, если их не перехватил локальный try/catch.

2. Асинхронные ошибки — возникают вне основного потока выполнения.

// 1. Необработанные промисы
Promise.reject(new Error("Промис отклонён"));

// 2. Ошибки в таймерах
setTimeout(() => {
  throw new Error("Ошибка в setTimeout");
}, 1000);

// 3. Ошибки в событиях
button.addEventListener('click', () => {
  throw new Error("Ошибка в обработчике клика");
});
Асинхронные ошибки не перехватываются обычным try/catch, а требуют специальных глобальных обработчиков, 
таких как window.onerror (браузеры). Асинхронные ошибки часто остаются незамеченными,
но приводят к «тихому» падению функциональности, утечкам памяти и непредсказуемому поведению.

Объект Error
Объект Error — это стандартный встроенный класс в JavaScript для представления ошибок. 
Он используется, когда нужно создать исключение с дополнительной информацией о проблеме,
 то есть вывести название и описание ошибки:

const error = new Error('Сообщение об ошибке');
console.log(error.message); // "Сообщение об ошибке"
Объект Error — это фундаментальная часть системы обработки ошибок в JavaScript.
 Он напрямую интегрирован с механизмом try/catch:

try
{
  throw new Error('Произошла ошибка'); // Создание ошибки
}
catch (err)
{ // err — это экземпляр Error
  console.log(err.message); // "Произошла ошибка"
}
Как try/catch работает с Error?
В блоке try создаётся ошибка:

throw new SyntaxError('Неверный синтаксис');

Выполнение немедленно переходит в catch.

Объект ошибки доступен через параметр catch:

catch (err) { // err — это SyntaxError console.log(err.name); // "SyntaxError"}

Пример с пользовательской ошибкой:

class NetworkError extends Error {
  constructor(url) {
    super(`Сбой подключения к ${url}`);
    this.name = 'NetworkError';
  }
}

try {
  throw new NetworkError('https://api.example.com');
} catch (err) {
  if (err instanceof NetworkError) {
    console.error('Проблемы с сетью:', err.message);
  } else {
    console.error('Другая ошибка:', err);
  }
}
Пример обработки разных типов Error:

try {
  // Код, который может выбросить разные ошибки
  JSON.parse('{invalid json}');
} catch (err) {
  if (err instanceof SyntaxError) {
    console.log('Ошибка синтаксиса:', err.message);
  } else if (err instanceof TypeError) {
    console.log('Ошибка типа данных:', err.message);
  } else {
    console.log('Неизвестная ошибка:', err);
  }
}
Объекты Error попадают в глобальные обработчики, если их не перехватили:

window.onerror = (message, url, line, col, error) =>
{ console.log('Неперехваченная ошибка:', error); };

// Пример необработанной ошибки
setTimeout(() => { throw new TypeError('Глобальная ошибка');
}, 1000);
Итог:

Error — базовый класс для всех ошибок в JavaScript;
try/catch — механизм для локальной обработки ошибок;
встроенные ошибки (SyntaxError, TypeError) помогают точнее диагностировать проблемы;
кастомные ошибки упрощают поддержку кода;
глобальные обработчики (error, unhandledrejection) страхуют от пропущенных ошибок.
If/else vs try/catch
If/else — это инструмент для управления потоком выполнения на основе предсказуемых условий. 
Он реализует ветвление логики программы, где разработчик явно описывает все возможные сценарии.

Try/catch — механизм обработки исключительных ситуаций, предназначенный для перехвата непредвиденных ошибок.
 Это защитная конструкция, которая изолирует опасные операции и предоставляет «аварийный выход» при сбоях.

Разберёмся, от чего зависит выбор нужного нам инструмента.

Области применения
If/else лучше использовать для:

валидации входных параметров;
обработки ожидаемых альтернативных сценариев;
реализации бизнес-логики с чёткими условиями;
проверки состояний системы.
Try/catch эффективен для:

работы с внешними ресурсами (API, БД, файловой системой);
опасных операций (парсинга, математических вычислений);
ситуаций, где возможны непредсказуемые сбои;
обеспечения отказоустойчивости критически важных участков кода.
Критерии сравнения
Производительность
Конструкция if/else имеет минимальные накладные расходы, так как не требует создания объектов ошибок 
и управления контекстом выполнения. Try/catch создаёт дополнительную нагрузку из-за необходимости 
формирования стека вызовов и обработки исключений, что особенно заметно в циклах.

Читаемость и поддержка
If/else обеспечивает линейную и предсказуемую структуру кода, где все ветви видны явно. Try/catch разделяет 
код на «нормальный» и «аварийный» пути, что может усложнить восприятие логики, 
но делает обработку ошибок более систематизированной.

Гибкость обработки
If/else требует явного предвидения всех возможных условий. Try/catch перехватывает любые исключения в своём блоке, 
включая ошибки из вложенных вызовов, что делает его более универсальным для сложных операций.

Безопасность
Try/catch гарантирует, что ни одна ошибка не останется необработанной в своём блоке, предотвращая 
неожиданные «падения» приложения. If/else оставляет возможность пропустить какие-то условия, 
что может привести к непредсказуемому поведению.

Рекомендации по выбору
Выбор между этими конструкциями должен основываться на природе проверяемого условия:

Используем if/else для проверки ожидаемых состояний и реализации бизнес-логики. 
Это инструмент для управления нормальным потоком выполнения программы.
Применяем try/catch для обработки исключительных ситуаций и ошибок, которые не являются частью нормального
 workflow-приложения. Это механизм обеспечения надёжности и отказоустойчивости.
Комбинируем оба подхода: предварительную валидацию выполняем через if/else, а опасные операции 
оборачиваем в try/catch. Это обеспечит и производительность, и безопасность.
Выводы

If/else — инструмент проектирования, try/catch — инструмент защиты.
Условия if/else должны покрывать все ожидаемые сценарии.
Try/catch перехватывает как ожидаемые, так и неожиданные ошибки.
Производительность if/else выше, но try/catch обеспечивает лучшую надёжность.
В хорошо спроектированной системе оба механизма могут использоваться совместно, дополняя друг друга.
Несмотря на кажущуюся простоту этих конструкций, разработчики регулярно допускают характерные ошибки.
 Рассмотрим их на конкретных примерах и разберёмся, как избежать подобных проблем.

Типичные ошибки
Тип ошибки 1: Неперехваченная ошибка (Uncaught Error)
Описание:

Когда throw выбрасывает исключение, но нет блока try/catch для его обработки, программа аварийно завершается.

Пример ошибки (код):

function validateAge(age)
{
  if (age < 18) throw new Error("Доступ запрещён");
    console.log("Доступ разрешён");
}
validateAge(16); // Uncaught Error: Доступ запрещён
Объяснение:

Без try/catch ошибка «прорывается» наружу и останавливает выполнение скрипта, что плохо для пользовательского опыта.

Решение:

javascript
try {
  validateAge(16);
} catch (err) {
  console.error("Ошибка:", err.message); // "Доступ запрещён"
}
console.log("Программа продолжает работу");
Всегда оборачиваем потенциально опасные операции в try/catch.

Тип ошибки 2: «Проглатывание» ошибок в пустом catch
Описание:

Когда блок catch есть, но ошибка игнорируется, что затрудняет отладку.

Пример ошибки (код):

javascript
try {
  JSON.parse("{ некорректный JSON }");
} catch {
  // Пустой блок - ошибка исчезла!
}
Объяснение:

Ошибка теряется, пользователь не видит проблему, а разработчик не знает о сбое.

Решение:

javascript
try {
  JSON.parse("{ некорректный JSON }");
} catch (err) {
  console.error("Ошибка парсинга:", err.message);
  // Или хотя бы:
  // console.error(err);
}
Никогда не оставляем пустой catch. Минимум — логирование ошибки.

Тип ошибки 3: Неправильное использование throw (не Error)
Описание:

Когда вместо объекта Error бросают строку или число, теряя стек вызовов.

Пример ошибки (код):

throw "Что-то пошло не так"; // Плохо!
Объяснение:

Строковые ошибки не содержат .stack, что критично для отладки сложных сценариев.

Решение:

throw new Error("Что-то пошло не так"); // Хорошо!
Всегда используем new Error() для броска ошибок.

Тип ошибки 4: Try/catch не ловит асинхронные ошибки
Описание:

Ошибки в setTimeout, Promise или fetch не перехватываются обычным try/catch.

Пример ошибки (код):

try {
  setTimeout(() => {
    throw new Error("Асинхронная ошибка!");
  }, 1000);
} catch (err) {
  console.log("Не сработает!"); // Ошибка проскочит
}
Объяснение:

Try/catch работает только для синхронного кода. Асинхронные ошибки возникают позже,
когда контекст выполнения уже вышел из try.

Решение:

// Для промисов:
someAsyncFunction()
  .then(...)
  .catch(err => console.error(err));

// Для async/await:
(async () => {
  try {
    await someAsyncFunction();
  } catch (err) {
    console.error(err);
  }
})();
Для асинхронного кода используем .catch() или async/await с try/catch.

Тип ошибки 5: Неожиданное поведение finally
Описание:

Блок finally выполняется всегда, даже если в try или catch был return, что может привести к неочевидным результатам.

Пример ошибки (код):

function getData() {
  try {
    return "Данные";
  } catch {
    return "Ошибка";
  } finally {
    return "Finally"; // Переопределит возврат!
  }
}
console.log(getData()); // "Finally", а не "Данные"!
Объяснение:

Finally имеет приоритет над return в try/catch. Это особенность JavaScript.

Решение:

function getData() {
  try {
    return "Данные";
  } catch {
    return "Ошибка";
  } finally {
    console.log("Завершение работы"); // Без return
  }
}
Избегаем return в finally, если не понимаем последствий.

Практикум
Задача 1: Валидация данных пользователя
Ситуация:

Вы разрабатываете форму регистрации и хотите проверить возраст пользователя. Если возраст младше 18 лет,
 нужно выбросить ошибку.

Задача:

Напишите функцию validateAge(age), которая:

Бросает ошибку с сообщением «Доступ запрещён», если возраст < 18.
Возвращает «Доступ разрешён», если возраст ≥ 18.
Ошибка не должна прерывать выполнение программы.
Решение:

function validateAge(age) {
  if (age < 18) {
    throw new Error("Доступ запрещён");
  }
  return "Доступ разрешён";
}

try {
  const result = validateAge(16);
  console.log(result);
} catch (error) {
  console.error(error.message); // "Доступ запрещён"
}
console.log("Проверка завершена");
Задача 2: Обработка нескольких промисов
Ситуация:

Вы делаете несколько параллельных запросов к API и хотите корректно обработать ошибки.

Задача:

Используйте Promise.allSettled() для:

Параллельного выполнения 3 промисов.
Анализа результатов (успех/ошибка).
Вывода статистики.
Решение:

const requests = [
  Promise.resolve("Данные 1"),
  Promise.reject("Ошибка 404"),
  Promise.resolve("Данные 3")
];

async function handleRequests() {
  const results = await Promise.allSettled(requests);

  const success = results.filter(r => r.status === "fulfilled");
  const errors = results.filter(r => r.status === "rejected");

  console.log(`Успешно: ${success.length}`);
  console.log(`Ошибок: ${errors.length}`);
  errors.forEach(err => console.error(err.reason));
}

handleRequests();
Итоги
Сегодня мы:

узнали, что такое ошибки и зачем их обрабатывать;
разобрались, как ошибки влияют на выполнение программы;
научились избегать ошибок и обрабатывать их с помощью try/catch;
поняли, почему try/catch лучше, чем if/else в некоторых случаях.
Грамотная работа с ошибками – это не просто «техническая необходимость»,
а признак профессионального подхода к разработке. Теперь мы сможем создавать по-настоящему надёжные приложения.