Урок №9. Методы массивов.

Учебные материалы

На прошлом занятии мы узнали, что массивы — это удобный способ хранить и упорядочивать данные; 
научились создавать их, обращаться к элементам и перебирать с помощью циклов. 
Уже на этих базовых примерах стало ясно: массивы — мощный инструмент, 
ведь почти в каждой программе приходится работать с наборами данных.

Но если ограничиться только циклами, код быстро становится громоздким. 
Представьте, что вам каждый раз нужно самому собирать шкаф из досок — без инструментов. 
Неудобно? Вот и с массивами так же.

Чтобы не писать одни и те же функции вручную, 
в JavaScript есть встроенные методы массивов. 
Это как готовые инструменты: добавление, удаление, фильтрация, 
поиск — всё уже предусмотрено. Осталось только научиться ими пользоваться.

Сегодня мы:

изучим основные методы для добавления и удаления элементов из массива, такие как push(), pop(), shift() и unshift();
поймём, как изменять массивы с помощью методов splice() и slice();
научимся использовать методы, позволяющие сортировать и перебирать элементы массива, 
а также анализировать весь набор данных одновременно.




Глоссарий к девятому занятию
Array (Массив)
Массив — это структура данных, которая используется для хранения множества значений. 
В JavaScript массивы упорядочены, а каждый элемент в массиве доступен через его индекс, начиная с нуля.

Element (Элемент)
Элемент — это отдельное значение, хранящееся в массиве. 
Элементы могут быть любого типа: числа, строки, объекты и т. д. 
Например, в массиве [1, 2, 3] числа 1, 2 и 3 — это элементы массива.

Method (Метод)
Метод — это функция, которая является свойством объекта. 
В контексте массивов это функции, которые выполняют различные действия с массивами, 
такие как добавление, удаление или модификация элементов.

Push (Протолкнуть — добавить в конец один элемент)
Метод push добавляет один или несколько элементов в конец массива. 
Например, array.push(4) добавит число 4 в конец массива. 
Метод изменяет сам массив и возвращает его новую длину.

Pop (Лопнуть — удалить последний элемент)
Метод pop удаляет последний элемент из массива. 
Например, array.pop() удалит последний элемент в массиве и вернёт его значение.

Shift (Сдвинуть — в JavaScript имеется в виду удалить первый элемент массива и сдвинуть остальные элементы к началу)
Метод shift удаляет первый элемент из массива. 
Например, если массив [1, 2, 3], то после array.shift() массив станет [2, 3].

Unshift (Отменить сдвиг — добавить элемент в начало, 
    подвинув при этом все остальные элементы)
Метод unshift добавляет один или несколько элементов в начало массива. 
Например, array.unshift(0) добавит 0 в начало массива.

Splice (Стыковать разные части)
Метод splice позволяет изменять массив: удалять элементы или добавлять новые. 
Например, array.splice(1, 1, 4) удалит элемент с индексом 1 и добавит число 4 на его место.

Slice (Срез)
Метод slice создаёт новый массив, 
начиная с указанного индекса и до другого индекса (не включая его). 
Например, array.slice(1, 3) вернёт новый массив, содержащий элементы с индексами 1 и 2.

ForEach (Для каждого из)
Метод forEach выполняет указанную функцию для каждого элемента массива. 
Это удобный способ пройти по всем элементам массива без создания нового массива.

Find (Найти)
Метод find возвращает первый элемент массива, который удовлетворяет заданному условию. 
Если такой элемент не найден, метод возвращает undefined. 
Например, array.find(el => el > 2) найдёт первый элемент, который больше 2.

Filter (Фильтровать)
Метод filter создаёт новый массив, содержащий все элементы, 
которые удовлетворяют заданному условию. 
Например, array.filter(el => el % 2 === 0) вернёт массив только с чётными числами.

Map (Преобразовать / задать соответствие)
Метод map создаёт новый массив, 
в котором каждый элемент является результатом выполнения функции над
 соответствующим элементом исходного массива. 
 Например, array.map(el => el * 2) умножит каждый элемент массива на 2.

Reduce (Уменьшить/схлопнуть)
Метод reduce применяет функцию ко всем элементам массива, 
сводя их к одному значению. 
Например, array.reduce((sum, el) => sum + el, 0) посчитает сумму всех элементов массива.

Sort (Сортировать)
Метод sort сортирует элементы массива. 
Важно помнить, что сортировка изменяет исходный массив. 
Например, array.sort() отсортирует массив по умолчанию, 
но результат может быть не всегда ожидаемым, если элементы не являются строками.

Reverse (Обратить порядок)
Метод reverse изменяет порядок элементов в массиве на противоположный. 
Например, после array.reverse() массив [1, 2, 3] станет [3, 2, 1].

IndexOf (Индекс элемента)
Метод indexOf ищет элемент в массиве и возвращает его индекс. 
Если элемент не найден, возвращается -1. Например, array.indexOf(2) вернёт индекс элемента 2 в массиве.





Как мы уже убедились на предыдущем занятии, массивы в JavaScript — это мощный инструмент, 
позволяющий хранить коллекции данных и управлять ими.
 Одним из самых полезных инструментов при работе с массивами в JavaScript являются методы массивов. 
 Они помогают добавлять, удалять, изменять и искать данные, 
 а также производить сложные действия с массивами. 
 Давайте разберём несколько основных методов, которые делают работу с массивами удобной и гибкой.

1. Основные методы для добавления и удаления элементов
push() и pop()
Представим, что в нашей программе мы имеем дело с массивом данных. 
Тогда почти наверняка, помимо перебора элементов, 
нам потребуется умение изменять массив: добавлять элементы к текущим значениям или удалять их.

Методы push() и pop() используются для добавления и удаления элементов с конца массива.

push() добавляет один или несколько элементов в конец массива и возвращает новую длину массива.

Пример:

let fruits = ["apple", "banana"];
fruits.push("cherry");
console.log(fruits); // ["apple", "banana", "cherry"]
pop() удаляет последний элемент из массива и возвращает его.
Пример:

let fruits = ["apple", "banana", "cherry"];
let lastFruit = fruits.pop();
console.log(lastFruit); // "cherry"
console.log(fruits);    // ["apple", "banana"]
unshift() и shift()
Есть также и аналогичные методы unshift() и shift(). 
Они работают с началом массива, добавляя и удаляя элементы с первого места.

unshift() добавляет один или несколько элементов в начало массива и возвращает новую длину массива.

Пример:

let fruits = ["banana", "cherry"];
fruits.unshift("apple");
console.log(fruits); // ["apple", "banana", "cherry"]
shift() удаляет первый элемент из массива и возвращает его.
Пример:

let fruits = ["apple", "banana", "cherry"];
let firstFruit = fruits.shift();
console.log(firstFruit); // "apple"
console.log(fruits);     // ["banana", "cherry"]
Особенности shift() и unshift()

Как уже понятно из названия методов, 
при добавлении или удалении элементов с начала массива все остальные элементы сдвигаются на одну позицию. 
Это делает такие операции менее эффективными, 
чем добавление или удаление элементов с конца массива с помощью push() и pop(). 
Сдвиг всех элементов массива требует дополнительных вычислений и времени, 
что увеличивает время выполнения операции, особенно для больших массивов. 
Но, если часто работать с элементами в начале массива, это может затронуть производительность программы.

2. Операции с элементами массива
Часто при разработке приходится также добавлять и изменять данные прямо посередине массивов 
или создавать копии массивов данных. Для этого в Javascript используются методы splice() и slice()

splice()
Метод splice() позволяет изменять содержимое массива путём удаления или добавления элементов в любом его месте.

Синтаксис: array.splice(start, deleteCount, item1, item2, ...)
start — индекс, с которого начинается изменение.
deleteCount — количество элементов для удаления.
item1, item2, ... — элементы, которые будут добавлены на место удалённых.
* Метод возвращает массив удалённых элементов. Если ничего не удаляется, возвращается пустой массив.

Пример:

let fruits = ["apple", "banana", "cherry"];
let removed = fruits.splice(1, 1, "kiwi", "mango");

console.log(fruits);  // ["apple", "kiwi", "mango", "cherry"]
console.log(removed); // ["banana"]
slice()
Метод slice() используется для создания нового массива, 
который является копией части оригинального массива. 
В отличие от splice(), slice(), не изменяет исходный массив.

Синтаксис: array.slice(start, end)

start — индекс, с которого начинается копирование.
end — индекс, на котором копирование заканчивается (не включая этот элемент).
Пример:

let fruits = ["apple", "banana", "cherry", "kiwi"];
let citrus = fruits.slice(1, 3);
console.log(citrus); // ["banana", "cherry"]
3. Поиск и фильтрация данных
find() и findIndex()
Методы find() и findIndex() используются для поиска первого элемента в массиве, 
который соответствует условию. 
Эти методы чрезвычайно полезны для приложений с поиском по данным: 
они помогают найти значения по условию или убедиться, что подходящих значений нет.

find() возвращает первый элемент, который удовлетворяет условию.
findIndex() возвращает индекс первого элемента, который удовлетворяет условию.
Пример:

let fruits = ["apple", "banana", "cherry"];
let result = fruits.find(fruit => fruit === "banana");
console.log(result); // "banana"
let fruits = ["apple", "banana", "cherry"];
let index = fruits.findIndex(fruit => fruit === "banana");
console.log(index); // 1
includes()
Метод includes() проверяет, содержит ли массив определённый элемент.

Пример:

let fruits = ["apple", "banana", "cherry"];
let hasBanana = fruits.includes("banana");
console.log(hasBanana); // true
filter()
Остановимся чуть подробнее на методе filter(), 
который используется для создания нового массива, состоящего из элементов, 
которые удовлетворяют заданному условию. Этот метод очень полезен, 
когда нужно отфильтровать элементы исходного массива, не меняя его.

Как это работает? Метод filter() принимает в качестве аргумента функцию обратного вызова (callback-функцию), 
которая выполняется для каждого элемента массива. 
Эта функция должна вернуть логическое значение (true или false) для каждого элемента:

Если функция возвращает true, элемент остаётся в новом массиве.
Если возвращается false, элемент исключается.
filter() как сито: оставляет только нужные элементы.

Важный момент: метод filter() не изменяет исходный массив, 
а возвращает новый массив с отфильтрованными элементами.

Синтаксис:

let newArray = array.filter(callback(element, index, array));
callback — это функция, которая выполняется для каждого элемента массива.
element — текущий элемент массива.
index — индекс текущего элемента (необязателен).
array — сам массив, на котором был вызван метод filter() (тоже необязателен).
Пример:

Предположим, у нас есть массив чисел и мы хотим отфильтровать только чётные числа. 
Вот как это можно сделать с помощью filter():

let numbers = [1, 2, 3, 4, 5, 6, 7, 8];
let evenNumbers = numbers.filter(function(number) {
  return number % 2 === 0;  // Условие: число чётное
});
console.log(evenNumbers); // [2, 4, 6, 8]
В этом примере:

Мы вызываем filter() на массиве numbers.
В качестве аргумента передаем функцию, которая проверяет, является ли число чётным.
Результатом работы filter() является новый массив evenNumbers, 
содержащий только те числа из исходного массива, которые удовлетворяют условию.
Почему синтаксис именно такой? Метод filter() использует функцию обратного вызова, 
потому что это позволяет задавать динамическое условие фильтрации. 
Мы можем передать любую функцию, которая будет работать с элементами массива, 
и таким образом на ходу изменять поведение фильтрации в зависимости от того, что нам нужно.

Когда это полезно использовать? Метод filter() полезен, когда нужно:

извлекать элементы, соответствующие определенному условию;
работать с данными без изменения исходного массива;
упрощать код (особенно при работе с большими наборами данных, 
    таких как списки пользователей, товаров или других сущностей).
Например, в реальных приложениях можно использовать filter() для:

фильтрации списка пользователей по возрасту или статусу;
отбору товаров по категориям или ценовым диапазонам;
выбору записей, которые соответствуют определённым критериям.
Таким образом, filter() — это мощный инструмент для выборки данных, 
который помогает писать чистый и понятный код.

4. Преобразование и сортировка данных
Основа работы с данными в реальных приложениях – умение их преобразовывать и сортировать. 
Давайте рассмотрим несколько методов обработки данных, которые пригодятся для создания самых сложных приложений.

map()
Метод map() используется для создания нового массива, 
где каждый элемент является результатом применения функции к каждому элементу исходного массива. 
Этот метод полезен, когда нужно преобразовать элементы массива, 
например умножить все числа на два или преобразовать строки в верхний регистр.

Как это работает? По аналогии с filter() метод map() принимает функцию обратного вызова (callback-функцию), 
которая выполняется для каждого элемента массива. Она принимает три аргумента:

element — текущий элемент массива,
index — индекс текущего элемента,
array — сам массив (необязателен).
map() — как переводчик, который перебирает слова и переписывает каждое в новой форме.

Важно, что map() не изменяет оригинальный массив, а возвращает новый, 
состоящий из результатов выполнения функции для каждого элемента.

Пример:

Предположим, у нас есть массив чисел и мы хотим создать новый массив, где каждое число будет умножено на два:

let numbers = [1, 2, 3, 4];
let doubled = numbers.map(number => number * 2);
console.log(doubled); // [2, 4, 6, 8]
В этом примере метод map() проходит по каждому числу в массиве numbers и умножает его на два. 
Результатом является новый массив doubled, в котором все элементы увеличены.

Когда полезно использовать? Метод map() полезен, когда:

нужно преобразовать каждый элемент массива;
важно сохранить исходный массив неизменным.
Пример использования: можно использовать map() для преобразования списка строк
 в массив чисел или для обновления значений в объекте.

sort()
Метод sort() используется для сортировки элементов массива. 
Он изменяет сам массив, переставляя элементы в определенном порядке, 
и возвращает уже отсортированный массив. 
Однако важно помнить, что метод sort() сортирует элементы массива по строковому представлению (по умолчанию). 
Это может привести к неожиданным результатам, если вы работаете с числами или другими типами данных.

sort() по умолчанию сортирует элементы так, как если бы он сравнивал надписи на папках по алфавиту, 
а не по числовому значению, поэтому числа могут «путаться» и идти не по порядку.

Параметры sort()

Метод принимает необязательный параметр — функцию сравнения, которая задаёт логику сортировки. 
Она получает два аргумента, которые представляют два элемента массива.

array.sort(function(a, b) {
  // логика сравнения элементов
});
a и b — это два элемента массива, которые сравниваются в процессе сортировки.
Функция сравнения должна вернуть одно из следующих значений:

Меньше нуля: если a должен идти раньше, чем b (в порядке возрастания).
Больше нуля: если b должен идти раньше, чем a.
Ноль: если элементы равны по значению (их порядок в массиве не меняется).
Пример сортировки чисел:

Если не передать функцию сравнения, метод sort() будет сортировать элементы как строки, 
что приведет к неправильным результатам для чисел.

let numbers = [40, 100, 1, 5];
numbers.sort();
console.log(numbers); // [1, 100, 40, 5] — неверно, числа сортируются как строки
Чтобы отсортировать числа корректно, передаем функцию сравнения:

let numbers = [40, 100, 1, 5];
numbers.sort((a, b) => a - b);
console.log(numbers); // [1, 5, 40, 100]
Здесь функция сравнения (a, b) => a - b сортирует массив по возрастанию: 
если разница a - b отрицательна, то a будет стоять перед b. Если разница положительна — наоборот.

Пример сортировки строк:

Когда сортируются строки, функция сравнения по умолчанию работает корректно, так как строки сортируются по алфавиту.

let fruits = ["banana", "apple", "cherry"];
fruits.sort();
console.log(fruits); // ["apple", "banana", "cherry"]
Когда полезно использовать sort()? Метод sort() полезен, когда:

нужно упорядочить элементы массива по определенному критерию;
требуется изменить порядок элементов в оригинальном массиве.
Метод хорошо подходит для:

сортировки строк в алфавитном порядке;
сортировки чисел с учетом их значений;
сортировки сложных объектов по числовым или строковым свойствам.
Однако важно помнить, что sort() изменяет сам массив, и, 
если вам нужно сохранить оригинальный массив, стоит сначала создать его копию перед сортировкой.

reverse()
Наконец, рассмотрим метод reverse(), 
который используется для изменения порядка элементов массива на противоположный. 
То есть, если массив был в порядке [1, 2, 3], после применения reverse() он будет выглядеть как [3, 2, 1].

Синтаксис:

array.reverse();
Метод не принимает никаких параметров и возвращает сам массив, 
но с изменённым порядком элементов. Важно заметить, 
что reverse() работает непосредственно с исходным массивом и изменяет его. 
Это значит, что результат уже содержится в самом массиве.

Зачем нужен reverse()? Метод полезен, если нужно просто «перевернуть» элементы массива. 
Например, если нужно вывести элементы в обратном порядке или реализовать такие операции, 
как отмена очереди или симуляция стека.

Зачем он нужен, если уже есть sort()? 
Хотя метод sort() позволяет изменять порядок элементов, 
он предназначен для сортировки массива по заданному критерию. 
reverse() же работает исключительно на изменение порядка элементов без дополнительных условий сортировки. 
Это делает его идеальным выбором, когда нужно просто инвертировать элементы массива, 
не заботясь о том, как они отсортированы.

Пример:

let fruits = ["apple", "banana", "cherry"];
fruits.reverse();
console.log(fruits); // ["cherry", "banana", "apple"]
Таким образом, reverse() является отличным инструментом, 
если нужно быстро изменить порядок элементов в массиве, сохранив их текущие значения.

5. Сложные операции с массивами
Разберем еще один крайне полезный метод для работы с массивами в JavaScript, 
который помогает в одно действие анализировать всю коллекцию данных и 
возвращать результат анализа в виде числа или, например, произвольного объекта.

reduce()
Метод reduce() является одним из самых мощных и универсальных инструментов для работы с массивами в JavaScript. 
Его основное назначение — помочь выполнить агрегацию или сводку данных, 
преобразовав все элементы массива в одно итоговое значение. 
Это может быть сумма, произведение, создание нового массива или объекта или даже более сложные вычисления.

reduce() — как калькулятор, который сводит список значений к одному итоговому числу.

Когда мы работаем с массивами, 
часто возникает необходимость обработать все их элементы и получить один результат. 
Например, сложить все числа, найти максимальный элемент или преобразовать массив в объект с определенной структурой. 
Метод reduce() идеально подходит для таких случаев, 
позволяя выполнить эти операции с минимальными усилиями и максимальной гибкостью.

Синтаксис метода выглядит так:

array.reduce(callback(accumulator, currentValue), initialValue)
callback — это функция, которая вызывается для каждого элемента массива.
accumulator — это значение, которое накапливает промежуточные результаты операции. 
Оно передается от одного элемента к следующему, накапливая итоговое значение.
currentValue — это текущий элемент массива, который обрабатывается в данный момент.
initialValue — это начальное значение для аккумулятора. 
Если оно не указано, то первым значением будет первый элемент массива.
Представьте, что вы хотите сложить все числа в массиве. С reduce(), это будет выглядеть так:

let numbers = [1, 2, 3, 4];
let sum = numbers.reduce((acc, currentValue) => acc + currentValue, 0);
console.log(sum); // 10
В этом примере каждое число массива метод добавляет к сумме, начиная с 0. 
Процесс продолжается до тех пор, 
пока не обработаются все элементы и не будет получен результат — сумма всех чисел массива.

Метод reduce() также полезен, 
когда нужно превратить массив в объект или выполнить более сложные преобразования. 
Представьте, что у вас есть массив строк и вы хотите создать объект, 
где ключами будут сами строки, а значениями — их длина. Вот как это можно сделать с помощью reduce():

let words = ["apple", "banana", "cherry"];
let lengthMap = words.reduce((acc, word) => {
  acc[word] = word.length;
  return acc;
}, {});
console.log(lengthMap); // { apple: 5, banana: 6, cherry: 6 }
Здесь метод создаёт объект, где каждый элемент массива становится ключом, а его длина — значением.

Хотя reduce() может выглядеть немного сложным для новичков, 
его сила и гибкость с лихвой компенсируют сложность. 
Это идеальный метод для решения задач, 
где нужно пройтись по всем элементам массива и «свернуть» их в одно итоговое значение. 
Используя reduce(), можно значительно упростить код, 
избавившись от необходимости писать длинные циклы и условные операторы.

В целом reduce() — это инструмент, 
который не только выполняет операцию для каждого элемента массива, 
но и позволяет строить сложные логические цепочки. 
Он делает код более читаемым, компактным и выразительным.

Методы массивов в JavaScript действительно мощные, но и у них есть подводные камни. 
Некоторые из них неочевидны: метод вроде работает, но результат — совсем не тот, что вы ожидали.







Типичные ошибки
Тип ошибки 1: Неправильная сортировка массива с числами
Ошибка:
Метод sort() сортирует массивы по строковому значению по умолчанию. 
Если в массиве содержатся числа, это приведет к ошибке.

Пример ошибки:

let numbers = [10, 2, 3, 1];
numbers.sort();
console.log(numbers);  // [1, 10, 2, 3], сортировка по строковому представлению
Почему это происходит:
Метод sort() сравнивает элементы массива как строки, а не как числа.

Решение:
Для корректной сортировки чисел нужно передать функцию сравнения.

numbers.sort((a, b) => a - b);
console.log(numbers);  // [1, 2, 3, 10]






Тип ошибки 2: Ошибка с вложенными массивами при использовании map() или reduce()
Ошибка:
Если в массиве есть вложенные массивы, метод map() или reduce() 
без дополнительной обработки не «распакуют» их. 
Это может привести к неправильному результату или логической ошибке.

Пример ошибки:

let arr = [[1, 2], [3, 4], [5, 6]];
let result = arr.map(item => item + 1);  // Ошибка: item — это массив, а не число
console.log(result);  // Ошибка в логике
Почему это происходит:
В случае с вложенными массивами методы map() и reduce() применяют функцию 
к каждому вложенному массиву, а не к отдельным элементам этих массивов.

Решение:
Используйте flat() или другие методы для «распаковки» вложенных массивов.

let result = arr.flat().map(item => item + 1);
console.log(result);  // [2, 3, 4, 5, 6, 7]
* Метод flat() появился в стандарте JavaScript ES2019, так что в старых версиях браузеров он может не поддерживаться.






Тип ошибки 3: Ошибка при изменении элементов массива с использованием forEach()
Ошибка:
Метод forEach() изменяет элементы массива только в контексте самого массива, но, 
если в процессе работы метода нужно изменить его структуру. 
Это может привести к ошибкам при удалении элементов или изменении порядка.

Пример ошибки:

let arr = [1, 2, 3];
arr.forEach((item, index) => {
    arr[index] = item * 2;  // Пытаемся изменить элементы массива
});
console.log(arr);  // [2, 4, 6], но будьте осторожны при изменении в цикле
Почему это происходит:
forEach() не позволяет безопасно изменять структуру массива в процессе его перебора.

Решение:
Для изменения массива можно использовать другие методы: например, map(), чтобы создать новый массив.

let result = arr.map(item => item * 2);
console.log(result);  // [2, 4, 6]











Практикум
Задача 1: Фильтрация и обработка массива
Ситуация:
Представьте, что вы создаете утилиту для обработки массивов чисел. 
Ваша задача — написать функцию, которая выполняет несколько действий:

Убирает чётные числа из массива.
Сортирует массив по возрастанию.
Заменяет третий элемент массива на сумму всех его элементов.
Задача:
Напишите функцию, которая принимает массив чисел и возвращает новый массив, 
обработанный по вышеуказанным правилам.

Шаги реализации:

Удалите из массива чётные числа.
Отсортируйте массив по возрастанию.
Используйте reduce() для вычисления суммы элементов.
Замените третий элемент массива на сумму.
Решение:

function processArray(arr) {
    let filtered = arr.filter(num => num % 2 !== 0); // Убираем чётные числа
    filtered.sort((a, b) => a - b);                 // Сортируем по возрастанию

    if (filtered.length >= 3) {
        const sum = filtered.reduce((acc, num) => acc + num, 0); // Считаем сумму
        filtered[2] = sum; // Заменяем третий элемент на сумму
    }

    return filtered;
}

console.log(processArray([10, 15, 2, 7, 3, 8])); // [3, 7, 25]
console.log(processArray([1, 2, 3]));           // [1, 3, 4]







Задача 2: Группировка чисел по чётности
Ситуация:
Вы разрабатываете статистический модуль, который должен анализировать массив чисел. 
Нужно разделить числа на две группы: чётные и нечётные. 
Затем нужно вернуть объект, где ключи — это тип чисел (even или odd), а значения — массивы чисел.

Задача:
Напишите функцию, которая принимает массив чисел и возвращает объект с двумя свойствами:

even — массив чётных чисел,
odd — массив нечётных чисел.
Шаги реализации:

Используйте метод reduce() для группировки чисел в объект.
Проверьте каждое число на чётность с помощью оператора %.
Добавьте число в соответствующую группу.
Решение:

function groupByParity(arr) {
    return arr.reduce((acc, num) => {
        if (num % 2 === 0) {
            acc.even.push(num); // Добавляем в массив чётных
        } else {
            acc.odd.push(num); // Добавляем в массив нечётных
        }
        return acc;
    }, { even: [], odd: [] }); // Начальное значение — объект с пустыми массивами
}

console.log(groupByParity([1, 2, 3, 4, 5, 6]));
// { even: [2, 4, 6], odd: [1, 3, 5] }

console.log(groupByParity([10, 15, 20, 25]));
// { even: [10, 20], odd: [15, 25] }





Эти задачи помогут вам:

Освоить основные методы работы с массивами: filter, sort, reduce.
Научиться эффективно группировать и преобразовывать данные.
Решать практические задачи, связанные с обработкой массивов.



Итоги:
На этом занятии мы изучили ключевые аспекты работы с методами массивов в JavaScript и разобрали их применение на практике:

освоили методы для добавления и удаления элементов из массива (например, push(), pop(), shift(), unshift());
научились извлекать и заменять части массива с помощью splice() и slice();
разобрались, как фильтровать (filter()), преобразовывать (map()), сортировать (sort()) и сводить массив к одному значению (reduce()).