Урок №12. Методы объектов в JavaScript.

Учебные материалы

На прошлом занятии мы сделали важный шаг: познакомились с объектами и увидели, 
как просто с их помощью собрать связанные данные в единую структуру, 
задать имена для каждого элемента и даже встроить в объект функции.

Сегодня идём дальше, 
и за фасадом простого синтаксиса скрываются мощные возможности — мы научимся управлять объектами более точно: 
защищать, копировать, объединять и извлекать из них информацию. 
Всё это — не просто теория, а инструменты, которые пригодятся вам в реальных проектах.



Сегодня мы:
узнаем, как сделать свойства объекта неизменяемыми;
научимся извлекать все ключи, значения и пары объекта;
разберёмся, как проверять наличие нужного ключа;
потренируемся перебирать объекты и массивы корректно;
поймём, как копировать и объединять объекты и что такое строгий режим.



Глоссарий к двенадцатому занятию
Descriptor (Дескриптор)
Объект, который описывает, можно ли свойство объекта менять, удалять или показывать.

Mutate (Изменять)
Процесс изменения объекта: добавление, удаление, переименование полей.

Mutable (Изменяемый)
Объект, который можно менять после создания.

Immutable (Неизменяемый)
Объект, который нельзя поменять (например, если он заморожен).

Writable (Можно менять)
Настройка, которая разрешает изменять значение свойства.

Configurable (Можно перенастроить)
Настройка, которая позволяет удалить свойство или изменить его правила.

Value (Значение)
Содержимое свойства объекта (например, текст, число или другой объект).

In (В объекте)
Оператор, который проверяет, есть ли нужное свойство в объекте.

Spread (Оператор …)
Раскладывает содержимое массива или объекта по частям.

Rest (Оператор …)
Собирает оставшиеся значения в одну переменную.

Seal (Запечатать)
Метод, который запрещает добавлять и удалять свойства, но разрешает менять старые.

JSON
Текстовый способ записать объект, чтобы его можно было сохранить или передать.

Parse (Разобрать)
Превратить JSON-строку обратно в объект.

Stringify (Упаковать в строку)
Превратить объект в строку для отправки или хранения.





Как устроены объекты: глубже, чем кажется
На прошлом занятии вы уже создали свой первый объект: задали поля, использовали методы, 
даже хранили внутри массивы и другие объекты. 
Казалось бы, всё просто: ключ, значение, точка или скобки — и доступ к нужным данным у вас в руках.

Но за этой простотой скрывается целый мир. 
Оказывается, у каждого свойства объекта есть дополнительные настройки: можно запретить его изменение, 
удаление или даже просмотр. 
Объекты можно защищать от вмешательства, сливать друг с другом, клонировать, запечатывать и превращать в строки.

Уборка в тумбочке. Перебор for ... of и for ... in
Иногда субботним дождливым вечером хочется навести порядок в ящиках тумбочки и перебрать всё содержимое.

const cabinet = {
  // А где же topbox?
  middlebox: 'pencils',
  bottombox: 'socks'
}
Существует три корректных способа перебрать объект с доступом к ключам и один сомнительный (мы его не рекомендуем):

Цикл:
for (let box in cabinet) {
  console.log(box, cabinet[box]);
}

2. Список ключей:

let boxes_only = Object.keys(cabinet)
console.log(boxes_only)

3. Список пар [ключ, ‘значение’]:

let my_boxes = Object.entries(cabinet)
// Кстати! Перебор МАССИВА поэлементно делается с помощью for ... of
for (let elem of my_boxes) {
console.log(elem); // На каждой строке пары [ключ, 'значение']
}
// Не забывайте, что пребор массива с помощью for ... in даст вам его индексы
for (let i in my_boxes) {
console.log(i, my_boxes[i])
}

4. Список значений:

let svalka = Object.values(cabinet)
Последний способ должен возвращать значения в том же порядке, в котором ключи возвращает Object.keys(), 
но мы рекомендуем использовать его только в случаях, когда ключ не имеет значения (например, 
    если просто нужно вытряхнуть всё содержимое тумбочки на диван и получить массив всех вещей без подписей).

Важно! Изменение количества элементов массива или ключей объекта во время перебора — плохая идея. 
Не все интерпретаторы JavaScript справляются с этим корректно. 
Если вам нужно что-то удалить, сначала получите список ключей через Object.keys(), а уже потом перебирайте его.

Например, у вас есть пенал, в котором часть ручек засохла. Пора навести порядок:

let pencil_case = {
  'green':  'bad',
  'red':    'good',
  'blue':   'normal',
  'orange': 'bad',
  'black':  'good',
}
for (let pencil of Object.keys(pencil_case)) {
  if (pencil_case[pencil] == 'bad') {
    delete pencil_case[pensil]
  }
}
console.log(pencil_case)

Поиск в тумбочке. Оператор in
Та-дам! Всё лежит ровно и аккуратно, но вы выбросили кучу ненужного. 
Как узнать, на месте ли нужная вещь? Лежит ли она всё ещё в нашей тумбочке?

Вот бы в жизни так было просто…

Оператор in в JavaScript имеет двойное назначение. 
Вы уже видели, что в сочетании с for он позволяет перебирать объект по ключам, а массив — по индексам. 
Однако он также может ответить на логический вопрос: присутствует ли ключ в объекте? 
Забегая вперёд: ответ будет положительным даже в случае, если ключ существует, 
но его значение «невидимо», например undefined.

// Проверяем ключ в объекте:
console.log('blue' in pencil_case)
// Проверяем ключ в ключах:
console.log('blue' in Object.keys(pencil_case))
Почему во втором случае false? 
Потому что in проверяет наличие индекса в массиве, а не значения. 
Поэтому 'blue' in Object.keys(...)работает не так, как может показаться.

Метод includes: «включает в себя»
А как проверить, есть ли значение в массиве?

let arr = [1,2,3];
console.log(arr.includes(2))
// true

let arr = [1,2,{"3": 7}]
console.log(arr.includes({"3": 7}))
// false
Как же так? Ведь вроде бы объект { "3": 7 } там есть!

На самом деле includes сравнивает значения с использованием строгого равенства (===), 
а для объектов это означает сравнение по ссылке. 
Метод сработает только для примитивных типов или тех же самых объектов по ссылке.

Закольцовка
Любой объект технически может содержать ссылку на самого себя и даже подтвердить это:

arr.push(arr)
console.log(arr)
console.log(arr.includes(arr))
// <ref *1> [ 1, 2, { '3': 7 }, [Circular *1] ]
// true
Такая структура называется циклической. 
За исключением специальных случаев (например, при работе с графами или структурами DOM), 
лучше избегать закольцованных объектов, чтобы не запутаться и не столкнуться с ошибками сериализации.

Тумбочка с ключиком — defineProperty
С прошлого урока вы уже умеете создавать неизменяемые объекты (с помощью Object.freeze()). 
Но что, если вы хотите запретить изменения только одного свойства, а не всего объекта?

Для этого в JavaScript существует продвинутый способ задания свойств — Object.defineProperty(obj, prop, descriptor).

Вот как работает эта команда:

obj — объект, в котором вы создаёте или изменяете свойство.
prop — имя свойства в виде строки.
descriptor — объект, описывающий поведение свойства. В него можно включить:
value — значение свойства;
writable — возможность изменять значение (по умолчанию false);
enumerable — видимость свойства при переборе объекта (for...in, Object.keys и др.);
configurable — возможность в будущем изменить это свойство (включая writable, enumerable и само configurable).
Метафора: ящики тумбочки
Представим, что у вас есть тумбочка с тремя ящиками. Все они прозрачные, и вы можете видеть, что лежит внутри.

writable: false — ящик заперт, положить в него ничего нельзя.

enumerable: false — ящик не виден при поверхностном осмотре (например, он открывается только с обратной стороны).

configurable: false — ключ от ящика утерян, теперь его нельзя ни запереть, ни переделать.


Так,

writable = false означает, что ящик заперт;
enumerable = false означает, что ящик, например, открывается сзади тумбочки и не виден спереди;
configurable = false означает, что ключ потерян. 
Если к этому моменту ящик был заперт, можно любоваться его содержимым (ящик-то прозрачный), 
а если ящик был открыт, его нельзя запереть.
// Добавим в нашу тумбочку ящик
Object.defineProperty(cabinet, "topbox", {
  enumerable:   true,   // default false
  configurable: true,   // default false
  writable:     true,   // default false
  value: ''             // default undefined
})

// Положим в ящик паспорт
cabinet.topbox = 'Паспорт'

// Запрём ящичек
Object.defineProperty(cabinet, "topbox", {
  writable:     false,   // было true
})

// Заменим паспорт на диплом
cabinet.topbox = 'Диплом'

// Посмотрим, что в тумбочке?
console.log(cabinet)

Результат: в тумбочке по-прежнему лежит паспорт. 
JavaScript в обычном режиме никак не сообщит вам об ошибке, 
а просто молча проигнорирует попытку изменить свойство.

Но если вы используете строгий режим ('use strict';), 
то получите ошибку, и это будет честнее: система предупредит, что доступ запрещён.

Пара слов о строгом режиме
Иногда JavaScript слишком снисходителен к ошибкам: 
молча игнорирует их или ведёт себя непредсказуемо. 
Чтобы добавить интерпретатору немного строгости и дать ему право на возмущение, 
в начале скрипта (на самой первой строке) напишите:

'use strict'
Этот режим полезен почти всегда, и мы настоятельно рекомендуем включать его по умолчанию.

Попробуйте снова запустить программу. Теперь вы получите желанную ошибку:

TypeError: Cannot assign to read only property 'topbox'
Вот оно — настоящее профессиональное удовлетворение: вы уже на том уровне, 
где ошибка в нужном месте радует гораздо больше, чем тихое игнорирование проблемы.

Замочек покрепче
// Отопрём ящичек
Object.defineProperty(cabinet, "topbox", {
  writable:     true,   // было false
})

// Положим туда несколько документов
cabinet.topbox = ['Паспорт']  // Ящичек стал массивом
cabinet.topbox.push('Диплом')
cabinet.topbox.push('Водительские права')

// Запрём ящичек
Object.defineProperty(cabinet, "topbox", {
  writable:     false,   // было true
})

// Ой, забыли свидетельство о рождении положить. Оно тоненькое – просунем его в щёлку, не отпирая ящичек...
cabinet.topbox.push('Свидетельство о рождении')
// Посмотрим, что в тумбочке?
console.log(cabinet)

Получилось! Даже в строгом режиме. Почему так?

Вспомните: в JavaScript writable: false запрещает переназначение свойства, 
но не замораживает содержимое, если это объект или массив. 
Мы всё ещё можем изменить внутреннюю структуру. 
Например, добавить элемент в массив или изменить значение в объекте.

Чтобы сделать содержимое действительно неизменяемым, используем:

// Завернём все документы в папочку и заклеим скотчем
Object.freeze(cabinet.topbox)
cabinet.topbox.push('Аттестат')

Теперь всё по-настоящему запечатано: в щель уже не просунешь ни строчки.

Попробуйте сами: создайте объект, у которого одно из свойств — это массив. 
Заморозьте объект и попытайтесь изменить массив. Появится ли ошибка? Почему результат именно такой?

Подсказка: Object.freeze() действует только на первый уровень объекта. 
Глубокая заморозка — отдельная тема.

spread — разложить всё по коробочкам
Иногда нужно не просто открыть ящик, а аккуратно разложить всё, что в нём лежит, по новым коробочкам. 
Конечно, это можно сделать вручную с помощью цикла, что вы уже умеете. 
Но есть способ удобнее: spread (оператор расширения). 
Он доступен во многих языках программирования и особенно изящен в JavaScript.

const [a, b, c] = [1, 2, 3];
let arr = [4, 5];
let [d, f] = arr;
console.log(a, b, c, d, f, );
// Теперь – "разложение" в массив
let brr = [...arr];
arr[0] = 40;
brr[1] = 50;
console.log(arr, brr);
// Ожидаемый вывод: [ 40, 5 ] [ 4, 50 ]
Мы не получили две ссылки на один и тот же массив: изменения одного не затронули другой. 
Именно это делает spread мощным инструментом при работе с массивами и объектами.

Вернёмся к нашей тумбочке:

// Распределим копии документов по отдельным файликам
cabinet.middlebox = [...cabinet.topbox];
// Копировать документы можно, не отпирая ящичек (через стекло), а вот заменить – только в открытом
Object.defineProperty(cabinet, "topbox", {
  writable:     true,
})
// Выбросим испорченные документы, заменим на те, что в среднем ящике
cabinet.topbox = cabinet.middlebox;
// Хорошо бы не оставлять лишние ссылки на наш объект, а почему – узнаете в уроке, посвящённом сборке мусора
cabinet.middlebox = [];  // Теперь средний ящик не хранит ссылку на верхний, он просто пуст
console.log(cabinet)
spread применительно к объектам
Если в папины ботинки вылить мамины духи…

Оператор …spread работает не только с массивами, но и с объектами. 
С его помощью можно создавать копии и объединять объекты.

А что произойдёт, если взять два объекта и объединить их таким образом?

const bag_mom = {
  wallet: 1000,  // рублей
  parfume: 'Chanel#5'
}
let bag_dad = {
  wallet: 'documents',
  mobile: 'charger'
}
bag_dad = {...bag_dad, ...bag_mom}
console.log(bag_dad)
// { wallet: 1000, mobile: 'charger', parfume: 'Chanel#5' }
Теперь у папы в рюкзаке — мамины духи. 
Документы исчезли, зато появилась 1000 рублей. Хорошо, что зарядка от телефона осталась!

Что произошло?

Когда вы пишете { ...obj1, ...obj2 }, JavaScript:
копирует все свойства из obj1;
поверх них копирует свойства из obj2, перезаписывая совпадающие ключи.
Таким образом, второй объект имеет приоритет, а его значения побеждают в случае конфликта ключей.

Такой способ удобен, если нужно аккуратно объединить объекты или «накрыть» один конфигурацией из другого. 
Но будьте осторожны: spread создаёт поверхностную копию, значит, вложенные объекты и массивы будут по-прежнему ссылками.

rest — сбор оставшихся аргументов в массив
Иногда нужно принять в функции неограниченное количество аргументов. 
Раньше для этого использовали специальный объект arguments:

function my_sum(first, second) {
  let result = 0;
  for (let arg of arguments) {
    result += arg;
  }
  return result;
}
console.log(my_sum(1, 2, 3, 4, 5));
Но сейчас есть более удобный и современный способ — оператор rest (...). 
Он собирает оставшиеся аргументы в массив:

function my_sum(...args) {
  return args.reduce((sum, val) => sum + val, 0);
}
console.log(my_sum(1, 2, 3, 4, 5)); // 15
Такой подход проще и безопаснее. Кроме того, rest работает и при деструктуризации:

const [first, ...rest] = [10, 20, 30, 40];
console.log(first); // 10
console.log(rest);  // [20, 30, 40]

Важно: rest — это сбор значений в массив, а spread — это их разложение. 
Несмотря на одинаковый синтаксис ..., работают они в разных контекстах.

JSON
В завершение разговора об объектах стоит упомянуть один из самых мощных и 
популярных текстовых форматов передачи данных — JSON (JavaScript Object Notation). 
Это формат, который описывает всё, что вам нужно, в виде обычных объектов JavaScript.

Как это работает?
Если вы хотите превратить объект в строку (например, 
    чтобы сохранить его в файл или передать по сети), используйте:

JSON.stringify(объект)

Если вы получили строку с объектом (например, 
    с сервера) и хотите преобразовать её обратно в JavaScript-объект, используйте:

JSON.parse(объектУпакованныйВСтроку)

Так мы можем отправить объект в путь, 
словно аккуратно упакованную тумбочку в фургоне с содержимым, но в текстовом виде.


Важно помнить: JSON — это не просто строка, а универсальный способ общения между программами, 
сайтами и сервисами. Именно благодаря ему ваши данные понятны и в браузере, 
и на сервере, и в мобильном приложении.








Типичные ошибки
Тип ошибки 1: Перебор массива с помощью цикла for ... in с ожиданием элементов

Цикл for...in перебирает ключи объекта или индексы массива, а не сами значения. 
Это приводит к неожиданному результату, особенно для тех, 
кто привык к for...in в Python, где такой цикл возвращает элементы коллекции.

Пример ошибки:

let arr = [0, 1, 2, 3];
let colors = ['R', 'G', 'B'];
for (let i in arr) {
  console.log(i);  // работает ожидаемо: выводит числа, которые хранятся в массиве
}
for (let color in colors) {
  console.log(color);
  // Выведет
  // 0
  // 1
  // 2
}
Объяснение:

В обоих случаях выводятся индексы массива, 
просто в первом примере они случайно совпали с содержимым. 
Это распространённая ловушка: может показаться, что for...in выводит элементы, 
но на самом деле он проходит по ключам (то есть индексам в случае массива).

Решение:

let colors = ['R', 'G', 'B'];
for (let color in colors) {
  console.log(color, colors[color]);
  // Выведет
  // 0 R
  // 1 G
  // 2 B
}
В этом примере переменная color содержит индексы массива, а не сами значения. 
Мы специально не стали менять название переменной, чтобы подчеркнуть: 
от имени переменной результат не зависит, так как важно понимать, что именно перебирается.

Для повышения читаемости кода всё же лучше использовать говорящие имена переменных:

let colors = ['R', 'G', 'B'];
for (let color_index in colors) {
  console.log(color_index, colors[color_index]);
  // Выведет
  // 0 R
  // 1 G
  // 2 B
}






Тип ошибки 2: Объединить массивы с помощью сложения

Эта ошибка часто встречается у тех, кто ранее писал на Python, 
где + используется для объединения списков.

Пример ошибки:

let arr = ['6', '7']
let brr = ['8', '9']
console.log(arr + brr)
// строка: '6,78,9'
Объяснение:

В JavaScript оператор + при работе с массивами приводит их к строкам и просто склеивает эти строки. 
Получается вовсе не массив, а одна большая строка.

Решение:

let arr = ['6', '7']
let brr = ['8', '9']
console.log([...arr, ...brr])








Практикум
Задача 1: Мороженое на палочке
Ситуация: вы в шаге от завершения проекта. До дедлайна осталось полчаса. 
И вдруг — свет и интернет исчезают. Документация недоступна, примеры из чатов не открыть. 
Вам срочно нужно проверить, действительно ли Object.freeze() 
защищает весь объект целиком или только его «корочку» — верхний уровень.

Задача:

Проверьте, действительно ли Object.freeze() делает все части объекта 
неизменяемыми (или затрагивает только поля верхнего уровня).

Решение:

Создаём объект, в котором:

одно свойство — массив,
второе — вложенный объект.
Ответ:

const icecream = {
  stick: ['взбитые сливки', 'шоколадная глазурь'],
  cover: {
    color: 'blue',
    material: 'aluminium'
  }
};
Object.freeze(icecream);
delete icecream.stick[1]
delete icecream.stick[0]
icecream.stick = 'strawberry'
icecream.cover.color = 'red'
console.log(icecream);
/* В консоли:
{
  stick: [ <2 empty items> ],
  cover: { color: 'red', material: 'aluminium' }
}
*/
Пояснение:

Как видим, «насадить клубнику на палочку» (добавить новый элемент в верхнеуровневый массив) 
не удалось: fruits защищён от изменений благодаря freeze().

А вот «съесть мороженое» или «сменить обёртку» (изменить значение внутри вложенного объекта settings) по-прежнему можно. 
Это значит, что глубоко вложенные структуры остаются изменяемыми, даже если сам объект заморожен.







Задача 2: Глубокое копирование и упаковка в строку
Ситуация: вы уже уверенно чувствуете себя на фронте и теперь работаете с серверной частью. 
Нужно передать данные на сервер, где они будут обработаны. 
Даже если сервер у вас на Node.js, по дороге — большой и непредсказуемый интернет. 
Там возможны ошибки, потери и несовместимости. 
Поэтому данные между клиентом и сервером часто передаются в виде текста, а не в виде сложных структур.

Писать отдельную логику для сериализации каждого объекта в бинарный формат дорого и сложно. 
А вот текст — он уже «родной» для JavaScript.

Воспользоваться встроенными методами для упаковки объекта в строку и его последующей распаковки.

Задача:

Показать, как с помощью JSON.stringify() и JSON.parse() можно сделать глубокую копию объекта, 
не зависящую от исходника.

Реализация:

let human = {
  name: 'Vasya',
  age: 25
}
let other_human = JSON.parse(JSON.stringify(human));
other_human.name = 'Petya';
console.log(human, other_human);


Важно: метод JSON.stringify() не сработает, 
если объект содержит закольцованные ссылки (объект ссылается сам на себя). 
В таком случае вы получите ошибку TypeError: Converting circular structure to JSON.








Итоги:
На этом занятии мы:

научились делать отдельные свойства объекта неизменяемыми с помощью defineProperty и Object.freeze;
освоили перебор объектов: по ключам, значениям и парам, а также безопасные методы удаления;
узнали, как проверять наличие ключей с помощью in и распаковывать объекты и массивы с помощью spread и rest;
разобрались, как копировать и объединять объекты, избегая лишних ссылок;
познакомились с JSON — универсальным форматом хранения и передачи данных.