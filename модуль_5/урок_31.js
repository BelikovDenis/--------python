 Урок №31. Типизация функций, перегрузка функций.

 Учебные материалы

 На прошлом занятии мы познакомились с основами TypeScript и научились создавать переменные с типами.
  Сегодня мы углубимся в типизацию функций — укажем, какие данные функция принимает и что она возвращает.
   Это помогает писать надёжный код: если, например, вы случайно передадите строку вместо числа в функцию подсчёта баллов,
    TypeScript заранее предупредит об ошибке. Мы также разберём перегрузку функций — механизм, 
    позволяющий одной функции работать с разным количеством аргументов, как универсальный рецепт,
     который всегда даёт правильный результат.

Сегодня мы:

Узнаем, как задавать типы для аргументов и возвращаемых значений функций в TypeScript.
Разберём, как перегрузка функций позволяет одной функции работать с разными наборами аргументов.
Научимся создавать функции с чёткой типизацией, чтобы избежать ошибок.
Напишем функцию sum, которая может складывать два или три числа.
Поймём, как TypeScript делает функции надёжнее, проверяя их на этапе написания кода.
После занятия мы сможем создавать функции с типами и использовать перегрузку, чтобы они работали гибко и без ошибок.




Глоссарий к тридцать первому занятию
Типизация функций
Типизация функций — указание типов данных для аргументов функции и её возвращаемого значения в TypeScript.

Перегрузка функций
Перегрузка функций — возможность определить несколько вариантов одной функции с разным количеством или типами аргументов.

Аргумент
Аргумент — значение, которое передаётся в функцию при её вызове.

Возвращаемое значение
Возвращаемое значение — результат, который функция возвращает после выполнения.

TSConfig (ТСКонфиг)
TSConfig — файл tsconfig.json, который настраивает параметры компиляции TypeScript.




Типизация функций в TypeScript
В JavaScript функции — это как инструкции для робота: мы задаём, что он должен сделать с данными, 
которые получает. Но JavaScript не проверяет, правильные ли данные мы передали. Например,
 если мы пишем функцию для сложения чисел, а вместо чисел передаём строки, JavaScript может склеить их вместо сложения,
  дав неожиданный результат вроде «10» + «20» = «1020». TypeScript решает эту проблему, позволяя 
  указать типы для аргументов (что функция принимает) и возвращаемого значения (что она возвращает).
   Это как написать на коробке с инструментами «Только молоток и отвёртка», чтобы случайно не взять ножницы.

Задаём типы для аргументов и возвращаемого значения
Представим, что вы создаёте функцию, которая проверяет, хватает ли баллов для достижения цели.
 Это просто пример: вы хотите, чтобы функция принимала два числа — текущие баллы и минимальный порог
  — и возвращала true, если баллов достаточно, или false, если нет:

function hasEnoughPoints(points: number, minimum: number): boolean {
  return points >= minimum;
}
console.log(hasEnoughPoints(100, 80)); // true
Здесь points: number и minimum: number — это типы аргументов, которые говорят TypeScript,
 что функция принимает только числа. : boolean указывает, что функция возвращает булево значение 
 (true или false). Если вы попробуете передать строку, TypeScript выдаст ошибку ещё до компиляции кода:

console.log(hasEnoughPoints("100", 80)); // Ошибка: Argument of type 'string' is not assignable to parameter of type 'number'
Исправим, передав число:

console.log(hasEnoughPoints(100, 80)); // true
Этот подход делает код надёжным: TypeScript заранее проверяет, что вы используете правильные данные,
 и не даёт программе запуститься с ошибками.

Зачем типизировать функции?
Типизация функций приносит несколько преимуществ, которые делают программирование проще и безопаснее:

Меньше ошибок: TypeScript проверяет наш код ещё на этапе написания, не позволяя передать неправильные данные.
 Это как калькулятор, который не даст нам сложить числа, если мы случайно ввели буквы.
Код понятнее: типы — это как ярлыки на коробках. Если мы видим : number, мы знаем, что нужно передать число, а не строку.
Удобство в команде: когда мы делимся кодом с друзьями или коллегами, типы показывают,
 как правильно использовать функцию, без лишних вопросов.
Подсказки в редакторе: в редакторах вроде VS Code TypeScript подсказывает, какие аргументы ожидает функция и что она возвращает.
 Это как иметь умного помощника, который напоминает, что нужно сделать.
Например, если вы начинаете писать hasEnoughPoints(, редактор сразу покажет,
     что нужны два числа и что функция вернёт true или false. Это ускоряет работу и 
     помогает новичкам быстрее разобраться в коде.

Пример с вычислением среднего
Вот ещё один пример: допустим, вы пишете функцию, которая считает среднее значение списка чисел, например,
 баллов за задания. Функция принимает массив чисел и возвращает их среднее:

function getAverage(numbers: number[]): number {
  const sum = numbers.reduce((total, num) => total + num, 0);
  return sum / numbers.length;
}
console.log(getAverage([90, 80, 85])); // 85
Здесь numbers: number[] говорит, что функция принимает массив чисел, а : number указывает, 
что она возвращает число (среднее значение). Если вы случайно передадите массив с неправильными данными,
 TypeScript укажет на ошибку:

console.log(getAverage([90, "80", 85])); // Ошибка: Argument of type 'string' is not assignable to parameter of type 'number'
Исправим:

console.log(getAverage([90, 80, 85])); // 85
Этот пример показывает, как TypeScript помогает избежать ошибок в работе с массивами. Вы сразу понимаете,
 что функция ожидает только числа, и не тратите время на поиск багов.

Функции без возвращаемого значения
Иногда функция не возвращает никакого значения, а просто выполняет действие, например, выводит сообщение в консоль.
 В таких случаях используется тип void, который говорит TypeScript, что функция ничего не возвращает:

function displayMessage(text: string): void {
  console.log(`Сообщение: ${text}`);
}
displayMessage("Задание выполнено!"); // Сообщение: Задание выполнено!
Тип : void означает, что функция не должна возвращать никаких данных. Если вы случайно добавите возврат значения,
 TypeScript выдаст ошибку:

function displayMessage(text: string): void {
  return text; // Ошибка: Type 'string' is not assignable to type 'void'
}
Это полезно, когда вы хотите быть уверены, что функция используется только для побочных эффектов,
 таких как вывод в консоль или изменение данных, а не для возврата результата.

Функции с необязательными аргументами
Бывают случаи, когда аргумент функции необязателен. Например, вы пишете функцию, которая создаёт приветствие,
 где можно указать дополнительный префикс, но это необязательно:

function createGreeting(name: string, prefix?: string): string {
  if (prefix) {
    return `${prefix}, ${name}!`;
  }
  return `Привет, ${name}!`;
}
console.log(createGreeting("Аня")); // Привет, Аня!
console.log(createGreeting("Борис", "Здравствуй")); // Здравствуй, Борис!
Знак ? в prefix?: string делает аргумент prefix необязательным. TypeScript проверит, 
чтобы, если prefix передан, он был строкой. Если вы попытаетесь передать число, возникнет ошибка:

console.log(createGreeting("Аня", 123)); // Ошибка: Argument of type 'number' is not assignable to parameter of type 'string'
Необязательные аргументы делают функции гибкими, позволяя использовать их в разных ситуациях, 
но TypeScript следит, чтобы вы не нарушили правила типизации.

Ещё один пример
Допустим, вы создаёте функцию, которая объединяет список строк в одно сообщение, например, для вывода списка задач. 
Функция принимает массив строк и возвращает их объединение через запятую:

function joinItems(items: string[]): string {
  return items.join(", ");
}
console.log(joinItems(["яблоко", "груша", "банан"])); // яблоко, груша, банан
Если вы случайно передадите массив с числами, TypeScript остановит вас:

console.log(joinItems([1, 2, 3])); // Ошибка: Argument of type 'number[]' is not assignable to parameter of type 'string[]'
Исправим:

console.log(joinItems(["яблоко", "груша", "банан"])); // яблоко, груша, банан
Этот пример показывает, как TypeScript помогает работать с массивами, гарантируя,
 что данные в них соответствуют ожидаемому типу.

Перегрузка функций в TypeScript
Перегрузка функций — это способ сделать одну функцию универсальной, чтобы она могла работать с
 разным количеством или типами аргументов. Это как многофункциональный инструмент, который может
  закручивать шурупы или открывать бутылки в зависимости от того, как вы его используете.
   В TypeScript перегрузка задаётся с помощью сигнатур функций — описаний, как функция может быть
    вызвана — перед её реализацией.

Что такое перегрузка функций?
Представим, что вы хотите создать функцию sum, которая может складывать два или три числа.
 В JavaScript вы могли бы написать одну функцию с необязательным аргументом, но TypeScript требует
  чётко указать все возможные варианты вызова, чтобы гарантировать безопасность. 
  Перегрузка позволяет описать эти варианты заранее, чтобы TypeScript знал, чего ожидать.

Пример перегрузки
Вот функция sum, которая работает с двумя или тремя числами:

function sum(a: number, b: number): number;
function sum(a: number, b: number, c: number): number;
function sum(a: number, b: number, c?: number): number {
  if (c !== undefined) {
    return a + b + c;
  }
  return a + b;
}
console.log(sum(10, 20)); // 30
console.log(sum(10, 20, 30)); // 60
Первые две строки — это сигнатуры перегрузки. Они говорят TypeScript, что функция может:

Принимать два числа и возвращать число (function sum(a: number, b: number): number;).
Принимать три числа и возвращать число (function sum(a: number, b: number, c: number): number;).
Реализация функции (третья строка) использует необязательный аргумент c?: number, чтобы поддерживать оба варианта. 
TypeScript проверяет, что каждый вызов функции соответствует одной из сигнатур.
 Если передать неправильное количество аргументов или тип, вы получите ошибку:

console.log(sum(10)); // Ошибка: Expected 2-3 arguments, but got 1
console.log(sum("10", 20)); // Ошибка: Argument of type 'string' is not assignable to parameter of type 'number'
Пример с подсчётом времени
Вот ещё один пример: допустим, вы пишете функцию для подсчёта общего времени, потраченного на выполнение заданий,
 например, по математике, естествознанию и, возможно, истории. Иногда вы хотите учитывать только два задания,
  а иногда — три:

function totalTime(math: number, science: number): number;
function totalTime(math: number, science: number, history: number): number;
function totalTime(math: number, science: number, history?: number): number {
  if (history !== undefined) {
    return math + science + history;
  }
  return math + science;
}
console.log(totalTime(45, 30)); // 75
console.log(totalTime(45, 30, 60)); // 135
Функция принимает время в минутах на задания по математике, естествознанию и, опционально, истории.
 TypeScript гарантирует, что вы передаёте либо два, либо три числа, и возвращает их сумму.
  Если попытаться передать неверное количество аргументов:

console.log(totalTime(45)); // Ошибка: Expected 2-3 arguments, but got 1
Или неверный тип:

console.log(totalTime("45", 30)); // Ошибка: Argument of type 'string' is not assignable to parameter of type 'number'
Ещё один пример перегрузки
Допустим, вы создаёте функцию, которая форматирует текст, принимая либо только сообщение,
 либо сообщение с заголовком. Это полезно, например, для создания уведомлений:

function formatMessage(content: string): string;
function formatMessage(content: string, header: string): string;
function formatMessage(content: string, header?: string): string {
  if (header) {
    return `${header.toUpperCase()}: ${content}`;
  }
  return content;
}
console.log(formatMessage("Встреча в 15:00")); // Встреча в 15:00
console.log(formatMessage("Встреча в 15:00", "уведомление")); // УВЕДОМЛЕНИЕ: Встреча в 15:00
TypeScript проверяет, что вы передаёте правильные данные:

console.log(formatMessage(123)); // Ошибка: Argument of type 'number' is not assignable to parameter of type 'string'
Этот пример показывает, как перегрузка делает функцию универсальной: она может работать с 
разными вариантами входных данных, но остаётся безопасной благодаря проверкам TypeScript.

Зачем нужна перегрузка?
Перегрузка функций делает ваш код мощнее и удобнее:

Гибкость: одна функция может обрабатывать разные случаи, например, складывать два или три числа, не создавая путаницы.
Надёжность: TypeScript проверяет, что вы вызываете функцию правильно, согласно её сигнатурам, предотвращая ошибки.
Удобство: вместо нескольких функций с похожей логикой вы пишете одну, что упрощает поддержку кода и делает его чище.
Почему это полезно?
Типизация и перегрузка функций — это как иметь суперинструмент, который помогает писать код,
 готовый к реальным проектам. Они дают:

Читаемость: вы сразу понимаете, какие данные нужны функции и что она возвращает, просто глядя на её сигнатуру.
Безопасность: TypeScript не позволит передать неправильные данные или неверное количество аргументов, спасая вас от багов.
Гибкость: одна функция может решать несколько задач, как универсальный калькулятор, который работает с любым количеством чисел.
Эти возможности делают TypeScript незаменимым для больших проектов, где важно, чтобы код был понятным и надёжным.
 Представьте, что вы создаёте приложение, которое используют тысячи людей: типизация и перегрузка помогают
  сделать его стабильным и удобным для разработки. Это навык, который используют профессиональные программисты,
   и вы уже делаете первые шаги к этому уровню!

Теперь, когда вы знаете, как типизировать функции и использовать перегрузку, давайте разберём типичные ошибки,
 которые могут возникнуть, если не следовать правилам TypeScript.

Типичные ошибки
Тип ошибки 1: Неправильная типизация аргументов
Описание:

Новички часто забывают указать типы аргументов или указывают их неверно, из-за чего TypeScript выдаёт ошибку.
 Это как пытаться сложить числа на калькуляторе, но случайно ввести текст.

Пример ошибки:

function addPoints(a, b): number {
  return a + b; // Ошибка: Parameter 'a' implicitly has an 'any' type
}
console.log(addPoints("10", 20));
Объяснение:

Без указания типов (a: number, b: number) TypeScript предполагает тип any, что отключает проверку типов.
 Это может привести к ошибкам, которые TypeScript должен был предотвратить, например,
  склеиванию строк вместо сложения чисел.

Решение:

function addPoints(a: number, b: number): number {
  return a + b;
}
console.log(addPoints(10, 20)); // 30
// console.log(addPoints("10", 20)); // Ошибка: Argument of type 'string' is not assignable to parameter of type 'number'
Тип ошибки 2: Неверный тип возвращаемого значения
Описание:

Если функция возвращает значение, не соответствующее указанному типу, TypeScript выдаёт ошибку.
 Это как обещать приготовить суп, а подать десерт.

Пример ошибки:

function getAverage(a: number, b: number): number {
  return "Среднее: " + (a + b) / 2; // Ошибка: Type 'string' is not assignable to type 'number'
}
Объяснение:

Тип возвращаемого значения указан как number, но функция возвращает строку.
 TypeScript не позволит скомпилировать такой код, потому что он нарушает правило типизации.

Решение:

function getAverage(a: number, b: number): number {
  return (a + b) / 2;
}
console.log(getAverage(90, 80)); // 85
Тип ошибки 3: Использование типа any в функциях
Описание:

Тип any отключает проверки TypeScript, делая функцию ненадёжной. Это как писать код без проверки, надеясь, что всё сработает.

Пример ошибки:

function printValue(value: any): any {
  return value;
}
console.log(printValue("текст").toUpperCase()); // Работает: ТЕКСТ
console.log(printValue(42).toUpperCase()); // Ошибка во время выполнения: value.toUpperCase is not a function
Объяснение:

Тип any позволяет передавать и возвращать любые данные, но это может привести к ошибкам,
 которые TypeScript не поймает. В примере выше код с числом вызовет ошибку только при запуске.

Решение:

function printValue(value: string): string {
  return value;
}
console.log(printValue("текст").toUpperCase()); // ТЕКСТ
// console.log(printValue(42)); // Ошибка: Argument of type 'number' is not assignable to parameter of type 'string'
Практикум
Задача 1: Создание функции с типизацией
Ситуация:

Вы пишете приложение для проверки баллов, допустим, для подсчёта очков в игре. Это просто пример,
 где нужно определить, достаточно ли баллов для достижения цели.

Задача:

Создайте функцию checkPass с двумя аргументами: score (число) и passingScore (число).
Функция должна возвращать boolean (true, если баллов достаточно, иначе — false).
Выведите результат в консоль: «Достигнуто: [результат]».
Попробуйте передать строку вместо числа и проверьте, что TypeScript выдаёт ошибку.
Решение:

function checkPass(score: number, passingScore: number): boolean {
  return score >= passingScore;
}
console.log(`Достигнуто: ${checkPass(85, 80)}`); // Достигнуто: true
// console.log(checkPass("85", 80)); // Ошибка: Argument of type 'string' is not assignable to parameter of type 'number'
Задача 2: Функция с перегрузкой
Ситуация:

Вы создаёте приложение для подсчёта общего времени, потраченного на выполнение заданий, например,
 по математике и естествознанию, а иногда ещё и по истории.

Задача:

Создайте функцию totalTime с перегрузкой:

Первый вариант принимает два числа (время в минутах) и возвращает их сумму.
Второй вариант принимает три числа и возвращает их сумму.
Выведите результаты для двух и трёх аргументов: «Общее время: [результат] минут».
Попробуйте передать один аргумент и проверьте ошибку TypeScript.
Решение:

function totalTime(a: number, b: number): number;
function totalTime(a: number, b: number, c: number): number;
function totalTime(a: number, b: number, c?: number): number {
  if (c !== undefined) {
    return a + b + c;
  }
  return a + b;
}
console.log(`Общее время: ${totalTime(45, 30)} минут`); // Общее время: 75 минут
console.log(`Общее время: ${totalTime(45, 30, 60)} минут`); // Общее время: 135 минут
// console.log(totalTime(45)); // Ошибка: Expected 2-3 arguments, but got 1
Итоги
Сегодня мы:

Узнали, как типизировать функции в TypeScript, задавая типы для аргументов и возвращаемых значений, чтобы сделать код надёжным.
Разобрали перегрузку функций, которая позволяет одной функции работать с разным количеством аргументов, как универсальный инструмент.
Научились создавать функции с чёткой типизацией, такие как sum, которая складывает два или три числа.
Поняли, как TypeScript предотвращает ошибки, проверяя код ещё до его запуска.
Закрепили навыки написания безопасного и гибкого кода с помощью типизации и перегрузки.
Теперь вы можете писать функции, которые легко понять, сложно сломать и которые готовы к применению в реальных проектах.
 Это навыки, которые используют профессиональные программисты, и вы уже на пути к созданию крутых приложений!