 Урок №22. Headers, Cookies и CORS. 

 Учебные материалы

 В работе веб-приложения обмен данными между клиентом и сервером играет ключевую роль. 
 Этот обмен должен быть не только эффективным, но и безопасным, 
 особенно когда речь идёт о передаче конфиденциальной информации или взаимодействии между разными доменами. 
 Для корректной и безопасной работы приложений используются три основных механизма, которые мы и рассмотрим на этом занятии.

Сегодня мы:

разберёмся, как HTTP-заголовки управляют обменом данными между браузером и сервером;
узнаем, как работают Cookies: от момента установки (Set-Cookie) до автоматической отправки браузером;
поймём, почему CORS блокирует запросы к разным доменам и как правильно настроить заголовки Access-Control-Allow-;
научимся защищать Cookies от атак (XSS, CSRF) с помощью атрибутов HttpOnly, Secure и SameSite;
разберём реальные примеры уязвимостей;
реализуем авторизацию через Cookies + JWT и отладим проблемы с куками в браузере.
После занятия мы сможем гибко управлять HTTP-запросами, настраивая заголовки для кэширования, 
авторизации и безопасности, так, чтобы ваши приложения работали быстрее и были надёжнее.

Глоссарий к двадцать второму занятию
HTTP Headers (HTTP-заголовки)

HTTP Headers — это метаданные HTTP-запросов и ответов. Они управляют поведением клиента и сервера.

Authorization (Авторизация)

Authorization — токены для аутентификации. 
CORS-заголовки (Access-Control-Allow-Origin, Access-Control-Allow-Methods и др.) регулируют кросс-доменные запросы.

Cookies (Куки)

Cookies — это небольшие данные, которые сервер отправляет браузеру, 
и браузер автоматически включает их в последующие запросы (через заголовок Cookie). 
Используются для аутентификации, сессий, трекинга. 
Могут быть защищены флагами (HttpOnly, Secure, SameSite). 
Связаны с CORS, потому что куки могут передаваться только между определёнными доменами (из-за политик безопасности).

CORS (Совместное использование ресурсов из разных источников)

CORS (Cross-Origin Resource Sharing) — это механизм, 
который разрешает или запрещает кросс-доменные запросы (например, 
    когда фронтенд на site.com обращается к API на api.site.com). 
    Заголовки CORS (Access-Control-Allow-Origin, 
Access-Control-Allow-Credentials и др.) определяют, можно ли делать запросы между разными доменами.

CSRF (Подделка межсайтовых запросов)

CSRF (Cross-Site Request Forgery) — тип атаки, при которой злоумышленник заставляет 
браузер пользователя выполнять нежелательные действия на доверенном сайте.

JWT (Веб-токен JSON)

JWT — стандарт для создания токенов авторизации в формате JSON. 
Позволяет безопасно передавать данные между сторонами в виде подписанных токенов.

OAuth (Протокол авторизации)

OAuth — открытый протокол для безопасного делегированного доступа. 
Позволяет предоставлять третьим сторонам ограниченный доступ к пользовательским данным.

CSP (Политика безопасности контента)

CSP — механизм безопасности, 
позволяющий ограничивать источники загрузки контента (скриптов, стилей и т.д.) для предотвращения XSS-атак.

XSS-атака (Межсайтовый скриптинг)

Cross-Site Scripting — это уязвимость веб-приложений, которая позволяет злоумышленнику 
внедрить вредоносный код прямо на страницу, где с сайтом взаимодействуют другие пользователи, 
и, например, перехватить куки или токены доступа, которые используются для входа на сайт, 
а затем действовать «от имени» жертвы, выполняя любые операции на сайте, доступные пользователю.






Взаимосвязь HTTP Headers, Cookies и CORS
Сегодня мы будем изучать HTTP Headers, Cookies и CORS, но для начала ответим на вопрос: что эти три 
концепции делают в одном занятии? 
Всё дело в том, что они тесно связаны с HTTP-запросами и безопасностью веб-приложений.

Давайте разберём их взаимосвязь:

HTTP-заголовки (Headers) — это метаданные, которые передаются между клиентом (например, 
браузером) и сервером в HTTP-запросах и ответах. 
Заголовки могут включать информацию о типе содержимого, авторизации, кэшировании и других параметрах.

Например:

Content-Type: указывает тип содержимого (к примеру, application/json).
Authorization: используется для передачи токенов доступа.
Cookies — это небольшие фрагменты данных, которые сервер отправляет клиенту и которые браузер 
сохраняет для последующего использования. Они часто применяются для хранения информации 
о сессиях пользователя, предпочтениях и других данных. Когда клиент отправляет запрос на сервер, 
браузер автоматически добавляет соответствующие Cookies в заголовок запроса.

CORS — это механизм безопасности, который предоставляет или ограничивает доступ к ресурсам
 на одном домене из другого домена. Например, если веб-приложение на example.com пытается получить 
 данные с api.example.com, браузер проверяет заголовки CORS, чтобы определить, разрешён 
 ли такой запрос. Сервер может отправить заголовки, 
 такие как Access-Control-Allow-Origin, чтобы указать, какие домены могут получить доступ к его ресурсам.

Взаимосвязь получается следующая:

1. Безопасность и доступ

CORS помогает защитить ресурсы от несанкционированного доступа с других доменов. 
Это особенно важно при работе с Cookies, 
так как они могут содержать чувствительную информацию (например, токены аутентификации).

2. Использование заголовков

При реализации CORS сервер использует специальные заголовки (например, Access-Control-Allow-Origin), 
чтобы управлять доступом к ресурсам. 
Также может использоваться заголовок Set-Cookie, чтобы установить Cookies в ответе на запрос.

3. Аутентификация

Cookies часто используются для аутентификации пользователей. Когда клиент делает запрос к серверу,
 он включает соответствующие Cookies в заголовках запроса. Если CORS не настроен правильно, 
 браузер может блокировать такие запросы, что приведёт к проблемам с аутентификацией.
Эти три концепции объединены общей темой безопасности и управления доступом в веб-приложениях. 
Одновременное их изучение позволит лучше понять, как эти концепции работают вместе в контексте веб-разработки, 
они критически важны для безопасного обмена данными между клиентом и сервером, 
особенно в современных SPA-приложениях, где фронтенд и бэкенд часто работают на разных доменах.

Когда использовать?
HTTP Headers — используем в каждом запросе, чтобы управлять данными (кэш, тип контента, 
    безопасность), как паспорт при пересечении границы (есть всегда).
Cookies — нужны только для хранения состояния (логины, настройки), как VIP-карта в кофейне (только при заказе).
CORS — исключительно когда фронтенд и API на разных доменах, как разрешение на въезд в закрытый район (только для чужих доменов).
Как работает HTTP Headers, Cookies и CORS?

Без заголовков — нет коммуникации, без кук — нет сессий, без CORS — кросс-доменные запросы заблокированы.

Пример:
Фронтенд (site.com) делает запрос к бэкенду (api.site.com). Браузер автоматически добавляет куки в заголовок Cookie. 
Сервер API должен ответить с CORS-заголовками, например: Access-Control-Allow-Origin: site.com, 
Access-Control-Allow-Credentials: true. Иначе браузер заблокирует ответ из-за политики безопасности.

Историческая справка
1994: Рождение кук в Netscape

Веб-разработчики компании Netscape Communications столкнулись с проблемой сохранения 
состояния пользовательских сессий при переходе между страницами. Решением стало создание 
механизма HTTP-кук (cookies), который впервые был реализован в браузере Netscape Navigator. 
Это позволило сайтам запоминать пользователей и их действия, 
что стало фундаментом для электронной коммерции и персонализированных веб-сервисов. 
Первые куки использовались для простых задач — хранения идентификатора 
корзины покупок и предпочтений пользователя.

2004: Появление Same-origin policy в Firefox

С ростом сложности веб-приложений возникла необходимость в защите пользовательских данных 
от межсайтовых атак. Браузер Firefox одним из первых внедрил политику одинакового источника 
(SOP — Same-origin policy), которая ограничивала выполнение скриптов и доступ к данным только 
в рамках одного домена. Это предотвращало возможность вредоносным сайтам получать доступ к 
конфиденциальной информации на других ресурсах, 
хотя и создавало сложности для легитимных кросс-доменных взаимодействий.

2016: Введение атрибута SameSite в Chrome 51

Для борьбы с CSRF-атаками (межсайтовой подделкой запроса) Google Chrome в версии 51 представил
новый атрибут для кук — SameSite. Он позволял разработчикам контролировать, будут ли куки 
отправляться при кросс-сайтовых запросах. Изначально поддерживались три значения: 
Strict (полная блокировка), Lax (разрешены безопасные переходы) и 
None (разрешены все запросы при условии Secure-флага). 
Это стало важным шагом в защите веб-приложений от несанкционированных действий от имени пользователя.

2023: Стандарт Partitioned cookies

С развитием технологий трекинга и ужесточением политик конфиденциальности появилась необходимость 
в более тонком контроле доступа к кукам. Новый стандарт Partitioned cookies (разделённые куки) 
позволил изолировать хранилище кук между разными сайтами, даже если они встроены друг в друга через iframe. 
Это особенно важно для сервисов, использующих сторонние виджеты и аналитику, 
так как предотвращает нежелательный обмен пользовательскими данными между разными доменами 
без явного согласия пользователя. 
Стандарт был принят основными браузерами как ответ на ужесточение регуляторных требований к защите персональных данных.

HTTP-заголовки
Фундаментальный механизм работы современного веба, выполняющий роль «управляющих меток» для всех запросов и ответов.

HTTP-заголовки — это метаданные, которые передаются между клиентом (например, веб-браузером) и сервером 
в рамках HTTP-запросов и ответов. Они содержат информацию о запросе или ответе, а также о самом клиенте или сервере. 
Заголовки позволяют передавать дополнительные данные, которые помогают управлять поведением HTTP-соединения.

Структура HTTP-заголовка
Каждый HTTP-заголовок состоит из двух частей:

1. Имя заголовка — строка, указывающая на тип информации (например, Content-Type).

2. Значение заголовка — информация, связанная с этим именем (например, application/json).

Заголовки разделяются двоеточием и пробелом. Например: Content-Type: application/json.

Типы HTTP-заголовков
HTTP-заголовки можно разделить на несколько категорий:

1. Общие заголовки (General Headers): используются как в запросах, так и в ответах.

• Date — дата и время, когда был создан заголовок.

• Connection — управление соединением (например, keep-alive).

2. Заголовки запроса (Request Headers): отправляются клиентом на сервер.

• Accept — указывает, какие типы медиаконтента клиент может обрабатывать (например, Accept: application/json).

• User-Agent — информация о клиенте (браузере или приложении), который делает запрос.

• Authorization — данные для аутентификации (например, токены доступа).

3. Заголовки ответа (Response Headers): отправляются сервером в ответ на запрос.

• Content-Type — тип содержимого, возвращаемого сервером (например, Content-Type: text/html).

• Set-Cookie — используется для установки cookies в браузере клиента.

• Location — URL для перенаправления клиента.

4. Заголовки сущности (Entity Headers): предоставляют информацию о содержимом.

• Content-Length — длина тела сообщения в байтах.

• Content-Encoding — указывает кодирование контента (например, gzip).

5. Специальные заголовки: могут использоваться для специфических целей.

• CORS-заголовки — такие как Access-Control-Allow-Origin, которые управляют доступом к ресурсам из других доменов.

Примеры использования
Пример HTTP-запроса:

GET /api/data HTTP/1.1
Host: example.com
User-Agent: Mozilla/5.0
Accept: application/json
Authorization: token
Пример HTTP-ответа:

HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 123
Set-Cookie: sessionId=abc123; HttpOnly
Date: Wed, 21 Oct 2023 07:28:00 GMT
{"data": "Hello, World!"}
Вы можете выполнить команду curl https://ya.ru в powershel и увидеть ответ прямо в консоли.

HTTP-заголовки играют ключевую роль в веб-разработке, обеспечивая передачу важной информации 
между клиентом и сервером. Понимание их работы и правильное использование позволяет разработчикам 
создавать более эффективные и безопасные веб-приложения. 
Заголовки могут значительно влиять на производительность, безопасность и функциональность веб-сайтов и приложений.

Классификация заголовков по направлению передачи

Cookies
Когда пользователь посещает веб-сайт, сервер может отправить один или несколько Cookies в ответе. 
Браузер сохраняет эти Cookies на устройстве пользователя. При последующих запросах к тому же 
сайту браузер отправляет сохранённые куки обратно на сервер, 
что позволяет серверу распознавать пользователя и адаптировать контент в зависимости от его предпочтений.

Структура Cookies
Каждый Cookie состоит из следующих компонентов:

• Имя — уникальный идентификатор Cookie.

• Значение — информация, которую Cookie хранит.

• Домен — указывает, для какого домена Cookie будет действовать.

• Путь — определяет, для каких URL будет доступен Cookie.

• Срок действия — дата и время, когда Cookie истечёт. 
Если срок не установлен, Cookie будет действовать только в течение текущей сессии.

• Флаги:

— Secure — Cookie будет передаваться только по HTTPS.

— HttpOnly — Cookie не будет доступен через JavaScript, что помогает защитить его от атак XSS (межсайтового скриптинга).

— SameSite — ограничивает отправку Cookie при межсайтовых запросах, 
что помогает предотвратить атаки CSRF (межсайтовой подделки запросов).

Типы Cookies
1. Сессионные (Session Cookies) — временные Cookies, которые хранятся в памяти браузера 
и удаляются после его закрытия. Они используются для отслеживания сессий пользователей.

2. Постоянные (Persistent Cookies) — сохраняются на устройстве пользователя до истечения 
срока действия или до тех пор, пока пользователь их не удалит. 
Они используются для хранения информации между сессиями (например, предпочтения языка).

3. Cookies третьих лиц (Third-party Cookies) — устанавливаются доменами, отличными от того, 
который пользователь посещает. Они часто используются для рекламы и аналитики.

4. Безопасные (Secure Cookies) — передаются только по HTTPS-соединению, что защищает их от перехвата.

5. HttpOnly Cookies — недоступны через JavaScript, что помогает защитить их от атак.

Применение Cookies
• Аутентификация — хранение информации о пользователе (например, токены доступа) 
для автоматической авторизации при повторных посещениях.

• Персонализация — запоминание предпочтений пользователя (например, язык интерфейса или тема оформления).

• Аналитика — отслеживание поведения пользователей на сайте для улучшения его функциональности и контента.

• Реклама — использование для таргетированной рекламы и отслеживания эффективности рекламных кампаний.

Примеры установки и получения Cookies
Установка Cookie на стороне сервера (например, с использованием PHP):

setcookie("username", "JohnDoe", time() + (86400 * 30), "/"); // 86400 = 1 день
Чтение Cookie на стороне клиента (JavaScript):

let username = getCookie("username");
function getCookie(name) {
    let cookieArr = document.cookie.split(";");
    for(let i = 0; i < cookieArr.length; i++) {
        let cookiePair = cookieArr[i].split("=");
        if(name == cookiePair[0].trim()) {
            return decodeURIComponent(cookiePair[1]);
        }
    }
    return null;
}
Аспекты безопасности
Cookies могут быть уязвимыми для различных атак:

• XSS (межсайтовый скриптинг): если злоумышленник может выполнить скрипт на странице, 
он может получить доступ к Cookies. Использование флага HttpOnly помогает предотвратить это.

• CSRF (межсайтовая подделка запросов): злоумышленник может попытаться использовать Cookies 
для выполнения нежелательных действий от имени пользователя. 
Использование токенов CSRF может помочь в защите от этой атаки.

• Сниффинг: если соединение не защищено (HTTP вместо HTTPS), 
злоумышленник может перехватить Cookies. Использование флага Secure и шифрование соединения помогают защитить данные.

Разработчикам необходимо учитывать аспекты безопасности и конфиденциальности при использовании Cookies, 
чтобы защитить пользователей от потенциальных угроз. Правильное использование 
Cookies может значительно повысить удобство использования веб-приложений и сайтов.

Работа с Cookies в JavaScript
Рассмотрим примеры, в которых показано управление куками: запись/чтение/удаление данных с помощью document.cookie.

Запись куки
Чтобы выполнить запись, мы используем:

document.cookie = "ключ=значение; параметры", параметры управляют поведением:
— expires задаёт срок действия (например, expires=Thu, 18 Dec 2025 12:00:00 UTC);
— path=/ определяет видимость на страницах сайта;
— Secure разрешает передачу только по HTTPS.
Каждый вызов добавляет/обновляет одну куку (не перезаписывает всё!), 
а для удаления установим прошедшую дату в параметр expires.
Как вы, наверное, уже поняли, expires — это такой срок годности куки. 
Установка срока действия куки (expires) на дату в прошлом заставляет браузер 
немедленно удалить эту куку. Это стандартный способ программного удаления кук на клиентской стороне.

document.cookie = "username=John; expires=Thu, 18 Dec 2025 12:00:00 UTC; path=/";
expires – срок действия.
path – путь, для которого куки доступны.
Secure – только через HTTPS.
HttpOnly – нельзя прочитать через JS (ставится сервером).
Чтение куки
function getCookie(name) {
  const cookies = document.cookie.split(';');
  for (let cookie of cookies) {
    const [key, value] = cookie.trim().split('=');
    if (key === name) return value;
  }
  return null;
}

console.log(getCookie("username")); // "John"
Удаление куки
Чтобы выполнить удаление, установим куку с прошедшей датой и теми же параметрами (путь/домен), что при создании:

document.cookie = "username=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;";
Браузер проверяет срок expires, обнаруживает, что кука устарела, и удаляет её. 
Важно, чтобы параметры path=/ и domain совпадали с оригинальной кукой. 
Без правильного пути/домена браузер не удалит куку!

Безопасность Cookies
Основные угрозы:

XSS-атаки – если куки не HttpOnly, злоумышленник может украсть их через JS.
CSRF-атаки – если куки не SameSite, сайт evil.com может выполнить действия от имени пользователя.
Как защититься?

✔ Использовать HttpOnly для критичных кук (например, сессионных).
✔ Использовать Secure для передачи только по HTTPS.
✔ Включать SameSite=Strict или Lax для защиты от CSRF.
✔ Регулярно обновлять токены сессии.

Практический пример: авторизация через Cookies

Сервер отправляет куки после входа:
HTTP/1.1 200 OK
Set-Cookie: auth_token=xyz789; HttpOnly; Secure; SameSite=Strict; Path=/
2. Браузер отправляет куки при запросах:

fetch("/api/profile", { credentials: "include" // Отправка кук })
.then(res => res.json())
.then(data => console.log(data));
Cross-Origin Resource Sharing (CORS)
CORS — это механизм безопасности, реализованный в браузерах, 
который контролирует доступ к ресурсам между разными доменами. 
Он расширяет политику одинакового источника (SOP — Same-origin policy), 
разрешая безопасные кросс-доменные запросы при условии явного разрешения сервера.

Основная задача CORS — предотвратить несанкционированный доступ к данным с других доменов (например, 
    кража сессий через XSS), но при этом разрешить легитимные запросы (например, API для фронтенда на другом домене).

Чтобы разобраться с механизмом работы CORS, нужно рассказать о Политике одинакового источника, или Same-Origin Policy (SOP).

Политика SOP — это фундаментальное правило браузеров, которое запрещает скриптам с
 одного домена читать ответы с другого домена. Источник (origin) определяется тремя параметрами:

Протокол (HTTP/HTTPS)
Домен (example.com)
Порт (80, 443 и др.)
Примеры разных источников:

http://example.com ≠ https://example.com (разный протокол)
example.com ≠ api.example.com (разный поддомен)
Зачем нам ещё одно определение и почему оно важно?

Без SOP злоумышленник мог бы украсть куки сессии через <script> с вредоносного сайта 
и/или выполнить CSRF-атаку, например, перевод денег без ведома пользователя, 
согласитесь, весьма опасная ситуация.

Но что если вам нужно реализовать взаимодействие разных доменов, а политика SOP мешает? 
Например, ваш самодельный сайт про путешествия https://myTravel.ru должен показывать погоду, 
используя API https://weather-api.com. Вот тут-то надо вспомнить или узнать про CORS, 
иначе браузер не позволит вашему сайту получить данные о погоде.

Как CORS обходит ограничения SOP?

CORS действует через HTTP-заголовки, которые сервер отправляет в ответ на запросы. 
Браузер проверяет эти заголовки и решает, разрешить ли доступ к данным.

Ключевые заголовки CORS:
Access-Control-Allow-Origin
Указывает, какие домены могут получать доступ к ресурсу.

Пример: Access-Control-Allow-Origin: https://frontend.com.

Access-Control-Allow-Methods
Разрешает конкретные HTTP-методы (GET, POST, PUT и т. д.).

Пример: Access-Control-Allow-Methods: GET, POST.

Access-Control-Allow-Headers
Разрешает нестандартные заголовки (например, Authorization).

Пример: Access-Control-Allow-Headers: Content-Type, X-API-Key.

Access-Control-Allow-Credentials
Разрешает отправку кук и токенов авторизации (withCredentials: true в fetch).

Важно! Нельзя использовать с Access-Control-Allow-Origin: *.

Как работает?

Браузер сразу отправляет запрос с заголовком Origin, 
а сервер отвечает с Access-Control-Allow-Origin. Если заголовок совпадает, доступ разрешён.

Предварительные запросы (Preflight Requests)
Когда возникают?

Нестандартные методы (PUT, DELETE, PATCH).

Кастомные заголовки (X-API-Key).

Content-Type: application/json.

Если разрешено, браузер отправляет основной запрос.

Безопасность CORS
Неправильная настройка может привести к неприятным последствиям, например, 
можно свести политику SOP к нулю и открыть путь ранее описанным уязвимостям, что приведёт к:

Утечке данных
Если Access-Control-Allow-Origin:, любой сайт может читать ответы API.
CSRF-атаке
Если CORS разрешает запросы с куками (Access-Control-Allow-Credentials: true), злоумышленник может использовать их для подделки действий.
XSS + CORS
Если доверенный поддомен уязвим к XSS, атакующий может украсть данные через CORS.
Рекомендации:

Никогда не используйте Access-Control-Allow-Origin: для авторизованных запросов.
Ограничивайте методы (Access-Control-Allow-Methods).
Проверяйте заголовок Origin на сервере (белые списки).
Помните, что CORS — это не защита, а механизм контроля. CORS не заменяет другие меры 
безопасности (CSRF-токены, валидацию входных данных). 
Его задача — дать серверу контроль над тем, какие домены могут получать его ресурсы.

Альтернативы CORS:

JSONP (устарел, небезопасен)
Прокси-сервер (например, Nginx на одном домене с фронтендом)
WebSockets (не подчиняются SOP)
Исторический контекст

1995: Netscape вводит SOP для защиты DOM.
2004: Появление первых обходных методов (JSONP).
2014: CORS становится стандартом W3C.
Давайте подытожим
CORS не блокирует запросы, а лишь требует явного разрешения от сервера.
Без CORS современные SPA и микросервисные архитектуры были бы невозможны.
Настройка CORS — ответственность бэкенда, но фронтенд-разработчик должен понимать его принципы.
Практическое применение: заголовки, Cookies и CORS
Давайте посмотрим, как эти технологии работают в реальных проектах? Разберём ключевые сценарии:

1. Аутентификация пользователя
Cookies

Сервер отправляет куку с сессионным ID после входа:

Set-Cookie: session_id=abc123; Secure; HttpOnly; SameSite=Lax
Браузер автоматически прикрепляет её к каждому запросу.
Headers

Для API-ориентированной аутентификации:

Authorization: Bearer eyJhbGciOiJIUzI1Ni...

2. Кросс-доменные запросы (CORS)
Когда фронтенд на https://front.com запрашивает API на https://api.com,

браузер отправляет preflight-запрос (OPTIONS):

Origin: https://front.com
Access-Control-Request-Method: POST
Сервер отвечает:

Access-Control-Allow-Origin: https://front.com
Access-Control-Allow-Methods: POST, GET
Access-Control-Allow-Credentials: true
Клиент делает основной запрос с куками:

fetch('https://api.com/data', {
  method: 'POST',
  credentials: 'include' // Важно для отправки кук!
});
3. Персонализация контента Cookies + Headers
Пользователь выбирает тёмную тему → сохраняем в куки:

document.cookie = "theme=dark; max-age=2592000; path=/";

Сервер читает заголовок Cookie и подставляет нужные стили.

4. Защита от атак Security Headers:
Content-Security-Policy: default-src 'self';
X-Frame-Options: DENY
Strict-Transport-Security: max-age=31536000
Защита кук:

HttpOnly — блокирует доступ JavaScript к сессионным кукам

SameSite=Strict — предотвращает CSRF-атаки

Secure — передача только по HTTPS
5. Оптимизация производительности Headers для кэширования:
Cache-Control: public, max-age=3600
ETag: "33a64df5"
→ Браузер кэширует статику (CSS/JS) на 1 час, экономя трафик.
Еще одна аналогия:

Headers — дирижёр HTTP-коммуникации (безопасность, кэш, тип данных).
Cookies — хранитель состояния пользователя (сессии, настройки).
CORS — мост между доменами (обеспечивает безопасный доступ).
Правильное применение этих технологий позволяет создавать быстрые, безопасные и удобные приложения.

Типичные ошибки
Тип ошибки 1: Куки без Secure на HTTPS
Описание:

Кука передаётся по незащищённому соединению. Это как отправить пароль открыткой — перехватить легко.

Пример ошибки:

// На HTTPS-сайте:
document.cookie = "session=123; path=/";
// Браузер заблокирует куку → она не сохранится
Объяснение:

На HTTPS-сайтах куки без флага Secure игнорируются.

Решение:

document.cookie = `session=123; Secure; path=/`;
Всегда добавляем Secure.

Тип ошибки 2: HttpOnly-куки через JS
Описание:

Попытка прочитать куку, которую сервер пометил как HttpOnly. 
Это как пытаться втянуть в себя суп через соломинку — не получится.

Пример ошибки:

console.log(getCookie("session_id")); // null
// Сервер установил куку:
// Set-Cookie: session_id=abc; HttpOnly; Secure
Объяснение:

HttpOnly-куки невидимы для JavaScript (доступны только серверу). 
Флаг HttpOnly специально создан для защиты от XSS-атак. Он делает куки невидимыми для JavaScript, 
но при этом они автоматически прикрепляются к каждому HTTP-запросу на сервер. 
Это важный механизм безопасности для защиты сессионных данных.

Решение:

fetch('/api/data', {
  credentials: 'include' // Куки отправятся автоматически
});
Не используем JS для доступа к сессионным кукам — работаем через заголовки.

Тип ошибки 3: Кросс-доменные куки без credentials: ‘include’
Описание:

Куки не отправляются в CORS-запросе. Это как забыть паспорт на границе — вас не пропустят.

Пример ошибки:

fetch('https://api.example.com/login', {
  method: 'POST'
}); // Куки не прикрепляются → сессия не создаётся
Объяснение:

По умолчанию fetch не отправляет куки в кросс-доменных запросах.

Решение:

fetch('https://api.example.com/login', {
  method: 'POST',
  credentials: 'include' // Теперь куки отправятся!
});
Добавляем credentials: ‘include’.

Тип ошибки 4: CSRF-атаки из-за SameSite=None
Описание:

Кука без SameSite-защиты позволяет злоумышленникам подделывать запросы. Это как оставить дверь открытой в банк.

Пример ошибки:

// Уязвимая кука:
document.cookie = "user_token=xyz; path=/; Secure";
Объяснение:

Без SameSite=Lax/Strict браузер отправляет куки в любых кросс-доменных запросах (риск CSRF).

Решение:

document.cookie = "user_token=xyz; Secure; SameSite=Lax; path=/";
Всегда ставим SameSite.

Итог:

Всегда настраивайте CORS явно – возвращайте Access-Control-Allow-Origin для доверенных доменов, а не * для защищённых запросов.
Помечайте куки флагами Secure и SameSite – Secure для HTTPS-сайтов, SameSite=Lax/Strict для защиты от CSRF.
Не пытайтесь читать HttpOnly-куки через JS – работайте с ними только через заголовки запросов.
Используйте credentials: ‘include’ в кросс-доменных fetch – иначе куки/аутентификация не отправятся.
Проверяйте заголовки безопасности – Content-Security-Policy, X-Frame-Options и Strict-Transport-Security обязательны для production.






Практикум
Задача 1: Безопасная установка куки
Ситуация:

Вы работаете над личным кабинетом клиента. После ввода логина/пароля сервер выдаёт токен доступа. 
Ваша задача — безопасно сохранить его в браузере, чтобы:

пользователь оставался авторизованным неделю;
злоумышленники не могли украсть токен через XSS-уязвимости;
препятствовать CSRF-атакам (когда пользователя обманом заставляют выполнить действия в банке).
Задача:

Напишите функцию setAuthCookie(token), которая:

Устанавливает куку auth_token со значением токена.
Добавляет параметры: Secure, HttpOnly, SameSite=Strict.
Действует 7 дней.
Решение:

function setAuthCookie(token) {
  const greatestAge = 7 * 24 * 60 * 60; // 7 дней в секундах
  document.cookie = `auth_token=${token}; max-age=${greatestAge}; path=/; Secure; HttpOnly; SameSite=Strict`;
}

// Пример вызова:
setAuthCookie("eyJhbGciOiJIUzI1Ni...");











Задача 2: Настройка CORS для кросс-доменного запроса
Ситуация:

Мы запустили интернет-магазин электроники на https://gadget-store.ru.
 Для обработки платежей подключили сторонний сервис https://api.paysecure.io, 
 но при попытке оплаты появляется ошибка: Access to fetch at 'https://api.paysecure.io/process' 
 from origin 'https://gadget-store.ru' has been blocked by CORS policy.

Задача:

Сделать POST-запрос с разрешением CORS.
Разрешить отправку кук.
Обработать ошибку CORS.
Решение:

async function sendData() {
  try {
    const response = await fetch("https://api.com/data", {
      method: "POST",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify({ data: "test" }),
      credentials: "include" // Важно для кук!
    });

    if (!response.ok) throw new Error("CORS error");
    return await response.json();

  } catch (error) {
    if (error.message.includes("CORS")) {
      console.error("Ошибка CORS: проверьте настройки сервера");
    }
  }
}



Итоги
Сегодня мы:

разобрали роль HTTP-заголовков: как они управляют кэшированием, аутентификацией и безопасностью веб-приложений;

узнали, как работают Cookies, научились сохранять данные пользователя;

поняли механизм CORS: как он регулирует кросс-доменные запросы и почему 
без заголовков Access-Control-Allow-Origin браузер блокирует взаимодействие между разными доменами;

научились защищать Cookies от атак (XSS, CSRF) с помощью атрибутов HttpOnly, Secure и SameSite;

проработали типичные уязвимости – от CSRF-атак из-за отсутствия SameSite до утечки токенов через незащищённые куки;

реализовали авторизацию через Cookies + JWT;

отладили проблемы с куками в браузере.

Теперь мы можем управлять HTTP-запросами, настраивать заголовки, безопасно передавать данные между клиентом и сервером, даже если они на разных доменах.