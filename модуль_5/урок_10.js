Урок №10. Методы строк (split, substring, trim).

Учебные материалы

На прошлом занятии мы уже увидели, как встроенные методы упрощают работу с массивами. 
Со строками — та же история: вместо ручной обработки текста можно использовать готовые методы.

Обработка строк — одна из самых частых задач в разработке. 
Строки как текст в документах: мы постоянно что-то читаем, правим, ищем, форматируем. 
Методы строк — это наш «текстовый редактор», встроенный в язык.

Валидация email, разбивка предложений, удаление лишних пробелов, 
форматирование текста — всё это мы делаем регулярно.

Чтобы не изобретать велосипед каждый раз, важно знать, какие инструменты уже есть в языке. 
Сегодня мы познакомимся с ключевыми методами работы со строками и научимся применять их в реальных задачах.

Сегодня мы:

изучим методы для проверки наличия подстроки, разбиения строки на массивы, извлечения части строки и другие;
поймём, как эти методы можно использовать для обработки пользовательского ввода, 
работы с текстовыми данными и решения практических задач;
научимся применять методы строк для решения задач, которые часто могут встретиться в реальных проектах.







Глоссарий к десятому занятию
String (Строка)
Строка — это последовательность символов, используемая для хранения и работы с текстовой информацией. 
В JavaScript строки создаются с использованием кавычек (одинарных, двойных или обратных). 
Например, "Hello, World!" — это строка.

Substring (Подстрока)
Подстрока — это часть строки. Например, в строке "JavaScript" подстрока "Java" начинается с первого символа. 
Методы substring() или slice() позволяют извлекать подстроки из строки.

Split (Разделить)
Метод split() разделяет строку на массив частей, используя указанный разделитель. 
Например, "a,b,c".split(",") создаст массив ["a", "b", "c"].

Trim (Обрезать)
Метод trim() удаляет пробелы в начале и конце строки. Например, " Hello ".trim() вернёт строку "Hello".

Includes (Содержит)
Метод includes() проверяет, содержит ли строка указанную подстроку. 
Например, "JavaScript".includes("Java") вернёт true.

StartsWith (Начинается с)
Метод startsWith() проверяет, начинается ли строка с указанной подстроки. 
Например, "JavaScript".startsWith("Java") вернёт true.

EndsWith (Заканчивается на)
Метод endsWith() проверяет, заканчивается ли строка на указанную подстроку. 
Например, "JavaScript".endsWith("Script") вернёт true.

Concatenation (Конкатенация)
Конкатенация — это процесс объединения строк. В JavaScript это делается с помощью оператора +. 
Например, "Hello, " + "World!" вернёт строку "Hello, World!".

Length (Длина)
Свойство length возвращает количество символов в строке. Например, "Hello".length вернёт 5.

Character (Символ)
Символ — это один элемент строки. Например, буква "H" в строке "Hello" — это символ. 
Символ можно получить с помощью индекса: "Hello"[0] вернёт "H".





Строки
Один из основных типов данных в JavaScript, предназначенный для хранения и обработки текста. 
Важно подробно изучить, как устроены строки и как с ними грамотно работать, 
ведь текст используется повсеместно: для отображения сообщений, взаимодействия с пользователем, 
формирования URL-адресов, работы с API, в HTML-разметке и т. д.


Основные свойства и устройство строк
Что такое строки в JavaScript?
Как мы помним, строки представляют собой последовательности символов (букв, цифр, пробелов и других знаков), 
которые заключены в кавычки. Кавычки означают начало и конец строки. 
В JavaScript строки можно создавать с помощью трёх типов кавычек:

Одинарные кавычки (‘ ‘). Используются для простых строк.

let singleQuoteStr = 'Пример строки';
console.log(singleQuoteStr); // 'Пример строки'
Двойные кавычки (» «). Аналогичны одинарным, но часто применяются для строк, которые содержат апостроф.
let doubleQuoteStr = "Это тоже строка";
console.log(doubleQuoteStr); // "Это тоже строка"

Обратные кавычки (). Называются шаблонными строками (template literals). 
Они позволяют создавать многострочность и внедрять выражения с помощью синтаксиса ${...}. 
Так можно очень легким для чтения способом вложить переменные в строки.
let name = "Иван";
let templateLiteral = `Привет, ${name}!`; // Интерполяция
console.log(templateLiteral); // "Привет, Иван!"


Ключевые особенности строк в JavaScript

1. Иммутабельность строк
Иммутабельность означает, что строки нельзя изменить после их создания. 
Любое изменение строки создаёт новую строку, а не модифицирует исходную.

Пример:

let str = "Hello";
str[0] = "h"; // Невозможно изменить символ строки напрямую
console.log(str); // "Hello" — исходная строка остаётся неизменной

// Для изменения строки создаётся новая
let newStr = "h" + str.slice(1); // Заменяем первый символ на "h"
console.log(newStr); // "hello"

2. Индексация строк
Строки поддерживают доступ к отдельным символам по индексу. 
Индексация начинается с 0 (нумерация слева направо).

Пример:

let str = "JavaScript";
console.log(str[0]); // "J" — первый символ
console.log(str[4]); // "S" — пятый символ
console.log(str[str.length - 1]); // "t" — последний символ
Обратите внимание, что доступ к символам строки предоставляется только для чтения. 
Попытка изменить символ вызовет ошибку.

3. Длина строки
Длина строки измеряется количеством символов и определяется с помощью свойства .length.

Пример:

let str = "Hello, World!";
console.log(str.length); // 13 — строка состоит из 13 символов, включая пробелы и знаки препинания



4. Строки не являются массивами
Хотя строки и поддерживают доступ к символам по индексу, они не обладают всеми свойствами массивов. 
Например, нельзя использовать методы массивов, такие как .push() или .pop(), на строках. 
Однако строки имеют собственные методы, 
которые помогают работать с текстом (например, .slice(), .toUpperCase(), .replace() и другие).

Пример:

let str = "JavaScript";
console.log(str.slice(0, 4)); // "Java" — возвращает часть строки
console.log(str.toUpperCase()); // "JAVASCRIPT" — преобразует строку в верхний регистр
console.log(str.replace("Java", "Type")); // "TypeScript" — заменяет подстроку


Дополнительные возможности работы со строками
1. Многострочные строки
С помощью шаблонных строк можно создавать многострочные строки без использования символа перевода строки \n.

Пример:

let multiline = `Это первая строка.
А это вторая строка.
Третья строка — тоже здесь.`;
console.log(multiline);
/*
Вывод:
Это первая строка.
А это вторая строка.
Третья строка — тоже здесь.
*/

2. Вставка выражений (интерполяция)
Шаблонные строки позволяют встраивать переменные и выражения прямо внутрь строки.

Пример:

let a = 5;
let b = 10;
let sum = `Сумма ${a} + ${b} = ${a + b}`;
console.log(sum); // "Сумма 5 + 10 = 15"

3. Экранирование символов
Для использования специальных символов (например, 
кавычек или символов перевода строки) внутри строк применяются обратные слеши \.

Пример:

let str = 'I\'m a developer'; // Экранирование одинарной кавычки
console.log(str); // "I'm a developer"


Методы строк в JavaScript
Работа со строками — важная часть любой программы, 
причем часто для создания программы приходится прибегать к типичным подзадачам на строки. 
Например, для решения многих задач с текстовыми данными сперва необходимо совершить предобработку этих данных: 
исключить пробелы и специальные символы, привести весь текст к одному регистру и т. д. 
Подавляющее большинство таких типовых технических задач в JavaScript можно решить при помощи набора встроенных методов, 
которые упрощают манипуляции со строками.

Эти методы позволяют:

Анализировать строки (например, искать символы, подстроки).
Изменять текст (например, приводить к нужному регистру, обрезать лишние символы).
Разбивать строки или извлекать из них отдельные части.
Теперь рассмотрим основные методы строк и их применение.

1. Свойство .length
Первый шаг при работе со строкой — понять, сколько символов она содержит. 
Это важно, например, при проверке длины пользовательского пароля или ограничения длины текста в интерфейсе.

Пример использования:

let greeting = "Hello, world!";
console.log(greeting.length); // 13 — длина строки
Совет
Помните, что пробелы и знаки препинания тоже учитываются в длине строки.


2. Изменение регистра: toUpperCase() и toLowerCase()
Эти методы полезны, когда нужно стандартизировать текст. 
Например, при проверке ввода электронной почты или логина можно привести строку к одному регистру.

toUpperCase() переводит текст в верхний регистр.
toLowerCase() переводит текст в нижний регистр.
Пример использования:

let text = "JavaScript";
console.log(text.toUpperCase()); // "JAVASCRIPT"
console.log(text.toLowerCase()); // "javascript"
Особенность: эти методы не изменяют исходную строку, а возвращают новую.

3. Удаление лишних пробелов: trim()
При работе с пользовательским вводом часто встречается проблема лишних пробелов в начале или конце строки. 
Метод trim() позволяет удалить пробелы и оставить только полезную информацию.

trim() — это как стереть ластиком лишнее с краёв строки. 
Полезен перед тем, как начать сравнивать строки или сохранять ввод пользователя.

Пример использования:

let userInput = "   hello   ";
console.log(userInput.trim()); // "hello"
Есть и более специфические варианты (trimStart() и trimEnd()), которые удаляют пробелы только с одной стороны строки.

4. Извлечение подстрок: slice() и substring()
Иногда нужно получить часть строки, например первые 10 символов из статьи или доменное имя из URL. 
Здесь пригодятся методы slice() и substring().

Они работают, как нож, который аккуратно вырезает нужный кусочек текста из строки. 
Только один (slice) умеет отсчитывать с конца, а другой (substring) нет.

slice(startIndex, endIndex) возвращает подстроку, начиная с startIndex до endIndex (не включая его).
Особенность: поддерживает отрицательные индексы, чтобы отсчитывать символы с конца строки.

let message = "Welcome to JavaScript";
console.log(message.slice(11, 21)); // "JavaScript"
console.log(message.slice(-10)); // "JavaScript" — с конца
substring(startIndex, endIndex) похож на slice(), но не поддерживает отрицательные индексы.
console.log(message.substring(11, 21)); // "JavaScript"

5. Замена подстрок: replace() и replaceAll()
Когда нужно заменить одно или все вхождения подстроки в строке, 
используйте методы replace() и replaceAll(). 
Это может быть полезно, например, для корректировки текста перед выводом.

Пример использования:

let phrase = "apple and apple pie";
console.log(phrase.replace("apple", "orange")); // "orange and apple pie"
console.log(phrase.replaceAll("apple", "orange")); // "orange and orange pie"
Особенность: replace() меняет только первое вхождение, а replaceAll() — все.

6. Разделение строки: split()
Метод split() превращает строку в массив, разграничивая её по указанному разделителю. 
Это часто используется для обработки данных, например CSV-файлов.

Представьте строку как записку, в которой через запятую перечислены задачи. 
Метод split() похож на ножницы, которые разрезают эту записку по запятым и превращают её в список задач.

Пример использования:

let csv = "red,green,blue";
let colors = csv.split(",");
console.log(colors); // ["red", "green", "blue"]

7. Поиск в строке: includes(), indexOf(), и lastIndexOf()
Если нужно понять, содержит ли строка определённую подстроку или узнать её положение, используйте эти методы:

includes()– проверяет наличие подстроки.

let sentence = "Learning JavaScript is fun!";
console.log(sentence.includes("JavaScript")); // true
console.log(sentence.includes("Python")); // false
indexOf() и lastIndexOf() – возвращают индекс первого или последнего вхождения подстроки.
let data = "banana";
console.log(data.indexOf("a")); // 1
console.log(data.lastIndexOf("a")); // 5
Совет
Если indexOf() возвращает -1, значит, подстрока не найдена.

8. Работа с символами: charAt() и charCodeAt()
Эти методы позволяют получить символ по индексу или узнать его Unicode-код.

Пример использования:

let letter = "A";
console.log(letter.charAt(0)); // "A"
console.log(letter.charCodeAt(0)); // 65 — код символа "A"

9. Поиск по шаблону: match()
Если нужно найти одно или несколько совпадений по шаблону (например, все слова, 
цифры или определённые комбинации символов), используйте метод match() с регулярным выражением.

let text = "I have 2 apples and 10 bananas.";

// Найти все числа в строке
let numbers = text.match(/\d+/g);
console.log(numbers); // ["2", "10"]

// Найти все слова, начинающиеся с заглавной буквы
let sentence = "JavaScript is Great!";
let capitalWords = sentence.match(/\b[A-Z][a-z]*\b/g);
console.log(capitalWords); // ["JavaScript", "Great"]


Особенности:

Без флага g возвращает только первое совпадение.
С флагом g возвращает массив всех совпадений или null, если совпадений нет.
console.log("hello123".match(/\d+/));   // ["123"]
console.log("hello".match(/\d+/g));     // null
Совет
match() — мощный инструмент, когда нужно искать по шаблону, а не просто фиксированную подстроку.

Особенности работы со строками
Строки в JavaScript обладают рядом особенностей, которые делают их мощным инструментом, 
но требуют внимания в определённых ситуациях.


Шаблонные строки (Template Literals)
Шаблонные строки, заключённые в обратные кавычки ( ` ), — это удобный способ работать с динамическими строками. 
Они поддерживают интерполяцию с помощью ${} и позволяют легко комбинировать строки с переменными, 
выражениями и функциями. Это особенно полезно, 
когда необходимо собрать строку из нескольких частей или вывести переменную в тексте.

Пример использования:

let name = "Alice";
let greeting = `Hello, ${name}!`;
console.log(greeting); // "Hello, Alice!"
Шаблонные строки делают код более читаемым и избавляют от необходимости вручную объединять строки с помощью оператора +.

Работа с эмодзи и многобайтовыми символами
Некоторые символы, такие как эмодзи, занимают больше одного байта в строках. 
Это может вызывать проблемы при подсчёте длины строки или индексации символов. 
Например, эмодзи может занимать два или более символов Unicode. 
Для корректной работы с ними нужно использовать методы из интерфейса Intl или методы String.prototype, 
такие как codePointAt() и fromCodePoint(), которые учитывают многобайтовые символы.

Сравнение строк
В JavaScript строки сравниваются на основе их Unicode-кода, а не лексикографически, 
как это может быть ожидаемо в некоторых языках. Это означает, 
что строки будут сравниваться символ за символом с учётом их Unicode-значений.

Пример:

console.log("Z" > "a"); // false
console.log("Zoo" < "alphabet"); // true
Это поведение важно учитывать, особенно если вы работаете с текстами на разных языках или с символами, 
имеющими различные Unicode-значения.

Эффективность
Строки в JavaScript — это неизменяемые объекты. 
Когда вы вызываете метод, который изменяет строку (например, replace() или slice()), создаётся новая строка, 
а не изменяется существующая. 
Это значит, что при работе с большими объёмами текстовых данных нужно учитывать производительность, 
так как создание новых строк может потребовать дополнительных ресурсов.

Когда что использовать?
В зависимости от задачи различные методы и особенности строк могут быть полезны в разных контекстах.

Для проверки содержания строки: используйте includes(), чтобы узнать, содержит ли строка нужный фрагмент.
Для обработки ввода пользователя: trim() удаляет лишние пробелы с обеих сторон строки, что важно для ввода данных.
Для создания сложных строк: используйте шаблонные строки, чтобы легко и удобно комбинировать текст с переменными.
Для поиска и замены текста: используйте replace() для замены подстрок или регулярные выражения для более сложных случаев.
Даже зная основные методы работы со строками, можно легко столкнуться с неожиданными результатами. 
Некоторые особенности JavaScript не всегда очевидны: одни методы ведут себя не так, 
как кажется на первый взгляд, другие — требуют особой осторожности при использовании.



Рисунок 1
Типичные ошибки
Тип ошибки 1: Попытка изменить символ строки по индексу
Ошибка:
Строки в JavaScript — это неизменяемые объекты, 
поэтому попытка изменить символ строки по индексу приведет к ошибке.

Пример ошибки:

let str = "Hello";
str[0] = "h";  // Ошибка: строки неизменяемы
console.log(str);  // "Hello", но попытка изменения не приведет к результату
Почему это происходит:
Строки в JavaScript являются неизменяемыми, 
и попытка напрямую изменить символ по индексу не будет работать.

Решение:
Для изменения строки нужно создать новую строку с использованием методов, таких как slice() или оператор конкатенации.

let newStr = "h" + str.slice(1);
console.log(newStr);  // "hello"






Тип ошибки 2: Использование отрицательных индексов в методах substring() и slice()
Важно:
Метод substring() автоматически превращает отрицательные индексы в 0, 
так что использование отрицательных индексов фактически не вызывает ошибку выполнения кода, 
но может привести к неожиданным результатам.

Метод slice(), наоборот, поддерживает отрицательные индексы, начиная отсчёт с конца строки, 
что может быть неожиданным, если вы привыкли к substring().

Пример:

let str = "Hello, World!";
console.log(str.substring(-5, -1));  // "" - пустая строка
console.log(str.slice(-5, -1)); // orld!
Решение:
Используйте только положительные индексы для метода substring(). 
Если нужно использовать отрицательные индексы, применяйте slice():

console.log(str.substring(0, 5));  // "Hello"
console.log(str.slice(-5, -1));    // "orld"







Тип ошибки 3: Неправильное понимание поведения split() без аргумента
Что происходит:

Без указания аргумента (разделителя), split() не делит строку, 
а просто возвращает массив с одним элементом — самой строкой.

Пример:

let str = "Hello, World!";
let result = str.split();
console.log(result);  // ["Hello, World!"]
Как делать правильно:

Лучше всего вручную указывать разделитель. Если мы хотим поделить строку по буквам, 
указываем пустую строку как разделитель, используя кавычки.

let words = str.split(" ");   // ["Hello,", "World!"]
let chars = str.split("");    // ["H", "e", "l", "l", "o", ",", " ", "W", "o", "r", "l", "d", "!"]





Тип ошибки 4: Ошибка при преобразовании строк в числа
Ошибка:
При преобразовании строк, содержащих символы, в числа с помощью parseInt() или parseFloat() могут возникнуть ошибки, 
если строку не получится корректно интерпретировать как число.

Пример ошибки:

let str = "hello";
let num = parseInt(str);  // Ошибка: NaN
console.log(num);  // NaN
Почему это происходит:
Методы parseInt() и parseFloat() пытаются преобразовать строку в число, но, 
если строка не начинается с числовых символов, результатом будет NaN. (Not-a-Number).

Решение:
Перед преобразованием строки в число всегда проверяйте её содержание.

let str = "42";
let num = parseInt(str);  // 42
let invalidStr = "hello";
let invalidNum = parseInt(invalidStr);  // NaN








Тип ошибки 5: Ошибка при неправильном использовании регулярных выражений с методами строк
Ошибка:
Методы строк, такие как replace(), match() или split(), ожидают регулярные выражения, 
но при передаче невалидного регулярного выражения может произойти ошибка выполнения.

Пример ошибки:

let str = "Hello, World!";
let result = str.replace(/[^a-zA-Z]/, "");  // Ошибка
console.log(result);  // Ожидаемое поведение не происходит
Почему это происходит:
Регулярные выражения требуют правильного синтаксиса. 
В примере выше регулярное выражение с использованием диапазона символов [^a-zA-Z] может не работать так, как ожидалось.

Решение:
Убедитесь, что ваше регулярное выражение правильно составлено. 
Например, используйте флаг g для глобальной замены.

let str = "Hello, World!";
let result = str.replace(/[^a-zA-Z]/g, "");
console.log(result);  // "HelloWorld"









Практикум
Задача 1: Поиск и суммирование чисел в строке
Ситуация:
Вы работаете над анализатором текста, который должен извлекать все числа из строки и подсчитывать их сумму. 
Такой анализ может быть полезен для обработки текстовых документов, 
где присутствуют суммы или значения (например, в отчётах о доходах или расходах).

Задача:
Напишите функцию, которая принимает строку, извлекает все числа (целые и с плавающей запятой) и выводит их сумму.

Шаги реализации:

Используйте регулярное выражение для поиска всех чисел в строке.
Преобразуйте найденные строки в числа.
Используйте метод reduce() для вычисления суммы.
Решение:

function sumNumbersInString(str) {
    const numbers = str.match(/-?\d+(\.\d+)?/g); // Ищем все числа, включая отрицательные и с плавающей запятой
    if (numbers) {
        return numbers.reduce((acc, num) => acc + parseFloat(num), 0); // Суммируем числа
    } else {
        return 0; // Если чисел нет, возвращаем 0
    }
}

console.log(sumNumbersInString("The total cost is 150, the discount is 30.5, and the tax is 20."));
// 200.5

console.log(sumNumbersInString("No numbers here!"));
// 0












Задача 2: Преобразование формата дат
Ситуация:
Представьте, что вы разрабатываете систему, которая должна обрабатывать даты, 
введённые пользователями в различных форматах. 
Например, в американском формате (MM/DD/YYYY) и европейском формате (DD/MM/YYYY). 
Необходимо преобразовывать даты из одного формата в другой для унификации данных перед их дальнейшей обработкой или выводом на интерфейс.

Задача:
Напишите функцию, которая принимает строку с датой в американском формате (MM/DD/YYYY) 
и возвращает эту дату в европейском формате (DD/MM/YYYY).

Шаги реализации:

Разделите строку на части с помощью метода split().
Используйте дистрибуцию для перестановки элементов (месяц и день).
Соберите строку в новый формат с помощью join().
Решение:

function convertDateFormat(dateStr) {
    const [month, day, year] = dateStr.split('/'); // Разделяем строку на части
    return `${day}/${month}/${year}`; // Формируем дату в европейском формате
}

console.log(convertDateFormat("12/25/2025")); // 25/12/2025
console.log(convertDateFormat("07/04/2023")); // 04/07/2023









Итоги
На этом занятии мы изучили ключевые аспекты работы с методами строк 
в JavaScript и освоили их практическое применение:

подробно разобрали, как устроены строки в JavaScript и как с ними работать;
узнали, как применяются методы, такие как toLowerCase(), toUpperCase(), slice(), 
substring(), replace(), split() и другие, для манипуляции строками.
познакомились с ситуациями, 
в которых важно правильно обрабатывать строки для предотвращения сбоев в коде.
поняли, как обработка строк при помощи встроенных js-методов может облегчить разработку в реальных задачах,
 например в преобразовании дат и в поиске чисел в строках.