Урок №18. Асинхронность в JavaScript.

Учебные материалы

Асинхронность — важная концепция в JavaScript. 
Она позволяет выполнить код без ожидания завершения предыдущих операций.
Ранее мы уже познакомились с callback-функциями, 
которые помогают управлять порядком выполнения кода. 
Однако такой подход часто приводит к callback hell — сильной вложенности колбэков, 
делающих код трудным для понимания и поддержки.


Сегодня мы подробнее изучим, что такое асинхронность, 
почему она важна и какие средства для комфортной и эффективной работы с асинхронными задачами предоставляет JavaScript.

Мы:
узнаем, что такое асинхронность в JavaScript и зачем она нужна;
рассмотрим, как код выполняется в браузере с помощью Event Loop;
научимся различать микрозадачи (microtasks) и макрозадачи (macrotasks), 
узнаем их особенности, разберём примеры;
познакомимся со способами реализации асинхронного кода:
использованием функции setTimeout,
работой с Web Worker,
созданием и использованием промисов (Promise);
узнаем, как современные подходы помогают избегать проблем, возникающих при использовании callback-функций (callback hell).



Глоссарий к занятию
Асинхронность (asynchrony)
Подход в программировании, при котором выполнение задачи происходит параллельно с другими операциями, 
без блокировки основного потока исполнения.

Event Loop (цикл событий)
Механизм в JavaScript, управляющий выполнением кода, обработкой событий и очередью задач (микро- и макрозадач).

Микрозадачи (microtasks)
Очередь задач, которая выполняется с момента текущего исполняемого кода и 
до выполнения следующей макрозадачи (например, обработка промисов, queueMicrotask).

Макрозадачи (macrotasks)
Очередь задач, выполнение которой происходит после завершения всех микрозадач 
текущего цикла событий (например, setTimeout, setInterval, события DOM).

Promise (промис)
Объект в JavaScript, представляющий результат асинхронной операции. 
Имеет три состояния: «Ожидание» (pending), «Выполнен успешно» (fulfilled), «Отклонён» (rejected).

Web Worker (веб-воркер)
Специальный скрипт, запускаемый браузером в фоновом режиме. 
Позволяет выполнять вычисления параллельно с основным потоком и не блокирует пользовательский интерфейс.

Callback Hell (колбэк-хел)
Ситуация, когда в коде используется множество вложенных callback-функций, 
что делает код сложным для чтения и поддержки.






Что такое асинхронность и зачем она нужна?
Асинхронность — это подход, при котором задачи могут выполняться параллельно, 
а программа не ждёт завершения одной операции перед началом другой.

Представьте, что вы заказали еду в кафе. При этом вы можете заняться чем-то еще, 
пока официант не принесёт готовое блюдо. Так работает и асинхронный код: 
задачи выполняются в фоновом режиме, а основной код продолжает работать дальше.

В программировании асинхронность особенно важна для веб-приложений. 
Например, при загрузке данных с сервера: если бы JavaScript не был асинхронным, 
браузер «замерзал» бы до получения данных. Асинхронность позволяет нам:

загружать данные с сервера без блокировки интерфейса;
выполнять сложные вычисления без влияния на производительность страницы;
эффективно управлять взаимодействием с пользователем без ожидания длительных операций.

Callback hell и его решение
Ранее мы уже сталкивались с callback-функциями. 
Они позволяют выполнять код после завершения асинхронных операций, 
передавая управление специальным функциям-обработчикам (колбэкам).

Однако такой подход быстро приводит к проблеме, которая получила название callback hell. 
Callback hell возникает, когда появляется множество вложенных друг в друга колбэков, 
что делает код запутанным, трудным для понимания и поддержки.

Например, раньше часто встречался подобный код:

getData(function(a) {
  processData(a, function(b) {
    saveData(b, function(c) {
      updateUI(c, function() {
        console.log('Готово!');
      });
    });
  });
});
Такой подход имеет явные минусы:

Код трудно читать и понимать.

Ошибки сложно отследить.
Поддержка и изменение кода затруднены.
Для решения этой проблемы были созданы промисы (Promises) и другие современные 
подходы (например, async/await), о которых мы поговорим далее.

Как JavaScript исполняет код: Event Loop
JavaScript — однопоточный язык. Это означает, что в конкретный момент он выполняет только одну инструкцию. 
В отличие от других языков, в нём нет встроенной поддержки многопоточности, 
зато есть умный механизм, позволяющий выполнять асинхронные задачи параллельно с основным кодом 
без блокировки страницы. Этот механизм называется Event Loop.

Представьте официанта в кафе. Он принимает заказ (основной код), 
потом передаёт его на кухню (асинхронная операция), а сам продолжает обслуживать других клиентов. 
Когда блюдо будет готово, его принесут (callback вернётся в очередь и выполнится). 
Так же и Event Loop: он не ждёт, пока таймер или загрузка завершатся, 
а занимается другими «делами», пока не придёт время выполнить отложенные задачи.

Как работает Event Loop:

JavaScript начинает выполнять код сверху вниз — это называется стек вызовов. 
Пока в стеке есть задания, они обрабатываются по очереди.
Если в процессе выполнения встречается асинхронная операция (например, setTimeout, 
  fetch или слушатель события click), 
она передаётся браузеру или движку JavaScript и продолжает выполняться в фоновом режиме.
Когда операция завершается, результат не добавляется в стек вызовов напрямую. 
Вместо этого создаётся callback — специальная функция, 
которая ставится в одну из очередей задач: в очередь макро- или микрозадач.
Только когда основной стек опустеет, 
Event Loop берёт задачи из очереди и начинает выполнять их: сначала микрозадачи, затем макрозадачи.
Это создаёт ощущение многозадачности, хотя на самом деле JavaScript остаётся однопоточным.

Пример:

console.log('1');

setTimeout(() => {
  console.log('2');
}, 0);

console.log('3');
Ожидаемый результат:

1

3

2

Почему так?

Сначала выполняется синхронный код: console.log('1') и console.log('3').

setTimeout(...) откладывает вызов функции — она попадает в очередь макрозадач.

После завершения основного стека (1, 3) Event Loop начинает обрабатывать очередь задач. 
И только тогда вызывается отложенный console.log('2').

Чтобы лучше понять, как выбираются и обрабатываются эти задачи, нужно узнать про микрозадачи и макрозадачи.

Микрозадачи и макрозадачи
Чтобы разобраться, как JavaScript обрабатывает асинхронные задачи, 
посмотрим, как работает Event Loop и какие бывают типы задач.

Асинхронные операции в коде (например, таймер или загрузка данных) не выполняются мгновенно: 
JavaScript сначала помещает их в особые очереди, а уже потом по определённым правилам исполняет.

Существует два типа таких задач:

макрозадачи (macrotasks),
микрозадачи (microtasks).
Они отличаются по приоритету, и именно это определяет порядок, в котором они будут выполнены.

Макрозадачи — это «крупные» события, 
которые происходят реже и ставятся в очередь после завершения текущих операций. Примеры:

setTimeout,
setInterval,
события DOM (например, click, submit),
сообщения от Web Workers,
setImmediate (в Node.js).
Макрозадачи выполняются по одной за итерацию.
Даже если вы передаёте setTimeout(..., 0), это всё равно макрозадача, и она не выполнится мгновенно.

Микрозадачи — это более «срочные» действия. Они выполняются сразу после текущего кода и до того, 
как начнёт выполняться хоть одна макрозадача.

Примеры микрозадач:

.then() у промисов,
queueMicrotask,
MutationObserver (в браузере).
Можно представить, что микрозадачи — это маленькие заметки, которые нужно выполнить прямо сейчас, 
а макрозадачи — это запланированные дела, до которых дойдут руки, когда будет свободное время.

JavaScript следует определённому порядку:

Выполняется основной стек вызовов (синхронный код).
Затем — все микрозадачи (в порядке поступления).
Потом — одна макрозадача.
После неё — снова все микрозадачи, если они появились.
И так далее — бесконечный цикл Event Loop.
Рассмотрим пример:

console.log('Start');

setTimeout(() => {
  console.log('Macrotask');
}, 0);

Promise.resolve().then(() => {
  console.log('Microtask');
});

console.log('End');
Результат в консоли:

Start

End

Microtask

Macrotask

Почему так?

Сначала выполнился синхронный код: Start и End.

Затем промис — микрозадача: Microtask.

И только потом сработал таймер — макрозадача: Macrotask.

Понимание этой очередности помогает писать предсказуемый асинхронный код и избегать ошибок, 
связанных с неожиданным порядком выполнения.

setTimeout и задержка выполнения
Представьте, что вы ставите чайник на плиту и заводите таймер на 5 минут. 
Пока он кипит, вы не стоите около него в ожидании, а, например, моете посуду, 
читаете книгу или работаете. Когда прозвенит таймер, вы услышите сигнал и нальёте себе чай.

Функция setTimeout в JavaScript работает так же. 
Она как бы говорит: «Выполни вот эту функцию через указанное время, 
но пока продолжай выполнять остальной код».

Пример:

console.log('До таймера');

setTimeout(() => {
  console.log('Прошло 1000 мс');
}, 1000);

console.log('После таймера');
Ожидаемый вывод:

До таймера

Сразу после вызова setTimeout

После таймера

Почему так?

Функция setTimeout не приостанавливает выполнение кода. 
Вместо этого она сообщает браузеру: «Через указанное количество миллисекунд вызови эту функцию». 
Пока браузер отсчитывает время, JavaScript продолжает выполнять остальные строки кода.

Когда таймер срабатывает, колбэк попадает в очередь макрозадач и будет выполняться только после завершения всех текущих микрозадач и очистки стека вызовов.

Простыми словами, этот код не будет ждать завершения setTimeout. 
Сначала выведется "До таймера", затем сразу — "После таймера" и только через секунду — "Прошло 1000 мс".

Это простой, но мощный инструмент для отложенного выполнения, 
особенно полезный в анимациях, загрузках и имитации пауз в логике.

Промисы (Promise) как договор в JavaScript
Представьте, что вы заказали товар в интернет-магазине. 
Вы не знаете, когда он приедет, но вам обещали: «Или доставим, или сообщим о проблеме». 
Пока заказ едет, вы занимаетесь своими делами. 
В какой-то момент вам приходит уведомление: доставка прошла успешно или произошла ошибка.

Так работает Promise в JavaScript — это объект, 
который представляет результат асинхронной операции. Он может быть в одном из трёх состояний:

pending (ожидание) — начальное состояние, операция ещё не завершена;
fulfilled (успешно выполнено) — операция завершена успешно;
rejected (с ошибкой) — операция завершилась с ошибкой.
const myPromise = new Promise((resolve, reject) => {
  const success = true;

  if (success) {
    resolve('Успех! Всё получилось.');
  } else {
    reject('Ошибка! Что-то пошло не так.');
  }
});










Типичные ошибки
Тип ошибки 1: Путаница в порядке выполнения
Описание: многие ожидают, что setTimeout(fn, 0) выполнится сразу после текущего кода, 
не учитывая приоритет микрозадач.

Пример ошибки (код):

setTimeout(() => console.log("timeout"), 0);
Promise.resolve().then(() => console.log("promise"));
console.log("sync");
Объяснение: сначала выполняется синхронный код (console.log("sync")), 
затем – микрозадачи (промис), и только после этого – макрозадачи (setTimeout).

Результат:

sync
promise
timeout
Решение: микрозадачи (например, промисы) всегда выполняются до макрозадач (например, setTimeout). 
Не рассчитывайте, что setTimeout(…, 0) выполнится мгновенно после синхронного кода.





Тип ошибки 2: Получение результата промиса до его завершения
Описание: попытка получить результат асинхронной операции из промиса «снаружи» без использования .then() или await.

Пример ошибки (код):

let data = fetchData(); // Возвращает промис
console.log(data);      // Promise {<pending>}
Объяснение: в переменной data содержится промис, а не результат. Операция асинхронна, результата ещё нет.

Результат:

Promise {<pending>}
Решение: обрабатывайте результат асинхронно.

fetchData().then(result => {
  console.log(result);
});
или через async/await:

async function run() {
  const result = await fetchData();
  console.log(result);
}
run();
Объяснение:

В переменной data — промис, а не результат. 
Операция асинхронная, результат ещё не получен. Использование data напрямую выводит Promise {<pending>}.












Тип ошибки 3: Отсутствие обработки ошибок в промисах
Описание: без обработки в промисах через .catch() можно пропустить важные ошибки.

Пример ошибки (код):

loadData().then(() => {
  throw new Error("Ошибка при загрузке");
});
Объяснение: ошибка, возникшая внутри .then(), не будет обработана, если не добавить .catch().

Результат: в консоли появится необработанная ошибка, программа может аварийно завершиться.

Решение: обязательно добавляйте .catch() для обработки ошибок.

loadData()
.then(() => {
  throw new Error("Ошибка при загрузке");
})
.catch(error => {
  console.error("Произошла ошибка:", error);
});
Объяснение:

Ошибка в .then() не будет обработана без .catch(). 
Это приводит к необработанной ошибке в консоли, что может вызвать аварийное завершение программы.







Тип ошибки 4: Непонимание архитектуры Event Loop
Описание: ожидание, что JavaScript «приостанавливается» во время асинхронных операций (например, setTimeout).

Пример ошибки (код):

console.log("Начало");

setTimeout(() => {
  console.log("Таймер");
}, 0);

console.log("Конец");
Объяснение: setTimeout не блокирует выполнение кода, он просто ставит задачу в очередь. 
Сначала выполнится весь синхронный код, а таймер — только после этого.

Результат:

Начало
Конец
Таймер
Решение: асинхронные операции в JavaScript никогда не «замораживают» выполнение, 
основной поток всегда идёт до конца, до выполнения отложенных задач.

Эти ошибки особенно часто встречаются у новичков при изучении асинхронного JavaScript. 
Главное — всегда помнить, что асинхронность не пауза, а способ организации работы со временем и событиями.





Практикум
Задача 1: Порядок выполнения кода
Ситуация: важно понимать, 
в каком порядке JavaScript выполняет код, особенно когда в игру вступают асинхронные операции.

Задача: предсказать порядок вывода в консоли.

Код:

console.log("A");

setTimeout(() => {
  console.log("B");
}, 0);

Promise.resolve().then(() => {
  console.log("C");
});

console.log("D");
Вопрос: каким будет порядок вывода?

Ожидаемый ответ:

A
D
C
B







Задача 2: Задержка с setTimeout
Ситуация: вы хотите показать сообщение пользователю через 2 секунды после загрузки страницы.

Задача: использовать setTimeout, чтобы отложить вывод текста в консоль.

Решение:

console.log("Загрузка...");

setTimeout(() => {
  console.log("Прошло 2 секунды!");
}, 2000);








Задача 3: Работа с промисом
Ситуация: вы хотите имитировать получение данных с сервера. Иногда данные приходят, иногда — нет.

Задача: создайте промис, который случайным образом завершится успешно или с ошибкой через 1 секунду.

Решение:

const getData = new Promise((resolve, reject) => {
  const success = Math.random() > 0.5;

  setTimeout(() => {
    if (success) {
      resolve("Данные успешно получены!");
    } else {
      reject("Ошибка при получении данных");
    }
  }, 1000);
});

getData
  .then(result => console.log(result))
  .catch(error => console.error(error));




Эти задания помогут вам разобраться в том, как работает асинхронный код в JavaScript: 
в каком порядке выполняются операции, как взаимодействуют таймеры и промисы и почему важна архитектура Event Loop.

Итоги:
На этом занятии мы:

разобрались, что такое асинхронность и зачем она нужна в JavaScript;
изучили, как работает Event Loop — механизм, который управляет порядком выполнения кода;
познакомились микро- и макрозадачами и узнали, чем они отличаются;
научились использовать setTimeout для отложенного выполнения кода;
сделали первые шаги по работе с Promise — современным способом управления асинхронными операциями.
Теперь вы знаете, что JavaScript не «дожидается» завершения операций и продолжает работать дальше, 
и умеете правильно планировать действия, которые должны быть совершены позже. 
Это знание поможет вам создавать быстрые и отзывчивые веб-приложения.