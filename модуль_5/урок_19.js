Урок №19. Promises в JavaScript.

Учебные материалы

На прошлом занятии мы познакомились с асинхронностью в JavaScript, узнали, 
как работает Event Loop, и сделали первые шаги с промисами, в работу с которыми мы сегодня углубимся. 
Промисы — это мощный инструмент для управления асинхронными операциями. 
Они помогают избежать запутанного кода (callback hell) и делают асинхронный код более читаемым и предсказуемым.

Представьте, что вы заказываете пиццу: вы не стоите у печи, ожидая, пока она приготовится, 
а занимаетесь другими делами. 
Повар обещает сообщить о готовности пиццы (успех) или о том, что что-то пошло не так (ошибка). 
Промисы работают аналогично: они представляют результат будущей операции.

Сегодня мы:

разберём, что такое промисы и зачем они нужны;
научимся создавать промисы и управлять их состояниями;
освоим чейнинг промисов для последовательных операций;
изучим обработку ошибок в промисах;
решим «хитрую» задачу с setTimeout и промисами, чтобы понять их взаимодействие.






Глоссарий к девятнадцатому занятию
Promise (промис)

Promise — объект, представляющий результат асинхронной операции. 
Имеет три состояния: pending (ожидание), fulfilled (успешно выполнено), rejected (ошибка).

Чейнинг (chaining)

Чейнинг — последовательное соединение методов .then(), 
позволяющее выполнять операции одну за другой, передавая результаты.

Обработка ошибок

Обработка ошибок — использование .catch() или второго 
аргумента .then() для перехвата и обработки ошибок в промисах.

Resolve (резолв)

Resolve — функция в промисе, вызываемая для успешного завершения с результатом.

Reject (реджект)

Reject — функция в промисе, вызываемая для завершения с ошибкой.


Теоретический блок
Что такое промисы и зачем они нужны?
Промис (Promise) — это объект в JavaScript, который представляет результат асинхронной операции, 
которая ещё не завершена, но завершится в будущем. 
Промис выступает как «договор»: он обещает вернуть либо успешный результат, либо ошибку, 
позволяя программе продолжать выполнение других задач и не дожидаясь окончания операции.

Представьте, что вы заказываете книгу в онлайн-магазине. 
Вы не стоите у двери, ожидая курьера, а продолжаете заниматься своими делами. 
Магазин обещает доставить книгу (успех) или сообщить, если её нет в наличии (ошибка). 
Промисы работают аналогично: они позволяют JavaScript выполнять асинхронные операции (например, 
    загрузку данных с сервера или ожидание таймера) без блокировки основного кода.

Зачем нужны промисы?
Управление асинхронностью. Позволяют обрабатывать задачи, которые требуют времени, 
такие как запросы к API, чтение файлов или анимации, без «замораживания» интерфейса.
Избежание callback hell. Промисы предлагают линейную структуру, упрощающую логику.
Последовательность операций. С помощью чейнинга можно гарантировать нужный порядок выполнения.
Упрощённая обработка ошибок. .catch() позволяет централизованно обрабатывать ошибки.
Пример callback hell:
getUser(function(user) {
  if (user) {
    getPosts(user.id, function(posts) {
      if (posts) {
        savePosts(posts, function(result) {
          if (result) {
            console.log('Готово');
          } else {
            console.error('Ошибка сохранения');
          }
        });
      } else {
        console.error('Ошибка загрузки постов');
      }
    });
  } else {
    console.error('Ошибка загрузки пользователя');
  }
});
Тот же код с промисами:

getUser()
.then(user => getPosts(user.id))
.then(posts => savePosts(posts))
.then(() => console.log('Готово'))
.catch(error => console.error(`Ошибка: ${error}`));

Промисы делают код линейным, с единой точкой обработки ошибок.

Когда использовать промисы:
Загрузка данных с сервера (например, через fetch).
Ожидание пользовательских действий с задержкой (например, анимации).
Выполнение последовательных операций, зависящих друг от друга.
Создание промисов
Промис создаётся с помощью конструктора Promise, 
который принимает функцию-исполнитель с двумя параметрами: resolve (для успешного завершения) 
и reject (для ошибки). Функция выполняется сразу при создании промиса, а её результат определяет его состояние.

Состояния промиса:

pending (ожидание),
fulfilled (выполнено),
rejected (ошибка).
Пример (имитация загрузки данных):
const loadData = new Promise((resolve, reject) => {
  setTimeout(() => {
    const success = Math.random() > 0.5;
    if (success) {
      resolve({ data: 'Успешно загружено' });
    } else {
      reject('Ошибка: сервер не отвечает');
    }
  }, 1000);
});

loadData
  .then(result => console.log(result.data))
  .catch(error => console.log(error));

  Аналогия:

Промис — как заказ в ресторане. Вы делаете заказ (создаёте промис), 
повар готовит еду (асинхронная операция). Когда блюдо готово, 
официант приносит его (resolve) или сообщает, что ингредиентов нет (reject).

Пример (проверка возраста):

function checkAge(age) {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      if (age >= 18) {
        resolve('Доступ разрешён');
      } else {
        reject('Доступ запрещён: возраст меньше 18');
      }
    }, 500);
  });
}

checkAge(20)
  .then(result => console.log(result))
  .catch(error => console.log(error));

checkAge(16)
  .then(result => console.log(result))
  .catch(error => console.log(error));

  Чейнинг промисов

Чейнинг (chaining) — это возможность соединять методы .then() в цепочку. Каждый .then() получает результат предыдущего.

Пример (последовательная обработка данных):

new Promise((resolve) => {
  setTimeout(() => resolve(10), 1000);
})
  .then(num => {
    console.log(`Исходное число: ${num}`);
    return num * 2;
  })
  .then(num => {
    console.log(`Удвоенное: ${num}`);
    return new Promise(resolve => {
      setTimeout(() => resolve(num + 5), 1000);
    });
  })
  .then(num => {
    console.log(`Итог: ${num}`);
  })
  .catch(error => console.log(`Ошибка: ${error}`));
Вывод:

Исходное число: 10
Удвоенное: 20
Итог: 25
Аналогия:

Чейнинг — как конвейер на заводе. На каждом этапе (.then()) деталь (данные) обрабатывается 
и передаётся дальше. Если на этапе возникает ошибка, она попадает в .catch().

Пример (загрузка профиля и друзей):

function getProfile() {
  return new Promise(resolve => {
    setTimeout(() => resolve({ userId: 1, name: 'Anna' }), 1000);
  });
}

function getFriends(userId) {
  return new Promise(resolve => {
    setTimeout(() => resolve(['Bob', 'Clara']), 1000);
  });
}

getProfile()
  .then(profile => {
    console.log(`Пользователь: ${profile.name}`);
    return getFriends(profile.userId);
  })
  .then(friends => {
    console.log(`Друзья: ${friends.join(', ')}`);
  })
  .catch(error => console.log(`Ошибка: ${error}`));
Вывод:

Пользователь: Anna
Друзья: Bob, Clara


Обработка ошибок

Ошибки в промисах могут возникать при вызове reject или при выбросе исключений (throw) 
внутри .then(). Промисы предоставляют два способа обработки ошибок:

метод .catch() в конце цепочки для централизованной обработки;

второй аргумент .then(onFulfilled, onRejected) для локальной обработки.

Пример с .catch()

new Promise((resolve, reject) => {
  setTimeout(() => reject('Сервер недоступен'), 1000);
})
  .then(result => console.log(result))
  .catch(error => console.log(`Ошибка: ${error}`));
Пример с исключением:

new Promise(resolve => {
  setTimeout(() => resolve('Данные'), 1000);
})
  .then(data => {
    throw new Error('Некорректные данные');
  })
  .then(result => console.log(result))
  .catch(error => console.log(`Ошибка: ${error.message}`));

  Пример с локальной обработкой:

new Promise(resolve => {
  setTimeout(() => resolve(0), 1000);
})
  .then(
    num => num + 1,
    error => console.log(`Ошибка: ${error}`)
  )
  .then(result => console.log(`Результат: ${result}`));

  Механизм приостановки и консистентность

Промисы не приостанавливают выполнение JavaScript, так как язык однопоточный. 
Вместо этого они используют очередь микрозадач, управляемую Event Loop. 
Когда промис завершается (resolve или reject), его обработчик (.then() или .catch()) 
добавляется в очередь микрозадач и выполняется после текущего синхронного кода.

Пример:

console.log('Начало');

new Promise(resolve => {
  setTimeout(() => resolve('Готово'), 0);
})
  .then(result => console.log(result));

console.log('Конец');
Вывод:

Начало
Конец
Готово
Промисы и callback hell

Callback hell возникает, когда асинхронные операции требуют множества вложенных колбэков. Это делает код:

трудным для чтения из-за «пирамиды» вложенности,

сложным для отладки,

тяжёлым для поддержки.

Промисы решают эту проблему, заменяя вложенные колбэки линейной цепочкой .then().

Преимущества

Читаемость: код выглядит как последовательность шагов.

Обработка ошибок: единый .catch() перехватывает ошибки на любом этапе.

Гибкость: чейнинг позволяет добавлять новые шаги.

Callback-версия:

function loadUser(callback) {
  setTimeout(() => callback({ id: 1 }), 1000);
}

function loadOrders(id, callback) {
  setTimeout(() => callback(['order1', 'order2']), 1000);
}

loadUser(user => {
  loadOrders(user.id, orders => {
    console.log(`Заказы: ${orders}`);
  });
});
Промис-версия:

function loadUser() {
  return new Promise(resolve => setTimeout(() => resolve({ id: 1 }), 1000));
}

function loadOrders(id) {
  return new Promise(resolve => setTimeout(() => resolve(['order1', 'order2']), 1000));
}

loadUser()
  .then(user => loadOrders(user.id))
  .then(orders => console.log(`Заказы: ${orders}`))
  .catch(error => console.log(`Ошибка: ${error}`));
Промисы делают код короче, понятнее и легче для масштабирования.









Типичные ошибки
Ошибка 1: Игнорирование возврата в .then()
Описание:

Если в .then() ничего не вернуть, следующий .then() получит undefined.

Пример ошибки:

Promise.resolve(1)
.then(num => {
  console.log(num); // 1
})
.then(result => console.log(result)); // undefined
Решение:

Promise.resolve(1)
.then(num => {
  console.log(num); // 1
  return num;
})
.then(result => console.log(result)); // 1
Объяснение:

Если в .then() не вернуть значение, следующий .then() получит undefined. 
Это происходит, потому что цепочка промисов передаёт результат только при явном возврате.





Ошибка 2: Неправильная обработка ошибок
Описание:

Пропуск .catch() может привести к необработанным ошибкам.

Пример ошибки:

Promise.reject('Ошибка')
.then(() => console.log('Успех'));
Решение:

Promise.reject('Ошибка')
.then(() => console.log('Успех'))
.catch(error => console.log(error)); // Ошибка
Объяснение:

Без .catch() ошибка в промисе остаётся необработанной, что может привести к сбою программы. 
Пропуск обработки ошибок делает код ненадёжным.





Ошибка 3: Путаница с setTimeout и промисами
Описание:

Ожидание, что setTimeout в промисе выполнится раньше микрозадач.

Пример:

Promise.resolve().then(() => console.log('Промис'));
setTimeout(() => console.log('Таймер'), 0);
Вывод:

Промис
Таймер
Решение:

Помните, что .then() — это микрозадача, а setTimeout — макрозадача. Микрозадачи выполняются раньше.

Объяснение:

setTimeout даже с нулевой задержкой создаёт макрозадачу, которая выполняется после микрозадач, 
таких как .then(). Поэтому промисы в .then() всегда опережают setTimeout.









Практикум
Задача 1: Проверка доступа с промисом (базовый уровень)
Ситуация:

Вы разрабатываете систему авторизации для сайта. Пользователь вводит код доступа, 
и система проверяет его с небольшой задержкой, имитируя запрос к серверу.

Задача:

Создайте функцию checkAccess(code), которая возвращает промис.
Промис должен через 1 секунду проверить, равен ли код «1234». 
Если да, промис резолвится с сообщением «Доступ разрешён», иначе отклоняется с сообщением «Неверный код».
Используйте .then() и .catch() для вывода результата в консоль.
Решение:

function checkAccess(code) {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      if (code === '1234') {
        resolve('Доступ разрешён');
      } else {
        reject('Неверный код');
      }
    }, 1000);
  });
}

checkAccess('1234')
  .then(result => console.log(result))
  .catch(error => console.log(error));





  Задача 2: «Хитрая» задача с чейнингом и setTimeout (продвинутый уровень)
Ситуация:

На собеседовании вам дали задачу проанализировать код с промисами и setTimeout, 
чтобы предсказать порядок вывода. Это проверяет понимание асинхронности и чейнинга.

Задача:

Напишите код, комбинирующий промисы и setTimeout, и предскажите порядок вывода в консоль.

Код:

console.log('A');
new Promise(resolve => {
  console.log('B');
  resolve('C');
}).then(result => console.log(result));
setTimeout(() => console.log('D'), 0);
new Promise(resolve => {
  setTimeout(() => resolve('E'), 0);
}).then(result => console.log(result));
console.log('F');
Ожидаемый вывод:

A
B
F
C
E
D
Объяснение:

Синхронный код выполняется сразу: A, B (внутри первого промиса), F.
Микрозадачи (.then()) выполняются после синхронного кода: C (первый промис резолвится сразу).
Макрозадачи (setTimeout) выполняются последними: E (второй промис резолвится через setTimeout), затем D (простой setTimeout).







Итоги:
Сегодня мы:

разобрали, что такое промисы и как они помогают управлять асинхронными операциями в JavaScript;
научились создавать промисы с использованием resolve и reject для обработки успешных и ошибочных результатов;
освоили чейнинг промисов через .then() для выполнения последовательных операций;
изучили обработку ошибок в промисах с помощью .catch() и второго аргумента .then();
узнали, как промисы решают проблему callback hell, делая код более читаемым и структурированным.
Теперь вы можете использовать промисы для управления асинхронными задачами, 
такими как загрузка данных или выполнение операций с задержкой, и писать более понятный и надёжный код.