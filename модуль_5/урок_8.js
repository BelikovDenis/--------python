Урок №8. Работа с массивами, свойство length.

Учебные материалы
Сегодня мы поговорим о массивах в JavaScript.

Из курса Python мы помним списки – в JavaScript их аналогом служат массивы. 
Массивы в JavaScript – это отдельный тип объектов, 
представляющий собой упорядоченную коллекцию элементов.

Но почему такие структуры важны в любом языке программирования?

Почти в каждой программе приходится работать с наборами однотипных данных: 
списком покупок, оценками, комментариями пользователей и так далее. 
Без специальных структур вроде массивов пришлось бы создавать для каждого значения отдельную переменную: 
item1, item2, item3… и так до бесконечности. Этот способ быстро становится неудобным и неуправляемым.

Массивы решают эту проблему. 
Они позволяют хранить множество значений внутри одной переменной, 
обращаться к каждому элементу по номеру, легко перебирать и изменять данные.

Сегодня мы:

узнаем, что такое массив в JavaScript и зачем он нужен;
научимся создавать массивы и обращаться к элементам по индексу;
разберёмся, как работает свойство length и что оно показывает;
посмотрим, как массивы устроены в памяти.






Глоссарий к восьмому занятию
Array (Массив)

Array — это структура данных, которая позволяет хранить несколько значений в одной переменной. 
Значения в массиве упорядочены и доступны по индексам, начиная с нуля.

Index (Индекс)

Index — это числовое значение, которое указывает положение элемента в массиве. 
Индексация в JavaScript начинается с 0, то есть первый элемент массива имеет индекс 0.

Length (Длина)

Length — это свойство, которое возвращает количество элементов в массиве. 
Используется для определения длины массива или проверки границ при его обработке.

Iteration (Итерация)

Iteration — это процесс последовательного перебора всех элементов массива для выполнения операций с ними. 
Для итерации в JavaScript часто используются циклы, например for или for...of.

Out of bounds (Выход за границы)

Out of bounds — это ошибка, 
которая происходит при попытке обратиться к элементу массива по индексу, 
который превышает его длину или меньше 0.

Element (Элемент)

Element — это отдельное значение, хранящееся в массиве. 
Элементы могут быть любого типа данных (например, числа, строки или объекты).

Array traversal (Перебор массива)

Array traversal — это процесс обработки каждого элемента массива (например, 
    для их отображения, фильтрации или изменения).






Массивы — это одна из самых важных структур данных в программировании, 
так как они позволяют хранить коллекции данных и эффективно управлять ими.

1. Что такое массив?
Массив — это специальный объект в JavaScript, 
который позволяет хранить несколько значений в одной переменной. 
Такие значения называются элементами массива, 
а их местоположение в массиве определяется порядковым номером, 
который называется индексом. Индексы всегда начинаются с 0.

Простой пример:

let fruits = ["apple", "banana", "cherry"];
console.log(fruits[0]); // Выведет: apple
console.log(fruits[2]); // Выведет: cherry

Два ключевых свойства массивов, которые помогают быстро и эффективно работать с данными:

Упорядоченность. Элементы массива хранятся в определённом порядке. 
Например, первый элемент имеет индекс 0, второй — 1 и так далее.
Быстрый доступ. Массив позволяет мгновенно обратиться к любому элементу по индексу. 
Это свойство делает массивы особенно полезными для задач, где нужен быстрый доступ к данным.

Гибкость массива

Массивы в JavaScript, в отличие от других языков программирования, 
способны хранить значения разных типов данных одновременно. Например:

let mixedArray = [42, "hello", true, [1, 2, 3]];
console.log(mixedArray[0]);    // Выведет: 42 (число)
console.log(mixedArray[1]);    // Выведет: "hello" (строка)
console.log(mixedArray[2]);    // Выведет: true (логическое значение)
console.log(mixedArray[3][1]); // Выведет: 2 (вложенный массив)


Почему массивы удобны?
Хранение большого количества данных. 
Массивы позволяют удобно объединять множество элементов в единую структуру. 
Например, вместо того чтобы создавать множество отдельных переменных для хранения списка фруктов, можно использовать массив:

let fruit1 = "apple";
let fruit2 = "banana";
let fruit3 = "cherry";
// Вместо этого:
let fruits = ["apple", "banana", "cherry"];
Итерация по элементам. Массивы поддерживают удобные методы и циклы, которые помогают обрабатывать данные. Например:

let fruits = ["apple", "banana", "cherry"];
for (let i = 0; i < fruits.length; i++) {
    console.log(fruits[i]);
}
// Выведет: apple, banana, cherry
Поддержка динамических операций. Массивы в JavaScript динамические, 
то есть их размер можно изменять во время выполнения программы. 
Элементы можно добавлять или удалять. Пример:

let fruits = ["apple", "banana"];
fruits.push("cherry"); // Добавляет новый элемент в конец
console.log(fruits);   // Выведет: ["apple", "banana", "cherry"]
fruits.pop();          // Удаляет последний элемент
console.log(fruits);   // Выведет: ["apple", "banana"]

3. Как устроены массивы и как они хранятся в памяти?
Массивы в JavaScript устроены иначе, чем в языках вроде C или Java. 
В классических языках программирования массивы — это структуры фиксированного размера, 
элементы которых хранятся в памяти в последовательных ячейках. 
В JavaScript массивы — это динамические структуры данных, 
реализованные как специализированные объекты. 
Эта гибкость делает их мощным инструментом, но имеет свои особенности.

3.1. Элементы массива и память
В отличие от языков со строго типизированными массивами, 
где все элементы — одного типа (например, только числа или строки), массивы в JavaScript:

Могут содержать элементы любых типов данных: строки, числа, объекты, другие массивы и т. д.
Хранятся в памяти как список ссылок на элементы, а не как непрерывный блок данных.
Что это значит?

Элементы массива в JavaScript не обязаны располагаться в соседних ячейках памяти. 
Вместо этого массив хранит ссылки (адреса) на фактические значения.

Это позволяет хранить данные разных типов в одном массиве
let mixedArray = [42, "hello", { key: "value" }, [1, 2, 3]];
console.log(mixedArray[1]); // "hello"
console.log(mixedArray[2].key); // "value"

3.2. Массивы как объекты
Внутренне массивы в JavaScript представляют собой особый тип данных — объекты. 
Их мы подробно рассмотрим на следующих занятиях. 
Пока же для нас важно то, что массивы, как и другие объекты в JavaScript, обладают:

свойствами (например, length),
методами (например, push, pop, map).
Ключевая разница с классическими массивами:

У массива в JavaScript индексы — это ключи объекта, которые автоматически преобразуются в строки. Например
let arr = ["a", "b", "c"];
console.log(arr["1"]); // "b" (индекс 1 представлен и действительно записан в памяти как строка)
Но! Движки JavaScript оптимизируют массивы, чтобы они работали быстрее, 
если их ключи — это последовательные числа. Например:

let arr = [];
arr[0] = "a";
arr[1] = "b";
arr[2] = "c";
// Движок хранит такой массив в виде списка с последовательными индексами.
Нестандартные индексы как свойства объекта
Если использовать отрицательные или нецелые индексы (например, arr[-1] или arr[1.5]), 
это не вызовет ошибку, но такие значения будут интерпретироваться как обычные свойства объекта, 
а не как элементы массива. То же самое касается строковых ключей:

let arr = ["a", "b", "c"];
arr[-1] = "last";
arr["hello"] = "world";

console.log(arr.length);    // всё ещё 3
console.log(arr[-1]);       // "last", но это не элемент массива
console.log(arr["hello"]);  // "world"

3.3. Динамическая структура и пропуски индексов
Массивы в JavaScript не имеют фиксированного размера. Вы можете:

добавлять элементы, используя методы (push, unshift) или явно задавая индекс;
создавать массивы с пропусками между индексами
let arr = [];
arr[3] = "hello"; // Массив с единственным элементом на 4-й позиции
console.log(arr); // [undefined, undefined, undefined, "hello"]
console.log(arr.length); // 4

Что происходит в памяти?

При таком использовании массив создаётся с «дырами», 
а на пропущенные индексы устанавливаются значения undefined.
 Это позволяет гибко управлять массивами, но может снизить производительность.

3.4. Оптимизация массивов движком JavaScript
Чтобы повысить производительность работы с массивами, 
JavaScript-движки (например, V8 в Chrome) используют различные внутренние механизмы оптимизации.

Основные принципы оптимизации

Хранение в виде плотных массивов. 
Если массив создаётся с последовательными индексами и без пропусков, 
движок хранит его в оптимизированной форме (как массив фиксированного размера).

let denseArray = [1, 2, 3, 4]; // Плотный массив
Доступ к элементам такого массива быстрее, так как элементы расположены в памяти последовательно.
Переход к разреженному массиву. Если массив содержит пропущенные индексы, 
движок начинает обрабатывать его как объект, снижая производительность.

let sparseArray = [];
sparseArray[1000] = "data"; // Разреженный массив
Разреженные массивы хранят индексы и значения в хеш-таблицах, а не как плотный список.
Изменение типа данных. Если массив изначально содержал элементы одного типа (например, числа), 
а затем к нему добавился элемент другого типа (например, строка), оптимизация может нарушиться:

let arr = [1, 2, 3];
arr[3] = "hello"; // Движок больше не может использовать оптимизацию для чисел

3.5. Производительность при доступе по индексу
Даже несмотря на гибкость, 
доступ к элементам массива по индексу остаётся быстрым благодаря внутренним механизмам:

Если массив плотный, индекс используется как прямое смещение в памяти.
Если массив разреженный, движок выполняет поиск в хеш-таблице.
Совет:
Чтобы обеспечить максимальную производительность:

Используйте плотные массивы с последовательными индексами.
Старайтесь не смешивать типы данных в одном массиве.
Не создавайте массивы с большими пропусками между индексами.

3.6. Пример: плотный vs разреженный массив
// Плотный массив
let denseArray = [1, 2, 3, 4];
console.log(denseArray[2]); // Быстрый доступ

// Разреженный массив
let sparseArray = [];
sparseArray[1000] = "hello";
console.log(sparseArray[1000]); // Медленнее из-за поиска

4. Особенности работы с массивами в JavaScript
Работа с массивами встречается в программировании постоянно, особенно когда нужно хранить, 
обрабатывать или анализировать большие объемы данных. 
Чтобы сделать эту работу более эффективной, 
JavaScript предлагает нам дополнительные инструменты для работы с массивами. Давайте рассмотрим их чуть подробнее.

Длина массива и пустые слоты
Когда мы создаём массив, нам часто необходимо проверить, 
какова его длина или является ли он пустым. 
Свойство length помогает нам узнать количество элементов. 
Однако нужно помнить, что это свойство учитывает даже пустые ячейки массива. 
Например, если вы установите значение в пятой позиции массива, 
JavaScript автоматически добавит пустые слоты:

let arr = [];
arr[5] = "hello";
console.log(arr.length); // 6
Такая ситуация может возникнуть, 
если данные добавляются в массив на основе внешних условий или при работе 
с разреженными структурами данных. Например, при обработке данных с пропущенными значениями.

Перебор элементов массива
Перебор — одна из самых частых задач при работе с массивами. 
JavaScript предлагает несколько подходов, которые позволяют выбрать самый удобный инструмент для конкретной задачи.

Классический цикл for даёт полный контроль над индексами и шагом перебора, 
его выгодно использовать, когда сам индекс массива нам требуется внутри блока кода цикла:

let numbers = [10, 20, 30];
for (let i = 0; i < numbers.length; i++) {
    console.log(`Элемент ${i}: ${numbers[i]}`);
}
Современный for...of делает код более читаемым и может быть использован, когда индексы не нужны:
for (let value of numbers) {
    console.log(value); // 10, 20, 30
}
Метод forEach() идеально подходит, если требуется просто применить функцию к каждому элементу массива. 
Он перебирает элементы массива и выполняет заданную функцию для каждого из них. 
Его главное преимущество — лаконичность и читаемость кода, особенно, 
если вам не нужны индексы или возвращаемое значение.
let numbers = [10, 20, 30];
numbers.forEach((value) => {
    console.log(`Значение: ${value}`);
});
// Вывод:
// Значение: 10
// Значение: 20
// Значение: 30
forEach() часто используется, 
когда нужно выполнить действие для каждого элемента массива, 
но при этом не требуется создавать новый массив или возвращать результат. 
Например, это может быть вывод элементов на страницу, 
запись данных в лог или вызов какой-либо функции для каждого элемента.

Основные методы массивов
Методы массивов позволяют быстро и эффективно обрабатывать данные. 
Даже простые задачи, такие как фильтрация или преобразование, становятся удобными.

Метод filter() может отфильтровать массив по заданному условию. 
Например, чтобы выбрать только положительные числа:

let numbers = [-3, 5, 8, -1];
let positive = numbers.filter(x => x > 0);
console.log(positive); // [5, 8]
map() создаёт новый массив, трансформируя каждый элемент. 
Это удобно, если нужно, например, удвоить все числа:
let doubled = numbers.map(x => x * 2);
console.log(doubled); // [-6, 10, 16, -2]
reduce() используется, чтобы собрать массив в одно значение, например найти сумму:
let sum = numbers.reduce((acc, x) => acc + x, 0);
console.log(sum); // 9
Эти методы полезны при работе с большими данными: от вычислений статистики до подготовки данных для графиков.

Многомерные массивы
Если нужно работать с таблицами или матрицами, многомерные массивы — лучший инструмент. 
Например, вот так можно сохранить игровое поле или данные таблицы:

let matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
];
console.log(matrix[1][2]); // 6
Вложенные циклы позволяют легко обрабатывать такие структуры:

for (let row of matrix) {
    for (let value of row) {
        console.log(value);
    }
}
Этот подход пригодится, например, для обработки таблиц, шахматных досок или рендеринга интерфейсов.








Типичные ошибки
Тип ошибки 1: Ошибка обращения к несуществующему элементу массива
Ошибка:
При обращении к элементу массива по индексу, который не существует, 
результатом будет undefined. Это может вызвать непредвиденные ошибки, 
если результат используется в последующих вычислениях.

Пример ошибки:

let arr = [1, 2, 3];
console.log(arr[5]);  // undefined
console.log(arr[5] + 1);  // NaN, так как undefined + 1 невозможно
Почему это происходит:
Массивы в JavaScript допускают обращение к любому индексу, 
но если элемент с таким индексом отсутствует, результат будет undefined.

Решение:

Всегда проверяйте существование элемента перед его использованием:

if (arr[5] !== undefined) {
    console.log(arr[5] + 1);
} else {
    console.log("Элемент не существует");
}
Почему это важно: такие ошибки могут стать причиной некорректных вычислений или поломки программы, 
так как результат используется в других логических конструкциях.





Тип ошибки 2: Добавление нечислового индекса в массив
Ошибка:
Если добавить в массив ключ в виде строки, он будет записан как свойство объекта, 
а не как элемент массива. Это может привести к путанице.

Пример ошибки:

let arr = [1, 2, 3];
arr["key"] = "value";

console.log(arr.length);  // 3, строковые ключи не учитываются
console.log(arr["key"]);  // "value"
Почему это происходит: массивы в JavaScript являются объектами, 
и строковые индексы добавляются как свойства объекта, а не как элементы массива.

Решение:
Используйте объекты для хранения данных в виде ключа-значения:

let obj = { key: "value" };
console.log(obj.key);  // "value"
Почему это важно: если забыть о разнице между индексами массива и свойствами объекта, 
можно получить некорректные результаты при подсчёте длины массива или переборе его элементов.






Тип ошибки 3: Ошибка при копировании массива
Ошибка:
При попытке скопировать массив через присваивание происходит передача ссылки, 
а не создание нового массива. Изменение одного массива повлияет на другой.

Пример ошибки:

let arr1 = [1, 2, 3];
let arr2 = arr1;  // arr2 и arr1 указывают на один и тот же массив

arr2.push(4);
console.log(arr1);  // [1, 2, 3, 4]
Почему это происходит:
Массивы в JavaScript являются объектами и передаются по ссылке, а не по значению.

Решение:
Используйте методы для создания копии массива.

Через slice:

let arr1 = [1, 2, 3];
let arr2 = arr1.slice();
arr2.push(4);
console.log(arr1);  // [1, 2, 3]
console.log(arr2);  // [1, 2, 3, 4]

2. Через оператор распространения (...):

let arr1 = [1, 2, 3];
let arr2 = [...arr1];
arr2.push(4);
console.log(arr1);  // [1, 2, 3]
console.log(arr2);  // [1, 2, 3, 4]

Почему это важно: если не понимать эту особенность, можно случайно изменить массивы, что приведёт к багам.

Тип ошибки 4: Неправильный перебор массива
Ошибка:
Использование цикла for...in для перебора массива может привести к неожиданным результатам, 
так как он перебирает все свойства объекта, включая дополнительные ключи.

Пример ошибки:

let arr = [1, 2, 3];
arr.key = "value";

for (let i in arr) {
    console.log(i);  // Выведет: 0, 1, 2, key
}
Почему это происходит:
Цикл for...in предназначен для перебора всех ключей объекта, 
включая строковые свойства, а не только индексы массива.

Решение:
Для перебора массива используйте for, for...of или методы массива (forEach, map):

let arr = [1, 2, 3];

// Обычный цикл
for (let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
}

// Цикл for...of
for (let value of arr) {
    console.log(value);
}

// Метод forEach
arr.forEach(value => console.log(value));

Почему это важно: неправильный перебор может включить в цикл лишние свойства массива, что вызовет баги в коде.












Практикум
Задача 1: Фильтрация массива чисел
Аутентичная ситуация:

Ситуации, в которых приходится обращаться к массивам, встречаются в работе программиста повсеместно. 
Представим, к примеру, что нам необходимо вывести на админку интернет-магазина инфографику, 
которая показывала бы товары, купленные больше заданного числа раз. 
Чтобы решить такую задачу, 
нам необходимо будет перебрать массив данных о закупках товаров 
и вывести все элементы массива со значением больше заданного.

Задача:

Напишите функцию, которая принимает массив чисел и пороговое значение. 
Функция должна возвращать новый массив, содержащий только те числа из исходного массива,
 которые выше порогового значения.

Шаги реализации:

Создайте функцию, принимающую два параметра: массив чисел и пороговое значение.
Переберите массив с использованием цикла for.
Проверьте, превышает ли текущий элемент пороговое значение.
Если превышает, добавьте его в новый массив.
Верните новый массив.
Решение:

function filterGreaterThan(numbers, threshold) {
    let result = [];
    for (let i = 0; i < numbers.length; i++) {
        if (numbers[i] > threshold) {
            result.push(numbers[i]);
        }
    }
    return result;
}

console.log(filterGreaterThan([1, 5, 8, 3, 12], 5));  // [8, 12]
console.log(filterGreaterThan([10, 20, 30], 25));    // [30]





Задача 2: Подсчёт среднего значения массива
Аутентичная ситуация:

Давайте рассмотрим ситуацию, в которой нам пригодится умение определять среднее значение массива. 
Например, представим, что нам нужно вычислить средний балл студентов по их оценкам, 
чтобы определить, соответствует ли их успеваемость установленным стандартам.

Задача:

Напишите функцию, которая принимает массив чисел и возвращает их среднее значение. 
Если массив пустой, функция должна вернуть сообщение: «Массив пуст».

Шаги реализации:

Создайте функцию, принимающую массив чисел.
Проверьте, пустой ли массив.
Если массив не пустой, найдите сумму всех элементов.
Разделите сумму на количество элементов, чтобы получить среднее.
Верните результат.
Решение:

function calculateAverage(numbers) {
    if (numbers.length === 0) {
        return "Массив пуст";
    }
    let sum = 0;
    for (let i = 0; i < numbers.length; i++) {
        sum += numbers[i];
    }
    return sum / numbers.length;
}

console.log(calculateAverage([10, 20, 30]));  // 20
console.log(calculateAverage([]));            // Массив пуст


Эти задачи помогут вам попрактиковаться в работе с массивами, 
циклическими конструкциями и базовыми математическими операциями.

Итоги:
На этом занятии мы:

освоили создание массивов и добавление элементов;
применили обращение к элементам по индексу;
использовали свойство length для определения длины массива;
выполнили перебор массива с помощью разных конструкций;
преобразовали и отфильтровали массивы с помощью встроенных методов.