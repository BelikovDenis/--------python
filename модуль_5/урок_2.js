Урок №2. Операторы (арифметические, логические). Условия (if, else, switch)


Учебные материалы


JavaScript — это язык, который помогает не только «оживлять» страницы, 
но и принимать решения прямо внутри кода. 
Если на прошлом занятии мы познакомились с переменными, 
типами данных и преобразованием значений, 
то теперь сделаем следующий шаг — научимся управлять логикой выполнения программы.

Сегодня мы:

узнаем, как выполнять математические операции с 
помощью арифметических операторов и проверять условия с использованием операторов сравнения;
разберём, как использовать условные конструкции if, else и switch для управления выполнением кода;
познакомимся с тернарным оператором для компактной записи условий.






Глоссарий ко второму занятию
Operator (Оператор)

Operator — символ или ключевое слово, 
которое используется для выполнения действий с данными (например, +, -, &&).

Arithmetic (Арифметический)

Arithmetic — относится к операциям сложения, вычитания, 
умножения, деления и другим математическим вычислениям.

Condition (Условие)

Condition — проверка, которая возвращает true (истина) или false (ложь) и определяет, 
какой блок кода будет выполнен.

Branch (Ветвление)

Branch — структура кода, 
которая позволяет выполнять разные действия в зависимости от условий (if, else, switch).

Increment (Инкремент)
Increment — операция, которая увеличивает значение переменной на единицу (i++).

Decrement (Декремент)
Decrement — операция, которая уменьшает значение переменной на единицу (i--).

Ternary (Тернарный)

Ternary — сокращённый способ записи условия в одну строку с использованием оператора ? :.


Операторы и условные конструкции — это основа принятия решений в коде. 
С их помощью мы задаём, что именно должно происходить при тех или иных условиях: 
выполнение вычислений, сравнение значений, выбор одного из нескольких путей.

Вспомним, как в Python мы использовали операторы и конструкции if, else, 
чтобы управлять потоком выполнения. В JavaScript это работает похоже, 
но с некоторыми отличиями в синтаксисе. Мы будем использовать те же логические принципы, 
но в стиле JavaScript: с операторами ==, ===, &&, || и конструкциями if, else, switch.

Чтобы уверенно управлять логикой выполнения программ в JavaScript, важно разобраться, 
какие инструменты предоставляет язык: 
от арифметических и логических операторов до условных конструкций разной степени сложности.




Что такое операторы?

Операторы в JavaScript — это основные «строительные блоки», 
с помощью которых мы можем работать с данными, 
проводить их обработку и создавать логические связи. 
Они позволяют выполнять действия со значениями или переменными.

Операторы бывают различных видов: арифметические, логические, операторы сравнения и другие.

Каждый из них выполняет свою функцию, но вместе они помогают строить логику программ.

Двигаемся дальше и перейдём к рассмотрению самых популярных и часто используемых типов операторов.




Арифметические операторы

Начнём с арифметических операторов. 
Они используются для выполнения базовых математических операций и широко применяются, 
например, в калькуляторах, подсчёте результатов и работе с данными.


Основные арифметические операторы:

сложение (+),
вычитание (-),
умножение (*),
деление (/),
остаток от деления (%).

Пример:

let a = 10;

let b = 3;

console.log(a + b);  // 13
console.log(a - b);  // 7
console.log(a * b);  // 30
console.log(a / b);  // 3.3333...
console.log(a % b);  // 1 (остаток от деления 10 на 3)

Операторы сравнения

После математических операций часто возникает необходимость проверить, 
соответствуют ли данные заданным условиям. Для этого мы используем операторы сравнения.

Они позволяют сравнивать значения и возвращают либо true (истина), либо false (ложь):

Равно (==) — сравнивает значения без учёта типа.
Строго равно (===) — сравнивает значения с учётом типа.
Не равно (!=) и строго не равно (!==).
Больше (>), меньше (<), больше или равно (>=), меньше или равно (<=).

Пример:

console.log(5 == "5");   // true
console.log(5 === "5");  // false
console.log(10 > 5);     // true
console.log(5 !== 5);    // false

Логические операторы

Часто в программах возникает необходимость проверить несколько условий одновременно. 
Для этого используются логические операторы. 
Они помогают объединять простые условия в более сложные.

И (&&) — возвращает true, если оба условия истинны.
Или (||) — возвращает true, если хотя бы одно условие истинно.
Не (!) — возвращает обратное значение условия.

Пример:

let a = true;
let b = false;

console.log(a && b);  // false
console.log(a || b);  // true
console.log(!a);      // false

Ниже приведем полную таблицу результата выполнения операций и (&&) 
и или (||) в зависимости от того, являются ли истинными значения двух переменных: A и B.

Такая таблица называется таблицей истинности для логических функций и (&&) и или (||).


Рисунок 1
Инкремент и декремент

Для работы с числами в JavaScript есть специальные операторы, 
которые позволяют увеличивать или уменьшать значение переменной на единицу. 
Это инкремент (++) и декремент (—).

Эти операторы часто используются в циклах и вычислениях. 
Они могут применяться в двух формах: инкремент (++) 
увеличивает значение переменной на 1, а декремент (--) уменьшает.

Они могут использоваться в префиксной или постфиксной форме:

Префиксная форма (++i): увеличивает/уменьшает значение перед использованием.
Постфиксная форма (i++): сначала возвращает значение, а затем увеличивает/уменьшает его.
Пример:

let i = 5;

console.log(++i);  // 6 (сначала увеличиваем, потом выводим)
console.log(i++);  // 6 (сначала выводим, потом увеличиваем)
console.log(i);    // 7
let i = 5;

console.log(--i);  // 4 (сначала уменьшаем, потом выводим)
console.log(i--);  // 4 (сначала выводим, потом уменьшаем)
console.log(i);    // 3
Условные конструкции


Рисунок 2
Теперь, когда мы уже познакомились с операторами, 
самое время рассмотреть условные конструкции. 
Они позволяют управлять процессом выполнения кода в зависимости от результата проверки условий.

if и else: Эта конструкция позволяет выполнить один блок кода, 
если условие истинно, и другой блок, если оно ложно:

if и else
let age = 18;

if (age >= 18) {
    console.log("Доступ разрешён");
} else {
    console.log("Доступ запрещён");
}
else if

Для проверки нескольких условий используется else if:

let score = 85;

if (score >= 90) {
    console.log("Отлично");
} else if (score >= 75) {
    console.log("Хорошо");
} else {
    console.log("Удовлетворительно");
}
switch
Для проверки значений переменной на равенство удобен оператор switch. 
Здесь обязательно использовать break, чтобы избежать выполнения других блоков. 
Несколько условий могут быть объединены для одного блока.

Важно: использование break после каждого case обязательно, 
иначе выполнение программы перейдёт к следующему блоку. 
Также несколько условий могут быть объединены для выполнения одного блока кода.

В конце конструкции switch можно указать блок default: он выполнится, 
если ни одно из условий не подошло. 
Это своего рода вариант по умолчанию, когда программа не нашла подходящего case.

Блок default можно использовать как аналог else в if-else. 
Он не обязателен, но часто помогает предусмотреть неожиданные значения.

Пример:

let day = 3;

switch (day) {
    case 1:
    case 7: // Сюда попадут и 1, и 7
        console.log("Выходной");
        break;
    case 2:
    case 3:
    case 4:
    case 5:
    case 6:
        console.log("Рабочий день");
        break;
    default:
        console.log("Неизвестный день");
}

Тернарный оператор

Для простых условий можно использовать тернарный оператор (? :). 
Он позволяет записать проверку и выполнение кода в одну строку:

let age = 20;
let message = age >= 18 ? "Доступ разрешён" : "Доступ запрещён";
console.log(message);







Типичные ошибки
Тип ошибки 1: Деление на 0 — Infinity или значение при делении на ноль

Деление на 0 в JavaScript не вызывает ошибки, но возвращает специальное значение Infinity.

Пример ошибки:

let result = 10 / 0;
console.log(result);  // Infinity
Решение:

Добавьте проверку делителя перед выполнением операции:

let divisor = 0;

if (divisor === 0) {
    console.log("Деление на 0 невозможно.");
} else {
    console.log(10 / divisor);
}





Тип ошибки 2: Присваивание вместо сравнения

Возникает при использовании оператора = вместо == или ===.

Пример ошибки:

let x = 5;

if (x = 10) {  // Присваивание, а не сравнение
    console.log("Ошибка!");
}
Решение:

Используйте == для сравнения значений или === для строгого сравнения:

if (x === 10) {
    console.log("Сравнение выполнено корректно.");
}







Тип ошибки 3: Проблемы с областью видимости

Переменные, объявленные с let или const, имеют блочную область видимости. 
Попытка обратиться к переменной до её инициализации вызывает ошибку.

Пример ошибки:

console.log(x);  // ReferenceError: Cannot access 'x' before initialization
let x = 5;
Решение:

Объявляйте и инициализируйте переменные перед их использованием:

let x = 5;
console.log(x);  // 5







Тип ошибки 4: Ошибка в сравнении типов

Использование оператора == вместо === может привести 
к неожиданным результатам из-за преобразования типов. 
В частности, проверка на равенство может выполниться между данными совсем разных типов, 
что не всегда является целью.

Пример ошибки:

console.log(5 == "5");   // true
console.log(5 === "5");  // false

Решение:

Используйте === для строгого сравнения, если хотите учитывать типы данных:

console.log(5 === "5");  // false
console.log(5 === 5);    // true






Тип ошибки 5: Отсутствие break в switch

Отсутствие ключевого слова break в switch может привести к выполнению нескольких кейсов.

Пример ошибки:

let color = "red";

switch (color) {
    case "red":
        console.log("Красный");
    case "blue":
        console.log("Синий");
    default:
        console.log("Неизвестный цвет");
}
// Вывод: "Красный", "Синий", "Неизвестный цвет"

Решение:

Всегда добавляйте break, чтобы прервать выполнение кода:

switch (color) {
    case "red":
        console.log("Красный");
        break;
    case "blue":
        console.log("Синий");
        break;
    default:
        console.log("Неизвестный цвет");
}



Тип ошибки 6: Использование некорректных операторов

Возникает при некорректном использовании операторов, например при смешивании чисел и строк.

Пример ошибки:

let result = 5 + "5";  // "55", а не 10
console.log(result);
Решение:

Преобразуйте данные перед выполнением операций:

let result = 5 + Number("5");  // 10
console.log(result);






Эти примеры помогут распознавать и исправлять распространённые ошибки, 
возникающие при работе с операторами и условиями в JavaScript.






Практикум
Задача 1: Определение чётности числа

Ситуация: мы создаем приложение для магазина, где требуется быстро определить, 
является количество товаров на складе чётным или нечётным.

Задача: написать программу, 
которая запрашивает у пользователя число и проверяет, чётное оно или нечётное.

Шаги реализации:

Запросите у пользователя число с помощью prompt().
Преобразуйте введённое значение в число с помощью Number().
Проверьте, является ли значение числом и конечным с помощью isNaN() и isFinite().
Используйте оператор %, чтобы определить, делится ли число на 2.
Выведите в консоль результат проверки.
Решение:

let number = prompt("Введите число:");

number = Number(number);

if (isNaN(number) || !isFinite(number)) {
    console.log("Ошибка: введите конечное число.");
} else if (number % 2 === 0) {
    console.log("Число чётное.");
} else {
    console.log("Число нечётное.");
}






Задача 2: Программа рекомендаций по температуре

Ситуация: мы пишем программу для приложения прогноза погоды, 
которая будет давать рекомендации в зависимости от текущей температуры.

Задача: написать программу, 
которая запрашивает у пользователя температуру воздуха и выводит рекомендации:

Если температура ниже 0 °C, программа выводит: "На улице холодно, оденьтесь теплее."
Если температура от 0 °C до 20 °C, программа выводит: "Погода прохладная, возьмите с собой куртку."
Если температура выше 20 °C, программа выводит: "На улице тепло, можно одеваться легче."

Шаги реализации:

Запросите у пользователя температуру с помощью prompt().
Преобразуйте введённое значение в число с помощью Number().
Используйте конструкцию if...else if...else для проверки температуры и вывода соответствующих рекомендаций.
Решение:

let temperature = prompt("Введите текущую температуру воздуха (в градусах Цельсия):");

temperature = Number(temperature);

if (isNaN(temperature)) {
    console.log("Ошибка: введите корректное значение температуры.");
} else if (temperature < 0) {
    console.log("На улице холодно, оденьтесь теплее.");
} else if (temperature <= 20) {
    console.log("Погода прохладная, возьмите с собой куртку.");
} else {
    console.log("На улице тепло, можно одеваться легче.");
}








Итоги:
На сегодняшнем занятии мы познакомились 
с основами работы с операторами и условными конструкциями в JavaScript:

изучили арифметические и логические операторы в JavaScript 
для выполнения базовых математических операций;
узнали, как использовать if и else для проверки условий 
и выполнения различных блоков кода в зависимости от результата;
научились применять операторы, характерные для JavaScript, 
для создания логики в программах.
В дальнейшем мы будем изучать более сложные конструкции и алгоритмы, 
но понимание операторов и условий — это важный шаг в программировании, 
который поможет вам строить логику любых приложений.
