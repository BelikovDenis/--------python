Урок №30. Введение в typescript, установка в проект.
Учебные материалы
На прошлых занятиях мы изучали JavaScript. Теперь же мы переходим к TypeScript — инструменту,
 который добавляет строгую типизацию в JavaScript, помогая писать более надёжный код и избегать
  ошибок ещё на этапе разработки.

JavaScript появился в 1995 году как простой язык для веб-страниц, но со временем его начали использовать
 для больших приложений, таких как игры или сайты вроде Google. Из-за его гибкости (можно использовать
   любые данные) в сложных проектах появлялись ошибки, которые находили только при тестировании. 
   В 2012 году компания Microsoft создала TypeScript, чтобы добавить в JavaScript строгие правила,
    как в языках вроде C# или Java, но сохранить его простоту. Сегодня TypeScript используют в популярных проектах, 
    таких как сайты и приложения, чтобы код был надёжнее.

Представьте, что вы строите дом: JavaScript — это когда вы кладёте кирпичи, не проверяя их размеры,
 и надеетесь, что всё сойдётся. TypeScript — это как линейка, которая проверяет каждый кирпич перед укладкой,
  чтобы избежать кривых стен. TypeScript не меняет JavaScript, а расширяет его, добавляя типы, 
  которые помогают находить ошибки до запуска кода.

Сегодня мы:

узнаем, что такое TypeScript, зачем он нужен и как он помогает в разработке;
разберём историю создания TypeScript и его преимущества перед JavaScript;
научимся устанавливать TypeScript и подключать его к проекту;
создадим несколько переменных с разными типами, чтобы понять строгую типизацию;
поймём, как TypeScript предотвращает ошибки на этапе написания кода, не доходя до тестирования.
После занятия вы сможете настроить TypeScript в своём проекте и использовать строгую типизацию для создания надёжного кода.



Глоссарий к тридцатому занятию
TypeScript (Тайпскрипт)
TypeScript — надстройка над JavaScript, добавляющая строгую типизацию и проверку типов на этапе компиляции.

Типизация
Типизация — процесс указания типов данных для переменных, функций и объектов, чтобы ограничить их использование.

Компиляция
Компиляция — процесс преобразования TypeScript-кода в обычный JavaScript с помощью компилятора tsc.

Тип
Тип — описание ожидаемого формата данных (например, string, number, boolean).

TSConfig (ТСКонфиг)
TSConfig — файл tsconfig.json, который настраивает параметры компиляции TypeScript.





Что такое TypeScript и зачем он нужен?
TypeScript — это инструмент, который делает JavaScript лучше, добавляя правила, чтобы ваш код был надёжнее. 
Представим, что вы пишете список школьных оценок. Вы хотите, чтобы в списке были только числа (например, 5, 4, 3),
 но случайно добавляете слово «пять». В обычном JavaScript это может вызвать ошибку, которую вы заметите только
  при запуске программы. TypeScript — это как помощник, который проверяет ваш код до запуска и говорит: «Эй, 
  тут должно быть число, а не слово!» Это помогает писать программы без ошибок и делает их понятнее.

TypeScript — это не совсем новый язык, а улучшенная версия JavaScript. Он добавляет типы данных, которые указывают,
 какие значения можно использовать (например, только числа или только строки). Ваш код на TypeScript проверяется
  в редакторе (например, VS Code), и, если что-то не так, вы сразу видите ошибку. Потом TypeScript превращает
   ваш код в обычный JavaScript, который работает в браузерах или на компьютере.

Зачем нужен TypeScript?
Меньше ошибок: TypeScript проверяет наш код, пока мы его пишем, и не даёт использовать неправильные данные 
(например, слово вместо числа). Это как если бы ваш учитель математики сразу указал на ошибку в задаче до того,
 как вы её сдадите.
Понятнее код: типы — это как ярлыки на коробках с канцелярией. Если написано «карандаши», мы знаем,
 что там только карандаши, а не ручки.
Легче учиться: для школьников TypeScript как подсказка, которая помогает понять, какие данные нужно использовать.
Работает везде: TypeScript превращает ваш код в JavaScript, который работает в любом браузере или на Node.js.
Подсказки в редакторе: в редакторах вроде VS Code TypeScript показывает, какие значения можно использовать,
 чтобы мы быстрее писали код.
Допустим, вы пишете программу для хранения имени ученика и его оценки. Вы хотите, чтобы имя было строкой (например,
   Маша), а оценка — числом (например, 5). TypeScript заставляет вас указать это явно,
    чтобы случайно не написать оценку словом «пять». Это помогает избежать путаницы.

Когда использовать TypeScript?
Если вы делаете школьный проект и хотите, чтобы код был понятным и без ошибок.
Когда работаете совместно с одноклассниками, чтобы все знали, какие данные использовать.
Если хотите научиться писать код, как профессиональные программисты.
TypeScript — это как калькулятор, который не только считает, но и проверяет, правильно ли вы ввели числа.
 Он учит вас быть внимательнее и помогает создавать программы, которые работают правильно с первого раза.

Пошаговая установка и подключение TypeScript в проект
Чтобы начать использовать TypeScript, нужно установить его на компьютер и настроить проект.
 TypeScript-код пишется в файлах с расширением .ts (например, index.ts), а потом превращается в обычный 
 JavaScript (.js) с помощью программы tsc (TypeScript Compiler). Этот JavaScript потом можно запустить 
 в браузере или на Node.js.

Шаг 1: Убедимся, что есть Node.js
Node.js — это программа, которая позволяет запускать JavaScript на компьютере и устанавливать инструменты,
 такие как TypeScript.

Откроем терминал:

На Windows — командная строка или PowerShell.
На macOS/Linux — терминал.
Проверим, установлен ли Node.js:

node --version
npm --version
Если мы видим версии (например, v18.16.0 для Node.js и 9.5.0 для npm), всё готово. 
Если нет, скачаем Node.js с nodejs.org и установим его.

Зачем это нужно? Node.js даёт нам npm — инструмент, который установит TypeScript.

Шаг 2: Установим TypeScript
В терминале введём:

npm install -g typescript
Флаг -g означает «глобально», чтобы TypeScript работал для всех ваших проектов.

Проверим установку:

tsc --version
Должно появиться что-то вроде Version 5.5.3. Если не работает, попробуем переустановить.

Шаг 3: Создаём проект
Создаём папку для проекта, например, my-school-project:

mkdir my-school-project
cd my-school-project
Создаём файл package.json для управления проектом:

npm init -y
Это как дневник проекта, где записаны его настройки.

Шаг 4: Настроим TypeScript
Введём:

tsc --init
Это создаст файл tsconfig.json, он говорит TypeScript, как проверять и превращать наш код в JavaScript.

Откроем tsconfig.json в текстовом редакторе (например, VS Code) и заменим его содержимое на:

{
  "compilerOptions": {
    "target": "es6", // Компилировать в современный JavaScript
    "outDir": "./dist", // Сохранять .js файлы в папку dist
    "strict": true, // Включить строгие проверки
    "module": "commonjs" // Формат для Node.js
  },
  "include": ["*.ts"] // Компилировать все .ts файлы
}
Что это значит?

target: "es6" — код станет JavaScript, который работает в современных браузерах.
outDir: "./dist" — все скомпилированные файлы будут в папке dist.
strict: true — TypeScript будет проверять код очень строго.
include: ["*.ts"] — компилировать только файлы .ts.
Шаг 5: Напишем первый код
Создадим файл index.ts в папке проекта и добавим простой код:

let message: string = "Я учу TypeScript!";
console.log(message);
Здесь : string говорит, что message — это строка. TypeScript проверит,
 чтобы мы не присвоили этому числовое или другое значение.

Шаг 6: Скомпилируем код
В терминале введём:

tsc
Это создаст папку dist и файл dist/index.js — обычный JavaScript, который получился из нашего кода.

Откроем dist/index.js и мы увидим:

let message = "Я учу TypeScript!";
console.log(message);
TypeScript убрал : string, потому что типы нужны только для проверки, а не для выполнения.

Шаг 7: Запустим программу
Введём:

node dist/index.js
Вывод: Я учу TypeScript!

Что будет, если ошибиться?

Если мы попробуем запустить .ts-файл напрямую:

node index.ts
Мы получим ошибку: Cannot find module 'index.ts'. TypeScript-файлы нужно сначала скомпилировать с помощью tsc.

Пример ошибки в коде

Если в index.ts написать:

let message: string = 42; // Ошибка: Type 'number' is not assignable to type 'string'
TypeScript не даст скомпилировать код, пока мы не исправим:

let message: string = "Теперь всё правильно!";
Установка TypeScript — это как собрать новый набор для уроков: вы делаете всё по шагам и в итоге получаете инструмент,
 который помогает писать код без ошибок. Это идеально для школьных проектов, где вы хотите сосредоточиться на задаче,
  а не на исправлении багов.

Создание переменных с типами
Теперь, когда TypeScript настроен, давайте научимся создавать переменные с типами.
 Типы — это как надписи на школьных тетрадях: если написано «математика», вы не будете писать там стихи. 
 Это помогает TypeScript следить, чтобы вы использовали правильные данные.

Основные типы
TypeScript имеет три главных типа, которые мы будем использовать:

string — для текста, например, «Маша» или «привет»;
number — для чисел, например, 5 или 3.14;
boolean — для true (правда) или false (ложь).
Пример:

let studentName: string = "Алексей";
let studentGrade: number = 5;
let isPresent: boolean = true;

console.log(`Ученик: ${studentName}, оценка: ${studentGrade}, на уроке: ${isPresent}`);
Вывод: Ученик: Алексей, оценка: 5, на уроке: true.

Проверка ошибок
Попробуем сделать ошибку:

let studentGrade: number = "пять"; // Ошибка: Type 'string' is not assignable to type 'number'
TypeScript сразу скажет, что studentGrade должно быть числом. Исправим:

let studentGrade: number = 5;
Массивы
Массивы — это списки, например, оценок или имён. В TypeScript мы указываем, какого типа данные в массиве.

Пример с массивом оценок:

let grades: number[] = [5, 4, 5];
console.log(`Оценки: ${grades.join(", ")}`);
Вывод: Оценки: 5, 4, 5.

Если попробовать добавить слово:

grades.push("3"); // Ошибка: Argument of type 'string' is not assignable to parameter of type 'number'
TypeScript не даст этого сделать. Исправим:

grades.push(3);
console.log(`Новые оценки: ${grades.join(", ")}`); // Оценки: 5, 4, 5, 3
Массив строк

Теперь пример с именами учеников:

let students: string[] = ["Аня", "Борис"];
students.push("Вика");
console.log(`Ученики: ${students.join(", ")}`);
Вывод: Ученики: Аня, Борис, Вика.

Если добавить число:

students.push(42); // Ошибка: Argument of type 'number' is not assignable to parameter of type 'string'
Тип any (осторожно!)
Тип any позволяет использовать любое значение, как в обычном JavaScript. Но это отключает проверки TypeScript, 
так что используем его только в крайнем случае.

Пример:

let something: any = "текст";
something = 42; // Без ошибок
something = true; // Без ошибок
console.log(something); // true
Проблема: с any TypeScript не поможет найти ошибки. Лучше всегда использовать конкретные типы:

let something: string = "текст";
something = 42; // Ошибка: Type 'number' is not assignable to type 'string'
Пример
Допустим, мы пишем код, чтобы проверить, какие задания выполнены:

let tasks: boolean[] = [true, false, true]; // true — выполнено, false — нет
let completed: number = tasks.filter(task => task).length;
console.log(`Выполнено заданий: ${completed}`);
Вывод: Выполнено заданий: 2.

Если ошибиться:

tasks.push("да"); // Ошибка: Argument of type 'string' is not assignable to parameter of type 'boolean'
TypeScript заставит нас исправить:

tasks.push(true);
Почему типы полезны?
Меньше ошибок: TypeScript не даёт использовать неправильные данные, например, слово вместо числа.
Легче учиться: типы — это как подсказки в учебнике, которые помогают понять, что вы делаете.
Код понятнее: если вы видите string\[], вы знаете, что это список строк, а не чисел.
TypeScript — это как ластик, который исправляет ошибки в вашей программе до того, как вы покажете её учителю.
 Он помогает вам писать код, который работает правильно, и учит быть внимательнее.

Теперь, когда мы узнали, что такое TypeScript, как его установить и как создавать переменные с типами,
 давайте посмотрим, какие ошибки могут возникнуть, если не следовать правилам типизации или настройки проекта.
  Эти ошибки часто встречаются у новичков, но TypeScript помогает их заметить и исправить, 
  чтобы ваш код оставался надёжным и понятным.

Типичные ошибки
Тип ошибки 1: Неправильное указание типа переменной
Описание:

Новички часто указывают неверный тип или забывают его указать, из-за чего TypeScript либо выдаёт ошибку,
 либо использует тип any, теряя преимущества типизации. Это как надеть ботинки не того размера — вроде идти можно,
  но неудобно.

Пример ошибки (код):

let count = 5; // Тип не указан, TypeScript предполагает any
count = "пять"; // Без ошибки, но это неправильно
console.log(count);
Объяснение:

Без явного указания типа TypeScript присваивает переменной тип any, что отключает проверку типов. 
Это лишает смысла использование TypeScript.

Решение:

let count: number = 5;
count = "пять"; // Ошибка: Type 'string' is not assignable to type 'number'
console.log(count);
Всегда указывайте тип переменной, чтобы TypeScript мог проверить её использование.

Тип ошибки 2: Игнорирование tsconfig.json
Описание:

Некоторые забывают настроить tsconfig.json или не компилируют код, пытаясь запустить .ts-файл напрямую. 
Это как пытаться приготовить суп без рецепта — результат непредсказуем.

Пример ошибки (код):

// index.ts
let message: string = "Привет!";
console.log(message);
// Запуск: node index.ts
// Ошибка: Cannot find module 'index.ts'. Did you mean to set the 'moduleResolution' option to 'node'?
Объяснение:

Файлы .ts нельзя запускать напрямую через node, так как они требуют компиляции в .js. 
Без tsconfig.json компиляция может быть настроена неверно.

Решение:

tsc --init # Создаём tsconfig.json
tsc # Компилируем index.ts в index.js
node index.js # Запускаем скомпилированный файл
Настройте tsconfig.json и компилируйте код с помощью tsc перед запуском.

Тип ошибки 3: Использование any вместо конкретного типа
Описание:

Использование типа any позволяет писать код, как в обычном JavaScript, но отключает проверку типов,
 делая TypeScript бесполезным. Это как писать контрольную без проверки ответов.

Пример ошибки (код):

let value: any = 10;
value = "десять"; // Без ошибки
value.toUpperCase(); // Ошибка во время выполнения: value.toUpperCase is not a function
Объяснение:

Тип any позволяет присваивать переменной любые значения, что может привести к ошибкам,
 которые TypeScript должен был предотвратить.

Решение:

let value: number = 10;
value = "десять"; // Ошибка: Type 'string' is not assignable to type 'number'
console.log(value);
Используйте конкретные типы (string, number, boolean) вместо any.

Тип ошибки 4: Неверная типизация массивов
Описание:

Новички часто ошибаются при указании типов для массивов, что приводит к неожиданным ошибкам.
 Это как собрать корзину фруктов, но случайно положить туда овощ.

Пример ошибки (код):

let items: string[] = ["яблоко", "груша"];
items.push(42); // Ошибка: Argument of type 'number' is not assignable to parameter of type 'string'
Объяснение:

TypeScript ожидает, что массив items содержит только строки. 
Попытка добавить число вызывает ошибку компиляции.

Решение:

let items: string[] = ["яблоко", "груша"];
items.push("банан"); // Работает
console.log(items); // ["яблоко", "груша", "банан"]
Проверяйте тип элементов массива при его объявлении.

Практикум
Задача 1: Создание переменных с типами
Ситуация:

Вы создаёте приложение для учёта школьных данных, где нужно хранить информацию о студенте: имя, возраст и статус активности.

Задача:

Напишите код в TypeScript, создав три переменные: studentName (строку), studentAge (число), isActive (булево).
 Присвойте им значения и выведите в консоль: «Студент: [имя], [возраст] лет, активен: [статус]».

Решение:

let studentName: string = "Маша";
let studentAge: number = 14;
let isActive: boolean = true;

console.log(`Студент: ${studentName}, ${studentAge} лет, активен: ${isActive}`);
Задача 2: Проверка массива оценок
Ситуация:

В том же приложении вы хотите сохранить и вывести оценки студента, убедившись, что это только числа.

Задача:

Создайте массив grades с типом number[], заполните его значениями.
 Добавьте новую оценку и выведите массив в консоль в формате: «Оценки: [список через запятую]».
  Попробуйте добавить строку в массив и проверьте, что TypeScript выдаст ошибку.

Решение:

let grades: number[] = [90, 85, 88];
grades.push(92);
// grades.push("95"); // Ошибка: Argument of type 'string' is not assignable to parameter of type 'number'
console.log(`Оценки: ${grades.join(', ')}`);
Итоги
Сегодня мы:

узнали, что такое TypeScript и как он помогает писать надёжный код с помощью строгой типизации;
разобрали историю создания TypeScript и его преимущества перед JavaScript;
научились устанавливать TypeScript и настраивать проект с tsconfig.json;
создали переменные с разными типами, включая строки, числа, булевы значения и массивы;
поняли, как TypeScript предотвращает ошибки на этапе разработки, делая код безопаснее.
Теперь вы можете настроить TypeScript в своём проекте, использовать типизацию для переменных и массивов и писать код,
 который легче отлаживать и поддерживать.