Урок №14. Манипуляции с DOM.

Учебные материалы

Поскольку вы уже уверенно ориентируетесь в структуре страницы, 
умеете находить элементы по id и с помощью CSS-селекторов, 
пришло время перейти к следующему шагу — динамическому изменению страницы с помощью JavaScript.

Сегодня мы:

узнаем, зачем и в каких случаях важно изменять структуру страницы на лету;
научимся:
создавать и добавлять в DOM элементы, которых изначально не было в HTML;
вызывать функции при нажатии кнопок и других действиях пользователя;
удалять ненужные элементы со страницы;
изменять параметры страницы, такие как заголовок, иконка и описание;
редактировать стили и содержимое выбранных элементов.
Страница перестаёт быть статичной: она начинает реагировать, меняться и оживать на глазах.



Глоссарий к четырнадцатому занятию
createElement (Cоздать элемент)

Функция страницы-документа, позволяющая создать элемент без привязки к ней.

appendChild (Добавить потомка)

Функция страницы-документа, позволяющая прикрепить потомка к родительскому элементу.

removeChild (Удалить потомка)

Функция элемента, позволяющая удалить потомка, если на него есть ссылка.

remove (Удалить)

Функция, позволяющая элементу удалить себя.

setAttribute (Установить атрибут)

Функция, позволяющая создать атрибут при его отсутствии или назначить новое значение существующему атрибуту.

removeAttribute (Удалить атрибут)

Функция, позволяющая удалить атрибут элемента.

textContent (Текстовое содержимое)

Свойство, задающее и хранящее текст элемента:

надписи на кнопке,
заголовка,
абзаца,
элемента списка,
ячейки таблицы.

Onclick (По нажатию)

Свойство, хранящее функцию, выполняющуюся по нажатию на кнопку.

Value (Значение)

Свойство, использующееся для хранения и изменения значения поля ввода.







Зачем создавать и удалять элементы динамически?
Создание, удаление и отображение элементов в динамике — это способ изменять страницу 
прямо в браузере без её перезагрузки. Всё делает скрипт, а не верстальщик. 
Представьте: в HTML — только пустое <body>, а всё содержимое (заголовки, абзацы, 
    кнопки, тексты) появляется постепенно и по сценарию, написанному на JavaScript.

Зачем это нужно? Ведь вы уже научились писать HTML-страницы, 
почему бы не продолжать и дальше делать всё вручную, а JavaScript оставить для расчётов и общения с сервером?

Ответ простой: вы не всегда заранее знаете, какие элементы и в каком количестве должны быть на странице.

Пример 1. Онлайн-магазин или маркетплейс. Вы не можете заранее предсказать, 
сколько продавцов выложат свои товары завтра. Покупатель может искать только шампунь, 
а если вы покажете ему всё подряд — ноутбуки, корм для кошек и кроссовки — он уйдёт. 
Именно динамическое создание карточек товаров позволяет показывать только нужное.

Пример 2. Система бронирования билетов. Представьте, что вы выбираете билет на поезд. 
Пока думаете — кто-то купил ваше место. Нужно удалить его со страницы без перезагрузки, 
чтобы вы не бронировали то, чего уже нет. Перезагружать страницу каждую секунду — плохой UX. 
Лучше обновить только нужную часть, и для этого требуется JavaScript.

Как добавить элемент на страницу? Мы разберём, какие шаги нужно выполнить, 
чтобы с помощью кода создать новый DOM-элемент, 
настроить его содержимое и вставить в нужное место на странице.

Создание и отображение элемента
Чтобы элемент появился, необходимо выполнить четыре шага:

Определить тип создаваемого элемента. Например, вы хотите добавить кнопку button.

Создать DOM-элемент с помощью JavaScript. 
Для этого используем метод document.createElement(…) и сохраняем результат в переменную:

let my_button = document.createElement('button')
3. Найти, куда именно на странице вставить новый элемент. 
Сейчас мы вставим кнопку в <body>. Но в будущем (например, 
при создании строк в таблице) вам придётся искать конкретное место на странице для размещения элемента.

4. Прикрепить элемент к найденному узлу DOM-дерева. Используем метод appendChild(…):

document.body.appendChild(my_button)
Почему так сложно? Почему нельзя просто создать и «считать, что она есть»? 
Потому что создание объекта в памяти — это не то же самое, что его появление на экране. 
Браузер «рисует» только то, что есть в DOM. Пока вы не вставили элемент в DOM-дерево, 
он существует только в оперативной памяти и невидим для пользователя.

На практике вы почти никогда не будете вставлять элементы «в лоб» прямо в <body>. 
Обычно вы сначала находите нужное место на странице (например, определённый блок или таблицу) 
и только потом вставляете туда нужный элемент. Мы отработаем это в практическом задании.

Настройка элемента
Создать элемент — только начало. Чтобы он выглядел и работал как надо, его нужно настроить. 
Например, таблица без бордюров невидима, а кнопка без текста похожа на серое рисовое зёрнышко.

Добавление текста. Почти у любого элемента есть текстовое содержимое. 
Его можно задать разными способами:
my_button.textContent = 'Кнопочка'
Раньше для этого создавали отдельный текстовый узел с помощью createTextNode, 
но современные браузеры прекрасно понимают textContent.

Добавление поведения. По умолчанию кнопка ничего не делает. 
Чтобы она начала реагировать на действия пользователя, её нужно «обучить»:
my_button.onclick = () => alert('Вот зачем сразу нажимать?')
Здесь () => — стрелочная функция, которую мы не вызываем вручную (без ()). 
Браузер сам вызовет её при клике. Можно назначить и обычную функцию.

Выполните такой код у себя на странице, подключив скрипт отдельным файлом.

Мы подробнее поговорим об этом на практике.

Альтернатива: через HTML. Вы также можете указать реакцию на клик прямо в HTML, как атрибут onclick:
<button onclick="alert('Молодец!')">Нажми меня</button>
Такой способ быстрее в простых случаях, 
но менее удобен в больших проектах: мешает отделять структуру от поведения.

Редактирование атрибутов и стилей выбранного элемента
Для того чтобы к элементу можно было добавлять (и убирать) атрибуты, 
существуют функции setAttribute и removeAttribute.

Однако большинство атрибутов удобно настраивать как готовые поля. 
В частности, для настроек стиля существует поле style, но для свойств, 
содержащих пробел, следует заменить его на большую букву:

// Изменение цвета
element.style.color = 'blue'
// background-color
element.style.backgroundColor = 'green'
Работа с классами

У элемента может быть несколько классов одновременно, и с ними тоже можно работать программно:

element.class = 'someclass'
element.classList.add('my-class-name')
element.classList.remove('my-class-name')

Однако большинство атрибутов удобно настраивать как готовые поля. 
В частности, для настроек стиля существует поле style, но для свойств, 
содержащих пробел, следует заменить его на большую букву:

```javascript
// Изменение цвета
element.style.color = 'blue'
// background-color
element.style.backgroundColor = 'green'
Такой подход особенно полезен, если у вас уже есть готовые стили в CSS — вам не 
нужно их переписывать в JavaScript, достаточно просто применить нужный класс.

element.class = 'someclass'
element.classList.add('my-class-name')
element.classList.remove('my-class-name')
Удаление элемента
Иногда элемент становится больше не нужен. 
Например, пользователь закрыл уведомление, удалил товар из корзины или закрыл вкладку. 
В этом случае его можно удалить из DOM.

document.body.removeChild(my_button)
Смотрите: мы уже понимали, какая ссылка указывает на кнопку. 
Если мы не знаем этого, необходимо сперва найти элемент. 
При этом для удаления нет необходимости искать «родителя»: элемент может удалить себя сам.

my_button.remove()
Важно не путать:

createElement вызывается от имени document.
removeChild вызывается от имени родительского элемента с параметром, указывающим на удаляемый элемент.
remove от имени элемента, который необходимо удалить (самоуничтожение).
innerHTML и почему его нельзя использовать
Каждый HTML-элемент имеет специальное свойство innerHTML, 
которое позволяет задать или получить весь HTML-код внутри элемента в виде строки.

HTML:

<ol><li></li></ol>
javascript:

document.querySelector('ol').innerHTML = ''
Так можно быстро очистить список или заменить содержимое элемента. 
Но у этого метода есть несколько существенных недостатков.

Почему не стоит злоупотреблять innerHTML`

Нарушает читаемость и модульность: 
HTML оказывается «спрятан» в строках JS-кода, что затрудняет поддержку.
Стирает всё содержимое: 
при любом изменении перезаписываются и удаляются все потомки, события, данные.
Опасность XSS-атак: если вы вставляете в innerHTML данные пользователя без фильтрации, 
открывается дверь вредоносным скриптам.

Поля ввода
Важнейшими свойствами полей ввода являются:

value (у всех типов),
valueAsNumber (у number),
valueAsDate (у полей даты).
Отключить ввод
Чтобы поле ввода нельзя было редактировать, устанавливают атрибут disabled.

input_object.disabled = true
Изменение параметров страницы
Встречают, как известно, по одёжке. У каждой страницы есть заголовок, 
именно он отображается на вкладке браузера. Иногда его хочется поменять ещё в HTML, 
но бывают и случаи, когда заголовок нужно изменить динамически. 
Например, при получении нового письма.

document.title = "Почта: У ВАС НОВОЕ СООБЩЕНИЕ!"
Смена иконки страницы (favicon)

Но даже заголовок не всегда виден. В отличие от него, иконка вкладки (favicon) всегда на виду. 
Давайте динамически её изменим. Этот приём пригодится, например, 
если нужно визуально показать изменения (новое сообщение, статус подключения и т. д.).

Напишем код, позволяющий поменять иконку.

let favicon = document.querySelector("link[rel~='icon']")
if (!favicon) {
    favicon = document.createElement('link')
    favicon.rel = 'icon'
    document.head.appendChild(favicon)
}
favicon.href = 'myicon.png'
Важно: заранее подготовьте изображение myicon.png размером 64×64 
пикселя и разместите его рядом с вашим HTML и JS-файлами.

Казалось бы, мы разобрались, как создавать, удалять и настраивать элементы, 
менять содержимое и стили, заголовок и иконку страницы, 
и именно на этом этапе начинаются ошибки: что-то не появляется, хотя вы создали элемент, 
кнопка не реагирует, хотя вы «повесили» обработчик, 
а вместо красивого списка — пустая страница. Почему?

Пора рассмотреть частые ошибки и подводные камни, 
с которыми сталкиваются даже опытные разработчики, и научиться их избегать.







Типичные ошибки
Тип ошибки 1: Создать и не прикрепить

Ошибка возникает, когда человек думает, что достаточно создать элемент, чтобы тот появился.

Пример ошибки:

let button = document.createElement('button')
Наблюдение: кнопка не видна.

Объяснение:

Браузер не знает, где вы захотите видеть кнопку, и, слава разработчикам, не пытается это придумать сам.

Решение:

let button = document.createElement('button')
document.body.appendChild(button)
Кнопка появилась.









Тип ошибки 2: Написать программу и не подключить её

Ошибка возникает, когда начинающий программист думает, 
что достаточно положить два файла рядом, 
чтобы они работали вместе смогли основать компанию и получить много денег.

Пример ошибки:

page.html

<input id='myfield'>
prorgam.js

myfield.value = '$5000'
Далее человек открывает страницу и не видит ожидаемых 5 тысяч долларов.

Объяснение:

Чтобы скрипт начал работать, его необходимо подключить. 
Было бы катастрофой, если бы браузер немедленно исполнял всё, что попадает в папку рядом.

Решение:

Добавьте скрипт в заголовок.

page.html

<script src='program.js' defer> </script>
<input id='myfield'>
prorgam.js

myfield.value = '$5000'









Практикум
Задача 1: Удалить товар
Ситуация:

Вы получили жалобу от клиента: добавить товар в корзину можно, а удалить – нет. 
А если он раздумал что-то покупать?

Задача:

Вам дана страница с таблицей, в которой имеется одна строка с описанием товара. 
Требуется средствами JavaScript добавить кнопку, которая удалит строку с товаром. 
Ниже приведен фрагмент кода страницы, полный текст вы можете написать сами.

<table>
  <tr>
    <td>Фарфоровая статуэтка</td>
    <td>1500р</td>
  </tr>
</table>

Шаги реализации:

Ищем строку таблицы. Одну, но при желании можете найти и обработать их все.
Создаём ячейку, прикрепляем к строке.
Создаём кнопку с надписью «Удалить», прикрепляем к ячейке.
Привязываем к кнопке функцию самоудаления этой строки таблицы.
Решение:

// Ищем строку таблицы
let item = document.querySelector('tr')
// Cоздаём ячейку
let td = document.createElement('td')
// Прикрепляем к строке
item.appendChild(td)
// Создаем кнопку
let my_button = document.createElement('button')
// Добавляем на кнопку надпись "Удалить"
my_button.textContent = 'Удалить'
// Прикрепляем кнопку к ячейке
td.appendChild(my_button)
// Привязываем к кнопке функцию самоудаления этой строки таблицы
my_button.onclick = () => item.remove()
Реализация: https://github.com/doublewera/module5_react_future_code/tree/main/Lesson14/Practicum1.







Задача 2: Шаблон визитки
Ситуация:

Вас попросили сделать страницы-визитки для сотрудников вашей компании. 
Для единообразия посоветовали создать шаблон, в котором фотография, 
дата поступления на работу, должность и навыки отсутствуют, 
но есть места, куда их требуется вписать.

Задача:

Сверстайте шаблон, добавив поле для поиска по фамилии и кнопку, 
к которой будет привязана функция, заполняющая пустые места шаблона информацией.

Шаги реализации:

Будем опираться на такой шаблон страницы:

<td><img id="photo" alt="Фотография"/></td>
<td>
<h1 id="username"></h1>
<h3 id="year">Год поступления: </h3>
<h3>Навыки:</h3>
<ul id="keyfeatures"></ul>
</td>

2. Создадим массив сотрудников:

let people = {
 'Сидоров': {
     username: 'Петр Сергеевич',
     year: 1978,
     keyfeatures: ['Директор']
 },
 'Сидорова': {
     username: 'Анна Петровна',
     year: 1998,
     keyfeatures: ['Отдел кадров']
 }
}

3. Напишем функцию, которая:

function find_and_show(userid) {
 //  если сотрудник есть в базе
 if (userid in people) {
     // ставит ему его фотографию
     photo.src = userid + '.png'
     // вписывает его фамилию имя и отчество
     username.textContent =  userid + ' ' + people[userid].username
     // указывает год поступления
     year.textContent = 'Год поступления: ' + people[userid].year
     // уничтожает список навыков предыдущей карточки. Проверьте, что будет, если эту строку убрать!
     keyfeatures.innerHTML = ''
     // предпочтительнее обойти список циклом и удалить элементы с помощью removeChild
     // добавляет навыки в список
     let li;
     // обходит хранящийся в базе список навыков
     for (let todo of people[userid].keyfeatures) {
         // для каждого навыка создает элемент списка
         li = document.createElement('li')
         // вписывает навык
         li.textContent = todo
         // прикрепляет элемент к списку
         keyfeatures.appendChild(li)
     }
 } else {
     // Если сотрудника нет, в консоль выводится сообщение об ошибке
     console.error('Сотрудник ' + userid + ' не работает в организации.')
 }
}



Реализация: https://github.com/doublewera/module5_js_future_code/tree/main/Lesson14/Practicum2.


Итоги
Сегодня мы:

узнали, зачем нужно уметь динамически менять структуру и состав элементов на странице;
научились добавлять элементы в DOM, удалять ненужное;
разобрались, как копировать элементы и редактировать их стили;
изменяли параметры страницы, в том числе заголовок и иконку.
Теперь страница готова к реальной работе: 
может реагировать на действия пользователя и данные от сервера, обновляясь без перезагрузки.
