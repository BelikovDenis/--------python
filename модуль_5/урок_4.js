Урок №4. Функции: объявление и вызов. Стрелочные функции. Параметры и возвращаемые значения.


Учебные материалы


Функции — один из базовых и самых полезных инструментов программирования. 
Они позволяют структурировать код, избегать повторений и выполнять задачи по запросу. 
Мы уже сталкивались с функциями в Python. Теперь познакомимся с тем, как они работают в JavaScript.

JavaScript поддерживает несколько способов создания функций, 
включая привычное объявление через function и более компактный синтаксис стрелочных функций. 
Также в JavaScript у функций есть особенности,
 которые связаны с областью видимости и контекстом выполнения (this) 
 и которые важно понимать с самого начала.



Сегодня мы:

разберём, как объявлять и вызывать функции в JavaScript: 
с помощью ключевого слова function и в виде функциональных выражений;
узнаем, чем отличаются параметры и аргументы функции и как 
передавать значения внутрь функции;
научимся возвращать результат из функции с помощью оператора return;
познакомимся со стрелочными функциями (arrow functions): узнаем, 
как они устроены, чем отличаются от обычных и когда их удобно использовать;
обсудим, как работает контекст выполнения (this) в разных типах функций 
и какие ошибки чаще всего возникают при его неправильной трактовке.




Глоссарий к четвёртому занятию
Function (Функция)

Function — это блок кода, который выполняет определённую задачу и может быть вызван по имени. 
Функции помогают избежать повторения кода и делают его более структурированным.

Function declaration (Объявление функции)

Function declaration — это способ создания функции с помощью ключевого слова function, 
за которым следует имя функции, параметры и тело функции.

Return (Возвращение)

Return — это оператор, который используется для возвращения значения из функции обратно в место вызова.

Parameters (Параметры)

Parameters — это значения, которые передаются в функцию при её вызове. 
Они становятся локальными переменными внутри функции.

Arrow function (Стрелочная функция)

Arrow function — это сокращённый синтаксис для объявления функции в JavaScript; 
он использует стрелку =>. Стрелочные функции имеют компактную запись и отличаются поведением с this.

Callback function (Функция обратного вызова)

Callback function — это функция, 
переданная в другую функцию как аргумент и вызываемая по завершении работы основной функции.

Anonymous function (Анонимная функция)

Anonymous function — это функция, не имеющая имени, 
часто используется как аргумент в других функциях.






Функции — это «строительные блоки» программы. 
Они позволяют выносить повторяющийся код в отдельное место и вызывать его по мере необходимости.

В JavaScript, как и в других языках, функции помогают:

избегать дублирования кода;
делить задачи на небольшие, понятные шаги;
изолировать логику и управлять данными с помощью параметров и возвращаемых значений.
Мы уже знакомы с функциями в Python. Теперь посмотрим, 
как они реализованы в JavaScript и чем синтаксис и поведение могут отличаться.

1. Что такое функция в JavaScript?
Как мы помним, функция — это блок кода, 
который выполняет определённую задачу и может быть вызван в любой части программы. 
Функции помогают избежать дублирования кода, улучшая его читаемость и поддержку. 
Каждый раз, когда нужно выполнить одно и то же действие, 
можно просто вызвать функцию, вместо того чтобы повторно писать тот же код.

Пример из жизни. Представьте, что в вашей компании работает 500 сотрудников, 
и все они хотят сварить кофе. 
Чтобы не заставлять каждого вручную молоть зёрна и варить напиток у себя за столом, 
гораздо логичнее установить кофейные автоматы. 
Вы просто подаёте нужные ингредиенты — воду и капсулу — и получаете результат: готовую кружку кофе.

С функциями в программировании — то же самое. 
Ваша программа — это «офис», где нужно решать задачи. 
Вместо того чтобы повторять одни и те же действия в каждом участке кода, 
вы создаёте «автомат» — функцию, которая выполняет нужную операцию. 
Вы можете обращаться к ней сколько угодно раз, 
передавая входные данные и получая результат. Это делает код чище, короче и понятнее.


Рисунок 1
Функцию можно объявить с помощью ключевого слова function, 
за которым следует имя функции, список параметров в круглых скобках 
и тело функции в фигурных скобках.

Такая запись называется function declaration — объявление функции. 
Это как если бы вы заранее в офисе повесили табличку «Кофемашина здесь»: к ней можно подойти в любой момент.

Существует и другой способ — function expression. 
Вы говорите: «Вот эта штука сейчас станет кофемашиной». 
И только с этого момента к ней можно обращаться.

Примеры:

function greet(name) {
    console.log("Привет, " + name + "!");
}

greet("Таня");  // Выведет: Привет, Таня!
const greet = function() {
    console.log("Здравствуйте!");
};

greet(); // Работает аналогично, но сработает только после объявления
Здесь:

greet — имя функции.
name — параметр функции.
Функция выводит приветствие для переданного имени.
Остановимся подробнее на том, как правильно работать с параметрами функций в JavaScript.





2. Параметры и аргументы функции
Параметры — это «ингредиенты», которые мы передаем на вход функции. 
Внутри функции параметры становятся её локальными переменными. 
Рассмотрим простейший пример передачи в функцию двух числовых параметров.

Пример:

function add(a, b) {
    return a + b;
}

let result = add(3, 5);  // Передаем 3 и 5 как аргументы
console.log(result);  // Выведет: 8
Параметры a и b — это переменные, в которые записываются значения, переданные при вызове функции.
Оператор return используется для возврата результата работы функции.
Важно
В классических функциях доступен объект arguments, 
который содержит все переданные аргументы. 
Это полезно, когда функция должна обрабатывать неизвестное количество параметров.
Пример использования arguments:

function sumAll() {
    let sum = 0;
    for (let i = 0; i < arguments.length; i++) {
        sum += arguments[i];
    }
    return sum;
}

console.log(sumAll(1, 2, 3, 4));  // Выведет: 10




3. Возвращаемые значения
Функция может возвращать значения с помощью оператора return.

Такая запись довольно интуитивная, 
потому что во многих случаях значение функции мы хотим «запомнить» — присвоить 
какой-нибудь объявленной переменной.

Например, чтобы написать функцию умножения, 
нам хочется уметь писать выражения вида z = x * y. 
Чтобы суметь передать значение самой операции x * y переменной z, 
в теле функции нам необходимо воспользоваться ключевым словом return:

Пример:

function multiply(x, y) {
    return x * y;
}

let product = multiply(4, 6);  // Передаем 4 и 6 как аргументы
console.log(product);  // Выведет: 24

В этом примере функция multiply возвращает произведение двух чисел, которое сохраняется в переменной product.

JavaScipt предлагает также альтернативный 
и более лаконичный способ записи функций – стрелочные функции. Рассмотрим их отдельно.




4. Стрелочные функции (Arrow Functions)
Стрелочные функции (или arrow functions) ввели впервые в ES6. 
Это более компактный способ создания функций. 
Иногда их называют лямбда-функциями (если они выполняют одно действие).

Пример:

const add = (a, b) => a + b;

console.log(add(3, 7));  // Выведет: 10

Синтаксис стрелочной функции:

Вместо ключевого слова function используется стрелка =>.
Если функция состоит из одного выражения, фигурные скобки и return можно опустить.
Перейдём к сравнению стрелочных функций и обычных, чтобы лучше понять их различия. 
Одним из них будет различие в понимании ключевого слова this для стрелочных и классических функций.

5. Что такое this и отличия стрелочных и обычных функций
this — это специальная переменная в JavaScript, 
которая используется для доступа к текущему контексту выполнения функции. 
Проще говоря, значение this указывает на объект, в контексте которого была вызвана функция.

Контекст
Это окружение, в котором выполняется код, определяющее, 
к какому объекту или области видимости будет относиться this.
Как работает this?

Значение this может изменяться в зависимости от способа вызова функции. 
Рассмотрим основные сценарии.

Глобальный контекст
В глобальной области видимости значение this указывает на глобальный объект.
В браузерах глобальный объект — window.
В Node.js — global.
console.log(this);  // В браузере выведет объект window
Метод объекта
Если функция вызвана как метод объекта, this ссылается на этот объект.
const person = {
    name: "Alice",
    greet: function() {
        console.log(this.name);  // 'this' ссылается на объект person
    }
};

person.greet();  // Выведет: Alice
Обычная функция
При вызове обычной функции (не метода) значение this зависит от режима:
В строгом режиме (use strict) значение this равно undefined.
В обычном режиме значение this ссылается на глобальный объект.
function showThis() {
    console.log(this);
}

showThis();  // В обычном режиме: window (в браузере), в строгом режиме: undefined
Стрелочные функции
Стрелочные функции не имеют собственного this. 
Они используют значение this из внешнего контекста, в котором были созданы.
const person = {
    name: "Alice",
    greet: () => {
        console.log(this.name);  // 'this' берётся из внешнего контекста (глобального объекта)
    }
};

person.greet();  // Выведет: undefined (глобальный объект не имеет свойства name)
Важное разграничение употребления this для функций в JavaScript:

Обычные функции: this зависит от того, как функция была вызвана.
Стрелочные функции: this всегда наследуется из внешнего контекста.
Если вы хотите сохранить контекст, используйте bind, стрелочные функции или сохранение ссылки на this в переменной (let self = this).



6. Анонимные функции
Анонимные функции — это функции, которые не имеют имени.
 Они часто используются как параметры других функций или как обработчики событий.

Пример:

setTimeout(function() {
    console.log("This is an anonymous function");
}, 1000);
Анонимные функции полезны в случаях, когда функцию нужно использовать только один раз.

Функции — удобный инструмент, но и с ними случаются проколы. 
Вроде бы всё написано правильно, а результат странный. 
Почему undefined вместо числа? Куда делось значение, 
которое вы точно возвращали? Почему this вдруг указывает не туда?

Такие моменты знакомы почти каждому, кто начинал писать функции в JavaScript. 
Однако у вас есть преимущество: вы можете заглянуть вперёд и заранее узнать, 
где чаще всего спотыкаются другие. 
Это позволит вам избежать типичных ошибок или быстро их распознать и исправить, 
если что-то пошло не так.





Типичные ошибки
Тип ошибки 1: Вызов функции до её объявления (временная мёртвая зона)

Когда мы пытаемся вызвать функцию до её объявления с использованием let или const, 
возникает ошибка. Это связано отчасти с тем, 
что переменные попадают в временную мёртвую зону (TDZ) — область до инициализации переменной, 
которая уже существует, но доступ к которой невозможен.

Важно: let и const поднимаются во внешнюю область видимости (hoisting), как и var, 
но до момента фактического присваивания значения (инициализации) доступ к ним заблокирован TDZ.

Пример ошибки:

console.log(greet());  // Ошибка: greet is not a function
let greet = function() {
    return "Hello!";
};


Решение:
Объявляйте функции до их использования. Это особенно важно при использовании let и const.

let greet = function() {
    return "Hello!";
};
console.log(greet());  // Выведет: Hello!






Тип ошибки 2: Несоответствие количества аргументов

В JavaScript, если количество переданных аргументов функции 
не совпадает с количеством ожидаемых параметров, параметры, 
для которых не были переданы значения, получают значение undefined. 
Это может привести к результату NaN (Not-a-Number) или некорректному поведению программы.

Пример ошибки:

function sum(a, b) {
    return a + b;  // Попытка сложения, если b равно undefined, приведёт к NaN
}
console.log(sum(5));  // NaN, так как b не был передан

Решение:

Используйте значения по умолчанию для параметров, чтобы избежать ошибок с undefined.
Добавьте проверки на наличие значений у параметров перед выполнением операций.
Пример с использованием значений по умолчанию:

function sum(a = 0, b = 0) {
    return a + b;
}
console.log(sum(5));  // 5 (используется значение по умолчанию для b)
console.log(sum(5, 3));  // 8
Пример с проверкой значений:

function sum(a, b) {
    if (a === undefined || b === undefined) {
        return "Ошибка: недостаточно аргументов";
    }
    return a + b;
}
console.log(sum(5));  // Ошибка: недостаточно аргументов
console.log(sum(5, 3));  // 8







Тип ошибки 3: Ошибка с this в обычных функциях

Ошибка возникает, когда значение this в обычных функциях зависит от контекста вызова, 
а не от места их определения. Это может привести к ситуации, 
когда this указывает не на ожидаемый объект, а на глобальный объект, 
например window в браузере, global в Node.js или на undefined.

При передаче метода объекта (обычной функции) в другую переменную или функцию связь между методом 
и объектом теряется. В результате this перестаёт ссылаться на объект, 
которому метод принадлежит, а поведение функции становится неожиданным.

Пример ошибки:

function showName() {
    console.log(this.name);  // 'this' зависит от того, как вызвана функция
}

const person = {
    name: "Alice",
    showName: showName
};

person.showName();  // Alice — 'this' ссылается на объект person

const nameFunc = person.showName;
nameFunc();  // undefined — 'this' больше не связано с объектом person

Решение:

Использование стрелочных функций.
Стрелочные функции не имеют собственного значения this и наследуют его из внешнего контекста.
const person = {
    name: "Alice",
    showName: () => {
        console.log(this.name);  // 'this' унаследовано от внешнего контекста
    }
};
person.showName();  // undefined, потому что 'this' указывает на глобальный объект
Важно помнить, что стрелочные функции не всегда подходят, 
если необходимо явно привязать this к текущему объекту.

Привязка метода к объекту с помощью bind.
Метод bind позволяет явно привязать значение this к функции.
function showName() {
    console.log(this.name);
}

const person = {
    name: "Alice",
    showName: showName.bind(person)
};

const nameFunc = person.showName;
nameFunc();  // Alice
Использование метода внутри объекта без его передачи вне контекста.
Всегда вызывать метод объекта напрямую, чтобы сохранить правильный контекст.
const person = {
    name: "Alice",
    showName: function() {
        console.log(this.name);
    }
};

person.showName();  // Alice











Тип ошибки 4: Ошибка с параметрами стрелочных функций

Ошибка:
Если в стрелочной функции забыть поставить скобки при одном параметре, 
это приведёт к синтаксической ошибке.

Пример ошибки:

const square = x => return x * x;  // Ошибка: 'return' не может быть использован без фигурных скобок
Решение:
Используйте фигурные скобки, если хотите использовать return,
 или уберите return, если это однострочная функция.

const square = x => { return x * x };  // Правильно
Или для однострочной записи:

const square = x => x * x;  // Правильно








Тип ошибки 5: Ошибка с возвращаемыми значениями

Ошибка:
Если функция не возвращает значение, но вы пытаетесь использовать результат, 
это может привести к ошибке или неопределённому результату.

Пример ошибки:

function add(a, b) {
    console.log(a + b);  // Не возвращает значение
}

let result = add(2, 3);
console.log(result);  // undefined

Решение:
Убедитесь, что функция возвращает значение с помощью оператора return.

function add(a, b) {
    return a + b;  // Возвращает сумму
}

let result = add(2, 3);
console.log(result);  // 5












Практикум
Задача 1: Функция для нахождения наибольшего числа

Ситуация:

Представьте, что вам нужно создать функцию, 
которая будет сравнивать два числа и возвращать наибольшее из них. 
Это полезная задача, которая поможет вам отработать навык взаимодействия с функциями, 
параметрами и возвратом значений.

Задача:

Напишите функцию, которая принимает два числа и возвращает наибольшее из них.
Если числа одинаковые, функция должна вернуть сообщение «Числа одинаковы».

Шаги реализации:

Создайте функцию, которая принимает два числа в качестве параметров.
Используйте условие для сравнения чисел.
Возвращайте сообщение или наибольшее число, если числа одинаковы.
Решение:

function findMax(a, b) {
    if (a > b) {
        return a;
    } else if (a < b) {
        return b;
    } else {
        return "Числа одинаковы";
    }
}

console.log(findMax(5, 3));  // 5
console.log(findMax(3, 3));  // Числа одинаковы






Задача 2: Функция для проверки чётности числа

Ситуация:

Многие приложения требуют проверки числа на чётность или нечётность. 
Написание такой функции поможет вам понять, как работать с числами и условиями в JavaScript.

Задача:

Напишите функцию, которая проверяет, является ли переданное число чётным. 
Если число чётное, функция должна вернуть «Чётное», если нечётное — «Нечётное».

Шаги реализации:

Создайте функцию, которая принимает одно число как параметр.
Используйте оператор % для проверки чётности числа.
Возвращайте соответствующее сообщение.
Решение:

function checkEvenOdd(number) {
    if (number % 2 === 0) {
        return "Чётное";
    } else {
        return "Нечётное";
    }
}

console.log(checkEvenOdd(4));  // Чётное
console.log(checkEvenOdd(7));  // Нечётное


Эти задачи помогут вам попрактиковаться в создании функций, 
работе с параметрами и условиями, а также научат правильно возвращать значения из функций.

Итоги:
На этом занятии мы изучили основы работы с функциями в JavaScript:

узнали, что такое функция и как её создавать с помощью ключевого слова function;
познакомились с синтаксисом стрелочных функций и отметили их преимущества перед обычными функциями;
научились объявлять функции, которые выполняют задачи: нахождение наибольшего числа, проверка чётности числа.