Урок №5. Callback-функции.

Учебные материалы

Callback-функции — это ключевая концепция, 
без которой невозможно представить работу современных веб-приложений. 
Когда вы работаете в браузере, многие действия (например, 
ожидание ответа от сервера или клик пользователя по кнопке) занимают время. 
При этом интерфейсу нужно оставаться отзывчивым: 
пользователь не должен ждать завершения всех процессов, чтобы продолжить взаимодействие с сайтом.

Это можно сравнить с тем, что вы ждёте закипания воды, не отходя от чайника. 
Намного удобнее поставить таймер и заняться чем-то ещё. 
Когда вода закипит, прозвенит сигнал. 
Callback-функции работают по такому же принципу: вы «задаёте» действие на будущее, 
а JavaScript тем временем выполняет что-то ещё.

Чтобы это реализовать, JavaScript использует особые механизмы работы с асинхронным кодом. 
Один из них — callback-функции. Они позволяют «запланировать» выполнение кода на тот момент, 
когда произойдёт нужное событие: данные загрузятся, 
таймер сработает или пользователь совершит действие.

Сегодня мы:

разберем, что такое callback-функции и как передавать одну функцию в другую в качестве аргумента;

узнаем, чем отличаются синхронные и асинхронные вызовы callback-функций и в каких ситуациях используется каждый из них;

научимся использовать callback-функции для обработки событий, таймеров и задержек без блокировки основного потока кода;

потренируемся передавать данные в callback-функции и возвращать результаты из них.



Глоссарий к пятому занятию

Callback function (Функция обратного вызова)
Callback function — это функция, 
которая передаётся в другую функцию в качестве аргумента и вызывается внутри 
этой функции по завершении её выполнения. 
Используется для обработки результата или выполнения действия после завершения асинхронной операции.

Asynchronous (Асинхронный)
Asynchronous — это тип выполнения, при котором код может продолжать совершение других операций, 
не дожидаясь завершения текущей. Это важно при работе с операциями ввода/вывода, например запросами к серверу.

Argument (Аргумент)
Argument — это значение, которое передаётся в функцию при её вызове. 
Аргументы могут быть переменными, литералами или функциями.

Higher-order function (Функция высшего порядка)
Higher-order function — это функция, 
которая может принимать другие функции в качестве аргументов или возвращать их в качестве результата. 
В JavaScript могут быть функции высшего порядка, что позволяет использовать callback-функции.

Execution context (Контекст выполнения)
Execution context — это объект со значениями переменных, 
this и цепочкой областей видимости, в рамках которых выполняется код функции (в том числе callback).

Synchronous (Синхронный)
Synchronous — это тип выполнения, 
при котором операции выполняются одна за другой и каждая последующая ожидает завершения предыдущей.

Эти термины помогут вам лучше понять принципы работы с callback-функциями и асинхронным кодом в JavaScript.




Что такое callback-функция?
Callback-функция — это функция, которую мы передаём другой функции в качестве аргумента, 
чтобы она была вызвана позже (в нужный момент). 
Это позволяет точно управлять порядком выполнения кода и выстраивать логичные сценарии действий в программе.

Как это работает на практике:

Одна функция принимает другую в качестве параметра.
Внутри первой функции в нужный момент вызывается переданная функция. Это и есть callback.
Таким образом мы можем «связать» действия: сначала завершается одно — затем запускается другое.
Что значит «в нужный момент»? Существует два типа вызова callback-функций: синхронный и асинхронный.


Синхронный вызов
Синхронный callback вызывается прямо внутри функции: в том месте, где он прописан. 
Основная функция завершает своё выполнение только после того, как отработал переданный callback.

Пример:

function greet(callback) {
    callback();
}

greet(() => {
    console.log("Сразу всем привет!");
});

...
Здесь функция greet вызывает callback немедленно. 
JavaScript не перейдёт к следующей строке до тех пор, пока не выполнится callback().

Асинхронный вызов

Второй тип — асинхронный. Здесь callback срабатывает в ответ на событие: таймер, 
клик мыши, ответ от сервера и т. д. Сам JavaScript не ждёт окончания работы callback: 
он продолжает выполнять остальной код, а callback запускается позже автоматически, с помощью механизма event loop.

Представьте официанта. Если он принимает заказ и ждёт на кухне, пока его приготовят, 
— это синхронный подход. А если он передаёт заказ и продолжает обслуживать других гостей — это асинхронный подход. 
Callback-функция — это инструкция на случай, что делать, когда блюдо будет готово.

Пример:

setTimeout(() => {
    console.log("Мы подождали одну секунду, и теперь всем привет");
}, 1000);

console.log("Этот код выполнится сразу");

В этом примере callback передаётся в setTimeout и вызывается через секунду. 
Несмотря на задержку, основной код (строка с console.log(«Этот 
    код выполнится сразу»)) продолжает выполняться. Это и есть асинхронность.


    2. Почему callback-функции так важны?
Давайте представим более реальный пример веб-программы, где нам необходимо подгрузить данные с сервера.

Без callback-функций мы бы подождали, пока данные загрузятся, 
и дальше стали бы обрабатывать страницу и данные по необходимости. 
Тогда программа висла бы во время загрузки данных с сервера.

Благодаря callback JavaScript продолжает выполнять другие задачи, а по завершении операции вызывает нужную функцию.

Такой подход особенно полезен при асинхронных операциях: загрузке данных, работе с таймерами и обработке событий.

Пример асинхронной операции с callback-функцией:

function fetchData(callback) {
    setTimeout(function() {
        console.log("Данные загружены!");
        callback("Вот ваши данные");
    }, 2000);  // Симуляция задержки 2 секунды
}

function displayData(data) {
    console.log(data);
}

fetchData(displayData);  // Выведет "Данные загружены!" и затем "Вот ваши данные"
Здесь fetchData имитирует загрузку данных. 
После завершения операции вызывается callback-функция displayData для обработки результата.

3. Функции высшего порядка и передача callback
С помощью callback мы можем создавать функций высшего порядка, то есть те, 
что принимают другие функции как аргументы или возвращают их в качестве результата.

На примере посмотрим, что имеется в виду и как это может быть полезно:

Пример:

function calculate(a, b, operation) {
    return operation(a, b);
}

function add(x, y) {
    return x + y;
}

console.log(calculate(5, 3, add));  // Выведет: 8
Здесь calculate — это функция высшего порядка, 
которая принимает функцию add в качестве параметра и использует её для выполнения вычислений.

В этом примере (благодаря callback) нам не нужно прописывать логику ветвления 
для функции-калькулятора calculate, чтобы выполнить правильную операцию с числами-аргументами.

Вместо этого мы просто получаем на вход calculate и функцию сложения add целиком в качестве аргумента-callback.

Callback Hell
Callback-функции могут быть полезны в разных ситуациях (как в примере выше), 
но в то же время могут сделать код неудобным и сложным для восприятия. 
Например, трудно читать вложенные структуры, где в функцию передается callback, 
в этот callback передается еще callback и т. д. Такая «пирамида» называется callback hell.

Это похоже на матрёшку: вы открываете одну функцию, внутри неё другая, 
потом ещё — и так до бесконечности. Такой код становится тяжёлым для чтения и сопровождения.

Пример callback hell:

asyncFunction1(function(result1) {
    asyncFunction2(result1, function(result2) {
        asyncFunction3(result2, function(result3) {
            console.log("Финальный результат: " + result3);
        });
    });
});
Решением проблемы являются промисы (Promises) и async/await, 
которые делают асинхронный код более линейным и читаемым, 
но эти механизмы мы разберём подробнее на следующих уроках. 
А сейчас перейдем к разбору типичных ошибок.







Типичные ошибки
Тип ошибки 1: Ошибка при вызове функции без передачи callback
Ошибка:
Когда мы пытаемся вызвать функцию, ожидающую callback, но не передаем её, 
это приводит к ошибке: аргумент функции не передан.

Пример ошибки:

function fetchData(callback) {
    console.log("Fetching data...");
    callback("Data fetched");
}

fetchData();  // Ошибка, callback не передан
Решение:
Необходимо убедиться, что при вызове функции, которая ожидает callback, 
мы передаём его как аргумент. Для тестирования можно логировать работу callback:

function fetchData(callback) {
    console.log("Fetching data...");
    callback("Data fetched");
}

fetchData(function(result) {
    console.log(result);  // Data fetched
});







Тип ошибки 2: Ошибка с асинхронными функциями и последовательностью
Ошибка:
Если в callback-функции выполняются асинхронные операции, 
то результат может быть доступен не сразу, а в момент, когда возвращается результат callback.

Если при вызове результата выполнения callback мы пытаемся использовать данные до их получения, 
то ломаем временной континуум и получаем undefined,

Пример ошибки:

function fetchData(callback) {
    setTimeout(function() {
        console.log("Data fetched!");
        callback("Here is your data");
    }, 2000);
}

function processData() {
    const result = fetchData(function(data) {
        console.log(data);  // Будет выполнено не сразу
    });
    console.log(result);  // undefined, так как fetchData асинхронно
}

processData();
Решение:
Убедимся, что действия с данными, полученными из асинхронных операций, выполняются внутри callback-функции.

function fetchData(callback) {
    setTimeout(function() {
        console.log("Data fetched!");
        callback("Here is your data");
    }, 2000);
}

function processData() {
    fetchData(function(data) {
        console.log(data);  // Теперь данные будут доступны
    });
}

processData();






Тип ошибки 3: Проблемы с областью видимости переменных внутри callback
Ошибка:
Внутри callback часто используются внешние переменные. При этом, если в функции, 
в которую передаётся callback, есть локальная переменная с таким же именем, она не будет автоматически доступной, 
так как callback «смотрит» на то, что было видно в момент его определения, а не на локальные переменные вызывающей функции.

Пример неожиданного поведения:

let name = "Алиса"; // Внешняя переменная

function greet(callback) {
    let name = "Миелофон";  // Локальная переменная внутри greet
    callback();             // Но она НЕ попадает в зону видимости callback
}

greet(function() {
    console.log(name);  // Выведет "Алиса", потому что callback "видит" только внешнюю переменную
});
Решение:
Если мы хотим, чтобы callback использовал переменную, объявленную внутри greet, 
нам нужно явно передать эту переменную как параметр в callback:

function greet(name, callback) {
    callback(name);  // Передаём значение внутрь callback
}

greet("Миелофон", function(name) {
    console.log(name);  // Выведет: Миелофон
});





Тип ошибки 4: Ошибка с асинхронностью в callback-функциях (callback hell)
Ошибка:
Выше мы уже описывали нежелательное поведение, 
когда callback многократно передаётся один в другой (callback hell). 
Технически это не ошибка, но код становится сложным для чтения и отладки, 
и не всегда понятно, что в нём происходит.

Пример callback hell:

fetchData(function(result1) {
    processData(result1, function(result2) {
        saveData(result2, function(result3) {
            console.log("All done!");
        });
    });
});
Решение:
В дальнейшем по ходу курса мы разберём механизмы Promises или async/await, 
которые позволяют избавиться от глубоких вложенностей, сохраняя при этом асинхронное поведение.

fetchData()
    .then(result1 => processData(result1))
    .then(result2 => saveData(result2))
    .then(() => console.log("All done!"))
    .catch(error => console.error(error));














Практикум
Задача 1: Выполнение математической операции с использованием callback-функции
Аутентичная ситуация:
Представьте, что вы разрабатываете калькулятор, 
в котором пользователи смогут задавать произвольные операции для выполнения. 
Например, сложение, умножение или нахождение разности между двумя числами. 
Для этого мы используем callback-функцию, которая задаёт конкретную операцию.

Задача:
Напишите функцию calculate, которая принимает два числа, и callback-функцию, 
выполняющую операцию. Функция должна возвращать результат этой операции.

Шаги реализации:

Создайте функцию calculate, принимающую три аргумента: два числа и callback-функцию.
Вызовите callback-функцию с переданными числами и верните результат.
Для проверки создайте несколько callback-функций, реализующих сложение, вычитание и умножение.
Решение:

function calculate(a, b, operation) {
    return operation(a, b);
}

const add = (x, y) => x + y;
const subtract = (x, y) => x - y;
const multiply = (x, y) => x * y;

console.log(calculate(5, 3, add));        // 8
console.log(calculate(10, 7, subtract));  // 3
console.log(calculate(4, 6, multiply));   // 24











Задача 2: Фильтрация массива с использованием callback-функции
Аутентичная ситуация:
В приложении для управления списком задач нужно фильтровать элементы массива. 
Например, получить только завершённые задачи или задачи с высоким приоритетом. 
Для этого удобно использовать callback-функцию, определяющую критерии фильтрации.

Задача:
Напишите функцию filterArray, которая принимает массив и callback-функцию. 
Функция должна возвращать новый массив, содержащий только те элементы, 
которые удовлетворяют условию callback-функции.

Шаги реализации:

Создайте функцию filterArray, принимающую массив и callback-функцию.
Итерируйте по массиву, проверяя каждый элемент с помощью callback-функции.
Верните новый массив, содержащий элементы, которые удовлетворяют условию.
Решение:

function filterArray(array, criteria) {
    const result = [];
    for (let item of array) {
        if (criteria(item)) {
            result.push(item);
        }
    }
    return result;
}

const tasks = [
    { title: "Task 1", completed: true },
    { title: "Task 2", completed: false },
    { title: "Task 3", completed: true }
];

const completedTasks = filterArray(tasks, task => task.completed);

console.log(completedTasks);
// [{ title: "Task 1", completed: true }, { title: "Task 3", completed: true }]






Итоги:
На этом занятии мы изучили основы работы с callback-функциями в JavaScript:

узнали, что такое callback-функция и как она передаётся в другую функцию в качестве аргумента, 
чтобы быть вызванной по завершении операции;
освоили концепцию использования callback-функций для обработки асинхронных операций, 
таких как запросы к серверу или обработка пользовательского ввода;
узнали, что callback-функции являются примером функций высшего порядка, 
которые могут принимать другие функции в качестве аргументов.