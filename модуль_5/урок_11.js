 Урок №11. Объекты в JavaScript.

Учебные материалы

На прошлых занятиях мы познакомились со строками и массивами — научились хранить текст и 
создавать упорядоченные списки данных. 
Это уже открыло для нас множество возможностей, например, 
мы могли собрать информацию о человеке в массив: фамилию, имя, отчество, 
дату рождения и даже логическое значение, женат ли он.

Но есть загвоздка: запомнить, что именно хранится под индексом 2, не так-то просто. 
А если полей станет ещё больше, и вовсе легко запутаться.

Сегодня мы попробуем хранить такую информацию удобнее:

Познакомимся с объектами и принципом поиска значения по ключу — основой большинства современных структур данных.
Узнаем, как извлекать нужную информацию по имени поля (ключа), а не по номеру.
Разберёмся, какие типы данных можно использовать в качестве ключей и почему не любые из них подойдут.
Научимся добавлять, изменять и удалять свойства объекта.
Поработаем с реальными задачами: создадим объект книги, соберём массив товаров и посчитаем стоимость заказа.







Глоссарий к одиннадцатому занятию
Object (Объект)

В объектно-ориентированном программировании (ООП) объект — это экземпляр класса, то есть переменная сложного типа. 
Мы узнаем об этом подробнее на занятии 25, 
но уже сейчас их можно использовать в JavaScript без предварительного проектирования классов. 
Объекты позволяют описывать реальные предметы и явления через набор свойств и действий, 
то есть через переменные и функции.

Field (Поле)

Поле в ООП — это переменная внутри объекта. 
Она содержит данные, характеризующие объект, например: имя, возраст или координаты.

Method (Метод)

Метод в ООП — это функция, принадлежащая объекту. 
С его помощью объект может выполнять действия, например: выводить сообщение, 
рассчитывать стоимость или переключать режим.

Key (Ключ)

Ключ — это имя свойства в объекте. 
С его помощью мы можем обратиться к нужному значению внутри объекта. 
Ключ всегда записывается как строка. Например, в паре name: "Иван" слово name — это ключ.

Осторожно! Слово key имеет много значений. 
В программировании оно может означать и клавишу на клавиатуре, 
и элемент в шифровании, и часть ассоциативной структуры данных.

Value (Значение)
Значение — это содержимое, связанное с ключом. 
В JavaScript значением может быть что угодно: число, строка, массив, объект или даже функция. 
Например, в name: "Иван" строка "Иван" — это значение.

Hash (Мешанина, крошево)

Хеш — это специальное число, которое вычисляется по строке или другому объекту, 
чтобы ускорить поиск и сравнение. Например, строку "JavaScript" можно преобразовать в число вроде 875401, 
и уже его сравнивать с другими хешами. Это позволяет не перебирать символы по одному. 
Мы не будем углубляться в алгоритмы хеширования, 
в конце занятия есть ссылки, если захочется узнать об этом больше.




Программисты не стали изобретать для кода (искусственного языка) новые слова 
 они заимствовали слова из реальной жизни (естественного языка) и дали им новые технические смыслы. 
 Казалось бы, удобно — всё уже знакомо. Но на практике это создаёт трудности. 
 Когда мы слышим слова «объект», «ключ» или «метод», мы представляем стол, 
 замок или способ действовать, а в коде они означают нечто иное.

Историческая справка — клавиша!

Все знают, что такое клавиша. Но знаете ли вы, откуда произошло это слово?

Оно образовано от латинского clavis, что означает «ключ». 
Причём здесь ключ? — спросите вы. 
Всё просто: в старинных музыкальных инструментах, например, в органе, 
клавиши действительно выполняли функцию ключа — они открывали доступ воздуху в органные трубы. 
А трубы, в свою очередь, отвечали за извлечение звуков разной высоты.

На органах X века клавиши часто подписывались буквами латинского алфавита, 
чтобы музыкант ориентировался не на ноты, а на последовательность букв. 
Это упрощало игру и позволяло не путаться в звуковом ряду.

Позже такая буквенная запись перешла с инструмента на бумагу. 
Так появилась музыкальная нотация, где в начале строки ставился ключ — специальный символ, 
определяющий, какие ноты будут звучать на каких линейках. 
Слово сохранилось, а вместе с ним и его смысл: ключ как способ расшифровки, открытия и управления.

Давайте начнём не с синтаксиса, а с образов. 
Сначала — о том, что мы вообще называем объектом в жизни, 
а потом — как это понятие используется в программировании.

Что такое объект?
Вы сидите за столом и слушаете или читаете урок. 
Стол твёрдый, его можно потрогать. 
Урок — нет, но его можно услышать, прочитать, запомнить или придумать самостоятельно. 
Ещё в школе нас приучают делить мир на предметы и явления. 
У стола есть такие характеристики, как материал, высота, ширина и длина. 
У урока тоже есть длина — его продолжительность. А ещё содержание, цель и результат.

Согласитесь, перечисленные критерии не позволили бы с точностью воссоздать именно этот стол. 
Но они помогли бы подобрать подходящий, например, достаточно высокий, 
чтобы за ним можно было удобно сидеть, и с деревянной поверхностью, 
на которой работала бы мышка. Мы описали лишь те свойства, которые важны для конкретной задачи.

То же самое происходит в программировании. 
Когда мы описываем объект, то не стремимся перечислить всё возможное. 
Мы выбираем только те характеристики и действия, которые имеют значение в данном контексте. 
Такое упрощённое, но достаточное описание и называют объектом.

Более того, с объектами можно не только хранить свойства, но и выполнять действия. 
Если вы слушаете урок в записи, то можете остановить воспроизведение, перемотать или начать сначала. 
Если стол стоит не там, где удобно, вы можете его передвинуть. 
Эти действия — то, что объект может делать. 
В программировании их называют методами, а характеристики — полями.

Рассмотрим простой пример. Машина проехала путь s с постоянной скоростью v. 
Сколько времени она потратила на дорогу?

let s = 250;  // km
const v = 50; // km/h
let t = s / v;
console.log(t, " часов");

А если у нас много машин? 
У каждой свои скорость, расстояние и время в пути. 
Можно создать три отдельных массива: speed, distance, time. 
Но тогда легко запутаться, где чья скорость и сколько ехала именно вторая машина.

А можно сделать проще: создать объект «машина», который сам хранит свои параметры, 
и собрать массив уже из таких объектов. Каждая машина будет знать свои скорость, путь и время. 
Так устроено большинство программ: они работают не с абстрактными наборами данных, 
а с объектами, описывающими реальные сущности.

Мы уже работали с объектами
Один из первых объектов, с которым мы познакомились в JavaScript, это console. 
Он умеет выводить сообщения и ошибки. Например:

console.log("Привет, мир!");
console.error("Что-то пошло не так...");
Здесь log и error — это методы объекта console, то есть действия, которые он может выполнять.

Как создать свой объект?
В JavaScript есть как минимум три способа создать объект. Рассмотрим каждый по порядку.

1. Некоторые типы — уже объекты
Строки и массивы — это тоже объекты. 
У них есть свойства (например, length) и методы (например, slice).

let s = "А я всё понял!";
console.log(s.length);
console.log(s.slice(4));

2. Через конструктор и оператор new
Можно использовать встроенные классы JavaScript, например, для получения текущей даты:

let dt = new Date();
console.log(dt.getDate());
Здесь Date — встроенный объект, а new создаёт его новый экземпляр.

3. Через литерал объекта
Самым удобным способом будет использование литерала объекта — фигурные скобки с парами ключ: «значение».

const human = {
    "name": "Константин",
    "date of birth": new Date(2000, 1, 1),
    "height": 185,
    "welcome": () => console.log("Привет!")
};
Объекты хороши тем, что позволяют объединить связанные данные в одну сущность, 
как папка объединяет документы, шкаф — вещи, а посудомойка — тарелки.

Синтаксис объекта
Объект создаётся с помощью фигурных скобок {}. Внутри пишутся пары:

ключ — имя свойства (обычно строка);
значение — данные или функция;
пары отделяются запятыми;
между ключом и значением ставится двоеточие.
Пример:

const book = {
  title: "1984",
  author: "Джордж Оруэлл"
};
JavaScript автоматически превращает ключи в строки. 
Поэтому, если имя ключа — допустимое имя переменной, кавычки можно опустить:

const example = {
  name: "Анна",     // ключ без кавычек
  "full name": "Анна Иванова" // ключ с пробелом — кавычки обязательны
};
Обратите внимание! Вычисляемые выражения не могут быть ключами, и, например, 
массив нельзя использовать как ключ без приведения к строке.

const human = {
    name: "Константин",
    "date of birth": new Date(2000, 1, 1),
    height: 185,
    welcome: () => console.log("Привет!")
};
Объект в JavaScript — это не просто хранилище данных. 
Он гибок: его можно дополнять, обновлять и даже очищать. 
В отличие от массивов, где структура фиксирована, объект можно менять на ходу, 
добавляя новые свойства, редактируя старые и удаляя ненужные.

Давайте посмотрим, как это работает на практике.

Как изменять или добавлять поля?
В JavaScript добавление и изменение свойств объекта происходит одинаково — с помощью обычного присваивания по ключу:

через точку;
через квадратные скобки;
с помощью метода OObject.defineProperty(obj, prop, descriptor) (подробно — в следующем занятии).
Если ключ уже существует, значение будет заменено. Если ключа не было, будет создана новая пара.

human["name"] = "Константин Константинович";
human.work = "Слесарь-сантехник";
Выведем объект в консоль и посмотрим, как изменился Константин.

«Константин, как ты мог?… Human же был константой?!»

Мы подошли к очень важному моменту. Все переменные в JavaScript, которые хранят простые типы, 
создаются на стеке — особой области памяти для относительно продолжительного содержания данных 
с сохранением порядка их создания.


Рисунок 1
А сложные типы данных (массивы, объекты, функции) хранятся по ссылке в куче — области для короткоживущих данных, 
порядок расположения которых не важен, важны только адреса.


Рисунок 2
Это означает, что при создании новой переменной возможны два сценария:

Если переменной присваивается новое значение — например, строка, число, 
массив или объект — в памяти выделяется место под это значение, 
а переменная получает адрес первого байта этой области.

Если же переменная создаётся как результат присваивания уже существующей переменной, 
никакой новой области не создаётся — просто записывается тот же адрес. 
Так появляются две ссылки на один и тот же объект.


Рисунок 3

Рисунок 4

Рисунок 5
Это как два пульта от одного телевизора. Если вы подключили второй пульт, вы не создали новый телевизор, 
а просто получили ещё один способ управлять тем же самым устройством. 
Хотите переключить канал? Пожалуйста, с любого пульта.

То же самое происходит и с объектами. Если вы присвоили объект другой переменной, 
то теперь у вас две переменные, которые управляют одним и тем же объектом. 
Изменение через одну ссылку будет видно и через другую.

Можно представить это и так: переменные — это бумажки с адресами, а объект — дом. 
Если у вас две бумажки с одним и тем же адресом, вы оба раза придёте в одно и то же место.


Рисунок 6
А что насчет const?

Когда мы используем спецификатор const, мы фиксируем саму ссылку, 
то есть не можем переназначить переменную на другой объект. 
Но содержимое объекта (его поля) менять можно. 
Константа в данном случае — это адрес, а не «всё, что по этому адресу».


Рисунок 7
А если мы хотим запретить любые изменения?

Тогда используем специальный метод:

Object.freeze(human);
После этого любые попытки изменить или удалить свойства этого объекта будут проигнорированы. 
Попробуйте: переименуйте Константина и проверьте, что получится.

Удаление ключей

Допустим, Константин уволился и больше не работает сантехником. Удалим это свойство:

delete human.work;
Но, если вы предварительно заморозили объект с помощью Object.freeze(), удаление не произойдёт. 
Попробуйте на своём примере: создайте объект, заморозьте его, а потом попытайтесь что-нибудь удалить.

Присвоить. Буквально

Если мы присваиваем один объект другому:

let foreign_car = {
  brand: 'Mercedes'
};
let local_car = foreign_car;
local_car.brand = 'Lada';
console.log(foreign_car);

Мы не создаём копию: обе переменные ссылаются на один и тот же объект. 
Изменения через одну переменную будут видны через другую.

Как сделать копию объекта?

Object.assign позволяет создать новый объект или объединить их. Вторую его особенность мы рассмотрим в следующий раз.

// куда скопировать, что скопировать - не запускайте этот код!
let result = Object.assign(target, source);
Пример:

let foreign_car = {
  brand: 'Mercedes'
};
let local_car = Object.assign({}, foreign_car);
local_car.brand = 'Lada';
console.log(foreign_car, local_car);
Теперь это два разных объекта: один — «Мерседес», другой — «Лада».

Какие ключи можно использовать?

В JavaScript ключи всегда преобразуются в строки. 
Даже если мы укажем число, булевое значение или null, они будут автоматически превращены в строки:

const example = {
  10: "десять", // станет "10"
  true: "истина", // станет "true"
  null: "ничего" // станет "null"
};
Это может удивить, особенно если вы раньше работали с Python, где в словаре (dict) можно использовать ключи разных типов.

Сравнение на равенство
Сравнение на равенство в JavaScript работает только для примитивных типов: чисел, 
строк, булевых значений и т.д. Это мы уже встречали на первом занятии.

А вот с объектами всё не так просто. 
При сравнении двух объектов оператор === проверяет не содержимое, а ссылки, то есть равны ли адреса, 
на которые ссылаются переменные. Даже если два объекта выглядят одинаково, 
они считаются разными, если были созданы независимо.

Чтобы сравнить содержимое объектов, нужно писать специальную функцию. 
Казалось бы, задача типовая, но до сих пор не существует универсального решения, 
которое подходило бы для всех случаев.

Почему так?

Потому что в реальной жизни нам не всегда нужна полная идентичность. 
Вспомните стол в начале занятия. Когда вы за ним сидите, важно, чтобы он был подходящей высоты, 
с удобной поверхностью и достаточно широким, чтобы поместился ноутбук. 
Вас не интересует, сколько у него точно миллиметров по краю или кто его изготовил.

Так же и с объектами: иногда достаточно сравнить 2-3 свойства, а остальное можно опустить. 
Полное совпадение требуется далеко не всегда.

Как написать функцию для глубокого сравнения объектов, обсудим в одном из следующих занятий.

Массив объектов. Вложенные объекты
Объекты можно объединять в массивы. 
Это удобно, когда у вас есть несколько однотипных сущностей, например, студентов:

const students = [
  {
    name: 'Ася',
    marks: [5,4,5,3,4,3,5],
  },
  {
    name: 'Вася',
    marks: [2,3,2,4,3,3,4,2]
  }
];
Чтобы узнать имя первого студента, достаточно обратиться к первому элементу массива, а затем — к полю name:

console.log(students[0].name);
Такой приём является одним из самых частых в практике — массив объектов с доступом по индексу и по ключу.

Но и это ещё не всё. Объекты могут содержать другие объекты, и таких уровней вложенности может быть сколько угодно:

const car = {
  coordinates: {
    lat: 55,
    lon: 37
  },
  velocity: 50
};
console.log(car.coordinates.lat);
Здесь внутри объекта car находится вложенный coordinates, 
а внутри него — уже конкретные значения широты и долготы. 
Мы пошагово добрались до нужного значения с помощью цепочки точек.

Как вы понимаете, это только начало. 
Вы получили инструмент, с помощью которого можно строить многоуровневые конструкции, 
способные описывать самые разные и сложные объекты — от заказов в интернет-магазине до данных о погоде в разных странах.

Использование методов
Вернёмся к нашему знакомому:

const human = {
"name": "Константин",
"date of birth": new Date(2000, 1, 1),
"height": 185,
"welcome": () => console.log("Привет!"),
"goodbye": function() {console.log('Пока!')}

Как мы могли заметить, ключу welcome соответствует стрелочная функция, 
а ключу goodbye — обычная. И та, и другая — это методы объекта, то есть действия, которые он может выполнять.

Методы — это просто функции, которые приписаны объекту как одни из его свойств. 
Мы можем вызывать их, добавлять, менять и даже присваивать после создания объекта.

human.welcome(); // "Привет!"
human.goodbye(); // "Пока!"




Вы уже умеете создавать объекты, добавлять в них поля, вызывать методы. 
Всё выглядит просто, пока не появляются первые баги. 
Давайте заглянем туда, где чаще всего спотыкаются новички. 
Ошибки простые, но коварные — разберём их заранее, чтобы не наступать на эти грабли в коде.





Типичные ошибки
Тип ошибки 1: Использование точки для доступа к ключу с пробелом
При попытке обратиться к ключу объекта, содержащему пробелы, через точку возникнет синтаксическая ошибка.

Пример ошибки:

const cat = {
  name: 'Мурзик',
  'любимая еда': 'рыбка'
};

console.log(cat.любимая еда) // Ошибка!
Объяснение:

JavaScript ожидает после точки имя свойства (ключ) без специальных символов — по сути, 
идентификатор как название переменной или функции. 
Ключ с пробелом можно получить только через квадратные скобки.

Решение:

cat["любимая еда"]; // Правильно







Тип ошибки 2: Сравнение объектов с помощью операторов == и ===
Даже если объекты выглядят одинаково, они считаются разными, потому что в памяти это различные сущности.

Пример ошибки:

const a = { name: "Алиса" };
const b = { name: "Алиса" };
const c = b;

console.log(a === b); // false - разные объекты
console.log(c === b); // true - разные ссылки на один объект
Объяснение:

Даже если объекты выглядят одинаково, они могут быть разными сущностями в памяти. Сравниваются не значения, а ссылки на них.

Решение:

Не используйте == и === длля сравнения объектов на равенство. 
В практикуме приведено решение этой задачи и оно значительно сложнее, чем в одну строку.

Для сравнения содержимого объектов нужно проверять каждое свойство отдельно.








Практикум
Задача 1: Ассоциативный массив
Ситуация:
Вам стало жалко библиотекаря, и вы решили помочь: написать простую программу, 
которая подскажет, где искать нужную книгу на полке.

Задача:
Создайте структуру данных, которая позволяет быстро определить местоположение книги по её названию.

Шаги реализации:

Выбрать подходящий тип данных, объект отлично подойдёт.
Написать для примера 3 книги.
Показать библиотекарю, как добавлять и менять книги в каталоге.
Реализация:

const catalog = {
  'Физики шутят': 'Второй зал, седьмой шкаф, пятая полка',
  'Приключения Электроника': 'Первый зал, десятый шкаф, вторая полка',
  'Сборник задач для поступающих в ВУЗы': 'Третий зал, восьмой шкаф, семнадцатая полка'
};
console.log(catalog['Физики шутят']);






Задача 2: Массив объектов
Ситуация:

Вы — веб-мастер интернет-магазина. Настало время написать корзину, которая умеет считать итоговую сумму заказа.

Задача: НСоздайте массив продуктов. Каждый продукт — объект с полями:

name — название товара;
price — цена за единицу;
quantity — количество.
Посчитайте общую стоимость заказа.

Шаги реализации:

Выбрать подходящий тип данных. Подойдёт позиционный массив, каждым элементом которого является объект.
Описать пример структуры данных. Создать результат = 0.
Перебрать массив поэлементно, умножая количество на цену и прибавляя произведение к результату.
Реализация:

const cart = [
    {
      name: 'Розы алые',
      price: 75,
      count: 1000000
    },
    {
      name: 'Ленты белые',
      price: 10,
      count: 333333
    }
];
let result = 0;
for (let product of cart) {
  result += product.price * product.count;
}
console.log(result, ' рублей');









Итоги
Сегодня мы:

познакомились с понятиями «объект», «поле», «метод»;
научились добавлять, изменять и удалять свойства объекта;
узнали, как извлекать нужную информацию по имени поля (ключа);
поработали с реальными задачами, которые очень удобно решать с применением объектов.
