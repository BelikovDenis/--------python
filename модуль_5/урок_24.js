Урок №24. Set, Map.

Учебные материалы

На предыдущих занятиях мы изучили массивы и объекты — основные структуры данных в JavaScript. 
Сегодня познакомимся с двумя новыми коллекциями: Set и Map. Они похожи на массивы и объекты,
 но решают специфичные задачи более эффективно.

Set — это коллекция уникальных значений, где каждый элемент встречается только один раз.
Map — это коллекция пар «ключ-значение», где ключом может быть любой тип данных (в отличие от объектов,
     где ключи только строки или символы).

Сегодня мы:

познакомимся с коллекциями Set и Map — эффективными альтернативами массивам и объектам в JavaScript;
научимся правильно удалять дубликаты из массивов с помощью Set за одну строку кода;
разберём, чем Map принципиально отличается от обычных объектов и когда его стоит использовать;
освоим основные методы работы с Set (add, has, delete) и Map (set, get, has);
сравним производительность Set и Map с массивами;
поймём, почему Set и Map сохраняют порядок добавления элементов в отличие от объектов.
После занятия вы перестанете использовать только массивы и объекты «на все случаи жизни» и
 будете выбирать оптимальные инструменты под конкретные задачи.

Глоссарий к двадцать четвёртому занятию
Set (Множество)

Set — коллекция для хранения уникальных значений любого типа. Гарантирует,
 что каждый элемент встречается только один раз.

Map (Словарь)

Map — коллекция пар «ключ-значение», где ключом может быть любой тип данных (включая объекты) 
в отличие от обычных объектов.

Unique (Уникальность)

Unique — свойство Set автоматически удалять дубликаты при добавлении. Для примитивов сравнение по значению, 
для объектов — по ссылке.

Iteration (Итерация)

Iteration — Set и Map сохраняют порядок добавления элементов и поддерживают методы для 
перебора: forEach(), for...of.

Потренироваться в переводах и лучше запомнить определения каждого термина мы советуем в приложении Quizlet.
 Чтобы открыть карточки к занятию, перейдите по ссылке.

Коллекции в JavaScript
Коллекции в JavaScript
Это структуры данных, предназначенные для хранения и управления наборами значений. В отличие от
 массивов (Array) и обычных объектов (Object), они предоставляют специализированные методы для работы с данными,
  обеспечивая лучшую производительность и удобство в определённых сценариях.
Самые основные из них:

Set — хранит уникальные значения любого типа:
const set = new Set([1, 2, 3]);
Map — хранит пары «ключ-значение» (ключи могут быть любого типа):
const map = new Map([['a', 1]]);
Также напомним о таких структурах данных, как Array и Object. Они не считаются коллекциями,
 но также используются для хранения данных.

Array – упорядоченный список элементов (индексированный).
Object – классическая коллекция «ключ-значение» (но ключи только string/Symbol).
Важный момент:

В JavaScript объекты (Object) не гарантируют порядок перечисления свойств, в то время как новые
 коллекции Set и Map сохраняют порядок добавления элементов. Это важное отличие, которое стоит понимать.

Set и Map были добавлены в ES6 с другим подходом:

Set хранит элементы в порядке их добавления.
Map хранит пары «ключ-значение» в порядке добавления.
Причины различий

Исторические: объекты существуют с первых версий JS, когда порядок не был важен.
Оптимизация производительности: движки оптимизировали объекты без учёта порядка.
Специфические потребности: Set и Map создавались именно как коллекции, где порядок часто важен.
Согласованность: поведение Set/Map одинаково во всех браузерах в отличие от объектов в ранних версиях JS.
Когда что использовать?

Используем объекты, когда нам нужны простые структуры без строгого порядка.
Используем Map, когда важен порядок добавления или нужны ключи не-строки.
Используем Set, когда нужна коллекция уникальных значений с сохранением порядка.
Современные движки JavaScript могут оптимизировать все три структуры, поэтому выбор должен основываться
 на семантике вашего кода, а не на микрооптимизациях.

Set (Множество)
Главной особенностью коллекции Set является уникальность значений, то есть каждое значение может
 встречаться только один раз, дубликаты автоматически удаляются. Set может содержать значения любого
  типа — примитивы и объекты, а также сохраняется порядок вставки элементов в отличие от обычных объектов.
   Коллекцию Set можно перебирать с помощью for или forEach.

Создание Set

const emptySet = new Set();

Эта строка создаёт пустое множество, в которое можно добавлять элементы с помощью метода add().
Полезно, когда нужно постепенно наполнять коллекцию уникальными значениями.

Создание Set с начальными значениями

const set = new Set([1, 2, 3, 4, 5]);

Инициализирует Set переданными значениями. Дубликаты автоматически удаляются,
 поэтому в итоговом множестве останутся только уникальные элементы.

Основные методы и свойства

add(value) добавляет элемент в множество:
const set = new Set();
set.add(1);
set.add(2);
set.add(2); // Не добавится, так как 2 уже есть
console.log(set); // Set(2) {1, 2}
Если элемент уже существует, метод add() игнорирует повторное добавление.
 Это гарантирует уникальность значений в коллекции.

has(value) проверяет наличие элемента:
const set = new Set([1, 2, 3]);
console.log(set.has(2)); // true
console.log(set.has(4)); // false
Возвращает true, если элемент есть в множестве, и false — если нет.
 Полезно для быстрой проверки существования значения.

delete(value) удаляет элемент:
const set = new Set([1, 2, 3]);
set.delete(2);
console.log(set); // Set(2) {1, 3}
Удаляет указанный элемент, если он присутствует. Если элемента нет, метод не делает ничего.

clear() полностью очищает множество:
const set = new Set([1, 2, 3]);
set.clear();
console.log(set); // Set(0) {}
Полностью удаляет все элементы из коллекции, оставляя её пустой.

size — свойство, возвращающее количество элементов:
const set = new Set([1, 2, 3, 3, 3]);
console.log(set.size); // 3 (дубликаты не учитываются)
Свойство size возвращает текущее количество уникальных элементов в множестве. Дубликаты игнорируются.

Перебор коллекции циклом

forEach():

const set = new Set(['a', 'b', 'c']);
set.forEach((value, valueAgain, set) => {
console.log(value); // a, b, c
});
Аналогично массивам, forEach() перебирает элементы в порядке их добавления.
 Второй аргумент (valueAgain) дублирует значение для совместимости с Map.

for:

const set = new Set(['a', 'b', 'c']);
for (const item of set) {
  console.log(item); // a, b, c
}
Цикл for позволяет итерироваться по элементам множества напрямую, без использования индексов.

Получение итераторов:
const set = new Set(['a', 'b', 'c']);
console.log(set.keys()); // [Set Iterator] {'a', 'b', 'c'}
console.log(set.values()); // [Set Iterator] {'a', 'b', 'c'}
console.log(set.entries()); // [Set Entries] {['a', 'a'], ['b', 'b'], ['c', 'c']}
Методы keys() и values() возвращают итераторы по элементам множества, а entries() — пары [значение, 
    значение] (для совместимости с Map).

Практическое применение

Добавление повторяющихся элементов:
const obj1 = {a: 1};
const obj2 = {a: 1};
const set = new Set();

set.add(obj1);
set.add(obj2); // Добавится, так как это другой объект
set.add(obj1); // Не добавится, так как это тот же объект

console.log(set.size); // 2
Преобразование Set в массив:
const set = new Set([1, 2, 3]);
const array = [...set]; // [1, 2, 3]
// или
const array2 = Array.from(set); // [1, 2, 3]
Set сравнивает объекты по ссылке, а не по содержимому. 
Даже если два объекта идентичны, они считаются разными элементами.

Удаление дубликатов из массива:
const arr = [1, 2, 2, 3, 4, 4, 5];
const unique = [...new Set(arr)]; // [1, 2, 3, 4, 5]
Один из самых простых способов удалить повторяющиеся элементы из массива — преобразовать его в Set и обратно.

Проверка на уникальность:
function allUnique(arr)
{
  return arr.length === new Set(arr).size;
}
Если размер Set совпадает с длиной исходного массива, значит все элементы уникальны.
 В противном случае в массиве есть дубликаты.

Хранение коллекции DOM-элементов:
const elements = new Set(document.querySelectorAll('div'));
Set удобен для работы с коллекциями DOM-узлов, так как позволяет быстро проверять наличие 
элементов и избегать дублирования.


					

				
Map-коллекция
Map — это коллекция пар «ключ-значение», которая:

Сохраняет порядок добавления элементов (в отличие от обычных объектов).
Позволяет использовать любые типы данных в качестве ключей (объекты, функции, примитивы).
Предоставляет удобные методы для работы с данными.
Имеет свойство size для определения количества элементов.
Практическое применение

Создание коллекции Map с начальными значениями:

const mapWithData = new Map([['key1', 'value1'],[77,'Москва']]);

Создание пустой коллекции Map:
`const map = new Map();
Создаёт пустую коллекцию Map, в которую можно добавлять элементы с помощью метода set(). 
Идеально подходит для постепенного наполнения данных.

Коллекция с начальными значениями:
const mapWithData = new Map([
  ['key1', 'value1'],
  [77, 'Москва']
]);
Инициализирует Map переданными парами «ключ-значение». 
Каждый элемент должен быть представлен массивом [ключ, значение].

Основные методы и свойства

set(key, value) добавляет или обновляет элемент:
const map = new Map();
map.set('name', 'Alice');
map.set(10, 'Ten');
map.set({}, 'Object value');

console.log(map.get('name')); // 'Alice'
console.log(map.get(10));     // 'Ten'
console.log(map.get({}));     // undefined (другой объект)
Добавляет новую пару «ключ-значение». Если ключ уже существует, его значение будет перезаписано. 
Поддерживает любые типы ключей.

get(key) получает значение по ключу:
console.log(map.get('name')); // 'Alice'
console.log(map.get(10));     // 'Ten'
console.log(map.get({}));     // undefined (другой объект)
Возвращает значение, связанное с указанным ключом. Если ключа нет, возвращает undefined.

has(key) проверяет наличие ключа:
console.log(map.has('name')); // true
console.log(map.has(99));     // false
Проверяет, существует ли указанный ключ в коллекции. Возвращает true или false.

delete(key) удаляет элемент по ключу:
map.delete(10);
console.log(map.has(10)); // false
Удаляет пару «ключ-значение» по указанному ключу. Возвращает true, если элемент был удалён,
 и false — если ключа не было.

clear() очищает коллекцию:
map.clear();
console.log(map.size); // 0
Полностью очищает Map, удаляя все элементы.

size — количество элементов:
console.log(map.size); // 2
Свойство size возвращает текущее количество элементов в коллекции.

Перебор коллекции

forEach():
map.forEach((value, key) => {console.log(`${key}: ${value}`);
});
Перебирает все элементы Map в порядке их добавления. Аргументы callback-функции: значение, ключ.

for:
for (const [key, value] of map)
{
  console.log(key, value);
}
Цикл for позволяет итерироваться по парам «ключ-значение» напрямую. Можно использовать деструктуризацию [key, value].

Получение итераторов:
console.log(map.keys());    // [Map Iterator] {'name', 10}
console.log(map.values()); // [Map Iterator] {'Alice', 'Ten'}
console.log(map.entries()); // [Map Entries] {['name', 'Alice'], [10, 'Ten']}
Методы keys(), values() и entries() возвращают итераторы для ключей, значений и пар «ключ-значение» соответственно.

Объекты как ключи:
const objKey = {id: 1};
map.set(objKey, 'Объект-ключ');
console.log(map.get(objKey)); // 'Объект-ключ'
console.log(map.get({id: 1})); // undefined (другой объект)
Map сравнивает объекты-ключи по ссылке, а не по содержимому. Даже идентичные объекты считаются разными ключами.

Примитивы как ключи:
map.set(1, 'Number');
map.set('1', 'String');
console.log(map.get(1));  // 'Number'
console.log(map.get('1')); // 'String'
Разные типы данных (например, число 1 и строка ‘1’) считаются разными ключами.

Преобразование Map в массив и объект в объект (если ключи — строки или символы):
const obj = Object.fromEntries(map);
Преобразует Map в обычный объект. Работает, только если все ключи — строки или символы.

Хранение сложных структур:
const userMap = new Map();
userMap.set(user1, {settings: {theme: 'dark'}});
userMap.set(user2, {settings: {theme: 'light'}});
Идеально подходит для связывания объектов с дополнительными данными.

Кэширование результатов:
const cache = new Map();

function computeExpensiveValue(key) {
  if (cache.has(key)) {
    return cache.get(key);
  }
  const result = /* сложные вычисления */;
  cache.set(key, result);
  return result;
}
Map часто используется для кэширования (хранения копий часто используемых данных в более быстром хранилище).

Замена объектов для словарей:
const countryCodes = new Map([
  [7, 'Россия'],
  [1, 'США'],
  [49, 'Германия']
]);
Когда нужен словарь с нестроковыми ключами или гарантированным порядком элементов.

Мы разобрали коллекции Set и Map, они помогают эффективно работать с уникальными значениями (Set) 
и парными данными (Map). Но как не допустить ошибок в процессе работы с ними? Рассмотрим далее.

Типичные ошибки
Тип ошибки 1: Использование объектов в качестве ключей без понимания ссылочной природы
Описание:

Разработчики часто пытаются использовать объекты в качестве ключей в Map, ожидая, что они будут
 сравниваться по содержимому. Однако в JavaScript объекты сравниваются по ссылке, а не по значению.
  Это как пытаться открыть дверь ключом, который выглядит визуально так же, но от другого замка.

Пример ошибки (код):

const map = new Map();
const key1 = { id: 1 };
const key2 = { id: 1 }; // Другой объект с тем же содержимым

map.set(key1, 'Alice');
console.log(map.get(key2)); // undefined (ожидается 'Alice')
Объяснение:

Key1 и key2 — разные объекты в памяти, даже если их свойства идентичны. Map сравнивает ключи по ссылке, 
поэтому map.get(key2) не находит значение.

Решение:

Используем один и тот же объект в качестве ключа:

const key = { id: 1 };
map.set(key, 'Alice');
console.log(map.get(key)); // 'Alice'
Если нужно сравнивать по содержимому, преобразуйте объект в строку (например, JSON.stringify):
const map = new Map();
map.set(JSON.stringify({ id: 1 }), 'Alice');
console.log(map.get(JSON.stringify({ id: 1 }))); // 'Alice'
Тип ошибки 2: Путаница между Map и Set
Описание:

Новички иногда путают Map (хранит пары «ключ-значение») и Set (хранит только уникальные значения).
 Это как перепутать кошелёк (где есть только купюры) с мешком для монет (где важны только сами монеты).

Пример ошибки (код):

const set = new Set();
set.add('name', 'Alice'); // Ожидается добавление пары ключ-значение
console.log(set); // Set(1) { 'name' } (значение 'Alice' потеряно)
Объяснение:

Set принимает только один аргумент — значение для добавления. Второй аргумент (‘Alice’) игнорируется.

Решение:

Используем Map для пар «ключ-значение»:

const map = new Map();
map.set('name', 'Alice');
console.log(map.get('name')); // 'Alice'
Используем Set для уникальных значений:

const set = new Set();
set.add('Alice');
console.log(set.has('Alice')); // true
Тип ошибки 3: Игнорирование порядка элементов в Map
Описание:

Map сохраняет порядок добавления элементов, но разработчики иногда забывают об этом и пытаются сортировать 
его как массив. Это как пытаться упорядочить очередь в магазине, не учитывая, кто пришёл первым.

Пример ошибки (код):

const map = new Map();
map.set('z', 1);
map.set('a', 2);
console.log([...map].sort()); // [ ['a', 2], ['z', 1] ] (порядок изменён)
Объяснение:

При преобразовании Map в массив и сортировке исходный порядок теряется. Это может сломать логику, 
если мы рассчитываем на порядок вставки.

Решение:

Для сортировки создаём новый Map:

const sortedMap = new Map([...map].sort());
Если порядок важен, используйте Map без сортировки:
for (const [key, value] of map) {
  console.log(key, value); // z 1, a 2 (порядок сохранён)
}
Тип ошибки 4: Использование NaN в Set
Описание:

Set считает NaN равным самому себе (хотя NaN !== NaN в JavaScript). 
Это может привести к неожиданностям, если вы не знаете об этой особенности.

Пример ошибки (код):

const set = new Set();
set.add(NaN);
set.add(NaN); // Дубликат не добавлен
console.log(set); // Set(1) { NaN } (ожидается 2 элемента)
Объяснение:

Спецификация Set использует алгоритм SameValueZero, где NaN === NaN. Это отличается от обычного сравнения в JavaScript.

Решение:

Если нужно хранить несколько NaN, преобразуем их в строки:

const set = new Set();
set.add('NaN_1');
set.add('NaN_2');
console.log(set); // Set(2) { 'NaN_1', 'NaN_2' }
Тип ошибки 5: Неэффективный поиск в Set
Описание:

Set оптимизирован для проверки наличия элемента (has), но некоторые пытаются 
использовать Array.from(set).includes(), что медленнее.

Пример ошибки (код):

const set = new Set([1, 2, 3]);
if (Array.from(set).includes(2)) { // Медленно для больших коллекций
  console.log('Найдено');
}
Объяснение:

Преобразование Set в массив и поиск через includes имеет сложность O(n), а set.has() — O(1).

Решение:

if (set.has(2)) { // Быстро даже для миллионов элементов
  console.log('Найдено');
}
Практикум
Задача 1: Учёт посещаемости студентов
Ситуация:

Вы разрабатываете систему учёта посещаемости. Нужно хранить данные о студентах, 
которые посетили лекцию, избегая дубликатов.

Задача:

Создайте Set для хранения уникальных ID студентов. Напишите функцию addStudent(id), которая добавляет
 ID в Set и выводит обновлённый список посещаемости.

Решение:

const attendance = new Set();

function addStudent(id) {
  if (attendance.has(id)) {
    console.log(`Студент ${id} уже отметился!`);
  } else {
    attendance.add(id);
    console.log(`Студент ${id} добавлен.`);
  }
  console.log('Текущая посещаемость:', [...attendance]);
}

addStudent(101); // Студент 101 добавлен. Текущая посещаемость: [101]
addStudent(102); // Студент 102 добавлен. Текущая посещаемость: [101, 102]
addStudent(101); // Студент 101 уже отметился!
Задача 2: Фильтрация дубликатов в заданиях
Ситуация:

В системе генерации заданий нужно исключить повторяющиеся задачи для студентов.

Задача:

Даны два массива задач: currentTasks и newTasks. Напишите функцию getUniqueTasks(), которая возвращает
 новый массив, содержащий только уникальные задачи, используя Set.

Решение:

const currentTasks = ['Задача 1', 'Задача 2', 'Задача 3'];
const newTasks = ['Задача 3', 'Задача 4', 'Задача 5'];

function getUniqueTasks() {
  const combinedTasks = [...currentTasks, ...newTasks];
  const uniqueTasks = new Set(combinedTasks);
  return [...uniqueTasks];
}

console.log(getUniqueTasks());
// ['Задача 1', 'Задача 2', 'Задача 3', 'Задача 4', 'Задача 5']
Итоги
Сегодня мы:

узнали, что такое Map и Set в JavaScript и для каких задач они предназначены;
научились создавать коллекции Map для хранения пар «ключ-значение» с любыми типами ключей;
увидели, как Set автоматически удаляет дубликаты и хранит только уникальные значения;
освоили основные методы set(), get(), has(), delete() — для Map и add(), has(), delete() — для Set;
сравнили Map с обычными объектами, а Set — с массивами, чтобы понять их преимущества;
узнали, как перебирать элементы Map и Set с помощью for...of, forEach() и методов keys(), values(), entries();
применили Map для кэширования результатов функций, а Set — для фильтрации повторяющихся данных.