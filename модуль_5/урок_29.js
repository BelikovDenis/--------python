Урок №29. Операторы call, bind, apply. Статические методы.

Учебные материалы

В JavaScript функции — это объекты, и у них есть свои методы. Три важных метода функций — call,
 bind и apply — позволяют управлять контекстом (this) и передавать аргументы.
  Контекст выполнения — это одна из фундаментальных концепций JS, которая часто вызывает путаницу у
   начинающих разработчиков. В этом занятии мы разберёмся, что это такое и как им управлять.
   Также изучим статические методы классов, которые принадлежат самому классу, а не его экземплярам.

Сегодня мы:

разберём методы функций call, apply и bind;
поймём, как они управляют контекстом выполнения (this);
изучим практическое применение этих методов;
рассмотрим реальные сценарии их использования;
познакомимся со статическими методами классов;
узнаем, чем статические методы отличаются от обычных;
поймём, когда стоит использовать статические методы;
разберёмся, когда использовать call/apply, а когда — bind.
После этого занятия мы сможем уверенно управлять контекстом выполнения в JavaScript и выбирать
 оптимальные подходы для разных задач.




Глоссарий к двадцать девятому занятию
Call (Вызов)
Call — метод, который вызывает функцию с указанным значением this и отдельными аргументами.

Apply (Применить)
Apply — метод функций, аналогичный call, но принимающий аргументы в виде массива.

Bind (Привязать)
Bind — метод, создающий новую функцию с привязанным контекстом this и, возможно, предустановленными аргументами.

Execution Context (Контекст выполнения)
Execution Context — окружение, в котором выполняется код, определяющее значение this.

Статический метод
Статический метод — метод класса, доступный через сам класс, а не через его экземпляры. Объявляется с ключевым словом static.

Заимствование методов
Заимствование методов — паттерн использования методов одного объекта для другого с помощью call, apply или bind.

Currying (Каррирование)
Currying — техника преобразования функции с несколькими аргументами в последовательность функций с одним аргументом.

Метод экземпляра
Метод экземпляра — обычный метод класса, доступный только его экземплярам и работающий с их данными.




Управление контекстом в JavaScript
В JavaScript важно не только то, что мы вызываем, но и где и от чьего имени это происходит. 
От контекста выполнения зависит, на какие данные ссылается код в момент исполнения. 
Контекст определяет, какие переменные и функции «видны», какие свойства доступны, и как интерпретируется ключевое слово this.

Что такое контекст в JavaScript?
Контекст выполнения — это логическое окружение, в котором работает код. Каждый раз, когда вызывается функция, 
создаётся новый контекст выполнения, который помещается в стек вызовов. В нём хранятся переменные,
 ссылки на внешние объекты и само значение this.

Чтобы легче представить себе, что такое контекст, давайте представим знакомую всем ситуацию.
 Перед вами сцена, а на ней актёр.


Рисунок 1
Контекст — это сцена. Актёры на сцене — это объекты. А this — это прожектор, 
который освещает одного из них в каждый конкретный момент. На сцене может быть много персонажей, 
но в момент зритель видит только того, на кого направлен свет. Именно этот «освещённый» объект становится this.

Или вот другой пример из жизни

Кафе. Вечер. Кто-то говорит: «Я заплачу за всех!». Кто конкретно сказал эту фразу? Это и есть вопрос this.

Кафе — это глобальный контекст (например, window в браузере).

Посетители — объекты, каждый со своими методами и свойствами.

Фраза «Я заплачу!» — функция. Кто её произнёс, тот и становится this.

В коде это может выглядеть так:

Пример:

const person = {
  name: 'Alice',
  greet: function() {
    console.log(`Hello, my name is ${this.name}`);
  }
};

person.greet(); // Hello, my name is Alice
Объяснение:

Создан объект person с методом greet, который выводит имя из своего контекста (this.name).

При вызове person.greet() контекст this автоматически становится объектом person, поэтому выводится Hello,
 my name is Alice.

Ключевой момент:

this в методах объекта всегда ссылается на сам объект, если функция вызвана через точку (obj.method()).
В этом примере this внутри метода greet ссылается на объект person.

Контекст определяется в момент вызова функции.

Основные правила

Простой вызов функции: this будет ссылаться на глобальный объект (window в браузере,
     global в Node.js) или undefined в строгом режиме.
function showThis() {
  console.log(this);
}

showThis(); // window или undefined (в strict mode)
Метод объекта: this ссылается на объект, которому принадлежит метод.
const obj = {
  method() {
    console.log(this);
  }
};

obj.method(); // obj
Конструктор (new): this ссылается на новый созданный объект.
Когда функция вызывается как метод объекта (через точку obj.method()), this автоматически ссылается
 на этот объект (obj). Это стандартное поведение для методов в JavaScript.

function Person(name) {
  this.name = name;
}

const alice = new Person('Alice');
console.log(alice.name); // Alice
События DOM: this ссылается на элемент, который вызвал событие.
button.addEventListener('click', function() {
  console.log(this); // button element
});
При вызове функции с оператором new this ссылается на вновь созданный пустой объект,
 который затем возвращается как результат. В примере this.name = name записывает значение в этот новый объект.

Методы управления контекстом
JavaScript предоставляет несколько способов явного управления контекстом.

В JavaScript функции могут менять свой контекст (this) в зависимости от того, как их вызывают. 
Иногда нужно принудительно указать, на какой объект должен ссылаться this. Для этого используют:

call – вызвать функцию с заданным this и аргументами, чтобы передать в функцию нужный контекст и аргументы по одному;
apply() – то же, что call(), но аргументы передаются массивом, полезно, если аргументы приходят в виде массива;
bind() – создать новую функцию с привязанным this.
Давайте подробнее рассмотрим эти методы.

this как стрелка компаса
В JavaScript this — это указатель на объект, от имени которого выполняется функция. 
Но, в отличие от строго зафиксированной системы координат, в JS эта стрелка может сбиваться,
 указывая вовсе не туда, куда ожидается.


Рисунок 2
Представим, что this — это стрелка компаса. Она должна всегда показывать на нужный объект (условный «север»).

Однако: Обычная функция — это компас без фиксации Когда функция вызывается вне объекта (например,
     как колбэк), стрелка начинает крутиться: this может указывать на window, undefined (в strict mode) или вообще потеряться.

Пример:

const user = { name: 'Петя', greet() { console.log(this.name); } };
const greet = user.greet;
greet(); // undefined
Метод объекта — это временная настройка компаса Если вызвать метод как obj.method(), this указывает правильно. 
Но стоит передать этот метод куда-то (например, в setTimeout), и стрелка сбивается.

setTimeout(user.greet, 1000); // this снова потерян
Метод .bind() — это ремонт и калибровка компаса Он создаёт новую функцию, у которой this зафиксирован. 
Как будто вы прикрутили стрелку к нужному направлению — теперь она не собьётся.

const boundGreet = user.greet.bind(user);
setTimeout(boundGreet, 1000); // Петя
Метафора в действии Без bind: Путешественник идёт по лесу, но его компас (значение this) постоянно сбивается — он теряет путь.

С bind: Компас надёжно отрегулирован — даже в незнакомой среде путешественник знает, куда идти.

Именно поэтому методы bind, call, apply — это ключевые инструменты, позволяющие «настроить» this и не потеряться в коде.

Метод call()
Метод call() — это один из основных способов управления контекстом выполнения (this) в JavaScript. 
Он позволяет явно указать, на какой объект будет ссылаться this при вызове функции.

Синтаксис:

func.call(thisArg, arg1, arg2, ..., argN)
thisArg — значение, которое будет использоваться как this при вызове функции

arg1, ..., argN — аргументы, передаваемые в функцию
Простой пример:

function greet() {
  console.log(`Привет, меня зовут ${this.name}`);
}
const person = { name: 'Петя' };
greet.call(person); // Привет, меня зовут Петя
Здесь мы «заставляем» функцию greet использовать объект person в качестве контекста.

Когда использовать call()?

Заимствование методов:
const car = { brand:'Toyota', showBrand() {console.log(this.brand); } };

const bike = { brand: 'Harley' };
car.showBrand.call(bike); // Harley
Вызов функций с аргументами:
function introduce(age, profession) {
  console.log(`Меня зовут ${this.name}, мне ${age} и я ${profession}`);
}

const user = { name: 'Иван' };

introduce.call(user, 30, 'разработчик');
// Меня зовут Иван, мне 30 и я разработчик
Работа с конструкторами:
function Car(brand)
{
  this.brand = brand;  // Устанавливаем свойство brand
}

  // Наследуем свойство brand от Car
function ElectricCar(brand, battery)
{
  Car.call(this, brand);  // this теперь ссылается на новый объект ElectricCar
  // Добавляем уникальное свойство для ElectricCar
  this.battery = battery;
}

// Создаем экземпляр ElectricCar
const tesla = new ElectricCar('Tesla', '100kWh');

console.log(tesla);
// Выведет: ElectricCar { brand: 'Tesla', battery: '100kWh' }
Особенности call()

Немедленный вызов — call() вызывает функцию сразу же.
Передача аргументов — аргументы передаются через запятую.
Изменяемый контекст — первый аргумент определяет this.
Метод apply()
Метод apply() — это инструмент для управления контекстом выполнения функций в JavaScript,
 который позволяет вызывать функцию с указанным значением this и аргументами, переданными
  в виде массива (или подобного массиву объекта).

Основы метода apply()

Синтаксис:

func.apply(thisArg, [argsArray])

thisArg — значение, которое будет использоваться как this при вызове функции.

argsArray — массив (или массивоподобный объект) аргументов для передачи в функцию.

Простой пример:

function introduce(profession, city) {
  console.log(`Меня зовут ${this.name}, я ${profession} из ${city}`);
}

const person = { name: 'Мария' };

introduce.apply(person, ['дизайнер', 'Москва']);
// Меня зовут Мария, я дизайнер из Москва
Ключевые особенности apply()
Вызов функции с указанным контекстом:

позволяет явно задать значение this;
передавать аргументы в виде массива — удобно при работе с динамическим числом аргументов;
немедленно выполнять — функция вызывается сразу при использовании apply().
Основные сценарии использования

Вызов функций с массивом аргументов:
const numbers = [5, 6, 2, 3, 7];

// Находим максимальное число в массиве
const max = Math.max(...numbers);

console.log(max); // 7
Заимствование методов:
const arrayLike = {
  0: 'a',
  1: 'b',
  length: 2
};

// Преобразуем массивоподобный объект в настоящий массив
const realArray = Array.prototype.slice.apply(arrayLike);
console.log(realArray); // ['a', 'b']
Цепочка конструкторов:
function Product(name, price) {
  this.name = name;
  this.price = price;
}

function Food(name, price, category) {
  Product.apply(this, [name, price]);
  this.category = category;
}

const cheese = new Food('Сыр', 200, 'Молочные продукты');
Разница между apply() и call()
Разница между методами apply() и call() в JavaScript заключается в том, как они передают аргументы в функцию.
Метод call() принимает аргументы через запятую, а apply() — в виде массива.
Выбор между этими методами зависит от того, как удобнее передавать аргументы в функцию: как 
отдельные значения или как массив. Например, использование apply() может быть полезно, когда
 количество параметров неизвестно заранее и они хранятся в массиве. И, наоборот, call() удобнее использовать,
  когда параметры передаются напрямую.

Каррирование (Currying) в JavaScript
Что такое каррирование?

Каррирование — это техника преобразования функции с несколькими аргументами в последовательность функций,
 каждая из которых принимает один аргумент.

Простой пример

Исходная функция:

function sum(a, b, c) {
  return a + b + c;
}
sum(1, 2, 3); // 6
После каррирования:

function curriedSum(a) {
  return function(b) {
    return function(c) {
      return a + b + c;
    };
  };
}
curriedSum(1)(2)(3); // 6
Этот код демонстрирует технику каррирования (currying) в JavaScript — преобразования функции
 с несколькими аргументами в последовательность функций с одним аргументом.
Каррированная функция для сложения трёх чисел. Она последовательно принимает аргументы по одному:
 сначала a, затем b и, наконец, c, возвращая их сумму. Вызов curriedSum(1)(2)(3) эквивалентен 
 обычному сложению 1 + 2 + 3, но с поэтапной передачей значений.

Давайте разберём пошагово:

curriedSum(1) — вызывает внешнюю функцию и возвращает первую вложенную, где a зафиксирован как 1;
(2) — вызывает эту возвращённую функцию, фиксируя b как 2 и возвращая следующую вложенную функцию;
(3) — вызывает последнюю функцию, складывая все три значения: 1 (a) + 2 (b) + 3(c).
Такой подход позволяет частично применять аргументы (например, создать специализированную функцию
     addFive = curriedSum(5)) и делает композицию функций более удобной.

Аналогия из жизни

Представим кухонный комбайн в нескольких вариантах:

Обычная функция — это комбайн, куда вы сразу загружаете все ингредиенты.

Каррированная версия — это комбайн, где вы поочерёдно устанавливаете насадки.

Как работает каррирование?
Шаг 1: Функция принимает первый аргумент и возвращает новую функцию.

Шаг 2: Новая функция принимает второй аргумент и возвращает следующую.

Шаг 3: Процесс повторяется, пока не будут переданы все аргументы.

// Каррированная функция для расчета доставки
function createDelivery(basePrice) {
  return function(distance) {
    return basePrice + distance * 0.5;
  };
}

const moscowDelivery = createDelivery(100); // Фиксируем basePrice=100
moscowDelivery(10); // 105 (100 + 10*0.5)
Чтобы понять это на интуитивном уровне, представим сборку из конструктора LEGO.

Обычная функция — это уже собранная модель. Вы сразу добавляете все детали и моментально получаете готовый результат.

Каррированная функция — это сборка по частям. Вы начинаете с фундамента, затем строите стены, крышу и т.д.
 На каждом этапе добавляется одна «деталь» (аргумент), и создаётся функция, которая ждёт следующую.


Рисунок 3
Функция разбита на части, как конструкторLEGO.

Связь с методами bind, call, apply
Каррирование через bind

Метод bind может фиксировать аргументы аналогично каррированию:

function greet(greeting, name) {
  console.log(`${greeting}, ${name}!`);
}

const greetHello = greet.bind(null, "Hello"); // Фиксируем greeting
greetHello("Ivan"); // "Hello, Ivan!"
Можно создать специализированные функции:

// Каррированная функция умножения
const multiply = (a) => (b) => a * b;
const double = multiply(2); // Фиксируем a=2
double(5); // 10
Удобство композиции

Функции с одним аргументом легче комбинировать:

const add = (a) => (b) => a + b;
const square = (x) => x * x;

// Композиция: square(add(2)(3)) → square(5) → 25
Отложенное выполнение

Можно передавать аргументы поэтапно:

const fetchData = (baseUrl) => (endpoint) => fetch(`${baseUrl}/${endpoint}`);

const apiV1 = fetchData("https://api.example.com/v1");
apiV1("users"); // GET https://api.example.com/v1/users
Автоматическое каррирование

Универсальная функция-каррировщик:

function curry(fn) {
  return function curried(...args) {
    if (args.length >= fn.length) {
      return fn.apply(this, args);
    } else {
      return function(...args2) {
        return curried.apply(this, args.concat(args2));
      };
    }
  };
}

// Пример использования
const curriedSum = curry((a, b, c) => a + b + c);
curriedSum(1)(2)(3); // 6
Практические примеры
Пример 1: Валидация данных

// Каррированная проверка минимальной длины
const minLength = (min) => (text) => text.length >= min;
const isLongEnough = minLength(5); // Фиксируем min=5

isLongEnough("Hello"); // true
isLongEnough("Hi");    // false
Пример 2: Логирование

// Каррированное логирование с уровнем важности
const log = (level) => (message) => console.log(`[${level}] ${message}`);
const logError = log("ERROR"); // Фиксируем level

logError("Сервер упал!"); // [ERROR] Сервер упал!
Пример 3: API-запросы

// Каррированный HTTP-клиент
const createApiClient = (baseUrl) => (endpoint) => (params) => {
  return fetch(`${baseUrl}/${endpoint}`, { ...params });
};

const githubApi = createApiClient("https://api.github.com");
const getUser = githubApi("users");
getUser({ username: "torvalds" }); // Запрос к https://api.github.com/users
Плюсы и минусы каррирования
Преимущества

Гибкость: создание специализированных функций.
Чистый код: уменьшает дублирование.
Удобство тестирования: легко мокать (от англ. mock) частично применённые функции.
Недостатки

Сложность: может ухудшить читаемость для новичков.
Производительность: каждый вызов — новая функция (микрооптимизация).
Вывод

Каррирование — это «разбор» функции на цепочку из функций с одним аргументом.

Полезно для:

Фиксации аргументов (bind).
Фабричных методов (статических функций).
Композиции и отложенного выполнения.
В сегодняшней теме связывается с bind через частичное применение аргументов.
Главное — используем каррирование там, где оно упрощает код, а не усложняет его.

Метод bind()
Метод bind() — это инструмент для управления контекстом (this) в JavaScript. В отличие от call() и apply(),
 которые вызывают функцию сразу, bind() создаёт новую функцию с привязанным контекстом, которую можно вызвать позже.

Синтаксис:

const boundFunc = originalFunc.bind(thisArg, arg1, arg2, ...);
thisArg — значение, которое станет this внутри функции.

arg1, arg2, ... — аргументы, которые будут "зафиксированы" (опционально).
Простой пример:

const user = {
  name: "Алексей",
  greet() {
    console.log(`Привет, ${this.name}!`);
  },
};

const greetLater = user.greet.bind(user);
greetLater(); // Привет, Алексей!
Здесь bind() создает новую функцию greetLater, в которой this всегда ссылается на user.
Когда использовать bind()?
В JavaScript метод bind() служит для явного управления контекстом выполнения функции (this).
 Его основное предназначение — создавать новую функцию с жёстко привязанным контекстом,
  который не будет меняться в зависимости от места вызова.

Вот ключевые случаи, когда bind() действительно необходим:

Фиксация контекста в асинхронных операциях и колбэках
Когда мы передаём метод объекта как колбэк (например, в setTimeout, обработчики событий или промисы),
 контекст this по умолчанию теряется. Это происходит потому, что функция вызывается не как метод объекта,
  а как самостоятельная функция. В таких случаях bind() позволяет заранее зафиксировать нужный контекст,
   чтобы this всегда ссылался на правильный объект.

Работа с методами класса в реактивных фреймворках
В современных фреймворках (React, Vue и др.) методы компонентов часто передаются как колбэки. 
Если не использовать bind() или стрелочные функции, this внутри метода может перестать ссылаться на
 экземпляр компонента, что приведёт к ошибкам при обращении к его свойствам и методам.

Каррирование и частичное применение функций
Хотя это менее распространённый сценарий, bind() позволяет фиксировать не только контекст, 
но и часть аргументов функции. Это полезно, когда нужно создать специализированную версию функции 
с предустановленными параметрами.

Совместимость с legacy-кодом
В старом коде, написанном до широкого распространения стрелочных функций, bind() часто использовался
 для решения проблем с контекстом. Если вы поддерживаете такой код или работаете с библиотеками, 
 которые не используют современный синтаксис, понимание bind() обязательно.

Когда НЕ нужно использовать bind()?
Стрелочные функции — в современном JavaScript они автоматически сохраняют контекст,
 поэтому во многих случаях заменяют bind().
Если контекст не важен — для чистых функций (не использующих this) или статических методов bind() избыточен.
Когда функция вызывается сразу — в этом случае проще использовать call() или apply().
const button = document.querySelector("button");

const app = {
  message: "Кнопка нажата!",
  handleClick() {
    console.log(this.message); // Ошибка: this === button (если не привязан)
  },
};

button.addEventListener("click", app.handleClick.bind(app)); // Теперь this === app
Каррирование (частичное применение аргументов).
function multiply(a, b) {
  return a * b;
}

const double = multiply.bind(null, 2); // Фиксируем первый аргумент (a = 2)
console.log(double(5)); // 10 (2 * 5)
Создание методов с фиксированным контекстом.
class Timer {
  constructor() {
    this.seconds = 0;
    this.tick = this.tick.bind(this); // Фиксируем контекст
    setInterval(this.tick, 1000);
  }

  tick() {
    this.seconds++;
    console.log(this.seconds);
  }
}
Каррирование для математических функций.
function power(base, exponent) {
  return Math.pow(base, exponent);
}

const square = power.bind(null, 2); // base = 2
console.log(square(3)); // 8 (2^3)
Метод bind() полезен, когда:

нужно сохранить контекст для отложенного вызова (колбэки, таймеры);
требуется каррирование (фиксация части аргументов);
работаем с классами и методами, где this должен быть предсказуем.
Используйте метод bind (), когда нужно заранее зафиксировать контекст (например, для колбэков в событиях).
 В современных проектах bind() можно заменить стрелочными функциями, если нужно сохранить контекст без
  дополнительного вызова. Если же функция вызывается сразу, предпочтительнее использовать call() или apply().

Сравнение методов call, apply и bind
Методы call, apply и bind управляют контекстом (this) функции. Чтобы понять их разницу, представим, что:

функция — это передаваемое сообщение

this — это получатель


Рисунок 4
call() — Обычный телефонный звонок (прямое соединение) Вы набираете номер (вызываете функцию) и
 сразу подключаетесь к собеседнику (this). Можете передать несколько аргументов через запятую 
 (передать приветствие от всех, кто рядом). Соединение немедленное — функция выполняется сразу.
function sayHello(greeting, punctuation) {
  console.log(`${greeting}, ${this.name}${punctuation}`);
}

const person = { name: "Алекс" };
sayHello.call(person, "Привет", "!");  // "Привет, Алекс!"
Используйте метод call(), когда нужно сразу вызвать функцию, явно указав контекст и передав аргументы по отдельности.

apply — Телефон с громкоговорителем (передача массива данных) Вы связываетесь с собеседником (this), 
но говорите через динамик (приветствие от всех, кто находится рядом одновременно).
Это полезно, когда аргументов много или они уже собраны в список.
 Соединение тоже мгновенное — функция запускается сразу.

function introduce(greeting, punctuation) {
  console.log(`${greeting}, я ${this.name}${punctuation}`);
}

const person = { name: "Мария" };
introduce.apply(person, ["Здравствуйте", "."]);  // "Здравствуйте, я Мария."
Используйте метод apply (), когда Если аргументы уже в массиве (например, из Math.max.apply(null, [1, 2, 3])) или их много.

bind — Радиостанция с фиксированной частотой (постоянная привязка) соединение в несколько этапов:
вы настраиваете канал связи, но не передаёте сообщение сразу;
получаете сообщение по ранее настроенному каналу связи. (Получаете новую функцию,
     которая всегда будет вызываться в заданном контексте).
Соединение отложенное — функция выполнится только при явном вызове.

function sendMessage(message) {
  console.log(`${this.name}, ${message}`);
}

const person = { name: "Иван" };
const sendToIvan = sendMessage.bind(person, "вам письмо!");
sendToIvan();  // "Иван, вам письмо!"
Статические методы классов
Что такое статические методы?

В JavaScript статические методы — это функции, которые принадлежат не экземплярам класса, а самому классу.
 Они вызываются напрямую через имя класса и не имеют доступа к данным конкретного объекта.

Представьте фабрику: Обычные методы — это рабочие на производственной линии. Они знают, с какими деталями работают,
 и управляют каждым изделием по отдельности. Статические методы — это диспетчерская или центральный пульт управления.
  Они не касаются отдельных деталей, но управляют фабрикой в целом: запускают конвейер,
   меняют режимы работы, ведут общий учёт.


Рисунок 5
Особенности:

Объявляются с ключевым словом static.
Не имеют доступа к данным конкретного экземпляра (при использовании this ссылается на сам класс,
     а не на экземпляр).
Используются для служебных функций, связанных с классом.
Наследуются дочерними классами.
Синтаксис статических методов:

class MyClass {
  static myStaticMethod() {
    return 'Это статический метод';
  }
}

// Вызов
MyClass.myStaticMethod(); // "Это статический метод"
Когда использовать статические методы?

Функции, которым не нужен доступ к состоянию объекта
Альтернативные конструкторы
Математические вычисления
Валидация данных
Фабричные методы
Для работы с классом в целом
Важно помнить!
Статические методы не заменяют обычные методы. Если логика метода зависит от состояния конкретного объекта,
 он должен быть нестатическим. Чрезмерное использование static может привести к нарушению принципов ООП и усложнению поддержки кода.

Типичные ошибки
Тип ошибки 1: Попытка использовать this для доступа к свойствам экземпляра
Описание:

В статических методах JavaScript поведение ключевого слова this принципиально отличается от его поведения
 в обычных методах класса. Это различие часто становится источником ошибок, особенно для разработчиков,
  пришедших из классического объектно-ориентированного программирования.

Пример ошибки (код):

class User {
  static role = 'admin';

  static printRole() {
    console.log(this.role); // Верно: 'admin' (обращение к статическому свойству)
    console.log(this.name); // Ошибка: undefined (экземпляр не существует)
  }
}
User.printRole();
Объяснение:

this в статическом методе — это конструктор класса (User), а не экземпляр.
 Обращение к нестатическим свойствам (например, this.name) вернёт undefined, так как они существуют только в экземплярах.

Решение:

Используйте статические методы только для работы со статическими свойствами или логики, 
не требующей экземпляров.

class User {
  static createAdmin() {
    return new this(); // Верно: this === User
  }
}
Тип ошибки 2: Вызов нестатического метода из статического
Описание:

Статические методы в JavaScript работают на уровне класса, а не его экземпляров.
 Когда разработчик пытается вызвать нестатический метод (метод экземпляра) напрямую из статического метода,
  возникает ошибка, так как:

Статический метод не знает о существовании экземпляров (они могут быть не созданы).
this в статическом методе — это сам класс, а не объект.
Пример ошибки (код):

class Calculator {
  add(a, b) {
    return a + b;
  }

  static sum(a, b) {
    return this.add(a, b); // Ошибка: add не существует в статическом контексте
  }
}
Объяснение:

В этом примере возникает ошибка, потому что статический метод sum пытается вызвать нестатический add напрямую.
 Нестатические методы доступны только через экземпляры класса, созданные оператором new, 
 тогда как статические работают на уровне самого класса. Когда мы вызываем this.add() внутри статического метода,
  this ссылается на класс Calculator, а не на его экземпляр, поэтому метод add не найден.

Решение:

Для исправления нужно либо создать экземпляр класса внутри статического метода (new this().add(a,b)), 
либо сделать метод add статическим, если он не использует данные конкретного экземпляра.

static sum(a, b) {
  const instance = new this();
  return instance.add(a, b); // Теперь работает
}
Тип ошибки 3: Забыли объявить метод как static
Описание:

Эта ошибка возникает, когда разработчик создаёт метод, который логически относится к классу целиком
 (а не к отдельным экземплярам), но забывает пометить его ключевым словом static. 
 В результате метод добавляется в прототип класса и становится доступным только через экземпляры,
  что противоречит его предназначению и делает невозможным вызов через имя класса.

Пример ошибки (код):

class Logger {
  log(message)
  {
    console.log(message);
  }
}
Logger.log('Test'); // Ошибка: log не является статическим методом
Описание:

Эта ошибка является зеркальной противоположностью Типа ошибки 2. Если там проблема была в попытке
 вызвать нестатический метод из статического, то здесь проблема в том, что метод, который по логике 
 должен быть статическим, не помечен соответствующим образом.

Решение:

Добавьте static.

class Logger {
  static log(message) {
    console.log(message);
  }
}
Logger.log('Test'); // OK
Всегда явно помечайте методы как static, если они логически относятся к классу, а не к его экземплярам.
 Это сделает ваш код более чистым, логичным и эффективным.

Тип ошибки 4: Наследование статических методов без учёта this
Описание:

При наследовании this в статическом методе дочернего класса ссылается на него, а не на родительский класс.

Пример ошибки (код):

class Parent {
  static create() {
    return new this();
  }
}

class Child extends Parent {}
Child.create(); // Создаст экземпляр Child, а не Parent
Объяснение:

Если нужно создать экземпляр родительского класса, используйте явное указание.

static create() {
  return new Parent();
}
Если метод не работает с данными экземпляров, объявляйте его как static. 
Для доступа к экземплярам создавайте их явно через new this().

Решение:

class Parent {
  static create() {
    return new Parent(); // Явное указание класса
  }
}

class Child extends Parent {}

const child = Child.create(); // Теперь всегда создаётся Parent
console.log(child instanceof Parent); // true
console.log(child instanceof Child); // false
Практикум
Задача 1: Сравнение скорости животных
Ситуация:

Вы разрабатываете приложение для зоопарка, где нужно сравнить скорость двух животных.
 У каждого животного есть имя и показатель скорости.

Задача:

Создайте класс Animal с полями name и speed.
Добавьте статический метод compareSpeed, который принимает двух животных и возвращает строку с результатом сравнения.
Создайте два экземпляра животных с разными показателями скорости.
Вызовите статический метод для их сравнения.
Решение:

class Animal {
  constructor(name, speed) {
    this.name = name;
    this.speed = speed;
  }

  static compareSpeed(animal1, animal2) {
    if (animal1.speed > animal2.speed) {
      return `${animal1.name} быстрее ${animal2.name}`;
    } else {
      return `${animal2.name} быстрее ${animal1.name}`;
    }
  }
}

const cheetah = new Animal('Гепард', 120);
const turtle = new Animal('Черепаха', 5);

console.log(Animal.compareSpeed(cheetah, turtle)); // "Гепард быстрее Черепахи"
Задача 2: Каррирование функции
Ситуация:

Вам нужно создать функцию для расчёта стоимости доставки, где базовая цена фиксирована,
 а расстояние может меняться.

Задача:

Создайте функцию calculateDelivery(basePrice, distance).
Используйте bind для создания новой функции с фиксированной базой.
Вызовите новую функцию для разных расстояний.
Решение:

function calculateDelivery(basePrice, distance) {
  return basePrice + distance * 0.5;
}

const moscowDelivery = calculateDelivery.bind(null, 100);

console.log(moscowDelivery(10)); // 105 (100 + 10*0.5)
console.log(moscowDelivery(20)); // 110 (100 + 20*0.5)
Итоги
Сегодня мы:

разобрали методы функций call, apply и bind;
поняли, как они управляют контекстом выполнения (this);
изучили практическое применение этих методов;
рассмотрели реальные сценарии использования;
познакомились со статическими методами классов;
узнали, чем статические методы отличаются от обычных;
поняли, когда стоит использовать статические методы;
разобрались, когда использовать call/apply, а когда — bind.
Теперь вы можете уверенно управлять контекстом выполнения в JavaScript и выбирать оптимальные подходы для разных задач.