На прошлом занятии мы разобрали, как работают самовызывающиеся функции (IIFE), 
рекурсия и замыкания — три мощных инструмента, помогающие структурировать код, 
управлять областью видимости и сохранять состояние.

Сегодня мы:

попрактикуемся в создании самовызывающихся функций, 
чтобы изолировать переменные и избежать загрязнения глобальной области;
напишем рекурсивные функции для вычисления чисел Фибоначчи, факториала, 
а также для проверки палиндромов и генерации перестановок;
создадим функции с замыканиями для реализации счётчиков и кеширующих обёрток.


Глоссарий к седьмому занятию
Область видимости (Scope) — контекст, в котором доступны переменные. 
При помощи IIFE мы создаём отдельную локальную область, защищая переменные от влияния извне.

Глобальное загрязнение (Global Pollution) — ситуация, 
когда переменные попадают в глобальную область видимости и могут конфликтовать 
с другими переменными или функциями. IIFE помогает этого избежать.

Перестановка (Permutation) — различные способы упорядочивания элементов. 
Рекурсивные функции позволяют находить все возможные перестановки элементов массива.

Кеширование (Memoization) — оптимизация, 
при которой результат функции запоминается и используется повторно. 
Часто реализуется через замыкание, чтобы хранить результаты внутри обёртки.

Обёртка (Wrapper Function) — функция, 
которая расширяет или изменяет поведение другой функции. 
Например, можно обернуть функцию в кеширующую обёртку, чтобы повысить производительность.






Прежде чем приступить к задачам, давайте освежим в памяти основные концепции, 
которые мы будем использовать. 
Это поможет вам уверенно ориентироваться в практике и понимать, почему решения работают именно так.

Мы кратко вспомним:

как работают самовызывающиеся функции (IIFE) и зачем они нужны;
что такое рекурсия и как она позволяет строить решения шаг за шагом;
как замыкания сохраняют данные и позволяют создавать гибкие функции с памятью.

Самовызывающиеся функции (IIFE)
Самовызывающаяся функция (IIFE) — это функция, 
которая выполняется сразу после определения. 
Её основная задача — создание изолированной области видимости, 
что особенно полезно, когда нужно избежать загрязнения глобального пространства переменных.

Пример:

(function() {
    let message = "Привет из IIFE!";
    console.log(message);
})();
В этом примере переменная message доступна только внутри IIFE, 
и мы избегаем её случайного изменения в других частях программы.

Рекурсия (Recursion)
Рекурсия — это подход, при котором функция вызывает саму себя для решения задачи. 
Однако, чтобы избежать бесконечного вызова, 
рекурсивная функция должна иметь базовый случай — условие завершения.

Пример: функция для подсчёта факториала числа.

function factorial(n) {
    if (n <= 1) {
        return 1;  // Базовый случай
    }
    return n * factorial(n - 1);  // Рекурсивный вызов
}

console.log(factorial(5));  // Выведет: 120
Здесь функция factorial вызывает саму себя, пока не достигнет базового случая n === 1.


Замыкания (Closure)
Замыкание — это способность функции «запоминать» переменные из области видимости, 
в которой она была создана, даже если эта область уже недоступна.

Это позволяет формировать гибкие функции, которые могут работать с данными, сохранёнными в их окружении.

Пример:

function createCounter() {
    let count = 0;
    return function() {
        count++;
        return count;
    };
}

const counter = createCounter();
console.log(counter());  // 1
console.log(counter());  // 2

Здесь функция createCounter возвращает внутреннюю функцию, 
которая может использовать переменную count из внешней области видимости. 
Благодаря замыканию count остаётся доступной, даже когда выполнение функции createCounter завершилось.






Типичные ошибки
Ошибка 1: Глобальное загрязнение при отсутствии IIFE
Описание ошибки: если не использовать IIFE или другие способы изоляции, 
переменные из вспомогательных функций могут попасть в 
глобальную область видимости и случайно переопределить другие переменные.

Пример ошибки:

let count = 0;

function run() {
    count = 100;  // Затирает глобальную переменную
}

run();
console.log(count);  // 100, хотя, возможно, ожидалось 0
Решение: изолируйте вспомогательные переменные внутри IIFE.

(function() {
    let count = 100;  // Локальная переменная внутри IIFE
    console.log(count);
})();




Ошибка 2: Слишком глубокая рекурсия
Описание ошибки: даже при правильном базовом случае чрезмерная глубина рекурсии (например, 
    fibonacci(10000)) может привести к переполнению стека вызовов.

Пример ошибки:

function fib(n) {
    if (n <= 1) return n;
    return fib(n - 1) + fib(n - 2);
}

console.log(fib(10000));  // RangeError: Maximum call stack size exceeded
Решение: для задач с глубокой рекурсией используйте:

мемоизацию (кэширование),
итеративный подход,
хвостовую рекурсию (если поддерживается движком).
function fibMemo() {
    let cache = {};

    return function f(n) {
        if (n <= 1) return n;
        if (cache[n]) return cache[n];
        cache[n] = f(n - 1) + f(n - 2);
        return cache[n];
    };
}

const fib = fibMemo();
console.log(fib(100));  // Быстро и без переполнения






Ошибка 3: Замыкание без обновления внешнего состояния
Описание ошибки: при создании счётчика через замыкание можно забыть обновить его. 
Это приведёт к тому, что функция всегда будет возвращать одно и то же значение.

Пример ошибки:

function createCounter() {
    let count = 0;
    return function() {
        return count;  // Не увеличивается!
    };
}

const counter = createCounter();
console.log(counter());  // 0
console.log(counter());  // 0
Решение: не забудьте обновить переменную.

function createCounter() {
    let count = 0;
    return function() {
        count++;
        return count;
    };
}





Ошибка 4: Нарушение принципа инкапсуляции
Описание ошибки: при реализации кеширующей функции можно 
случайно «выставить наружу» внутренние данные, позволив внешнему коду их изменять.

Пример ошибки:

function createCache() {
    let cache = {};
    return {
        cache: cache,  // Утечка внутреннего состояния!
        get: key => cache[key],
        set: (key, value) => cache[key] = value
    };
}

const c = createCache();
c.set("x", 10);
c.cache["x"] = 999;  // Нарушение инкапсуляции
console.log(c.get("x"));  // 999
Решение: не возвращайте ссылку на внутренние структуры данных.

function createSafeCache() {
    const cache = {};
    return {
        get: key => cache[key],
        set: (key, value) => cache[key] = value
    };
}





Ошибка 5: Использование var в цикле при создании замыканий
(Повтор ошибки из теории, но встречается на практике чаще всего, особенно в задачах на генерацию счётчиков и обёрток).

Описание ошибки: все функции, созданные в цикле, ссылаются на одну и ту же переменную i, если используется var.

Пример:

function makeFunctions() {
    let funcs = [];
    for (var i = 0; i < 3; i++) {
        funcs.push(function() {
            return i;
        });
    }
    return funcs;
}

let fs = makeFunctions();
console.log(fs[0]());  // 3
console.log(fs[1]());  // 3
console.log(fs[2]());  // 3
Решение: используйте let или создайте дополнительное замыкание.

for (let i = 0; i < 3; i++) {
    // работает как ожидается
}
или

(function(i) {
    // создаём копию i
})(i);










Теперь, когда мы освежили в памяти ключевые понятия (самовызывающиеся функции, 
    рекурсию и замыкания) и разобрали типичные ошибки, связанные с ними, 
    пора перейти к практике. Впереди — задачи, 
    которые помогут вам закрепить знания и увидеть, как концепции работают в реальных сценариях.











Практика
Задача 1: Вывод чисел Фибоначчи до N-го элемента (Рекурсия)
Аутентичная ситуация:
Представьте, 
что вы разрабатываете математическую библиотеку для анализа числовых последовательностей. 
Одной из функций должна быть генерация чисел ряда Фибоначчи.

Задача:
Напишите рекурсивную функцию fibonacci, которая принимает число n и возвращает значение N-го элемента ряда Фибоначчи.

Шаги реализации:

Определите базовый случай для функции, чтобы остановить бесконечную рекурсию:
Если n равно 1, верните 0.
Если n равно 2, верните 1.
Опишите рекурсивный случай, который вызывает функцию fibonacci для предыдущих двух элементов и возвращает их сумму.
Реализуйте вывод всех элементов ряда Фибоначчи до N-го.
Решение:

function fibonacci(n) {
    if (n === 1) return 0;  // Базовый случай
    if (n === 2) return 1;  // Базовый случай
    return fibonacci(n - 1) + fibonacci(n - 2);  // Рекурсивный случай
}

// Пример использования:
let n = 7;  // N-й элемент
for (let i = 1; i <= n; i++) {
    console.log(`Fibonacci(${i}):`, fibonacci(i));
}











Задача 2: Создание кеширующей функции через замыкание
Аутентичная ситуация:
Вы работаете над приложением, 
которое выполняет частые повторяющиеся вычисления, 
например вычисление факториала числа. 
Чтобы улучшить производительность, 
необходимо создать механизм кеширования для сохранения уже вычисленных значений.

Задача:
Создайте функцию createCachedFactorial, которая возвращает замыкание:
 оно должно принимать число, вычислять его факториал и сохранять результат в кеше для повторного использования.

Шаги реализации:

Создайте функцию createCachedFactorial, внутри которой объявите объект cache для хранения уже вычисленных значений.
В возвращаемой функции проверьте, есть ли число в кеше:
Если да, верните результат из кеша.
Если нет, выполните вычисление, сохраните результат в кеше и верните его.
Используйте рекурсию для вычисления факториала.
Решение:

function createCachedFactorial() {
    let cache = {};  // Замыкание для хранения кеша

    function factorial(n) {
        if (n <= 1) return 1;  // Базовый случай
        if (cache[n]) return cache[n];  // Проверка кеша

        let result = n * factorial(n - 1);  // Рекурсивное вычисление
        cache[n] = result;  // Сохранение результата в кеше
        return result;
    }

    return factorial;
}

// Пример использования:
const cachedFactorial = createCachedFactorial();

console.log(cachedFactorial(5));  // 120
console.log(cachedFactorial(5));  // 120 (взято из кеша)
console.log(cachedFactorial(6));  // 720

Эти задачи помогут нам лучше понять, 
как использовать рекурсию для решения сложных задач и как применять замыкания для создания оптимизированных решений.

Итоги:
На этом занятии мы сосредоточились на практике:

Написали рекурсивную функцию для вычисления чисел Фибоначчи, 
разобрав базовый случай и принцип пошагового вызова. 
Теперь вы умеете применять рекурсию для решения задач, 
в которых требуется последовательное разложение задачи на подзадачи.

Создали кеширующую функцию с использованием замыкания для оптимизации вычисления факториала. 
Мы увидели, как замыкания позволяют сохранять внутреннее состояние 
функции между вызовами и повышать производительность без дублирования расчётов.