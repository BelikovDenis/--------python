Урок №3. Циклы (for, while, do while). Область видимости. 

Учебные материалы

Циклы — это один из ключевых инструментов в программировании; 
позволяет выполнять повторяющиеся действия. 
Они особенно полезны, когда требуется перебирать набор данных, 
выполнять серию вычислений или повторять одно и то же действие до тех пор, 
пока выполняется определённое условие.


Если на прошлом занятии мы научились управлять выполнением кода с помощью условий (if, else, switch),
то сегодня пойдём дальше и научимся повторять код с помощью циклов for, while, do...while.

Помимо циклов, мы также разберём область видимости переменных — важную тему, 
при изучении которой научимся понимать, 
где в программе доступны наши переменные и как избежать конфликтов между ними. 
Это особенно важно при использовании разных способов объявления: var, let, сonst.


Сегодня мы:

вспомним, зачем нужны циклы, и разберём три их типа в JavaScript (for, while, и do while), 
а также научимся работать с ними в повторяющихся операциях.
научимся применять каждый из них в зависимости от задачи: 
когда количество повторений известно заранее, 
когда оно зависит от условий, когда код должен выполниться хотя бы один раз;
разберём, как работает область видимости переменных и чем отличаются var, let и const.






Глоссарий к третьему занятию
Loop (Цикл)

Loop — структура, которая позволяет многократно выполнять один и тот же блок кода.

For loop (Цикл for)

For loop — цикл, который выполняется заранее заданное количество раз, 
часто используется, когда известно количество повторений.

While loop (Цикл while)

While loop — цикл, который выполняется, пока выполняется заданное условие (или пока оно истинно).

Do while loop (Цикл do while)

Do while loop — цикл, который выполняется хотя бы один раз, 
а затем продолжает выполняться, если условие истинно.

Scope (Область видимости)

Scope — контекст, в котором определена переменная, функция или объект, 
который определяет, где они могут быть использованы.

Global scope (Глобальная область видимости)

Global scope — область видимости, которая охватывает весь код программы. 
Переменные, объявленные в глобальной области видимости, доступны в коде везде.

Local scope (Локальная область видимости)

Local scope — область видимости, которая ограничена функцией или блоком кода, 
в котором объявлены переменные или функции.

Block scope (Блочная область видимости)

Block scope — область видимости, которая ограничена фигурными скобками ({}), 
например в цикле или условной конструкции.






В этом разделе мы подробно разберём три типа циклов, 
которые используются в JavaScript: for, while, do...while. 
Также мы изучим, как управлять областью видимости переменных внутри таких конструкций.
Понимание этих основ позволит писать код, который легко читается, 
не содержит скрытых ошибок и работает предсказуемо.


Циклы в JavaScript


Рисунок 1
Как мы помним из курса по Python, циклы — это конструкции, 
которые позволяют многократно выполнять блоки кода. 
Они особенно полезны при работе с большим количеством данных, 
когда нам необходимо реализовать повторяющиеся операции.

В JavaScript есть три основных типа циклов: for, while и do while. 
Давайте разберем их по отдельности и рассмотрим, в каких случаях эти типы циклов стоит использовать.

Цикл for

Когда мы заранее знаем, сколько раз нужно выполнить код, лучше всего использовать цикл for.

Его структура предельно ясна:

for (let i = 0; i < 5; i++) {
    console.log(i);
}

В этом примере:

let i = 0 — начальное значение переменной i.
i < 5 — условие, при котором цикл выполняется.
i++ — шаг цикла, увеличение переменной i на 1 после каждого выполнения блока кода.

Здесь блок кода выполнится 5 раз, выводя числа от 0 до 4.

Цикл while

Если количество повторений заранее не известно, 
но зависит от выполнения какого-то условия, мы используем цикл while.

let i = 0;
while (i < 5) {
    console.log(i);
    i++;
}

Цикл продолжит выполнение до тех пор, пока условие (здесь i < 5) остаётся истинным. 
Если условие изначально ложное, код внутри цикла не выполнится.

Цикл do while

Иногда нужно выполнить блок кода хотя бы один раз, даже если условие не выполняется. 
Для таких случаев используется цикл do while:

let i = 0;
do {
    console.log(i);
    i++;
} while (i < 5);

Этот цикл точно выполняет тело хотя бы один раз, а затем проверяет условие.

Область видимости в JavaScript


Рисунок 2
Теперь, когда мы познакомились с циклами в JavaScript, нам будет важно понять, 
как управлять переменными внутри них. 
Область видимости определяет, где в коде можно использовать переменные.

В JavaScript есть три типа области видимости.

Глобальная область видимости: переменные, объявленные вне блоков {}, доступны везде.
let globalVar = "I am global";
{
    console.log(globalVar);  // Доступно
}

Локальная область видимости: переменные, объявленные внутри функции, доступны только в ней.
function myFunction() {
    let localVar = "I am local";
    console.log(localVar);  // Доступно
}

console.log(localVar);  // Ошибка: localVar не определена

Блочная область видимости: переменные, объявленные с помощью let или const внутри блока {}, доступны только внутри этого блока.
{
    let blockVar = "I am inside block";
    console.log(blockVar);  // Доступно
}
console.log(blockVar);  // Ошибка: blockVar не определена

Особенности работы с var, let, и const в циклах

Переменные, объявленные с помощью разных ключевых слов, ведут себя по-разному:

var: переменные, объявленные через var, 
имеют функциональную область видимости или глобальную область видимости, 
если объявлены вне функции.

Пример:

for (var i = 0; i < 3; i++) {
    console.log(i);  // Выведет 0, 1, 2
}
console.log(i);  // Доступно, значение переменной i равно 3

Кроме того, использование var в циклах может вызывать неожиданные 
результаты при работе с асинхронным кодом. Например, 
если вы используете внутри цикла переменную-счетчик внутри setTimeout(), 
результат будет выглядеть так, будто переменная-счетчик не менялась:

for (var i = 0; i < 3; i++) {
    setTimeout(() => console.log(i), 100);  // Выведет 3, 3, 3
}

Это происходит, потому что все итерации цикла используют одну и ту же переменную i.

Для решения проблемы можно использовать let, 
который создаёт новую переменную на каждой итерации:

for (let i = 0; i < 3; i++) {
    setTimeout(() => console.log(i), 100);  // Выведет 0, 1, 2
}
let и const: имеют блочную область видимости, что делает их более предсказуемыми в поведении.
for (let i = 0; i < 3; i++) {
    console.log(i);  // Выведет 0, 1, 2
}
console.log(i);  // Ошибка: i не определена

Бесконечные циклы

Бесконечный цикл — нежелательная процедура, которая возникает, 
если условие цикла никогда не становится ложным:

while (true) {
    console.log("Бесконечный цикл");
}
Чтобы избежать зависания программы, 
можно использовать оператор break для завершения цикла:

let i = 0;
while (true) {
    console.log(i);
    i++;
    if (i === 5) {
        break;  // Прерывание цикла
    }
}
Оператор continue

Оператор continue пропускает текущую итерацию и переходит к следующей:

for (let i = 0; i < 5; i++) {
    if (i === 2) {
        continue;  // Пропускаем итерацию, где i равно 2
    }
    console.log(i);  // Выведет 0, 1, 3, 4
}






Типичные ошибки
Тип ошибки 1: Бесконечный цикл

Бесконечный цикл может возникать, когда изначальное условие выполняется. 
Он не прерывается вне зависимости от содержимого цикла и продолжает выполняться бесконечно.

Пример ошибки:

let i = 0;
while (i < 10) {
    console.log(i);  // Ожидаем вывод чисел от 0 до 9, но цикл никогда не завершится, необходимо приостанавливать программу
}
Решение:
Проверьте условие и убедитесь, что переменная изменяется внутри цикла, 
чтобы условие в какой-то момент стало ложным.

let i = 0;
while (i < 10) {
    console.log(i);
    i++;  // Увеличиваем переменную, чтобы цикл завершился
}




Тип ошибки 2: Использование переменной до её объявления и hoisting

Когда мы объявляем переменную с помощью ключевых слов let или const внутри цикла, 
область видимости ограничивается самим циклом, 
а при попытке использовать переменную после цикла обнаруживается ошибка.

Пример

for (let i = 0; i < 5; i++) {
    console.log(i);
}
console.log(i);  // Ошибка: i не доступна здесь

На самом деле это не баг, а фича: 
даже если в последующем коде у нас окажется другая переменная i, 
означающая какой-то совсем другой параметр, мы точно будем знать, 
что к каждой i мы можем обратиться только из своего блока кода, 
и не перепутаем их значения.

Если же нам нужно снова обратиться к переменной из цикла извне, 
следует объявить переменную с помощью ключевого слова var. 
Объявленная переменная «поднимется» во внешнюю область видимости – это называется hoisting.

Пример:

for (var i = 0; i < 5; i++) {
    console.log(i);
}
console.log(i);  // Очень осторожно: i доступна везде, включая эту строку, из-за hoisting
Решение:
Используйте let или const, которые имеют блочную область видимости и не подвержены hoisting.

for (let i = 0; i < 5; i++) {
    console.log(i);
}
console.log(i);  // Ошибка: i не доступна здесь





Тип ошибки 3: Область видимости переменной в цикле

Переменные, объявленные с помощью var внутри цикла, 
будут доступны за пределами цикла, что может привести к неожиданным результатам.

Пример ошибки:

for (var i = 0; i < 3; i++) {
    console.log(i);  // Выводит 0, 1, 2
}
console.log(i);  // 3, т. к. i доступна за пределами цикла
Решение:
Используйте let или const для объявления переменных в цикле, чтобы они имели блочную область видимости.

for (let i = 0; i < 3; i++) {
    console.log(i);  // Выводит 0, 1, 2
}
console.log(i);  // Ошибка: i не доступна за пределами цикла





Тип ошибки 4: Ошибка при выходе из цикла с условием

Если условие в цикле неправильно задано или оно никогда не выполняется, 
цикл может не запуститься совсем или работать бесконечно.

Пример ошибки:

let i = 10;
while (i < 10) {  // Условие неправильно
    console.log(i);
    i++;
}

Решение:
Проверьте условие: оно должно выполняться, когда цикл активен.

let i = 0;
while (i < 10) {  // Условие теперь верное
    console.log(i);
    i++;
}







Практикум
Задача 1: Сумма чисел от 1 до N

Ситуация: мы разрабатываем функцию для финансового приложения. 
Нам нужно посчитать сумму чисел от 1 до заданного пользователем числа, 
чтобы использовать её для расчётов, например в накопительных системах.

Задача: написать программу, 
которая запрашивает у пользователя число N и выводит сумму всех чисел от 1 до N включительно.

Шаги реализации:

Запросите у пользователя число N с помощью prompt().
Убедитесь, что пользователь ввёл корректное число.
Используйте цикл for для подсчёта суммы чисел.
Выведите результат в консоль.
Решение:

let n = prompt("Введите число N:");
n = Number(n);

if (isNaN(n) || n <= 0) {
    console.log("Ошибка: введите положительное число.");
} else {
    let sum = 0;
    for (let i = 1; i <= n; i++) {
        sum += i;
    }
    console.log(`Сумма чисел от 1 до ${n}: ${sum}`);
}





Задача 2: Студенческий органайзер

Ситуация: мы разрабатываем приложение для студентов, 
которое помогает им управлять своим временем и отслеживать, 
сколько часов они могут посвятить сну. Студент хочет узнать, 
сколько времени на сон у него есть каждый день в течение недели, 
учитывая продолжительность учебных пар и время, потраченное на дорогу.

Задача: написать программу, 
которая для каждого дня недели (с понедельника по пятницу) запрашивает количество пар, 
которые есть у студента, и вычисляет, сколько времени у него остаётся на сон. 

Учтите следующие моменты:

Пара длится 1,5 часа.
Дорога в университет и обратно занимает 1 час.
Если в какой-то день недели 0 пар, то студент может спать весь день (ведь ехать тоже никуда не нужно).
Если количество пар больше 5, выводится ошибка.

Шаги реализации:

Для каждого дня недели с понедельника по пятницу запросите у пользователя количество пар.
Проверьте, что введённое значение — это число от 0 до 5.
Для каждого дня вычислите, сколько часов студент может потратить на сон с учётом времени на пары и дорогу.
Выведите результат для каждого дня недели.
Решение:

const hoursInDay = 24; // Общее количество часов в сутки
const classDuration = 1.5; // Продолжительность одной пары в часах
const travelTime = 1; // Время в пути (в одну сторону)

for (let day = 1; day <= 5; day++) { // Для каждого дня с понедельника по пятницу
  let classes = parseInt(prompt(`Введите количество пар в день ${day} (от 0 до 5):`));

  // Проверка на корректность ввода
  if (isNaN(classes) || classes < 0 || classes > 5) {
    console.log("Ошибка: количество пар должно быть от 0 до 5.");
    break; // Прерываем цикл, если введено некорректное значение
  }

  let totalClassTime = classes * classDuration; // Время, потраченное на пары
  let totalTravelTime = 2 * travelTime; // Время на дорогу туда и обратно

  // Если пар нет, можно спать весь день
  if (classes === 0) {
    console.log(`В день ${day} у вас нет пар и дороги! У вас есть целый день для сна.`);
  } else {
    // Вычисляем оставшееся время на сон
    let availableSleepTime = hoursInDay - totalClassTime - totalTravelTime;
    console.log(`В день ${day} у вас ${availableSleepTime} часов для сна.`);
  }
}











Итоги:
На этом занятии мы изучили основы работы с циклами и областью видимости в JavaScript:

познакомились с тремя основными типами циклов: for, while, и do while;
научились использовать операторы инкремента и декремента для изменения значений переменных в цикле;
изучили распространённые ошибки, такие как бесконечные циклы, 
проблемы с областью видимости и неправильное использование break, и научились их избегать.