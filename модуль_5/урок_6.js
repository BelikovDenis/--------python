Урок №6. Самовызывающиеся функции. Рекурсия. Замыкания – Теория.

Учебные материалы

Сегодня мы поговорим сразу о трёх продвинутых концепциях JavaScript, 
которые открывают новые возможности при работе с функциями.

Самовызывающиеся функции (IIFE). Они позволяют изолировать код и запускать его немедленно, 
не оставляя следов в глобальной области.

Рекурсия — способ решения задач, при котором функция вызывает саму себя, 
пока не достигнет нужного условия.

Замыкания — механизм, благодаря которому функция «помнит» своё окружение, даже если вызывается вне его.

На первый взгляд, это три совершенно разные идеи. 
Но на практике они помогают решать похожие задачи: изолировать логику, 
управлять состоянием, сохранять данные, избегать конфликтов переменных и писать компактный, выразительный код.

Сегодня мы:

разберём, как устроены самовызывающиеся функции (IIFE) и для чего они используются;
узнаем, как работает рекурсия — способ, при котором функция вызывает саму себя;
научимся определять базовый случай и управлять глубиной рекурсии;
познакомимся с понятием замыкания и узнаем, как функция может сохранять своё окружение;
обсудим, что такое чистые функции и как писать код без побочных эффектов.




Глоссарий к шестому занятию
IIFE (Immediately Invoked Function Expression) — самовызывающаяся функция — специальная функция, 
которая вызывается сразу после её определения. Используется для создания изолированных областей видимости.

Recursion (Рекурсия) — техника программирования, 
при которой функция вызывает саму себя для выполнения задачи.

Base case (Базовый случай) — условие, при котором рекурсивная функция прекращает вызывать саму себя, 
предотвращая бесконечный цикл.

Closure (Замыкание) — механизм, благодаря которому функция «запоминает» своё окружение, 
даже если вызывается вне своей области видимости.

Pure function (Чистая функция) — функция, 
которая при одном и том же входе всегда возвращает одинаковый результат и не имеет побочных эффектов.

Lexical environment (Лексическое окружение) — контекст выполнения кода, 
в котором определены переменные и функции.


Самовызывающаяся функция — это функция, которая выполняется сразу после своего определения. 
Такая конструкция позволяет создать изолированную область видимости без добавления 
лишних переменных и функций в глобальное пространство.

Представьте кофеварку, которая не ждёт команды, а включается сразу после подключения к сети.
 Самовызывающаяся функция делает то же самое: определяется — и сразу запускается.

Это особенно полезно, когда:

нужно выполнить код один раз при инициализации;
важно, чтобы переменные не были доступны снаружи;
вы создаёте модули или обёртки, логика которых должна быть изолирована.

1. Самовызывающиеся функции (IIFE)
Самовызывающиеся функции называются IIFE (Immediately Invoked Function Expressions), 
то есть «выражения функций, вызываемые немедленно». 
Это особая форма записи, в которой функция определяется и сразу же выполняется.

Где это пригодится?

IIFE часто применяются:

при создании библиотек и модулей, чтобы скрыть внутреннюю реализацию;
при одноразовой инициализации (например, при загрузке страницы, 
    когда нужно настроить окружение без лишних переменных в глобальной области);
для изоляции переменных и предотвращения конфликтов имён.
Пример: при загрузке страницы вы хотите настроить тему интерфейса. 
Вместо того чтобы оставлять вспомогательные переменные в глобальной области, 
вы можете обернуть настройку в IIFE.

Иногда нам нужно, чтобы часть кода выполнилась один раз (сразу при загрузке) 
и при этом не оставила за собой следов: никаких переменных или функций в глобальной области. 
В таких случаях и используются IIFE — Immediately Invoked Function Expression.

Как это работает?
IIFE оборачиваются в круглые скобки, чтобы превратить объявление функции в выражение. 
Затем эта функция немедленно вызывается с помощью дополнительных круглых скобок.

Пример IIFE:

(function() {
    console.log("Я выполнюсь сразу после определения!");
})();
Для ещё большей краткости можно писать даже так:

(() => {
    console.log("Меня назыывают стрелочная IIFE из-за стрелки в объявлении ");
})();
Почему это полезно?

Изоляция кода: переменные внутри IIFE не доступны за её пределами.
Инициализация данных: можно выполнять одноразовые действия, 
такие как настройка окружения или вычисление начальных значений.
Что было бы без IIFE?

Допустим, при загрузке страницы мы хотим настроить определённые пользовательские параметры. 
Технически можно обойтись обычными функциями:

let theme = "light";
let fontSize = 14;
console.log("Настройки пользователя загружены.");
// ...и дальше идёт основной код страницы
Но теперь переменные theme и fontSize лежат в глобальной области — 
они могут случайно пересекаться с другими переменными.

Чтобы избежать этого, мы можем создать функцию initSettings, затем вызвать её, а потом вручную удалить:

function initSettings() {
    let theme = "light";
    let fontSize = 14;
    console.log("Настройки пользователя загружены.");
}
initSettings();
// Всё выполнено, но имя функции осталось в области видимости
С IIFE всё лаконично и просто

(function() {
    let theme = "light";
    let fontSize = 14;
    console.log("Настройки пользователя загружены.");
})();
Код выполнился.

Внешняя область осталась «чистой»: никаких лишних переменных или имён функций.

Логика изолирована и выполнена сразу.







2. Рекурсия

Как мы помним из курса по Python, рекурсия — не совсем инструмент языка программирования. 
Это скорее способ решения задачи.

Вкратце напомним, что рекурсия – это подход, при котором функция вызывает саму себя. 
Еще важно напомнить, что обычно нам не хочется, чтобы программа выполнялась бесконечно.

Рекурсия похожа на инструктаж: «Когда закончишь — передай следующему». 
Каждый «следующий» делает то же самое, пока процесс не дойдёт до базового случая — последнего в цепочке.

Именно поэтому для успешной работы с рекурсией необходимо задать базовый случай: прописать, 
когда программа должна перестать вызывать саму себя и как ей себя вести.

Пример: простой обратный отчет

function countdown(n) {
    if (n <= 0) {  // Базовый случай
        console.log("Старт!");
        return;
    }
    console.log(n);
    countdown(n - 1);  // Рекурсивный вызов
}

countdown(5);
// Вывод: 5, 4, 3, 2, 1, Старт!

Конечно, многие задачи, связанные с перечислением, можно решать и с помощью циклов. 
Часто выбор между рекурсией и циклом остаётся делом вкуса и удобства. 
Рекурсия же, хоть и выглядит с первого взгляда странновато, на самом деле хорошо знакома каждому человеку.

Взгляните на обратный отсчёт: когда вы считаете в уме от 5 до 1, 
вы не держите сразу в голове весь список чисел, 
а просто «вызываете» следующее число (на единицу меньше). 
Вот и рекурсия работает так же: шаг за шагом, пока не дойдёт до конца.

Рекурсия vs циклы

Иногда использование циклов всё же предпочтительно:

Для простых конструкций и перечислений глубокая рекурсия может быть сложной для чтения и отладки.
В сложных алгоритмах (если рекурсия идёт слишком глубоко) программа может завершиться ошибкой, 
если не справится по памяти.
Последнее может произойти и в нетрудных с виду задачах. 
Представьте, например, такой алгоритм возведения произвольного числа в произвольную степень:

function power(base, exponent) {
    if (exponent === 0) return 1;
    return base * power(base, exponent - 1);
}
То есть, например, чтобы получить 2 в кубе (степень 3), 
это как получить 2 в квадрате (степень 2), но на один повыше, 
только еще раз умноженное на 2

Теперь представим, что мы хотим вычислить 2 в степени 10000:

console.log(power(2, 10000));
Тогда наша функция, для того чтобы посчитать для нас power(2, 10000), 
должна сперва посчитать и держать в памяти результат от power(2, 9999). 
Для этого она должна держать в памяти результат power(2, 9998) и так далее, вплоть до базового случая power(2, 0)

– итого программа должна будет держать в памяти все 10001 выражения и наверняка не справится по памяти, 
вызвав ошибку:

Uncaught RangeError: Maximum call stack size exceeded

Почему с рекурсией бывает удобнее

В простых случаях цикл может оказаться короче и понятнее. 
Но представим, что нам нужно, например, обойти вложенные папки, 
у каждой из которых внутри могут быть другие папки. 
Или пройтись по DOM-элементам, у которых есть вложенные «дети». 
В таких ситуациях структура данных имеет неопределённую глубину. 
И именно здесь рекурсия оказывается особенно мощной.

Если у вас коробка в коробке, а в ней ещё коробка — цикл не поможет, 
потому что вы заранее не знаете, сколько там уровней. 
А вот рекурсия — универсальный способ открыть всё до конца.

Вот пример, где мы хотим перебрать элементы в списке, отдельные составляющие которого – тоже списки:

function printNestedList(items) {
    for (let item of items) {
        if (Array.isArray(item)) { // Проверяем, что item – массив; разберём этот метод подробнее на следующих занятиях
            printNestedList(item);  // Если item – массив, к нему можно применить printNestedList
        } else {
            console.log(item);
        }
    }
}

printNestedList(["a", ["b", ["c", "d"]], "e"]);
// Выведет: a, b, c, d, e
Здесь без рекурсии пришлось бы заранее знать, насколько глубоко нужно вложиться, 
и писать много повторяющегося кода.





3. Замыкания (Closures)
Замыкание – это функция, 
которая запоминает переменные из внешней области видимости даже после того, 
как эта внешняя функция уже завершила своё выполнение.

Проще говоря, если функция «родилась» внутри другой функции, 
она будет помнить всё, что там происходило.

Зачем это нужно?
Замыкания часто используются при написании обработчиков событий, 
в функциях с «памятью», при создании фабрик функций, в модулях и при реализации инкапсуляции: 
когда нам нужно «спрятать» переменные и не давать к ним доступ напрямую.

Давайте на практике разберём, как работать с замыканиями.

Как это работает?
Когда функция создаётся, она сохраняет ссылку на переменные в своём внешнем окружении. 
Эти переменные остаются доступными для функции, даже если сама функция передаётся куда-то ещё.

Пример:

function createCounter() {
    let count = 0;  // Замкнутая переменная

    return function() {  // Вложенная функция — замыкание
        count++;
        return count;
    };
}

const counter = createCounter();  // Функция возвращает замыкание

console.log(counter());  // 1
console.log(counter());  // 2
console.log(counter());  // 3

Здесь функция createCounter возвращает вложенную функцию, 
которая сохраняет доступ к переменной count. 
Каждый вызов counter увеличивает count, демонстрируя, что переменная «живёт» внутри замыкания.

Зачем нужны замыкания?
Сохранение состояния: замыкания позволяют хранить данные, связанные с функцией. 
Это полезно, когда нам нужно сохранить настройки или параметры.

Инкапсуляция данных: замыкания помогают создавать приватные переменные, 
которые нельзя изменить из внешнего кода.

Пример:

function createUser(name) {
    let password = "1234";  // Приватная переменная

    return {
        getName: function() {
            return name;
        },
        checkPassword: function(input) {
            return input === password;
        }
    };
}

const user = createUser("Alice");
console.log(user.getName());  // Alice
console.log(user.checkPassword("1234"));  // true
console.log(user.password);  // undefined, переменная недоступна






3. Асинхронные операции: замыкания часто используются в асинхронных операциях (например, 
    при работе с таймерами или обработчиками событий).

Пример: мы хотим отложить выполнение какого-то действия, например вывод сообщения. 
Благодаря замыканиям переменные из внешней области видимости «сохраняются» и используются позже (когда операция завершится).

function delayMessage(message, delay, callback) {
setTimeout(() => {
      callback(message);  // замыкание "помнит" callback
    }, delay);
}

delayMessage("Выполнился через 2 секунды!", 2000, function(result) {
    console.log(result);
});












4. Мемоизация и кеширование: замыкания используются для хранения уже вычисленных данных, 
чтобы ускорить повторные вычисления.

Пример:

function memoize(fn) {
    const cache = {};  // Замкнутый объект для хранения результатов

    return function(arg) {
        if (cache[arg] !== undefined) {
            console.log("Fetching from cache:", arg);
            return cache[arg];
        }

        const result = fn(arg);
        cache[arg] = result;
        console.log("Computing result for:", arg);
        return result;
    };
}

function square(x) {
    return x * x;
}

const memoizedSquare = memoize(square);
console.log(memoizedSquare(4));  // Вычисляет и сохраняет в кеш
console.log(memoizedSquare(4));  // Возвращает из кеша

С этим примером есть одна проблема: такая мемоизация работает только для одного аргумента. 
Кроме того, это может быть только аргумент примитивного типа (число, строка и т. д.).

Если мы передадим, например, массив или объект, кеш сломается. 
Так происходит потому, что в JavaScript ключи в обычных объектах ({}) автоматически превращаются в строки.

Технически это можно пофиксить, используя методы работы с объектами:

const args = Array.from(arguments);
const key = JSON.stringify(args);
if (cache[key] !== undefined) { ... }
Например, мы можем вручную приводить аргументы к строке. 
Но продвинутые методы мы разберём позже на наших занятиях.

Когда стоит использовать замыкания?
Когда нужно создавать функции с привязанным состоянием (например, счётчики).
Для защиты данных, чтобы сделать их приватными.
В асинхронных операциях, чтобы «захватить» переменные.
При оптимизации вычислений через мемоизацию.
Замыкания — это мощный инструмент, но они требуют внимательного подхода, 
так как могут увеличивать использование памяти, если забыть про очистку ссылок на замкнутые переменные.







4. Ключевое слово arguments
arguments — это объект, доступный внутри каждой функции, 
содержащий все переданные ей аргументы. 
Он полезен в случае, когда функция принимает переменное количество параметров.

Пример использования arguments:

function sum() {
    let total = 0;
    for (let i = 0; i < arguments.length; i++) {
        total += arguments[i];
    }
    return total;
}

console.log(sum(1, 2, 3, 4));  // 10
Особенности:

arguments существует только в обычных функциях (но не в стрелочных).
Для более современного подхода используйте оператор расширения (...).
Пример с оператором ...:

function sum(...numbers) {
    return numbers.reduce((total, num) => total + num, 0);
}

console.log(sum(1, 2, 3, 4));  // 10







5. Чистые функции
Чистые функции — это функции, которые:

При одинаковых входных данных всегда возвращают одинаковый результат.
Не имеют побочных эффектов (не изменяют внешние переменные или состояние).
Пример чистой функции:

function add(a, b) {
    return a + b;  // Результат зависит только от входных данных
}
Чистые функции важны для написания предсказуемого и тестируемого кода.
Давайте рассмотрим теперь типичные ошибки и разберём, как их избежать.








Типичные ошибки
Ошибка 1: Самовызывающаяся функция без правильной синтаксической обёртки
Описание ошибки:
Самовызывающиеся функции (IIFE) требуют правильной обёртки, 
чтобы интерпретатор JavaScript корректно понял, что это выражение, 
а не объявление функции. Пропущенные круглые скобки могут вызвать синтаксическую ошибку.

Пример ошибки:

function() {
    console.log("Hello!");
}();  // SyntaxError: Function statements require a name

Решение:
Чтобы функция правильно воспринималась (как выражение), используйте обёртку из круглых скобок:

(function() {
    console.log("Hello!");
})();  // Выведет: Hello!

Или альтернативный вариант:

(() => {
    console.log("Hello!");
})();  // Выведет: Hello!





Ошибка 2: Бесконечная рекурсия
Описание ошибки:
Рекурсия требует точного условия завершения. 
Если условие завершения не указано или некорректно, 
программа может войти в бесконечный цикл вызовов, что приведёт к переполнению стека вызовов.

Пример ошибки:

function countDown(num) {
    console.log(num);
    countDown(num - 1);  // Нет условия завершения
}

countDown(5);  // Uncaught RangeError: Maximum call stack size exceeded

Решение:
Всегда добавляйте условие завершения, чтобы избежать бесконечной рекурсии:

function countDown(num) {
    if (num <= 0) {
        console.log("Done!");
        return;
    }
    console.log(num);
    countDown(num - 1);
}

countDown(5);  // 5, 4, 3, 2, 1, Done!









Ошибка 3: Потеря контекста в замыкании
Отдельно разберём распространенную ошибку: 
в замыкании теряется контекст переменной из-за её повторного использования.

Что происходит?
Когда мы создаём замыкания (например, внутри цикла), 
каждое из них ссылается на одну и ту же переменную, 
объявленную в более широкой области видимости. 
Если переменная изменяется, все замыкания «видят» это изменение, 
потому что используют одну и ту же переменную.

Пример ошибки:
Рассмотрим следующий код:

function createCounters() {
    let counters = [];
    for (var i = 0; i < 3; i++) {
        counters.push(function() {
            return i;  // Замыкается на переменную i
        });
    }
    return counters;
}

const counters = createCounters();
console.log(counters[0]());  // Ожидаем 0, но получаем 3
console.log(counters[1]());  // Ожидаем 1, но получаем 3
console.log(counters[2]());  // Ожидаем 2, но получаем 3

Почему это происходит?
В цикле for мы используем var для объявления переменной i.
Переменная i, объявленная через var, имеет функциональную область видимости, а не блочную.
Все функции, добавленные в массив counters, создают замыкания, но ссылаются на одну и ту же переменную i.
После завершения цикла значение i равно 3 (потому что цикл завершился), и каждая функция возвращает это значение.
Как исправить?
Использовать let: переменная let имеет блочную область видимости, 
то есть для каждой итерации цикла создаётся своя (отдельная) переменная.
Исправленный пример:

function createCounters() {
    let counters = [];
    for (let i = 0; i < 3; i++) {  // Используем let вместо var
        counters.push(function() {
            return i;  // Каждое замыкание получает своё значение i
        });
    }
    return counters;
}

const counters = createCounters();
console.log(counters[0]());  // 0
console.log(counters[1]());  // 1
console.log(counters[2]());  // 2

Теперь каждое замыкание «запоминает» своё значение i, 
потому что let создаёт новую переменную для каждой итерации.

Создать замыкание через функцию: если вы вынуждены использовать var, 
можно создать замыкание вручную с помощью дополнительной функции, 
которая принимает текущее значение i.
Пример:

function createCounters() {
    let counters = [];
    for (var i = 0; i < 3; i++) {
        (function(j) {  // Создаём новую функцию, чтобы замкнуть текущее значение i
            counters.push(function() {
                return j;
            });
        })(i);  // Передаём текущее значение i
    }
    return counters;
}

const counters = createCounters();
console.log(counters[0]());  // 0
console.log(counters[1]());  // 1
console.log(counters[2]());  // 2


Что здесь происходит?
На каждой итерации цикла вызывается функция (function(j) { ... })(i), в которую передаётся текущее значение i.

Внутри этой функции создаётся новое замыкание на переменную j, которая уже не меняется.

Таким образом, каждая функция в массиве counters запоминает своё уникальное значение j.









Ошибка 4: Неправильное использование чистых функций
Описание ошибки:
Чистые функции не должны изменять внешние переменные (иметь «побочные эффекты»). 
Однако нарушение этого принципа встречается часто.

Пример ошибки:

let total = 0;

function addToTotal(amount) {
    total += amount;  // Изменяет внешнюю переменную
    return total;
}

console.log(addToTotal(10));  // 10
console.log(addToTotal(20));  // 30
Решение:
Необходимо, когда это возможно, избегать изменения внешних переменных. 
Вместо этого следует возвращать новый результат:

function add(a, b) {
    return a + b;  // Чистая функция, которая не изменяет внешние переменные
}

console.log(add(10, 20));  // 30







Ошибка 5: Утечка памяти при замыканиях
Что происходит:
Если внутри функции создаётся большое значение (например, массив или объект) 
и на это значение замыкается возвращаемая функция, 
то даже после завершения внешней функции данные остаются в памяти, потому что на них есть ссылка. 
JavaScript не может их «выбросить» из памяти, пока существует хотя бы одна функция, 
которая может к ним обращаться.

Пример ошибки:

function createHandler() {
    const largeArray = new Array(1000000).fill(0);  // Занимает много памяти

    return function() {
        console.log("Handler called");
    };
}

const handler = createHandler();
// largeArray остаётся в памяти, хотя больше не используется

Решение:
Необходимо избегать ссылок на ненужные данные в замыканиях:

function createHandler() {
    return function() {
        console.log("Handler called");
    };
}

const handler = createHandler();  // Ненужные данные не сохраняются









// Практическая работа
// Тема: Самовызывающиеся функции (IIFE), рекурсия, замыкания
// Задача: «Корпоративная структура компании»

// -----------------------------------------------------------------------------
// ОПИСАНИЕ ЗАДАЧИ
// -----------------------------------------------------------------------------
// Необходимо разработать модуль «Организационная структура» с использованием
// IIFE, рекурсии и замыканий. Модуль должен управлять иерархией сотрудников:
// каждый сотрудник может иметь неограниченное количество подчинённых,
// те, в свою очередь, — своих подчинённых, и так далее.
//
// Модуль должен предоставлять следующие возможности (публичное API):
//   1. Добавление сотрудника (в корень или как подчинённого к существующему).
//   2. Удаление сотрудника вместе со всеми его подчинёнными (рекурсивно).
//   3. Поиск сотрудника по имени (первое вхождение) – возвращает информацию о сотруднике и его путь.
//   4. Подсчёт общего количества сотрудников в компании (рекурсивно).
//   5. Подсчёт суммарной зарплаты всех сотрудников (рекурсивно).
//   6. Вывод всей иерархии в консоль в виде дерева с отступами.
//   7. Возможность загрузить начальную структуру (например, генеральный директор и топ-менеджеры).
//
// ВНУТРЕННЯЯ РЕАЛИЗАЦИЯ ДОЛЖНА:
//   - Использовать IIFE для создания приватной области видимости и немедленного
//     создания начальной структуры (при необходимости).
//   - Хранить дерево сотрудников в приватной переменной, недоступной извне.
//   - Использовать рекурсию во всех операциях, связанных с обходом дерева.
//   - Использовать замыкания: методы, возвращаемые модулем, должны «замыкать»
//     ссылку на приватное дерево.
//   - При возврате данных внешнему коду (например, результатов поиска)
//     возвращать копии объектов, чтобы избежать неконтролируемого изменения извне.
//
// СТРУКТУРА ОБЪЕКТА СОТРУДНИКА:
//   {
//     id: уникальный идентификатор (можно генерировать через Date.now() + случайное число),
//     name: строка,
//     position: должность,
//     salary: число (зарплата),
//     subordinates: [] // массив подчинённых (объектов такой же структуры)
//   }
//
// -----------------------------------------------------------------------------
// ПРИМЕР ИСПОЛЬЗОВАНИЯ (после реализации):
//
//   const company = CompanyStructure();
//
//   company.addEmployee('Иван Петров', 'Генеральный директор', 500000);
//   company.addEmployee('Мария Соколова', 'Финансовый директор', 300000, 'Иван Петров');
//   company.addEmployee('Пётр Сидоров', 'Технический директор', 320000, 'Иван Петров');
//   company.addEmployee('Анна Иванова', 'Главный бухгалтер', 150000, 'Мария Соколова');
//   company.addEmployee('Сергей Смирнов', 'Ведущий разработчик', 180000, 'Пётр Сидоров');
//
//   company.printTree();
//   // Должно вывести нечто подобное:
//   //   [CEO] Иван Петров (500000)
//   //     [CFO] Мария Соколова (300000)
//   //       [Chief Accountant] Анна Иванова (150000)
//   //     [CTO] Пётр Сидоров (320000)
//   //       [Lead Dev] Сергей Смирнов (180000)
//
//   console.log(company.countEmployees()); // 5
//   console.log(company.totalSalary());    // 1450000
//
//   const found = company.findEmployee('Анна Иванова');
//   console.log(found); // объект с данными сотрудника и путём к нему
//
//   company.removeEmployee('Пётр Сидоров');
//   company.printTree(); // Пётр Сидоров и все его подчинённые удалены
//
// -----------------------------------------------------------------------------
// ГОТОВОЕ РЕШЕНИЕ
// -----------------------------------------------------------------------------

const CompanyStructure = (function() {
    // Приватное дерево сотрудников
    let employees = [];

    // Генерация уникального идентификатора
    function generateId() {
        return `${Date.now()}-${Math.random().toString(36).substr(2, 6)}`;
    }

    // ---------- ВСПОМОГАТЕЛЬНЫЕ РЕКУРСИВНЫЕ ФУНКЦИИ (приватные) ----------
    
    // Поиск сотрудника по имени (возвращает объект с узлом, родительским массивом и индексом)
    function findEmployeeByName(name, node = null, parentArray = null, index = -1) {
        if (!node) {
            // Поиск по корневому массиву
            for (let i = 0; i < employees.length; i++) {
                const result = findEmployeeByName(name, employees[i], employees, i);
                if (result) return result;
            }
            return null;
        } else {
            if (node.name === name) {
                return {
                    employee: node,
                    parentArray: parentArray,
                    index: index
                };
            }
            if (node.subordinates && node.subordinates.length) {
                for (let i = 0; i < node.subordinates.length; i++) {
                    const result = findEmployeeByName(name, node.subordinates[i], node.subordinates, i);
                    if (result) return result;
                }
            }
            return null;
        }
    }

    // Рекурсивное удаление узла из родительского массива
    function removeNode(parentArray, index) {
        parentArray.splice(index, 1);
    }

    // Рекурсивный подсчёт количества сотрудников
    function countEmployeesRecursive(nodeArray) {
        let count = nodeArray.length;
        for (let node of nodeArray) {
            if (node.subordinates && node.subordinates.length) {
                count += countEmployeesRecursive(node.subordinates);
            }
        }
        return count;
    }

    // Рекурсивный подсчёт суммарной зарплаты
    function totalSalaryRecursive(nodeArray) {
        let sum = 0;
        for (let node of nodeArray) {
            sum += node.salary;
            if (node.subordinates && node.subordinates.length) {
                sum += totalSalaryRecursive(node.subordinates);
            }
        }
        return sum;
    }

    // Рекурсивный вывод дерева с отступами
    function printTreeRecursive(nodeArray, level = 0) {
        const indent = '  '.repeat(level);
        nodeArray.forEach(node => {
            console.log(`${indent}[${node.position}] ${node.name} (${node.salary})`);
            if (node.subordinates && node.subordinates.length) {
                printTreeRecursive(node.subordinates, level + 1);
            }
        });
    }

    // Глубокое копирование объекта сотрудника (для безопасного возврата)
    function deepCopyEmployee(employee) {
        return JSON.parse(JSON.stringify(employee));
    }

    // ---------- ПУБЛИЧНОЕ API (ЗАМЫКАНИЯ) ----------
    return {
        // Добавить сотрудника
        addEmployee: function(name, position, salary, managerName = null) {
            const newEmployee = {
                id: generateId(),
                name: name,
                position: position,
                salary: salary,
                subordinates: []
            };

            if (managerName === null) {
                // Добавляем в корень
                employees.push(newEmployee);
            } else {
                const managerNode = findEmployeeByName(managerName);
                if (managerNode) {
                    managerNode.employee.subordinates.push(newEmployee);
                } else {
                    throw new Error(`Руководитель с именем "${managerName}" не найден`);
                }
            }
            return newEmployee.id;
        },

        // Удалить сотрудника по имени (вместе со всеми подчинёнными)
        removeEmployee: function(name) {
            const result = findEmployeeByName(name);
            if (result) {
                removeNode(result.parentArray, result.index);
                return true;
            }
            return false;
        },

        // Найти сотрудника по имени (возвращает копию и путь)
        findEmployee: function(name) {
            const result = findEmployeeByName(name);
            if (!result) return null;
            
            // Создаём копию, чтобы не раскрывать внутренние объекты
            const employeeCopy = deepCopyEmployee(result.employee);
            
            // Определяем путь (последовательность имён от корня до сотрудника)
            const path = [];
            let current = result.employee;
            while (current) {
                path.unshift(current.name);
                // Здесь нам нужно найти родителя, но для пути достаточно имён,
                // мы можем собрать их, поднимаясь по дереву. 
                // Упростим: возвращаем только массив имён (можно доработать при необходимости).
                // В текущей структуре мы не храним ссылку на родителя, поэтому для построения полного пути
                // потребуется дополнительный поиск. Оставим как демонстрацию идеи.
            }
            // Для простоты вернём только скопированного сотрудника и его менеджера (если есть).
            // Но в рамках задачи можно просто вернуть найденного сотрудника.
            return employeeCopy;
        },

        // Общее количество сотрудников
        countEmployees: function() {
            return countEmployeesRecursive(employees);
        },

        // Суммарная зарплата
        totalSalary: function() {
            return totalSalaryRecursive(employees);
        },

        // Вывести иерархию в консоль
        printTree: function() {
            console.log('=== Организационная структура компании ===');
            if (employees.length === 0) {
                console.log('(нет сотрудников)');
            } else {
                printTreeRecursive(employees);
            }
            console.log('==========================================');
        },

        // Загрузить начальную структуру (например, демо-данные)
        loadDemoData: function() {
            // Очищаем текущую структуру
            employees = [];
            
            // Добавляем генерального директора
            const ceoId = this.addEmployee('Алексей Смирнов', 'Генеральный директор', 600000);
            
            // Топ-менеджеры
            this.addEmployee('Елена Воробьёва', 'Финансовый директор', 350000, 'Алексей Смирнов');
            this.addEmployee('Дмитрий Козлов', 'Технический директор', 380000, 'Алексей Смирнов');
            this.addEmployee('Ольга Новикова', 'Директор по персоналу', 280000, 'Алексей Смирнов');
            
            // Подчинённые технического директора
            this.addEmployee('Илья Морозов', 'Руководитель разработки', 200000, 'Дмитрий Козлов');
            this.addEmployee('Татьяна Лебедева', 'Руководитель тестирования', 180000, 'Дмитрий Козлов');
            
            // Подчинённые руководителя разработки
            this.addEmployee('Максим Егоров', 'Senior Developer', 150000, 'Илья Морозов');
            this.addEmployee('Анастасия Сокол', 'Junior Developer', 80000, 'Илья Морозов');
        }
    };
})();

// -----------------------------------------------------------------------------
// ДЕМОНСТРАЦИЯ РАБОТЫ
// -----------------------------------------------------------------------------

// 1. Загружаем демо-данные (используем публичный метод, который внутри использует IIFE-контекст)
CompanyStructure.loadDemoData();

// 2. Выводим структуру
CompanyStructure.printTree();

// 3. Подсчёты
console.log(`Общее количество сотрудников: ${CompanyStructure.countEmployees()}`);
console.log(`Суммарная зарплата: ${CompanyStructure.totalSalary()} руб.`);

// 4. Поиск сотрудника
const found = CompanyStructure.findEmployee('Илья Морозов');
console.log('Найден сотрудник:', found);

// 5. Удаление руководителя разработки со всей командой
console.log('\nУдаляем "Илья Морозов" и его подчинённых...');
CompanyStructure.removeEmployee('Илья Морозов');

// 6. Проверяем результат
CompanyStructure.printTree();
console.log(`Сотрудников после удаления: ${CompanyStructure.countEmployees()}`);
console.log(`Суммарная зарплата после удаления: ${CompanyStructure.totalSalary()} руб.`);

// -----------------------------------------------------------------------------
// ПОЯСНЕНИЯ ПО ИСПОЛЬЗОВАНИЮ ТРЁХ КЛЮЧЕВЫХ КОНЦЕПЦИЙ
// -----------------------------------------------------------------------------
// 1. САМОВЫЗЫВАЮЩАЯСЯ ФУНКЦИЯ (IIFE):
//      const CompanyStructure = (function() { ... })();
//    - Создаёт изолированную область видимости.
//    - Приватные переменные (employees, вспомогательные функции) недоступны извне.
//    - Может выполнить немедленную инициализацию (в данном примере — пустой старт,
//      демо-данные загружаются отдельным вызовом, но их можно было бы добавить прямо в IIFE).
//
// 2. РЕКУРСИЯ:
//      - findEmployeeByName, removeNode, countEmployeesRecursive, totalSalaryRecursive,
//        printTreeRecursive — все обходят дерево, вызывая сами себя для подмассивов subordinates.
//      - Базовый случай: отсутствие подчинённых или достижение искомого узла.
//
// 3. ЗАМЫКАНИЯ:
//      - Все методы возвращаемого объекта используют переменную employees,
//        определённую во внешней функции. Даже после завершения IIFE эта переменная
//        остаётся доступной благодаря замыканию.
//      - Внутренние функции (findEmployeeByName и др.) также замкнуты на employees.
//
// ДОПОЛНИТЕЛЬНЫЕ ТРЕБОВАНИЯ (без других технологий):
//      - Не используются браузерные API, Node.js-модули, таймеры, события.
//      - Только нативные возможности JavaScript: объекты, массивы, функции.
// -----------------------------------------------------------------------------














Итоги:
Сегодня наше занятие было посвящено теоретической подготовке. а именно мы:

Изучили что такое самовызывающиеся функции (IIFE) и как они позволяют выполнять код немедленно после определения.
Разобрали как работает рекурсия — техника, при которой функция вызывает саму себя.
Рассмотрели принцип действия замыкания — механизма, при котором функция сохраняет доступ к переменным из внешнего окружения.
На следующем занятии мы будем использовать рекурсию для решения профессиональных задач.