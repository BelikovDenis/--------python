При работе с базами данных часто возникает необходимость анализировать их по группам, 
чтобы извлечь полезную информацию. 
Например, когда нам нужно посчитать общее количество продаж 
по каждому продукту или вычислить среднюю зарплату в разных отделах. 
Для таких задач в SQL используются операции группировки и агрегатные функции.


С помощью группировки и агрегирования можно преобразовывать большие 
массивы данных в компактные и информативные сводки.

Сегодня мы изучим, как работать с группировкой и агрегатными функциями в SQL. Мы научимся:

группировать данные;
применять агрегатные функции, такие как SUM(), AVG(), COUNT(), MAX(), MIN();
комбинировать группировку с условиями, используя HAVING.





Глоссарий к седьмому занятию
GROUP BY (Группировка)

GROUP BY — оператор SQL, который позволяет объединять строки таблицы 
в группы по определённому признаку, например по столбцу с категорией товара. 
После группировки можно применять агрегатные функции к каждой группе.

Aggregate functions (Агрегатные функции)

Aggregate functions — функции, 
которые выполняют вычисления над группами данных 
и возвращают одно значение для каждой из них. 
Примеры агрегатных функций:

SUM() — вычисляет сумму значений в группе,
AVG() — вычисляет среднее значение для группы,
COUNT() — подсчитывает количество строк в группе,
MAX() — находит максимальное значение в группе,
MIN() — находит минимальное значение в группе.
HAVING (Условие группировки)

HAVING — условие, которое применяется после группировки 
для фильтрации агрегированных данных. 
В отличие от WHERE, которое фильтрует строки до группировки, 
HAVING фильтрует агрегированные результаты.



DISTINCT (Исключение дубликатов)

DISTINCT — оператор, 
который позволяет удалить повторяющиеся строки 
из результата запроса и оставить только уникальные значения.


Что такое группировка?

Группировка — это процесс объединения строк 
в таблице по определённому признаку, 
например по категории товара или отделу. 
После группировки можно применять агрегатные 
функции для вычисления различных показателей по каждой группе. 
Это помогает структурировать данные и получить сводную информацию.

Предположим, у нас есть таблица sales. 
Мы хотим посчитать, сколько товаров было реализовано каждым продавцом. 
Сгруппируем данные по имени продавца, 
а затем посчитаем количество продаж по каждому из них.



Что такое агрегатные функции?

Агрегатные функции — это функции, 
которые принимают несколько значений и
 возвращают одно агрегированное значение. 
 К ним относятся такие функции, 
 как SUM(), AVG(), COUNT(), MAX() и MIN(). 
 Они позволяют вычислять сумму, среднее значение, 
 количество строк и другие статистические показатели для каждой группы.

Например, в таблице employees мы можем использовать агрегатные 
функции для вычисления средней зарплаты 
в каждом отделе или поиска самого молодого сотрудника компании.

Группировка — это своего рода сортировка данных по категориям, 
предназначающаяся для того, чтобы понять, как эти данные распределены.




Агрегатные функции — это инструменты для извлечения важных 
статистических данных из каждой группы.

Агрегатные функции

Агрегатные функции в SQL позволяют обрабатывать множество строк 
и возвращать одно значение для каждой группы строк. 
В SQLite агрегатные функции важны для анализа данных 
и создания сводных отчётов. 
Давайте подробнее разберем каждую из них с примерами.

SUM() — вычисление суммы

Функция SUM() используется для вычисления суммы значений 
в выбранном столбце. Она особенно полезна для подсчёта общих сумм, 
например суммарного дохода или стоимости всех товаров.

Синтаксис:

SELECT SUM(column_name) FROM table_name;
Пример. Предположим, у нас есть таблица sales, 
где хранятся данные о продажах


Чтобы посчитать общую сумму продаж (все строки), используем:

SELECT SUM(sales_amount) FROM sales;
Результат:

SUM(sales_amount)
------------------
1100

Это означает, что общая сумма продаж составляет 1100.

AVG() — вычисление среднего значения

Функция AVG() используется для вычисления среднего значения столбца. 
Это может быть полезно для вычисления средней зарплаты, 
стоимости товаров или среднего балла студентов.

Синтаксис:

SELECT AVG(column_name) FROM table_name;
Пример. Продолжим работать с таблицей sales, но теперь посчитаем среднюю сумму продажи:

SELECT AVG(sales_amount) FROM sales;
Результат:

AVG(sales_amount)
------------------
275

Средняя сумма продажи составляет 275.

COUNT() — подсчет количества строк

Функция COUNT() возвращает количество строк, 
которые соответствуют условию. В отличие от других агрегатных функций, 
COUNT(*) подсчитывает количество строк в таблице, включая те, 
где значения могут быть NULL. COUNT(column_name) 
подсчитывает количество непустых значений в указанном столбце.

Синтаксис:

SELECT COUNT(column_name) FROM table_name;
Пример: мы хотим посчитать, сколько строк содержит таблица sales. Используем:

SELECT COUNT(*) FROM sales;

Результат:

COUNT(*)
---------
4
Таблица sales содержит 4 строки.

Чтобы посчитать количество продаж для каждого продукта:

SELECT product_id, COUNT(*) FROM sales GROUP BY product_id;
Результат:

product_id  COUNT(*)
-----------  --------
1           2
2           1
3           1
Это покажет, сколько раз каждый продукт был продан.

MIN() и MAX() — нахождение минимального и максимального значений

Функции MIN() и MAX() позволяют находить минимальное 
и максимальное значение в столбце. 
Это полезно, например, для поиска самой низкой и высокой цены товара, 
самого маленького и самого большого балла или самого младшего и самого старшего сотрудника.

Синтаксис:

SELECT MIN(column_name), MAX(column_name) FROM table_name;
Пример. Необходимо узнать минимальную и максимальную сумму продаж из таблицы sales:

SELECT MIN(sales_amount), MAX(sales_amount) FROM sales;
Результат:

MIN(sales_amount) | MAX(sales_amount)
--------------------------------------
150               | 450

Минимальная сумма продажи составляет 150, а максимальная — 450.

Группировка данных с помощью GROUP BY

Часто агрегатные функции используются с оператором GROUP BY, 
который позволяет разбивать данные на группы по определённому признаку.

Представьте, что у вас есть корзина с фруктами и вы хотите узнать, 
сколько среди них у вас яблок, груш и бананов. 
Вместо того чтобы пересчитывать все фрукты вручную, 
вы можете разложить их по группам и посчитать количество фруктов в каждой.

SQL работает так же: GROUP BY группирует строки 
с одинаковыми значениями в выбранном столбце, 
а агрегатные функции (например, SUM(), AVG(), 
COUNT()) применяются к каждой группе отдельно.

Так, если у нас есть таблица sales, мы можем узнать сумму продаж по каждому продукту:

SELECT product_name, SUM(sales_amount)
FROM sales
GROUP BY product_name;

Основной синтаксис оператора GROUP BY выглядит так:

SELECT column1, AGGREGATE_FUNCTION(column2)
FROM table_name
GROUP BY column1;

column1 — столбец или столбцы, по которым происходит группировка.
AGGREGATE_FUNCTION(column2) — агрегатная функция (например, SUM(), COUNT(), AVG()), 
которая применяется к столбцу для каждой группы.

Допустим, у нас есть таблица sales с данными о продажах:


Нам нужно посчитать общую сумму продаж по каждому 
продукту или найти среднюю зарплату по отделам.

Чтобы узнать общую сумму продаж для каждого продукта, используем запрос с GROUP BY:

SELECT product_id, SUM(sales_amount)
FROM sales
GROUP BY product_id;
Результат:

product_id | SUM(sales_amount)
-----------|------------------
1          | 500
2          | 350
3          | 450

В этом примере:

строки сгруппированы по product_id;
для каждой группы посчитана сумма продаж с помощью агрегатной функции SUM().
Если мы хотим сгруппировать данные по нескольким столбцам, 
можно указать несколько колонок в GROUP BY. 
Например, у нас есть таблица с заказами, где указаны и продукт, и регион. 
Нам нужно посчитать общую сумму продаж по отдельному продукту в каждом регионе:

SELECT product_id, region, SUM(sales_amount)
FROM sales
GROUP BY product_id, region;
Результат:

product_id | region | SUM(sales_amount)
-----------|--------|-------------------
1          | North  | 500
2          | South  | 150
2          | North  | 200
3          | East   | 450

Здесь данные сгруппированы по двум столбцам: product_id и region. 
Сумма продаж считается для каждой уникальной комбинации этих значений.

Использование HAVING с GROUP BY

HAVING используется для фильтрации результатов после выполнения группировки. 
В отличие от WHERE, которое применяется до группировки, 
HAVING фильтрует агрегированные данные.

Например, мы хотим найти только продукты, общая сумма продаж которых больше 300:

SELECT product_id, SUM(sales_amount)
FROM sales
GROUP BY product_id
HAVING SUM(sales_amount) > 300;

Результат:

product_id | SUM(sales_amount)
-----------|------------------
1          | 500
2          | 350
3          | 450

В этом примере:

данные сгруппированы по product_id;
применена функция SUM() для подсчета суммы продаж;
с помощью HAVING отфильтрованы только те группы, где сумма продаж больше 300.




Разница между порядком записи и порядком выполнения SQL-запроса

При использовании SQL-запросов важно помнить, 
что порядок записи команд не совпадает с порядком их выполнения.

Запись SQL-запроса (синтаксический порядок):

SELECT column_name
FROM table_name
WHERE condition
GROUP BY column_name
HAVING condition
ORDER BY column_name;


Фактический порядок выполнения при этом будет следующим:

FROM – выбираем таблицу.
WHERE – фильтруем строки.
GROUP BY – группируем данные.
HAVING – фильтруем группы.
SELECT – выбираем нужные столбцы.
ORDER BY – сортируем результат.

В процессе выполнения запросов данные сначала отбираются, 
затем обрабатываются, и только потом формируется итоговый набор.


DISTINCT

Оператор DISTINCT используется для исключения повторяющихся значений 
в результате запроса. Он применяется в тех случаях, 
когда нужно получить только уникальные значения по одному или нескольким столбцам.

Синтаксис:

SELECT DISTINCT column1 FROM table;
Рассмотрим пример использования DISTINCT при агрегировании.

Посчитаем количество уникальных отделов в таблице employees:

SELECT COUNT(DISTINCT department) FROM employees;
Этот запрос вернет число различных отделов, исключая повторяющиеся названия.










Типичные ошибки
Тип ошибки 1: Несоответствие столбцов в GROUP BY и в SELECT

Одной из самых частых ошибок является использование в запросе столбцов, 
которые не входят в агрегатные функции и не указаны в GROUP BY. 
SQL требует, чтобы все столбцы, 
которые не обрабатываются агрегатной функцией, присутствовали в GROUP BY.

Пример ошибки:

SELECT product_id, sales_amount, SUM(sales_amount)
FROM sales
GROUP BY product_id;
В этом запросе столбец sales_amount не используется 
в агрегатной функции и не указан в GROUP BY. 
Это вызовет ошибку, потому что SQL не знает, 
как агрегировать sales_amount для каждой группы.

Решение:

Чтобы исправить ошибку, 
столбец следует либо добавить в агрегатную функцию, 
либо включить в GROUP BY:

SELECT product_id, SUM(sales_amount)
FROM sales
GROUP BY product_id;

Теперь SQL правильно агрегирует данные по продуктам.









Тип ошибки 2: Использование агрегатных функций в WHERE вместо HAVING

Оператор WHERE фильтрует строки до группировки, 
а агрегатные функции работают после группировки, 
поэтому использование агрегатных функций в WHERE вызовет ошибку.

Пример ошибки:

SELECT product_id, SUM(sales_amount)
FROM sales
WHERE SUM(sales_amount) > 500
GROUP BY product_id;


Этот запрос вызовет ошибку, потому что агрегатная функция SUM() 
не может быть использована в WHERE. В этом случае нужно применять HAVING.

Решение:

Используйте HAVING для фильтрации агрегированных данных:

SELECT product_id, SUM(sales_amount)
FROM sales
GROUP BY product_id
HAVING SUM(sales_amount) > 500;

Теперь мы фильтруем агрегированные данные по сумме продаж.







Практикум
Задача 1: Анализ продаж

Ситуация:
Вы владелец интернет-магазина, продающего электронику и периферию. 
Ваша база данных SQLite содержит информацию о продажах товаров из разных категорий. 
Вам нужно проанализировать продажи и определить, 
для каких категорий общая сумма продаж превышает 4000.

Задача:
Создайте таблицу sales, в которой хранится информация о продажах, 
и выполните анализ, чтобы найти категории товаров, 
общая сумма продаж которых превышает 4000. 
Используйте оператор GROUP BY, агрегатные функции и HAVING.

Шаги реализации:

Создайте таблицу sales и заполните ее данными:
sale_id (INTEGER) — уникальный идентификатор продажи,
product_name (TEXT) — название товара,
category (TEXT) — категория товара,
sales_amount (REAL) — сумма продажи,
sale_date (TEXT) — дата продажи в формате YYYY-MM-DD.

CREATE TABLE sales (
    sale_id INTEGER PRIMARY KEY AUTOINCREMENT,
    product_name TEXT NOT NULL,
    category TEXT NOT NULL,
    sales_amount REAL NOT NULL,
    sale_date TEXT NOT NULL
);

INSERT INTO sales (product_name, category, sales_amount, sale_date) VALUES
('Ноутбук', 'Электроника', 1500, '2024-02-01'),
('Смартфон', 'Электроника', 1000, '2024-02-02'),
('Монитор', 'Электроника', 800, '2024-02-03'),
('Клавиатура', 'Периферия', 600, '2024-02-04'),
('Мышь', 'Периферия', 200, '2024-02-05'),
('Ноутбук', 'Электроника', 1600, '2024-02-06'),
('Смартфон', 'Электроника', 1500, '2024-02-07'),
('Клавиатура', 'Периферия', 700, '2024-02-08'),
('Мышь', 'Периферия', 250, '2024-02-09'),
('Ноутбук', 'Электроника', 1550, '2024-02-10'),
('Смартфон', 'Электроника', 1200, '2024-02-11');

Выполните запрос с GROUP BY и HAVING, чтобы найти категории товаров, 
общая сумма продаж которых превышает 4000:
SELECT category, SUM(sales_amount) AS total_sales
FROM sales
GROUP BY category
HAVING SUM(sales_amount) > 4000;










Задача 2: Анализ уникальных продаж по товарам

Ситуация:
Вы хотите узнать, 
сколько уникальных продаж было совершено 
в каждой категории товаров в вашем магазине. 
Это поможет определить популярность товарных категорий 
без учёта повторяющихся продаж отдельных товаров.

Задача:
Используйте таблицу sales, 
чтобы посчитать количество уникальных продаж для каждой категории товаров. 
Для этого воспользуйтесь оператором DISTINCT в сочетании с GROUP BY.

Шаги реализации:

Используйте уже существующую таблицу sales, созданную в предыдущей задаче.

Выполните запрос с использованием DISTINCT, 
чтобы посчитать количество уникальных продаж в каждой категории товаров:

SELECT category, COUNT(DISTINCT sale_date) AS unique_sales_count
FROM sales
GROUP BY category;






Итоги:
Сегодня мы освоили новые навыки для работы с базами данных в SQL:

Агрегатные функции:

COUNT — подсчитывает количество записей,
SUM — вычисляет сумму значений,
AVG — рассчитывает среднее значение,
MIN — находит минимальное значение,
MAX — находит максимальное значение.
Оператор GROUP BY — позволяет группировать данные по определённому столбцу 
и выполнять вычисления с агрегатными функциями на каждой группе.

Оператор HAVING — фильтрует агрегированные данные после выполнения группировки.

Оператор DISTINCT — используется для выбора уникальных значений в столбце.

изучили агрегатные функции COUNT, SUM, AVG, MIN, MAX, 
которые помогают выполнять вычисления с данными в SQL;
разобрались, как использовать GROUP BY для 
группировки строк по значениям в столбце;
узнали, как оператор HAVING позволяет фильтровать агрегированные данные после группировки;
познакомились с оператором DISTINCT, 
который помогает выбирать только уникальные значения.