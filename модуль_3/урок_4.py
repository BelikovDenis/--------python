Мы научились решать прикладные задачи с помощью Python и освоили работу с Git 
для управления сложными разработками. 
Сегодня мы начнём знакомство с базами данных 
– важным инструментом для работы над серьёзными проектами.

База данных (БД) – это структура для хранения, управления и обработки информации. 
С помощью неё мы можем структурировать данные, 
быстро находить нужную информацию и обеспечивать доступ к ней. 
Таким образом, БД является эффективным инструментом для работы 
с большим количеством данных (например, из миллионов строк).

Примеры использования баз данных в Python-приложениях:

Разработка веб-приложений — хранение данных пользователей, товаров, заказов с помощью БД.
Анализ данных и Data Science — работа с большими объёмами информации, подготовка данных к обработке.
Автоматизация бизнес-процессов — обработка отчётов, хранение и управление данными.
Разработка чат-ботов и ИИ — хранение диалогов, истории взаимодействий с пользователями.
Финансовые приложения — обработка финансовых операций, расчёты.



Сегодня мы:

познакомимся с основами языка запросов к базам данных SQL;
освоим работу с инструментом DB Browser for SQLite;
научимся выполнять SELECT-запросы для выборки данных;
изучим основные операции для работы с БД: фильтрацию и сортировку данных.




Глоссарий к четвертому занятию
SQL (Structured Query Language)

SQL — это стандартный язык запросов для работы с реляционными базами данных. 
С помощью SQL можно извлекать информацию, хранящуюся в базе данных, изменять её и управлять ей.

SELECT-запрос

SELECT — это команда в SQL, используемая для выборки информации из таблиц базы данных. 
Запрос позволяет указать, какие именно данные нужно получить. 
При этом имеется возможность использовать фильтрацию, сортировку и агрегацию.

INSERT-запрос

INSERT — это команда SQL, которая используется для добавления новых записей в таблицу базы данных.

WHERE

WHERE — это условие в SQL, которое используется для фильтрации данных, 
с тем чтобы выборка содержала только те записи, которые удовлетворяют заданному условию.

ORDER BY

ORDER BY — это команда SQL, используемая для сортировки данных, 
полученных с помощью SELECT-запроса, по определённым столбцам в порядке возрастания или убывания.

LIMIT

LIMIT — это команда SQL, используемая для ограничения количества строк, 
возвращаемых в результате SELECT-запроса.



Каждому из нас знакома работа с таблицами Excel, 
где данные представлены в виде строк и столбцов, 
а внутри одной книги можно хранить множество таблиц.

Excel – удобный инструмент для управления данными, 
однако он не подходит для работы с большими объемами информации. 
Такую таблицу сложно интегрировать в Python-приложение, 
так как её тяжело обрабатывать. 
При использовании Excel-таблиц часто возникают риски для безопасности наших данных, 
поэтому в разработке мы предпочитаем использовать реляционные и нереляционные базы данных.

Базы данных позволяют:

хранить большие объёмы информации,
быстро находить данные по сложным условиям,
обеспечивать частые обновления и защиту данных.


Базы данных делятся на два основных типа:

Реляционные базы данных (SQL). 
Хранят информацию в виде таблиц с определенной структурой (строки и столбцы). 
Они используют язык SQL. К таким типам БД относятся SQLite, PostgreSQL, MySQL.



Нереляционные базы данных (NoSQL). 
Могут хранить информацию в виде документов, 
графов или пар «ключ — значение» (без строгих структур). 
Такие базы подходят для работы с неструктурированными данными: 
например, они распространены в разработке чат-ботов, 
которые отвечают на вопросы по документам (MongoDB, Firebase, Cassandra).



На будущих занятиях нам предстоит работа с реляционными базами данных. 
В Python они используются для разработки веб-приложений, анализа данных, 
создания чат-ботов и систем искусственного интеллекта, 
а также для финансовых продуктов.


Реляционные базы данных состоят из таблиц. 
Каждая таблица состоит из строк и столбцов. 
Строки представляют собой отдельные записи, а столбцы — атрибуты (поля) этих записей.

Чтобы производить действия с таблицами, 
необходимо научиться составлять запросы к ним. 
Для создания запросов используется специальный язык — SQL.

Что такое SQL?

SQL (Structured Query Language) — это язык запросов, 
который позволяет извлекать нужные строки, 
обновлять, удалять и добавлять информацию в базах данных.

Структура SQL проста для изучения. Синтаксис SQL определяет правила, 
по которым мы составляем запросы для извлечения, фильтрации, сортировки и изменения данных.

Мы используем SQL для работы с системами управления базами данных (СУБД). 
На наших занятиях мы будем использовать СУБД SQLite. 
Преимущества SQLite заключаются в том, 
что эта СУБД не требует отдельного сервера для работы, а еще она совместима с Python.



DB Browser for SQLite

Подобно тому, как для работы с кодом мы используем удобные средства (например, VS Code), 
для работы с базами данных SQL мы тоже применяем разные инструменты. 
DB Browser for SQLite — это бесплатный и открытый инструмент для работы с SQLite-базами данных. 
Он имеет интуитивно понятный графический интерфейс для создания, 
просмотра и редактирования баз данных с помощью SQLite.

С помощью DB Browser можно выполнять SQL-запросы и просматривать результаты, 
а также импортировать и экспортировать данные в различные форматы.

Скачайте программу DB Browser с официального сайта и установите её, 
следуя инструкциям (для вашей операционной системы).



База данных Titanic

На этом занятии мы будем использовать один из самых популярных наборов данных — базу данных Titanic. 
Это набор данных о пассажирах на борту «Титаника». 
Он состоит из столбцов, в которых указаны данные о выживших, 
а также данные о возрасте, поле, классах кают пассажиров и другие характеристики.

Вы можете скачать набор данных Titanic: Machine Learning from Disaster с платформы Kaggle. 
Вот ссылка на страницу с набором данных:

Kaggle Titanic Dataset.
Там вы найдете файл в формате CSV, 
который легко импортировать в вашу базу данных для дальнейшего использования.

Как добавить данные в DB Browser for SQLite:

Скачайте файл train.csv с Kaggle.
Откройте DB Browser for SQLite.
Создайте новую базу данных, выбрав New Database и задав имя файла (например, titanic.db).
Перейдите на вкладку File > Import > Table from CSV file.

Выберите файл train.csv и следуйте инструкциям, чтобы импортировать данные в таблицу.
Назовите таблицу. Например, titanic.


Таблица, которую мы будем использовать, содержит следующие столбцы:

PassengerId — уникальный идентификатор пассажира.
Pclass — класс каюты (1, 2, 3).
Name — имя пассажира.
Sex — пол пассажира.
Age — возраст пассажира.
SibSp — количество братьев/сестёр или супругов на борту.
Parch — количество родителей или детей на борту.
Ticket — номер билета.
Fare — стоимость билета.
Cabin — номер каюты.
Embarked — порт посадки (C — Cherbourg, Q — Queenstown, S — Southampton).
Survived — выживание (0 — не выжил, 1 — выжил).
Интерфейс DB Browser с таблицей titanic:


Рисунок 1
SELECT-запросы

SELECT-запрос в SQL используется для извлечения данных из таблицы. 
Это базовый запрос, без которого не обходится ни одна работа с базами данных.

SELECT позволяет извлечь из большого количества информации только необходимую нам, 
а затем организовать её и представить в удобном виде. 
С помощью SELECT-запросов мы получаем из объёмной таблицы небольшой срез по заданным условиям.

Используя SELECT, можно выбрать из таблицы отдельные столбцы 
и строки или вывести все данные сразу. Также вы можете применять фильтры, 
сортировку и группировку к своим запросам.


Рассмотрим основные компоненты SELECT-запроса:

SELECT— определяет, какие столбцы нужно извлечь.
FROM— указывает источник данных (какую таблицу из базы мы используем).
WHERE — фильтрует строки по условиям (например, отображение списка пассажиров «Титаника» старше 18 лет).
ORDER BY — сортирует результат (например, по убыванию или по возрастанию).
LIMIT — ограничивает количество строк (например, позволяет показать только первые 5 строк таблицы).

Основной синтаксис:

SELECT column1, column2, ...
FROM table_name;
SELECT — указывает, какие столбцы вы хотите выбрать.
FROM — указывает, из какой таблицы будут извлекаться данные.

Если вы хотите выбрать все столбцы, используйте *:

SELECT * FROM table_name;

Выполнение простых SELECT-запросов

Выборка всех данных из таблицы. Чтобы увидеть все записи в таблице, используем запрос:
SELECT * FROM titanic;

Он извлечёт все столбцы для всех пассажиров. 
Такие запросы помогают нам узнать, какие данные находятся в таблице.

Пример вывода результатов в DB Browser:


Рисунок 2
2. Выборка только некоторых столбцов. 
Если вам нужно выбрать только отдельные столбцы (например, имя и пол пассажира), используйте:

SELECT Name, Sex FROM titanic;
Этот запрос вернёт только столбцы Name и Sex для каждого пассажира.

3. Фильтрация данных с помощью WHERE. 
Если вы хотите отфильтровать данные (например, выбрать только мужчин), 
используйте условие WHERE:

SELECT Name, Sex FROM titanic WHERE Sex = 'male';
Здесь мы выбираем только тех пассажиров, у которых в столбце Sex указано значение 'male'.

4. Фильтрация по числовым значениям. 
Вы можете фильтровать данные по числовым значениям. 
К примеру, выбрать всех пассажиров, возраст которых больше 30 лет:

SELECT Name, Age FROM titanic WHERE Age > 30;
Сортировка данных с помощью ORDER BY

ORDER BY используется для сортировки данных по одному или нескольким столбцам. 
По умолчанию сортировка выполняется по возрастанию (ASC), 
но можно указать и сортировку по убыванию (DESC).

Сортировка по одному столбцу. 
Для сортировки по возрасту пассажиров в порядке возрастания используем:

SELECT Name, Age FROM titanic ORDER BY Age;
Сортировка по нескольким столбцам. 
Вы можете сортировать данные по нескольким столбцам. 
Например, сначала по возрасту, а затем по имени:

SELECT Name, Age FROM titanic ORDER BY Age, Name;
Сортировка в порядке убывания. Для сортировки по возрасту в порядке убывания используйте DESC:

SELECT Name, Age FROM titanic ORDER BY Age DESC;

Применение нескольких условий с WHERE

Можно использовать несколько условий в фильтре с помощью AND и OR. 
Например, выбрать всех выживших пассажиров старше 40 лет:

SELECT Name, Age, Survived FROM titanic WHERE Survived = 1 AND Age > 40;
Или выбрать всех пассажиров, которые либо выжили, либо были моложе 10 лет:

SELECT Name, Age, Survived FROM titanic WHERE Survived = 1 OR Age < 10;
Оператор NOT используется для исключения какого-либо условия в SQL. 
Например, если мы хотим выбрать всех пассажиров, 
которые не находились в первом классе, мы используем конструкцию:

SELECT Name, Pclass FROM titanic WHERE NOT Pclass = 1;

Этот запрос вернёт пассажиров только второго и третьего классов.

Мы разобрали основные SQL-команды:

SELECT — для выборки данных,
WHERE — для фильтрации,
ORDER BY — для сортировки,
LIMIT — для ограничения количества строк.
Перейдем к практике: рассмотрим, 
как комбинировать условия фильтрации и сортировки данных на настоящих дата-сетах.






Типичные ошибки
Тип ошибки 1: Ошибка в синтаксисе SELECT-запроса

Одной из самых распространённых ошибок является неправильный синтаксис запроса. 
Например, когда вы забываете указать имя таблицы после FROM или неправильно пишете имя столбца.

Пример ошибки:

Если забыть указать имя таблицы, запрос выдаст ошибку:

SELECT Name, Age FROM;
Ошибка:

syntax error near 'FROM'
Решение:

Убедитесь, что в запросе указано имя таблицы, из которой извлекаются данные:

SELECT Name, Age FROM passengers;







Тип ошибки 2: Неправильное использование WHERE с текстовыми значениями

При фильтрации данных с текстовыми значениями важно соблюдать правильный синтаксис 
и заключать строковые данные в одиночные кавычки: если забыть их поставить, возникнут ошибки.

Пример ошибки:

Попытка выполнить запрос без кавычек для текстового значения вызовет ошибку:

SELECT Name FROM passengers WHERE Sex = male;
Ошибка:

unknown column 'male' in 'where clause'

Решение:

Заключите текстовое значение в одиночные кавычки:

SELECT Name FROM passengers WHERE Sex = 'male';









Тип ошибки 3: Ошибка при написании имени столбца

При выполнении SQL-запросов важно правильно указывать имена столбцов. 
Ошибки в написании названий приводят к сбоям при выполнении запроса.

Пример ошибки:

Если столбец указан с ошибкой или его не существует в таблице, SQL выдаст ошибку:

SELECT Name, Agee FROM passengers;
Ошибка:

no such column: Agee
Решение:

Убедитесь, что имя столбца написано правильно. 
Например, если правильное имя столбца Age, исправленный запрос будет выглядеть так:

SELECT Name, Age FROM passengers;






Практикум
Задача 1: Выполняем SELECT-запросы по базе данных фильмов

Ситуация:
Вы работаете с реальной базой данных фильмов, которая получена из проекта на Kaggle. 
База содержит такую информацию, как название, жанр, размер кассовых сборов, 
длительность фильма, рейтинг и другие параметры.

В этой задаче вам предстоит выполнить SELECT-запрос 
для извлечения всех записей о фильмах из таблицы.

Задача:

Используя таблицу с данными о фильмах, 
выполните запрос для вывода всех данных и ознакомьтесь с результатами.

Шаги реализации:

Загрузка базы данных. Скачайте файл базы данных из проекта на Kaggle. 
Для этого перейдите по ссылке для скачивания.

В архиве будет SQLite-база movie.sqlite, которую можно открыть в DB Browser.

Импорт в СУБД. Запустите DB Browser for SQLite 
и откройте базу данных movie.sqlite через интерфейс программы.

Изучите структуру базы данных. 
Наша БД состоит из трёх таблиц, названия которых можно увидеть во вкладке 
Browse Data: IMDB, earning, genre.


3. Выполнение SELECT-запроса. 
После импорта данных выполните SELECT-запрос для вывода всех записей из таблицы IMDB:

SELECT * FROM IMDB;


4. Проверка результата. 
После выполнения запроса вы должны увидеть все записи из таблицы IMDB.






Реализация
1. Создайте виртуальное окружение в папке проекта:

bash
python -m venv venv


2.Активируйте виртуальное окружение:

На Windows:

bash
source venv/Scripts/activate
На macOS/Linux:

bash
source venv/bin/activate

pip install pandas

import sqlite3
import pandas as pd

def explore_movie_database(db_path='movie.sqlite'):
    """
    Исследование базы данных фильмов и выполнение SELECT-запросов
    """
    try:
        # Подключение к базе данных
        conn = sqlite3.connect(db_path)
        cursor = conn.cursor()
        
        print("=" * 60)
        print("ИССЛЕДОВАНИЕ БАЗЫ ДАННЫХ ФИЛЬМОВ")
        print("=" * 60)
        
        # Шаг 1: Просмотр структуры базы данных
        print("\n1. СТРУКТУРА БАЗЫ ДАННЫХ:")
        print("-" * 40)
        
        # Получение списка таблиц
        cursor.execute("SELECT name FROM sqlite_master WHERE type='table';")
        tables = cursor.fetchall()
        
        print("Таблицы в базе данных:")
        for table in tables:
            table_name = table[0]
            print(f"  - {table_name}")
            
            # Просмотр структуры каждой таблицы
            cursor.execute(f"PRAGMA table_info({table_name})")
            columns = cursor.fetchall()
            print(f"    Столбцы: {[col[1] for col in columns]}")
        
        # Шаг 2: Выполнение SELECT-запроса для таблицы IMDB
        print("\n2. ДАННЫЕ ИЗ ТАБЛИЦЫ IMDB:")
        print("-" * 40)
        
        # Получение всех данных из таблицы IMDB
        cursor.execute("SELECT * FROM IMDB")
        movies = cursor.fetchall()
        
        # Получение названий столбцов
        cursor.execute("PRAGMA table_info(IMDB)")
        columns = [col[1] for col in cursor.fetchall()]
        
        print(f"Найдено записей: {len(movies)}")
        print(f"Столбцы: {', '.join(columns)}")
        
        # Вывод первых 5 записей для ознакомления
        print("\nПервые 5 записей:")
        print("-" * 80)
        
        # Создаем DataFrame для красивого вывода
        df = pd.DataFrame(movies[:5], columns=columns)
        print(df.to_string(index=False))
        
        # Шаг 3: Основная статистика
        print("\n3. ОСНОВНАЯ СТАТИСТИКА:")
        print("-" * 40)
        
        # Количество фильмов
        cursor.execute("SELECT COUNT(*) FROM IMDB")
        total_movies = cursor.fetchone()[0]
        print(f"Общее количество фильмов: {total_movies}")
        
        # Годы выпуска
        cursor.execute("""
            SELECT MIN(Year), MAX(Year), AVG(Year) 
            FROM IMDB 
            WHERE Year IS NOT NULL
        """)
        min_year, max_year, avg_year = cursor.fetchone()
        print(f"Годы выпуска: {min_year} - {max_year}")
        
        # Рейтинги
        cursor.execute("""
            SELECT MIN(Rating), MAX(Rating), AVG(Rating) 
            FROM IMDB 
            WHERE Rating IS NOT NULL
        """)
        min_rating, max_rating, avg_rating = cursor.fetchone()
        print(f"Рейтинги: {min_rating:.1f} - {max_rating:.1f} (средний: {avg_rating:.1f})")
        
        # Шаг 4: Примеры дополнительных запросов
        print("\n4. ПРИМЕРЫ ДОПОЛНИТЕЛЬНЫХ ЗАПРОСОВ:")
        print("-" * 40)
        
        # Топ-5 фильмов по рейтингу
        print("Топ-5 фильмов по рейтингу:")
        cursor.execute("""
            SELECT Title, Year, Rating, Genre 
            FROM IMDB 
            WHERE Rating IS NOT NULL 
            ORDER BY Rating DESC 
            LIMIT 5
        """)
        top_movies = cursor.fetchall()
        for i, movie in enumerate(top_movies, 1):
            print(f"  {i}. {movie[0]} ({movie[1]}) - {movie[2]:.1f} - {movie[3]}")
        
        # Распределение по годам
        print("\nКоличество фильмов по десятилетиям:")
        cursor.execute("""
            SELECT (Year/10)*10 as Decade, COUNT(*) 
            FROM IMDB 
            WHERE Year IS NOT NULL 
            GROUP BY Decade 
            ORDER BY Decade
        """)
        decades = cursor.fetchall()
        for decade, count in decades:
            print(f"  {decade}-е: {count} фильмов")
        
    except sqlite3.Error as e:
        print(f"Ошибка базы данных: {e}")
    except FileNotFoundError:
        print("Файл базы данных не найден. Убедитесь, что файл 'movie.sqlite' находится в текущей директории.")
    except Exception as e:
        print(f"Общая ошибка: {e}")
    finally:
        if 'conn' in locals():
            conn.close()
            print("\nСоединение с базой данных закрыто.")

def specific_queries(db_path='movie.sqlite'):
    """
    Выполнение конкретных запросов для анализа данных
    """
    try:
        conn = sqlite3.connect(db_path)
        cursor = conn.cursor()
        
        print("\n" + "="*60)
        print("ДЕТАЛЬНЫЙ АНАЛИЗ ДАННЫХ")
        print("="*60)
        
        # Запрос 1: Фильмы с самым высоким рейтингом
        print("\n1. ФИЛЬМЫ С ВЫСОКИМ РЕЙТИНГОМ (>8.5):")
        cursor.execute("""
            SELECT Title, Year, Rating, Duration, Genre 
            FROM IMDB 
            WHERE Rating > 8.5 
            ORDER BY Rating DESC
        """)
        high_rated = cursor.fetchall()
        for movie in high_rated[:10]:  # Показать первые 10
            print(f"  - {movie[0]} ({movie[1]}): {movie[2]:.1f}, {movie[3]} мин, {movie[4]}")
        
        # Запрос 2: Самые длинные фильмы
        print("\n2. САМЫЕ ДЛИННЫЕ ФИЛЬМЫ:")
        cursor.execute("""
            SELECT Title, Year, Duration, Rating 
            FROM IMDB 
            WHERE Duration IS NOT NULL 
            ORDER BY Duration DESC 
            LIMIT 10
        """)
        longest = cursor.fetchall()
        for movie in longest:
            print(f"  - {movie[0]} ({movie[1]}): {movie[2]} мин, рейтинг: {movie[3]}")
            
    except sqlite3.Error as e:
        print(f"Ошибка базы данных: {e}")
    finally:
        if 'conn' in locals():
            conn.close()

# Основная функция
if __name__ == "__main__":
    # Исследование базы данных
    explore_movie_database()
    
    # Дополнительные запросы
    specific_queries()
    
    print("\n" + "="*60)
    print("АНАЛИЗ ЗАВЕРШЕН")
    print("="*60)







Итоги:
На этом занятии мы изучили основы работы с SQL-запросами:

научились использовать SELECT извлечения строк из баз данных;
освоили фильтрацию данных с помощью WHERE;
познакомились с командой для сортировки извлечённых результатов ORDER BY.
Все эти команды лежат в основе более сложных запросов и незаменимы для проведения анализа данных.