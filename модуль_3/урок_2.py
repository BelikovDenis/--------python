Мы изучили основные команды Git, 
научились создавать репозитории и отслеживать изменения в проекте: 
«путешествовать во времени» и смотреть предыдущие версии файлов.

Работа с системой контроля версий делает разработку более безопасной. 
Мы можем менять проект без страха потерять данные или сломать рабочую версию программы, 
потому что Git позволяет нам сделать откат к предыдущей версии.

Когда мы хотим внести изменения в проект, 
например разработать новую функцию (накатить новую фичу!), мы часто рискуем, 
так как изменения в коде могут вызвать ошибки в работе программы.

Для экспериментов с кодом мы обычно используем ветки. 
Они позволяют нам создавать параллельные варианты проекта, 
в которые можно вносить изменения, не затрагивая основную (стабильную) версию.

Сегодня мы научимся:

Научимся создавать ветки для экспериментов и разработки новых функций.
Освоим процесс слияния веток и интеграции изменений в основную версию проекта.
Разберемся, как разрешать конфликты при слиянии, 
если изменения в разных ветках противоречат друг другу.



Глоссарий ко второму занятию
Branch (Ветка)
Branch – это отдельная «линия» разработки внутри репозитория. 
При ее создании мы формируем новую версию нашего репозитория и уже в ней 
производим изменения, не затрагивая основную ветку (например, main).

Merge (Слияние)
Merge – это процесс переноса изменений одной ветки в другую. 
Используется для интеграции новой функции или внесения исправлений в основной проект.

Conflict (Конфликт)
Conflict – это ситуация, когда изменения, сделанные в разных ветках, 
не могут быть автоматически объединены. 
Например, если два разработчика внесли изменения в одну и ту же строку кода в одном файле, 
Git не сможет решить, какой из вариантов следует сохранить. 
В этом случае необходимо произвести разрешение конфликта.

Conflict Resolution (Разрешение конфликта)
Conflict Resolution – это процесс выбора: мы решаем, 
какие изменения оставить при возникновении конфликта. 
Разрешение конфликта производится вручную.

Представьте, что вы работаете в команде над проектом и получили задание разработать новую
 систему для авторизации пользователей вашего продукта.

Любое изменение, которое вы будете вносить, повлияет на стабильность программы, 
поэтому работать в основной версии проекта рискованно.

Чтобы не нарушать работу программы, в Git мы используем ветки.



Работа с ветками в Git

Ветка – это независимая линия разработки. 
Внутри веток можно производить любые изменения: 
это не влияет на основную версию проекта.

Ветки помогают, когда мы работаем в команде. 
Например, когда несколько разработчиков занимаются разными задачами. 
Также работа с ветками делает любые эксперименты безопасными, 
поскольку изменения не затрагивают основную (стабильную) версию вашего проекта.

Основная ветка проекта обычно называется main или master. 
Она создается по умолчанию, как только вы инициализируете репозиторий.

Создадим ветку с названием auth-module. Там мы будем проводить эксперименты:
git branch auth-module

Команда branch создает новую ветку, но не переключает вас на неё. 
Чтобы оказаться на новой ветке, мы используем команду switch:
git switch auth-module


Вместо двух команд мы можем использовать одну:
git checkout -b auth-module


В этом примере мы применили команду checkout с параметром -b. 
Она создает новую ветку и сразу переключает нас на неё.

Теперь мы находимся в ветке auth-module. 
Все изменения будут сохраняться именно здесь и не затронут main или master.


После того как мы внесли правки в нашу систему авторизации и зафиксировали изменения, 
возвращаемся в основную ветку. Для этого мы снова используем switch или checkout:

git switch main
git checkout main
Команда git switch появилась в новых версиях Git для быстрого переключения между ветками. 
Сейчас для переключения между ветками мы предпочитаем использовать git switch, 
а git checkout применяем для более сложных операций, 
например для создания новой ветки и перехода на неё.

Слияние веток

Как только вы закончили разработку системы авторизации и убедились в том, 
что новая версия программы работает без ошибок, можно произвести слияние веток.

Слияние производится на той ветке, в которую мы хотим слить изменения. 
В нашем случае это ветка main или master.

Слияние производится с помощью команды merge:

git merge auth-module
Мы слили изменения из ветки auth-module в текущую ветку main (master). 
Теперь две ветки объединены, мы обновили стабильную версию нашего проекта.

Ветка auth-module нам больше не нужна, её можно удалить:

git branch -d auth-module
Команда branch -d удалит ветку только в том случае, если она слита с другой веткой. 
Если ветка не была слита, Git предупредит вас об этом.

Если вы хотите удалить ветку, которая не была слита, используйте команду branch -D. 
Однако будьте осторожны, иначе вы рискуете потерять ценные данные.

Посмотреть список веток в нашем репозитории можно с помощью команды git branch без параметров.





Конфликты

В нашем примере с разработкой системы авторизации мы рассмотрели процесс слияния без конфликтов. 
Это значит, что изменения в ветках auth-module и main (master) не 
противоречили друг другу и что слияние произошло автоматически.

Ситуации, когда информация в ветках пересекается, например, 
когда в одних и тех же строках в одинаковых файлах содержится разный код, 
называются конфликтами.

Если при попытке произвести слияние произошел конфликт, Git предупредит вас, 
и вам нужно будет вручную разрешить конфликты в файлах.

Рассмотрим процесс разрешения конфликтов на примере работы с двумя ветками main (master) и test, 
в которых будут одинаковые изменения в одном и том же файле.

Создаем файл file.txt с текстом Hello, world! в ветке main или master:

echo "Hello, world!" > file.txt
git add file.txt
git commit -m "Add file.txt"

Создаем ветку test и переключаемся на неё:

git checkout -b test

Вносим изменения в файл file.txt в ветке test, например:

Hello, test branch!

Добавляем изменения и фиксируем их:

git add file.txt
git commit -m "Changes in test branch"
Переключаемся обратно на ветку main или master и вносим другие изменения в тот же файл, например:

Hello, main branch!
Добавляем изменения в основной ветке и фиксируем:

git add file.txt
git commit -m "Changes in main branch"

Теперь, когда в обеих ветках внесены изменения в одну и ту же строку файла, 
можно попробовать слить ветку test и ветку main или master.
git merge test

При попытке слияния возникнет конфликт, Git отобразит следующее сообщение:

Auto-merging file.txt
CONFLICT (add/add): Merge conflict in file.txt
Automatic merge failed; fix conflicts and then commit the result.

Git не может решить, какое из изменений оставить, и предлагает вручную разрешить конфликт. 
В терминале VS Code отобразится сообщение и файл с маркерами конфликта:

Рассмотрим процесс разрешения конфликта пошагово. 
В файле file.txt появились маркеры конфликта, которые указывают, 
какие строки в документе не дают нам слить ветки:

<<<<<<< HEAD
Hello, master branch!
=======
Hello, test branch!
>>>>>>> test
Теперь вам нужно решить, какие из изменений, отмеченных маркерами, следует оставить. 
Например, вы можете выбрать один вариант из двух, изменив file.txt:

Hello, main branch!
В VS Code внести изменения можно с помощью Merge Editor:

После разрешения конфликта нажимаем в VS Code Complete merge.

Теперь слияние завершено и конфликт разрешён.

Итак, мы создали конфликт путем изменения одной и той же строки в двух разных ветках. 
После того как Git уведомил нас о конфликте, мы вручную разрешили его, 
отредактировав файл с помощью Merge Editor в VS Code, и завершили слияние.

Выведем на экран историю изменений с помощью git log --graph.

*   commit 8f0d3c7bd191be0b4a9941ed66c363d87ff86fbc (HEAD -> master)
|\  Merge: 257765d 016128d
| | Author: <username> <username@email.com>
| | Date:   <Date>
| |
| |     Merge branch 'test'
| |
| |     Resolve conflict
| |
| * commit 016128d95da1f924113ce9a3cf4221a7b2410498 (test)
| | Author: <username> <username@email.com>
| | Date:   <Date>
| |
| |     Changes in test branch
| |
* | commit 257765d748f2279c67b0a1ed26c7e29926f4ad37
| | Author: <username> <username@email.com>
| | Date:   <Date>
| |
| |     Changes in master branch
| |
* | commit a5cf089e12056fb8a569fe3e197c90de5f548d31
|/  Author: <username> <username@email.com>
|   Date:   <Date>
|
|       Initial commit
|
* commit f946ebae332555c3e44899741fca6d9cc76e9bde


На графике отображаются наши коммиты на разных ветках, процесс разрешения конфликта и слияние.

Теперь мы можем удалить ветку test с помощью команды git branch -d test.


Вывод графа

Когда внесено много изменений и работа ведётся с несколькими ветками, 
команда log --graph — просто спасение!

Git позволяет настроить внешний вид нашего графика. 
Например, можно использовать короткий формат вывода:

git log --graph --oneline

Вы увидите историю изменений в следующем формате:

* cba2190 Fix issue in results.csv
*   eb417cd Merge branches
|\
| * 1fca03b Create a new file
| * 40396b1 Delete temporary file
* | 017afc8 Add new data file
* | 266cc42 Update data file
|/
* 103b63a Add JSON configuration files


В этом примере Git отображает:

хеши (например, cba2190, eb417cd),
линии, изображающие структуру ветвления:
|\ и |/ показывают слияние веток;
* указывает на коммиты в репозитории.




Откат слияния

После слияния веток коммит создается автоматически. 
Чтобы отменить (откатить) слияние, мы используем команду git revert:
git revert -m 1 <hash-коммита-слияния>


В этом случае мы сохраним всю историю изменений, но отменим эффект слияния веток.

Что происходит в команде git revert:

-m 1 указывает, какую родительскую ветку сохранить (обычно указывается 1 для основной ветки);
<hash-коммита-слияния> — на этом месте мы указываем хеш коммита, 
который появился после git merge в логе изменений.








Типичные ошибки
Тип ошибки 1: Слияние основной ветки (main или master)

Иногда изменения сливаются из основной ветки (main или master), 
однако в среде разработчика это считается грубой ошибкой.

В основной ветке хранится стабильная версия проекта. 
Объединяя основную ветку с любой другой веткой проекта, 
вы рискуете потерять стабильную версию и подводите своих коллег, которые не узнают, 
куда «спряталась» основная версия.

Пример ошибки:

Вы начали сливать изменения из ветки main или master в вашу рабочую ветку, 
вместо того чтобы слить ветку с протестированными изменениями в основную:

git merge main

Решение:

Создавайте отдельные ветки для новых функций или изменений, 
а затем сливайте их в основную ветку после тестирования, а не наоборот.

Чтобы слить изменения из вашей рабочей ветки в основную, 
переключитесь на основную ветку и используйте merge с указанием имени ветки, 
из которой сливаются изменения:

git checkout main
git merge feature









Тип ошибки 2: Работа в основной ветке (main или master)

Часто все изменения хочется внести сразу в основную ветку. 
Однако основная ветка должна оставаться стабильной, 
иначе мы можем утратить рабочую версию программы.

Пример ошибки:

Вы добавили новую функцию сразу в ветку main, не протестировав программу, 
а затем внезапно обнаружили, что новый код сломал основную версию.

Решение:

Для новых функций или изменений всегда создавайте отдельные ветки, например:
git checkout -b feature




После завершения работы объедините ветку с основной. 
Однако не торопитесь: убедитесь в том, что код протестирован.








Практикум
Задача 1: Создание репозитория, добавление изменений и работа с ветками

Ситуация:

В процессе работы над проектом вы решили использовать ветки для разработки новых функций. 
У вас уже есть репозиторий my-project, где создан файл README.md и настроен .gitignore. 
Теперь вам нужно создать новую ветку, в которой вы будете проводить эксперименты. 
Добавьте новый файл в созданной ветке и выполните слияние.


Задача:

Создайте новую ветку, внесите изменения в этой ветке, а затем выполните слияние с основной веткой.

Шаги реализации:

Перейдите в репозиторий my-project.
Создайте новую ветку.
Внесите изменения в новую ветку.
Выполните коммит.
Выполните слияние ветки с основной.
Реализация:

Перейдите в репозиторий my-project:

cd my-project


2. Создайте ветку для разработки новых функций (например, feature):

git checkout -b feature


3. В новой ветке создайте файл script.py и добавьте в него простой Python-код. Например:

# script.py
print("Hello from the feature branch!")
Сохраните изменения.


4. Добавьте файл в индекс и создайте коммит:

git add script.py
git commit -m "Add script.py with a print statement"


5. Переключитесь на основную ветку:

git checkout main


6. Слейте изменения из ветки feature в основную:

git merge feature


7. (Опционально) Удалить ветку feature. Если работа завершена, удалите ветку:

git branch -d feature













Задача 2: Создание репозитория, добавление функции и работа с ветками

Ситуация:

В процессе работы над проектом вы решили создать новую функцию для приветствия 
пользователя и использовать ветки для разработки новых функций. 
У вас уже есть репозиторий my-project, где создан файл README.md и настроен .gitignore. 
Теперь вам нужно сформировать новую ветку для экспериментов, 
добавить в ней функцию приветствия и выполнить слияние изменений с основной веткой.

Задача:

Создайте новую ветку, добавьте функцию приветствия пользователя в этой ветке, 
а затем выполните слияние изменений с основной веткой.

Шаги реализации:

Перейдите в репозиторий my-project:

cd my-project


2. Создайте ветку для разработки новой функции (например, feature/greet):

git checkout -b feature/greet


3. В новой ветке создайте файл greet.py и добавьте функцию для приветствия. Например:

# greet.py
def greet_user(name):
    return f"Hello, {name}!"

if __name__ == "__main__":
    user_name = input("Enter your name: ")
    print(greet_user(user_name))
Сохраните изменения.



4. Добавьте файл в индекс и создайте коммит:

git add greet.py
git commit -m "Add greet_user function for user greeting"


5. Переключитесь на основную ветку:

git checkout main


6. Слейте изменения из ветки feature/greet в основную:

git merge feature/greet


7. (Опционально) Удалите ветку feature/greet.
Если работа завершена, удалите ветку:

git branch -d feature/greet





Итоги:
Работа с ветками позволяет проводить эксперименты, внедрять новые фичи и работать в команде. 
Однако работа с ветками иногда приводит к конфликтам. По окончании занятия мы:

Освоили работу с ветками.
Научились разрешать конфликты при слиянии изменений.
Разобрали процесс создания веток, переключения между ними и слияния изменений.
На следующем занятии мы познакомимся с платформой GitHub и научимся работать с Pull Request.