Мы уже познакомились с основами SQLite: узнали, как работают базы данных, 
как создавать таблицы, добавлять и извлекать данные с помощью SQL-запросов. 
Теперь настало время научиться использовать в проектах SQLite с помощью Python.

SQLite в Python позволяет:

создавать интерактивные приложения, которые работают с базами данных;
проводить анализ данных, например подсчёт продаж, клиентов или заказов;
хранить информацию без необходимости настраивать сложные серверные решения.
Работа с SQLite через SQL и Python похожа на взаимодействие повара и официанта. 
Python управляет процессом работы с данными, как повар на кухне: 
SQL принимает запросы (как заказы посетителей ресторана), 
передаёт их в базу данных (на кухню) 
и представляет результаты запроса (приносит готовое блюдо).


Сегодня мы разберём:

как подключаться к SQLite из Python,
как выполнять SQL-запросы программно,
как получать и обрабатывать данные.



Глоссарий к девятому занятию
sqlite3

sqlite3 — встроенная библиотека Python для работы с SQLite. 
Позволяет подключаться к базе данных, 
выполнять SQL-запросы и обрабатывать результаты программно.

Connection (Соединение)

Connection — объект, создаваемый при подключении к базе данных 
с помощью sqlite3.connect(). 
Управляет взаимодействием с файлом базы данных и используется для выполнения запросов.

Cursor (Курсор)

Cursor — объект, который выполняет SQL-запросы и получает результаты. 
Создаётся с помощью метода .cursor() у объекта соединения.

fetchone(), fetchall(), fetchmany(n) (Получение результатов)

fetchone(), fetchall(), fetchmany(n) — методы курсора, 
позволяющие получать результаты SQL-запросов:

fetchone() — возвращает одну строку результата,
fetchall() — возвращает все строки результата в виде списка,
fetchmany(n) — возвращает n строк результата.

Commit (Коммит)

Commit — операция сохранения изменений в базе данных. Выполняется с помощью conn.commit().

Rollback (Откат)

Rollback — операция отмены несохранённых изменений в базе данных. 
Выполняется с помощью conn.rollback().

Close connection (Закрытие соединения)

Close connection — завершение работы с базой данных. 
Выполняется с помощью conn.close(), чтобы освободить ресурсы.

Мы уже познакомились с SQLite, научились создавать базы данных, 
добавлять и извлекать данные с помощью SQL-запросов. 
Теперь рассмотрим, как выполнять эти операции в Python с использованием библиотеки sqlite3.


Библиотека sqlite3 в Python

Python предоставляет встроенный модуль sqlite3, 
который позволяет работать с базами данных SQLite программно. 
Он не требует отдельной установки и доступен сразу после установки Python.

Модуль sqlite3 позволяет автоматизировать работу с базой данных:

программно отправлять SQL-запросы,
получать и обрабатывать результаты,
интегрировать работу с базой данных в свои приложения.
Рассмотрим пошагово работу с SQLite в Python.

Подключение к базе данных

Перед выполнением операций с базой данных необходимо установить соединение. 
В sqlite3 это делается с помощью метода sqlite3.connect().

Например:

import sqlite3

# Подключение к базе данных (если файла "example.db" нет, он будет создан автоматически)
conn = sqlite3.connect("example.db")

Созданный объект conn мы будем использовать для работы с базой данных.

Выполнение SQL-запросов

Для работы с SQL-запросами в SQLite используется объект курсора (cursor). 
Он создаётся через соединение с базой:

cursor = conn.cursor()
С помощью курсора можно выполнять SQL-запросы: 
создавать таблицы, добавлять данные, обновлять и удалять записи.

Пример создания таблицы:

cursor.execute("""
    CREATE TABLE IF NOT EXISTS users (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        name TEXT NOT NULL,
        age INTEGER
    )
""")

Метод execute() выполняет переданный SQL-запрос.

После выполнения запросов изменения в базе данных необходимо сохранить:

conn.commit()
Что такое commit в SQLite?
commit — это команда для сохранения изменений в базе данных.

Если не выполнить conn.commit(), все внесённые изменения (добавление, 
обновление или удаление данных) не сохранятся после закрытия соединения.

Это похоже на ситуации, когда мы пишем текст в документе, 
а потом закрываем его без сохранения: все наши правки исчезают.


Как работает commit?

Когда вы выполняете команду INSERT, UPDATE или DELETE, 
изменения сначала применяются в транзакции, 
но не записываются в сам файл базы данных. 
Чтобы они сохранились, нужно использовать команду commit.


Транзакция — это последовательность операций с базой данных (например, 
последовательность команд INSERT, UPDATE и DELETE), 
которые выполняются как единое целое. 
Это значит, что при вызове команды commit сохранения 
будут применяться ко всем операциям в транзакции сразу.

Пока commit не выполнен, 
изменения находятся во временном состоянии и не записываются в файл базы данных.

Рассмотрим пример транзакции в банковских операциях.

Представьте, что вы переводите деньги между банковскими счетами. 
Мы будем выполнять следующую последовательность действий:

Снимаем деньги с одного счёта.
Зачисляем их на другой счёт.

Если в процессе что-то пойдёт не так (например, отключится интернет), 
важно, чтобы ни одна из операций не была выполнена частично 
— иначе мы рискуем потерять деньги. 
Поэтому изменения сохранятся на нашем банковском счете, 
только когда будут выполнены обе операции (завершится транзакция).


Транзакция в SQLite работает точно так же: 
все изменения либо фиксируются, либо полностью откатываются.

Пример:

import sqlite3

conn = sqlite3.connect("mydb.db")  # 1. Открываем соединение
cursor = conn.cursor()  # 2. Создаём курсор

cursor.execute("""
    CREATE TABLE IF NOT EXISTS users (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        name TEXT NOT NULL,
        age INTEGER
    )
""")  # 3. Выполняем запрос

conn.commit()  # 4. Фиксируем изменения

conn.close()  # 5. Закрываем соединение


Добавление данных

Для добавления данных в таблицу используем команду INSERT INTO.

Пример добавления записей:

conn = sqlite3.connect("students.db")
cursor = conn.cursor()

cursor.execute("INSERT INTO students (name, age, course) VALUES (?, ?, ?)",
               ("Иван Иванов", 20, "Математика"))

cursor.execute("INSERT INTO students (name, age, course) VALUES (?, ?, ?)",
               ("Анна Смирнова", 22, "Физика"))

conn.commit()
conn.close()




Что такое rollback в SQLite?
rollback — это команда для отмены изменений, 
внесённых в базу данных в рамках текущей транзакции.

Если во время выполнения SQL-запросов что-то пошло не так (ошибка, 
сбой или отмена операции), 
rollback позволяет вернуть базу данных в исходное состояние, 
как если бы изменения никогда не происходили.

Представьте, что вы пишете сообщение, 
но случайно стираете важный фрагмент текста. 
Если у вас есть кнопка «Отмена» (Ctrl + Z), 
вы можете вернуть удалённую часть обратно. 
rollback работает так же: он отменяет последние изменения в базе данных, 
если они ещё не были зафиксированы commit.

Когда использовать rollback?

Если в процессе выполнения транзакции произошла ошибка.
Если нужно отменить изменения перед commit.
Пример:

import sqlite3

conn = sqlite3.connect("mydb.db")
cursor = conn.cursor()

try:
    cursor.execute("BEGIN TRANSACTION")  # Начинаем транзакцию
    cursor.execute("INSERT INTO users (name) VALUES (?)", ("Alice",))
    cursor.execute("INSERT INTO users (name) VALUES (?)", ("Bob",))

    raise Exception("Что-то пошло не так!")  # Искусственно создаём ошибку

    conn.commit()  # Этот код не выполнится из-за ошибки

except Exception as e:
    conn.rollback()  # Откатываем все изменения
    print("Ошибка:", e)

finally:
    conn.close()  # Закрываем соединение




В этом коде при возникновении ошибки выполняется rollback() 
и все изменения отменяются. 
Если бы commit() успел выполниться, 
rollback() уже не смог бы вернуть базу в исходное состояние.




Чтение данных

Для получения данных используется команда SELECT.

Пример вывода всех студентов из базы:

conn = sqlite3.connect("students.db")
cursor = conn.cursor()

cursor.execute("SELECT * FROM students")
students = cursor.fetchall()

for student in students:
    print(student)

conn.close()



Чтение результатов из базы данных SQLite

После выполнения SELECT-запросов в SQLite важно правильно обработать результаты.
 Библиотека sqlite3 в Python предоставляет три метода для получения данных из запроса:

fetchone() — получает одну строку из результата запроса.
fetchmany(n) — получает n строк из результата запроса.
fetchall() — получает все строки из результата запроса.

Метод fetchone() возвращает первую строку результата запроса в виде кортежа. 
Если строк больше нет, возвращается None.

Пример:

import sqlite3

# Подключение к базе
conn = sqlite3.connect("example.db")
cursor = conn.cursor()

# Выполняем запрос
cursor.execute("SELECT * FROM users")

# Получаем одну строку
row = cursor.fetchone()
print(row)  # Выведет, например: (1, 'Алиса', 25)

conn.close()



Метод fetchmany(n) возвращает список из n строк. 
Если строк меньше, чем n, он вернёт все оставшиеся строки.

Пример:

cursor.execute("SELECT * FROM users")
rows = cursor.fetchmany(2)  # Получим 2 строки
print(rows)
# Выведет, например: [(1, 'Алиса', 25), (2, 'Боб', 30)]

Этот метод полезен, 
если нужно обработать данные пакетами (например, 
загружать порциями для экономии памяти).

Пример чтения данных порциями:

cursor.execute("SELECT * FROM users")

while True:
    rows = cursor.fetchmany(2)  # Читаем по 2 строки за раз
    if not rows:
        break
    for row in rows:
        print(row)

Метод fetchall() возвращает все строки запроса в виде списка кортежей.

Пример:

cursor.execute("SELECT * FROM users")
rows = cursor.fetchall()

print(rows)
# [(1, 'Алиса', 25), (2, 'Боб', 30), (3, 'Елена', 28)]

Этот метод удобен, когда нужно обработать все результаты сразу, 
но, если данных очень много, он может потреблять много памяти.





Фильтрация данных

Чтобы получить данные по определённым условиям, используем WHERE.

Пример выборки студентов, которые изучают математику:

cursor.execute("SELECT * FROM students WHERE course = ?", ("Математика",))
math_students = cursor.fetchall()


Обновление данных

Для изменения данных используем UPDATE.

Пример изменения курса студента:

cursor.execute("UPDATE students SET course = ? WHERE name = ?", ("Информатика", "Иван Иванов"))
conn.commit()

Удаление данных

Удаление записей выполняется через DELETE FROM.

Пример удаления всех студентов старше 21 года:

cursor.execute("DELETE FROM students WHERE age > ?", (21,))
conn.commit()

Закрытие соединения. После работы с базой данных соединение всегда следует закрывать:
conn.close()








Типичные ошибки
Тип ошибки 1: Метод commit() не использован после внесения изменений в базу данных

Часто ошибки возникают, 
когда мы забываем вызвать метод commit() после внесения изменений в базу данных.

Например, если не вызвать commit() после таких операций, 
как INSERT, UPDATE или DELETE, то изменения не будут занесены в базу.

Пример ошибки:

import sqlite3

conn = sqlite3.connect("example.db")
cursor = conn.cursor()

cursor.execute("INSERT INTO users (name, age) VALUES ('Иван', 32)")
# Нет вызова commit()

conn.close()

В этом примере изменения не сохранятся в базе данных, 
так как мы не вызвали conn.commit().

Решение:

После любых изменений в базе необходимо вызвать commit():

conn.commit()  # Сохраняем изменения в базе данных
Теперь изменения будут записаны в базу данных.







Тип ошибки 2: Неверное использование метода fetchall()

Использование метода fetchall() для извлечения всех данных 
сразу может привести к перегрузке памяти, 
особенно если в нашей базе хранится много записей. 
Это может спровоцировать ошибку недостатка памяти или замедлить работу программы.

Пример ошибки:

cursor.execute("SELECT * FROM large_table")
rows = cursor.fetchall()  # Много данных
Если в вашей базе хранится много записей, 
метод fetchall() может перегрузить память устройства. 
Это приведет к избыточному потреблению ресурсов вашего компьютера.

Решение:

Используйте методы fetchone() или fetchmany(n), извлекайте данные по частям:

rows = cursor.fetchmany(1000)  # Читаем данные по 1000 строк
Или используйте fetchone() в цикле, где вам нужно обрабатывать данные по одной строке:

while True:
    row = cursor.fetchone()
    if row is None:
        break
    print(row)





Тип ошибки 3: Использование неподключённого соединения

Иногда ошибки возникают, когда мы пытаемся выполнить запрос после того, 
как соединение с базой данных было закрыто.

Пример ошибки:

conn = sqlite3.connect("example.db")
cursor = conn.cursor()

conn.close()  # Закрываем соединение

cursor.execute("SELECT * FROM users")  # Попытка выполнения после закрытия
В этом примере попытка выполнить запрос после того, 
как мы закрыли соединение, вызовет ошибку.

Решение:

Убедитесь, что соединение активно перед выполнением запросов:

if conn:
    cursor.execute("SELECT * FROM users")
else:
    print("Соединение с базой данных закрыто.")










Практикум
Задача 1: Создание базы данных для управления пользователями 
с использованием SQLite3 в Python

Ситуация:
Вы разрабатываете приложение для управления пользователями. 
Вам нужно создать базу данных SQLite, 
которая будет содержать таблицу с данными о пользователях. 
Она должна включать информацию о пользователях, их возрасте и адресе email.

Задача:
Создайте таблицу users, 
которая будет хранить информацию о пользователях. 
Затем выполните запросы для добавления данных, 
чтения данных и фильтрации записей по возрасту с использованием SQLite3 в Python.

Шаги реализации:

1. Создайте подключение к базе данных и таблицу users. 
Сначала создайте базу данных и таблицу с помощью Python и модуля sqlite3:

import sqlite3

# Подключение к базе данных (если файла нет, он будет создан)
conn = sqlite3.connect('users.db')
cursor = conn.cursor()

# Создание таблицы users
cursor.execute('''
CREATE TABLE IF NOT EXISTS users (
    user_id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    age INTEGER NOT NULL,
    email TEXT NOT NULL
);
''')

# Сохранение изменений
conn.commit()
2. Заполните таблицу users данными. 
Добавьте несколько записей о пользователях в таблицу users:

# Добавление данных
cursor.executemany('''
INSERT INTO users (name, age, email) VALUES (?, ?, ?)
''', [
    ('Иван Иванов', 28, 'ivan@example.com'),
    ('Мария Смирнова', 34, 'maria@example.com'),
    ('Петр Петров', 22, 'peter@example.com'),
    ('Алексей Иванов', 25, 'alexey@example.com'),
    ('Ольга Сидорова', 29, 'olga@example.com')
])

# Сохранение изменений
conn.commit()

3. Чтение данных из таблицы users. 
Выполните запрос для чтения всех данных из таблицы:

# Выполнение запроса
cursor.execute('SELECT * FROM users')

# Получение всех строк
users = cursor.fetchall()

# Вывод данных
for user in users:
    print(user)

4. Фильтрация данных. Выполните запрос для фильтрации пользователей, 
чей возраст больше 25 лет:

# Выполнение запроса с фильтрацией
cursor.execute('SELECT name, age, email FROM users WHERE age > 25')

# Получение и вывод данных
filtered_users = cursor.fetchall()
for user in filtered_users:
    print(user)

5. Закрытие соединения. После завершения работы с базой данных закройте соединение:

# Закрытие соединения с базой данных
conn.close()

Реализация
import sqlite3

# Шаг 1: Создание базы данных и таблицы
conn = sqlite3.connect('users.db')
cursor = conn.cursor()

cursor.execute('''
CREATE TABLE IF NOT EXISTS users (
    user_id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    age INTEGER NOT NULL,
    email TEXT NOT NULL
);
''')
conn.commit()

# Шаг 2: Заполнение таблицы данными
cursor.executemany('''
INSERT INTO users (name, age, email) VALUES (?, ?, ?)
''', [
    ('Иван Иванов', 28, 'ivan@example.com'),
    ('Мария Смирнова', 34, 'maria@example.com'),
    ('Петр Петров', 22, 'peter@example.com'),
    ('Алексей Иванов', 25, 'alexey@example.com'),
    ('Ольга Сидорова', 29, 'olga@example.com')
])
conn.commit()

# Шаг 3: Чтение всех данных
print("Все пользователи:")
cursor.execute('SELECT * FROM users')
users = cursor.fetchall()
for user in users:
    print(user)

print("\n" + "="*50 + "\n")

# Шаг 4: Фильтрация по возрасту
print("Пользователи старше 25 лет:")
cursor.execute('SELECT name, age, email FROM users WHERE age > 25')
filtered_users = cursor.fetchall()
for user in filtered_users:
    print(user)

# Шаг 5: Закрытие соединения
conn.close()








Задача 2: Работа с таблицей пользователей: обновление и удаление данных

Ситуация:
Вы продолжаете работать с базой данных пользователей. 
В этой задаче вам нужно будет обновить информацию 
о некоторых пользователях и удалить старые данные.

Задача:

Обновите возраст некоторых пользователей.
Удалите пользователей с определённым возрастом из базы данных.

Шаги реализации:

1. Обновление данных. 
Для начала обновим возраст отдельных пользователей. \
Например, увеличим возраст пользователя с ID 1 на 2 года, 
а возраст пользователя с ID 4 увеличим на 3 года:

# Подключение к базе данных
conn = sqlite3.connect('users.db')
cursor = conn.cursor()

# Обновление данных
cursor.execute('''
UPDATE users
SET age = age + 2
WHERE user_id = 1
''')

cursor.execute('''
UPDATE users
SET age = age + 3
WHERE user_id = 4
''')

# Сохранение изменений
conn.commit()

2. Удаление данных. Удалим пользователей, чей возраст менее 25 лет. 
Для этого используем команду DELETE:

# Удаление пользователей с возрастом меньше 25
cursor.execute('''
DELETE FROM users
WHERE age < 25
''')

# Сохранение изменений
conn.commit()

3. Проверка результатов. 
После выполнения обновлений и удалений выполните 
запрос для проверки изменений в базе данных:

# Выполнение запроса для чтения всех оставшихся пользователей
cursor.execute('SELECT * FROM users')

# Получение всех строк и вывод на экран
updated_users = cursor.fetchall()
for user in updated_users:
    print(user)

# Закрытие соединения
conn.close()

Реализация
import sqlite3

# Подключение к базе данных
conn = sqlite3.connect('users.db')
cursor = conn.cursor()

print("Исходные данные:")
cursor.execute('SELECT * FROM users')
users = cursor.fetchall()
for user in users:
    print(user)

print("\n" + "="*50 + "\n")

# Шаг 1: Обновление данных
print("Обновление возраста пользователей...")
cursor.execute('''
UPDATE users
SET age = age + 2
WHERE user_id = 1
''')

cursor.execute('''
UPDATE users
SET age = age + 3
WHERE user_id = 4
''')

conn.commit()
print("Возраст пользователей обновлен!")

print("\n" + "="*50 + "\n")

# Проверка после обновления
print("Данные после обновления возраста:")
cursor.execute('SELECT * FROM users')
users = cursor.fetchall()
for user in users:
    print(user)

print("\n" + "="*50 + "\n")

# Шаг 2: Удаление данных
print("Удаление пользователей младше 25 лет...")
cursor.execute('''
DELETE FROM users
WHERE age < 25
''')

conn.commit()
print("Пользователи младше 25 лет удалены!")

print("\n" + "="*50 + "\n")

# Шаг 3: Проверка результатов
print("Финальные данные после всех операций:")
cursor.execute('SELECT * FROM users')
updated_users = cursor.fetchall()
for user in updated_users:
    print(user)

# Закрытие соединения
conn.close()









Итоги:
Сегодня мы научились:

подключаться к базе данных SQLite в Python с помощью sqlite3.connect():
 теперь мы можем открыть базу данных students.db и создать соединение;
создавать таблицы с CREATE TABLE, добавлять данные с INSERT INTO, 
читать – с SELECT, обновлять – с UPDATE, 
удалять – с DELETE FROM в библиотеке sqlite3 на Python;
работать с транзакциями с commit и rollback, 
сохранять и отказывать изменения так, чтобы не терялась важная информация;
закрывать соединение с базой с conn.close(), 
например при завершении работы с students.db