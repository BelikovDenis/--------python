На предыдущих занятиях мы научились работать с HTTP-запросами:
отправлять их с помощью библиотеки requests, настраивать заголовки, использовать API-ключи,
обрабатывать коды ошибок и устанавливать тайм-ауты.
Мы освоили работу с REST API и получили базовые навыки извлечения данных из внешних источников.



Однако на практике мы часто сталкиваемся с ситуацией,
когда нужной информации нет в открытом API:
она размещена прямо на страницах сайтов.
В этом случае на помощь приходит парсинг HTML — автоматический сбор данных с веб-страниц.



Сегодня мы научимся:

извлекать данные из HTML с помощью библиотек BeautifulSoup;
находить нужные элементы по тегам, классам и атрибутам;
обходить защиту от парсинга (например, работу с User-Agent и задержками);
обрабатывать ошибки при парсинге и избегать зависаний скриптов.






Глоссарий к шестнадцатому занятию
Парсинг (Parsing)

Парсинг – автоматизированное извлечение данных из HTML-кода веб-страниц.

DOM (Document Object Model)

DOM – древовидная структура HTML-документа, с которой работают парсеры.

BeautifulSoup

BeautifulSoup – библиотека Python для парсинга HTML и XML-документов.

CSS-селекторы (CSS Selectors)

CSS-селекторы – способ выбора элементов HTML по их тегам, классам (class), идентификаторам (id) и атрибутам.

User-Agent

User-Agent – HTTP-заголовок, идентифицирующий браузер или программу, делающую запрос (используется для обхода блокировок).





Что такое парсинг?

Парсинг – это процесс автоматизированного извлечения данных с веб-страниц.
В отличие от API, где информация предоставляется в структурированном виде
(например, JSON), парсинг предполагает анализ HTML-кода страницы.


Анализ HTML-кода позволяет сделать выборку нужных данных, 
например вывести все ссылки, которые указаны на страницах, собрать данные, 
представленные в таблицах или скачать тексты, изображения и другие данные с сайта.



Где и зачем используется парсинг?

Парсинг применяется в различных сферах, включая:

анализ рынка (сбор цен, отзывов, ассортимента конкурентов);
мониторинг новостей и соцсетей (агрегация контента, трендов);
поиск вакансий и контактов (парсинг сайтов для поиска работы, email-рассылок);
научные исследования (анализ открытых данных, научных статей);
автоматизацию рутинных задач (проверка наличия товаров в интернет-магазине).




Основные этапы парсинга

Отправка HTTP-запроса.

Используются библиотеки (например, requests).
Важно учитывать заголовки (например, User-Agent) и возможные ограничения (CAPTCHA, блокировки).


Загрузка и анализ HTML.

Полученный HTML обрабатывается парсерами, например BeautifulSoup.


Извлечение данных.

Поиск элементов сайта. 
Например, поиск может производиться через CSS-селекторы, 
через которые можно получить доступ к тегам, классам, 
идентификаторам и атрибутам элементов веб-страниц.


Очистка и структурирование.

Удаление лишних символов (пробелы, HTML-теги).
Преобразование данных в удобный формат (например, к виду JSON, CSV или базы данных).



Сохранение результатов.

Запись в файлы (например, с помощью библиотеки pandas) или базы данных, такие как SQLite.



Обработка ошибок и оптимизация.

Защита от блокировок (например, использование User-Agent для идентификации программы, делающей запрос).
Обработка исключений (например, 404).






Парсинг веб-страниц с BeautifulSoup

BeautifulSoup — это популярная библиотека Python для парсинга HTML и XML-документов. 
Она позволяет легко извлекать данные из веб-страниц.


Начнем с установки библиотеки:

pip install beautifulsoup4 requests
Рассмотрим процесс парсинга веб-страниц на примере страницы с «Хабра». 
Импортируем библиотеку requests для получения информации с веб-страницы и 
библиотеку BeautifulSoup для парсинга:



import requests
from bs4 import BeautifulSoup

# Прописываем ссылку на ресурс, который будем парсить
url = "https://habr.com/ru/articles/
response = requests.get(url)
html = response.text  # Получаем HTML-код страницы


Создание объекта BeautifulSoup

# 'html.parser' — стандартный парсер для обработки HTML-документов
soup = BeautifulSoup(html, "html.parser")

Поиск элементов в HTML

BeautifulSoup предлагает несколько способов поиска данных:

Поиск по тегам

# Находит первый тег с заголовками первого уровня <h1>
title = soup.find("h1")
# Все теги параграфов <p>
all_paragraphs = soup.find_all("p")


Поиск по классу (class)
# Находит элемент div с классом "tm-article-body"
article_content = soup.find("div", class_="tm-article-body")


Поиск по атрибуту
# Находит ссылку с указанным href (hyperreference, значением гиперссылки)
author_link = soup.find("a", {"href": "/ru/users/username/"})


Пример: извлечение заголовка и текста статьи

title = soup.find("h1", class_="tm-title").text.strip()  # Заголовок статьи
content = soup.find("div", class_="tm-article-body").text.strip()  # Основной текст
print(f"Заголовок: {title}\n\nТекст: {content[:200]}...")  # Выводим первые 200 символов

Разберем этот код:

Поиск заголовка (<h1>):

title = soup.find("h1", class_="tm-title").text.strip()
soup.find("h1", class_="tm-title") ищет первый элемент <h1> с классом tm-title.
.text извлекает текстовое содержимое.
.strip() удаляет лишние пробелы и переносы строк.

Поиск основного текста (<div>):

content = soup.find("div", class_="tm-article-body").text.strip()

Аналогично ищется <div> с классом tm-article-body и извлекается текст.

Вывод данных:

print(f"Заголовок: {title}\n\nТекст: {content[:200]}...")
Использование User-Agent для обхода блокировок

Некоторые сайты блокируют запросы от программ (ботов). 
Чтобы избежать этого, можно имитировать браузер, передавая User-Agent.

User-Agent — это строка в HTTP-заголовках, 
которую браузер или программа отправляет на сервер, 
чтобы представиться. Она содержит информацию о:

названии и версии браузера (Chrome, Firefox, Safari),
операционной системе (Windows, macOS, Linux),
типе устройства (ПК, смартфон, бот).

Пример User-Agent:

Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36

(Это Chrome 120 на Windows 10).


Как работает User-Agent?

Браузер/скрипт отправляет запрос с User-Agent.
Сервер анализирует его и может:
отдать обычную версию сайта (для ПК);
отдать мобильную версию (если это телефон);
заблокировать бота (если User-Agent подозрительный).

Как добавить User-Agent в скрипт для парсинга?

headers = {
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
}

response = requests.get(url, headers=headers)  # Отправляем запрос с заголовком

Почему это важно?

Без User-Agent сервер может вернуть 403 Forbidden.

Сайты анализируют заголовки, чтобы отличать ботов от реальных пользователей.

Обработка ошибок при парсинге

Парсинг может завершиться ошибкой по разным причинам:

Страница недоступна (404, 500).

Элемент не найден (изменилась структура сайта).
Сайт заблокировал запрос.



Напишем скрипт для парсинга с обработкой ошибок:

try:
    response = requests.get(url, headers=headers, timeout=5)  # Тайм-аут 5 секунд
    response.raise_for_status()  # Проверяем, нет ли ошибки HTTP (4xx/5xx)

    soup = BeautifulSoup(response.text, "html.parser")
    title = soup.find("h1").text.strip()
    print(f"Успешно! Заголовок: {title}")

except requests.exceptions.RequestException as e:
    print(f"Ошибка запроса: {e}")
except AttributeError:
    print("Элемент не найден (страница изменилась?)")
except Exception as e:
    print(f"Неизвестная ошибка: {e}")

Что проверяем при обработке ошибок?

Доступность страницы (requests.get + raise_for_status).
Наличие элементов (try-except при поиске в BeautifulSoup).
Тайм-ауты (timeout=5), чтобы скрипт не зависал.

Полный пример парсинга статьи с «Хабра»

Рассмотрим полный пример парсинга веб-страницы:

import requests
from bs4 import BeautifulSoup

url = "https://habr.com/ru/articles/803869/"

headers = {
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
}

try:
    response = requests.get(url, headers=headers, timeout=5)
    response.raise_for_status()

    soup = BeautifulSoup(response.text, "html.parser")

    title = soup.find("h1", class_="tm-title").text.strip()
    content = soup.find("div", class_="tm-article-body").text.strip()

    print(f"Заголовок: {title}\n")
    print(f"Текст статьи (начало):\n{content[:300]}...")

except requests.exceptions.RequestException as e:
    print(f"Ошибка при запросе: {e}")
except AttributeError:
    print("Не удалось найти нужные элементы на странице.")
except Exception as e:
    print(f"Неизвестная ошибка: {e}")
















Типичные ошибки
Тип ошибки 1: Неправильный выбор парсера

Проблема:

Использование стандартного html.parser 
для сложных или HTML-страниц с ошибками в структуре 
может привести к некорректному разбору структуры.

Пример ошибки:

Этот код может пропустить теги, если они некорректно оформлены в HTML-документе:

soup = BeautifulSoup(broken_html, "html.parser")
print(soup.find("div"))

Код вернет None, если в коде есть неточности, даже если теги div присутствуют на странице.

Решение:

Использовать более устойчивые парсеры, например lxml:

soup = BeautifulSoup(broken_html, "lxml")

Парсер lxml лучше обрабатывает HTML с ошибками в структуре.






Тип ошибки 2: Неправильная обработка кодировки

Проблема:
Сайты могут возвращать текст в разных кодировках 
(например, Windows-1251 вместо UTF-8). 
Это приводит к тому, что вместо исходного текста отображаются нечитаемые символы.

Пример ошибки:

response = requests.get("https://site.com")
soup = BeautifulSoup(response.text, "lxml")

В этом примере мы не прописали кодировку сайта, 
поэтому вместо текста мы можем получить набор символов, которые нужно дешифровать.

Решение:
Принудительно указывать кодировку:

response = requests.get("https://site.com")
response.encoding = "utf-8"  # Или "windows-1251" для более старых сайтов
soup = BeautifulSoup(response.text, "lxml")

Мы прописали кодировку сайта и обеспечили стабильность при выгрузке текстовых данных.
















Практикум
Задача 1: Парсинг сайта «Академии ТОП»

Ситуация:

Вы получили задачу разработать скрипт для сбора текстовой информации с 
главной страницы https://spb.top-academy.ru. 
Ваши коллеги посоветовали вам воспользоваться библиотекой 
BeautifulSoup для эффективной загрузки результатов.

Используйте следующий код для тестирования инструмента:

if __name__ == "__main__":
    url = "https://spb.top-academy.ru"
    paragraphs = get_paragraphs(url)

    if paragraphs:
        print(paragraphs[:100])
    else:
        print("Не удалось получить параграфы со страницы")

Задача:

Создайте новую функцию для GET-запроса к указанному сайту. 
Укажите заголовки, URL, пропишите тайм-ауты и добавьте обработку ошибок с raise_for_status.

Шаги реализации:

Импортируйте библиотеки:

import requests
from bs4 import BeautifulSoup

Создайте функцию get_paragraphs для выгрузки информации со страницы.
Создайте функцию:

def get_paragraphs(url):
    """Получение всех параграфов с указанной страницы"""

Пропишите заголовки:

headers = {
'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36'
}

Сделайте запрос к странице с помощью библиотеки requests, укажите URL, пропишите заголовки и установите тайм-аут:

response = requests.get(url, headers=headers, timeout=10)
Добавьте обработку ошибок:
response.raise_for_status()

Решение:

import requests
from bs4 import BeautifulSoup

def get_paragraphs(url):
    """Получение всех параграфов с указанной страницы"""
    headers = {
        'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36'
    }
    response = requests.get(url, headers=headers, timeout=10)
    response.raise_for_status()









Задача 2: Дорабатываем функцию get_paragraphs

Ситуация:
Вы продолжаете разрабатывать функцию для получения текстовой информации со страницы. 
Добавьте метод для парсинга информации с помощью BeautifulSoup.

Задача:
Доработайте функцию, добавив создание объекта soup и выгрузку параграфов.

Шаги реализации:

Создайте объект soup, который использует стандартный HTML-парсер.
soup = BeautifulSoup(response.text, 'html.parser')


Пропишите цикл для получения текстовой информации. Включите следующие элементы:
soup.find_all('p'):


Ищет все теги <p> (параграфы) в HTML-документе, который был передан в объект BeautifulSoup (переменная soup).
Возвращает список объектов Tag (каждый представляет один тег <p>).

Итерация for p in soup.find_all('p'):

Перебирает все найденные теги <p> по одному.
Для каждого тега временно сохраняет его в переменной p.
p.get_text():

Для каждого тега <p> извлекает текстовое содержимое (без HTML-тегов).
Удаляет все внутренние теги, оставляя только видимый текст.
.strip():

Удаляет лишние пробелы и переносы строк:
в начале и конце текста;
оставляет только значимое содержимое.
Условие if p.get_text().strip():

Фильтрует параграфы, оставляя только те, которые:
содержат непустой текст после очистки;
исключает пустые параграфы (которые могут быть в HTML для разметки).
Формирование списка [...]:

Создает новый список, содержащий только:
очищенные тексты параграфов;
без пустых элементов.
result = []
for p in soup.find_all('p'):
    text = p.get_text().strip()
    if text:  # если текст не пустой
        result.append(text)
return result
Решение:


soup = BeautifulSoup(response.text, 'html.parser')
result = []
for p in soup.find_all('p'):
    text = p.get_text().strip()
    if text:  # если текст не пустой
        result.append(text)
return result



Итоги:
Сегодня мы научились:

использовать библиотеку BeautifulSoup для парсинга веб-страниц;
прописывать заголовки для обхода блокировки страницы с помощью User-Agent;
извлекать информацию об элементах веб-страниц, обращаясь к CSS-селекторам.