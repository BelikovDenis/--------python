Мы освоили работу с инкапсуляцией на примере работы с ORM.
Сегодня мы продолжим изучать основные концепции ООП и познакомимся с наследованием.

Разберём:

что такое наследование и зачем оно нужно при построении ORM;

как правильно организовывать иерархии классов;

примеры использования наследования для создания ORM-систем.





Глоссарий к двенадцатому занятию
Наследование (Inheritance)
Наследование – механизм в объектно-ориентированном программировании, 
позволяющий создавать новый класс на основе существующего. 
Дочерний класс наследует атрибуты и методы родительского класса, 
что способствует повторному использованию кода.

Базовый класс (Base Class / Parent Class)
Базовый класс – класс, от которого наследуются другие классы. 
Содержит общие атрибуты и методы, которые могут быть использованы в дочерних классах.

Дочерний класс (Child Class / Derived Class)
Дочерний класс – класс, который наследует атрибуты и методы базового класса. 
Может добавлять новые атрибуты и методы или переопределять существующие.

Суперкласс (Superclass)
Суперкласс – другое название базового, или родительского, класса. 
От него наследуются другие классы.

Множественное наследование (Multiple Inheritance)
Множественное наследование – возможность класса наследовать атрибуты и
методы нескольких родительских классов. 
В Python оно поддерживается, но требует осторожности из-за возможных конфликтов.




Наследование — это один из ключевых принципов объектно-ориентированного программирования (ООП), 
который позволяет создавать новые классы на основе уже существующих.

Новый класс (дочерний) наследует атрибуты и методы родительского класса, 
что позволяет повторно использовать код и упрощать структуру программы.

В контексте ORM наследование особенно полезно для создания иерархий моделей данных. 
Например, если у вас есть общие поля или методы для нескольких таблиц базы данных, 
их можно вынести в базовый класс, а затем наследовать от него конкретные модели.


Как правильно организовывать иерархии классов?

Базовый класс (родительский): содержит общие атрибуты и методы, 
которые будут использоваться в дочерних классах.

Дочерний класс: наследует функциональность базового класса и 
может добавлять свои уникальные атрибуты и методы или переопределять существующие.

Пример:

class BaseModel:
    def __init__(self, id):
        self.id = id

    def save(self):
        print(f"Объект с id={self.id} сохранён в базу данных.")

class User(BaseModel):
    def __init__(self, id, name):
        super().__init__(id)  # Вызов конструктора родительского класса
        self.name = name

    def display_info(self):
        print(f"Пользователь: {self.name}, ID: {self.id}")

# Использование
user = User(1, "Alice")
user.save()          # Метод из родительского класса
user.display_info()  # Метод из дочернего класса

Давайте разберём этот код шаг за шагом, чтобы понять, 
как работает наследование и взаимодействие между классами.

Базовый класс BaseModel

class BaseModel:
    def __init__(self, id):
        self.id = id

    def save(self):
        print(f"Объект с id={self.id} сохранён в базу данных.")

Что здесь происходит:

__init__ (конструктор).
Это специальный метод, который автоматически вызывается при создании нового объекта класса. 
Он принимает параметр id и сохраняет его в атрибуте self.id.
Например, если мы создадим объект BaseModel(1), то у него будет атрибут id = 1.

Метод save.
Этот метод выводит сообщение о том, 
что объект с определённым id был сохранён в базу данных. 
В реальной ORM-системе этот метод мог бы выполнять запрос 
к базе данных для сохранения объекта.

Дочерний класс User

class User(BaseModel):
    def __init__(self, id, name):
        super().__init__(id)  # Вызов конструктора родительского класса
        self.name = name

    def display_info(self):
        print(f"Пользователь: {self.name}, ID: {self.id}")

Что здесь происходит:

Наследование.
Класс User наследует данные класса BaseModel. 
Это означает, что User автоматически получает все атрибуты и методы класса BaseModel, такие как id и save.


super().__init__(id).

Эта строка вызывает конструктор родительского класса (BaseModel). 
Она передаёт параметр id в родительский класс, чтобы инициализировать атрибут self.id.

Без вызова super().__init__(id) атрибут self.id не был бы создан.

Добавление нового атрибута name.
Класс User добавляет новый атрибут self.name, который хранит имя пользователя.

Метод display_info.
Этот метод выводит информацию о пользователе с помощью атрибутов self.name и self.id. 
Обратите внимание, что self.id был унаследован от BaseModel.

Использование классов

# Создание объекта класса User
user = User(1, "Alice")

# Вызов метода save из родительского класса
user.save()          # Вывод: "Объект с id=1 сохранён в базу данных."

# Вызов метода display_info из дочернего класса
user.display_info()  # Вывод: "Пользователь: Alice, ID: 1"

Что здесь происходит:

Создание объекта User.
Мы создаём объект user класса User, 
передавая ему два аргумента: id=1 и name="Alice".

Внутри класса User:

Вызывается конструктор __init__.
Через super().__init__(id) вызывается конструктор BaseModel, который инициализирует self.id = 1.
Инициализируется self.name = "Alice".

Вызов метода save.
Метод save был унаследован от класса BaseModel. 
Он выводит сообщение о сохранении объекта с id=1.

Вызов метода display_info.
Метод display_info определён в классе User. 
Он использует атрибуты self.name и self.id для вывода информации о пользователе.

Класс User наследует функциональность класса BaseModel, 
что позволяет избежать дублирования кода. 
Например, метод save не нужно писать заново в классе User.

Расширение функциональности.
Класс User добавляет новые атрибуты (name) и методы (display_info), 
которые специфичны для пользователей.

Использование super().
Ключевое слово super() позволяет вызывать методы родительского класса, 
что особенно полезно при работе с конструкторами.

В контексте ORM:

Класс BaseModel может представлять базовую модель данных, 
которая умеет сохранять себя в базу данных.

Класс User может представлять таблицу пользователей в базе данных, 
добавляя характерные для пользователей поля (например, name).

Таким образом, наследование позволяет создавать гибкие и легко расширяемые ORM-системы.

Пример использования наследования в ORM

Представим, что мы разрабатываем ORM для работы с базой данных. 
У нас есть несколько таблиц: Users, Products, Orders. 
Они имеют общие поля, такие как id и created_at, а также общий метод save(). 
Мы можем создать базовый класс BaseModel, который будет содержать эту общую логику, 
а затем наследовать от него конкретные модели.

from datetime import datetime

class BaseModel:
    def __init__(self, id):
        self.id = id
        self.created_at = datetime.now()

    def save(self):
        print(f"Объект с id={self.id} сохранён в базу данных.")

class User(BaseModel):
    def __init__(self, id, name):
        super().__init__(id)
        self.name = name

class Product(BaseModel):
    def __init__(self, id, title, price):
        super().__init__(id)
        self.title = title
        self.price = price

# Использование
user = User(1, "Alice")
product = Product(101, "Laptop", 999.99)

user.save()          # Объект с id=1 сохранён в базу данных.
product.save()       # Объект с id=101 сохранён в базу данных.


Преимущества наследования в ORM

Повторное использование кода: общая логика (например, сохранение объекта) 
выносится в базовый класс, что уменьшает дублирование кода.

Упрощение поддержки: изменения в базовом классе автоматически применяются ко всем дочерним классам.

Гибкость: дочерние классы могут расширять или переопределять функциональность базового класса, 
что делает систему более адаптируемой.










Типичные ошибки
Тип ошибки 1: Забыли вызвать super().__init__() в дочернем классе

Если вы не вызываете конструктор родительского класса (super().__init__()), 
то атрибуты родительского класса не будут инициализированы. 
Это может привести к ошибкам при попытке доступа к этим атрибутам.

Пример ошибки:

class Animal:
    def __init__(self, name):
        self.name = name

class Dog(Animal):
    def __init__(self, name, breed):
        self.breed = breed  # Забыли вызвать super().__init__(name)

dog = Dog("Бобик", "Дворняжка")
print(dog.name)  # Ошибка: AttributeError: 'Dog' object has no attribute 'name'

Решение:

Всегда вызывайте super().__init__() в конструкторе дочернего класса.

class Dog(Animal):
    def __init__(self, name, breed):
        super().__init__(name)  # Вызов конструктора родительского класса
        self.breed = breed










Тип ошибки 2: Конфликты имён при множественном наследовании

При множественном наследовании может возникнуть ситуация, 
когда два родительских класса имеют методы или атрибуты с одинаковыми именами. 
Это может привести к неожиданному результату.

Пример ошибки:

class A:
    def method(self):
        print("Метод класса A")

class B:
    def method(self):
        print("Метод класса B")

class C(A, B):
    pass

obj = C()
obj.method()  # Вывод: "Метод класса A" (но если порядок наследования изменится, результат может быть другим)

Решение:

Используйте метод разрешения порядка (MRO) и точно указывайте, какой метод нужно вызывать.

class C(A, B):
    def method(self):
        super().method()  # Вызов метода из первого родительского класса (A)
        B.method(self)    # Явный вызов метода из класса B

















Практикум
Задача 1: Наследование и ORM: расширение функциональности модели

Ситуация:
Ваша команда разрабатывает ORM для управления пользователями и администраторами в системе. 
У вас уже есть базовый класс User, который описывает обычного пользователя. 
Теперь необходимо создать класс Admin, который будет наследовать от User и
добавлять дополнительные атрибуты и методы, характерные для администратора.

Вы получили следующий код:

class User:
    def __init__(self, id, name, email):
        self._id = id       # Приватный атрибут
        self._name = name   # Приватный атрибут
        self._email = email # Приватный атрибут

    @property
    def id(self):
        return self._id

    @property
    def name(self):
        return self._name

    @property
    def email(self):
        return self._email

    def save(self):
        # Логика сохранения в базу данных
        print(f"Сохранение пользователя: {self.name}, email: {self.email}")

Задача:
Создайте класс Admin, который будет наследовать от класса User.

Шаги реализации:

Создайте класс Admin, который наследует от User.
Добавьте дополнительный атрибут _role (роль администратора), который будет приватным:

class Admin(User):
    def __init__(self, id, name, email, role):
        super().__init__(id, name, email)  # Вызов конструктора родительского класса
        self._role = role  # Приватный атрибут





реализация 
class User:
    def __init__(self, id, name, email):
        self._id = id       # Приватный атрибут
        self._name = name   # Приватный атрибут
        self._email = email # Приватный атрибут

    @property
    def id(self):
        return self._id

    @property
    def name(self):
        return self._name

    @property
    def email(self):
        return self._email

    def save(self):
        # Логика сохранения в базу данных
        print(f"Сохранение пользователя: {self.name}, email: {self.email}")


class Admin(User):
    def __init__(self, id, name, email, role):
        super().__init__(id, name, email)  # Вызов конструктора родительского класса
        self._role = role  # Приватный атрибут

    @property
    def role(self):
        return self._role

    def save(self):
        print(f"Сохранение администратора: {self.name}, email: {self.email}, роль: {self.role}")

    def manage_users(self):
        print(f"Администратор {self.name} управляет пользователями системы")

    def manage_permissions(self):
        print(f"Администратор {self.name} управляет правами доступа")


# Пример использования
if __name__ == "__main__":
    # Создаем администратора
    admin = Admin(1, "Алексей Иванов", "alex@example.com", "Супер-администратор")
    
    # Проверяем свойства
    print(f"ID: {admin.id}")
    print(f"Имя: {admin.name}")
    print(f"Email: {admin.email}")
    print(f"Роль: {admin.role}")
    
    # Используем методы
    admin.save()
    admin.manage_users()
    admin.manage_permissions()
    
    # Проверяем наследование - создаем обычного пользователя
    user = User(2, "Мария Петрова", "maria@example.com")
    user.save()














Задача 2: Улучшение класса ORM: добавление сеттеров

Ситуация:
Вам необходимо добавить геттеры и сеттеры, которые созданный вами класс наследует от родительского класса.

Задача:
Добавьте к классу User следующие методы:

Геттер для атрибута _role.

Метод delete_user(user_id), который будет имитировать удаление пользователя с указанным user_id.

Шаги реализации:

Добавьте геттер для атрибута _role.
@property
  def role(self):
      return self._role
Добавьте метод delete_user(user_id).
def delete_user(self, user_id):
        # Логика удаления пользователя
        print(f"Администратор {self.name} удалил пользователя с id {user_id}")







реализация
class User:
    def __init__(self, id, name, email):
        self._id = id       # Приватный атрибут
        self._name = name   # Приватный атрибут
        self._email = email # Приватный атрибут

    @property
    def id(self):
        return self._id

    @property
    def name(self):
        return self._name

    @name.setter
    def name(self, value):
        self._name = value

    @property
    def email(self):
        return self._email

    @email.setter
    def email(self, value):
        self._email = value

    def save(self):
        # Логика сохранения в базу данных
        print(f"Сохранение пользователя: {self.name}, email: {self.email}")


class Admin(User):
    def __init__(self, id, name, email, role):
        super().__init__(id, name, email)  # Вызов конструктора родительского класса
        self._role = role  # Приватный атрибут

    @property
    def role(self):
        return self._role

    @role.setter
    def role(self, value):
        self._role = value

    def save(self):
        print(f"Сохранение администратора: {self.name}, email: {self.email}, роль: {self.role}")

    def manage_users(self):
        print(f"Администратор {self.name} управляет пользователями системы")

    def manage_permissions(self):
        print(f"Администратор {self.name} управляет правами доступа")

    def delete_user(self, user_id):
        # Логика удаления пользователя
        print(f"Администратор {self.name} удалил пользователя с id {user_id}")


# Пример использования и тестирования
if __name__ == "__main__":
    # Создаем администратора
    admin = Admin(1, "Алексей Иванов", "alex@example.com", "Супер-администратор")
    
    # Тестируем геттеры
    print("=== Тестирование геттеров ===")
    print(f"ID: {admin.id}")
    print(f"Имя: {admin.name}")
    print(f"Email: {admin.email}")
    print(f"Роль: {admin.role}")
    
    # Тестируем сеттеры
    print("\n=== Тестирование сеттеров ===")
    admin.name = "Алексей Петров"
    admin.email = "alex.petrov@example.com"
    admin.role = "Системный администратор"
    
    print(f"Обновленное имя: {admin.name}")
    print(f"Обновленный email: {admin.email}")
    print(f"Обновленная роль: {admin.role}")
    
    # Тестируем методы
    print("\n=== Тестирование методов ===")
    admin.save()
    admin.manage_users()
    admin.manage_permissions()
    admin.delete_user(123)
    
    # Проверяем наследование - создаем обычного пользователя
    print("\n=== Тестирование базового класса User ===")
    user = User(2, "Мария Петрова", "maria@example.com")
    
    # Тестируем сеттеры User
    user.name = "Мария Сидорова"
    user.email = "maria.sidorova@example.com"
    
    print(f"Имя пользователя: {user.name}")
    print(f"Email пользователя: {user.email}")
    user.save()









Итоги:
Сегодня на занятии мы научились:

создавать родительский и дочерний классы;
использовать ключевое слово super для вызова метода родительского класса;
работать с множественным присваиванием.
Инструменты, которые мы сегодня освоили, позволяют структурировать код, повторно использовать код в программах и избегать ошибок при работе с ООП.