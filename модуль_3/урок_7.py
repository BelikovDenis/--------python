Мы научились писать запросы, чтобы решать базовые задачи с помощью SQL. 
Например, мы теперь умеем находить всех сотрудников определённого отдела 
или выбирать заказы, совершённые после конкретной даты.


Скоро нам придётся решать более сложные задачи:

Ситуация 1: сначала нам нужно вычислить средний размер заказа, 
а затем выбрать только те заказы, которые превышают этот размер.



Ситуация 2: сначала нам нужно определить сотрудников с зарплатой выше средней, 
а затем выбрать из них только тех, кто работает в отделе продаж.


Для таких задач существуют вложенные запросы и
 общие табличные выражения (CTE, Common Table Expressions).


Допустим, у нас есть таблица sales. 
В ней мы хотим найти все заказы, сумма которых выше средней. 
Сперва вложенный запрос вычислит среднюю сумму заказа, 
а затем внешний запрос выберет только те заказы, которые её превышают.


Общие табличные выражения (CTE) — это способ временно создать виртуальную таблицу, 
которую можно использовать в основном запросе. 
CTE делает сложные SQL-запросы более читаемыми и удобными для работы.


Допустим, у нас есть таблица employees. 
В ней мы хотим сначала определить, у кого из сотрудников зарплата выше средней, 
а затем выбрать из них только сотрудников отдела продаж. 
CTE позволяет разделить эти два шага и сделать код понятнее.


Вложенные запросы похожи на матрёшку: один запрос вложен в другой.
CTE — это своеобразный черновик, временная таблица, которая делает код более читаемым.


Сегодня мы:

разберём, что такое вложенные запросы и CTE;
узнаем, когда лучше использовать вложенные запросы, а когда CTE;
научимся разбивать сложные SQL-запросы на простые части;
напишем и выполним собственные запросы с вложенными подзапросами и CTE.






Глоссарий к седьмому занятию
Subquery (Вложенный запрос)
Subquery — SQL-запрос, который выполняется внутри другого запроса. 
Используется для предварительной выборки данных, 
которые затем обрабатываются во внешнем запросе.

Common Table Expression, CTE (Общее табличное выражение)
CTE — временный результат запроса, 
который можно использовать в основном SQL-запросе. 
Объявляется с помощью WITH и помогает сделать код 
более читаемым и удобным для повторного использования.

WITH (Оператор для CTE)
WITH — оператор, который позволяет объявить CTE перед основным SQL-запросом.

EXISTS (Оператор проверки существования)
EXISTS — оператор, который проверяет, 
возвращает ли вложенный запрос хотя бы одну строку. 
Чаще всего используется в условиях WHERE для фильтрации данных.

IN (Оператор проверки значений)
IN — оператор, проверяющий, содержится ли значение в списке, 
который может быть результатом вложенного запроса.

Correlated Subquery (Коррелированный подзапрос)
Correlated Subquery — вложенный запрос, 
который зависит от внешнего запроса и выполняется для каждой его строки.

Потренироваться в переводах и лучше запомнить определения каждого термина 
мы советуем в приложении Quizlet. 
Чтобы открыть карточки по занятию, нужно перейти по этой ссылке.

Когда нам нужно сделать сложные запросы, 
обработка которых состоит из нескольких этапов, 
мы можем сначала выполнить один запрос, посмотреть результат, 
а потом использовать его во втором запросе. 


Но такой подход имеет несколько недостатков:

нам придется вручную копировать и вставлять данные между запросами;
если база данных содержит много данных, 
нам придется загружать их в промежуточную таблицу или переменную,
 что замедлит нашу работу.
Вот почему используют вложенные запросы (Subqueries) и CTE: 
они позволяют делать всё автоматически, внутри одного запроса.



Вложенные запросы и CTE

Часто для работы с базами данных нам нужно создавать сложные запросы, 
которые состоят из нескольких этапов обработки. 
К таким запросам относятся вложенные запросы (Subqueries) 
и общие табличные выражения (CTE, Common Table Expressions).

Чтобы освоить синтаксис вложенных запросов и CTE, 
создадим небольшую базу данных в SQLite через DB Browser for SQLite.

Сформируем таблицу employees с информацией о сотрудниках:

CREATE TABLE employees (
    id INTEGER PRIMARY KEY,
    name TEXT,
    department TEXT,
    salary INTEGER
);

INSERT INTO employees (name, department, salary) VALUES
('Анна', 'Маркетинг', 60000),
('Иван', 'Продажи', 50000),
('Ольга', 'IT', 120000),
('Пётр', 'Продажи', 55000),
('Мария', 'IT', 110000),
('Дмитрий', 'Маркетинг', 65000),
('Екатерина', 'IT', 125000);

Работая с базой employees, мы можем столкнуться с задачами, 
которые невозможно решить простым SELECT. 
Например, найти сотрудников, у которых зарплата выше средней по компании.

Такая задача потребует обработки данных в несколько шагов. 
Здесь и приходят на помощь вложенные запросы и CTE.

Вложенные запросы

Вложенный запрос — это запрос внутри другого запроса. 
Он выполняется первым, а затем его результат используется во внешнем запросе. 
Это похоже на выполнение сложного математического выражения: 
сначала считаем скобки, затем используем результат.

Мы применяем вложенные запросы, когда:

нужно вычислить промежуточный результат, который мы будем использовать в основном запросе;
нельзя или неудобно использовать JOIN;
требуется фильтрация данных перед выполнением основного запроса.
Найдём сотрудников с зарплатой выше средней с помощью вложенных запросов:

SELECT name, salary
FROM employees
WHERE salary > (SELECT AVG(salary) FROM employees);
Рассмотрим запросы, которые мы использовали, чтобы решить задачу:

(SELECT AVG(salary) FROM employees) — находит среднюю зарплату по компании.
WHERE salary > (...) — фильтрует сотрудников, у которых зарплата выше средней.
SELECT name, salary — выводит имена и зарплаты таких сотрудников.

Мы использовали скобки, чтобы выделить вложенный запрос при решении нашей задачи. 
Для подсчёта среднего значения мы использовали функцию AVG.

Получаем следующий результат:


Correlated Subquery (Коррелированный подзапрос)

Коррелированный подзапрос — это SQL-запрос, 
который выполняется внутри другого запроса 
и зависит от каждой строки внешнего запроса. 
В отличие от обычного вложенного запроса, 
он выполняется многократно: по одной итерации для каждой строки внешнего запроса.

Мы используем коррелированные подзапросы, когда:

нужно сравнить данные в каждой строке с подмножеством данных в той же таблице;
требуется отфильтровать строки на основе сложных условий, 
которые зависят от значений в других строках;
невозможно или неудобно использовать JOIN.

Используя коррелированный подзапрос, найдём сотрудников, 
у которых зарплата выше средней по их отделу.

SELECT name, salary, department_id
FROM employees e1
WHERE salary > (
    SELECT AVG(salary)
    FROM employees e2
    WHERE e1.department_id = e2.department_id
);

Рассмотрим запросы, которые мы использовали, чтобы решить задачу:

SELECT AVG(salary) FROM employees e2 WHERE e1.department_id = e2.department_id — 
считает среднюю зарплату внутри каждого отдела. 
Здесь e1.department_id = e2.department_id связывает подзапрос с текущей строкой внешнего запроса.

WHERE salary > (...) — фильтрует сотрудников, 
у которых зарплата выше средней по их отделу.

SELECT name, salary, department_id — выводит имена, 
зарплаты и идентификаторы отделов таких сотрудников.


Такой запрос выполняется для каждой строки из employees, 
рассчитывая среднюю зарплату только для соответствующего отдела.

Запрос с коррелированным подзапросом сложно читать, а если данных много, 
он может работать неэффективно. Нам нужно что-то, 
что сделает запросы проще и понятнее.

EXISTS и IN — ещё два способа писать вложенные запросы. 
Они позволяют проверять наличие данных в подзапросе 
и формировать запросы в удобно читаемой форме.

IN — используется, когда нужно проверить, содержится ли значение в списке из подзапроса.

Оператор IN проверяет, содержится ли значение в заданном списке. 
Этот список может быть фиксированным или являться результатом вложенного запроса.

Например, найдём сотрудников, которые работают в департаментах с низкой средней зарплатой:

SELECT name, department_id
FROM employees
WHERE department_id IN (
    SELECT department_id
    FROM employees
    GROUP BY department_id
    HAVING AVG(salary) < 50000
);

Здесь мы сначала определяем департаменты с низкой средней зарплатой, 
а затем ищем сотрудников, работающих в этих департаментах.

EXISTS — используется, когда важно проверить, 
существует ли хотя бы одна подходящая строка.

Оператор EXISTS проверяет, возвращает ли вложенный запрос хотя бы одну строку. 
Он возвращает TRUE, если подзапросу соответствует хотя бы одна строка, 
и FALSE, если ничего не найдено. 
Обычно используется для фильтрации данных в условии WHERE.

Допустим, нам нужно найти сотрудников, у которых есть подчинённые:

SELECT name
FROM employees e1
WHERE EXISTS (
    SELECT 1
    FROM employees e2
    WHERE e1.id = e2.manager_id
);

Здесь подзапрос проверяет, есть ли у сотрудника хотя бы один подчинённый.

Но даже такие запросы могут становиться громоздкими. 
Что делать, если нам нужно использовать несколько таких конструкций в одном запросе?

Улучшаем читаемость с помощью CTE

Вместо сложных вложенных подзапросов мы можем использовать 
общие табличные выражения (CTE). 
Это по сути именованные подзапросы, которые можно переиспользовать в основном запросе.

Допустим, мы хотим найти сотрудников, 
у которых зарплата выше средней по их отделу, 
но при этом они работают в департаменте с низкой средней зарплатой.

Сделаем это с помощью нескольких CTE:

WITH AvgDepartmentSalary AS (
    SELECT department_id, AVG(salary) AS avg_salary
    FROM employees
    GROUP BY department_id
),

HighEarners AS (
    SELECT e.name, e.salary, e.department_id
    FROM employees e
    JOIN AvgDepartmentSalary ads ON e.department_id = ads.department_id
    WHERE e.salary > ads.avg_salary
)

SELECT he.name, he.salary
FROM HighEarners he
JOIN AvgDepartmentSalary ads ON he.department_id = ads.department_id
WHERE ads.avg_salary < 50000;

Здесь:

AvgDepartmentSalary вычисляет среднюю зарплату по каждому департаменту.
HighEarners находит сотрудников, у которых зарплата выше средней по их отделу.
Основной запрос выбирает из HighEarners только тех, 
кто работает в департаменте с низкой средней зарплатой.
С помощью нескольких CTE мы сделали запрос логичным и читаемым. 
Теперь вложенные конструкции оформлены как отдельные шаги 
и их можно переиспользовать внутри запроса.

CTE используется, когда:

мы хотим улучшить читаемость сложных запросов;
один и тот же подзапрос используется несколько раз;
нужно разбить длинный запрос на логически понятные части.
Как выбрать подходящий тип запросов:

Вложенные запросы подходят для простых операций, 
однако в сложных случаях могут ухудшить читаемость кода.
Коррелированные подзапросы используются, 
когда внешний запрос передаёт данные во вложенный. 
Однако они могут работать неэффективно, так как выполняются для каждой строки.

CTE удобны для сложных запросов, они делают код чище, 
позволяют переиспользовать данные, 
но существуют только в рамках одного запроса и не сохраняются в базе данных.
Рассмотрим еще один пример. 
Чтобы решить задачу с использованием CTE, 
мы можем сначала вычислить среднюю зарплату по всей компании, 
а затем использовать эту информацию в основном запросе для фильтрации сотрудников.

WITH avg_salary AS (
    SELECT AVG(salary) AS avg_salary
    FROM employees
)

SELECT name, salary
FROM employees
WHERE salary > (SELECT avg_salary FROM avg_salary);

Разберем наш запрос:

CTE (WITH avg_salary AS (...)) — создаём временную таблицу, 
которая вычисляет среднюю зарплату по всей компании.
SELECT AVG(salary) AS avg_salary FROM employees 
— находит среднюю зарплату по таблице employees.

Основной запрос — выбираем имена и зарплаты сотрудников.

SELECT name, salary FROM employees WHERE salary > (SELECT avg_salary FROM avg_salary) 
— используя данные из CTE, выбираем сотрудников, чья зарплата выше средней.

Таким образом, запрос с CTE позволяет решить задачу: 
вычислить среднюю зарплату на отдельном шаге 
и затем отфильтровать сотрудников на основе этого значения.

В этом примере мы использовали команду AS, 
чтобы создать псевдоним для результата расчёта средней зарплаты 
и дать имя временной таблице — avg_salary.

AS — это ключевое слово в SQL, 
которое используется для задания псевдонимов (или алиасов) 
для таблиц или столбцов в запросах. 
Псевдонимы повышают читаемость запросов. 
Их удобно использовать в запросах с вычислениями, во вложенных запросах и CTE.



Сферы применения AS

Псевдонимы для столбцов
При выполнении вычислений (например, 
когда вам надо посчитать сумму или среднее значение) удобно использовать AS, 
чтобы присвоить результату имя, не вызывая функцию множество раз.

Например:

SELECT AVG(salary) AS avg_salary
FROM employees;

Псевдонимы для таблиц (сокращения)
Когда запрос становится сложным, 
например при работе с несколькими таблицами, можно использовать AS, 
чтобы создать псевдонимы для таблиц. Это упрощает запросы, 
особенно когда мы работаем с таблицами, имеющими длинные наименования.

Например:

SELECT e.name, e.salary, d.department_name
FROM employees AS e
JOIN departments AS d ON e.department_id = d.id;

FROM employees AS e — таблице employees присвоен псевдоним e.

FROM departments AS d — таблице departments присвоен псевдоним d.

Псевдонимы для CTE (Общее табличное выражение)
В CTE часто используется AS для определения имени временной таблицы. 
Это имя будет использоваться в основном запросе.

Например:

WITH avg_salary AS (
    SELECT AVG(salary) FROM employees
)
SELECT name, salary
FROM employees
WHERE salary > (SELECT avg_salary FROM avg_salary);

WITH avg_salary AS (...) — создаём CTE с псевдонимом avg_salary, 
который будет содержать вычисление средней зарплаты.

Особенности использования AS:

AS не является обязательным, 
но эта команда упрощает код и повышает его читаемость в сложных запросах;
псевдонимы можно использовать для столбцов и таблиц, выражений и вычислений.

Мы научились создавать вложенные запросы и таблицы для решения сложных задач в SQL.

Вложенные запросы удобны для небольших задач, но могут ухудшить читаемость.

Коррелированные подзапросы дают гибкость, но работают медленно.

Операторы IN и EXISTS помогают проверять списки и существование записей.

CTE делает сложные запросы читаемыми и удобными для повторного использования.

Использование AS делает SQL-запросы удобными и хорошо читаемыми.

Применим наши знания на практике!








Типичные ошибки
Тип ошибки 1: Отсутствие алиасов в подзапросах или CTE

Использование команды AS не является обязательным, 
однако без алиасов во вложенных запросах и CTE код становится громоздким. 
Понять, какие данные используются в запросе и что вы хотите вывести, становится трудно.

Пример ошибки:

WITH avg_salary AS (
    SELECT AVG(salary)
    FROM employees
)

SELECT name, salary
FROM employees
WHERE salary > (SELECT avg_salary FROM salary_stats);

В этом примере мы не использовали псевдоним для выполнения вложенного запроса. 
SQL не может правильно интерпретировать имя столбца avg_salary.

Решение:

Добавим алиас avg_salary с помощью AS:

WITH avg_salary AS (
    SELECT AVG(salary) AS avg_salary
    FROM employees
)

SELECT name, salary
FROM employees
WHERE salary > (SELECT avg_salary FROM salary_stats);






Тип ошибки 2: Отсутствие скобок вокруг подзапросов в сложных выражениях

При составлении более сложных запросов важно правильно расставлять скобки, 
чтобы SQL правильно интерпретировал логику вашего запроса.

Пример ошибки:

SELECT name, salary
FROM employees
WHERE salary > SELECT AVG(salary) FROM employees;

Этот подзапрос не запустится, потому что мы не заключили его в скобки.

Решение:

Обозначим вложенный запрос с помощью скобок.

SELECT name, salary
FROM employees
WHERE salary > (SELECT AVG(salary) FROM employees);









Практикум
Задача 1: Анализ заказов в базе данных SQLite

Ситуация:
Вы владелец интернет-магазина и храните данные о заказах в базе данных SQLite. 
Вам нужно заполнить свою таблицу данными о клиентах 
и провести простой анализ заказов в интернет-магазине.

Задача:
Создайте таблицу orders с информацией о заказах ваших клиентов. 
Добавьте данные в таблицу и, используя вложенный запрос, 
найдите заказы, стоимость которых выше средней.

Шаги реализации:

Создайте таблицу orders и заполните её данными:
id (INTEGER, PRIMARY KEY) — уникальный идентификатор заказа,
customer_name (TEXT) — имя покупателя,
total_price (REAL) — стоимость заказа,
order_date (TEXT) — дата заказа в формате YYYY-MM-DD.
CREATE TABLE orders (
    id INTEGER PRIMARY KEY,
    customer_name TEXT NOT NULL,
    total_price REAL NOT NULL,
    order_date TEXT NOT NULL
);

INSERT INTO orders (customer_name, total_price, order_date) VALUES
('Анна', 1200, '2024-02-01'),
('Борис', 800, '2024-02-02'),
('Виктор', 1500, '2024-02-03'),
('Галина', 1100, '2024-02-04'),
('Дмитрий', 2000, '2024-02-05');

Выполните вложенный запрос для поиска заказов, стоимость которых выше средней:
SELECT * FROM orders
WHERE total_price > (SELECT AVG(total_price) FROM orders);
Этот запрос вернёт заказы, где total_price больше среднего значения всех заказов.

Задача 2: Анализ заказов с помощью CTE в базе данных SQLite

Ситуация:
Вы продолжаете анализ данных в базе данных заказов клиентов вашего 
интернет-магазина и понимаете, что выборка данных с заказами, 
стоимость которых выше средней, может понадобиться вам в будущем. 
Вы решили сделать запрос по поиску заказов, 
стоимость которых выше средней, еще раз, но теперь вам нужно воспользоваться CTE.

Задача:
Используя CTE, найдите в таблице orders заказы, стоимость которых выше средней.

Шаги реализации:

CTE создает временную таблицу, которую можно использовать в основном запросе. 
Сначала мы вычисляем среднюю стоимость заказов в CTE, 
а затем используем её для фильтрации данных.

WITH avg_price AS (
    SELECT AVG(total_price) AS average_order_price FROM orders
)

SELECT * FROM orders
WHERE total_price > (SELECT average_order_price FROM avg_price);
WITH avg_price AS (...) — создаём CTE, который вычисляет среднюю стоимость заказов.

В основном запросе SELECT * FROM orders WHERE total_price > (...) фильтруем заказы, 
у которых total_price выше среднего значения.

Итоги:
Сегодня мы:

научились использовать вложенные запросы и CTE;
узнали, чем отличаются вложенные и коррелированные подзапросы;
освоили работу с IN и EXISTS;
научились писать CTE и повышать читаемость сложных SQL-запросов, 
используя CTE вместо вложенных подзапросов.