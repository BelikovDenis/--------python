Представим завод, который производит автомобили.
На заводе работают рабочие (экземпляры), есть чертежи,
на основе которых производятся машины (классы). Также есть инструкции по безопасности,
которые актуальны для всех заводов (статические методы).

Для эффективного управления, нам нужно понять, как работать с разными уровнями ответственности.

В Python это реализуется через методы экземпляра, методы класса и статические методы.


На занятии мы:

разберемся в различиях между методами экземпляра, класса и статическими методами.
научимся применять декораторы @classmethod и @staticmethod.
поймем, в каких ситуациях использовать каждый тип метода.
освоим примеры из реальной жизни, где методы класса и статические методы делают код удобным и организованным.


Decorator — специальная конструкция,
которая позволяет «украшать» или дополнять поведение функции или метода,
не изменяя их исходный код (например, @staticmethod, @classmethod).


Методы в Python – это «инструменты», которые принадлежат классам и объектам.

Каждый метод решает определённую задачу: одни работают с конкретными объектами, другие – с самим классом, а третьи – вообще независимы.

Чтобы понять разницу, представим, что есть команда из трёх человек, каждый из которых выполняет свою роль:

Рабочий (метод экземпляра) – работает с конкретным «объектом» (машиной, человеком, животным).
Менеджер (метод класса) – управляет всей группой «объектов».
Консультант (статический метод) – даёт общие советы, но сам не связан ни с объектами, ни с группой.


Теперь разберём всё это подробнее.

Методы экземпляра

Что это?

Методы экземпляра работают с конкретным объектом (экземпляром) класса. Они используют self, чтобы обращаться к данным и методам объекта.

Как это работает?

Если класс – это чертёж, то объект – это построенный дом.
Методы экземпляра «ремонтируют» или «обслуживают» этот дом, меняя что-то внутри.

Пример:

class Car:
    def __init__(self, brand, color):
        self.brand = brand  # Марка машины
        self.color = color  # Цвет машины

    def drive(self):
        print(f"{self.brand} {self.color} едет!")  # Машина выполняет действие

# Создаём объект
car = Car("Toyota", "красный")
car.drive()  # Вывод: Toyota красный едет!

Разбор:

_init_ – это конструктор, который создаёт объект с атрибутами brand и color.
метод drive работает с конкретным объектом, используя self для доступа к его данным.




Когда использовать?
























Когда метод должен работать только с конкретным объектом.

Методы класса

Что это?

Методы класса работают с самим классом.
Вместо self они используют cls, который позволяет обращаться к общим данным класса, доступным всем объектам.

Как это работает?

Методы класса похожи на «менеджера», который управляет всей группой объектов.
Например, они могут менять общие настройки или узнавать статистику.

Пример:

class Car:
    wheels = 4  # Общее свойство для всех машин

    @classmethod
    def describe(cls):
        print(f"Все машины имеют {cls.wheels} колеса.")
Разбор:

атрибут wheels принадлежит всему классу, а не конкретному объекту.
метод describe использует cls, чтобы работать с атрибутом wheels.


Использование:

Car.describe()  # Вывод: Все машины имеют 4 колеса.

Когда использовать?





















Когда нужно работать с общими данными или настраивать класс.



Статические методы

Статические методы не зависят ни от объектов, ни от класса.
Они работают как обычные функции, но логически связаны с классом.

Как это работает?

Статические методы – это «консультанты».
Они дают советы (общую информацию), но не вмешиваются в работу объектов или класса.

Пример:

class Car:
    @staticmethod
    def general_info():
        print("Машины – это транспортные средства.")

# Использование:
Car.general_info()  # Вывод: Машины – это транспортные средства.
Разбор:

метод general_info не использует ни self, ни cls.
он просто предоставляет информацию.


Когда использовать?
















Когда нужно написать полезный метод, который логически относится к классу, но не взаимодействует с его данными.



Сравнение методов

Чтобы лучше понять разницу между методами, представьте, что мы работаем в кафе:

Методы экземпляра – это как официант, который обслуживает конкретного клиента.
Методы класса – это как управляющий, который отвечает за всё кафе.
Статические методы – это как книга рецептов, которая помогает всем,
но не зависит от конкретного кафе или клиента.




Когда использовать разные типы методов?

Методы экземпляра: если нужно работать с данными конкретного объекта.

Пример: У машины может быть метод drive, который работает только с этой машиной.


Методы класса: если нужно работать с классом, а не с объектами.

Пример: Изменение общей настройки, например, изменение стандартного числа колёс у всех машин.


Статические методы: если метод не использует данные объекта или класса.

Пример: Нужно проверить, является ли введённое число допустимым значением.



Пример из жизни: Банк

Представим, что у нас есть класс BankAccount, который управляет банковскими счетами:

Методы экземпляра управляют конкретным счётом.
Методы класса изменяют процентную ставку для всех счетов.
Статические методы проверяют, допустима ли сумма перевода.

class BankAccount:
    interest_rate = 0.05  # Процентная ставка (атрибут класса)

    def __init__(self, owner, balance):
        self.owner = owner
        self.balance = balance

    def deposit(self, amount):
        self.balance += amount
        print(f"{self.owner} пополнил счёт на {amount}. Новый баланс: {self.balance}")

    @classmethod
    def set_interest_rate(cls, new_rate):
        cls.interest_rate = new_rate
        print(f"Процентная ставка изменена на {cls.interest_rate}")

    @staticmethod
    def is_valid_amount(amount):
        return amount > 0

# Пример использования
account = BankAccount("Иван", 1000)

# Работа с экземпляром
account.deposit(500)  # Иван пополнил счёт на 500. Новый баланс: 1500

# Работа с классом
BankAccount.set_interest_rate(0.07)  # Процентная ставка изменена на 0.07

# Использование статического метода
print(BankAccount.is_valid_amount(200))  # True

Теперь, когда мы знаем разницу между методами и сможем выбирать правильный инструмент для каждой задачи!









Типичные ошибки
Тип ошибки 1: Неправильное использование декоратора

Ошибка возникает, если случайно забыть использовать @classmethod или @staticmethod.
В результате Python думает, что это метод экземпляра, и ждёт аргумент self.

Пример ошибки:

class Car:
    def general_info():  # Ожидалось @staticmethod
        print("Машины — это транспортные средства.")

Car.general_info()  # TypeError: general_info() missing 1 required positional argument: 'self'


Решение:

Добавьте декоратор @staticmethod для обозначения, что метод не зависит от объекта:

class Car:
    @staticmethod
    def general_info():
        print("Машины — это транспортные средства.")









Тип ошибки 2: Забыт аргумент self или cls

Ошибка возникает, если забыть указать self для метода экземпляра или cls для метода класса.
Python не сможет определить, как метод должен работать.

Пример ошибки:

class Car:
    def drive():  # Нет self
        print("Машина едет!")

car = Car()
car.drive()  # TypeError: drive() missing 1 required positional argument: 'self'


Решение:

Добавьте аргумент self в метод:

class Car:
    def drive(self):
        print("Машина едет!")



















Тип ошибки 3: Перепутаны методы экземпляра и класса

Ошибка возникает, если мы случайно используем self вместо cls или наоборот.

Пример ошибки:

class Car:
    wheels = 4

    @classmethod
    def describe(self):  # Использован self вместо cls
        print(f"У всех машин {self.wheels} колеса.")


Это не вызовет ошибку, но нарушает стиль кода. Использование self в методах класса вводит путаницу.

Решение:

Используйте правильный аргумент:

class Car:
    wheels = 4

    @classmethod
    def describe(cls):
        print(f"У всех машин {cls.wheels} колеса.")


















Тип ошибки 4: Неправильное обращение к статическому методу

Ошибка возникает, если мы пытаемся вызвать статический метод через объект, как будто это метод экземпляра.

Пример ошибки:

class MathHelper:
    @staticmethod
    def square(num):
        return num ** 2

helper = MathHelper()
helper.square(4)  # Работает, но статический метод лучше вызывать через класс.

Это не ошибка, но считается плохим стилем. Статические методы следует вызывать через класс.

Решение:

Вызовите статический метод через класс:

MathHelper.square(4)  # Правильный вызов

















Тип ошибки 5: Неправильное использование атрибутов класса и экземпляра

Ошибка возникает, если мы пытаемся изменить атрибут класса через объект,
и это приводит к созданию нового атрибута экземпляра вместо изменения общего значения.

Пример ошибки:

class Car:
    wheels = 4

car1 = Car()
car1.wheels = 6  # Изменяет атрибут только для объекта car1
print(Car.wheels)  # Вывод: 4

Решение:

Изменяйте атрибуты класса через класс, а не через объект:

Car.wheels = 6  # Правильное изменение

















Тип ошибки 6: Ошибка в доступе к атрибутам через методы

Ошибка возникает, если мы случайно обращаемся к атрибуту экземпляра, используя cls в методе класса или наоборот.

Пример ошибки:

class Car:
    wheels = 4

    @classmethod
    def change_color(cls, color):
        cls.color = color  # Ошибка, цвет относится к объекту, а не классу


Решение:

Используйте self для атрибутов экземпляра и cls для атрибутов класса:

class Car:
    wheels = 4

    def change_color(self, color):
        self.color = color















Тип ошибки 7: Избыточное использование self или cls

Ошибка возникает, если мы используем self или cls, где это не нужно, например, в статическом методе.

Пример ошибки:

class MathHelper:
    @staticmethod
    def square(self, num):  # Ошибка: self не нужен
        return num ** 2

Решение:

Удалите ненужный аргумент:

class MathHelper:
    @staticmethod
    def square(num):
        return num ** 2









Практикум
Задача 1: Управление книгами в библиотеке

Ситуация: мы управляем библиотекой и хотим добавить автоматизацию в работу с книгами.
Каждая книга может быть либо «доступна», либо «в аренде».
Для управления состоянием книг нам необходимо добавить метод класса,
который будет отслеживать общее количество книг в библиотеке, и статический метод, который проверяет, находится ли книга в библиотеке.

Задача: создайте программу, которая позволяет:

Добавлять книги в библиотеку.
Менять статус книги (например, с «доступна» на «в аренде»).
Проверять, доступна ли книга.
Узнавать общее количество книг в библиотеке.
Ожидаемый результат:

Пользователь может добавлять книги.
Пользователь видит общий список книг.
Книга отображается как доступная или в аренде в зависимости от её статуса.
Шаги реализации:

Создайте класс LibraryBook с атрибутами title, status и атрибутом класса total_books:
class LibraryBook:
    total_books = 0  # Атрибут класса для подсчёта всех книг

    def __init__(self, title, status="доступна"):
        self.title = title
        self.status = status
        LibraryBook.total_books += 1  # Увеличиваем общее количество книг


Реализуйте метод класса get_total_books, который возвращает общее количество книг:
@classmethod
  def get_total_books(cls):
      return f"Всего книг в библиотеке: {cls.total_books}"


Добавьте статический метод is_available, который проверяет, находится ли книга в статусе «доступна»:
@staticmethod
  def is_available(status):
      return status == "доступна"


Реализуйте метод change_status, который позволяет изменять статус книги:
def change_status(self, new_status):
     self.status = new_status


Напишите основной код программы для создания книг и проверки их статуса:
# Основная программа
book1 = LibraryBook("Мастер и Маргарита")
book2 = LibraryBook("1984")
print(book1.title, "-", "Доступна?" if LibraryBook.is_available(book1.status) else "Недоступна")
book1.change_status("в аренде")
print(book1.title, "-", "Доступна?" if LibraryBook.is_available(book1.status) else "Недоступна")
print(LibraryBook.get_total_books())
Реализация:

class LibraryBook:
    total_books = 0  # Атрибут класса для подсчёта всех книг

    def __init__(self, title, status="доступна"):
        self.title = title
        self.status = status
        LibraryBook.total_books += 1  # Увеличиваем общее количество книг

    @classmethod
    def get_total_books(cls):
        """Возвращает общее количество книг в библиотеке."""
        return f"Всего книг в библиотеке: {cls.total_books}"

    @staticmethod
    def is_available(status):
        """Проверяет, доступна ли книга."""
        return status == "доступна"

    def change_status(self, new_status):
        """Изменяет статус книги."""
        self.status = new_status


# Основная программа
book1 = LibraryBook("Мастер и Маргарита")
book2 = LibraryBook("1984")
print(book1.title, "-", "Доступна?" if LibraryBook.is_available(book1.status) else "Недоступна")
book1.change_status("в аренде")
print(book1.title, "-", "Доступна?" if LibraryBook.is_available(book1.status) else "Недоступна")
print(LibraryBook.get_total_books())














Задача 2: Управление заказами ресторана

Ситуация: мы создаем систему управления заказами в ресторане.
Каждый заказ связан с определённым номером стола.
В ресторане есть ограниченное количество столов, и если стол занят, то новый заказ для него не принимается.

Задача: создайте программу, которая позволяет:

Добавлять заказы на определённый стол.
Проверять, свободен ли стол.
Узнавать общее количество заказов.
Отображать все заказы для каждого стола.
Шаги реализации:

Создайте класс RestaurantOrder с атрибутами table_number и order_details:
class RestaurantOrder:
    total_orders = 0  # Атрибут класса для подсчёта заказов
    tables = {}  # Словарь для отслеживания заказов по столам

    def __init__(self, table_number, order_details):
        self.table_number = table_number
        self.order_details = order_details
        RestaurantOrder.total_orders += 1
        RestaurantOrder.tables.setdefault(table_number, []).append(order_details)


Реализуйте статический метод is_table_available, который проверяет, можно ли добавить заказ для стола:
@staticmethod
   def is_table_available(table_number):
       return len(RestaurantOrder.tables.get(table_number, [])) == 0


Реализуйте метод класса для отслеживания общего количества заказов:
@classmethod
  def get_total_orders(cls):
      return f"Всего заказов: {cls.total_orders}"


Добавьте метод для добавления нового заказа:
def add_order(self):
      RestaurantOrder.tables.setdefault(self.table_number, []).append(self.order_details)


Напишите основной код программы для добавления и проверки заказов:
# Основная программа
order1 = RestaurantOrder(1, "Салат Цезарь")
order2 = RestaurantOrder(2, "Пицца Маргарита")
print(RestaurantOrder.is_table_available(3))  # True, стол 3 свободен
print(RestaurantOrder.is_table_available(1))  # False, стол 1 занят
print(RestaurantOrder.get_total_orders())  # Вывод: Всего заказов: 2



Ожидаемый результат:

Пользователь может добавлять заказы для столов.
Пользователь может проверять, свободен ли стол.
Пользователь может видеть общее количество заказов.
Все заказы для каждого стола отображаются корректно.









Реализация:

class RestaurantOrder:
    total_orders = 0  # Атрибут класса для подсчёта всех заказов
    tables = {}  # Словарь для отслеживания заказов по столам

    def __init__(self, table_number, order_details):
        self.table_number = table_number
        self.order_details = order_details
        RestaurantOrder.total_orders += 1
        # Добавляем заказ в список для стола
        RestaurantOrder.tables.setdefault(table_number, []).append(order_details)

    @staticmethod
    def is_table_available(table_number):
        """Проверяет, можно ли добавить заказ для стола."""
        return len(RestaurantOrder.tables.get(table_number, [])) == 0

    @classmethod
    def get_total_orders(cls):
        """Возвращает общее количество заказов."""
        return f"Всего заказов: {cls.total_orders}"

    @classmethod
    def show_orders(cls):
        """Выводит заказы по столам."""
        for table, orders in cls.tables.items():
            print(f"Стол {table}: {', '.join(orders)}")


# Основная программа
order1 = RestaurantOrder(1, "Салат Цезарь")
order2 = RestaurantOrder(2, "Пицца Маргарита")
order3 = RestaurantOrder(1, "Чай")

# Проверяем доступность столов
print(f"Стол 3 свободен? {'Да' if RestaurantOrder.is_table_available(3) else 'Нет'}")  # True
print(f"Стол 1 свободен? {'Да' if RestaurantOrder.is_table_available(1) else 'Нет'}")  # False

# Вывод общего количества заказов
print(RestaurantOrder.get_total_orders())  # Вывод: Всего заказов: 3

# Показываем заказы по столам
RestaurantOrder.show_orders()
