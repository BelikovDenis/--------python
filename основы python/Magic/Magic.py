Когда мы взаимодействуем с объектами в Python, например, складываем их,
сравниваем или выводим на экран, за кулисами происходят удивительные вещи.
Эти действия обрабатываются с помощью специальных методов, которые называются magic-методы (или dunder-методы — от слов double underscore).

Magic-методы позволяют определить, как объекты нашего класса будут вести себя в различных ситуациях.

На этом уроке мы разберём, как использовать magic-методы для управления поведением объектов,
и увидим, как они позволяют создавать удобные и гибкие программы.



На этом занятии мы:

узнаем, что такое magic-методы и как они работают.
поймем, зачем нужны dunder-методы и в каких случаях их стоит использовать.
освоим популярные magic-методы, такие как _init_, _str_, _repr_, _add_, _len_, и _eq_.
научимся применять magic-методы для создания удобных и понятных классов.
закрепим знания с помощью практических задач и примеров.






Что такое magic-методы?

Magic-методы
это специальные методы в Python, которые начинаются и заканчиваются подчеркиванием, например, _init_, _str_, _add_.
Эти методы позволяют переопределять стандартное поведение объектов нашего класса
и адаптировать его под свои задачи.
Переопределение означает, что мы изменяем то, как объект ведет себя в стандартных ситуациях.


Что должно происходить, когда мы создаем объект?
Как объект должен выглядеть, если его напечатать?
Что должно случиться, если мы сложим два объекта?
Как программа должна сравнивать два объекта?


Пример:

Мы создали класс «Телефон». Magic-методы позволяют описать его поведение:

_str_ — предоставляет общее строковое описание объекта

_init_ — описывает момент изготовления конкретного телефона

_repr_ — выдает подробную информацию, по которой можно воссоздать объект



Magic-методы помогают сделать так, чтобы наши классы вели себя как привычные объекты Python,
такие как числа, строки или списки.



Популярные magic-методы

_init_ — конструктор (инициализация объекта)
Этот метод автоматически вызывается при создании объекта класса.
Он отвечает за начальные настройки объекта: присваивает значения его свойствам (атрибутам).


class Student:
    def __init__(self, name, age):
        self.name = name  # Атрибут name (имя)
        self.age = age    # Атрибут age (возраст)

# Пример использования:
student = Student("Иван", 16)  # Создаём объект Student
print(student.name)  # Вывод: Иван
print(student.age)   # Вывод: 16


Как это работает?

Когда мы пишем student = Student(«Иван», 16),
Python вызывает метод _init_ и передаёт аргументы «Иван» и 16 в параметры name и age.
Эти параметры сохраняются внутри объекта в виде атрибутов self.name и self.age.




_str_ и _repr_ — преобразование в строку
Эти методы определяют, как наш объект будет представляться в виде строки:

_str_ — отвечает за отображение объекта для пользователя.

_repr_ — предназначен для разработчиков, чтобы показывать более подробное описание объекта.

class Student:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def __str__(self):
        return f"Студент: {self.name}, возраст: {self.age}"

    def __repr__(self):
        return f"Student('{self.name}', {self.age})"

# Пример использования:
student = Student("Иван", 16)
print(student)       # Вывод: Студент: Иван, возраст: 16
print(repr(student)) # Вывод: Student('Иван', 16)


Почему это важно?

_str_ удобен для вывода пользователю.

_repr_ помогает разработчикам, потому что возвращает информацию, полезную для отладки.

_add_ — перегрузка оператора «+»

Этот метод позволяет настроить, что произойдет, если мы используем оператор + для объектов нашего класса.

class Vector:
    def __init__(self, x, y):
        self.x = x
        self.y = y

    def __add__(self, other):
        return Vector(self.x + other.x, self.y + other.y)

    def __str__(self):
        return f"Vector({self.x}, {self.y})"

# Пример использования:
v1 = Vector(1, 2)
v2 = Vector(3, 4)
v3 = v1 + v2  # Здесь вызывается __add__
print(v3)     # Вывод: Vector(4, 6)

Как это работает?

Когда Python видит v1 + v2, он автоматически вызывает метод _add_.
Внутри этого метода мы описываем, как складывать два объекта.

Например, в этом случае мы складываем координаты x и у у двух векторов.




_len_ — длина объекта
Этот метод отвечает за работу функции len() для ваших объектов.

class Group:
    def __init__(self, members):
        self.members = members  # Список участников

    def __len__(self):
        return len(self.members)  # Возвращаем длину списка

# Пример использования:
group = Group(["Иван", "Мария", "Петр"])
print(len(group))  # Вывод: 3

Как это работает?

Когда мы вызываем len(group), Python вызывает метод _len_, чтобы узнать, сколько элементов в members.




_eq_ — сравнение объектов
Этот метод позволяет настраивать, как объекты будут сравниваться с помощью оператора ==.

class Student:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def __eq__(self, other):
        return self.name == other.name and self.age == other.age

# Пример использования:
s1 = Student("Иван", 16)
s2 = Student("Иван", 16)
s3 = Student("Мария", 15)
print(s1 == s2)  # Вывод: True
print(s1 == s3)  # Вывод: False

Как это работает?

Когда мы пишем s1 == s2, Python вызывает метод _eq_, чтобы сравнить два объекта.
В данном случае мы проверяем, равны ли их имена и возраст.




Magic-методы делают наши классы удобными и гибкими.
Они позволяют нашим объектам работать с привычными операциями (сложение,
сравнение, преобразование в строку),
как встроенные типы данных Python. Это помогает создавать понятный, красивый и легко читаемый код.








Типичные ошибки
Тип ошибки 1: Неправильная реализация метода _add_

Ошибка возникает, когда при сложении двух объектов не создаётся новый объект.

Пример ошибки:


class Vector:
    def __init__(self, x, y):
        self.x = x
        self.y = y

    def __add__(self, other):
        self.x += other.x
        self.y += other.y
        return self  # Ошибка: изменяется текущий объект, а не создаётся новый.



Решение:

Вместо изменения текущего объекта создавайте новый объект.

class Vector:
    def __init__(self, x, y):
        self.x = x
        self.y = y

    def __add__(self, other):
        return Vector(self.x + other.x, self.y + other.y)








Тип ошибки 2: Отсутствие возврата строки в _str_

Возникает, когда метод _str_ возвращает не строку, а объект другого типа, например, int.

Пример ошибки:

class Student:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def __str__(self):
        return self.age  # Ошибка: возвращается число, а не строка.


Решение:

Метод _str_ всегда должен возвращать строку.

class Student:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def __str__(self):
        return f"{self.name}, {self.age} лет"






Тип ошибки 3: Отсутствие проверки типов в методе _add_

Если попытаться сложить объект класса с несоответствующим типом, Python выбросит ошибку.

Пример ошибки:

class Vector:
    def __init__(self, x, y):
        self.x = x
        self.y = y

    def __add__(self, other):
        return Vector(self.x + other.x, self.y + other.y)  # Ошибка, если other не является объектом Vector.



Решение:

Добавьте проверку типа для аргумента other в методе _add_.

class Vector:
    def __init__(self, x, y):
        self.x = x
        self.y = y

    def __add__(self, other):
        if not isinstance(other, Vector):
            raise TypeError("Сложение возможно только между объектами класса Vector")
        return Vector(self.x + other.x, self.y + other.y)






Тип ошибки 4: Некорректная реализация _eq_

Возникает, когда метод _eq_ сравнивает не все атрибуты или использует некорректную логику.

Пример ошибки:

class Student:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def __eq__(self, other):
        return self.name == other.name  # Ошибка: сравнивается только имя, возраст игнорируется.

Решение:

Сравнивайте все значимые атрибуты объекта.

class Student:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def __eq__(self, other):
        return self.name == other.name and self.age == other.age









Тип ошибки 5: Метод _len_ возвращает неправильное значение

Возникает, когда метод _len_ возвращает нецелое число.

Пример ошибки:

class Group:
    def __init__(self, members):
        self.members = members

    def __len__(self):
        return len(self.members) / 2  # Ошибка: возвращается дробное значение.

Решение:

Метод _len_ должен возвращать целое число.

class Group:
    def __init__(self, members):
        self.members = members

    def __len__(self):
        return len(self.members)







Тип ошибки 6: Несоответствие между _repr_ и _init_

Появляется, когда метод _repr_ возвращает строку, которую невозможно использовать для создания аналогичного объекта.

Пример ошибки:

class Student:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def __repr__(self):
        return f"Student({self.name}, {self.age})"  # Ошибка: пропущены кавычки вокруг имени.

Решение:

_repr_ должен возвращать строку, которая позволяет создать аналогичный объект.

class Student:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def __repr__(self):
        return f"Student('{self.name}', {self.age})"









Практикум
Задача 1: Калькулятор векторов

Ситуация: мы разрабатываем инструмент для работы с векторами в двумерной плоскости.
Он должен уметь складывать, вычитать векторы, а также отображать их длину и угол относительно оси X.

Задача: создайте класс Vector2D, который:

Имеет атрибуты:
x — координата по оси X.
y — координата по оси Y.
Использует следующие методы:
_add_: складывает два вектора.
_sub_: вычитает один вектор из другого.
_str_: возвращает строковое представление вектора.
_len_: возвращает длину вектора (округлённую до ближайшего целого числа).
length: вычисляет длину вектора.
angle: вычисляет угол вектора относительно оси X.
Шаги реализации:

Создайте класс Vector2D с атрибутами x и y.
Класс Vector2D будет представлять двумерный вектор.
Атрибуты x и y будут задавать координаты вектора.
Конструктор принимает эти координаты как параметры.

import math

class Vector2D:
    def __init__(self, x, y):
        self.x = x  # Координата x
        self.y = y  # Координата y

Реализуйте метод _add_ для сложения векторов.
Метод _add_ позволяет складывать два вектора.
Сложение происходит покоординатно: складываются x и y-координаты двух векторов.

def __add__(self, other):
    return Vector2D(self.x + other.x, self.y + other.y)

Реализуйте метод _sub_ для вычитания векторов.
Метод _sub_ вычитает один вектор из другого. Это тоже покоординатная операция.

def __sub__(self, other):
    return Vector2D(self.x - other.x, self.y - other.y)


Реализуйте метод _str_ для удобного отображения вектора.
Метод _str_ возвращает строковое представление вектора в виде (x, y).

def __str__(self):
    return f"({self.x}, {self.y})"


Реализуйте метод _len_, чтобы возвращать округлённую длину.
Метод _len_ вычисляет длину вектора (расстояние от начала координат до точки вектора) и возвращает её округлённое значение.

def __len__(self):
    return round(math.sqrt(self.x**2 + self.y**2))


Добавьте метод length для вычисления точной длины вектора.
Метод length возвращает длину вектора с высокой точностью, без округления.

def length(self):
    return math.sqrt(self.x**2 + self.y**2)


Добавьте метод angle для вычисления угла.
Метод angle вычисляет угол вектора относительно оси x в градусах. Для этого используется функция math.atan2.

def angle(self):
    return math.degrees(math.atan2(self.y, self.x))



Реализация:

import math

class Vector2D:
    def __init__(self, x, y):
        self.x = x
        self.y = y

    def __add__(self, other):
        return Vector2D(self.x + other.x, self.y + other.y)

    def __sub__(self, other):
        return Vector2D(self.x - other.x, self.y - other.y)

    def __str__(self):
        return f"Vector({self.x}, {self.y})"

    def __len__(self):
        return round(self.length())

    def length(self):
        return math.sqrt(self.x**2 + self.y**2)

    def angle(self):
        return math.degrees(math.atan2(self.y, self.x))

# Пример использования
v1 = Vector2D(3, 4)
v2 = Vector2D(1, 2)

print(v1 + v2)  # Вывод: Vector(4, 6)
print(v1 - v2)  # Вывод: Vector(2, 2)
print(len(v1))  # Вывод: 5
print(f"Длина: {v1.length():.2f}")  # Длина: 5.00
print(f"Угол: {v1.angle():.2f}")   # Угол: 53.13


Ожидаемый результат:

v1 + v2 вернёт Vector(4, 6).
len(v1) вернёт округлённую длину 5.
v1.angle() вернёт угол вектора относительно оси X, например 53.13°.
Задача 2: Виртуальная корзина покупок








Ситуация: мы создаем программу для интернет-магазина.
Нам нужно реализовать класс для корзины покупок, которая позволяет добавлять товары,
удалять их и подсчитывать общую стоимость.
Также пользователи должны видеть, что находится в корзине, в удобочитаемом виде.

Задача: создайте класс Cart, который:

Имеет атрибуты:
items — список товаров в корзине (по умолчанию пустой).
каждый товар — это словарь с ключами name (название), price (цена) и quantity (количество).
Использует следующие методы:
_str_: возвращает строку с перечнем товаров в корзине.
add_item: добавляет товар в корзину.
remove_item: удаляет товар из корзины.
_len_: возвращает количество уникальных товаров в корзине.
_add_: объединяет две корзины, возвращая новую.
Шаги реализации:

Создайте класс Cart с атрибутом items, который будет списком.
Атрибут items будет хранить список товаров,
где каждый товар представлен как словарь с ключами: name, price и quantity. Этот список будет изначально пустым.

class Cart:
    def __init__(self):
        self.items = []  # Список для хранения товаров


Реализуйте метод _str_ для удобного отображения содержимого корзины.
Метод _str_ должен возвращать строку, содержащую перечень товаров в корзине. Если корзина пуста, метод возвращает сообщение «Корзина пуста.».

def __str__(self):
    if not self.items:
        return "Корзина пуста."
    result = "Корзина:\n"
    for item in self.items:
        result += f"- {item['name']}: {item['quantity']} шт. по {item['price']} руб.\n"
    return result


Добавьте метод add_item для добавления товара.
Метод add_item добавляет новый товар в корзину, передавая название, цену и количество. Товар добавляется в список items в виде словаря.

def add_item(self, name, price, quantity=1):
    self.items.append({"name": name, "price": price, "quantity": quantity})


Добавьте метод remove_item для удаления товара.
Метод remove_item удаляет товар с заданным названием из корзины.
Для этого создаётся новый список, в котором остаются только товары, название которых не совпадает с указанным.

def remove_item(self, name):
    self.items = [item for item in self.items if item['name'] != name]



Реализуйте метод _len_, который возвращает количество уникальных товаров.
Этот метод возвращает длину списка items, то есть количество различных товаров в корзине.

def __len__(self):
    return len(self.items)



Реализуйте метод _add_, который объединяет две корзины.
Метод _add_ создаёт новую корзину, содержащую товары из обеих корзин. Для этого объединяются списки items двух объектов Cart.

def __add__(self, other):
    new_cart = Cart()
    new_cart.items = self.items + other.items
    return new_cart







Реализация:

class Cart:
    def __init__(self):
        self.items = []  # Пустая корзина

    def __str__(self):
        if not self.items:
            return "Корзина пуста."
        result = "Корзина:\n"
        for item in self.items:
            result += f"- {item['name']}: {item['quantity']} шт. по {item['price']} руб.\n"
        return result

    def add_item(self, name, price, quantity=1):
        self.items.append({"name": name, "price": price, "quantity": quantity})

    def remove_item(self, name):
        self.items = [item for item in self.items if item['name'] != name]

    def __len__(self):
        return len(self.items)

    def __add__(self, other):
        new_cart = Cart()
        new_cart.items = self.items + other.items
        return new_cart

# Пример использования
cart1 = Cart()
cart1.add_item("Яблоки", 50, 2)
cart1.add_item("Молоко", 80, 1)
cart2 = Cart()
cart2.add_item("Хлеб", 40, 1)

print(cart1)  # Выводит содержимое первой корзины
print(len(cart1))  # Вывод: 2
combined_cart = cart1 + cart2
print(combined_cart)  # Объединённая корзина



Ожидаемый результат:

cart1 будет содержать «Яблоки» и «Молоко».
len(cart1) вернёт 2.
Объединённая корзина покажет товары из обеих корзин.