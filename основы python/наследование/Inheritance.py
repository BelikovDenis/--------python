На этом уроке мы познакомимся с четырьмя ключевыми принципами объектно-ориентированного программирования (ООП):
наследованием, инкапсуляцией, полиморфизмом и абстракцией.


Чаще всего выделяют три главных принципа ООП: наследование, инкапсуляцию и полиморфизм.
Но иногда добавляют четвёртый принцип — абстракцию,
потому что она помогает упростить сложные вещи, сосредотачиваясь только на самом важном.


Эти концепции делают наш код более удобным для чтения, поддержки и повторного использования.

Представим, что мы создаем компьютерную игру.
У нас есть герои, враги, транспорт, здания и многое другое.

Чтобы не писать код для каждого объекта с нуля, мы можем использовать принципы ООП:


наследование позволяет создать базовый класс (например, «Персонаж»)
и на его основе определить новые классы (например, «Герой» и «Враг»).

инкапсуляция помогает «спрятать» внутренние данные объекта и позволить доступ только через специально созданные методы.

полиморфизм позволяет одинаково работать с объектами разных классов,
если они имеют общие методы.

абстракция помогает сосредоточиться только на ключевых аспектах объекта,
игнорируя несущественные детали.

Эти принципы не только упрощают процесс разработки, но и делают наш код модульным,
гибким и понятным для других разработчиков.



На этом занятии мы:

узнаем, что такое наследование и как его использовать для создания новых классов.
поймем, как защитить внутренние данные объекта с помощью инкапсуляции.
разберемся, что такое полиморфизм и как использовать его для работы с разными типами объектов.
узнаем, как абстракция помогает скрыть детали реализации и облегчает проектирование сложных систем.
применим принципы объектно-ориентированного программирования (ООП) на практике для создания реальных проектов.
К концу занятия мы сможем применять основные принципы ООП в своих проектах, чтобы писать удобный, читаемый и масштабируемый код.



Наследование (Inheritance)

Наследование позволяет одному классу (наследнику) использовать свойства и методы другого класса (родителя).

Это похоже на передачу генов от родителей к детям.
Если у родителя есть какая-то способность (например, ходить),
то ребёнок автоматически наследует её, но при необходимости может изменить её под свои нужды.

Пример:

У нас есть класс Animal, который описывает всех животных.
Мы можем создать дочерние классы Dog и Cat,
которые унаследуют всё от класса Animal, но добавят свои особенности.

class Animal:
    def __init__(self, name):
        self.name = name

    def speak(self):
        return "Я животное."

class Dog(Animal):
    def speak(self):
        return "Гав-гав!"

class Cat(Animal):
    def speak(self):
        return "Мяу-мяу!"

# Пример использования
dog = Dog("Шарик")
print(dog.name)  # Вывод: Шарик
print(dog.speak())  # Вывод: Гав-гав!

cat = Cat("Мурзик")
print(cat.name)  # Вывод: Мурзик
print(cat.speak())  # Вывод: Мяу-мяу!

Здесь класс Dog унаследовал от Animal атрибут name и метод speak,
но переопределил метод speak. И абсолютно также ведет себя класс Cat,
но там метод speak переопределен по-своему.


Рисунок 1
Когда нужно использовать наследование?

Когда классы имеют общие черты.
Чтобы избежать дублирования кода.
Чтобы упростить расширение функционала программы.







Инкапсуляция (Encapsulation)

Инкапсуляция — это сокрытие внутренней работы объекта.

Представим мобильный телефон: мы можем звонить или отправлять сообщения,
но не видим, как внутри работают его схемы.

Также и в программировании:
инкапсуляция защищает данные объекта и управляет доступом к ним через методы.

Как это работает?

Публичные атрибуты: доступны всем.
Защищённые атрибуты: начинаются с одного подчёркивания (_).
Предполагаются для использования внутри класса или его наследников.
Приватные атрибуты: начинаются с двойного подчёркивания (__).
Полностью скрыты от внешнего доступа.
Пример:

class BankAccount:
    def __init__(self, owner, balance):
        self.owner = owner  # Публичный атрибут
        self._account_number = "12345"  # Защищённый атрибут
        self.__balance = balance  # Приватный атрибут

    def get_balance(self):
        return self.__balance

    def deposit(self, amount):
        if amount > 0:
            self.__balance += amount
        return self.__balance

# Пример использования
account = BankAccount("Алиса", 1000)
print(account.owner)  # Вывод: Алиса
print(account.get_balance())  # Вывод: 1000
account.deposit(500)
print(account.get_balance())  # Вывод: 1500

Важно помнить:

В Python атрибуты с одним подчёркиванием (_protected) не запрещены для прямого доступа.
Это лишь соглашение, которое говорит программисту: «Не изменяйте это без необходимости!».

Это связано с философией Python — «Мы все взрослые и пишем осмысленный код»,
что делает язык гибким, но требует ответственности.




Полиморфизм (Polymorphism)

Полиморфизм позволяет объектам разных классов использовать методы с одинаковым названием,
но с разным поведением.
Это как одно и то же слово, которое может иметь разное значение в зависимости от контекста.

Пример:

Метод speak у разных классов может делать разные вещи.

class Cat:
    def speak(self):
        return "Мяу!"

class Dog:
    def speak(self):
        return "Гав-гав!"

# Пример использования
animals = [Cat(), Dog()]
for animal in animals:
    print(animal.speak())
# Вывод:
# Мяу!
# Гав-гав!




Абстракция (Abstraction)

Абстракция — это процесс выделения только важных характеристик объекта и скрытия ненужных деталей.
Она помогает сосредоточиться на том, что объект должен делать, а не на том, как он это делает.

Пример:

Представьте абстрактный класс Shape (Фигура),
у которого есть метод get_area (найти площадь).
Мы не знаем, как считать площадь круга или прямоугольника,
но знаем, что каждый класс-фигура должен уметь её считать.

from abc import ABC, abstractmethod

class Shape(ABC):  # Абстрактный класс
    @abstractmethod
    def get_area(self):
        pass

class Circle(Shape):
    def __init__(self, radius):
        self.radius = radius

    def get_area(self):
        return 3.14 * self.radius ** 2

class Rectangle(Shape):
    def __init__(self, width, height):
        self.width = width
        self.height = height

    def get_area(self):
        return self.width * self.height

# Пример использования
circle = Circle(5)
rectangle = Rectangle(4, 6)

print(circle.get_area())  # Вывод: 78.5
print(rectangle.get_area())  # Вывод: 24




Разница между наследованием, инкапсуляцией, полиморфизмом и абстракцией

Наследование помогает использовать код повторно.

Инкапсуляция защищает данные и упрощает доступ к ним.

Полиморфизм позволяет использовать один и тот же метод для разных объектов.

Абстракция скрывает детали реализации, позволяя сосредоточиться на функционале.

Использование этих концепций делает программы:

Читаемыми
Код становится понятнее, так как каждая часть отвечает за свою задачу.

Гибкими
Легко добавлять новые функции или изменять существующие.

Удобными
Мы можем использовать код повторно и не писать его заново.

Теперь, когда мы разобрались в этих понятиях, самое время применить их на практике!






Типичные ошибки
Тип ошибки 1: Неправильное использование super() в наследовании

Ошибка возникает, если забыть вызвать super()._init_() при переопределении конструктора в дочернем классе.
Это приведёт к тому, что родительский класс не инициализируется.

Пример ошибки:

class Animal:
    def __init__(self, name):
        self.name = name

class Dog(Animal):
    def __init__(self, name, breed):
        # Ошибка: Не вызван super().__init__(name)
        self.breed = breed

dog = Dog("Шарик", "Овчарка")
print(dog.name)  # AttributeError: 'Dog' object has no attribute 'name'

Решение:

Не забывайте вызывать конструктор родительского класса.

class Dog(Animal):
    def __init__(self, name, breed):
        super().__init__(name)
        self.breed = breed





Тип ошибки 2: Доступ к защищенным или приватным атрибутам

Ошибка возникает только при попытке получить доступ к приватным атрибутам (__private) за пределами класса,
так как Python вызывает AttributeError.
Однако для защищенных атрибутов (_protected) ошибки не будет,
но их использование за пределами класса или его наследников нарушает принцип инкапсуляции и считается плохой практикой.

Пример ошибки:

class BankAccount:
    def __init__(self, owner, balance):
        self._account_number = "12345"  # Защищенный атрибут
        self.__balance = balance  # Приватный атрибут

account = BankAccount("Алиса", 1000)
print(account.__balance)  # AttributeError: 'BankAccount' object has no attribute '__balance'

Решение:

Используйте методы (геттеры и сеттеры) для доступа к данным.

class BankAccount:
    def get_balance(self):
        return self.__balance

account = BankAccount("Алиса", 1000)
print(account.get_balance())  # Вывод: 1000

Важно:

В Python защищенные атрибуты (_protected) не запрещены для доступа,
и программа не выдаст ошибку, если мы обратимся к ним напрямую.
Однако это считается плохой практикой, так как нарушает принцип инкапсуляции.
Используйте защищенные атрибуты (_protected) только внутри класса или его наследников 
и избегайте их прямого изменения за пределами этих областей.


С приватными атрибутами (__private) Python действительно вызывает ошибку AttributeError,
если попытаться к ним обратиться напрямую, но это защита, которую стоит учитывать.







Тип ошибки 3: Ошибки при переопределении метода

Ошибка возникает не из-за синтаксической ошибки, а из-за нарушения логики программы,
когда метод родительского класса не вызывается в дочернем классе,
хотя это может быть необходимо.
Это приводит к неполному выполнению ожидаемого поведения.

Пример ошибки:

class Animal:
    def speak(self):
        print("Я животное.")

class Dog(Animal):
    def speak(self):
        print("Гав-гав!")
        # Не вызывается родительский метод

dog = Dog()
dog.speak()  # Вывод: Гав-гав!


Решение:

Если нужно, вызывайте метод родителя через super().

class Dog(Animal):
    def speak(self):
        super().speak()  # Вызываем метод родителя
        print("Гав-гав!")







Тип ошибки 4: Отсутствие реализации абстрактного метода

Ошибка возникает, когда класс наследуется от абстрактного класса,
но не реализует все его абстрактные методы.
Такой класс сам становится абстрактным и не может быть инициализирован.

Пример ошибки:

from abc import ABC, abstractmethod

class Shape(ABC):
    @abstractmethod
    def area(self):
        pass

class Circle(Shape):
    pass  # Ошибка: метод area не реализован

circle = Circle()  # TypeError: Can't instantiate abstract class Circle




Решение:

Обязательно реализуйте все абстрактные методы в дочернем классе.

class Circle(Shape):
    def area(self):
        return 3.14 * 5 ** 2








Тип ошибки 5: Полиморфизм без учёта поведения объектов

Ошибка возникает,
когда методы с одинаковым названием в разных классах имеют разные параметры или ожидаемое поведение.
Это нарушает принцип полиморфизма и может привести к непредсказуемым ошибкам.

Пример ошибки:

class Cat:
    def speak(self):
        return "Мяу!"

class Dog:
    def speak(self, times):  # Требует параметр
        return "Гав!" * times

animals = [Cat(), Dog()]
for animal in animals:
    print(animal.speak())  # Ошибка: speak() для Dog требует аргумент

Решение:

Убедитесь, что методы с одинаковыми названиями имеют одинаковый интерфейс (параметры и возвращаемое значение).




Тип ошибки 6: Проблемы с доступом к полям класса

Ошибка возникает, когда поле класса используется как поле экземпляра.
Это приводит к созданию локальной копии поля в экземпляре,
вместо изменения общего значения для всех экземпляров.

Пример ошибки:

class Counter:
    count = 0

    def increment(self):
        self.count += 1  # Ошибка: создаётся локальная копия count

counter = Counter()
counter.increment()
print(Counter.count)  # Вывод: 0 (глобальное поле не изменилось)





Решение:

Используйте методы класса, чтобы изменять поля класса.

class Counter:
    count = 0

    @classmethod
    def increment(cls):
        cls.count += 1






Практикум
Задача 1: Учет сотрудников с наследованием и инкапсуляцией

Ситуация: мы ведем базу данных сотрудников компании.
У каждого сотрудника есть имя, фамилия и базовая зарплата.
Помимо этого, у менеджеров есть бонусы, а у стажеров — сниженная ставка оплаты.
Необходимо использовать наследование и инкапсуляцию, чтобы организовать эту систему.

Задача: создать классы:

Employee (Базовый класс), который содержит:
Приватные поля: first_name, last_name, salary.
Метод для получения полной информации о сотруднике.
Метод для получения зарплаты.

Manager (Класс-наследник от Employee), который:
Имеет дополнительное поле bonus.
Переопределяет метод получения зарплаты, учитывая бонус.

Intern (Класс-наследник от Employee), который:
Имеет дополнительное поле discount_rate (например, 0.5 для 50%).
Переопределяет метод получения зарплаты, учитывая скидку.
Программа должна:

Создавать объекты разных типов сотрудников.
Выводить информацию о них, включая имя, фамилию и итоговую зарплату.
Шаги реализации:

Создайте базовый класс Employee с приватными полями и методами для получения данных.
class Employee:
    def __init__(self, first_name, last_name, salary):
        self.__first_name = first_name
        self.__last_name = last_name
        self.__salary = salary

    def get_info(self):
        return f"{self.__first_name} {self.__last_name}"

    def get_salary(self):
        return self.__salary


Реализуйте класс Manager, добавив поле bonus и изменив метод получения зарплаты.
class Manager(Employee):
    def __init__(self, first_name, last_name, salary, bonus):
        super().__init__(first_name, last_name, salary)
        self.__bonus = bonus

    def get_salary(self):
        return super().get_salary() + self.__bonus


Реализуйте класс Intern, добавив поле discount_rate и изменив метод получения зарплаты.
class Intern(Employee):
    def __init__(self, first_name, last_name, salary, discount_rate):
        super().__init__(first_name, last_name, salary)
        self.__discount_rate = discount_rate

    def get_salary(self):
        return super().get_salary() * self.__discount_rate


В основной программе создайте список сотрудников и выведите их данные.
employees = [
    Employee("Иван", "Иванов", 50000),
    Manager("Мария", "Петрова", 80000, 20000),
    Intern("Алексей", "Смирнов", 30000, 0.5)
]

for emp in employees:
    print(f"{emp.get_info()} - Зарплата: {emp.get_salary()}")




Реализация:

class Employee:
    """Базовый класс сотрудника."""
    def __init__(self, first_name, last_name, salary):
        self.__first_name = first_name  # Приватное поле
        self.__last_name = last_name    # Приватное поле
        self.__salary = salary          # Приватное поле

    def get_info(self):
        """Возвращает информацию о сотруднике."""
        return f"{self.__first_name} {self.__last_name}"

    def get_salary(self):
        """Возвращает базовую зарплату."""
        return self.__salary


class Manager(Employee):
    """Класс для менеджера, наследуется от Employee."""
    def __init__(self, first_name, last_name, salary, bonus):
        super().__init__(first_name, last_name, salary)
        self.__bonus = bonus  # Приватное поле для бонуса

    def get_salary(self):
        """Возвращает зарплату с учетом бонуса."""
        return super().get_salary() + self.__bonus


class Intern(Employee):
    """Класс для стажера, наследуется от Employee."""
    def __init__(self, first_name, last_name, salary, discount_rate):
        super().__init__(first_name, last_name, salary)
        self.__discount_rate = discount_rate  # Приватное поле для скидки

    def get_salary(self):
        """Возвращает зарплату с учетом скидки."""
        return super().get_salary() * self.__discount_rate


# Основная программа
employees = [
    Employee("Иван", "Иванов", 50000),
    Manager("Мария", "Петрова", 80000, 20000),
    Intern("Алексей", "Смирнов", 30000, 0.5)
]

for emp in employees:
    print(f"{emp.get_info()} - Зарплата: {emp.get_salary()}")