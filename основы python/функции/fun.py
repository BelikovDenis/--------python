На этом занятии мы исследуем, 
как функции и модули облегчают процесс программирования в Python. 
Функции можно представить как набор инструкций,
которые мы сохраняем под определенным именем и можем вызывать многократно, 
когда это нужно. Это как если бы мы записывали рецепт приготовления блюда, 
чтобы каждый раз не искать его снова. Модули же — это целые сборники таких рецептов.

Они позволяют организовать функции и переменные в удобные пакеты, 
чтобы использовать их в различных программах без копирования кода. 
Вместе они существенно повышают удобство и эффективность написания кода, 
делая его легко управляемым и переиспользуемым.

Мы освоим создание функций для оптимизации и структурирования программного кода:

узнаем, как создавать и использовать функции для улучшения структуры кода, делая его более доступным и удобным для внесения изменений.
освоим работу с параметрами и возвращение значений, чтобы точно управлять потоком данных внутри функций, что улучшит функциональность программ.
научимся использовать модули для добавления готовых решений в проекты, что поможет существенно экономить время и ресурсы.
изучим, как разделять крупные задачи на мелкие управляемые блоки, что облегчит решение сложных задач и улучшит управление проектами.
Эти навыки помогут не только повысить эффективность своего программирования, но и значительно улучшить его качество.
Мы научимся создавать более чистый, структурированный и масштабируемый код,
который можно легко поддерживать и расширять. 
Эти умения открывают двери к разработке сложных приложений и подготовят к решению любых задач в мире программирования.


Функции и модули — это основа структурированного программирования. 
Они помогают разделить код на логические блоки, упрощают чтение, повторное использование и поддержку.

Функции: зачем они нужны?

Функция — это набор инструкций, выполняющих одну задачу.
Вместо того чтобы писать один и тот же код много раз, мы можем определить функцию один раз и вызывать её при необходимости.

Пример из жизни: функция похожа на инструкцию «поставь чайник».
Эта инструкция объединяет несколько действий: налить воду, включить чайник,
дождаться кипения. Нам не нужно каждый раз описывать весь процесс — мы просто говорим: «Поставь чайник».

Почему это полезно?

Функции позволяют сократить количество повторений в коде.
Они делают код понятным и читаемым. Благодаря функциям, мы можем легко изменить одну часть программы, не трогая остальной код.

Пример в Python:

def boil_water():
    print("Налить воду")
    print("Включить чайник")
    print("Дождаться кипения")
Теперь мы можем вызвать эту функцию столько раз, сколько нужно:

boil_water()
Как создать функцию?

Функция определяется с помощью ключевого слова def,
за которым следует имя функции, скобки (где будут указаны аргументы, если они есть),
двоеточие, а затем — тело функции.

def greet(name):
    print(f"Привет, {name}!")
Аргументы функций

Функции могут принимать входные данные — аргументы, которые помогают сделать функцию более гибкой.

Пример:

def greet(name):
    print(f"Привет, {name}!")
Мы можем передать имя в функцию:

greet("Алиса")  # Вывод: Привет, Алиса!

Позиционные и именованные аргументы

Позиционные аргументы передаются в том порядке, в котором они указаны в функции.
Именованные аргументы позволяют явно указать, какой аргумент чему соответствует:

def introduce(name, age):
    print(f"Меня зовут {name}, мне {age} лет.")

introduce(name="Алиса", age=25)  # Вывод: Меня зовут Алиса, мне 25 лет.

Необязательные аргументы

Мы можем задать значения по умолчанию для аргументов:

def greet(name="гость"):
    print(f"Привет, {name}!")

greet()  # Вывод: Привет, гость!
greet("Алиса")  # Вывод: Привет, Алиса!


Возврат значений (return)

Функции могут возвращать результаты с помощью ключевого слова return. 
Это важно, так как позволяет использовать результат одной функции в другой.

Пример:

def add(a, b):
    return a + b
Теперь мы можем сохранить результат и использовать его:

result = add(3, 5)
print(result)  # Вывод: 8


Зачем нужен return?

Представьте, что функции — это кирпичи, из которых строится дом.
Каждый кирпич выполняет свою задачу и передаёт результат следующему кирпичу.
Без return функции не могут передавать друг другу информацию, а значит, не могут быть связаны в одну цепочку.

Функции похожи на магические заклинания. Мы однажды написали сложное заклинание,
потратили время на его создание, но теперь мы можем использовать его где угодно и сколько угодно раз. Это экономит время.

Правильный нейминг функций

Очень важно давать функциям понятные и описательные названия. Имя функции должно сразу говорить, что она делает.

Плохой пример:
def f(x):
    return x * x


Хороший пример:
def square_number(number):
    return number * number


Модули: зачем они нужны?

Модуль — это файл с функциями, который можно подключить к другим программам. 
Это как если бы мы могли использовать библиотеку готовых заклинаний вместо написания всех функций с нуля.

Примеры встроенных модулей

math — для математических вычислений.
os — для работы с файловой системой.
random — для генерации случайных чисел.
Пример использования:

import math
print(math.sqrt(16))  # Вывод: 4.0

Внешние модули

Помимо встроенных, есть множество внешних модулей, которые нужно устанавливать отдельно. 
Они расширяют возможности Python и могут быть скачаны через pip (менеджер пакетов Python).

Как установить модуль:

pip install requests
После этого можно использовать модуль в своём проекте:

import requests
response = requests.get("https://api.github.com")
print(response.status_code)  # Вывод: 200
Важно о версиях модулей

Модули часто обновляются. При создании сложных проектов важно фиксировать версии модулей,
чтобы проект работал стабильно, даже если модули обновятся.
Позже мы научимся это делать автоматически с помощью файла зависимостей.

Создание своих модулей

Мы можем вынести свой код в отдельный файл, чтобы использовать его в других программах.

Создайте файл utilities.py:
def greet(name):
    return f"Привет, {name}!"
Подключите модуль в основной программе:
from utilities import greet
print(greet("Алиса"))  # Вывод: Привет, Алиса!


Лапша кода превращается в модули

Если у нас есть длинный код с множеством функций,
лучше вынести их в отдельный модуль. Это сделает наш проект структурированным и понятным.

Структура пакета

Пакеты позволяют объединять модули в одну директорию для более удобного управления.

mypackage/
    __init__.py
    module1.py
    module2.py


Функции и модули помогают:

Делать код структурированным.
Повторно использовать функции в разных частях программы.
Упрощать работу над проектами, разделяя код на логические блоки.




Типичные ошибки

Тип ошибки 1: Вызов функции до её определения — NameError: name ‘greet’ is not defined

Ошибка возникает, если вызвать функцию до ее определения.

Пример ошибки:

greet("Alice")

def greet(name):
    print(f"Hello, {name}!")

Решение:

Всегда определяйте функции перед их вызовом.

def greet(name):
    print(f"Hello, {name}!")

greet("Alice")





Тип ошибки 2: Неправильное количество аргументов — TypeError: add() missing 1 required positional argument

Пример ошибки:

def add(a, b):
    return a + b

result = add(5)  # Пропущен второй аргумент
Решение:

Убедитесь, что передаёте все обязательные аргументы, или используйте значения по умолчанию.

def add(a, b=0):
    return a + b

result = add(5)  # Работает: 5 + 0 = 5






Тип ошибки 3: Использование несуществующего модуля — ModuleNotFoundError: No module named ‘non_existent_module’

Пример ошибки:

import non_existent_module
Решение:

Проверьте название модуля и установите его, если он отсутствует.

pip install module_name






Тип ошибки 4: Переопределение импортированной функции — TypeError: ‘str’ object is not callable

Пример ошибки:

from math import sqrt
sqrt = "Not a function"  # Переопределение имени функции
print(sqrt(16))
Решение:

Не используйте имена встроенных или импортированных функций для переменных.

from math import sqrt
print(sqrt(16))  # Работает корректно





Тип ошибки 5: Неправильный импорт функции — ImportError: cannot import name ‘calculate’ from ‘math’

Пример ошибки:

from math import calculate
Решение:

Проверьте, существует ли функция или модуль, который пытаетесь импортировать.
Для импорта встроенных модулей используйте только их доступные функции.

from math import sqrt
print(sqrt(16))  # Работает





Тип ошибки 6: Попытка изменить кортеж — TypeError: ‘tuple’ object does not support item assignment

Пример ошибки:

my_tuple = (1, 2, 3)
my_tuple[0] = 10  # Ошибка
Решение:

Кортежи неизменяемы. Если нужно внести изменения, преобразуйте их в список, а затем обратно в кортеж.

my_tuple = (1, 2, 3)
temp_list = list(my_tuple)
temp_list[0] = 10
my_tuple = tuple(temp_list)
print(my_tuple)  # (10, 2, 3)







Практикум
Задача 1: Создание калькулятора для математических операций

Ситуация: мы решили разработать простую программу для выполнения базовых математических операций,
чтобы использовать её для проверки своих расчетов. 
Программа должна быть удобной, модульной и поддерживать основные арифметические действия.

Задача: написать функцию calculator, которая принимает два числа и оператор 
(add, subtract, multiply, divide) и возвращает результат соответствующей операции.
Затем подключите эту функцию из модуля и реализуйте взаимодействие с пользователем.

Шаги реализации:

Создайте модуль math_operations.py с функцией calculator.
# файл math_operations.py
def calculator(a, b, operation):
Определите внутри функции обработку четырёх операций и проверку деления на ноль.
def calculator(a, b, operation):
    if operation == 'add':
        return a + b
    elif operation == 'subtract':
        return a - b
    elif operation == 'multiply':
        return a * b
    elif operation == 'divide':
        if b == 0:
            return "Ошибка: деление на ноль."
        return a / b
    else:
        return "Ошибка: некорректная операция."
Импортируйте модуль в основной скрипт и реализуйте ввод данных пользователем.
from math_operations import calculator

while True:
    print("\nПростой калькулятор:")
    try:
        a = float(input("Введите первое число: "))
        b = float(input("Введите второе число: "))
        operation = input("Введите операцию (add, subtract, multiply, divide): ").strip().lower()

        result = calculator(a, b, operation)
        print(f"Результат: {result}")
    except ValueError:
        print("Ошибка: введите корректные числа.")

    if input("Хотите продолжить? (yes/no): ").strip().lower() != "yes":
        break
Реализация модуля (math_operations.py):

def calculator(a, b, operation):
    if operation == 'add':
        return a + b
    elif operation == 'subtract':
        return a - b
    elif operation == 'multiply':
        return a * b
    elif operation == 'divide':
        if b == 0:
            return "Ошибка: деление на ноль."
        return a / b
    else:
        return "Ошибка: некорректная операция."
Реализация основной программы:

from math_operations import calculator

while True:
    print("\nПростой калькулятор:")
    try:
        a = float(input("Введите первое число: "))
        b = float(input("Введите второе число: "))
        operation = input("Введите операцию (add, subtract, multiply, divide): ").strip().lower()

        result = calculator(a, b, operation)
        print(f"Результат: {result}")
    except ValueError:
        print("Ошибка: введите корректные числа.")

    if input("Хотите продолжить? (yes/no): ").strip().lower() != "yes":
        break







Задача 2: Создание программы для управления списком книг

Ситуация: мы хотим создать программу для работы с книгами в библиотеке.
Каждая книга имеет название и автора.
Программа должна предоставлять функционал для добавления новых книг,
удаления их по названию и вывода текущего списка книг.

Задача: написать функцию library_manager, которая предоставляет следующие возможности:

Добавление книги.
Удаление книги по названию.
Просмотр текущего списка книг.
Создайте модуль library.py с функциями для этих операций,
а затем реализуйте основную программу, которая будет использовать этот модуль.

Шаги реализации:

Создание модуля library.py с функциями add_book, remove_book, list_books.
# файл library.py
def add_book(library, title, author):
    pass

def remove_book(library, title):
    pass

def list_books(library):
    pass
Реализация функций add_book, remove_book, list_books.
def add_book(library, title, author):
    if title in library:
        return "Книга уже существует."
    library[title] = author
    return "Книга добавлена."

def remove_book(library, title):
    if title in library:
        del library[title]
        return "Книга удалена."
    return "Книга не найдена."

def list_books(library):
    if not library:
        return "Библиотека пуста."
    return "\n".join([f"{title} — {author}" for title, author in library.items()])






Реализация основной программы, которая использует словарь для хранения данных о книгах.
Импортируем модуль library и в цикле реализовываем команды для управления библиотекой.
from library import add_book, remove_book, list_books

library = {}

while True:
    command = input("Введите команду (add, remove, list, exit): ").strip().lower()

    if command == "add":
        title = input("Введите название книги: ").strip()
        author = input("Введите автора книги: ").strip()
        print(add_book(library, title, author))

    elif command == "remove":
        title = input("Введите название книги для удаления: ").strip()
        print(remove_book(library, title))

    elif command == "list":
        print("Список книг:")
        print(list_books(library))

    elif command == "exit":
        print("Выход из программы.")
        break

    else:
        print("Неизвестная команда.")
Реализация модуля (library.py):

def add_book(library, title, author):
    if title in library:
        return "Книга уже существует."
    library[title] = author
    return "Книга добавлена."

def remove_book(library, title):
    if title in library:
        del library[title]
        return "Книга удалена."
    return "Книга не найдена."

def list_books(library):
    if not library:
        return "Библиотека пуста."
    return "\n".join([f"{title} — {author}" for title, author in library.items()])











Реализация основной программы:

from library import add_book, remove_book, list_books

library = {}

while True:
    command = input("Введите команду (add, remove, list, exit): ").strip().lower()

    if command == "add":
        title = input("Введите название книги: ").strip()
        author = input("Введите автора книги: ").strip()
        print(add_book(library, title, author))

    elif command == "remove":
        title = input("Введите название книги для удаления: ").strip()
        print(remove_book(library, title))

    elif command == "list":
        print("Список книг:")
        print(list_books(library))

    elif command == "exit":
        print("Выход из программы.")
        break

    else:
        print("Неизвестная команда.")









Итоги
На этом занятии мы познакомились с функциями и модулями,
двумя важнейшими инструментами в Python,
которые делают код более удобным для чтения, тестирования и повторного использования:

научились создавать функции, использовать аргументы и параметры.
применять возврат значений.
использовать встроенные модули и работать с внешними модулями.
создавать собственные модули.
поняли, что такое структура пакета.