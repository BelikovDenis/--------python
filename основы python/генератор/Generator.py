"""
Генератор
Это особый вид функции, который создаёт итератор.
Вместо ключевого слова return в генераторе используется yield.
Это позволяет «заморозить» выполнение функции,
сохранить текущее состояние и вернуться к нему позже.


Простой пример генератора:

def my_generator():
    yield 1
    yield 2
    yield 3

gen = my_generator()
print(next(gen))  # Вывод: 1
print(next(gen))  # Вывод: 2
print(next(gen))  # Вывод: 3


Генераторы работают так же, как итераторы,
но их создание проще: не нужно вручную управлять состоянием
(переменными, где мы находимся в последовательности).
Python делает это автоматически.
"""













"""
Почему генераторы полезны

Генераторы создают элементы «на лету».
Это особенно полезно, если мы работаем с:
бесконечными последовательностями;
игромными наборами данных.
Пример:

def count_up_to(limit):
    count = 1
    while count <= limit:
        yield count
        count += 1

for number in count_up_to(5):
    print(number)  # Вывод: 1 2 3 4 5
"""










"""
Пример того, как генератор экономит память:

import sys

# Сравнение списка и генератора
numbers_list = [x**2 for x in range(1000)]  # Список квадратов чисел
numbers_gen = (x**2 for x in range(1000))  # Генератор квадратов чисел

print("Размер списка:", sys.getsizeof(numbers_list), "байт")  # Например: 9024 байта
print("Размер генератора:", sys.getsizeof(numbers_gen), "байт")  # Например: 112 байт
Вот как это работает и почему генераторы экономят память:

Список (numbers_list)
Когда мы создаём список с помощью конструкции [x**2 for x in range(1000)],
Python сразу вычисляет все квадраты чисел от 0 до 999 и хранит их в памяти как единый объект.
Поэтому список занимает значительно больше памяти — в данном случае,
тысячи чисел и их значений хранятся одновременно.

Генератор (numbers_gen)
Генератор, создаваемый с помощью (x**2 for x in range(1000)), работает иначе.
Он не вычисляет значения сразу,
а только сохраняет «рецепт» или «шаблон» для их создания.
Значения будут генерироваться по одному, только тогда, когда они понадобятся.
Поэтому генератор использует совсем немного памяти — в основном,
для хранения своей логики и текущего состояния.

Результат (размеры в памяти):

Список занимает много памяти, так как все числа уже вычислены и хранятся.
Генератор занимает совсем немного памяти,
потому что в каждый момент времени он «знает»,
как вычислить следующее число, но не хранит все числа сразу.

Пример в цифрах:

Если numbers_list занимает, например, 9024 байт, это потому,
что все 1000 квадратов чисел уже созданы и загружены в память.
Если numbers_gen занимает всего 112 байт, это потому,
что он хранит только информацию о том, как генерировать числа, а не сами числа.
"""
















"""
Генераторные выражения

Генераторное выражение — это способ создать генератор в одну строку.
Оно позволяет описать, какие значения нужно сгенерировать,
и делает код компактным и удобным для чтения.

Пример:

squares = (x**2 for x in range(5))
print(next(squares))  # Вывод: 0
print(next(squares))  # Вывод: 1
print(list(squares))  # Вывод: [4, 9, 16]

"""



















"""
Генераторы списков

Список можно создать сразу с нужными элементами.

Например:

# Создание списка квадратов чисел от 0 до 4
squares = [x**2 for x in range(5)]
print(squares)  # Вывод: [0, 1, 4, 9, 16]



Генераторы словарей

Словарь можно создать, задав ключи и значения с помощью генератора:

# Создание словаря, где ключи — это числа, а значения — их квадраты

squares_dict = {x: x**2 for x in range(5)}
print(squares_dict)  # Вывод: {0: 0, 1: 1, 2: 4, 3: 9, 4: 16}




Генераторы множеств

Множество можно создать аналогично, чтобы получить уникальные значения:

# Создание множества квадратов чисел
squares_set = {x**2 for x in range(5)}
print(squares_set)  # Вывод: {0, 1, 4, 9, 16}



Эти конструкции позволяют быстро и удобно создавать коллекции,
комбинируя их с условиями:


# Список только чётных чисел от 0 до 9
even_numbers = [x for x in range(10) if x % 2 == 0]
print(even_numbers)  # Вывод: [0, 2, 4, 6, 8]



# Словарь только для нечётных чисел
odd_squares_dict = {x: x**2 for x in range(10) if x % 2 != 0}
print(odd_squares_dict)  # Вывод: {1: 1, 3: 9, 5: 25, 7: 49, 9: 81}


"""













"""
Примеры использования генераторов:

Пример 1: Генератор чисел Фибоначчи
Генератор создаёт последовательность чисел Фибоначчи,
по одному значению за раз:

def fibonacci(limit):
    a, b = 0, 1
    while a < limit:
        yield a
        a, b = b, a + b

for number in fibonacci(10):
    print(number)  # Вывод: 0 1 1 2 3 5 8





Пример 2: Генератор для построчного чтения файлов

Генератор экономит память, загружая строки файла только по мере необходимости:

def read_file(filename):
    with open(filename, "r") as file:
        for line in file:
            yield line.strip()

for line in read_file("example.txt"):
    print(line)  # Построчный вывод содержимого файла







Итог:

Генераторы позволяют создавать значения «на лету», экономя память.
Они делают код компактным и удобным для написания и чтения.
Генераторы особенно полезны при работе с большими наборами данных,
где загрузка всех данных сразу невозможна или неэффективна.
"""




















"""
Типичные ошибки
Тип ошибки 1: Забытое ключевое слово yield

Ошибка возникает, если вместо yield используется return,
функция перестаёт быть генератором.

Пример ошибки:

def my_generator():
return 1  # Ошибка: возвращает только одно значение, генератор не создаётся

Решение:

Используйте yield, чтобы функция стала генератором.

def my_generator():
yield 1





Тип ошибки 2: Истощение генератора

Генератор можно использовать только один раз.
После полного перебора он истощается,
и повторное использование не даст результата.

Пример ошибки:

gen = (x for x in range(3))
print(list(gen))  # [0, 1, 2]
print(list(gen))  # [] (генератор уже истощён)


Решение:

Создавайте новый генератор, если требуется повторное использование.

gen = (x for x in range(3))
print(list(gen))
gen = (x for x in range(3))  # Новый генератор
print(list(gen))




Тип ошибки 3: Неправильное использование генераторного выражения

Если передать генераторное выражение напрямую в функции,
которые используют итерацию, оно может быть истощено до того,
как мы сможем его повторно использовать.

Пример ошибки:

gen = (x**2 for x in range(5))
print(sum(gen))  # Генератор истощён
print(list(gen))  # [] (генератор уже пуст)


Решение:

Сохраните результат, если требуется повторное использование.

gen = (x**2 for x in range(5))
result = list(gen)  # Сохраняем результат
print(sum(result))  # 30
print(result)  # [0, 1, 4, 9, 16]




Тип ошибки 4: Ошибка в бесконечном генераторе

Бесконечный генератор без ограничения может зависнуть,
если не указать условие выхода.

Пример ошибки:

def infinite_numbers():
    num = 1
    while True:
        yield num
        num += 1

gen = infinite_numbers()
for n in gen:
    print(n)  # Никогда не завершится

Решение:

Добавьте ограничение при использовании бесконечного генератора.

def infinite_numbers():
    num = 1
    while True:
        yield num
        num += 1

gen = infinite_numbers()
for _ in range(10):  # Ограничиваем количество итераций
    print(next(gen))








Тип ошибки 5: Попытка использования yield вне функции — SyntaxError

Ключевое слово yield можно использовать только внутри функций.
Попытка вызвать его вне функции вызовет ошибку SyntaxError.

Пример ошибки:

yield 5  # SyntaxError: 'yield' outside function


Решение:

Используйте yield только в определении функций.

def my_generator():
yield 5

"""




















"""
Практикум


Задача 1: Генератор чисел с проверкой кратности

Ситуация: мы пишем программу,
которая помогает школьникам тренироваться в математике.
Нам нужно создать генератор чисел,
который выдаёт последовательность чисел,
кратных определённому значению (например, 3).
Ученики смогут указать верхний предел последовательности.


Задача:
напишите генератор multiples(limit, divisor),
который принимает два аргумента:

limit — максимальное число последовательности.
divisor — число, на которое проверяется кратность.
Генератор должен возвращать числа от 1 до limit,
которые делятся на divisor без остатка.

Ожидаемый результат:

Если пользователь ввёл limit = 10 и divisor = 3, вывод:

Числа от 1 до 10, кратные 3:
3
6
9


Шаги реализации:

Определите функцию-генератор multiples с аргументами limit и divisor.
def multiples(limit, divisor):


Используйте цикл for, чтобы перебрать числа от 1 до limit.
for number in range(1, limit + 1):


С помощью оператора % проверьте кратность числа.
if number % divisor == 0:


Если число кратное, верните его с помощью yield.
yield number


Реализация:

def multiples(limit, divisor):
    for number in range(1, limit + 1):
        if number % divisor == 0:
            yield number

# Основная программа
limit = int(input("Введите верхний предел: "))
divisor = int(input("Введите делитель: "))

print(f"Числа от 1 до {limit}, кратные {divisor}:")
for multiple in multiples(limit, divisor):
    print(multiple)




Вариант решения через генератор списка:

Генератор списка (LC)
def multiples_with_lc(limit, divisor):
    return [number for number in range(1, limit + 1) if number % divisor == 0]

# Основная программа
limit = int(input("Введите верхний предел: "))
divisor = int(input("Введите делитель: "))

print(f"Числа от 1 до {limit}, кратные {divisor}:")
print(multiples_with_lc(limit, divisor))
Использование генератора (multiples) экономит память,
так как числа возвращаются по одному.
Однако генератор списка быстрее для маленьких диапазонов,
поскольку создаёт список сразу.
Использовать генератор списка не рекомендуется для больших диапазонов,
чтобы избежать загрузки памяти.






Задача 2: Генератор чётных чисел

Ситуация: мы хотим разработать генератор,
который выводит только чётные числа в заданном диапазоне.
Это полезно для экономии времени и ресурсов,
когда нужны только определённые данные из большого набора.



Задача:
напишите генератор even_numbers(start, end), который принимает два аргумента:

start — начало диапазона.
end — конец диапазона.

Генератор должен возвращать только чётные числа в указанном диапазоне.

Ожидаемый результат:

Если пользователь ввёл start = 5 и end = 12, вывод:

Чётные числа от 5 до 12:
6
8
10
12

Шаги реализации:

Определите функцию-генератор even_numbers с аргументами start и end.
def even_numbers(start, end):

Используйте цикл for, чтобы перебрать числа от start до end.
for number in range(start, end + 1):

С помощью оператора % проверьте, является ли число чётным.
if number % 2 == 0:

Если число чётное, верните его с помощью yield.
yield number





Реализация:

def even_numbers(start, end):
    for number in range(start, end + 1):
        if number % 2 == 0:
            yield number

# Основная программа
start = int(input("Введите начало диапазона: "))
end = int(input("Введите конец диапазона: "))

print(f"Чётные числа от {start} до {end}:")
for even in even_numbers(start, end):
    print(even)




Вариант решения через генератор списка:

# Генератор списка
def even_numbers_with_lc(start, end):
    return [number for number in range(start, end + 1) if number % 2 == 0]

# Основная программа
start = int(input("Введите начало диапазона: "))
end = int(input("Введите конец диапазона: "))

print(f"Чётные числа от {start} до {end}:")
print(even_numbers_with_lc(start, end))


Генератор (even_numbers) эффективнее для больших диапазонов,
а генератор списка проще для небольших задач.
Однако генератор списка для больших диапазонов потребляет больше памяти.


"""