Основные операции с файлами

Открытие файла
Чтобы начать работу с файлом,
его нужно открыть с помощью функции open(). Она принимает следующие аргументы:

Имя файла — строка с названием файла (например, «data.txt»).
Режим работы — строка, указывающая,
что делать с файлом (читать, записывать и т.д.).
Кодировка — особенно важна при работе с текстовыми файлами,
чтобы избежать ошибок.
Например, для русского текста указываем encoding=»utf-8″.
Пример:

file = open("example.txt", "r", encoding="utf-8")  # Открываем файл для чтения с указанием кодировки















Использование конструкции with

В Python для работы с файлами часто используется конструкция with.
Она автоматически закрывает файл после завершения работы с ним,
даже если во время выполнения программы возникла ошибка.
Это избавляет от необходимости вручную вызывать метод close().

Пример:

with open("example.txt", "r", encoding="utf-8") as file:
    content = file.read()
    print(content)  # Читаем и выводим содержимое файла


В этом примере файл открывается для чтения,
а по завершении работы блок with автоматически закрывает его.
Это делает код более безопасным и лаконичным.




Чтение данных
Чтение всего содержимого:
Используем read(), чтобы считать весь текст файла.

file = open("example.txt", "r", encoding="utf-8")
content = file.read()
print(content)
file.close()  # Закрываем файл





Чтение по строкам:
Используем readline() или readlines() для чтения строки за строкой или получения всех строк в виде списка.

with open("example.txt", "r", encoding="utf-8") as file:
    for line in file:
        print(line.strip())  # Убираем лишние пробелы





Запись данных
Перезапись данных:
Метод write() перезаписывает содержимое файла.

ith open("example.txt", "w", encoding="utf-8") as file:
    file.write("Привет, мир!\n")
    file.write("Ещё одна строка.")







Добавление данных:
Чтобы не удалять содержимое, используем режим «a».

with open("example.txt", "a", encoding="utf-8") as file:
    file.write("\nНовая строка в конце файла.")





Закрытие файла
Функция close() завершает работу с файлом и освобождает ресурсы.
Если файл не закрыть, это может привести к утечке памяти.
Но гораздо удобнее использовать конструкцию with,
которая автоматически закрывает файл даже в случае ошибок.







Работа с конструкцией try-except

При открытии файла могут возникать ошибки (например, файл не найден).
Чтобы обработать такие ситуации, используем блок try-except.

Пример:



try:
    with open("example.txt", "r", encoding="utf-8") as file:
        content = file.read()
        print(content)
except FileNotFoundError:
    print("Файл не найден.")
except IOError:
    print("Ошибка ввода-вывода.")












Пример: Телефонный справочник

Представьте, что мы пишем консольное приложение — телефонный справочник.
Словарь с контактами сохраняется в файл,
чтобы после перезапуска программы не потерять данные.
В этом случае файлы выступают как внешнее хранилище информации.

Пример:

contacts = {"Иван": "123-456", "Мария": "789-012"}

# Сохранение контактов в файл
with open("contacts.txt", "w", encoding="utf-8") as file:
    for name, phone in contacts.items():
        file.write(f"{name}: {phone}\n")

# Чтение контактов из файла
try:
    with open("contacts.txt", "r", encoding="utf-8") as file:
        for line in file:
            print(line.strip())
except FileNotFoundError:
    print("Файл с контактами не найден.")






Чтение больших файлов

Для работы с большими файлами используем построчное чтение,
чтобы не загружать весь файл в память.

with open("bigfile.txt", "r", encoding="utf-8") as file:
    for line in file:
        print(line.strip())







Работа с двоичными файлами

Двоичные файлы (например, изображения) открываются в режиме «rb».
Это позволяет работать с данными, которые не являются текстом.

Пример:

with open("image.jpg", "rb") as file:
data = file.read()
print(f"Прочитано {len(data)} байт.")







Проверка существования файла

Для проверки, существует ли файл, используем модуль os.

import os
if os.path.exists("example.txt"):
    print("Файл существует.")
else:
    print("Файл не найден.")

















Типичные ошибки
Тип ошибки 1: Файл не найден — FileNotFoundError

Ошибка возникает при попытке открыть несуществующий файл.

Пример ошибки:

file = open("nonexistent.txt", "r")  # FileNotFoundError


Решение:

Перед открытием файла в режиме чтения проверьте его существование.

Исправленный код:

import os
if os.path.exists("nonexistent.txt"):
    with open("nonexistent.txt", "r") as file:
        print(file.read())
else:
    print("Файл не найден.")










Тип ошибки 2: Неправильный режим работы с файлом — UnsupportedOperation

Ошибка возникает при попытке записать данные в файл, открытый в режиме чтения («r»).

Пример ошибки:

file = open("example.txt", "r")
file.write("Новый текст")  # UnsupportedOperation


Решение:

Используйте правильный режим («w» или «a») для записи данных.

Исправленный код:

with open("example.txt", "w") as file:
file.write("Новый текст")















Тип ошибки 3: Невозможность закрыть файл

Ошибка возникает, если мы забыли закрыть файл. Это может привести к утечке памяти или повреждению файла.

Пример ошибки:

file = open("example.txt", "r")
content = file.read()
# file.close() не вызван


Решение:

Используйте конструкцию with, чтобы Python автоматически закрыл файл.

Исправленный код:

with open("example.txt", "r") as file:
    content = file.read()


















Тип ошибки 4: Запись несоответствующего типа данных — TypeError

Ошибка возникает при попытке записать в файл данные, которые не являются строками.

Пример ошибки:

with open("example.txt", "w") as file:
    file.write(12345)  # TypeError


Решение:

Перед записью преобразуйте данные в строку с помощью str().

Исправленный код:

with open("example.txt", "w") as file:
    file.write(str(12345))


















Тип ошибки 5: Чтение файла, открытого в режиме записи — UnsupportedOperation

Ошибка возникает при попытке прочитать файл, открытый в режиме записи («w»).

Пример ошибки:

with open("example.txt", "w") as file:
    print(file.read())  # UnsupportedOperation

Решение:

Используйте режим «r» или «r+», если нужно и читать, и записывать данные.

Исправленный код:

with open("example.txt", "r+") as file:
    print(file.read())  # Чтение и запись
























Тип ошибки 6: Чтение больших файлов целиком — MemoryError

Ошибка возникает при попытке загрузить большой файл в память с помощью read().

Пример ошибки:

with open("bigfile.txt", "r") as file:
    content = file.read()  # Потенциальное переполнение памяти


Решение:

Используйте построчное чтение с помощью цикла.

Исправленный код:

with open("bigfile.txt", "r") as file:
    for line in file:
        print(line.strip())




























Практикум

Задача 1: Список участников мероприятия

Ситуация: мы организуем мероприятие, например, школьный фестиваль,
и хотим вести список участников.
Для упрощения работы мы решили автоматизировать процесс регистрации.
Каждый раз, когда кто-то регистрируется, его имя должно сохраняться в файл.
Кроме того,
мы хотим иметь возможность просмотреть полный список уже зарегистрированных участников в любое время.


Эта программа будет полезна не только для мероприятий,
но и для любых ситуаций,
где требуется учёт участников или объектов, например, регистрация на занятия,
учёт библиотечных книг или управление списком гостей на вечеринке.


Задача: создайте программу, которая позволяет:

Добавлять имена участников в файл.
Просматривать полный список участников, уже записанных в файл.
Программа должна работать в интерактивном режиме и поддерживать следующие команды:
add: Запрашивает имя участника и добавляет его в файл.
show: Считывает и отображает список всех участников.
exit: Завершает работу программы.
Если файл с участниками отсутствует,
программа должна автоматически его создать.
Если список участников пуст,
при выводе программы должно отображаться сообщение: «Список пуст.».

Указания:

Программа должна работать с текстовым файлом для хранения списка участников.
Если файла не существует, программа должна автоматически его создать.
Должны быть реализованы два режима работы:
Добавление участников: программа запрашивает имя и сохраняет его в файл.
Просмотр списка участников: программа считывает и выводит всех участников из файла.
Для работы с файлами необходимо использовать конструкцию with, чтобы избежать утечек ресурсов.
Программа должна быть интерактивной и позволять вводить команды add, show и exit.
Шаги реализации:

Проверка существования файла
Программа должна проверять, существует ли файл с участниками.



Если файл отсутствует, его нужно создать.

import os

file_name = "participants.txt"

# Проверяем, существует ли файл
if not os.path.exists(file_name):
    with open(file_name, "w") as file:
        pass  # Создаем пустой файл



Функция для добавления участников
Реализуйте функцию, которая добавляет имя нового участника в файл.

def add_participant(file_name, participant_name):
    with open(file_name, "a") as file:
        file.write(participant_name + "\n")  # Записываем имя в файл




Функция для отображения списка участников
Реализуйте функцию, которая считывает и выводит список всех участников из файла.

def show_participants(file_name):
    if os.path.exists(file_name):  # Проверяем существование файла
        with open(file_name, "r") as file:
            participants = file.readlines()  # Считываем строки из файла
            if participants:
                print("Список участников:")
                for participant in participants:
                    print(participant.strip())  # Убираем лишние пробелы и переносы строк
            else:
                print("Список пуст.")  # Если файл пуст
    else:
        print("Файл с участниками не найден.")




Основная программа
Реализуйте основной цикл программы, который позволяет вводить команды:

add — для добавления нового участника. show — для вывода списка участников. exit — для завершения программы.

while True:
    command = input("Введите команду (add, show, exit): ").strip().lower()
    if command == "add":
        name = input("Введите имя участника: ").strip()  # Запрашиваем имя участника
        add_participant(file_name, name)
        print("Участник добавлен.")
    elif command == "show":
        show_participants(file_name)
    elif command == "exit":
        print("Программа завершена.")
        break  # Выходим из цикла
    else:
        print("Неизвестная команда. Попробуйте снова.")



Реализация:

import os

def count_words(file_name):
    if os.path.exists(file_name):
        with open(file_name, "r") as file:
            content = file.read()
            words = content.split()
            print(f"Количество слов в файле: {len(words)}")
    else:
        print("Файл не найден.")

# Основная программа
file_name = input("Введите имя текстового файла: ").strip()
count_words(file_name)
