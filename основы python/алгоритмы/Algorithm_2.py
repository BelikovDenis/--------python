Сегодня мы узнаем, как компьютеры работают с данными ещё быстрее и умнее!
Мы познакомимся с ключевыми структурами данных — стеком и очередью.
Эти структуры помогают легко добавлять,
удалять или находить нужные данные, делая работу с ними более эффективной.



Кроме того, мы научимся искать элементы в списке не по порядку,
а супербыстро — с помощью бинарного поиска.



А ещё освоим один из самых эффективных методов сортировки — сортировку слиянием.
В отличие от пузырьковой сортировки, которая долго обрабатывает большие списки,
сортировка слиянием делит данные на части,
обрабатывает их отдельно, а затем объединяет,
что делает её подходящей для работы с большими объёмами данных.




Все эти инструменты активно применяются в реальной жизни:
в играх, приложениях и даже при поиске информации в интернете.



На занятии мы освоим алгоритмы бинарного поиска и поиска в структурах данных:

узнаем, как работают стек и очередь, и понять, где их можно использовать.
научимся искать элементы в списке быстрее с помощью бинарного поиска.
освоим сортировку слиянием для упорядочивания данных.
применим эти методы на практике.
поймем, как они облегчают решение задач.






Сегодня мы будем разбирать три важных понятия: стек, очередь и сортировка слиянием.
Всё это — структуры и алгоритмы, которые помогают организовать и обрабатывать данные.



Стек
Это структура данных,
в которой элементы добавляются и удаляются по принципу стопки тарелок:
сначала кладём тарелки одну на другую, а потом снимаем их с верхушки.


Это называется «последним пришёл — первым ушёл» (Last In, First Out, LIFO).

Представим, что у нас есть стопка книг.
Мы кладем новую книгу сверху, а если хотим убрать книгу,
то сначала возьмем ту, которая наверху.


Основные операции со стеком:

Добавить элемент в стек (push): Мы кладем новую тарелку (или элемент) на вершину стека.

Удалить элемент из стека (pop): Мы снимаем верхнюю тарелку (удаляем последний добавленный элемент).

Посмотреть, что наверху (peek): Мы проверяем, какая тарелка (или элемент) сейчас сверху, но не убираем её.



Разберём пример с кодом:

Создадим стек (в Python это обычный список):
stack = []

Добавим элементы в стек:
stack.append(1)  # Добавляем "1" в стек
stack.append(2)  # Добавляем "2" в стек
print(stack)  # [1, 2]


Удалим элемент из стека:
stack.pop()  # Убираем "2" из стека
print(stack)  # [1]


Посмотрим, что наверху:
top = stack[-1]  # Смотрим верхний элемент (это "1")
print(top)  # 1








Что такое очередь?

Очередь
Это структура данных, где элементы обрабатываются по принципу очереди в магазине: первый вошёл, первый вышел.
Это называется «первым пришёл — первым ушёл» (First In, First Out, FIFO).

Представим, что стоим в очереди за мороженым.
Если мы пришли первыми, то нас обслужат раньше тех, кто пришёл после нас.


Основные операции с очередью:


Добавить элемент в очередь (enqueue): Мы становимся в конец очереди.


Удалить элемент из очереди (dequeue): Нас обслужили, и мы выходим из начала очереди.


Разберём пример с кодом:

Создадим очередь (в Python для этого удобно использовать deque из модуля collections):
from collections import deque
queue = deque()


Добавим элементы в очередь:
queue.append(1)  # Пришёл первый
queue.append(2)  # Пришёл второй
print(queue)  # deque([1, 2])


3. Удалим элемент из начала очереди:

queue.popleft()  # Ушёл первый
print(queue)  # deque([2])







Что такое бинарный поиск?

Бинарный поиск — это быстрый алгоритм,
который используется для поиска элемента в отсортированном списке.


Вместо того чтобы проверять каждый элемент по порядку (как в линейном поиске),
бинарный поиск уменьшает количество проверяемых элементов,
деля список пополам на каждом шаге.
Это экономит много времени, особенно в больших наборах данных.





Как это работает?


Представим, что играем с другом в игру «Угадай число» в диапазоне от 1 до 100.
Мы называем число, а друг отвечает:
«Ты угадал», «Больше» или «Меньше».
Какое число назвать первым?
Правильный ответ — 50, потому что так мы делим диапазон пополам.
Затем, в зависимости от ответа друга,
мы продолжаем делить оставшийся диапазон пополам,
пока не найдем правильное число.

Это и есть принцип работы бинарного поиска.
Но важно помнить, что бинарный поиск эффективен только для заранее отсортированных значений.


Шаги алгоритма:

Проверяем элемент в середине списка:
Если это искомый элемент, поиск завершён.
Если искомый элемент меньше, то смотрим только на левую половину списка.
Если больше, то берём только правую половину списка.
Повторяем процесс с выбранной половиной, пока не найдём элемент или не убедимся, что его нет.


Пример:

Пусть у нас есть отсортированный список:
[1, 3, 5, 7, 9, 11, 13], и мы хотим найти число 7.

Начинаем с середины списка.
Средний элемент — это 7 (по индексу 3). Ура, элемент найден!

Если бы мы искали число 9, то сравнили бы 7 с 9.
Поняв, что 9 больше, мы взяли бы только правую половину списка: [9, 11, 13].

И повторили бы процесс с правой половиной, пока не нашли элемент.

Код бинарного поиска:

def binary_search(lst, target):
    left = 0  # Начальный индекс
    right = len(lst) - 1  # Конечный индекс

    while left <= right:
        mid = (left + right) // 2  # Середина списка
        if lst[mid] == target:
            return f"Элемент найден на позиции {mid}"  # Успех!
        elif lst[mid] < target:
            left = mid + 1  # Ищем в правой половине
        else:
            right = mid - 1  # Ищем в левой половине

    return "Элемент не найден"  # Если элемент отсутствует



Пример использования:

numbers = [1, 3, 5, 7, 9, 11, 13]
print(binary_search(numbers, 7))  # Вывод: Элемент найден на позиции 3
print(binary_search(numbers, 4))  # Вывод: Элемент не найден

Преимущества бинарного поиска:

Работает намного быстрее линейного поиска для больших отсортированных списков.
Количество элементов, которые нужно проверить, уменьшается вдвое на каждом шаге.


Важно: Бинарный поиск работает только с отсортированными списками!
Если данные не отсортированы, их сначала нужно упорядочить.



Что такое сортировка слиянием?

Сортировка слиянием — это алгоритм,
который упорядочивает данные по принципу разделения и слияния:

Мы делим список на две части.
Сортируем каждую часть.
Соединяем их обратно, уже в правильном порядке.
Представим, что у нас есть пачка карточек с числами.
Сначала мы делим ее пополам, затем упорядочиваем каждую половину,
а потом соединяем их вместе, чтобы получить общий отсортированный список.


Как работает сортировка слиянием:

Делим список: Если список из более чем одного элемента, разделяем его на две половины.
Рекурсивно сортируем части: Продолжаем делить, пока не останется по одному элементу в каждой части.
Сливаем обратно: Объединяем части, вставляя числа в правильном порядке.
Пример работы сортировки слиянием:

def merge_sort(lst):
    # Если в списке меньше 2 элементов, он уже отсортирован
    if len(lst) <= 1:
        return lst

    # Делим список на две части
    mid = len(lst) // 2
    left_half = merge_sort(lst[:mid])  # Сортируем левую половину
    right_half = merge_sort(lst[mid:])  # Сортируем правую половину

    # Слияние двух половин в отсортированный список
    sorted_list = []
    i = j = 0
    while i < len(left_half) and j < len(right_half):
        if left_half[i] < right_half[j]:
            sorted_list.append(left_half[i])
            i += 1
        else:
            sorted_list.append(right_half[j])
            j += 1

    # Добавляем оставшиеся элементы из левой или правой половины
    sorted_list.extend(left_half[i:])
    sorted_list.extend(right_half[j:])

    return sorted_list

Пошаговый разбор:

У нас есть список: [38, 27, 43, 3, 9, 82, 10].

Разделяем его: [38, 27, 43] и [3, 9, 82, 10].

Ещё раз делим: [38], [27, 43] и [3, 9], [82, 10].

Сортируем маленькие части:
[27, 43] → [27, 43]

[3, 9] → [3, 9]


Сливаем обратно:
[27, 43] + [38] → [27, 38, 43]

[3, 9] + [10, 82] → [3, 9, 10, 82]


Объединяем всё вместе:
[27, 38, 43] + [3, 9, 10, 82] → [3, 9, 10, 27, 38, 43, 82].


Пример использования:

numbers = [38, 27, 43, 3, 9, 82, 10]
sorted_numbers = merge_sort(numbers)
print("Отсортированный список:", sorted_numbers)  # [3, 9, 10, 27, 38, 43, 82]


Почему это важно?


Стек полезен для откатов (например, отмена действий в текстовом редакторе).
Очередь используется в задачах с соблюдением порядка (например, задания на печать в принтере).
Сортировка слиянием помогает упорядочивать большие наборы данных и используется, например, в базах данных.







Типичные ошибки

Тип ошибки 1: Изменение списка во время итерации

При работе с циклом for и итерации по списку нельзя изменять сам список (удалять или добавлять элементы),
    так как это приводит к непредсказуемому поведению или ошибке.

Пример ошибки:

stack = [1, 2, 3, 4]
for num in stack:
    if num % 2 == 0:
        stack.remove(num)  # Ошибка: Изменяем список во время итерации
print(stack)

Программа не вызывает явную ошибку, но результат будет неправильным,
потому что удаление элемента смещает индексы, что пропускает некоторые элементы.

Решение:

Итерация по копии списка.

stack = [1, 2, 3, 4]
for num in stack[:]:  # Итерация по копии
    if num % 2 == 0:
        stack.remove(num)
print(stack)  # Вывод: [1, 3]






Тип ошибки 2: Бесконечный цикл в очереди

Ошибка возникает, если при работе с очередью не добавлять условие выхода из цикла.
Программа может застрять в бесконечном цикле.

Пример ошибки:

from collections import deque

queue = deque([1, 2, 3])
while queue:  # Неправильное управление очередью
    print(queue[0])  # Только выводим первый элемент, но не удаляем его
# Программа зависнет




Решение:

Удаляйте элементы из начала очереди:

from collections import deque

queue = deque([1, 2, 3])
while queue:
    print(queue.popleft())  # Удаляем элементы по одному








Тип ошибки 3: Пропущенный базовый случай в рекурсии — RecursionError

В сортировке слиянием (и других рекурсивных алгоритмах) важно указать базовый случай, чтобы остановить рекурсию.

Пример ошибки:

def merge_sort(lst):
    mid = len(lst) // 2
    left = merge_sort(lst[:mid])  # Рекурсия без базового случая
    right = merge_sort(lst[mid:])
    return left + right

# RecursionError: maximum recursion depth exceeded


Решение:

Добавьте базовый случай для остановки рекурсии:

def merge_sort(lst):
    if len(lst) <= 1:  # Базовый случай: список длиной 1 уже отсортирован
        return lst

    mid = len(lst) // 2
    left = merge_sort(lst[:mid])
    right = merge_sort(lst[mid:])

    sorted_list = []
    i = j = 0
    while i < len(left) and j < len(right):
        if left[i] < right[j]:
            sorted_list.append(left[i])
            i += 1
        else:
            sorted_list.append(right[j])
            j += 1

    sorted_list.extend(left[i:])
    sorted_list.extend(right[j:])
    return sorted_list








Тип ошибки 4: Отрицательные индексы в очереди

Очередь работает только с добавлением и удалением элементов с её концов.
Попытка обратиться к элементу через отрицательный индекс вызовет ошибку логики.

Пример ошибки:

queue = deque([1, 2, 3])
print(queue[-1])  # Получаем доступ к последнему элементу
queue.popleft()  # Удаляем элемент из начала
print(queue[-1])  # Ошибка логики: теперь последний элемент изменился

Программа работает, но результаты непредсказуемы, если логика основана на -1.


Решение:

Не используйте отрицательные индексы, работайте с удалением через popleft().

queue = deque([1, 2, 3])
while queue:
    print(queue.popleft())  # Последовательно удаляем элементы








Тип ошибки 5: Необработанные исключения в стеке — IndexError

Ошибка возникает, если стек пуст, а мы пытаемся удалить элемент с помощью pop().

Пример ошибки:

stack = []
stack.pop()  # Ошибка, так как стек пуст

# IndexError: pop from empty list

Решение:

Проверяйте, пуст ли стек, перед удалением элемента.

stack = []
if stack:
    stack.pop()
else:
    print("Стек пуст, удаление невозможно.")














Практикум
Задача 1: Работа с очередью в магазине

Ситуация: мы управляем очередью в магазине самообслуживания.
Каждому покупателю присваивается номер, по которому он проходит через кассу.
Нужно создать программу, которая помогает управлять очередью:
добавлять новых покупателей, обслуживать их по порядку и выводить текущую очередь.

Задача: напишите программу, которая:

Добавляет нового покупателя в конец очереди.
Обслуживает покупателя в начале очереди (удаляет его).
Показывает текущую очередь.
Шаги реализации:

Импортируем модуль для работы с очередью
Используем deque из модуля collections,
так как он лучше всего подходит для реализации очереди.

from collections import deque

queue = deque()  # Создаём пустую очередь


Создаём бесконечный цикл для работы с программой
Чтобы программа могла принимать команды до тех пор,
пока пользователь не завершит работу, используем while True.

Добавляем покупателей в очередь
Если пользователь вводит команду add,
программа запрашивает имя покупателя и добавляет его в конец очереди.

while True:
    command = input("Введите команду (add, serve, show, exit): ").strip().lower()
if command == "add":
    customer = input("Введите имя покупателя: ")
    queue.append(customer)
    print(f"Покупатель {customer} добавлен в очередь.")


Обслуживаем первого покупателя в очереди
Если пользователь вводит команду serve,
программа обслуживает первого покупателя (удаляет его из начала очереди).
Если очередь пуста, выводим соответствующее сообщение.

elif command == "serve":
    if queue:
        served = queue.popleft()
        print(f"Покупатель {served} обслужен.")
    else:
        print("Очередь пуста.")


Показываем текущую очередь
Если пользователь вводит команду show, программа отображает текущую очередь. Если очередь пуста, выводим сообщение об этом.

elif command == "show":
    print("Текущая очередь:", list(queue) if queue else "Очередь пуста.")


Завершаем программу
Если пользователь вводит команду exit, программа завершает работу и выходит из цикла.

elif command == "exit":
    print("Работа завершена.")
    break


Обрабатываем неизвестные команды
Если пользователь вводит команду, которую программа не знает, выводим сообщение об ошибке.

else:
    print("Неизвестная команда. Попробуйте снова.")





Реализация:

from collections import deque

# Создаём пустую очередь
queue = deque()

# Запуск основного цикла программы
while True:
    # Запрос команды от пользователя
    command = input("Введите команду (add, serve, show, exit): ").strip().lower()

    # Добавление покупателя в очередь
    if command == "add":
        customer = input("Введите имя покупателя: ")
        queue.append(customer)
        print(f"Покупатель {customer} добавлен в очередь.")

    # Обслуживание первого покупателя в очереди
    elif command == "serve":
        if queue:
            served = queue.popleft()
            print(f"Покупатель {served} обслужен.")
        else:
            print("Очередь пуста.")

    # Показ текущей очереди
    elif command == "show":
        print("Текущая очередь:", list(queue) if queue else "Очередь пуста.")

    # Завершение программы
    elif command == "exit":
        print("Работа завершена.")
        break

    # Обработка неизвестных команд
    else:
        print("Неизвестная команда. Попробуйте снова.")



















Задача 2: Управление стеком задач

Ситуация: мы разрабатываем систему для отслеживания задач.
Новые задачи добавляются в начало списка (стека),
а выполнение начинается с самой последней добавленной задачи.
Наша цель — создать программу, которая помогает добавлять задачи,
завершать их и проверять текущий список задач.



Задача: напишите программу, которая:

Добавляет новую задачу в стек.
Удаляет последнюю добавленную задачу.
Показывает текущие задачи.
Шаги реализации:



Создаём стек с помощью списка:
stack = []  # Пустой стек


Добавляем задачу в стек:
Пользователь вводит название задачи.
Задача добавляется в конец списка.
task = input("Введите задачу: ")
stack.append(task)
print(f"Задача '{task}' добавлена.")


Завершаем задачу (удаляем её с вершины стека):
Проверяем, есть ли задачи в стеке.
Если стек не пуст, извлекаем задачу с помощью pop() и выводим её.

Если стек пуст, выводим сообщение, что задач нет.
if stack:
    completed = stack.pop()
    print(f"Задача '{completed}' завершена.")
else:
    print("Нет задач для завершения.")


Отображаем текущие задачи:
Если в стеке есть задачи, выводим их.
Если стек пуст, сообщаем, что задач нет.
print("Текущие задачи:", stack if stack else "Нет активных задач.")


Завершаем работу программы:
Пользователь вводит команду exit, и программа завершает выполнение.
print("Работа завершена.")
break


Обрабатываем неизвестные команды:
Если команда неизвестна, выводим сообщение и предлагаем ввести команду повторно.
print("Неизвестная команда. Попробуйте снова.")



Реализация:

# Создаём пустой стек
stack = []

# Запуск основного цикла программы
while True:
    # Запрос команды от пользователя
    command = input("Введите команду (add, complete, show, exit): ").strip().lower()

    # Добавление задачи в стек
    if command == "add":
        task = input("Введите задачу: ")
        stack.append(task)
        print(f"Задача '{task}' добавлена.")

    # Завершение задачи (удаление с вершины стека)
    elif command == "complete":
        if stack:
            completed = stack.pop()
            print(f"Задача '{completed}' завершена.")
        else:
            print("Нет задач для завершения.")

    # Отображение текущих задач
    elif command == "show":
        print("Текущие задачи:", stack if stack else "Нет активных задач.")

    # Завершение работы программы
    elif command == "exit":
        print("Работа завершена.")
        break

    # Обработка неизвестных команд
    else:
        print("Неизвестная команда. Попробуйте снова.")