Алгоритм
Это набор последовательных действий, которые нужно выполнить,
чтобы достичь цели или решить задачу.
Например, чтобы приготовить бутерброд, мы следуем шагам:
взять хлеб, намазать масло, положить начинку. Это и есть алгоритм.





Но что важно: не все алгоритмы одинаково удобны в использовании.

Оптимальный и неоптимальный алгоритмы

Представим, что ищем своё имя в списке из 100 человек:

Первый способ (неоптимальный):
Проверять имена одно за другим, начиная с первого.
Если имя окажется последним, то придётся пройти весь список.


Второй способ (оптимальный):
Если список отсортирован по алфавиту,
мы можем разделить его на две части,
сразу отбросив половину (например, имя находится не в первой половине).
Это называется «бинарный поиск», и он работает намного быстрее.


Результат будет одинаковым — мы найдем имя.
Но поиск вторым способом выполнится быстрее,
потому что выполняет меньше проверок.
Это и есть разница между оптимальными и неоптимальными алгоритмами.
















Оценка эффективности алгоритмов: О-нотация

Когда мы говорим об «оптимальности» алгоритма,
важно уметь оценить его скорость работы и использование ресурсов.
Для этого программисты используют специальный инструмент — О-нотацию (Big-O Notation).

О-нотация показывает, как быстро растёт время выполнения алгоритма
(или использование памяти) в зависимости от размера входных данных.
Она не даёт точного времени работы,
а показывает порядок роста: как поведение алгоритма изменяется при увеличении количества данных.

Пример:

Представим, что нам нужно проверить список из 10 имен.
Если мы будем проверять каждое имя по очереди,
добавление ещё 10 имён увеличит время работы в два раза.

Если же мы будем использовать бинарный поиск,
добавление 10 новых имён почти не увеличит время,
потому что алгоритм умно «разрезает» список.



Основные виды сложности:

O(1) — Константная сложность.
Алгоритм выполняется за одно и то же время, независимо от количества данных.



Пример:получение элемента списка по индексу.

O(n) — Линейная сложность.
Время выполнения растёт пропорционально количеству данных.



Пример: линейный поиск.

O(log n) — Логарифмическая сложность.
Время выполнения увеличивается медленно, даже если данных становится в разы больше.



Пример: бинарный поиск.

O(n²) — Квадратичная сложность.
Алгоритм работает намного дольше, если данных становится больше.



Пример: сортировка пузырьком.

Зачем это нужно?

О-нотация помогает выбрать наиболее эффективный алгоритм для конкретной задачи.
Если список содержит 10 элементов, O(n²) может быть приемлемым.
Но для списка из 1 миллиона элементов такой алгоритм будет работать слишком медленно.

Когда мы создаём программы,
важно не только писать правильный код, но и делать его быстрым и оптимальным.

О-нотация — это инструмент, который помогает оценить,
насколько хорошо работает наш алгоритм, особенно при большом количестве данных.

Что такое структура данных?
Структура данных — это специальный способ хранения и организации информации в программе,
который позволяет эффективно решать задачи.
Если переменные хранят только одно значение (например, число или строку),
то структуры данных, такие как списки,
словари или множества, могут хранить сразу много элементов.



Пример:

Переменные похожи на одну полку, где лежит только одна вещь.
А структура данных — это целый шкаф, где можно хранить много вещей,
причём на каждой полке может быть своя категория (например, книги на одной, одежда на другой).

Основные структуры данных

В Python есть несколько базовых структур данных,
каждая из которых предназначена для решения определённых задач.
Вот их описание и примеры использования:


Список (list): Это упорядоченная коллекция элементов различных типов.
Элементы списка можно изменять или удалять.

Примеры использования:

Хранение чисел для обработки массивов.
Сохранение строк для последующего анализа текста.
Пример: [1, 2, 3, 4, 5]




Кортеж (tuple):
То же, что и список, но неизменяемый.
Используется, когда данные не должны быть изменены.

Примеры использования:

В качестве констант для хранения фиксированных данных.
Как ключи словаря.
Пример: (10, 20, 30)




Множество (set):
Это неупорядоченная коллекция, которая хранит только уникальные элементы, без повторений.

Примеры использования:

Проверка дублирующихся элементов в списках.
Удаление повторяющихся элементов.
Пример: {1, 2, 3, 3} → {1, 2, 3}




Словарь (dictionary):
Хранит данные в виде пар «ключ — значение», что позволяет быстро находить информацию по ключу.

Примеры использования:

Кэширование результатов запросов.
Перевод между разными представлениями данных.
Пример: {«ключ»: «значение»} или {«name»: «Alice», «age»: 25}

Итерируемые объекты (iterable objects):
Все перечисленные структуры являются итерируемыми объектами.
Это значит, что по ним можно легко пройти с помощью цикла for.


Пример:

for item in [1, 2, 3]:
    print(item)








При выборе подходящей структуры данных важно учитывать особенности задачи:

Нужна ли изменяемость?
Важен ли порядок элементов?
Требуется ли уникальность значений?
Простые алгоритмы
Линейный поиск
Что делает линейный поиск?


Линейный поиск — это простой способ найти элемент в списке.
Мы начинаем с первого элемента и проверяем каждый элемент по порядку,
пока не найдём нужный или не дойдём до конца списка.

Представим, что ищем свою фамилию в списке. Мы читаем фамилии сверху вниз, пока не найдёте свою.

Как это выглядит в коде?

def linear_search(lst, target):
    for index, value in enumerate(lst):  # Перебираем элементы списка с их индексами
        if value == target:  # Если элемент совпадает с тем, что мы ищем
            return f"Элемент найден на позиции {index}"  # Сообщаем, где нашли элемент
    return "Элемент не найден"  # Если элемент не найден, возвращаем сообщение

Пример использования:

У нас есть список чисел:

numbers = [10, 20, 30, 40, 50]
Мы хотим найти число 30:

print(linear_search(numbers, 30))  # Вывод: Элемент найден на позиции 2
Теперь попробуем найти число 60, которого нет в списке:

print(linear_search(numbers, 60))  # Вывод: Элемент не найден
Разберём шаги:

Шаг 1: Проверяем первый элемент (10). Это не 30.
Шаг 2: Проверяем второй элемент (20). Это не 30.
Шаг 3: Проверяем третий элемент (30). Это то, что мы ищем! Останавливаемся.

Если бы элемента не было в списке,
мы прошли бы все элементы и вернули сообщение «Элемент не найден».

Сортировка пузырьком
Что делает сортировка пузырьком?

Сортировка пузырьком — это способ упорядочить элементы в списке,
сравнивая попарно соседние значения.
Если соседние элементы расположены не по нужному порядку
(например, большее число стоит перед меньшим), они меняются местами.
Этот процесс продолжается многократно, пока все элементы в списке не окажутся на своих местах.

Представим, что у нас есть шары с номерами.
Мы хотим выстроить их по порядку.
Берём два соседних шара, сравниваем их и меняем местами, если нужно.

Как это выглядит в коде?

def bubble_sort(lst):
    n = len(lst)  # Сколько всего элементов в списке
    for i in range(n - 1):  # Сколько раз нужно пройти по списку
        for j in range(n - 1 - i):  # Сравниваем соседние элементы
            if lst[j] > lst[j + 1]:  # Если элемент больше следующего
                lst[j], lst[j + 1] = lst[j + 1], lst[j]  # Меняем их местами
    return lst  # Возвращаем отсортированный список
Пример использования:

У нас есть список:

numbers = [64, 34, 25, 12, 22, 11, 90]
Вызываем функцию сортировки:

print(bubble_sort(numbers))  # Вывод: [11, 12, 22, 25, 34, 64, 90]
Разберём шаги:

Шаг 1: Сравниваем первые два элемента (64 и 34). 64 > 34, меняем местами. Теперь список: [34, 64, 25, 12, 22, 11, 90].

Шаг 2: Сравниваем 64 и 25. Меняем местами. Список: [34, 25, 64, 12, 22, 11, 90].

Продолжаем, пока не дойдём до конца. Самое большое число (90) окажется на своём месте.

Шаг 3: Повторяем весь процесс для оставшихся элементов, не включая уже отсортированные.
Каждый раз самое большое число всплывает вверх (как пузырёк в воде, отсюда и название).



Где это применяется?
Линейный поиск — простое и понятное решение, когда нужно быстро найти элемент в небольшом списке.
Сортировка пузырьком помогает понять, как можно упорядочить данные, и учит думать логически, разбивая задачу на шаги.





Типичные ошибки
Тип ошибки 1:  Удаление элемента из списка во время итерации — RuntimeError

Ошибка возникает, когда при выполнении сортировки или поиска попытаться изменить список,
по которому в данный момент выполняется итерация.

Пример ошибки:

numbers = [10, 20, 30, 40]
for num in numbers:
    if num % 20 == 0:
        numbers.remove(num)  # RuntimeError: list changed size during iteration
print(numbers)

Решение:

Используйте копию списка для итерации

Создайте копию списка и выполняйте изменения в исходном списке.

numbers = [10, 20, 30, 40]
for num in numbers[:]:  # Итерация по копии списка
    if num % 20 == 0:
        numbers.remove(num)
print(numbers)  # Вывод: [10, 30]




Тип ошибки 2: Бесконечный цикл в сортировке

Ошибка происходит,
когда в алгоритме пузырьковой сортировки забыть уменьшать количество итераций,
что приведёт к бесконечному циклу.

Пример ошибки:

def bubble_sort(lst):
    n = len(lst)
    while True:  # Ошибка: Условие выхода из цикла отсутствует
        for i in range(n - 1):
            if lst[i] > lst[i + 1]:
                lst[i], lst[i + 1] = lst[i + 1], lst[i]
    return lst
Решение:

Добавьте правильное условие выхода из цикла.

def bubble_sort(lst):
    n = len(lst)
    for i in range(n - 1):
        for j in range(n - 1 - i):
            if lst[j] > lst[j + 1]:
                lst[j], lst[j + 1] = lst[j + 1], lst[j]
    return lst







Тип ошибки 3: Линейный поиск без обработки отсутствия элемента

Ошибка возникает, если линейный поиск не найдёт элемент,
а код не обработает этот случай, программа может работать некорректно.

Пример ошибки:

numbers = [10, 20, 30]
target = 40
for num in numbers:
    if num == target:
        print("Элемент найден")
# Ничего не выведет, если элемент не найден


Решение:

Добавьте флаг или возвращаемое значение для обработки случая, когда элемент не найден.

def linear_search(lst, target):
    for num in lst:
        if num == target:
            return "Элемент найден"
    return "Элемент не найден"

numbers = [10, 20, 30]
print(linear_search(numbers, 40))  # Элемент не найден





























Практикум
Задача 1: Сортировка оценок методом вставки

Ситуация: мы хотим проанализировать оценки учеников,
но перед этим нужно их упорядочить.
Мы решили использовать сортировку методом вставки для упорядочивания оценок от минимальной к максимальной.

Задача: написать программу, которая:

Принимает список оценок (ввод от пользователя).
Использует сортировку методом вставки для упорядочивания оценок.
Выводит отсортированный список.
Шаги реализации:

Создание списка для хранения оценок:

grades_input = input("Введите оценки учеников через пробел: ")  # Считываем строку
grades = [int(grade) for grade in grades_input.split()]  # Преобразуем в список чисел

Определение функции сортировки методом вставки:

Перебор элементов списка начиная со второго:
for i in range(1, len(lst)):

Установка текущего элемента как «ключевого»:
key = lst[i]

Сравнение «ключевого» элемента с предыдущими элементами:
j = i - 1
while j >= 0 and key < lst[j]:

Сдвиг большего элемента вправо:
lst[j + 1] = lst[j]
j -= 1

Вставка «ключевого» элемента в нужное место:
lst[j + 1] = key

Вызов функции сортировки:
sorted_grades = insertion_sort(grades)
Вывод отсортированного списка:
print("Отсортированные оценки:", sorted_grades)

Реализация:

def insertion_sort(lst):
    for i in range(1, len(lst)):
        key = lst[i]
        j = i - 1
        while j >= 0 and key < lst[j]:
            lst[j + 1] = lst[j]
            j -= 1
        lst[j + 1] = key
    return lst

Основная программа:

grades_input = input("Введите оценки учеников через пробел: ")  # Считываем строку
grades = [int(grade) for grade in grades_input.split()]  # Преобразуем в список чисел
print("Исходные оценки:", grades)
sorted_grades = insertion_sort(grades)
print("Отсортированные оценки:", sorted_grades)









Задача 2: Поиск максимального элемента

Ситуация: мы организуем спортивное соревнование и хотим определить максимальный результат среди участников.
Напишите программу, которая поможет найти этот результат,
используя алгоритм линейного поиска.

Задача: написать программу, которая:

Принимает список результатов участников (ввод от пользователя).
Использует линейный поиск для нахождения максимального значения.
Выводит максимальный результат.
Шаги реализации:

Создание списка для хранения результатов участников:
scores_input = input("Введите результаты участников через пробел: ")
scores = [int(score) for score in scores_input.split()]

Определение функции поиска максимального значения:
Инициализация переменной для хранения максимального значения:
max_value = lst[0]

Перебор каждого элемента списка:
for num in lst:

Сравнение текущего элемента с максимальным значением:
if num > max_value:

Обновление максимального значения, если найден больший элемент:
max_value = num

Вызов функции для поиска максимального значения:
max_score = find_max(scores)

Вывод максимального результата:
print("Максимальный результат:", max_score)


Реализация:

def find_max(lst):
    max_value = lst[0]
    for num in lst:
        if num > max_value:
            max_value = num
    return max_value

# Основная программа
scores_input = input("Введите результаты участников через пробел: ")
scores = [int(score) for score in scores_input.split()]
print("Результаты участников:", scores)
max_score = find_max(scores)
print("Максимальный результат:", max_score)
