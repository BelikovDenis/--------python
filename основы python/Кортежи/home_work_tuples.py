Управление базой данных сотрудников

Ситуация: мы создаем систему для управления информацией о сотрудниках в компании.
Каждый сотрудник имеет уникальный ID, имя, должность и набор навыков.

Задача: используйте словарь для хранения информации о каждом сотруднике,
где ключом будет кортеж, содержащий ID и имя, а значением — словарь с должностью и множеством навыков.







#  Создание пустого словаря для сотрудников.
employees = {}
#  Добавление информации о сотрудниках в словарь.
employees[(1, "Alice")] = {"position": "Manager",
                           "skills": {"leadership", "communication"}}
employees[(2, "Bob")] = {"position": "Developer",
                         "skills": {"python", "databases"}}

for (id, name), info in ,,,.items():
    print(',,,')












#  Задача №2
Напишите функцию assess_yield(), которая будет вычислять перспективность урожая и давать ему оценку.

Функция assess_yield() должна принимать два аргумента:
количество растений: number_of_plants (целое число);
средняя урожайность одного растения: average_weight (в граммах, дробное число).


Функция должна возвращать кортеж из двух элементов: общий предполагаемый вес урожая в килограммах и строку — сообщение с оценкой урожайности.

Сообщения должны быть разными, в зависимости от предполагаемого веса урожая:

Если общий вес урожая строго больше 100 кг, программа должна вернуть сообщение «Ожидается отличный урожай.».

Если общий вес урожая строго больше 50 кг, программа должна вернуть сообщение «Ожидается хороший урожай.».

Если общий вес урожая больше нуля — программа должна вернуть сообщение «Урожай будет так себе.».

Во всех остальных случаях программа должна вернуть сообщение «Урожая не будет.».

При проверке общего веса урожая не применяйте в коде двойные сравнения вроде 50 < total_weight <= 100.

Кортеж, который вернёт функция, должен быть распакован, из элементов кортежа должна быть составлена и напечатана строка:
<вычисленный_вес> кг. <Сообщение_с_оценкой_урожая>.

Например:
1.5 кг. Урожай будет так себе. 
Соблюдайте формат сообщений: пробелы и точки должны быть расставлены именно так, как в примере; сообщение должно в точности соответствовать шаблону.
Структура кода:

def assess_yield(number_of_plants, average_weight):
    total_weight = (number_of_plants * average_weight) / 1000
    ...

# Вызов функции:
result = assess_yield(15, 100)

# При правильной работе функции 
# переменной result должен быть присвоен кортеж (1.5, 'Урожай будет так себе')

# Здесь надо распаковать кортеж result в две переменные.

# Здесь надо напечатать две переменные и необходимые дополнительные строки.
# Должно быть напечатано: 
# 1.5 кг. Урожай будет так себе. 









#ПОДСКАЗКА
Для расчёта общего веса умножьте number_of_plants на average_weight и переведите вес в килограммы.
Используйте условные операторы для определения оценки урожайности.
Возвращаемый результат должен быть кортежем, состоящим из числа (вес урожая) и строки (оценка урожайности).
Распаковка кортежа tpl в переменные a и b выполняется так: a, b = tpl.
Строку напечатайте через одну-единственную функцию print().