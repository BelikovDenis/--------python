Прямое создание кортежа:

my_tuple = (1, 2, 3)




Кортеж с одним элементом (обратите внимание на запятую):

single_tuple = (4,)  # Без запятой это будет просто число в скобках




Кортеж без скобок:

auto_tuple = 5, 6, 7




Преобразование списка в кортеж:

list_to_tuple = tuple([8, 9, 10])
















Вот некоторые операции и методы, которые мы можем использовать с кортежами:

Доступ по индексу:

second_item = my_tuple[1]





Срезы:

sub_tuple = my_tuple[0:2]  # Получаем кортеж (1, 2)




Конкатенация:

new_tuple = my_tuple + (4, 5)




Умножение:

repeated_tuple = my_tuple * 2  # Результат: (1, 2, 3, 1, 2, 3)



Проверка наличия элемента:

has_item = 1 in my_tuple  # Возвращает True, если 1 есть в кортеже



Длина кортежа:

length = len(my_tuple)




Метод .count(x) — подсчитывает количество вхождений элемента x в кортеж:

count = my_tuple.count(1)



Метод .index(x) — возвращает первый индекс элемента x в кортеже:

index = my_tuple.index(3)


Кортежи особенно полезны, когда мы работаем с функциями, которые возвращают несколько значений.
В таких случаях Python автоматически упаковывает эти значения в кортеж,
что позволяет легко разделить их на отдельные переменные.
Это упрощает обработку и передачу данных между компонентами программы без риска случайных изменений.


















Обзор коллекций в Python

Каждая коллекция в Python имеет свои особенности, и важно правильно выбирать её для каждой задачи:

Списки (list) — упорядоченные, изменяемые коллекции.
Применение: хранение чисел, строк или любых данных, которые нужно часто менять.
Пример: анализ массива данных, хранение временных данных.



Кортежи (tuple) — упорядоченные, неизменяемые коллекции.
Применение: фиксация данных, которые не должны изменяться.
Пример: координаты точек, настройки приложения.



Словари (dict) — коллекции пар «ключ-значение».
Применение: быстрый доступ к данным по ключу.
Пример: переводчик, профиль пользователя.



Множества (set) — неупорядоченные коллекции уникальных элементов.
Применение: удаление повторов, проверка наличия.
Пример: поиск уникальных слов в тексте.


Итерируемые объекты — все коллекции в Python можно перебрать с помощью цикла for.



При выборе коллекции подумайте:

Нужно ли сохранять порядок элементов?
Требуется ли в дальнейшем изменять данные в коллекции?
Важна ли уникальность элементов коллекции?
Каждая структура данных помогает эффективно решать конкретные задачи и делает код более понятным!



































Типичные ошибки


Тип ошибки 1: Попытка изменить кортеж — TypeError: ‘tuple’ object does not support item assignment

Кортежи являются неизменяемыми (immutable), поэтому мы не можем изменять их элементы после создания.

Пример ошибки:

my_tuple = (1, 2, 3)
my_tuple[1] = 10  # Ошибка: нельзя изменить элемент кортежа
Решение:

Если нам нужно внести изменения, создайте новый кортеж с желаемыми элементами.

my_tuple = (1, 2, 3)
my_tuple = my_tuple[:1] + (10,) + my_tuple[2:]  # Новый кортеж: (1, 10, 3)






Тип ошибки 2: Использование нехешируемого объекта в качестве ключа словаря — TypeError: unhashable type: ‘list’

Ключи в словаре должны быть хешируемыми, то есть их нельзя изменять. Списки и другие изменяемые типы данных не могут быть ключами.

Пример ошибки:

my_dict = {[1, 2, 3]: "value"}  # Ошибка: список не может быть ключом
Решение:

Используйте вместо списка кортеж, который является хешируемым и подходит для использования в качестве ключа.

my_dict = {(1, 2, 3): "value"}  # Работает






Тип ошибки 3: Обращение к несуществующему ключу в словаре — KeyError

Ошибка возникает, если мы попытаемся получить значение по ключу, которого нет в словаре, Python выбросит KeyError.

Пример ошибки:

my_dict = {"name": "Alice", "age": 25}
print(my_dict["city"])  # Ошибка: ключ 'city' отсутствует
Решение:

Используйте метод .get(), который возвращает None (или заданное значение по умолчанию), если ключ отсутствует.

my_dict = {"name": "Alice", "age": 25}
print(my_dict.get("city", "Город не указан"))  # Выведет: Город не указан






Тип ошибки 4: Цикл по изменяемому словарю

RuntimeError: dictionary changed size during iteration

Ошибка:

Изменение словаря во время его перебора может привести к ошибке.

Пример ошибки:

my_dict = {"a": 1, "b": 2, "c": 3}
for key in my_dict:
    my_dict.pop(key)  # Ошибка: словарь изменён во время итерации
Решение:

Создайте копию ключей словаря для безопасного удаления элементов.

my_dict = {"a": 1, "b": 2, "c": 3}
for key in list(my_dict.keys()):
    my_dict.pop(key)
print(my_dict)  # Пустой словарь



























