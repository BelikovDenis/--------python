простой пример списка
my_list = [1, 2.5, "Python", True]






Список можно заполнять поэлементно, используя метод append(), который добавляет элементы в конец списка.
fruits = []  # Пустой список
fruits.append("яблоко")
fruits.append("банан")
fruits.append("вишня")





other_fruits = ["ананас", "черешня"]
fruits.extend(other_fruits)  # Теперь fruits содержит пять элементов






for fruit in fruits:
    print(fruit)





Перебор по индексам (с доступом к индексу): Если нам нужно знать позиции (индексы) элементов, используйте функцию range():
for i in range(len(fruits)):
    print(f"Индекс: {i}, Элемент: {fruits[i]}")

Этот метод позволяет не только получить значения элементов,
но и выполнить действия, зависящие от их индексов (например, заменить элементы на определённой позиции).
 





Удаление элементов:
Для удаления элементов по значению используйте remove():
fruits.remove("банан")  # Удаляет первый 'banana' из списка







Для удаления по индексу служит pop():
removed_fruit = fruits.pop(1)  # Удаляет и возвращает элемент на позиции 1





Мы можем изменить значение элемента, обратившись к нему по индексу:

fruits[0] = "черника"  # Заменяет элемент на позиции 0 на 'blueberry'








Сортировать список можно методом sort(), который изменяет исходный список:


numbers = [3, 1, 4, 1, 5]
numbers.sort()  # Теперь numbers отсортирован


Для переворота списка используйте reverse():
numbers.reverse()  # Теперь numbers перевернут







Каждый элемент в списке имеет свой индекс.
Индексация начинается с нуля, то есть первый элемент имеет индекс 0, второй 1, и так далее.

numbers = [10, 20, 30, 40, 50]
print(numbers[0])  # Первый элемент: 10
print(numbers[3])  # Четвёртый элемент: 40





В Python можно использовать отрицательные индексы, чтобы обращаться к элементам с конца списка. 
Например, -1 означает последний элемент, -2 — предпоследний, и так далее.

numbers = [10, 20, 30, 40, 50]
print(numbers[-1])  # Последний элемент: 50
print(numbers[-3])  # Третий с конца элемент: 30







Срезы (slices)
Срезы позволяют извлекать часть списка, указав начальный и конечный индексы.
Начальный индекс включается в результат, а конечный — нет.

numbers = [10, 20, 30, 40, 50]
slice_of_numbers = numbers[1:4]  # Элементы с индексами 1, 2, 3
print(slice_of_numbers)  # Вывод: [20, 30, 40]
Если опустить начальный индекс, срез начнётся с первого элемента, а если опустить конечный — завершится на последнем:

print(numbers[:3])  # Срез до индекса 3 (не включая): [10, 20, 30]
print(numbers[2:])  # Срез от индекса 2 до конца: [30, 40, 50]







Длина списка (len)

Функция len() возвращает количество элементов в списке.
Это полезно для определения границ индексов и срезов.

numbers = [10, 20, 30, 40, 50]
print(len(numbers))  # Вывод: 5 (всего 5 элементов)
Пример использования:

numbers = [10, 20, 30, 40, 50]

# Срез от второго до последнего элемента
print(numbers[1:len(numbers)-1])  # Вывод: [20, 30, 40]

# Последний элемент через отрицательный индекс
print(numbers[-1])  # Вывод: 50








Вложенные списки

Списки могут содержать другие списки, что делает возможным создание многомерных структур.

matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]
